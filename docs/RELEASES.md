# Releases

### REL-042 — V8.9 Security Quick-Wins Sprint 1 (4 IDOR + timing-safe Cron-Secret)
- Date: 2026-06-01
- Image-Tag: `6e2d79b` (= `chore(SLC-891): /qa PASS CONDITIONAL — RPT-557 + Records-Sync`). Coolify-Redeploy 2026-05-30T06:25 UTC. Container `app-k9f5pn5upfq7etoefb5ukbcg-062232193645` healthy seit Deploy. Stack 16/17 healthy (supabase-studio pre-existing unhealthy, unrelated).
- Scope: V8.9 Security Quick-Wins Sprint 1 — erster der 4 Pre-Live-Security-Sprints aus Cross-Repo-Audit 2026-05-30. 1 Slice SLC-891, 6 Micro-Tasks, BL-497 done. **0 Schema-Migration**, **0 ENV-Aenderung** (CRON_SECRET wert unveraendert, nur Vergleichs-Algorithmus). 5 SEC-Findings resolved:
  - **MT-1 SEC-001** `POST /api/meetings/[id]/generate-agenda` — User-Client-Vor-Check auf Meeting-Owner-Visibility per `maybeSingle()`. Foreign-UUID → HTTP 404 vor `createAdminClient`-Call.
  - **MT-2 SEC-002** `POST /api/signals/extract` — Body-Deal-ID-Visibility-Check via User-Client. Foreign-Deal-ID → HTTP 404.
  - **MT-3 SEC-003** `POST /api/knowledge/query` — `loadDealContext()` nutzt User-Client + `caller_uid`-Filter in `search_knowledge_chunks`-RPC (additiv, RPC bleibt backward-compat). Partial-Mitigation: tieferer RPC-Fix (`search_knowledge_chunks` RLS-Boundary) deferred V8.11 RLS-Sweep (BL-500).
  - **MT-4 SEC-004** `POST /api/meetings/[id]/retry-summary` + `/retry-transcript` — analoger Owner-Vor-Check.
  - **MT-5 SEC-010** `verifyCronSecret()` + `verifyExportApiKey()` — `crypto.timingSafeEqual` mit Buffer-Length-Check. Pattern-Reuse aus `cockpit/src/lib/calcom/webhook-handler.ts:61-67`. 17 Cron-Endpoints + 6 Export-Routes auf einen Schlag timing-attack-haerter.
- Summary: V8.9 schliesst die 5 hoch-prioritisierten Security-Findings aus Cross-Repo-Audit 2026-05-30 (SEC-001..004 aktive IDOR-Routes + SEC-010 timing-safe Cron/Export). Defense-in-Depth — alle IDOR-Routes hatten Auth-Check, aber fehlende Owner-Visibility-Pruefung erlaubte Cross-Owner-Bedrock-Cost-Trigger + PII-Verarbeitung. Mit V8.9 ist BS produktiv-haerter gegenueber Cross-Owner-Probing. Sprint 1 ist der kleinste der 4 Pre-Live-Sprints (~4h Code). **Verification-Aggregat:** TSC EXIT=0, ESLint EXACT V8.8-Baseline 142e/57w, Vitest jsdom 1152/1152 PASS (Baseline unchanged), Coolify-DB-Vitest 15/15 PASS in 584ms (4 neue IDOR-Tests + 2 timing-safe-Tests via node:20 in `k9f5pn5upfq7etoefb5ukbcg_business-net`), Build 80/80 static pages compiled in 18.7s. Stub-Detection 0 findings. Post-Deploy 48h+ Burn-In PASS (RPT-558): 0 errors in 6968 App-Log-Zeilen, 1152 Cron-Loop-Iterationen mit `errors=0` (timing-safe-Refactor live proven stable), HTTP-Smoke intern 200. RPT-557 + RPT-558.
- Risks:
  - **LOW (SEC-003 partial-mitigation)** — `loadDealContext` schuetzt jetzt, aber `search_knowledge_chunks`-RPC selbst hat keinen RLS-Boundary. Cross-Service-Call mit gefaelschter Deal-UUID wuerde noch durchkommen wenn jemand direkt die RPC aufruft. Deferred V8.11 RLS-Sweep (BL-500, ~1-2 Wochen). Aktuell nicht exploitable (Call-Path geht ueber Action mit User-Client).
- Rollback Notes: V8.9 hat **0 Schema-Migration** + **0 ENV-Aenderung**. Rollback per Coolify-Image-History → REL-041 (`226b599`) trivial. Bei Rollback verschwinden 4 IDOR-Schutzpfade + timing-safe-Refactor zurueck auf `!==`-Vergleich. Keine Daten-Migration noetig. RTO: 3 Min Image-Rollback. Bei Cron-Auth-Problemen nach Redeploy: Coolify-Cron-Secret-Wert pruefen ob er Sonderzeichen enthaelt die unter `crypto.timingSafeEqual` Buffer-Encoding-Issues haben — bisher 48h sauber, kein Drift beobachtet.
- Post-Launch (RPT-558, 2026-06-01): **STABLE bestaetigt via /post-launch Light-Touch.** Burn-In 48h+: Container durchgehend healthy, RestartCount=0. 0 errors/exceptions/stack in 6968 App-Log-Zeilen. **1152 Cron-Loop-Iterationen mit errors=0|completed** — Live-Beweis fuer timing-safe-Refactor-Stabilitaet (R-2 implicit verified, AC-8 Probe 3+4 implicit). Stack 16/17 healthy (supabase-studio pre-existing). Disk 82% stabil. HTTP-Smoke intern via Coolify-Proxy → App: HTTP 200 mit erwartetem Login-HTML + `x-nextjs-cache: HIT`. AC-8 Probe 1+2 (IDOR-Routes mit Admin-Session) via Vitest 15/15 Coolify-DB-PASS abgedeckt. Keine Hotfix-/Rollback-Pflicht.

### REL-041 — V8.8 Help-Annotated-Screenshots Foundation (Pilot mein-tag)
- Date: 2026-05-28
- Image-Tag: `226b599` (= `226b5992a0ebf297a057cb5cd7e7316f6d682d36`). Coolify-Redeploy manuell via User-Coolify-UI (`feedback_manual_deploy`). Container `app-k9f5pn5upfq7etoefb5ukbcg-172443124322` + Full-Supabase-Stack healthy seit Deploy. Stack 15/16 healthy (supabase-studio pre-existing unhealthy, unrelated zu V8.8 — siehe V8.1-STATE.md-Notes).
- Scope: V8.8 Help-System Redesign Foundation. 1 Slice SLC-881, 7 Micro-Tasks, BL-489 done. **0 Schema-Migration**, **0 ENV-Aenderung**, **0 Compose-Aenderung**, **0 Cron-Aenderung**, **0 KI-Pfad-Aenderung**. Frontend-only-Slice.
  - **MT-1** `cockpit/src/lib/help/hotspot-schema.ts` — zod-Schema mit Bounds-Refine (x+w<=100, y+h<=100), 12 Vitest-Cases.
  - **MT-2** `cockpit/src/lib/help/hotspot-loader.ts` — Server-Function: readFile → parseHotspotPageJson → pre-render bodyHtml via renderLegalMarkdown. ENOENT-Fallback gibt null fuer V8.3-Backward-Compat. 5 Vitest-Cases.
  - **MT-3** `cockpit/src/components/help/hotspot-modal.tsx` — Base UI Dialog-Wrapper, `.help-content` CSS-Reuse, max-h-[80vh] overflow-y-auto fuer Long-Body.
  - **MT-4** `cockpit/src/components/help/hotspot-image.tsx` — Client-Component: Desktop-Overlays + Mobile `<ol>` Liste via Tailwind CSS-only (`hidden md:block` + `md:hidden`, kein useMediaQuery, kein Hydration-Mismatch per DEC-247).
  - **MT-5** `cockpit/src/app/(app)/help/[slug]/page.tsx` + `cockpit/src/components/help/help-page-shell.tsx` — Server-Component liest hotspotData, uebergibt als imageBlock-Prop wenn vorhanden, V8.3-Markdown-only-Fallback bei ENOENT.
  - **MT-6** `cockpit/src/content/help/hotspots/mein-tag.json` + `cockpit/public/help/screenshots/mein-tag.webp` (2560×2112, 129KB) — Pilot mit 3 Hotspots (schnellaktionen + ki-workspace + aufgaben-heute). 3 Iter-Fixes nach Live-Smoke fuer Coord-Genauigkeit.
  - **MT-7** Verification-Aggregat + Cockpit-Records-Update.
- Summary: V8.8 ist die Foundation fuer das Help-System-Redesign. Pilot-Page `/help/mein-tag` zeigt echtes Screenshot der App-Seite mit 3 klickbaren Hotspots. Klick oeffnet Modal mit Markdown-Body. Mobile faellt auf nummerierte Liste zurueck. Die 11 nicht-migrierten Slugs bleiben unveraendert auf V8.3-Plain-Markdown. **Verification-Aggregat:** Vitest 1152/1152 PASS (V8.6 1135 + 17 neu), TSC exit 0, Build exit 0, Lint EXACT V8.6-Baseline 142e/57w. **Live-Smoke User-bestaetigt:** Hover-Border + Click-Modal + ESC/Backdrop + Mobile-Viewport iPhone-SE 5/5. **Backward-Compat User-bestaetigt:** /help/pipeline + /help/settings + /help/kampagnen 3/3 Plain-Markdown unveraendert. **Post-Deploy-HTTP-Smoke:** 4/4 200/307 OK (login 200 + /help/mein-tag 307 auth + /help/screenshots/mein-tag.webp 200 + /datenschutz 200). 3 V8.8-Coord-Iter-Fixes nach erster Live-Smoke-Iteration — Lehre: Hotspot-Coord-Estimation aus Browser-Screenshots stackt 3 Error-Sources (browser-px → image-px → percent). Direct-WebP-Inspection-Pattern besser fuer Single-Shot-Genauigkeit. **Strategische Folge-Entscheidung:** BL-495 + BL-496 deferred — IS BL-105 Content Authoring Studio (visueller Hotspot-Editor) wird in IS gebaut, BS-Repo konsumiert die JSON+WebP-Outputs als Build-Time-Artefakte. Cross-Product-Content-Foundation-Pattern etabliert (`feedback_is_content_studio_strategic`).
- Risks:
  - **L-1 (Low)** Markdown-Renderer `sanitize: false` in `cockpit/src/lib/legal/markdown.ts:16`. Aktuell OK (Admin-only-Content im Repo, kein Runtime-User-Input). Wird relevant bei IS BL-105 Content Studio (User-editable Hotspots) — dokumentiert in BL-105-Beschreibung als /architecture-Concern.
  - **L-2 (Low)** ISSUE-086: Pilot-Screenshot `mein-tag.webp` zeigt User-Name "Richard Bellaerts" in Task-Titel. OK in Internal-Test-Mode (User selbst). Pre-Customer-Live muss anonymisiert werden — Auto-Fix via BL-495 + IS BL-105.
  - **L-3 (Low)** Bestehende ISSUE-058 postcss + fast-uri + 4 weitere npm-audit-Findings pre-existing V8.6-Baseline, akzeptiert per DEC-161. Kein neuer Vuln durch V8.8/sharp.
- Rollback Notes: V8.8 hat **0 Schema-Migration** + **0 ENV-Aenderung**. Rollback per Coolify-Image-History → REL-040 (`c9a40e7`) ist trivial. Bei Rollback verschwinden Hotspot-Foundation + Pilot-Mein-Tag, /help-Pages fallen auf V8.3-Plain-Markdown zurueck. Keine Datenmigration noetig. RTO: 3 Min Image-Rollback.
- Post-Launch (RPT-556, 2026-05-29): **STABLE bestaetigt via /post-launch Light-Touch.** Burn-In 13h: App-Container `app-k9f5pn5upfq7etoefb5ukbcg-172443124322` durchgehend healthy, RestartCount=0. 0 errors/exceptions/stack/unhandled/uncaught in 1886 App-Log-Zeilen. Stack 16/17 healthy (supabase-studio pre-existing). Disk 83% stabil zur V8.6-Baseline. HTTP-Smoke intern via Coolify-Proxy → App: HTTP 200 mit erwartetem Login-HTML + `x-nextjs-cache: HIT`. Keine Hotfix-/Rollback-Pflicht. ISSUE-086 weiter pre-Customer-Live-Pflicht.

### REL-040 — V8.6 Test-Hygiene-Bundle (ISSUE-084 + ISSUE-085 resolved + test:tsc Drift-Detection)
- Date: 2026-05-26
- Image-Tag: `c9a40e7` (= `c9a40e7608a2456da733818f574f54960cd070b3`). Coolify-Redeploy manuell via User-Coolify-UI (`feedback_manual_deploy`). Beide Container (app + supabase-db) frisch hoch mit identischem Image-Hash, healthy.
- Scope: V8.6 Test-Hygiene-Bundle. 1 Slice SLC-861, 6 Micro-Tasks, BL-493. **Keine MIG, kein neues DEC, kein Schema-Touch.** Minimaler Production-Code-Touch in 1 File (vies-client.ts Type-Signatur, **Runtime-Verhalten 100% unveraendert**).
  - **MT-1** legal-documents-rls.test.ts: Default-Seed-Backup-Restore-Pattern in beforeAll/beforeEach/afterAll (idempotent via ON CONFLICT DO NOTHING). 4 false-negative-FAILs gruen.
  - **MT-2** useVoiceCapture.test.tsx: mock.calls-Cast per `feedback_vitest_mock_calls_typescript_cast` (OP V7.2 SLC-141).
  - **MT-3** reverse-charge-revert + skonto-revert.test.ts: `Parameters<typeof patchTouches...>[0]`-Cast (TS2559-Fix).
  - **MT-4** vies-client.ts: `NodeJS.ProcessEnv` → `Record<string, string | undefined>` Source-Side-Loosening. process.env-Default + Production-Caller (vies-actions.ts:39) structural-compatible.
  - **MT-5** package.json: `scripts.test:tsc = "tsc --noEmit"` + `test:all` erweitert. CI-/Pre-Commit-Drift-Detection.
  - **MT-6** Records: ISSUE-084 + ISSUE-085 resolved 2026-05-26, SLC-861 done, BL-493 done.
- Summary: V8.6 schliesst 2 Test-Hygiene-Issues + erweitert um Drift-Detection-Script. Reine Test-Layer-Hygiene + 1 Type-Loosening ohne Runtime-Effekt. Pattern-Reuse-Heavy. Verification-Aggregat: Vitest 1135/1135 PASS, test:tsc exit 0 (8 → 0), RLS-Suite 121/121 PASS gegen Coolify-DB inkl. legal-documents-rls 7/7 (vs Baseline 3/7+4-FAIL), build clean, lint 142e/57w EXACT V8.5-Baseline. Default-Seed-Restore post-RLS-Lauf verifiziert (2 Rows mit original updated_at, 0 Test-Pollution). Internal-Test-Mode bleibt aktiv. Post-Deploy-Smoke: 3/3 HTTP 200 OK (login 114ms + /p/strategaize-transition-bv/datenschutz 310ms + /datenschutz 133ms). RPT-544/545/546.
- Risks:
  - **R1 (Very Low)** Production-Code-Touch in vies-client.ts ist Type-Signature-only — Runtime-Verhalten unveraendert. Caller `vies-actions.ts:39` ruft ohne env-Argument → kein Break. Test verifiziert.
  - **L-1 (Low)** AC9-Annahme-Drift in Slice-Spec (erwartete 163 RLS-Tests, tatsaechlich 121). Keine Behavior-Drift, nur Schaetzung. Nicht-blockierend, Spec-Update optional.
  - **L-2 (Low)** RLS-Test-Repo-Sync-Friction: `/opt/business-system-test/cockpit` muss vor kuenftigen RLS-Test-File-Changes via tar-pipe gesynct werden. Skill-Improvement-Kandidat fuer Sync-Helper-Script.
- Rollback Notes: V8.6 hat **0 Schema-Migration**. Rollback per Coolify-Image-History → REL-039 (`cc6fcd4`) ist trivial. Bei Rollback: vies-client.ts Type-Signatur kehrt zu NodeJS.ProcessEnv zurueck (Runtime unbeeinflusst). Test-Hygiene-Verbesserungen leben in der Repo-History, nicht im Production-Image. RTO: 3 Min Image-Rollback.

### REL-039 — V8.5 Hygiene-Bundle (Reserved-Slug-Trigger + Compose-Preview-Fix + DSE-Footer-Coverage Consent+Briefing)
- Date: 2026-05-25
- Image-Tag: `cc6fcd4` (= main HEAD post-Master-Merge nach SLC-853 /qa Records-Update). Coolify-Redeploy manuell via User-Coolify-UI (`feedback_manual_deploy`) — beide Container (app + supabase-db) frisch hoch mit Image-Hash `cc6fcd40b77f46123b3912a655d80d844cc77043`, healthy.
- Scope: V8.5 Hygiene-Bundle — 3 Slices SLC-851..853 als V8.4-Burn-In-Followups. **MIG-039 LIVE** auf Hetzner (Reserved-Slug-Trigger). 1 V8.5-DEC (DEC-239 Helper-Export-Strategie statt komplette Renderer-Migration).
  - **SLC-851** Reserved-Slug-Enforcement (BL-490, ISSUE-080 Fix): MIG-039 PL/pgSQL-Function `public.is_reserved_slug(text)` + Trigger `teams_reserved_slug_guard BEFORE INSERT OR UPDATE OF slug ON teams` mit RAISE EXCEPTION ERRCODE 23514. Reserved-Liste DB-Mirror von `reserved-slugs.ts` (38 Strings). TS↔SQL-Sync-Pflicht dokumentiert in Memory `feedback_reserved_slug_sst_pattern.md`. Vitest 4/4 PASS gegen Coolify-DB.
  - **SLC-852** Compose-Preview tenantSlug-Drift-Fix (BL-491, ISSUE-081 Fix): `compose/page.tsx` Server-Component-Resolve `currentUserTenantSlug` via `getTenantSlugByOwnerUserId`, durch `compose-studio.tsx` an `live-preview.tsx` reichen, dort als 4. Param an `renderBrandedHtml` mit useMemo-Dep-Array-Update. Preview-vs-Send bit-identisch (DEC-095 SST). Vitest +2 Bit-Identity-Cases in render.test.ts.
  - **SLC-853** renderBrandedHtml-Caller-Audit + DSE-Footer-Coverage (BL-492): Audit-Report `docs/AUDIT_MAIL_RENDERERS_V85.md` mit 4 sendEmailWithTracking-Caller + 5 Direct-nodemailer-Caller klassifiziert. `buildDseFooterBlock` + `buildDseFooterParagraph` als Helper exportiert. `consent-request-de.ts` + `briefing-html.ts` Renderer um `dseFooterHtml`-Param erweitert (bit-regression-safe bei undef). `send-consent-mail.ts` + `meeting-briefing/route.ts` tenantSlug-Resolution + Helper-Call eingebaut. Vitest +17 neue Cases (1135/1135 PASS gesamt, +17 vs 1118 Baseline).
- Summary: V8.5 ist **Hygiene-Sprint** zwischen V8.4 (Customer-DSE-Foundation) und V9 (Real-Use-Erkenntnisse). 3 V8.4-Risk-Items abgeschlossen: (a) Reserved-Slug-DB-Trigger als Defense-in-Depth gegen Backfill-Collision, (b) Preview-vs-Send-Drift im Compose-Studio geschlossen, (c) DSE-Footer-Auto-Insert auf alle 2 Custom-Renderer (Consent + Briefing) erweitert. Pattern-Reuse-Heavy: V8.4 getTenantSlugByOwnerUserId + V8.4 buildDseFooterBlock-Architektur als Foundation. Code-Diff: 11 modified + 2 new + Records-Update. Live-Smoke 3/3 PASS post-Coolify-Redeploy 2026-05-25 (RPT-542): AC7 Compose-Preview iframe DSE-Footer-Link, AC11-Consent Mail an immo@bellaerts.de mit DSE-Footer User-Inbox-bestaetigt, AC11-Briefing Mail an richard@bellaerts.de mit DSE-Footer User-Inbox-bestaetigt. Internal-Test-Mode bleibt aktiv. Pre-Customer-Live-Compliance-Foundation jetzt **vollstaendig** in allen 3 Mail-Pfaden (Compose-Studio + Consent + Briefing).
- Risks:
  - **ISSUE-084 legal-documents-rls.test.ts FAILs** (open, defer V8.6) — MIG-038 Phase-5 Default-Seed-Konflikt im Test-Run. KEIN Production-Effekt.
  - **ISSUE-085 8 pre-existing TSC-Errors** in 4 unrelated Test-Files (open, defer V8.6) — KEIN Production-Effekt (Production-Build skipt Tests via tsconfig.exclude). V8.6 Test-Hygiene-Slice geplant.
  - **L-1 AC4 Spec-Drift Datenschutzerklaerung-Colon** (low) — Spec sagt `"Datenschutzerklaerung:"`-Link mit Colon, Implementation folgt V8.4 SLC-846 UX-Verbesserung mit Wort-Link `>Datenschutzerklaerung</a>`. Anti-Regression-Test in render.test.ts verhindert Re-Drift. Konsistent mit Production-Pattern.
  - **L-2 Caller-Site-Mock-Vitest fehlt** (low) — Per `feedback_caller_site_traversal_test.md` Lehre aus V8.4 ISSUE-083 idealer Folge-Test. Mitigated durch 100% Caller-Discovery via Grep (0 verwaiste Sites) + Code-Walkthrough + 10/10 Wiring-Chain-Links End-to-End validiert + Pattern-Reuse-Mirror gegen produktionsbestaetigtes SLC-846/SLC-852.
  - **V9+ Open Followup**: 4 weitere Direct-nodemailer-Caller-Sites (siehe AUDIT_MAIL_RENDERERS_V85.md) — DEC-239 Out-of-Scope fuer V8.5. Bei naechstem Customer-Mail-Refactor zusammen mit kompletter renderBrandedHtml-Migration angehen.
- Rollback Notes: V8.5 hat **MIG-039 LIVE** auf Hetzner (PL/pgSQL Function + Trigger). Rollback per Coolify-Image-History → REL-038 (`845ce9a` post-Hotfix-Stand) ist code-side trivial. DB-Rollback der MIG-039 nontrivial aber reversibel: `DROP TRIGGER teams_reserved_slug_guard ON teams; DROP FUNCTION public.is_reserved_slug(text);` — wenn diese ausgefuehrt, geht der ISSUE-080-Bug-Schutz weg, ansonsten kein Datenverlust. RTO: 3 Min Image-Rollback (DB-Trigger kann optional drinbleiben — Forward-Compatibility).
- Burn-In: PASS 2026-05-26 nach 24h Beobachtung (RPT-543). Container `app-k9f5pn5upfq7etoefb5ukbcg-064442498463` + `supabase-db-...-064442528447` durchgehend 24h healthy seit 2026-05-25T06:47 UTC, RestartCount=0. 16/16 Container im Stack RestartCount=0. **0 errors/exceptions in 24h App-Logs**, alle 10 aktiven Cron-Loops `errors=0`. audit_log 24h: 288 ai_signal_extract_run (errors=0) + 4 ai_followup_run + 1 organischer ki_workspace_report (User-Touch 09:37 UTC) + 2 call-Events. **0 V8.5-spezifische organische Events** erwartet (Internal-Test-Mode kein Customer-Traffic). Disk-Trend stabil 83% (vs V8.4-Stable 82%, +1pp normal), Docker-Images 13.58GB / 6% reclaimable (Coolify-Auto-Cleanup 5. Bestaetigung). Bedrock-Cost $0 V8.5-spezifisch (V8.5 fuegt 0 LLM-Pfade hinzu). Production-HTTP-Smokes 3/3 200 OK (login + /p/strategaize-transition-bv/datenschutz + /datenschutz) Latenz <350ms. **Stability assessment: V8.5 STABLE.** Keine Hotfix-Pflicht, keine neuen Issues, keine Rollback-Notwendigkeit. Pre-Customer-Live-Compliance-Foundation in allen 3 Mail-Pfaden vollstaendig + production-verified.

### REL-038 — V8.4 Customer-Facing Datenschutzerklaerung (Multi-Tenant, Public-Route + Editor + Consent-Link + Mail-Footer)
- Date: 2026-05-23
- Image-Tag: `9f21569` (= main HEAD post-Master-Merge `slc-841-customer-dse-schema-migration` mit `--no-ff` Merge-Commit). Coolify-Redeploy manuell via User-Coolify-UI durchgefuehrt (`feedback_manual_deploy`). Container `app-k9f5pn5upfq7etoefb5ukbcg-091846561846` healthy + RestartCount=0 post-Redeploy.
- Scope: V8.4 Customer-Facing-DSE Foundation. 1 Feature FEAT-824 / BL-488 deployed. 7 Slices SLC-841..847 deployed. **MIG-038 Phase 1-5 LIVE auf Hetzner** (legal_documents + teams.slug + Backfill + UNIQUE-Index + Default-Seed). 2 V8.4-DECs (DEC-237 Auftragsverarbeiter-Markdown-Block, DEC-238 Master-Merge-am-Slice-Ende).
  - **SLC-841** Schema-Foundation: legal_documents-Tabelle mit RLS (2 Policies select_team + admin_mutate), teams.slug ADD COLUMN + Backfill + UNIQUE-Index auf lower(slug) + DEFAULT-Patch. Vitest 6/6 RLS-Tests.
  - **SLC-842** Slug-Generator + Default-Seed: lib/team/slug.ts (1:1 OP-V7 Reuse mit NFD-Decompose + Umlaut-Translit), lib/team/reserved-slugs.ts (41 Eintraege), customer-dse-default.md (154 Zeilen + 6 Platzhalter + Auftragsverarbeiter-Tabelle 4 Sub-Verarbeiter), MIG-038 Phase 5 Apply (INSERT 0 2 + Idempotenz INSERT 0 0). Vitest 15/15 Slug + 3/3 Reserved.
  - **SLC-843** Public-Route /p/[tenant-slug]/datenschutz: Server-Component mit isReservedSlug-Check + teams.ilike-Lookup + legal_documents.eq-Lookup + 3 notFound-Branches + renderLegalMarkdown-Reuse + CustomerDsePageShell mit Tenant-Header. globals.css `.customer-dse-content` Schicht (95 Zeilen). Middleware publicPaths erweitert um `/p/` (IMP-736-Pflicht). Render-Sanity Vitest 5/5.
  - **SLC-844** Editor /settings/compliance/customer-dse: admin-only via assertRole, 3 Server-Actions (getCustomerDse + updateCustomerDse Length-Validation 100-50000 + resetCustomerDseToDefault), audit_log INSERT mit `customer_dse.updated/reset`. CustomerDseEditor Client-Component mit 2-Panel Textarea + Live-Preview via dynamic-import renderLegalMarkdown. Settings-Tile in sections.ts visibleFor: ADMIN_ONLY. Vitest 4/4 Server-Actions.
  - **SLC-845** Consent-Form DSE-Link: lib/team/lookup-slug.ts Helper getTenantSlugByOwnerUserId mit 2-Step-Lookup + graceful-null. consent/[token]/page.tsx PATCH mit owner_user_id select + conditional Link (target=_blank rel=noopener noreferrer). Vitest 6/6.
  - **SLC-846** Mail-Footer-Auto-Insert: renderBrandedHtml-Signatur erweitert um `tenantSlug?: string` + buildDseFooterBlock helper. send.ts PATCH: tenantSlug-Resolution aus ownerUserId via lookup-slug. Snapshot-Regression bit-identisch zu V8.3 (7 existing Tests gruen). Vitest 11/11 (7 Regression + 4 neue DSE-Cases inkl. XSS-Escape). V2-Followup: send-consent-mail + meeting-briefing eigene HTML-Builder, NICHT renderBrandedHtml (Spec R5).
  - **SLC-847** Gesamt-QA + Master-Merge + Deploy: Vitest 1116/1116 PASS (+37 vs V8.3-Baseline 1079). Lint 142e/57w EXACT V8.3-Baseline. Build clean. Live-Smokes S2/S5/S6/S7/S8/AC12 PASS. S1/S3/S4 deferred zu /post-launch.
- Summary: V8.4 ist **Pre-Customer-Live-Compliance-Foundation**. Multi-Tenant-faehige editierbare DSE die Customer im Consent-Form und Mail-Footer sehen. Default-Seed-Markdown mit Platzhaltern (Tenant-Admin-Pflicht zur Befuellung vor Customer-Live + Anwalts-Pruefung deferred per `feedback_compliance_gate_later`). Pattern-Reuse-Heavy: V8.2 renderLegalMarkdown + LegalPageShell + Middleware-Whitelist + OP-V7 SLC-131 Slug-Pattern + V7-RLS-Helper + V5.2 ComplianceTemplateBlock-Layout. Image-Tag `9f21569`. Internal-Test-Mode bleibt aktiv. Live-Smoke-Evidence: `curl /p/strategaize-transition-bv/datenschutz` HTTP 200 + Markdown-Render mit Auftragsverarbeiter-Tabelle. `/p/unknown-tenant-xyz/datenschutz` + `/p/admin/datenschutz` HTTP 404 (Reserved-Slug + Unknown). RPT-517..530 + RPT-531.
- Risks:
  - **ISSUE-080 Backfill-Slug-Collision** (Severity Medium, V8.5 Fix-Target) — MIG-038 Phase 2 Backfill prueft NICHT gegen reserved-slugs.ts. Team "Strategaize" wurde zu Slug `strategaize` (Reserved) → Public-Route HTTP 404. Live-Fix 2026-05-23: SQL UPDATE auf `strategaize-transition-bv`. V8.5 Backlog BL-490: Backfill + Trigger gegen Reserved-Liste enforcen.
  - **S1 Editor-Roundtrip Browser-Smoke ausstehend** (open, defer /post-launch) — Code-Side via Vitest 4/4 verifiziert, Live-Browser-Save-Reload pending. Owner kann via richard@bellaerts.de Admin-Login testen.
  - **S3 Consent-Token Browser-Smoke ausstehend** (open, defer /post-launch) — Token-Generation + Click-Test User-Pflicht.
  - **S4 Mail-Footer Live-Smoke ausstehend** (open, defer /post-launch) — Test-Mail an immo@bellaerts.de via Composing-Studio, HTML-Source-Check.
  - **Platzhalter `{{tenant_name}}` etc. im Default-Markdown** (accepted, V1-intended) — Tenant-Admin-Pflicht zur Befuellung via Editor SLC-844. V1 zeigt sie als Klartext auf Public-Route. V2 Auto-Replacement.
  - **send-consent-mail + meeting-briefing eigene HTML-Builder** (accepted, V2-Followup per Spec R5) — DSE-Footer-Auto-Insert greift nur fuer renderBrandedHtml-Mails. V8.5+ entweder consentRequestHtml erweitern oder auf renderBrandedHtml umstellen.
  - **8 Pre-existing TSC-Errors** in 3 unrelated Test-Files (accepted, identisch zu main, kein V8.4-Effekt).
- Rollback Notes: V8.4 hat **MIG-038 Phase 1-5 LIVE** auf Hetzner. Rollback per Coolify-Image-History → REL-037 (`e2fac23`) ist code-side trivial, ABER DB-Rollback nontrivial: `DROP TABLE legal_documents CASCADE; ALTER TABLE teams DROP COLUMN slug CASCADE;` waere irreversibel fuer existing Tenant-Slug-URLs. **Empfohlene Rollback-Strategie**: Code-Image rollback + DB bleibt forward-kompatibel (RLS-Policies + Tabelle bleibt unused). RTO: 3 Min Image-Rollback ohne DB-Migration-Reverse.
- Burn-In: PASS 2026-05-24 nach 16h Beobachtung (RPT-535). Container `app-k9f5pn5upfq7etoefb5ukbcg-144741208288` (Image-Tag `845ce9a` post-Hotfix-Redeploy fuer ISSUE-083-Fix) + `supabase-db-...-144741264447` beide RestartCount=0, durchgehend healthy seit 2026-05-23T14:50 UTC. Disk-Trend POSITIV 90% → 82% (Coolify-Auto-Cleanup 4. Bestaetigung nach REL-030/032/033/035). 0 errors/exceptions in 24h App-Logs, Cron-Loops alle errors=0. 1 expected audit_log-Event `customer_dse.updated` 13:49 UTC (Editor-Smoke RPT-532), keine Drift. Bedrock-Cost $0 letzte 24h (V8.4 fuegt 0 LLM-Pfade hinzu, kein User-LLM-Touch). Public-Route `/p/strategaize-transition-bv/datenschutz` HTTP 200 mit Auftragsverarbeiter-Tabelle-Render Live-verifiziert (AC12). Reserved-Slug `/p/unknown-tenant-xyz/datenschutz` HTTP 404 korrekt. **ISSUE-082 + ISSUE-083 Production-LIVE-Verify PASS**: `emails.owner_user_id uuid` Spalte in `information_schema.columns` (MIG-040 LIVE), `getTenantSlugByOwnerUserId`-Ref im Server-Action-Bundle `_next-internal_server_app_(app)_emails_compose_page_actions_0wwatfl.js` (Code-Fix LIVE im Production-Image). legal_documents 2 customer-dse Docs (Default-Seed + Editor-Test-Edit). Status released-stable. S4 Live-Browser-Smoke optional (User-Action, Code-Side abgenommen).

### REL-037 — V8.3 Hilfe-Section (Authenticated /help + Tile-Page + Detail-Pages + Sidebar HILFE)
- Date: 2026-05-22
- Image-Tag: `e2fac23` (= main HEAD post-Master-Merge + Records-Update; Live-verified via HTTP-Smoke `/help` HTTP 307 → `/login` und `/login` HTTP 200, Coolify-Auto-Deploy lief erfolgreich durch).
- Scope: V8.3 Hilfe-Section. 1 Feature FEAT-823 mit 1 Slice SLC-826 (4 MTs atomic-committed). **KEINE Schema-Migration** (Frontend-only, additive). **Keine V8.3-DECs** (Reuse `renderLegalMarkdown` aus V8.2 + Sidebar-Pattern aus V7 DEC-190).
  - **MT-1** Catalog `cockpit/src/lib/help/catalog.ts` mit `HelpSection`-Type + `HELP_SECTIONS` (4: Erste Schritte / Verwaltung / KI-Werkzeuge / Spezial-Themen) + `HelpGuide`-Interface + `HELP_CATALOG` (12 Eintraege mit slug/title/section/durationMinutes/roles). Helper-Functions: `getHelpGuideBySlug`, `listHelpSlugs`, `groupBySection`, `rolesBadgeLabel`. 17 Vitest-Cases inkl. 1:1-Slug-to-File-Existenz-Check. 12 Markdown-Content-Files in `cockpit/src/content/help/` als 1:1-Spiegelung von `/deliverables/user-guide/` (RPT-510-Output).
  - **MT-2** Tile-Page `cockpit/src/app/(app)/help/page.tsx` (Server Component) mit PageHeader + 4 Sections + Grid (1/2/3 cols responsive) + HelpTile-Component (Title + Clock-Icon-Dauer-Badge + Users-Icon-Rollen-Badge + Link auf `/help/[slug]`).
  - **MT-3** Detail-Page `cockpit/src/app/(app)/help/[slug]/page.tsx` (Server Component) mit `generateMetadata` aus Catalog + `generateStaticParams` fuer alle 12 Slugs + `notFound()` bei catalog-miss oder file-read-error. Reuse `renderLegalMarkdown` aus V8.2. Eigener `HelpPageShell`-Component `cockpit/src/components/help/help-page-shell.tsx` mit ArrowLeft Back-Link "Zurueck zur Uebersicht". `.help-content` CSS-Schicht in `globals.css` (eigene Schicht analog `.legal-content`, User-Direktive: keine Reuse damit beide Bereiche unabhaengig poliert werden koennen).
  - **MT-4** Sidebar HILFE-Section in `cockpit/src/lib/navigation/sidebar-config.ts`: SidebarSection-Type um `"HILFE"`, SECTION_LABEL.HILFE = `"HILFE"`, SECTION_ORDER an Index 5 zwischen `VERWALTUNG_MEIN` und `WERKZEUGE`, neuer SidebarItem `/help` mit HelpCircle-Icon + `visibleFor: ALL_ROLES`. 3 neue Vitest-Cases (Visibility for all roles + Position + eigene Top-Section ohne Parent). Inline-Snapshots fuer 3 Rollen erweitert.
- Summary: V8.3 ist **Theme 2b** der User-Direktive 2026-05-21 (nach Theme 1 DSGVO-Public V8.2). Authenticated In-App Hilfe-Center wird verfuegbar, Foundation fuer Pre-Customer-Live (Voice-Over-Skripte + Playwright-Screencap-Script existieren in `/deliverables/user-guide/` als RPT-510-Output bereit fuer Recording). Vitest 1079/1079 PASS (+20 vs V8.2-Baseline 1059). Lint 142e/57w **EXACT V8.1-Baseline** (0 neue Lint-Errors in den 17 neuen + 3 modifizierten Files). Live HTTP-Smoke: `/help` HTTP 307 → `/login` (Auth-Guard wirkt, kein Middleware-Patch noetig), `/help/mein-tag` HTTP 307 → `/login`, `/help/unknown-test-slug-xyz` HTTP 307 → `/login` (Middleware greift vor notFound()), `/login` HTTP 200. Beweis dass Routes existieren und Auth-Gate funktioniert. Internal-Test-Mode bleibt aktiv.
- Risks:
  - **Polish der Markdown-Content-Files ausstehend** (accepted, deferred) — 12 Guides sind 1:1 vom RPT-510 user-guide-Output gespiegelt, Text-Polish und Anwalts-Pruefung der Datenschutz-Sections kommt pre-Customer-Live per `feedback_compliance_gate_later`.
  - **Adress-/KvK-/BTW-Daten in Markdown-Files** (accepted, deferred) — siehe RPT-510 / V8.2-Risk-Profile. Inhaltlich noch Platzhalter.
  - **Phase 2 Live-Browser-Smoke post-Login** (open, low-prio) — `/help` Tile-Page-Rendering, `/help/mein-tag` Markdown-Render mit `.help-content`-Style, `/help/unknown` 404 nach Login, Sidebar HILFE-Section sichtbar in 3 Rollen. HTTP-Smoke hat 3 Pfade als 307 → /login verifiziert (Auth-Wand wirkt, Routes existieren), aber Page-Render und Sidebar-Visual sind nur per Login pruefbar. User-Action oder Playwright-MCP-Skript fuer Phase 2.
  - **8 Pre-existing TSC-Errors** in 3 unrelated Test-Files (accepted, identisch zu main) — KEINE neuen Errors durch SLC-826. Tech-Debt fuer V7.7+ Cleanup-Sprint.
  - **L1 Worktree-Junction + Turbopack-Inkompatibilitaet** (accepted, Workaround dokumentiert) — `npm run build` im Windows-Worktree mit Junction-node_modules schlaegt fehl. Workaround: Build via main-Repo + detached-HEAD-Checkout. Coolify-Container nicht betroffen (kein Junction). IMP-Kandidat fuer Dev-System: in `reference_worktree_nodemodules_junction_windows.md` notieren.
- Rollback Notes: V8.3 hat **0 Schema-Migrationen**. Rollback per Coolify-Image-History → REL-036 (`fad6654`) ist trivial. Bei Rollback verschwindende V8.3-Effekte: `/help` + `/help/[slug]` 404, Sidebar HILFE-Section weg. V8.3-Code-Diff war ausschliesslich additiv (17 neue Files, 3 modifiziert) — Git-Revert von Slice-Merge `ca6866f` bringt sauberen V8.2-Stand zurueck. RTO: 3 Min (Coolify-Image-Rollback).

### REL-036 — V8.2 DSGVO-Public Foundation (Datenschutz + Impressum + Footer)
- Date: 2026-05-22
- Image-Tag: `fad6654` (= main HEAD nach Slice-Merge `c2f1ae1` + Hotfix `fad6654`; Live-verified via Coolify Container `app-k9f5pn5...-083236381348` Started 2026-05-22T08:35:06 UTC, healthy nach 1 Min Uptime, RestartCount=0).
- Scope: V8.2 DSGVO-Public-Foundation. 1 Feature FEAT-821 mit 1 Slice SLC-825 (4 MTs). **KEINE Schema-Migration** (additive Frontend-only). **Keine V8.2-DECs** (Stack-Entscheidungen sind Reuse bestehender Strategaize-Patterns aus IS SLC-201 MT-7 etc.).
  - **MT-1** Markdown-Renderer-Helper `cockpit/src/lib/legal/markdown.ts` mit `renderLegalMarkdown(md): Promise<string>` via `remark@15 + remark-html@16 + remark-gfm@4` (Strategaize-Standard per `feedback_email_render_remark_pattern`). 5 Vitest-Cases gruen (h1+p+ul+gfm-table+a).
  - **MT-2** Content-Files `cockpit/src/content/legal/{datenschutz,impressum}.md` als Spiegel der Drafts in `/deliverables/` (TODO-Block raus, Entwurfs-Header bleibt ehrlich-im-Internal-Test-Mode).
  - **MT-3** 2 Public-Routes `cockpit/src/app/{datenschutz,impressum}/page.tsx` (Server-Component, fs.readFile zur Build-Zeit, Static-prerendered) + LegalPageShell-Component + `.legal-content` CSS-Schicht in globals.css (kein @tailwindcss/typography Plugin im Projekt — handgepflegte minimal-Selectors fuer h1-h3/p/ul/ol/li/a/table/blockquote/code/hr/strong/em). `next.config.ts` outputFileTracingIncludes ergaenzt fuer standalone-Build-File-Inclusion.
  - **MT-4** `LegalFooter`-Component `cockpit/src/components/layout/legal-footer.tsx` in 3 Layouts integriert: (a) `(app)/layout.tsx` outer flex-col + LegalFooter im `md:pl-64`-Wrapper fuer Sidebar-Adjustment, (b) `(auth)/login/page.tsx` mit Card im flex-1-Wrapper + LegalFooter am Ende, (c) LegalPageShell rendert Footer in beiden Public-Pages selbst (Konsistenz).
  - **Hotfix** Commit `fad6654`: `cockpit/src/lib/supabase/middleware.ts:48` `publicPaths` um `/datenschutz` + `/impressum` ergaenzt. Initial-Deploy hatte HTTP 307 Redirect auf `/login` weil Public-Routes nicht in Middleware-Whitelist standen. Build/Static-Prerender hatten den Bug nicht sichtbar gemacht (Middleware-Routing wirkt nur live, nicht zur Build-Zeit).
- Summary: V8.2 ist der erste Strategaize-Schritt zu **DSGVO-konformer Aussen-Sichtbarkeit** des Business Systems. Voraussetzung dafuer, dass das System ueber den Eigentuemer hinaus benutzt werden kann (User-Direktive 2026-05-21 Theme 1). Markdown-Drafts liegen in `/deliverables/` (RPT-505 /compliance Theme 1a), deployed Content in `cockpit/src/content/legal/`. Anwalts-Pruefung deferred per `feedback_compliance_gate_later` (Pre-Customer-Live-Gate). Vitest 1059/1059 PASS (+5 V8.2). Lint 142e/57w EXACT V8.1-Baseline (V8.2-Files lint-clean). Build clean 67/67 Pages, beide Routes als `○ Static` prerendered. Internal-Test-Mode bleibt aktiv.
- Risks:
  - **Adress-/KvK-/BTW-Daten sind Platzhalter** (accepted, by-design V8.2) — User-Action ausstehend, asynchron OK. Entwurfs-Header in beiden Files signalisiert Status ehrlich.
  - **Anwalts-Pruefung der Inhalte ausstehend** (accepted, deferred) — Pre-Customer-Live-Compliance-Gate per `feedback_compliance_gate_later`.
  - **Middleware-Public-Whitelist-Bug** (resolved per Hotfix `fad6654` waehrend /deploy Live-Smoke) — Build/Static-Prerender verifizieren Pages, aber NICHT Middleware-Public-Status. IMP-Kandidat fuer Dev-System: /qa muss Public-Routes-Slices explizit gegen Middleware-Whitelist pruefen.
  - **AC6 Footer-Sichtbarkeit im logged-in App** (accepted, code-side verified) — Browser-Live-Smoke wegen Chrome-MCP-Konflikt verschoben, Container ist healthy mit neuem Code, Footer per Build prerendered.
  - **AC7 Browser-Back-Button** (accepted, deferred) — Browser-Default-Verhalten, Public-Routes haben kein Auth-Gate (verified durch Hotfix), Back-Button funktioniert per Definition ohne Re-Login.
- Rollback Notes: V8.2 hat **0 Schema-Migrationen**. Rollback per Coolify-Image-History → REL-035 (`6168b5d`) ist trivial — keine DB-State-Aenderungen, keine RLS-Drift. Bei Rollback verschwindende V8.2-Effekte: `/datenschutz` + `/impressum` 404, Footer-Links auf Login + (app)/layout entfernt. V8.2-Code-Diff war ausschliesslich additiv (8 neue Files, 4 modifiziert) — Git-Revert von Slice-Merge + Hotfix bringt sauberen V8.1-Stand zurueck. RTO: 3 Min (Coolify-Image-Rollback).

### REL-035 — V8.1 Solopreneur-Mode + Sidebar-Konsolidierung + Teamlead-Permission-Erweiterung
- Date: 2026-05-21
- Image-Tag: `6168b5dfc0674af51f49c3e3b6f7d751f72051ea` (= main HEAD `6168b5d` /final-check-Commit; Live-verified via Coolify Container-Name `app-k9f5pn5...-154101454109` Started 2026-05-21T15:42:23 UTC, healthy nach 24min Uptime). **Auto-Deploy-Observation**: Container wurde von Coolify selbst neu deployed waehrend der /go-live-Auswertung (vermutlich Auto-Deploy-on-Push nach `git push` der /final-check-Records).
- Scope: V8.1 Hygiene-Sprint nach V8. 1 Feature FEAT-811 mit 4 Sub-Slices SLC-821..824. **KEINE Schema-Migration** (additive UI/Permission-Layer-Only). 4 V8.1-DECs (DEC-227..230, DEC-230 supersedes DEC-193+DEC-194 fuer Teamlead-Permission).
  - **SLC-821** Solopreneur-Mode (FEAT-811 / BL-482, RPT-494+495): Layout-Server-Side filtert TEAM-Sidebar-Section wenn `getTeamSize === 1`. Neuer Helper `cockpit/src/lib/team/team-size.ts` mit React `cache()`-Memoization (Pattern-Reuse `getProfile.ts`). Edge-Case `team_id === null` → Solo (return 1). Layout-Aufruf `app/(app)/layout.tsx`. 4 neue Vitest-Cases. Solo-Admin (team_id=NULL oder einziges Team-Mitglied) sieht KEINE TEAM-Sidebar — WERKZEUGE bleibt. Reine Sidebar-Filterung, /settings-Tile-Page nicht im Scope (Polish-Kandidat V8.2 falls gewuenscht).
  - **SLC-822** Sidebar-Konsolidierung Option A (FEAT-811 / BL-483, RPT-496): `VERWALTUNG_SETUP` → `WERKZEUGE`-Rename mit Item-Reduktion **14 → 3**. 11 Config-Items entfernt (Pipelines/Kampagnen/Templates/Workflow-Automation/NL-Sculptor-Audit/Branding/Zahlungsbedingungen/Produkte/Einwilligungstexte/Ziele/Cadences) — bleiben als Tiles auf /settings erreichbar. 3 operative Tools (Handoffs/Referrals/Audit-Log) bleiben in WERKZEUGE. Bestehender `/settings`-Eintrag in `VERWALTUNG_MEIN` unveraendert. Sidebar-Component `SECTION_PARENT.WERKZEUGE = undefined` (eigene Top-Section ohne Parent). Type-Refactor SidebarSection + SECTION_LABEL + SECTION_PARENT + SECTION_ORDER.
  - **SLC-823** Teamlead-Tile-Sichtbarkeit (FEAT-811 / BL-484, RPT-497): `/settings/team` Tile-Permission `ADMIN_ONLY` → `ADMIN_TEAMLEAD` in `settings/page.tsx:187`. Description neutralisiert ("Team-Mitglieder einsehen und verwalten"). Teamlead findet die Seite jetzt sowohl via Sidebar als auch via Tile-Page (vorher: nur Sidebar). Member sieht weiterhin keine System-Section.
  - **SLC-824** Teamlead-Edit-Erweiterung (FEAT-811 / BL-485, RPT-498+499): Permission-Layer-Refactor mit Defense-in-Depth-Verstaerkung. `inviteMember` Server-Action erlaubt Teamlead-Caller nur `role='member'` (`INVALID_ROLE_FOR_TEAMLEAD_INVITER`). `deleteProfile` erlaubt Teamlead-Caller bei Target im eigenen Team mit `role='member'` (3 Guards: `FORBIDDEN_NON_MEMBER`, `FORBIDDEN_OTHER_TEAM`, `FORBIDDEN_SELF`). Hard-Lock (DEC-193, 8 Owner-Tabellen) unveraendert fuer beide Rollen. `audit_log.changes.caller_role` neu fuer forensische Nachvollziehbarkeit. DSGVO-Backup-Felder `display_name_backup` + `role_backup` + `team_id_backup` persistiert. UI: invite-dialog Rollen-Dropdown bei Teamlead nur "Member", team-members-table Delete-Button nur bei Member-Rows non-self. 9 neue Vitest-Cases.
- Summary: V8.1 schliesst zwei pre-Customer-Live-Hygiene-Themen: (a) **Solopreneur** — Sidebar fuer Solo-Admins ohne Team-Aggregat-Mitglieder visuell entlasten (Erstes Onboarding-Erlebnis sauber). (b) **Sidebar-Inflation** — Verwaltungsbereich von 14 auf 3 Eintraege geschrumpft, Tile-Page als kanonischer Einstieg etabliert (V8-FEAT-801 Setup wirkt). (c) **Teamlead-Permission-Verschiebung** — Teamlead darf jetzt Member im eigenen Team einladen + loeschen ohne Org-Struktur-Aenderungen (Admin bleibt verantwortlich fuer Teamlead/Admin-Anlage). Risk-Profil minimal (0 Schema-Migration, 0 RLS-Aenderung — V7-MIG-033 Helper-Functions unangetastet). Vitest 1054/1054 PASS (+15 V8.1). Build PASS. V8.1-Files Lint-clean (pre-existing Baseline 142e/57w unveraendert). Live-Smoke 4 Pfade (Solopreneur/Admin/Teamlead/Member) PASS via Playwright-MCP (RPT-500). 8-Layer Defense-in-Depth in SLC-824 verifiziert (assertRole + assertNotReadOnlyContext + Zod + 3 Teamlead-Guards + FORBIDDEN_SELF + Hard-Lock + caller_role-Audit + DSGVO-Backup). Reports-Trail RPT-491..502. Internal-Test-Mode bleibt aktiv bis Pre-Production-Compliance-Gate.
- Risks:
  - **ISSUE-071 Disk-Druck 90% (3.8G free)** (accepted, mitigated) — Coolify-Auto-Cleanup hat trotz 90% Pre-Disk gesund durchgelaufen (Pattern konsistent zu REL-030/032/033). systemd-timer docker-prune.timer aktiv (Sun 03:00 UTC weekly). Erwartet Sinken auf 82-83% binnen 12-17h. Falls NICHT unter 85% binnen 4-6h: manuell `docker builder prune -af && docker image prune -af`.
  - **Auto-Deploy-Drift (NEU 2026-05-21)** — Coolify-App-Resource hat Auto-Deploy-on-Push aktiv (oder wurde manuell ueber Coolify-UI getriggert), was den Container schon vor /go-live-Decision auf das /final-check-Commit umgestellt hat. Kein Schaden in diesem Fall (Code war /qa+/final-check-validiert, Code-Diff 4bf5de7→6168b5d = 0 .ts/.tsx-Aenderungen). Per User-Direktive `feedback_manual_deploy` "User deployt IMMER manuell" → entweder Coolify-Auto-Deploy deaktivieren oder Memory aktualisieren. IMP-Kandidat fuer Dev-System.
  - **Solopreneur sieht Team-Verwaltung-Tile auf /settings** (accepted, by-design SLC-821-Scope) — Sidebar-Filter wirkt, Tile-Page-Filter ist nicht im V8.1-Scope. V8.2-Polish-Kandidat falls gewuenscht.
  - **ISSUE-058-Familie pre-existing npm-Vulnerabilities** (accepted) — postcss/fast-uri/ws/brace-expansion. Keine Production-Code-Pfade. Upstream-Fix-Wait.
  - **Pre-existing Lint-Baseline 142e/57w** (accepted) — V7.7 Tech-Debt-Sprint-Kandidat.
  - **supabase-studio Container `unhealthy`** (accepted) — pre-existing Coolify-Studio-Healthcheck-Drift, betrifft nur Admin-DB-UI.
  - **ISSUE-078 Sonner-Hydration React #418** (accepted, Low) — V7.7+ Mini-Slice-Kandidat.
- Rollback Notes: V8.1 hat **0 Schema-Migrationen**. Rollback per Coolify-Image-History → REL-034 (`c5e0f0c`) ist trivial — keine DB-State-Aenderungen, keine RLS-Drift, keine neuen Tabellen/Spalten. Bei Rollback verschwindende V8.1-Effekte: TEAM-Sidebar wieder sichtbar fuer Solo-Admins, Sidebar zeigt wieder 14 VERWALTUNG_SETUP-Eintraege (statt 3 WERKZEUGE), Teamlead sieht Tile "Rollen-Verwaltung" auf /settings nicht mehr, Teamlead kann nicht mehr Member einladen oder loeschen (V7-Verhalten ADMIN_ONLY zurueck). V8.1-erzeugte audit_log-Eintraege (`profile_deleted` mit `caller_role='teamlead'`) bleiben als historische Forensik-Trail. RTO: 3 Min (Coolify-Image-Rollback). Per-Item Git-Revert moeglich (4 Sub-Slice-Merge-Commits + 3 Records-Commits).
- Burn-In: PASS 2026-05-22 nach ~15h Beobachtung (RPT-504). Container RestartCount=0, Disk 90%→83% (Plan-Match 82-83% via Coolify-Auto-Cleanup), Stack 15/16 healthy (1 pre-existing supabase-studio unhealthy), 0 errors/exceptions in App-Logs, 181 saubere ai_signal_extract_run Cron-Runs alle errors=0, 0 Bedrock-Cost-Drift (V8.1 fuegt 0 LLM-Pfade hinzu, kein User-LLM-Touch in 15h), 0 profile_deleted Events (caller_role-Audit-Pfad ist codeseitig RPT-500 abgenommen, Live-Trigger steht aus). Status released-stable.

### REL-034 — V8 Hygiene-Sprint (Settings-Refactor + KI-Provider-Naming + Stage-Pflichtfelder-Modal)
- Date: 2026-05-20
- Image-Tag: `c5e0f0c18f940f9e7b21a6c40ab70e1b4507c548` (= main HEAD `c5e0f0c` nach Master-Merge SLC-811 + SLC-813; Live-verified via Coolify Container-Name `app-k9f5pn5...-145534829375` Started 2026-05-20T14:58:21 UTC).
- Scope: V8 Hygiene-Sprint, 3 Slices SLC-811..813, 4 Features FEAT-801..804. **KEINE Schema-Migration** (DEC-225). 8 V8-DECs (DEC-220..226).
  - **SLC-811** Settings-Refactor + /performance-Cleanup + Goals-Move + Aufgabe-Label (FEAT-801 + FEAT-803, RPT-482): /settings rendert 3 Sections (Persoenlich/Vertrieb/System) mit 14 Tiles bei Admin / 7 bei Teamlead / 3 bei Member. Rollen-Verwaltung-Tile als eigenes Tile in System-Section sichtbar. /performance-Page komplett geloescht (404). /performance/goals → /settings/goals (Git-Move). Drilldown-Button auf team-members-table aktiviert. Sidebar-Eintrag "Ziele" auf /settings/goals umgezeigt. Task-Label auf Deal-Detail → "Aufgabe" (Konsistenz mit Mein-Tag). middleware-guards aktualisiert.
  - **SLC-812** KI-Provider-Naming-Abstrahierung (FEAT-802 / BL-480, RPT-481): 4 UI-Strings substituiert — AnswerPane-Spinner "Bedrock arbeitet" → "KI arbeitet", ItemSheet KI-Section-Header "KI-Zusammenfassung" (war bereits korrekt), NL-Builder Error-Toast "Bedrock-Aufruf fehlgeschlagen" → "KI-Aufruf fehlgeschlagen", E-Mail-Compose Edit-Description "Bedrock modifiziert" → "KI modifiziert". `formatModelDisplayName()`-Helper exportiert + 7 Vitest-Cases (ready fuer V9+ Provider-Diversifikation, kein aktueller User-UI-Caller). 0 Bedrock/Claude/Anthropic/AWS-Strings im User-UI (Grep-verifiziert). Vendor-Lock-in-Vermeidung pre-Customer-Live.
  - **SLC-813** Stage-Requirements-Modal + KI-Verlustgrund-Vorschlag (FEAT-804 / BL-455 + BL-456, RPT-483 + RPT-484): Drag-Drop auf Stage mit fehlendem Pflichtfeld oeffnet Modal statt Toast-Error (5 STAGE_REQUIRED_FIELDS-Stages: "Angebot vorbereitet"/"Angebot offen" → `value`, "Verhandlung / Einwände" → `value`+`contact_id`, "Gewonnen" → `value`, "Verloren" → `won_lost_reason`). Bei "Verloren": KI-Verlustgrund-Vorschlag aus Activity-History (letzte 10 Activities) + E-Mail-Threads (letzte 3, Snippet 600→200 Trim) via Bedrock Claude Sonnet eu-central-1. Pattern-Reuse aus V7.5 sculptor-DI-Core + sculptor-cost + IS-SLC-109 heal-JSON-escapes. 6 Audit-Status (succeeded/skipped_empty_context/bedrock_error/parse_error/schema_error/deal_not_found). 42 neue Vitest-Tests (Modal 13 + Suggester 8 + Parser 9 + Prompt 6 + move-deal 6). `moveDealToStage` erweitert um 4. Parameter `requirementValues` (backward-compatible). audit_log-Trail vollstaendig (3 Eintraege pro Verloren-Confirm: ki_loss_reason_suggested + update + stage_change).
- Summary: V8 ist Hygiene-Sprint zwischen V7.6 Custom-Reports und V9 Real-Use-Erkenntnisse. **3 zusammenhaengende Themen**: (a) Settings-Layout-Refactor mit Rollen-Auffindbarkeit, (b) Vendor-Lock-in-Vermeidung pre-Customer-Live durch KI-Provider-Naming-Abstrahierung, (c) Erste KI-Augmentation am Pipeline-Workflow (Verlustgrund-Suggest). User-Discovery-Entscheidungen 2026-05-19: WhatsApp + Slack/Teams aus V8 raus (BL-443 Parking V10+), keine 4. Rolle (Steuerberater-Zugang ist OS-Thema). Live-Smoke 5/9 ACs PASS direct + 4 deferred mit Compensating Controls (RPT-488). 0 Schema-Migration. Risk-Profil niedrig. Master-Merge clean (0 Code-Konflikte zwischen SLC-811↔SLC-813, 2 triviale Records-Konflikte via "both done"-Resolution). Reports-Trail RPT-476..488. M-1 Resolution Option A: SLC-813 AC6 Slice-Spec praezisiert (Hint-Klausel akzeptiert, code unveraendert). **ISSUE-071 RESOLVED in dieser Session via systemd-timer docker-prune.timer (Sun 03:00 UTC weekly)** — Disk-Voll-Risiko nach 4 Auftritten endgueltig mitigiert. Internal-Test-Mode bleibt aktiv bis Pre-Production-Compliance-Gate.
- Risks:
  - **ISSUE-071 RESOLVED** in dieser Session — systemd-timer eingerichtet auf 91.98.20.191, Test-Lauf 722.4MB reclaimed (88%→82%), naechster automatischer Lauf Sun 2026-05-24 03:04:37 UTC.
  - **AC11 SLC-813 Live-Smoke deferred** (accepted, Compensating: 42 Unit-Tests + Live-Bedrock-Pfad funktioniert + audit_log-Schema live). Manual-User-Smoke-Skript in RPT-488 dokumentiert (30 Sek, optional).
  - **MED-1 Playwright-MCP-dnd-kit-Limitierung** in horizontal-scrolled-Containern (accepted, V7.7-IMP-Kandidat fuer Dev-System Skill-Improvement) — Pattern-Doku fuer kuenftige Smoke-Sessions.
  - **AC4 SLC-812 NL-Builder-Error-Toast** deferred (accepted, Bedrock-Failure-Trigger nicht in Live einfach erzeugbar, Compensating: Vitest verifiziert + Bundle-Substring vorhanden).
  - **Pre-Existing Lint-Schuld 200 Probleme** (accepted, Low, ausserhalb V8-Scope) — SLC-813 hat 1 pre-existing `as any` in pipeline/actions.ts eliminiert (Net-Effekt: -1). V7.7+ Cleanup-Slice-Kandidat.
  - **npm audit 5 vulnerabilities** (accepted, 1 high fast-uri transitive DevDep via shadcn CLI + 4 moderate next-postcss/ws/brace-expansion) — Pre-V8-Bestand per ISSUE-058. V7.7+ Major-Upgrade-Slice fuer next@latest. Force-Fix Catastrophic per DEC-161.
  - **COMPLIANCE.md V7.5+V7.6+V8-Sections fehlen** (accepted, Medium, Pre-Production-Compliance-Gate-Item) — V8 fuegt `ki_loss_reason_suggested`-Audit-Action hinzu. Per User-Direktive 2026-05-01 "kommt viel spaeter" vor Customer-Live.
- Rollback Notes: V8 hat **0 Schema-Migrationen** (DEC-225). Rollback per Coolify-Image-History → REL-033 `1ffae6e` ist trivial — keine DB-State-Aenderungen, keine Schema-Drift, keine neuen Tabellen/Spalten. Bei Rollback verschwindende V8-Effekte: Settings-Page geht zurueck auf V7.6-Layout (flach, ohne 3-Section-Gruppierung), /performance-Page wieder erreichbar, "Bedrock arbeitet"-Strings wieder im User-UI, Drag-Drop auf Stage mit Pflichtfeld-Luecke wirft wieder Toast-Error (statt Modal). V8-erzeugte audit_log-Eintraege (`ki_loss_reason_suggested`) bleiben als historische Forensik-Trail. V7.5+V7.6+V7-Code (Custom-Reports, NL-Sculptor, Multi-User) unangetastet. RTO: 3 Min (Coolify-Image-Rollback). Per-Item Git-Revert moeglich (3 Master-Merge-Commits: SLC-811 `577930f`, SLC-813 `c5e0f0c`, plus M-1-Spec-Fix `7f3d1b3`).

### REL-033 — V7.6 KI-Workspace Erweiterung (NL-Builder-Refactor + Custom-Reports)
- Date: 2026-05-19
- Image-Tag: `1ffae6e` (= main HEAD post /go-live-Doc-Commit; Live-verified via Coolify Build-ID `95JC00DBjXBUAOyGOIYoQ` + Container-Suffix `142831320347`).
- Scope: V7.6 KI-Workspace Erweiterung, 3 Slices SLC-761..763, 2 Features FEAT-761 + FEAT-762. MIG-037 angewendet auf Hetzner-DB (additive Schema-Erweiterung: `custom_reports`-Tabelle mit 4 RLS-Policies + 2 GRANTs auf authenticated + service_role + UNIQUE(owner_user_id, name)). 7 V7.6-DECs (DEC-212..218).
  - **SLC-761** NL-Builder als 6. Workspace-Button (FEAT-761, RPT-469): NL-Rule-Builder von Standalone-Card auf /mein-tag in den KI-Workspace verschoben. 6. Button "Workflow bauen" mit admin/teamlead-Sichtbarkeit. Alte Card-Datei + Tests geloescht. Workspace-Input-Bar im NL-Builder-Mode disabled. Cost-Display + Modell-Anzeige aus User-UI entfernt (DEC-214/AC7, bleibt im audit_log fuer Forensik). FEAT-751-Backend (sculpt/preview/apply) unveraendert.
  - **SLC-762** Custom-Reports Backend (FEAT-762, RPT-470): MIG-037 idempotent applied. 5 Server-Actions (saveCustomReport/listCustomReports/runCustomReport/renameCustomReport/deleteCustomReport) mit Defense-in-Depth 3-Layer (RLS + assertNotReadOnlyContext + Owner-Cross-Check in run.ts). 4 neue audit_log-Actions. Bedrock Region-Pin DEC-211 gehalten. RLS-Live-Test PASS gegen Coolify-DB.
  - **SLC-763** Custom-Reports Frontend (FEAT-762, RPT-471): AnswerPane "Als Bericht speichern"-Button (Free-Form-Only, DEC-216). Save-Modal (Native HTML Form + 409-Duplicate-Inline-Error). "Meine Berichte"-Dropdown (Empty/1-5/6+ Render-States + Type-Ahead-Filter ab 6+ + ⋮-Sub-Menu fuer Rename+Delete). Context-Filter mein-tag vs cockpit (DEC-215). KIWorkspace-Wire-Up via `__custom__`-loadRunner-Adapter. End-to-End Playwright-MCP Live-Smoke gegen Coolify 17/17 ACs PASS.
  - **MIG-037** idempotent applied (2026-05-19, RPT-470): `CREATE TABLE custom_reports` + 4 RLS-Policies (owner_user_id = auth.uid()) + GRANTs authenticated + service_role + 2 Indizes (owner+context-type, UNIQUE owner+name) + `NOTIFY pgrst, 'reload schema'`. Verifiziert via `pg_policy`-Query: 4 Policies (d/a/r/w).
- Summary: V7.6 erweitert den KI-Workspace um zwei zusammengehoerige Features. **FEAT-761** integriert den V7.5-NL-Rule-Builder visuell ins Workspace (statt Standalone-Card) — gleicher Backend-Pfad, neues UI-Pattern, kein Re-Wiring der Sculpt/Preview/Apply-Server-Actions. **FEAT-762** fuegt persistente User-eigene KI-Berichts-Vorlagen hinzu, die per "Meine Berichte"-Dropdown abrufbar sind (mein-tag-Scope vs cockpit-Scope getrennt). KEINE Button-Inflation per V6.6-Direktive. Cost-Tracking je Custom-Report-Run im audit_log.context.cost_usd. Per-User-Cost-Cap defer V7.7+. Risk-Profil niedrig (1 additive Schema-Migration, 1 UI-Refactor ohne Backend-Touch, neue Bedrock-Pfade mit Real-Cost-Audit, kein Auth-Refactor, kein Session-Bruch, kein V6.2-Dispatcher-Touch). Pre-Live: Final-Check RPT-473 CONDITIONALLY READY post Disk-Cleanup, Go-Live RPT-474 GO. Vitest 982/982 PASS. ESLint 143e/57w = Pre-V7.6-Baseline ohne Regress. Reports-Trail RPT-466..475. Internal-Test-Mode bleibt aktiv bis Pre-Production-Compliance-Gate.
- Risks:
  - **ISSUE-071 weiterhin offen** (Coolify-Cron fuer Docker-Cleanup) — Resilience-Item, hat sich pre-/deploy als Block-Risk gezeigt (93% Disk). Cleanup-Workaround funktioniert, Cron-Setup zunehmend kritisch fuer nachhaltige Single-User-Live-Phase.
  - **F-2 AUDIT_SERVER_ACTIONS_V7.md V7.6-Section** RESOLVED (in SLC-761 MT-1 nachgetragen mit V7.5- + V7.6-Section komplett).
  - **F-3 COMPLIANCE.md V7.5+V7.6-Sections fehlen** (accepted, Medium, Pre-Production-Compliance-Gate-Item) — NL-Sculptor + 4 V7.5-Audit-Actions + 4 V7.6-Custom-Report-Audit-Actions nicht erwaehnt. Per User-Direktive 2026-05-01 "kommt viel spaeter" vor Customer-Live.
  - **ISSUE-078 Sonner-Toast** (accepted, Low) — V7.6 BL-478, Card-State-Reset (SLC-757) funktional aequivalent.
  - **BL-480 KI-Provider-Anzeige im User-UI abstrahieren** (accepted, neu 2026-05-19) — Strings wie "Bedrock arbeitet ..." und "Bedrock modifiziert ..." sollen vor Customer-Live auf generisches "KI-System" umgestellt werden. Vendor-Lock-in-Vermeidung. Mini-Slice ~1-2h.
  - **ISSUE-079 Duplicate BL-442 in backlog.json** (accepted, Low) — Funktional unkritisch, V7.7+ Cleanup.
  - **Pre-Existing-Lint-Schuld 143 errors / 57 warnings** (accepted, Low, ausserhalb V7.6-Scope) — V7.7+ Cleanup-Slice empfohlen.
  - **npm audit 5 vulnerabilities** (accepted, 1 high `ws` Uninitialized-Memory + 4 moderate next-postcss) — Nicht V7.6-spezifisch, V7.7+ Major-Upgrade-Slice (next major bump).
- Rollback Notes: V7.6 hat 1 additive Schema-Migration (MIG-037 idempotent + CREATE TABLE custom_reports + 4 RLS-Policies, 1-Phasen-Backout-faehig). Rollback per Coolify-Image-History → vorheriger stable Tag REL-032 (`4228209`). Bei Rollback verschwindende V7.6-Effekte: KI-Workspace zeigt 5 Standard-Buttons + NL-Rule-Builder als Standalone-Card auf /mein-tag (V7.5-Layout zurueck), "Meine Berichte"-Dropdown unsichtbar, AnswerPane ohne "Als Bericht speichern"-Button. MIG-037-Tabelle `custom_reports` bleibt isoliert in DB (V7.5-Code ignoriert sie). Bestehende custom_reports-Eintraege bleiben in der DB, sind aber durch das UI nicht mehr erreichbar. audit_log-Eintraege `custom_report.created/executed/renamed/deleted` bleiben als historische Forensik-Trail. Per-Item Git-Revert moeglich (V7.6-Code-Commits atomic-revertable). User-bestehende Daten unangetastet. **DROP-TABLE-Befehl bei Bedarf:** `DROP TABLE custom_reports CASCADE;` — vorher pruefen ob die Daten noch gebraucht werden.

### REL-032 — V7.5 Natural-Language Workflow-Sculptor + ISSUE-066-Closure
- Date: 2026-05-18
- Image-Tag: `42282090bbf4e6c93a0f6dbc7ed9b777e7372051` (= main HEAD `4228209` post Go-Live-Doc-Commit + Parent `08427aa` F-1-Fix; Live-verified via `docker ps` 2026-05-18 16:56 UTC).
- Scope: V7.5 NL-Workflow-Sculptor + ISSUE-066-Closure, 7 Slices SLC-751..757, 2 Features FEAT-751 + FEAT-752. MIG-036 angewendet auf Hetzner-DB (additive Schema-Erweiterung: `automation_rules.created_via TEXT NOT NULL DEFAULT 'click_wizard'::text` + CHECK-Constraint `IN ('click_wizard','nl_sculptor')`). 9 V7.5-DECs (DEC-205..213).
  - **SLC-751** Middleware-Mitigation (FEAT-752, RPT-444): Pfad-Regex `/^\/team\/[^/]+\//` setzt `X-Read-Only-Mode: 1` Request-Header. `assertNotReadOnlyContext()` liest beide Layer parallel (AsyncLocalStorage + next/headers). Best-Effort `audit_log`-Insert mit Action `read_only_context_blocked` bei Block. Live-Smoke: Direct-Server-Action-Fetch als Teamlead gegen `/team/<member>/mein-tag` → HTTP 500 + audit_log-Eintrag `actor_id=...000078`, `entity_type=read_only_context`, `context.blocked_via=header`.
  - **SLC-752** Sculptor-Core + MIG-036 (FEAT-751, RPT-447): Single-Shot + 1x Re-Prompt-Loop (DEC-205), heal-JSON-Escapes Fallback (IS-SLC-109-Pattern-Reuse), audit_log INSERT pro Attempt mit nl_input + cost + session_id, calculateSculptCost mit Real-Cost-Display (DEC-208). 93 neue Vitest-Tests.
  - **SLC-753** Mein-Tag-NL-Surface + Sculpt-Server-Action (FEAT-751, RPT-450): `sculptNlRule()` Server-Action mit Role-Gate. NL-Rule-Builder-Card auf `/mein-tag` (Admin+Teamlead). Hotfix `bafe9ca` (ISSUE-076 PRICING-Coverage Kurz-Form-Alias). End-to-End-Live-Smoke gegen Production: cost=0.009312 in audit_log, UI rendert.
  - **SLC-754** Trockenlauf-Karte + Apply-Confirmation-Modal (FEAT-751, RPT-453): `previewNlRule()` via V6.2-`dryRunRule`-Reuse (DEC-132). `applyNlRule()` mit Re-Validation + Soft-Dedup + INSERT automation_rules `created_via='nl_sculptor'` + INSERT audit_log `create_via_nl`. ISSUE-077 entdeckt (Card-State-Reset-Bug).
  - **SLC-755** Voice-Input (FEAT-751, RPT-456): VoiceRecordButton-Reuse (V5.3) + `/api/transcribe` (Whisper-Adapter). transcript_source-Flag in audit_log differenziert 'text' vs 'voice'.
  - **SLC-756** Inspection-Log (FEAT-751, RPT-459): `/settings/workflow-automation/nl-history` Admin-only (assertRole), Sidebar-Eintrag, `listNlSculptHistory()` mit Owner-Scope-Filter, EnrichedNlHistoryRow mit actor_email-Resolve aus auth.users.
  - **SLC-757** NL-Rule-Builder Apply-Polish (FEAT-751, RPT-460): ISSUE-077 RESOLVED via `handleApply()` ruft `handleNewRule()` nach `res.ok` (Card-State-Reset komplett). Toast-Teil als ISSUE-078 (Turbopack 16.2 Hydration-Bug) abgespalten → V7.6 BL-478.
  - **F-1 Pre-Go-Live-Fix** (Commit `08427aa`): `await assertNotReadOnlyContext()` als first line in `sculptNlRule()` (Code-Symmetrie zu apply-nl-rule.ts). Test-Mock ergaenzt. Defense-in-Depth-Gap analog ISSUE-070 geschlossen vor Release.
  - **MIG-036** idempotent applied (2026-05-16, RPT-447): `ALTER TABLE automation_rules ADD COLUMN created_via TEXT NOT NULL DEFAULT 'click_wizard'::text` + CHECK-Constraint. 2 Bestands-Rows = `click_wizard`. V7.5-Rules angelegt mit `created_via='nl_sculptor'`.
- Summary: V7.5 ist der erste Natural-Language-getriebene Workflow-Builder im Business-System. Admin und Teamlead beschreiben in Klartext (Tipp- oder Voice-Eingabe) einen Workflow, Bedrock Claude Sonnet (eu-central-1, EU-DPA) sculptet einen V6.2-kompatiblen Rule-Schema-Output, Trockenlauf zeigt was triggern wuerde, Apply persistiert. Inspection-Log auf `/settings/workflow-automation/nl-history` ist Admin-Sichtbarkeit. ISSUE-066 Defense-in-Depth-Gap (AsyncLocalStorage propagiert nicht in Server-Action-Requests aus Drilldown) ist via Middleware-Header-Pattern geschlossen — `assertNotReadOnlyContext()` greift jetzt auch bei Direct-Server-Action-Calls. Risk-Profil niedrig (1 additive Schema-Migration, neue Bedrock-Pfade mit Real-Cost-Audit, kein Auth-Refactor, kein Session-Bruch). Pre-Live-Burn-In: Live-Smoke RPT-461 gegen Production-Image fb2488a PASS, Final-Check RPT-462 READY post F-1-Fix, Go-Live RPT-463 GO. Vitest 1078/1078 PASS (+161 vs V7.2-Baseline 917). ESLint 144e/55w = V6.6-Baseline ohne Regress. Reports-Trail RPT-440..463. Internal-Test-Mode bleibt aktiv bis Pre-Production-Compliance-Gate.
- Risks:
  - **ISSUE-071 Disk-Voll-Risiko Hetzner** (accepted, High operational, **3. Auftritt in 4 Tagen 2026-05-18**) — Workaround vom 2026-05-18 wirksam (Disk 100%→82%→91% nach Coolify-Redeploy, 3.4GB free). User-Pflicht-Action: weekly Cron `docker builder prune -af && docker image prune -af` als Coolify-Cron oder systemd-timer einrichten. Naechstes Disk-Voll-Event in ~5-10 Tagen ohne Cron erwartbar.
  - **F-1 sculpt-nl-rule-Guard** (RESOLVED in Pre-Go-Live, Commit `08427aa`) — kein Carry-Forward.
  - **F-2 AUDIT_SERVER_ACTIONS_V7.md stale** (accepted, Medium, Doc-Hygiene) — 4 V7.5-Server-Actions nicht inventarisiert. Analog ISSUE-069-Pattern. V7.6-Defer-Kandidat (~15 Min Doc-Edit).
  - **F-3 COMPLIANCE.md ohne V7.5-Section** (accepted, Medium, Pre-Production-Compliance-Gate-Item) — NL-Sculptor + 2 neue Audit-Actions (`sculpt_attempt`, `create_via_nl`) + 1 Middleware-Action (`read_only_context_blocked`) + Voice-fuer-Sculpt nicht erwaehnt. Per User-Direktive 2026-05-01 "kommt viel spaeter" vor Customer-Live.
  - **ISSUE-078 Sonner-Toast** (accepted, Low) — V7.6 BL-478, Card-State-Reset (SLC-757) funktional aequivalent. Reproducer-Mini-Repo geplant.
  - **ISSUE-058 postcss CVE** (accepted, Low) — DEC-161 Force-Fix-Catastrophic, Upstream-Patch pending in next@16.x.
  - **fast-uri 3.1.0 HIGH-CVE** (accepted, Low) — Transitive DevDep via shadcn CLI, Build-Time only.
  - **supabase-studio Container UNHEALTHY** (accepted, pre-existing V5.x) — Admin-UI only, kein Request-Pfad-Impact.
  - **ISSUE-042 OpenAI-Key + Pre-Production-Compliance-Gate** — User-Direktive 2026-05-01 "kommt viel spaeter". Default-Transcription-Provider weiterhin `openai` (Internal-Test-Mode).
- Rollback Notes: V7.5 hat 1 additive Schema-Migration (MIG-036 idempotent + ALTER-ADD-COLUMN-Pattern, 1-Phasen-Backout-faehig). Rollback per Coolify-Image-History → vorheriger stable Tag REL-030 `770dd55`. Bei Rollback verschwindende V7.5-Effekte: NL-Rule-Builder-Card auf `/mein-tag` unsichtbar, `/settings/workflow-automation/nl-history` 404, Middleware-Header-Mitigation faellt zurueck (ISSUE-066 wieder aktiv aber kein Live-Risk weil keine Drilldown-Mutate-Pfade), MIG-036-Spalte bleibt isoliert in DB (V6.2-Code ignoriert `automation_rules.created_via`). V7.5-erzeugte automation_rules-Eintraege (created_via='nl_sculptor') bleiben aktiv und werden weiterhin durch V6.2-Dispatcher gefeuert — kein Daten-Verlust. audit_log-Eintraege `sculpt_attempt` + `create_via_nl` + `read_only_context_blocked` bleiben als historische Forensik-Trail. Per-Item Git-Revert moeglich (8 V7.5-Code-Commits + Doc-Commits atomic-revertable). User-bestehende Daten unangetastet. **DROP-COLUMN-Befehl bei Bedarf:** `ALTER TABLE automation_rules DROP COLUMN created_via;` — vorher Inspection-Log-Query-Coupling pruefen.

### REL-031 — V7.2 Test-Infra-Cleanup (Data-Only Release, kein Image-Push, kein Coolify-Redeploy)
- Date: 2026-05-16
- Image-Tag: `770dd55` (unveraendert seit REL-030 — V7.2 ist Test-Infra-only, App-Image bleibt V7.1)
- Scope: V7.2 Test-Infra-Cleanup-Sprint, 1 Slice SLC-721 mit 3 MTs, 1 Feature FEAT-721. Kein Production-Code-Touch, keine Schema-Migration, keine ENV-Aenderung, keine neue Coolify-Resource, kein Coolify-Redeploy. Die Aenderung lebt auf 2 Achsen: (a) Test-Infra-Code im Repo (Seed-Script-Erweiterung um qa-admin, vitest.rls.config.ts Path-Alias-Resolver, Test-Adjustment fuer DEC-204), (b) Coolify-DB-Daten (3 qa-* Test-User in auth.users + 7 Profile in Test-Team-077 inkl. qa-admin + 882 Seed-Rows). Beide Achsen sind bereits live: Code per Master-Merge in main, Daten per docker-run-Apply in MT-3.
  - **MT-1** qa-admin im Seed-Script (BL-471, DEC-204): `cockpit/scripts/seed-multi-user.ts` erhielt TEST_ADMIN_ID-Konstante (UUID `0...0ba001`), Profile-INSERT mit role=admin + team_id=TEST_TEAM_ID, Aux-Fixtures-Loop um qa-admin erweitert (4 zusaetzliche Records in meetings/proposals/email_messages/calls). `cockpit/scripts/create-qa-test-users.mjs` TEST_USERS-Array um 3. Eintrag qa-admin@strategaize.test (Password `QaV72-Admin!`) erweitert. Beide Scripts idempotent.
  - **MT-2** vitest.rls.config.ts resolve.alias (ISSUE-074, DEC-203): `cockpit/vitest.rls.config.ts` erhielt `import path from "node:path"` + `resolve: { alias: { "@": path.resolve(__dirname, "./src") } }`-Block (Pattern-Reuse aus `vitest.config.ts:14-18`). KNOWN_ISSUES.md ISSUE-074 wording fix: `vite-tsconfig-paths` ist NICHT in dependencies (falscher Befund im urspruenglichen ISSUE), DEC-203 waehlt bewusst resolve.alias statt Plugin.
  - **MT-3** Coolify-Apply + Test-Run + Records-Sync: Seed-Script + Auth-User-Script via `docker run node:20 --network k9f5pn5upfq7etoefb5ukbcg_business-net` auf Coolify-DB applied (882 Rows in Run 1, identische 882 Rows in Idempotenz-Re-Run). `npm run test:all` gegen TEST_DATABASE_URL=Coolify-DB: **917 PASS / 0 FAIL / 0 SKIP** (779 jsdom + 138 RLS, davon 96 v7-rls-matrix vorher SKIP, 20 bulk-reassign vorher 0 wegen Path-Alias). 1 Test-Adjustment in `cockpit/__tests__/team/aggregate-queries.test.ts`: getTeamMembers + getTeamBedrockContext-Erwartungen 5→6 (qa-admin im Team-77, DEC-204) — Test-Code-only, Production-Logik unveraendert.
  - **Resolved**: ISSUE-073 (v7-rls-matrix 96 SKIP), ISSUE-074 (vitest.rls.config Path-Alias), BL-471/473/474.
- Summary: V7.2 ist data-only Test-Infra-Sprint nach REL-030. Schaltet 3 Accepted Risks aus V7.1 scharf: Multi-User-RLS-Matrix-Suite (96 Cases) jetzt Live-DB-getestet statt SKIPPED, Bulk-Reassign-Path RLS-Live-getestet (vorher 0 Tests geladen wegen `@/`-Import-Resolver-Fehlen), qa-admin als 3. Test-Rolle in Seed-Pipeline. Vorbereitung der Multi-User-Test-Basis fuer V7.5 (Natural-Language-Automation). Time-Box 3-4h: ~3h durchgekommen. Risk-Profil sehr niedrig — kein Production-Code, kein neuer Endpoint, kein Container-Touch. Pre-Release-Trail: RPT-430 (Requirements) → RPT-431 (Architecture) → RPT-432 (Slice-Planning) → RPT-433 (Backend) → RPT-434 (QA PASS-with-1-Note) → RPT-435 (Final-Check READY) → RPT-436 (Go-Live GO) → RPT-437 (Deploy). 5 Commits auf Branch `slc-721-test-infra-cleanup` Fast-Forward-merged in main.
- Risks:
  - **DEC-202 Container-Bootstrap-Hook fehlt** (accepted) — Coolify-Redeploy mit App-Image-Wechsel ueberschreibt die Coolify-DB-Daten NICHT (Postgres-Volume persistiert), aber bei einem evtl. zukuenftigen Postgres-Volume-Reset waeren die qa-* Seed-Daten weg. Workaround: erneuter `docker run node:20 ... seed-multi-user.ts` (~500ms). V7.3+ BL-475 plant Bootstrap-Hook.
  - **Server-Repo Default-Single-Branch-Fetch** (accepted) — `/opt/strategaize-business-system-test` braucht explizites `git fetch origin <branch>:<branch>` fuer neue Branches. V7.3+ Hygiene-Item.
  - **Mount-Pfad fuer campaigns-schema.test.ts** (accepted) — vitest-Run im node:20-Container braucht Repo-Root-Mount (`-v <repo>:/repo -w /repo/cockpit`), nicht nur cockpit-Mount. Skill-Improvement-Kandidat fuer Dev-System `coolify-test-setup.md`.
  - **L1 Test-Passwoerter hardcoded** in create-qa-test-users.mjs (accepted) — bestehende Test-User-Convention seit SLC-702, qa-* User RLS-isoliert in Test-Team-077.
- Rollback Notes: Rein additiv. Kein Coolify-Redeploy noetig, keine Rollback-Aktion am Production-Container. (1) Code-Rollback: `git revert` der 5 V7.2-Commits auf main (94b69b3, 1b2e046, 46497f9, 48211cb, a936d4a) — falls aus irgendeinem Grund unerwuenscht. Aber: Production-App-Image `770dd55` enthaelt KEINEN der V7.2-Code-Aenderungen (V7.2 ist nach REL-030 entwickelt + nur Test-Infra), daher wuerde ein Code-Rollback nur Test-Infra zurueckdrehen ohne Production-Impact. (2) Daten-Rollback (qa-* Seed-Daten loswerden): `docker exec supabase-db psql -U postgres -d postgres -c "DELETE FROM profiles WHERE id IN ('00000000-0000-0000-0000-0000000ba001','00000000-0000-0000-0000-000000000078','00000000-0000-0000-0000-000000000081',...); DELETE FROM auth.users WHERE email LIKE 'qa-%@strategaize.test';"`. Oder einfacher: `docker run node:20 ... seed-multi-user.ts --reset` (Reset-Mode loescht alle [TEST]-Records inkl. qa-admin).

### REL-030 — V7.1 Polish-Sprint (Settings-Permission-Layer + Drilldown-Vollausbau + Defense-in-Depth-Polish + SLC-714 KI-Workspace-Hotfix)
- Date: 2026-05-16
- Image-Tag: `770dd55` (Commit `770dd55b...`, Code-HEAD post-SLC-714, HEAD `e95a48c` enthaelt zusaetzlich RPT-427 Doc-only). Master-Merge `718b241` brachte SLC-713 auf main, Folge-Commit `770dd55` SLC-714-Hotfix.
- Scope: V7.1 Polish-Sprint, 4 Slices SLC-711 + SLC-712a + SLC-712b + SLC-713, 3 Features FEAT-711 + FEAT-712 + FEAT-713, Plus SLC-714 (BL-472) als Post-Smoke-Hotfix integriert. Keine Schema-Migration. Keine ENV-Aenderung. Keine neue Coolify-Resource.
  - **SLC-711** Settings-Permission-Layer (FEAT-711): 11 Settings-Sub-Pages gegen rollenfremde Zugriffe gesichert. Zwei-stufige Verteidigung: Server-Side `assertRole`-Guard (Direct-URL-Schutz) + Sidebar-Visibility-Filter (kein 403-Flash). Permission-Matrix DEC-196: Admin-only fuer Branding/Steuern/Pipelines/Produkte/Compliance/IMAP, Admin+Teamlead fuer Workflow-Automation/Templates/Kampagnen, Alle fuer Arbeitszeit/Meeting/Briefing. 3-Rollen-Tour Live-Smoke RPT-418 (Admin/Teamlead/Member via Playwright-MCP).
  - **SLC-712a** Pipeline-Drilldown via PipelineView-Reuse (FEAT-712): `/team/[user_id]/pipeline` zeigt jetzt die volle PipelineView read-only statt reduzierter Tabelle. Neue Props `readOnly?` + `viewAsUserId?` an PipelineView. Mutate-Elements (Drag-Drop, Edit, "Neuer Deal", Stage-Change-Dropdown) hidden. Filter-State-Storage-Key mit viewAsUserId-Postfix (DEC-200). PageHeader-Title bekommt "(Read-Only)"-Postfix. Live-Smoke RPT-420.
  - **SLC-712b** Aufgaben + Mein-Tag Drilldown (FEAT-712): `/team/[user_id]/aufgaben` und `/team/[user_id]/mein-tag` jetzt mit voller Component-Reuse read-only. KI-Workspace-scope auf target_user_id, Quick-Actions hidden. Live-Smoke RPT-422.
  - **SLC-713** V7-Defense-in-Depth Polish (FEAT-713): 4 first-line Guards via `await assertNotReadOnlyContext()` in 4 Mutate-Server-Actions (bulkReassignApply, saveInsight, updateWorkingHoursSettings, persistManualRun). persistManualRun aus `winloss.ts` zu sibling `winloss-persist.ts` (non-"use server") extrahiert. Audit-Doc `docs/AUDIT_SERVER_ACTIONS_V7.md` Section 10 ergaenzt + 1 Fehlklassifizierung korrigiert. 4 neue Vitest-Tests (read-only-context-Pattern). ISSUE-069 + ISSUE-070 resolved.
  - **SLC-714** Post-Smoke-Hotfix KI-Workspace-Hide (BL-472, FEAT-712): User-Smoke 2026-05-16 nach Master-Merge: Im Drilldown-`/team/<member>/mein-tag` wurde der KI-Workspace-Block weiterhin gerendert (SLC-712b-Spec sah das vor), aber alle Mutate-Pfade (5 Berichts-Buttons + Frage-Eingabe + Insight-Speichern) werfen Server-Errors im Read-Only-Context. UX-Direktive: Read-Only = keine KI-Interaktion. Fix: `mein-tag-client.tsx:344` hinter `!readOnly`-Gate analog zu allen anderen Mutate-Bloecken. Test `mein-tag-client.test.tsx` Case 4 invertiert. Volle Suite 779/779 PASS. Live-Smoke RPT-427 via Playwright-MCP gegen Production-HEAD `770dd55` (19/19 DOM-Assertions PASS: 0/5 KI-Buttons + 0/3 Quick-Actions im Drilldown, 5/5 + 3/3 im Self).
  - **ISSUE-075** (Production-User immo@bellaerts.de im Test-Team-77) resolved durch User-Cleanup: profile + auth.users + CASCADE-Tables entfernt. aggregate-queries.test.ts 6/6 PASS nach Cleanup.
- Summary: V7.1 ist Polish-Sprint nach V7 Multi-User-Release. Keine neuen Geschaeftsfeatures, drei Lockerheiten aus V7-Walkthrough nachgezogen plus Defense-in-Depth-Symmetrie plus 1 Post-Smoke-Hotfix. Risk-Profil niedrig (rein Application-Code im cockpit/, kein Schema-Touch, kein ENV-Touch, kein neuer Container). Pre-Release-Trail: RPT-413 (Discovery) → RPT-414 (Requirements) → RPT-415 (Architecture) → RPT-416 (Slice-Planning) → RPT-417+418 (SLC-711) → RPT-419+420 (SLC-712a) → RPT-421+422 (SLC-712b) → RPT-423+424 (SLC-713) → RPT-425 (Gesamt-/qa PASS-with-Caveats) → RPT-426 (Final-Check READY) → RPT-427 (SLC-714 Live-Smoke PASS). Vitest 779/779 PASS. Internal-Test-Mode bleibt aktiv.
- Risks:
  - **ISSUE-066 AsyncLocalStorage-Drilldown-Gap** (accepted, Medium) — Server-Action-Direct-Call aus Drilldown-Subtree kann Read-Only-Guard umgehen. UI-Layer 1 (versteckte Buttons + KI-Workspace weg via SLC-714) blockiert Praxis-Pfad. V7.5-Mitigation via Middleware-Header geplant.
  - **ISSUE-071 Disk-Voll-Risiko Hetzner** (accepted, High operational) — Workaround vom 2026-05-14 wirksam (~6GB free). User-Pflicht-Action: weekly `docker builder prune -af && docker image prune -af` via Coolify-Cron oder systemd-timer.
  - **ISSUE-073 v7-rls-matrix 96 SKIPPED** (accepted, Medium, pre-existing V7-Seed-Drift) — V7.2-Cleanup-Slice. V7.1 aendert keine RLS-Policies.
  - **ISSUE-074 vitest.rls.config.ts Path-Alias-Bug** (accepted, Medium, pre-existing V7-Test-Infra) — V7.2-Cleanup-Slice.
  - **ISSUE-058 postcss CVE** (accepted, Low, pre-existing) — Build-Time only, Force-Fix catastrophic.
  - **fast-uri 3.1.0 HIGH-CVE** (accepted, Low) — Transitive DevDep via shadcn CLI, Build-Time only.
  - **supabase-studio Container UNHEALTHY** (accepted, pre-existing V5.x) — Admin-UI only, kein Request-Pfad-Impact.
  - **Pre-Production-Compliance-Gate offen** — ISSUE-042 OpenAI-Key + Anwaltspruefung + Azure-EU-Whisper-Switch — User-Direktive 2026-05-01 "kommt viel spaeter".
- Rollback Notes: V7.1 ist 100% Application-Code (cockpit/), keine Schema-Migration, keine ENV-Aenderung. Rollback per Coolify-Image-History zu REL-029 Image-Tag `2131ec7`. Bei Rollback verschwindende V7.1-Effekte: Settings-Permission-Layer faellt zurueck (Member sieht alle Karten + Direct-URL ohne Redirect), `/team/[user]/pipeline` faellt auf reduzierte Tabelle zurueck (SLC-712a weg), `/team/[user]/aufgaben` + `/team/[user]/mein-tag` faellt auf reduzierte Variante zurueck, 4 Defense-in-Depth-Guards verschwinden (kein Live-Risk weil ISSUE-066 weiterhin blockt), KI-Workspace im Drilldown-MeinTag kommt zurueck (mit broken Mutate-Pfaden). Audit-Trail bleibt unveraendert. User-bestehende Daten unangetastet. Per-Commit Git-Revert moeglich (~5 V7.1-Code-Commits + ~10 Doc-Commits, atomic-revertable). DB-Schema unveraendert seit MIG-035.

### REL-029 — V7 Multi-User + Teamlead-Sprint (Foundation + Owner-Wiring + Team-Cockpit + Drilldown + Bulk-Reassign + Mobile-Hamburger + VERWALTUNG-Split + Invite-Flow)
- Date: 2026-05-14
- Image-Tag: `2131ec7` (Commit `2131ec744f785cea0508d4addadd9fd161748ffe`, BL-470 final fix mit set-password Page enthalten; HEAD `eec9ebb` enthaelt zusaetzlich Doc-only Closure-Commits — Code-Stand identisch).
- Scope: V7 Multi-User-Sprint, 7 Slices SLC-701..707, 3 Features FEAT-502 + FEAT-503 + FEAT-701. Plus BL-465 (vitest.config test:all), BL-467 (SMTP Coolify-ENV), BL-470 (Invite-Flow generateLink + eigener NodeMailer + Auth-Callback-Reverse-Proxy-Origin-Fix + set-password Page) als V7-Patches integriert. MIG-033 (V7-Schema-Phase A — owner_user_id auf 8 Kerntabellen + audit_log.view_as_target_user_id-Spalte) + MIG-034 (V7-Backfill-Phase B) + MIG-035 (V7-RLS-Switch-Phase C + 4 Helper-Functions: is_admin, is_teamlead, get_my_team_id, can_see_owner) angewendet auf Hetzner-DB (additive Schema-Erweiterung).
  - **SLC-701** Backend-Foundation (FEAT-502): 3-Phasen-Migration MIG-035 (owner_user_id auf 8 Kerntabellen + RLS-Policy-Switch + Helper-Functions). 138+ Live-DB-Tests gegen Coolify-DB (RLS-Matrix 96 + Drilldown 4 + Aggregat-Live-DB 6 + Bulk-Reassign 20). Test-Setup via `npm run test:rls` (BL-465-test:all-Script).
  - **SLC-702** Frontend-Foundation (FEAT-701 + FEAT-502 + FEAT-503): Layout-Wrap + rollen-konditionale Sidebar-Config (sidebar-config.ts mit visibleFor pro Section), Server-Side-Guards (assertRole, ReadOnlyContext-Helper, get-profile).
  - **SLC-703** Verwaltungs-UI (FEAT-502): /settings/team Page mit Mitglieder-Tabelle (6 Rows: 5 Members + Teamlead-Self), Invite-Dialog mit Team-Picker + Display-Resolver (ISSUE-063 Display-Resolver-Fix), Rolle-aendern Inline-Dropdown, Profile-Delete mit Hard-Lock bei offenen Owner-Records.
  - **SLC-704** Owner-Wiring (FEAT-502): ~80 Mutate-Server-Actions mit owner_user_id-Param + assertNotReadOnlyContext-Guard. Cron-Owner-Context + Workflow-Engine-Owner-Pass. ISSUE-064 (6 Files Guard-Gap) resolved.
  - **SLC-705** Team-Aggregat-Cockpit (FEAT-503): /team Page mit 4 KPI-Cards (Pipeline-Sum + Aktivitaeten + Conversion + Backlog) + Member-Tabelle (Aggregat) + KI-Workspace-Hybrid mit 3 Berichts-Buttons (Underperformance/Burnout/Stagnierende-Deals). ISSUE-065 `.single()`-Regression Self-Filter fixed.
  - **SLC-706** Drilldown-Routes (FEAT-503): /team/[user_id]/{mein-tag,pipeline,aufgaben} Read-Only-Sicht via Layout-Wrap (runWithReadOnlyContext via AsyncLocalStorage). Banner + view_as-Audit-Log pro Page-Load. Mutate-Buttons UI-side hidden. ISSUE-066 AsyncLocalStorage-Propagation-Gap dokumentiert + V7.5-Mitigation geplant.
  - **SLC-707** Polish + Bulk-Reassign + Mobile-Hamburger + VERWALTUNG-Split (FEAT-502 + FEAT-701): bulk-reassign-actions.ts mit Two-Phase-Audit (initiated + applied) + 8-Tables-Switch via SET LOCAL ROLE postgres (POSTGRES_URL ENV BL-464). Mobile-Hamburger Sheet-Drawer fuer <md:768px im (app)/layout.tsx (BL-457). VERWALTUNG-Split in Mein Profil + Setup Sub-Headers. Plus ISSUE-063 Display-Resolver-Fix.
  - **BL-465** vitest.config split: `test:all`-Script chained `test` (src/*, jsdom) + `test:rls` (__tests__/*, node-env, Coolify-DB). Default-Lauf bleibt schnell, Live-DB-Suite separat aufrufbar.
  - **BL-467** SMTP-Versand: Coolify-UI Environment Variables SMTP_PASS + SMTP_PASSWORD mit "Available at Buildtime + Runtime" Checkboxes (IMP-519-Lehre — bei neuen Variables sind sie standardmaessig UNCHECKED). Stack-Redeploy uebernahm Werte. Invite-Mail-Versand User-verifiziert.
  - **BL-470** Invite-Flow-Refactor: lib/auth/invite.ts auf `admin.auth.admin.generateLink({ type: 'invite' })` umgebaut + eigene Mail-Verification-URL gegen NEXT_PUBLIC_APP_URL + NodeMailer-Send mit HTML-Template. Plus app/auth/callback/route.ts 1:1 aus Onboarding-Plattform portiert (NEXT_PUBLIC_APP_URL statt request.nextUrl.origin + supabase.auth.signOut() vor verifyOtp() + cookies-Binding an NextResponse.redirect). Plus app/auth/set-password/{page.tsx,actions.ts} NEU. 8 Vitest-Mock-Tests fuer invite.ts. Live-Smoke User-PASS gegen immo@bellaerts.de.
- Summary: V7 ist der erste produktive Multi-User-Release von Business-System. Single-User-Mode (Internal-Test-Mode) ist damit aufgeloest — Admin kann Teamleads und Members einladen, Teamlead sieht Aggregat + Read-Only-Drilldown ueber sein Team. Risk-Profil: Eine additive Schema-Migration (MIG-035 idempotent + MIG-036 audit_log.view_as_target_user_id). Vitest 756/756 PASS (in Coolify-DB-Suite vorausgesetzt, separater Lauf `npm run test:rls`). Live-Smoke gegen business.strategaizetransition.com mit Playwright-MCP (RPT-409): Bulk-Reassign 174 Records + 9 audit_log-Eintraege + Mobile-Hamburger Auto-Close PASS. Invite-Mail-Flow End-to-End live verifiziert nach BL-470 (RPT-411). 4 V7-bezogene Memory-Files angelegt im Dev-System fuer kuenftige Strategaize-Projekte: feedback_check_existing_patterns_first.md (Pattern-Reuse-First-Direktive), feedback_auth_callback_proxy_origin.md (Reverse-Proxy-Origin-Pattern), feedback_no_browser_supabase (bestehend), reference_supabase_storage_layout (bestehend). Pre-V7-Final-Check RPT-410 (CONDITIONALLY READY). Live-Smoke RPT-409 + RPT-411. Deploy-Report RPT-412.
- Risks:
  - **ISSUE-066 AsyncLocalStorage-Drilldown-Gap** (accepted, Medium) — Server-Action-Direct-Call aus Drilldown-Subtree kann Read-Only-Guard umgehen. UI-Pfad heute nicht erreichbar, V7.5-Mitigation via Middleware-Header + Pfad-Check geplant.
  - **ISSUE-069 AUDIT_SERVER_ACTIONS_V7.md stale** (accepted, Medium) — Doc-Sync mit 5 Mutate-Files + 1 Fehlklassifizierung. BL-466 V7.1-Polish.
  - **ISSUE-070 4 Defense-in-Depth-Guards fehlen** (accepted, Medium) — bulk-reassign-actions.ts + insight-actions.ts + working-hours-actions.ts + winloss.ts ohne `await assertNotReadOnlyContext()`. Kein aktueller Exploit-Pfad (nicht aus Drilldown-Subtree aufrufbar). BL-466 V7.1-Polish.
  - **ISSUE-058 postcss-CVE** (accepted, Low, pre-existing) — Build-Time-only.
  - **ISSUE-071 Disk-Voll-Risiko Hetzner** (Workaround applied, High) — Disk-Cleanup vor diesem Deploy durchgefuehrt (96% → 83%). User-Pflicht-Action: Coolify-Cron `docker builder prune -af && docker image prune -af` weekly einrichten.
  - **fast-uri 3.1.0 HIGH-CVE** (accepted, Low) — Transitive DevDep via `shadcn` CLI (build-time only, kein Runtime-Path).
  - **supabase-studio Container UNHEALTHY** (accepted, pre-existing) — Admin-UI-only, kein Request-Pfad-Impact.
  - **Pre-Production-Compliance-Gate offen** — ISSUE-042 OpenAI-Key + Anwaltspruefung + Azure-EU-Whisper-Switch — User-Direktive 2026-05-01 "kommt viel spaeter".
- Rollback Notes: V7 hat 3 additive Schema-Migrationen (MIG-033 + MIG-034 + MIG-035, alle idempotent + CREATE-IF-NOT-EXISTS-Pattern, drei-Phasen-Backout-faehig). Rollback per Coolify-Image-History → vorheriger stable Tag REL-028 (`360c6ec`). Bei Rollback verschwindende V7-Effekte: TEAM-Sidebar-Section weg, /team + /team/[user_id]/* werden 404, /settings/team-Page wird unzugaenglich, Bulk-Reassign-Werkzeug nicht aufrufbar, Mobile-Hamburger faellt zurueck, VERWALTUNG-Split faellt zurueck, Invite-Mail-Flow funktioniert nicht. Owner-Wired Mutate-Actions (~80) verlieren owner_user_id-Param-Persistenz — Default-Verhalten faellt auf single-user-Mode zurueck (alle Records gehoeren dem Admin). Audit-Log behaelt view_as-Eintraege (bleiben in DB als historische Audit-Trail). MIG-035 RLS-Policies bleiben in DB — V6.6-Code ignoriert sie (RLS-Filter sind permissive fuer eingeloggten admin). MIG-033 view_as_target_user_id-Spalte bleibt in audit_log-Tabelle, wird vom V6.6-Code ignoriert. Per-Item Git-Revert moeglich (~13 V7-Commits + V7-Patches BL-465/467/470 atomic-revertable). User-bestehende Daten unangetastet.

### REL-028 — V6.6 Pre-V7-Audit-Sprint (KI-Workspace-Hybrid + Deals-Restruktur + KI-Cockpit + Win/Loss-Auto-Trigger + Kalender-Working-Hours)
- Date: 2026-05-11
- Image-Tag: `360c6ec` (Commit `360c6ec1a140ec8df6718cdf83dd2eaf093e4aa0`, SLC-667-feat enthaelt Hotfix `ab3ca54` SLC-666-CockpitKIWorkspace + alle V6.6-Code-Slices. HEAD `4b202dc` enthaelt zusaetzlich Docs-only Commits — Code-Stand identisch).
- Scope: V6.6 Pre-V7-Audit-Sprint, 7 Slices SLC-661..667, 6 Features FEAT-661..666. MIG-032 angewendet auf Hetzner-DB (additive Schema-Erweiterung: `auto_winloss_runs`-Tabelle + RLS, `user_settings.working_hours_start`/`_end`-Spalten, `automation_rules.is_system`-Flag). 16 V6.6-DECs (DEC-165..180). 1 System-Workflow-Rule `[SYSTEM] Auto Win/Loss Extract` active mit is_system=true. ISSUE-061 (Read-API Middleware-Wall) resolved via Commit `8379833`.
  - **SLC-661** KI-Workspace Foundation (FEAT-661): `KIWorkspace.tsx` als Cross-Page-Komponente + Wrapper-Pattern (Hotfix-Pattern IMP-434 = jeder Caller bekommt eigenen Wrapper mit statischem loadRunner-Switch fuer Bundle-Splitting). Audit-Trail-Event `ki_workspace_report` neu.
  - **SLC-662** Mein Tag (FEAT-661 + FEAT-662): `MeinTagKIWorkspace`-Wrapper mit 5 Berichts-Buttons (Tagesanalyse / Gestern / Seit Login / Wochen-Performance / Pipeline-Risiko). Legacy `mein-tag/ki-workspace.tsx` (591 LOC) entfernt. `/performance`-Seite faellt komplett (Server-Redirect auf `/mein-tag`). Sidebar-Reorder (ANALYSE → OPERATIV → ARBEITSBEREICHE → VERWALTUNG). Voice-Mic auf Tagesanalyse-Button.
  - **SLC-663** Deals-Listen-Restruktur (FEAT-663): `/deals` neu mit Type-Ahead-Input + Pipeline-Switcher + Top-10-Sektion + Won/Lost expandable `<details>`. SparklesCard cross-page entfernt.
  - **SLC-664** Deal-Detail Layout-Swap (FEAT-664): Action-Bar mit 7 Elementen (Task / E-Mail / Meeting / Anruf / Notiz / Angebot / Mehr). E-Mail-Button als `<a>` mit Compose-Prefill. `DealKIWorkspaceWrapper` mit 5 Berichts-Buttons (Briefing / Signale / Risiken / Naechster Schritt / Win/Loss). 4 Tabs (Timeline / Aufgaben / Angebote / Dokumente). Wissen-Tab + Edit-Tab + AI-Briefing-Panel + Toolbar-Button "Signale pruefen" entfernt. 3 obsolete Components geloescht.
  - **SLC-665** Activity-Sheet + Win/Loss-Auto-Trigger (FEAT-664 + FEAT-666): MIG-032 `auto_winloss_runs`-Tabelle mit Run-Tracking. Workflow-Rule dispatcher → executor → `auto_winloss_extract` → Bedrock + audit_log + auto_winloss_runs. Idempotenz-Throttle 5min via `auto_winloss_skipped_recent_run`. Read-API `/api/winloss/[dealId]` mit Bearer-Auth + Middleware-publicPaths-Fix (ISSUE-061). 3-fach Toggle + 2 echte Bedrock-Auto-Runs Live-verifiziert (RPT-379).
  - **SLC-666** /dashboard → KI-Analyse-Cockpit (FEAT-665): H1 "KI-Analyse-Cockpit". Quick-Action-Buttons (5: Aufgabe / E-Mail / Meeting / Anruf / Notiz). `CockpitKIWorkspace`-Wrapper mit 6 Berichts-Buttons (Pipeline-Snapshot / Top-Chancen / Conversion-Rate / Forecast / Win/Loss-Analyse / Stagnierende Deals). Alte KPI-Cards + Top-Chancen-Tabelle + DashboardSearch (Voice) entfernt. Hotfix `ab3ca54` direkt im Image enthalten.
  - **SLC-667** Kalender Working-Hours (FEAT-662): MIG-032 `user_settings.working_hours_start`/`_end`-Spalten. Settings-Page mit Save+Reset DB-Roundtrip. Toggle "Voller Tag" / "Working-Hours" mit localStorage-Persistenz (`cockpit:kalender:working-hours-toggle`). Hour-Labels 06:00-21:00 als Default. AI-Bereitschaft-Label statt KI-Reife.
- Summary: V6.6 ist der Pre-V7-Audit-Sprint, der das KI-Workspace-Hybrid-Pattern auf drei zentralen Pages (Mein Tag + Deal-Detail + Cockpit) konsolidiert, die Deals-Listen-Seite restrukturiert, das alte Dashboard zum KI-Analyse-Cockpit umbaut, Win/Loss-Auto-Trigger einfuehrt und die Kalender-Working-Hours konfigurierbar macht. Hygiene: SparklesCard cross-page entfernt, Sidebar-Reorder, /performance-Seite faellt, AI-Bereitschaft-Rename. Risk-Profil niedrig (1 additive Schema-Migration + 16 neue Bedrock-Pfade mit Cache-5min-TTL DEC-177 + Audit-Trail symmetrisch). Pre-Live-Burn-In: Container `Up 1h+` healthy auf Image `360c6ec` zum REL-028-Sign-Off-Zeitpunkt. Vitest 650/650 PASS lokal (+168 V6.6-Tests). ESLint 142e/54w (-24/-3 vs V5.7-Baseline 166/57). Audit-Trail letzte 24h: ai_signal_extract_run 286x matched RPT-342, V6.6-neu ki_workspace_report 3x + auto_winloss_triggered 2x + auto_winloss_skipped_recent_run 1x. Reports-Trail RPT-366..388 (Discovery/Req/Arch/Slice-Planning + 7x Slice-Implementation + 7x Slice-/qa + Gesamt-/qa RPT-386 + Final-Check RPT-387 + Go-Live RPT-388). Internal-Test-Mode bleibt aktiv bis Pre-Production-Compliance-Gate.
- Risks: **Bedrock-Cost-Spike durch 16 neue Reports** (Low) — Cache 5min TTL aktiv (DEC-177), Audit-Log gibt Trace, AWS Cost-Explorer post-Release beobachten. **Voice-Mic AC5 nur Click-Path verifiziert** (Low) — Audio-Stream in Headless-Browser nicht testbar, manueller Test in echtem Chrome vor Customer-Use empfohlen. **AC14+AC15 Cockpit-Reports (Win/Loss-Aggregate + Stagnierende-Deals) nicht live getriggert** (Low) — Pure-Function-Tests decken Logic, Live-Trigger erst moeglich bei Won/Lost/Idle-Test-Daten. **BL-460 Style-Guide-V2 Hex-Hardcodes in 4 V6.6-Files** (Low) — V6.7-Polish-Item, funktional identisch (Werte matchen Brand-Tokens). **BL-459 Quick-Action-Label "Task" vs "Aufgabe"** (Low) — V6.7-Polish-Item, Konsistenz-Drift. **BL-457 Mobile-Hamburger-Sidebar fehlt** (Low) — V7-Item, pre-existing pre-V6.6. **BL-458 Kalender Pre/Post-Stauchung im work-Mode** (Low) — V7-Item, Toggle-Workaround verfuegbar. **BL-418 React #418 Hydration auf Deal-Detail** (Low) — pre-existing seit SLC-554, 1 Console-Error ohne UI-Konsequenz. **ISSUE-058 postcss CVE** (accepted, Low) — Build-Time-only, wartet auf Next.js-Upstream-Patch. **ISSUE-042 OpenAI-Key** (accepted, High) — User-Pflicht-Action pre-Customer-Live, per User-Direktive 2026-05-01 "kommt viel spaeter". **supabase-studio unhealthy** (accepted) — 5 Wochen pre-existing, Admin-UI only.
- Rollback Notes: V6.6 ist 1 additive Schema-Migration (MIG-032 idempotent: `CREATE TABLE IF NOT EXISTS auto_winloss_runs` + `ALTER TABLE user_settings ADD COLUMN IF NOT EXISTS working_hours_start/_end` + `ALTER TABLE automation_rules ADD COLUMN IF NOT EXISTS is_system`). Rollback per Coolify-Image-History → vorheriger stable Tag REL-027 (`cb491ca`). Bei Rollback verschwindende V6.6-Effekte: KIWorkspace-Foundation in 3 Callern wird unreachable (alte Mein-Tag-KI-Workspace + Deal-AI-Briefing-Panel + alte Dashboard-KPI-Cards kommen zurueck), Deals-Listen faellt auf V6.5-Layout zurueck, Cockpit-Quick-Actions verschwinden, Win/Loss-Auto-Trigger wird inactive (System-Workflow-Rule bleibt in DB, hat aber keinen Effekt ohne V6.6-Executor-Code), Kalender faellt auf 24h-Layout ohne Working-Hours-Toggle zurueck, AI-Bereitschaft-Label heisst wieder KI-Reife. `auto_winloss_runs`-Tabelle bleibt isoliert in DB (kein DROP). `user_settings.working_hours_*` bleiben in DB, werden vom V6.5-Code ignoriert. `automation_rules.is_system`-Flag bleibt, ohne dass V6.5 ihn liest. SparklesCard kommt cross-page zurueck. /performance-Seite wird wieder erreichbar. Sidebar-Reihenfolge faellt zurueck. User-bestehende Daten (Deals, Activities, Settings) bleiben unveraendert. Per-Item Git-Revert moeglich (~13 V6.6-Code-Commits + Docs-Commits, atomic-revertable).

### REL-027 — V6.5 Hintergrund-Sprint (Theming + Compliance-Erweiterung + Hygiene)
- Date: 2026-05-08
- Image-Tag: `cb491ca` (Commit `cb491ca62acd47a0750fa71b8e56879b94213ec0`, Coolify-Redeploy aus SLC-657-Deploy 2026-05-07 — Image bereits 17h Live-Burn-In zum REL-027-Sign-Off-Zeitpunkt)
- Scope: V6.5 Hintergrund-Sprint parallel zu V7-Vorbereitung, 7 Slices SLC-651..657, 3 Features FEAT-651..653. MIG-030 vat_id_validations additiv idempotent applied (1 echter VIES-Cache-Eintrag aus Live-Smoke). MIG-031 ready-when-needed (by-design No-Op-Apply, 0 Legacy-Daten + 0 Kampagnen). 12 V6.5-DECs (DEC-152..163). 1 neue ISSUE-058 dokumentiert (postcss-CVE-Akzeptanz per DEC-161).
  - **SLC-651** Theming-Tokens-Setup (FEAT-651, BL-441 Phase A): 6 Brand-CSS-Vars im Tailwind-v4-`@theme inline`-Block + `.bg-gradient-primary` Utility. 8 Drift-Stellen in mein-tag-client.tsx als Beweis-Migration.
  - **SLC-652** Theming-Migration Tranche 1 (FEAT-651, BL-441 Phase B): Pipeline+Proposals-Bereich auf Brand-Tokens migriert. 27/30 Drift-Stellen ueber 14 Files. 2 Atomic Commits.
  - **SLC-653** Settings-Pages-Auslagern (FEAT-651, BL-436 UA-002): PipelineConfig + TemplatesConfig in eigene Pages `/settings/pipelines` + `/settings/templates`. 2 neue Link-Karten auf Settings-Landing.
  - **SLC-654** ViewToggle Generic + Pipeline-PageHeader-Slot (FEAT-651, BL-438 + BL-440): ViewToggle Generic-Refactor mit 4 Caller-Migration. PageHeader belowHeader-Prop additiv. +11 Vitest-Tests.
  - **SLC-655** VIES-Cache + Adapter (FEAT-652, BL-420): MIG-030 vat_id_validations live (8 Spalten + 3 Indexes + RLS). Pure-Function lookupVatId mit 4-Pfad-Logik. Server-Action + 7-State-Status-Badge in Branding + Company. +14 Vitest-Tests. Live-Smoke nachweisbar (1 echte VIES-Cache-Zeile).
  - **SLC-656** DE-§13b Reverse-Charge-Block (FEAT-652, BL-421): buildDeReverseChargeBlock Pure-Function mit DE-Pflichtformulierung (PRELIMINARY per DEC-162). Renderer-Branching DE/NL/none. Eligibility-Hook + Server-Validator generalisiert um businessCountry. +14 Vitest + 2 PDF-Snapshots. User-bestaetigt 2 Live-PDFs (NL+DE).
  - **SLC-657** Source-Migration + npm audit + ISSUE-058 (FEAT-653, BL-424 + BL-430): MIG-031 ready-when-needed (4 Files inkl. Mapping-Template), No-Op-Apply by-design. contact-form + company-form Legacy-Felder read-only-Display. CSV-Export campaign_name statt source_detail. ISSUE-058 dokumentiert.
- Summary: V6.5 ist gebuendelter Hintergrund-Sprint mit 3 Themen-Achsen (Theming, Compliance-Erweiterung NL→DE-Symmetrie, Hygiene). Risk-Profil niedrig (1 additive Schema-Migration + 1 No-Op-Migration, kein Auth/Session-Bruch, kein Bedrock-/Whisper-Refactor). Pre-Live-Burn-In 17h auf Image cb491ca ohne Container-Restart. Vitest 444/444 (+39 V6.5-Tests). Lint 168/55 = V5.7-Baseline +2 Errors (SLC-655, by-design). npm audit 0 high/0 critical. 16 Reports-Trail RPT-345..360. Gesamt-/qa V6.5 PASS RPT-361 (58/58 ACs, 54 voll + 4 partial-by-design). Final-Check CONDITIONALLY READY RPT-362. Go-Live GO RPT-363. Internal-Test-Mode bleibt aktiv bis zukuenftiger Compliance-Sprint.
- Risks: **SLC-655 audit_log-Wiring-Gap fuer VIES-Lookups** (Bundle-Finding RPT-361, Medium) — Spec AC-3+AC-4 fordern audit_log-Insert pro Cache-Miss; Implementation hat nur DB-Cache-Upsert. DSGVO-Trail via vat_id_validations faktisch vorhanden. Pre-Production-Compliance-Gate-Item, nicht Internal-Test-Mode-blocking. **DE-§13b PRELIMINARY-Phrase** (DEC-162) — Anwaltspruefung deferred per User-Direktive. **ISSUE-058 postcss + next CVE** — DEC-161 Force-Fix-Catastrophic-Akzeptanz. **Lint-Drift +2 Errors aus SLC-655** by-design. **VIES-Cache-Cleanup-Cron fehlt** — bei Skalierung sinnvoll. **@vitest/coverage-v8 nicht installiert** — bei naechstem npm-Slice. **Coolify-Cron `click-log-cleanup` (V6.4 BL-423)** — V6.5-erbendes Item. **`supabase-studio` UNHEALTHY** — V5.x-bekannt-kosmetisch. **Pre-Production-Compliance-Gate-Items** als zukuenftiger Compliance-Sprint geplant.
- Rollback Notes: V6.5 ist 1 additive Schema-Migration (MIG-030 idempotent CREATE-IF-NOT-EXISTS) + 1 by-design No-Op-Migration (MIG-031). Rollback per Coolify-Image-History zu REL-026 (`f99726b`). Bei Rollback verschwindende V6.5-Effekte: Theming-Tokens-Refactor in 15 Files (Visual-Regression auf Pipeline + Proposals, kein Funktions-Bruch), 2 neue Settings-Pages → 404, ViewToggle-Generic-Refactor (4 Caller zurueck auf 2-Modi-Inline), VIES-Adapter-Code unbenutzbar (vat_id_validations-Tabelle bleibt isoliert in DB ohne Code-Anbindung — kein DROP-Risk), Status-Badge in Branding+Company-Forms verschwindet, DE-§13b-Block nicht renderbar (NL-Block bleibt aktiv), Source-Migration-Forms zeigen wieder Active-Inputs (Backup-Felder unangetastet per DEC-160), CSV-Export-Spalte zurueck auf source_detail. User-bestehende Daten bleiben unveraendert. Per-Item Git-Revert moeglich (~16 V6.5-Commits, atomic-revertable).

### REL-026 — V6.4 Hygiene-Sprint (System-Stabilitaet + Code-Audit + UI-Audit)
- Date: 2026-05-07
- Image-Tag: `f99726b` (Commit `f99726b2033f1a2fb4263df7584210d4ef003cde`, Coolify-Redeploy 10:41 UTC)
- Scope: V6.4 Hygiene-Sprint, 5 Slices SLC-641..645, 3 Features FEAT-641..643. Keine Schema-Migration (DEC-148: Schema-Audit auf V6.5 deferred), keine neue UI-Logik, keine Auth/Session-Aenderung. Rein additiv + cleanup.
  - **SLC-641** System-Stabilitaet & DSGVO (FEAT-641): ISSUE-057 FollowupEngine `proposals.value -> total_gross` Fix (3 Stellen, Spec sagte 2). BL-423 DSGVO 90-Tage-Retention `/api/cron/click-log-cleanup` neu (Pure-Function `runClickLogCleanup` + verifyCronSecret-Auth + audit_log-Trail mit run_id-as-entity_id). Vitest +12 (3 followup-engine + 9 click-log-cleanup). Coolify-Cron-Setup-Snippet in RPT-335 (User-Action offen).
  - **SLC-642** Code-Audit Inventur (FEAT-642): RPT-336 mit 18 klassifizierten Items (CA-001..CA-018) ueber 5 Hot-Spots (Cron-Jobs + AI-Engines + Source-Schema + Tote Server-Actions + Tote API-Routes). User-Sign-Off-Pause via Pfad-A.
  - **SLC-643** Code-Cleanup (FEAT-642): 6 Items abgeraeumt (~728 Zeilen toter Code) — CA-001 backfill-Cron + lib geloescht, CA-002 Cal.com-Sync-Cron geloescht (V4-Era), CA-008 audit_log-Insert in FollowupEngine + Signal-Extract (DSGVO-Trail-Symmetrie), CA-015/016/017 tote Server-Actions geloescht (getForecastValue, getReferralsByReferrer, getUnassignedEmailCount). 12 Items als V6.5/V7-Defer dokumentiert. 7 atomare Commits.
  - **SLC-644** UI-Audit Inventur (FEAT-643): RPT-338 mit 13 klassifizierten Items (UA-001..UA-013) ueber 5 UI-Bereiche (Settings-Landing + Sidebar + Button-Konsistenz + Pipeline-Stages + Page-Header) + Style-Guide-V2-Drift-Liste (178 Drift-Stellen ueber 30 Files). Pfad-A signed off durch User.
  - **SLC-645** UI-Cleanup (FEAT-643): 5 Klar-Items umgesetzt — UA-010 Pipeline h1 text-2xl→text-3xl, UA-003 Einwilligungstexte bg-emerald-50→bg-rose-50 (Farbkollision aufgeloest), UA-005 Sidebar `Termine`→`Termine-Liste`, UA-006 Primary-CTA Green→Blue Gradient (Pipeline + Proposals; Style-Guide V2 verbindlich), UA-001 Settings-Landing auf PageHeader-Component (Layout-Refactor). 8 Items deferred V6.5 (UA-002/004/007/008/009 + UA-011/012/013 Theming-Sprint via BL-441). 5 atomare Commits.
- Summary: V6.4 ist erstes seriell durchgezogenes /qa→/final-check→/go-live→/deploy-Buendel mit 5 Slices in einer Session-Sequenz. Risk-Profil niedrig (rein additiv + cleanup, keine Schema-Migration). Live-Stack auf Coolify seit 10:41 UTC healthy. Audit-Log-Trail aktiv: 3 V6.4-Patterns produzieren Daten in DB (ai_followup_run + ai_signal_extract_run + click_log_cleanup). Container-Logs 60m: 0 `proposals.value`-Errors (ISSUE-057 verifiziert resolved). 7 V6.4-DECs in DECISIONS.md (DEC-145..151). 7 Reports-Trail (RPT-335..341 + RPT-342 Go-Live + RPT-343 Deploy). Vitest 405/405 PASS (12 neue aus V6.4). Internal-Test-Mode bleibt aktiv bis Pre-Production-Compliance-Gate (User-Direktive 2026-05-01 "kommt viel spaeter").
- Risks: **Coolify-Cron `click-log-cleanup` noch nicht angelegt** — Endpoint live, aber automatisches Triggering wartet auf User-UI-Setup. Snippet in RPT-335. Frueheste Wirkung 2026-08-04 (90d nach V6.2-Deploy), nicht zeitkritisch. **5 moderate npm-Vulnerabilities (BL-430 V6.5-Defer)** — postcss-Patch wuerde Next 9.3.3-Downgrade erfordern (catastrophic, NICHT durchfuehren). hono + ip-address haben sicheren autofix-Path. **Visuelle 5-Page-User-Smoke ausstehend** — Code-Side gruen, AC-4-Screenshots durch Code-Diff-Audit ersetzt (M1 RPT-339). **`supabase-studio` Container UNHEALTHY** — Admin-UI-Container, kein Request-Pfad-Impact, V5.x-bekannt-kosmetisch. **Pre-Production-Compliance-Gate offen** (M2 RPT-341) — ISSUE-042 OpenAI-Key + Switch auf Azure-EU-Whisper, per User-Direktive deferred.
- Rollback Notes: V6.4 ist rein additiv (keine Schema-Migration, keine ENV-Aenderung, keine neue Service). Rollback per Coolify-Image-History → vorheriger stable Tag REL-025 (`f7fd231`). Bei Rollback: V6.4-Cleanup-Aenderungen verschwinden (Pipeline h1 wieder text-2xl, Pipeline+Proposals-Buttons wieder gruen, Sidebar wieder "Termine", Einwilligungstexte wieder bg-emerald-50, Settings ohne PageHeader, geloeschte Server-Actions kommen zurueck, Cal.com-Sync-Cron + backfill-Cron kommen zurueck). audit_log-Coverage faellt auf V6.3-Stand (FollowupEngine + Signal-Extract verlieren ai_*_run-Eintraege). Click-Log-Cleanup-Endpoint verschwindet (404). User-bestehende Daten bleiben unveraendert. Per-Item Git-Revert moeglich: 11 atomare V6.4-Commits (b8fe654, 27e19f6, d82927b, a4f399b, 45906ed fuer UI; 3be37e4, 7c38eb4, aede22b, b28a610, ca43fe7, 9b7e5e2 fuer Code).

### REL-025 — V6.3 Polish-Bündel (BL-426 + BL-422 + ISSUE-050 + Backlog-Hygiene)
- Date: 2026-05-06
- Image-Tag: `f7fd231` (Commit `f7fd2310e83bc36beb84f928f4d97f89430d82c7`, Coolify-Redeploy 14:17 UTC)
- Scope: V6.3 Polish-Sprint, 1 Slice mit 7 Micro-Tasks. Keine Schema-Aenderungen, keine neuen Routes, keine Architektur-Pivots.
  - **MT-1** rule-builder.tsx useState lazy-init mit `typeof window`-Guard (lint react-hooks/set-state-in-effect Fix)
  - **MT-2** Sidebar-Eintrag "Workflow-Automation" mit Zap-Icon in `settings/layout.tsx` (BL-426 Sub-Item 2)
  - **MT-3** Page-Header-Refactor `/settings/payment-terms` — neuer `PaymentTermsPageContent`-Wrapper, Adjust-State-During-Render-Pattern statt useEffect, Inline-Button entfernt, createNonce-Counter-Re-Open (BL-426 Sub-Item 3)
  - **MT-4** `cockpit/src/lib/proposal/reverse-charge-revert.ts` Pure-Function-Lib + 20 Vitest (analog SLC-572 Skonto-Pattern) + Editor-Wiring `lastKnownGoodReverseChargeRef` + `revertPatchIfReverseChargeFailed` (BL-422 Defense-in-Depth gegen UI-State-Drift bei Server-Reject)
  - **MT-5** `cockpit/src/lib/audit/format.ts` Pure-Function `formatAuditChanges` + 12 Vitest + audit-log-client.tsx Wire-Up — heuristisches Detect doppelt-verschachtelt vs. flat (ISSUE-050 Audit-Log `[object Object]`-Render-Bug eliminiert)
  - **MT-6** `trigger-sources.ts` notes-Felder + SLC-622-Spec AC12 Wortlaut praeziser (V1-Reduktion: 4 von ~12 Pfaden dispatchen, dokumentiert)
  - **MT-7** Backlog-Hygiene: `npm audit fix` non-breaking 9→6 (1 high + 2 moderate resolved), BL-Dedup BL-427/428/429 als Duplicate-of-BL-423/424/425 markiert, neuer BL-430 fuer Breaking-Change-Path `npm audit fix --force` (V6.4-Defer)
- Summary: Polish-Sprint mit 5 V6.2-/qa-Findings + 1 V5.6-Carryover (ISSUE-050) + Backlog-Hygiene gebuendelt in einer Session. Bug-Klasse: alle UX-Polish-Items, kein Funktions-Bruch. Live-Image deployed seit 14:17 UTC. Internal-Test-Mode bleibt aktiv bis Pre-Production-Compliance-Gate (User-Direktive 2026-05-01 "kommt viel spaeter"). RPT-326 (Implementation), RPT-327 (/qa Live-Smoke), RPT-328 (/final-check). Vitest 393/393 PASS (32 neu: 20 reverse-charge-revert + 12 audit/format).
- Risks: **Test-Artefakte im Audit-Log** — `qa-test`-Title-Edits + 3 RC-Toggle-Cycles auf Proposal `0c1f7e10-...` durch /qa-Smokes; History-Rauschen, kein Daten-Verlust. **MT-4 UI-Toast-Wortlaut Live-Smoke nicht direkt gemessen** (Browser-Crash beim kontinuierlichen JS-Sampling); DB-Audit-Log + 20 Vitest-Cases beweisen Defense-in-Depth-Reject. **6 verbleibende `npm audit` moderate Vulnerabilities** (BL-430 V6.4-Defer, alle Build-/Dev-Toolchain ohne produktive Exposure). **`useReverseChargeEligibility`-Hook nicht reaktiv auf live Branding-Updates** — bei mid-session Branding-vat_id-Aenderung kann Toggle kurz enabled bleiben (Race-Path); Defense-in-Depth fängt das ab (DB-Beweis aus /qa).
- Rollback Notes: V6.3 ist rein additiv (4 neue Files + 8 modifizierte Files, kein Schema-Change). Rollback per Coolify-Image-History → vorheriger stable Tag REL-024 (`766e4ac`). User-bestehende Daten bleiben unveraendert. Bei Rollback verschwinden V6.3-Polish-Effekte: Sidebar-Workflow-Eintrag, Page-Header-Button payment-terms, RC-Drift-Fix, Audit-Log-Render-Fix. Vorhandene `qa-test`-Audit-Eintraege bleiben (DB unveraendert). Keine Cron-Aenderung, keine ENV-Aenderung, keine MIG noetig.

### REL-024 — V6.2 Workflow-Automation + Kampagnen-Attribution (Foundation + Engine + UI + Tracking)
- Date: 2026-05-06
- Image-Tag: `766e4ac` (V6.2-Hotfix: ISSUE-055 Workflow-Wizard UUID-Display + ISSUE-056 Kampagnen-Settings-Karte)
- Scope: V6.2 Final-Release. Alle 5 Slices abgeschlossen.
  - **SLC-621** Workflow-Foundation (MIG-029 Phase 1, `automation_rules` + `automation_runs` + Anti-Loop-UNIQUE + 3 Indizes + RLS + GRANTS, Trigger-Source-Audit-Liste, Condition-Engine 9 Operators AND-only, Field-Whitelist mit PII-Schutz, Trigger-Dispatcher, 5 Server Actions Rule-CRUD)
  - **SLC-622** Workflow-Engine (Action-Executor + Run-Lock + TOCTOU-Re-Eval + Recursion-Guard, 4 Action-Types `create_task`/`send_email_template`/`create_activity`/`update_field`, assignee-resolver, template-renderer, Cron-Endpoint `/api/cron/automation-runner` als Defense-in-Depth-Fallback, Sync-Execution-Path im Dispatcher, Trigger-Verdrahtung in 4 zentralen User-Pfaden, Stage-Delete-Soft-Disable)
  - **SLC-623** Builder-UI (Settings/Automation-Listing + Wizard mit 6 Schritten + Confirm-Dialog + Style-Guide-V2)
  - **SLC-624** Campaigns-Foundation (MIG-029 Phase 2, `campaigns` + 3 ALTER `campaign_id` UUID NULL ON DELETE SET NULL auf contacts/companies/deals + 2 UNIQUE-Indizes + 1 Partial-Index + Date-Range-CHECK + RLS + GRANTS, 5 Server Actions Campaign-CRUD, Listing/Editor/Detail mit 5 KPI-Cards + 3 Tabs, CampaignPicker in 3 Stammdaten-Forms inkl. Auto-Vorbelegung im Deal-Form via getContactCampaignId)
  - **SLC-625** Tracking + Reporting + Read-API (MIG-029 Phase 3, `campaign_links` + `campaign_link_clicks` mit FK CASCADE + Indizes + RLS + GRANTS + target_url-CHECK, Token-Generator base64url 8-char DEC-137, IP-Hash SHA-256+ENV-Salt DSGVO-konform, UTM→Campaign-Mapper hybrid DEC-135, appendUtmIfMissing Pure-Function, 4 Tracking-Link Server Actions + Public Redirect `/r/[token]` mit async Click-Log + click_count-Increment, Tracking-Links-Tab UI mit Modal + Copy-Button + Sparkline-Chart, Lead-Intake POST `/api/leads/intake` mit Bearer-Auth + First-Touch-Lock COALESCE DEC-138 + audit_log-Insert, Read-API GET `/api/campaigns/[id]/performance` mit Bearer-Auth + 11-Felder-JSON DEC-140, Funnel-Filter Campaign-Dropdown im /pipeline-Filter-Bar mit URL-Param-Persistenz DEC-139, CSV-Export Leads + Deals via `/api/campaigns/[id]/export?type=`, 25 Schema-Smoke-Tests + 20 Pure-Function-Tests)
- Summary: V6.2 ist die erste Workflow-Automation + erste End-to-End-Attribution in einem Strategaize-System. Workflow-Engine erlaubt Stage-getriggerten Auto-Tasks/-E-Mails/-Activities. Attribution-Block trackt Leads via UTM-Mapping ueber Public-Redirects mit DSGVO-konformem Click-Log. Externe Quellen (System 4) koennen via Bearer-Auth Leads pushen + KPIs pollen. Funnel-Report kann auf einzelne Kampagnen scoped werden. Internal-Test-Mode bleibt aktiv bis Pre-Production-Compliance-Gate.
- Risks: **Coolify-Cron muss manuell angelegt werden** — sonst Cron-Fallback fuer automation-runner inaktiv (Sync-Pfad funktioniert weiter). **IP_HASH_SALT-ENV** ist im V1 Default-Constant — Pre-Production muss eigenen Salt setzen (BL Polish-Item). **Action-Idempotenz** — `create_task`/`create_activity` sind nicht idempotent (Cron-Re-Run nach App-Crash kann Duplikate erzeugen). **Click-Log-Skalierung** — V1 toleriert <1000 Clicks/Tag; bei viralen Links 90-Tage-Cleanup-Cron als BL V6.3+. **click_count-Race** — read-modify-write bei concurrent clicks fuehrt zu eventual-consistency, kein Constraint-Verlust (V1 single-user akzeptabel). **First-Touch-Lock** — bestehende Contacts behalten ihre erste Attribution; UTM-Spam aendert nichts. **Public Redirect** ohne Rate-Limit — 8-char Token mit ~2.8e14 Combos toleriert kein Brute-Force; Edge-Rate-Limit als BL V6.3+ wenn realer Traffic-Verdacht. **Trigger-Source-Coverage**: Cron-Routes nicht verdrahtet, system-getriggerte Activities loesen keine Workflows aus.
- Rollback Notes: MIG-029 alle 3 Phasen sind additiv (CREATE TABLE IF NOT EXISTS + ADD COLUMN IF NOT EXISTS + Indizes + RLS + GRANTS). Rollback per Coolify-Image-History → vorheriger stable Tag REL-023 (`908eb81479e09df28cd96b85011462140880d208`). Schema bleibt drin, Code-Layer faellt auf V5.7-Verhalten zurueck (kein Workflow-Dispatch, kein Campaign-Picker, keine Tracking-Routes). User-bestehende Daten bleiben unveraendert. Wenn Workflows aktiv sind: vor Rollback alle `automation_rules` per UPDATE auf `status='disabled'` setzen. Tracking-Links und Click-Log bleiben in DB; `/r/<token>`-Routes liefern nach Rollback 404 (Route weg) — User-impact akzeptabel weil V1 Internal-Test-Mode.

#### Coolify-Cron-Setup nach Deploy (PFLICHT)

Nach erfolgreichem Coolify-Deploy von V6.2 muss in Coolify ein neuer Cron-Eintrag manuell angelegt werden, damit der Cron-Fallback aktiv wird:

| Feld | Wert |
|------|------|
| **Container** | `app` |
| **Schedule** | `* * * * *` (jede Minute) |
| **Command** | `node -e "fetch('http://localhost:3000/api/cron/automation-runner', {method:'POST', headers:{'x-cron-secret': process.env.CRON_SECRET}}).then(r=>r.text()).then(console.log)"` |

ENV `CRON_SECRET` ist bereits gesetzt (existing aus REL-018). Kein neues Secret noetig.

**Smoke-Test nach Anlage** (von Hetzner-Server-Konsole):
```bash
docker exec -it $(docker ps --format "{{.Names}}" | grep "^app-") node -e \
  "fetch('http://localhost:3000/api/cron/automation-runner', {method:'POST', headers:{'x-cron-secret': process.env.CRON_SECRET}}).then(r=>r.json()).then(console.log)"
```
Erwartete Antwort: `{ success: true, picked: 0, processed: 0, failed: 0 }` (bei leerer Queue).

Wenn `Unauthorized` returnt: CRON_SECRET-Header stimmt nicht — Coolify-ENV pruefen.

**Defense-in-Depth-Hinweis**: Auch ohne Cron funktioniert die Workflow-Engine im Happy-Path. Der Cron picked nur stuck-Runs (>60s pending/running) auf — relevant nur bei App-Crash zwischen Dispatch-Insert und Sync-Execution.

### REL-023 — V5.7 NL+DE-VAT-Saetze + Reverse-Charge + Skonto-Toggle Bugfix
- Date: 2026-05-05
- Scope: V5.7 Final-Release. 2 Slices: **SLC-571** NL+DE-VAT-Saetze + Reverse-Charge fuer EU-B2B-Cross-Border (9 MTs, FEAT-571, BL-417): MIG-028 (5 additive Aenderungen idempotent), VAT-ID Format-Validation-Layer (vat-id.ts, 30 Vitest), Branding-Settings Country-Dropdown DE/NL + vat_id-Feld, Company-Stammdaten vat_id, useReverseChargeEligibility-Hook + countryNameToCode-Mapper (24 Vitest), Editor-Steuersatz-Dropdown country-aware {0,7,9,19,21}, Reverse-Charge-Section + saveProposal Server-Action-Validation + Audit-Insert (`reverse_charge_toggled`-Event, validateReverseCharge mit 4 Reject-Pfaden, 14 Vitest), PDF-Renderer reverse-charge-block.ts + bilingualer Block (8 Vitest inkl. 3 Snapshots), COMPLIANCE.md V5.7-Section, DEC-122..128. **SLC-572** Skonto-Toggle UI-State-Drift Bugfix (4 MTs + 3 Follow-up-Fixes, FEAT-572, BL-419): lastKnownGoodSkontoRef + revertPatchIfSkontoFailed Pure-Function (skonto-revert.ts, 16 Vitest inkl. RPT-277-Repro), 3 Follow-up-Hot-Fixes nach Live-Smoke 2026-05-05 — ISSUE-051 (isOn-Inferenz auf percent OR days, Inputs bleiben gemountet), ISSUE-052 (PaymentTerms-Dropdown UUID-Display via render-callback), ISSUE-053 (Validation-Gate vor Server-Save verhindert Edit-Flicker). **Image-Tag:** `908eb81479e09df28cd96b85011462140880d208`. **Dependencies:** keine neuen NPM-Pakete. **Schema-Migration:** MIG-028 idempotent, additive, bereits live auf Hetzner.
- Summary: V5.7 macht das Cockpit NL-rechtskonform (Strategaize Transition GmbH sitzt in NL). VAT-Saetze in der NL-Logik 21/9/0% (statt deutsche 19/7/0); Reverse-Charge-Toggle fuer EU-B2B-Cross-Border (B2B-Empfaenger in EU != NL bekommen 0% mit Pflicht-Hinweis "BTW verlegd / Reverse Charge — Article 196 VAT Directive 2006/112/EC"); BTW-IDs fuer Strategaize + Empfaenger-Companies; PDF-Renderer-Block bilingual (DE-Hauptsprache + NL-Klammertext) direkt unter Tax-Row + Strategaize-BTW-Footer kontextrichtig. Plus Skonto-Toggle-Bugfix (BL-419/ISSUE-049) mit 3 Follow-up-Fixes nach iterativen Live-Smoke-Runden 2026-05-05. Internal-Test-Mode bleibt aktiv bis Pre-Production-Compliance-Gate (laut User 2026-05-01 "kommt viel spaeter").
- Risks: **DE-Reverse-Charge § 13b UStG** nicht implementiert (NL-First, DE-Mode hat Toggle disabled mit Tooltip-Hinweis) — BL-421 deferred. **VIES-Online-Lookup** fehlt — VAT-IDs nur Format-validiert per DEC-124 (BL-420 deferred, Format-only reicht fuer Internal-Test-Mode). **BL-422 RC-Toggle-UI-Drift** analog ISSUE-049 fuer Skonto — Wiring-Trace-Observation, low/cosmetic, selbstheilend nach Reload, deferred. **ISSUE-050 Audit-Log-UI-Renderer** rendert generic-update-Eintraege als `[object Object]` — pre-existing seit V5.6, NICHT V5.7-eingefuehrt, DB-Daten korrekt + SQL-Workaround verfuegbar. **supabase-studio-Container unhealthy** — pre-existing, admin-only, nicht app-kritisch.
- Rollback Notes: Rein additiv. MIG-028 sind 5 idempotente ALTER-Statements (`ADD COLUMN IF NOT EXISTS` + neue CHECK-Constraints) — kein DROP, kein ALTER von bestehenden Spalten. V5.7-Code ist abwaerts-kompatibel mit V5.6-DB-Schema (alte Spalten bleiben, neue sind nullable mit defensiven Defaults). Rollback per Coolify-Image-History → vorheriger stable Tag REL-022 (`a7b787d`). Kein Schema-Rollback noetig (additive Spalten bleiben unproblematisch in alter Code-Welt). User-bestehende Proposals bleiben unveraendert (V5.7 ist opt-in via Editor-Toggle).

### REL-022 — V5.6 Zahlungsbedingungen + Pre-Call Briefing
- Date: 2026-05-03
- Scope: V5.6 Final-Release. 4 Slices: **SLC-561** Payment-Terms-Foundation (`payment_terms_templates`-Tabelle + Settings-Sidebar-Nav, Default `30 Tage netto`); **SLC-562** Bedingungs-Dropdown + Skonto im Proposal-Editor (`proposals.skonto_percent` + `skonto_days`, beide-oder-keiner CHECK, Skonto-Mutex); **SLC-563** Split-Plan + PDF-Renderer (`proposal_payment_milestones`-Tabelle, Sum-Validation 100% strict, dnd-kit-Reihenfolge, days_after_signature); **SLC-564** Pre-Call-Briefing-Cron + `/settings/briefing` + Push/Email-Delivery + ActivityBriefingCard (`meetings.briefing_generated_at` + Partial-Index, `user_settings.briefing_trigger_minutes` + 2 Toggles, Activity-Persistierung, Sentinel-Strategy max 3 Retries). MIG-027 Schema. DEC-115..121. **NEUE INFRASTRUKTUR-AKTION (SLC-564):** Coolify-Cron `meeting-briefing` muss vor erstem Live-Test angelegt werden (sonst keine automatischen Briefings). Anleitung unten. **Keine neuen Dependencies.** Reuse von FEAT-301 (Deal-Briefing-Prompt+Validator), FEAT-409 (Browser-Push), V5.3 (`send.ts`-SMTP-Pipeline).
- Summary: SLC-561..563 sind bereits live im Cockpit (siehe STATE.md V5.6-Stand 2026-05-02). SLC-564 schliesst V5.6 ab: Vor jedem Meeting mit Deal-Zuordnung wird im konfigurierten Trigger-Fenster (Default 30 Min) ein KI-Briefing generiert und ueber Push (Service-Worker + VAPID) + E-Mail (kompakte HTML mit 5 Sections) zugestellt. Idempotenz via UPDATE-WHERE-NULL Winner-Takes-All. Sentinel-Strategy: max 3 Retries pro Meeting, danach permanent als `briefing_error` markiert. Briefings persistieren als `activities(type='briefing')` mit JSON-stringified Payload und werden in der Deal-Workspace-Timeline ueber neue `<ActivityBriefingCard>` (Expandable: Summary + Top-3 keyFacts/openRisks/suggestedNextSteps + Confidence-Badge) gerendert. Settings-Page `/settings/briefing` mit Trigger-Radio (15/30/45/60) + 2 Toggle-Switches + Push-Subscribe-Hint bei aktivem Push-Toggle ohne Subscription. Internal-Test-Mode bleibt aktiv bis Pre-Production-Compliance-Gate.
- Risks: Bedrock-Latenz bei 5+ parallelen Meetings im Tick: sequentieller Loop, naechster Tick holt Rest auf (Idempotenz). Push-Subscription Expired (HTTP 410): web-push setzt `push_subscription = NULL`, kein Throw, Cron-Lauf unbeeintraechtigt. SMTP-Failure: try/catch, log-only, Activity bleibt persistiert. LLM-JSON-Drift: `validateDealBriefing` prueft Schema, Sentinel greift nach 3 Failures. **MIG-027** ist additive Schema-Erweiterung (5 ALTER TABLE / CREATE TABLE), idempotent via `IF NOT EXISTS`. **Neue Dependency:** keine. **CRON_SECRET** ist seit V5.1 retention-Cron im app-Container gesetzt — kein zusaetzliches Setup. **Briefing ohne Deal:** Cron filtert via Partial-Index (`deal_id IS NOT NULL`), keine Bedrock-Calls fuer interne Termine. **Beide-Channels-off:** Cron skippt komplett (`{ok: true, skipped: 'all-channels-off'}`), kein Bedrock-Call, kein Activity-Insert. **F1 Hydration #418** auf `/proposals` Carryover aus V5.5.1 (UI funktional, kein V5.6-Blocker).
- Rollback Notes: Rein additiv. SLC-564 fuegt nur Cron-Endpoint `/api/cron/meeting-briefing`, Server-Helpers (`deal-context-loader`, `briefing-html`-Template), Settings-Page + Form, ActivityBriefingCard + Timeline-Switch-Case, Type-Definitionen + Konstante (MAX_BRIEFING_RETRIES=3) hinzu. Rollback per Coolify-Redeploy auf V5.6-pre-SLC-564-Commit (vor diesem Patch). Kein Schema-Rollback (MIG-027 ist Teil von SLC-561, bleibt). Coolify-Cron `meeting-briefing` separat deaktivieren ueber Coolify-UI falls noetig. Bestehende V5.6-Daten (Briefing-Activities, Settings-Eintraege, Marker-Werte in `meetings.briefing_generated_at`) bleiben unberuehrt.

#### Coolify-Cron `meeting-briefing` Setup (User-Aktion vor Final-Deploy)

**Wann:** Nach Coolify-Redeploy von V5.6 final (oder fruehestens nach Deploy von SLC-564).

**Voraussetzung:** ENV-Variable `CRON_SECRET` ist im Coolify-App-Container bereits gesetzt (existiert seit V5.1 retention-Cron, kein zusaetzliches Setup).

**Schritt 1 — Cron-Job in Coolify-UI anlegen:**

| Feld | Wert |
|------|------|
| Name | `meeting-briefing` |
| Schedule | `*/5 * * * *` (alle 5 Min) |
| Container | `app` |
| Command | `node -e 'fetch("https://business.strategaizetransition.com/api/cron/meeting-briefing", { method: "POST", headers: { "x-cron-secret": process.env.CRON_SECRET } }).then(r => r.text()).then(console.log)'` |

**Sicherheits-Hinweise:**
- `CRON_SECRET` IMMER ueber `process.env.CRON_SECRET` referenzieren — niemals als Klartext im Command (siehe REL-019 Coolify-Cron-Cleanup).
- Nur EIN Cron-Job mit diesem Namen anlegen — Duplikate erzeugen Doppel-Audit-Eintraege bei Doppellaeufen (Idempotenz schuetzt nur vor Activity-Doubling, nicht vor Audit-Doubling).
- Auth-Header ist `x-cron-secret`, NICHT `Authorization: Bearer` (Pattern aus `verify-cron-secret.ts`).

**Schritt 2 — Smoke-Test direkt nach Anlage (vor erstem 5-Min-Tick):**

```bash
ssh root@91.98.20.191
# Mit valid Secret + beide Toggles default on → entweder processedCount=0 (kein Meeting im Fenster) oder >=1 mit Audit-Eintraegen
curl -sX POST -H "x-cron-secret: $CRON_SECRET" \
  https://business.strategaizetransition.com/api/cron/meeting-briefing
# Erwartet: {"success":true,"processedCount":0,"skippedCount":0,"failedCount":0,"retryCount":0}

# Mit falschem Header → 401
curl -sX POST -H "x-cron-secret: wrong" \
  https://business.strategaizetransition.com/api/cron/meeting-briefing
# Erwartet: {"error":"Unauthorized"} mit HTTP 401

# Mit beiden Toggles off (UPDATE user_settings SET briefing_push_enabled=false, briefing_email_enabled=false) → skipped
curl -sX POST -H "x-cron-secret: $CRON_SECRET" \
  https://business.strategaizetransition.com/api/cron/meeting-briefing
# Erwartet: {"success":true,"skipped":"all-channels-off"}
```

**Schritt 3 — End-to-End-Smoke (im Coolify-UI nach Anlage des Cron):**

1. Test-Meeting in `triggerMinutes`+~3 Min anlegen (z.B. bei Default 30 Min: scheduled_at = now() + 32 Min) mit Deal-Zuordnung.
2. `briefing_push_enabled=true` UND `push_subscription` muss gesetzt sein (Settings-Page `/settings/briefing` → "Browser-Push aktivieren").
3. Innerhalb 5-10 Min sollten erscheinen:
   - Push-Notification im Browser (Service Worker)
   - E-Mail im Empfangs-Postfach (Sender-Domain mit SPF/DKIM, kein Spam-Folder)
   - `activities`-Row mit `type='briefing'` und JSON-Payload
   - `briefing_generated_at` auf dem Meeting gesetzt
   - Audit-Eintrag mit `action='ai_briefing_generated'`
4. Klick auf Push → `/deals/<dealId>` Workspace mit `<ActivityBriefingCard>` in Timeline.
5. Klick auf Email-Button "Deal-Workspace oeffnen" → gleicher Pfad.

**Idempotenz-Garantie:** Mehrfachlaufe innerhalb desselben 5-Min-Ticks erzeugen keine Doppel-Briefings. UPDATE-WHERE-NULL Pattern: erste Query setzt Marker, alle Folgequeries bekommen 0 rows (`processedCount=0, skippedCount=N`). Bei Bedrock-Failure: Marker wird auf NULL zurueckgesetzt, naechster Tick versucht erneut (max 3 Retries, danach permanent `briefing_error`).

**Failure-Verifikation (optional, im Hetzner-Test):**

```sql
-- Nach manueller Bedrock-Disable (z.B. AWS-Credentials kurzzeitig falsch setzen) → mehrfache Cron-Runs:
SELECT id, type, title, created_at FROM activities
WHERE type = 'briefing_error' AND deal_id = '<test-deal-id>'
ORDER BY created_at DESC;
-- Erwartet: 3 Rows mit "(1/3)", "(2/3)", "(3/3)"
SELECT briefing_generated_at FROM meetings WHERE id = '<test-meeting-id>';
-- Erwartet: gesetzt (nicht NULL) — Sentinel hat ihn nach 3. Failure NICHT zurueckgesetzt
```

### REL-021 — V5.5.1 Polish-Patch (Hydration + 3 V5.4-Carryover)
- Date: 2026-05-01
- Scope: 4 kleine Items in 2 Commits. (1) ISSUE-047 Hydration #418 Mitigation via `suppressHydrationWarning` auf `<html>`+`<body>` im Root-Layout (Code-Audit ergab: keine Date-Format-Drift im Render-Pfad, wahrscheinliche Quelle Browser-Extension). (2) SLC-541 M1 ConditionalColorPicker Refactor zu pure derived state (useState/useEffect entfernt). (3) ISSUE-045 Server-side Total-Size-Limit fuer E-Mail-Anhaenge (`/api/emails/attachments` POST listet Storage-State + summiert sizes). (4) SLC-542 L1 Filename-Kollision-Suffix " (n)" bei Duplikat-Upload (`upsert: true` → `upsert: false` mit `generateUniqueName`).
- Summary: 2 Commits — `42495cc` (V5.5.1 Hydration) + `d996307` (V5.4-Carryover-Sammelpatch) + Records-Sync `4415928`. TSC + Vitest 97/97 PASS pre-Deploy. Rein additiv, keine Schema-Aenderungen, keine ENV-Aenderungen, keine neuen Dependencies. Single list()-Call im Upload-Endpoint deckt Total-Size + Kollisions-Check gemeinsam ab. Deployed 2026-05-01 07:50:22 als Coolify-Deployment-ID 173, Image-Tag `4415928`, app-Container healthy, HTTPS-Endpoint HTTP 200. Live-Browser-Verifikation der 3 sichtbaren Items steht aus: (a) `/proposals` Console-Check fuer #418, (b) Composing-Studio Filename-Kollision durch zweimaligen Upload mit gleichem Namen, (c) Total-Size-Limit durch zwei verschiedene 12-MB-Files in derselben Session.
- Risks: Item 1 (Hydration) ist mitigation, kein root-cause-fix — falls #418 weiterhin auftritt, braucht es Live-Console-Inspection. Items 2-4 sind kleine isolierte Aenderungen ohne strukturelles Risiko. Re-Drag-Idempotenz (gleiche Datei zweimal in derselben Session ziehen) erzeugt jetzt Suffix-File statt zu ueberschreiben — minimaler Verhaltens-Drift, vom User wahrscheinlich unbemerkt.
- Rollback Notes: Beide Commits unabhaengig revert-bar. `git revert 42495cc` setzt `<body>` ohne suppressHydrationWarning zurueck. `git revert d996307` setzt ConditionalColorPicker auf useState/useEffect-Pattern und Upload-Route auf `upsert:true` mit `totalSizeSoFar=0` zurueck. Kein Schema-Rollback, kein Daten-Verlust. Bestehende Storage-Files mit Suffix " (n)" bleiben gueltig auch nach Rollback.

### REL-020 — V5.5 Angebot-Erstellung
- Date: 2026-05-01
- Scope: V5.5 Final-Release. Alle 5 Slices implementiert: **SLC-551** Schema-Erweiterung (`proposals.parent_proposal_id/valid_until/payment_terms/...`, neue Tabelle `proposal_items`, `email_attachments.source_type`+`proposal_id`+CHECK-Constraint, neue Buckets `proposal-pdfs`); **SLC-552** Workspace `/proposals/[id]/edit` mit nativem React-State + Custom-Debounce + dnd-kit; **SLC-553** pdfmake-Renderer (DEC-105) mit Server-Proxy `/api/proposals/[id]/pdf` (Mixed-Content-Hotfix `91020b2`) + Branding-Adapter; **SLC-554** Status-Lifecycle (`draft → sent → accepted | rejected | expired`) per Whitelist-gekapselter Server Action, lineare Versionierung mit Item-Snapshot-Kopie (V1 unangetastet, DEC-109), Read-only-Mode fuer Non-Draft-Workspace, Auto-Expire-Cron `0 2 * * *` Berlin (DEC-110); **SLC-555** Composing-Studio-Anbindung — `<ProposalAttachmentPicker>` mit shadcn-Dialog im Compose-Anhang-Bereich, neue Server Action `attachProposalToCompose` (PDF-aus-Bucket-Validierung), `email_attachments`-Junction-Insert mit `source_type='proposal'`+`proposal_id`, idempotenter Auto-Sent-Trigger via `transitionProposalStatus` nach erfolgreichem Send (DEC-108). MIG-026 Schema-Erweiterung. DEC-105..114. **NEUE INFRASTRUKTUR-AKTION (SLC-554):** Coolify-Cron `expire-proposals` muss vor erstem produktivem Sent-Status angelegt werden (Auto-Expire faellt sonst aus). Anleitung unten. **NEUE DEPENDENCY:** `pdfmake` (~700 KB) wurde in SLC-553 dem Build hinzugefuegt — Bundle-Auswirkung bereits in V5.5-Sub-Deploys verifiziert.
- Summary: SLC-551..554 sind bereits live im Cockpit (siehe STATE.md V5.5 Implementation, Hotfix `91020b2` PDF-Server-Proxy, RPT-260 SLC-554 QA-PASS, Cron-Endpoint `/api/cron/expire-proposals` Live + DB-Audit-Smoke). SLC-555 schliesst V5.5 ab: User kann im Composing-Studio neben PC-Direkt-Upload (V5.4-Pfad bleibt regression-frei) jetzt auch ein bestehendes Angebot-PDF aus dem Deal-Kontext anhaengen. Picker zeigt alle Status (DEC-112) mit Status-Badge + Confirm-Modal bei `expired`/`rejected`. Filename folgt SLC-553-Pattern `Angebot-{slug(deal.title)}-V{n}.pdf` (oder `.testmode.pdf` im Internal-Test-Mode, DEC-113). Send-Pipeline (`send.ts`) unterscheidet pro Anhang den Source-Bucket (`email-attachments` vs. `proposal-pdfs`). Internal-Test-Mode aktiv bis Pre-Production-Compliance-Gate vor V5.6.
- Risks: V5.4 PC-Direkt-Upload-Pfad muss bit-identisch bleiben (Junction-Default `source_type='upload'`, `proposal_id=NULL` greift; CHECK-Constraint deckt invalid States ab) — verifiziert ueber Cross-Cut-Smoke (3 Faelle: Proposal-only, Upload-only, Mix). V5.4-Cadence-Engine sendet ohne Anhaenge → Junction-Insert mit Default-Werten bleibt valide. SLC-554 deployt ohne den Coolify-Cron `expire-proposals` ist sicherheitsneutral (manuelle Status-Transitions funktionieren), aber Auto-Expire faellt aus. Auto-Sent-Trigger nach Mail-Send ist idempotent (DEC-108) — bei `transitionProposalStatus`-Error bleibt Mail-Erfolg erhalten, User kann Status manuell setzen. Read-only-Mode ist Server-Side enforced (Item-CRUD-Actions checken `status === 'draft'`), URL-Param `?readonly=1` ist nur UX-Hint. **Storage-Volumen-Wachstum:** Proposal-PDFs liegen dauerhaft im `proposal-pdfs`-Bucket, Loesch-Button im Compose entfernt nur State-Eintrag (Audit-Wahrheit). Cleanup-Cron fuer alte Proposal-PDFs ist V5.6+. **F1 Hydration #418** auf `/proposals` ist Carryover (UI funktional, kein Blocker, Investigation als V5.5.x-Patch oder Pre-V5.6). **ISSUE-042** OpenAI-Key Pre-Pflicht bleibt offen (gilt nicht direkt fuer V5.5, aber fuer V4.1+V5.1-Whisper-Flows). **Pre-Production-Compliance-Gate** vor V5.6: Anwalts-Pruefung `COMPLIANCE.md`, Azure-OpenAI-EU-Whisper-Switch, ISSUE-042-Schliessung — keiner dieser Punkte blockiert V5.5-Deploy in Internal-Test-Mode.
- Rollback Notes: Rein additiv. SLC-555 fuegt nur Server-Action `attachProposalToCompose`, neue Komponente `<ProposalAttachmentPicker>`, AttachmentMeta-Type-Erweiterung (Default `source_type='upload'` = V5.4-Verhalten), und Send-Pipeline-Discriminator hinzu — Rollback per Coolify-Redeploy auf V5.5-pre-SLC-555-Commit. Kein Schema-Rollback (MIG-026 inkl. `email_attachments.source_type`+`proposal_id` ist Teil von SLC-551, bleibt). Coolify-Cron `expire-proposals` separat deaktivieren ueber Coolify-UI falls noetig. Bestehende V5.5-Daten (Proposals, Items, generierte PDFs, Cron-expirte Rows) bleiben unberuehrt.

#### Coolify-Cron `expire-proposals` Setup (User-Aktion vor Final-Deploy)

**Wann:** Nach Coolify-Redeploy von V5.5 final (oder fruehestens nach Deploy von SLC-554).

**Voraussetzung:** ENV-Variable `CRON_SECRET` ist im Coolify-App-Container bereits gesetzt (existiert seit V5.1 retention-Cron, kein zusaetzliches Setup).

**Schritt 1 — Cron-Job in Coolify-UI anlegen:**

| Feld | Wert |
|------|------|
| Name | `expire-proposals` |
| Schedule | `0 2 * * *` (taeglich 02:00 Berlin Time) |
| Container | `app` |
| Command | `node -e 'fetch("https://business.strategaizetransition.com/api/cron/expire-proposals", { method: "POST", headers: { "x-cron-secret": process.env.CRON_SECRET } }).then(r => r.text()).then(console.log)'` |

**Sicherheits-Hinweise:**
- `CRON_SECRET` IMMER ueber `process.env.CRON_SECRET` referenzieren — niemals als Klartext in Command (siehe REL-019 Coolify-Cron-Cleanup).
- Nur EIN Cron-Job mit diesem Namen anlegen — Duplikate erzeugen Doppel-Audit-Eintraege bei Doppellaufen.
- Auth-Header ist `x-cron-secret`, NICHT `Authorization: Bearer` (Pattern aus `verify-cron-secret.ts`).

**Schritt 2 — Smoke-Test direkt nach Anlage (vor erstem 02:00-Lauf):**

```bash
# Manueller cURL-Test gegen die Public-Domain — gleiche Auth wie Cron-Trigger
ssh root@91.98.20.191
curl -sX POST -H "x-cron-secret: $CRON_SECRET" \
  https://business.strategaizetransition.com/api/cron/expire-proposals
# Erwartet: {"success":true,"expiredCount":0,"expiredIds":[]}
# Bei expirten Rows: korrekte Counts in der Response

# Mit falschem Header → 401
curl -sX POST -H "x-cron-secret: wrong" \
  https://business.strategaizetransition.com/api/cron/expire-proposals
# Erwartet: {"error":"Unauthorized"} mit HTTP 401
```

**Schritt 3 — Verifikation des ersten 02:00-Laufs:**

Am Tag nach Anlage zwischen 02:00 und 02:05 Berlin Time im Coolify-Cron-Log nach Eintrag `[Cron/ExpireProposals] expired=N ids=...` schauen. Bei `expired=0` ist das normal (kein expirter Datensatz vorhanden). Audit-Eintraege koennen mit folgender SQL gepueft werden:

```sql
SELECT created_at, action, entity_id, context
FROM audit_log
WHERE entity_type = 'proposal'
  AND context = 'Auto-expire by cron — valid_until passed'
ORDER BY created_at DESC
LIMIT 10;
```

**Idempotenz-Garantie:** Mehrfachlaufe des Crons (z.B. wegen Coolify-Restart) erzeugen keine Doppel-Audit-Eintraege — `expireOverdueProposals()` filtert auf `status='sent' AND valid_until < CURRENT_DATE`, expirte Rows werden beim zweiten Lauf nicht mehr getroffen.

### REL-019 — V5.4 Composing-Studio Polish + E-Mail-Anhaenge
- Date: 2026-04-29
- Scope: 2 Slices (SLC-541 V5.4-Polish + SLC-542 E-Mail-Anhaenge-Upload), 2 Features (FEAT-541 + FEAT-542), 8 DECs (DEC-097..104), MIG-025 (`email_attachments`-Junction-Table + privater Storage-Bucket). SLC-541 schliesst V5.3-Hygiene-Themen ab: Color-Picker AC9-Drift Fix per ConditionalColorPicker (DEC-102), ESLint Hook-Order Cleanup in NewTemplateDialog + InlineEditDialog (resetState-via-onOpenChange-Wrapper-Pattern, eliminiert react-hooks/set-state-in-effect-Errors), COMPLIANCE.md V5.3-Section + V5.4-Section (Anhang-Pipeline beschrieben), Coolify-Cron-Cleanup-User-Anleitung. SLC-542 fuegt Drag&Drop + File-Picker + MIME/Size-Whitelist + Multipart-SMTP via API-Route hinzu (Refactor von Server-Action analog Onboarding-Plattform-Pattern wegen 1 MB Body-Size-Limit + Filename-Encoding-Probleme).
- Summary: Deployment als Coolify-Redeploy auf Commit `c8637c6` (Refactor Anhang-Upload Server-Action → API-Route) durch User durchgefuehrt 2026-04-29. Pre-Deploy User-Live-Smoke gegen Hetzner durchgespielt: AC4+5 Drag&Drop (5.5 + 6.4 MB Word-Files), AC6+7 Pro-File-Limit (21.6 MB PDF korrekt rejected), AC10+11+12 Multipart-Mail PDF+PNG+ZIP an Gmail erfolgreich + 3 Junction-Rows in `email_attachments` + Tracking-Pixel-`open`-Event 38s nach Send. MIG-025 bereits seit 2026-04-28 live auf Hetzner-DB (per ssh-Migration-Pattern). **Keine neuen ENV-Variablen.** **Keine neuen Dependencies.** Coolify-Cron-Cleanup als User-Aktion mit Deploy via SQL durchgefuehrt: 5 Operationen, 17 → 15 Crons, alle 15 verbleibenden auf process.env.CRON_SECRET-Pattern, kein Klartext-Secret mehr, kein kaputter Literal-Placeholder mehr. Final-Check READY (RPT-246, 0 Blocker, 0 High, 0 Medium, 1 Low ISSUE-045 akzeptiert). Go-Live GO (RPT-247). tsc 0, Vitest 35/35, Next-Build green. 6 critical Container healthy.
- Risks: ISSUE-045 (Low) Server-side Total-Size-Limit ist Client-Convenience — Pro-File-Limit (10 MB) ist 3-fach hart enforced (Browser+Upload+Send), Total-Limit (25 MB) nur Browser-side. internal-tool single-user, V5.5+ Operations-Topic. ISSUE-042 (High) OpenAI-API-Key in untrackter Datei bleibt offen — **NICHT V5.4-blockierend** (V5.4-Whisper ist User-Self-Diktat, kein Kunden-Audio). Pre-Pflicht-Marker fuer V4.1 Meeting + V5.1 Call-Recording-Flows. DEC-104 Verwaiste-Storage-Files-Cleanup-Cron deferred — Compose-Tab-Schliessen ohne Send laesst Files im Bucket (V5.5+ Operations). 2 reviewed-and-accepted `console.error` in send-action.ts (Best-Effort Junction-Insert-Logging, kein User-Inhalt). Outlook-Compatibility-Smoke fuer Multipart organisch beim ersten echten Outbound-Send. SLC-541 M1 (ConditionalColorPicker derived-state Refactor) + SLC-542 L1 (Filename-Kollision-Suffix bei upsert) als V5.4.x-Patch-Carryover.
- Rollback Notes: Rein additiv. SLC-541 modifiziert nur UI-Komponenten + Doku — Rollback per Coolify-Redeploy auf V5.3-Stand (Commit `8d8e098` oder neuerer V5.3-Tag), kein Schema-Rollback. SLC-542 fuegt MIG-025 hinzu (additive Junction-Table + privater Bucket), bleibt bei Rollback live (neue Tabelle wird von V5.3-Code ignoriert). API-Route `/api/emails/attachments` verschwindet aus dem Build, AttachmentsSection-Komponente und `attachments-whitelist.ts` werden tot — kein Daten-Verlust. Coolify-Cron-Cleanup ist auch reversibel: Snapshot in `/tmp/coolify-cron-snapshot-20260429-100851.sql` auf Hetzner-Server, restore via `TRUNCATE scheduled_tasks; psql < snapshot.sql`. next.config.ts `bodySizeLimit: "4mb"` Aenderung ist add-only, kein Rollback noetig.

#### Coolify-Cron-Cleanup (DURCHGEFUEHRT 2026-04-29 via SQL)

**Status:** Erfolgreich durchgefuehrt 2026-04-29 vor V5.4-Deploy via direkter SQL auf der Coolify-Postgres-DB (`docker exec coolify-db psql ...`). 5 Operationen in einer Transaktion: 17 → 15 Crons, alle 15 verbleibenden auf `process.env.CRON_SECRET`-Pattern. Snapshot vorab gesichert in `/tmp/coolify-cron-snapshot-20260429-100851.sql`. Verifikations-Output siehe RPT-247.

**Aktionen-Tabelle:**

| # | Operation | Cron-ID | Aktion |
|---|-----------|---------|--------|
| 1 | DELETE | 18 | `Classify` (Container leer, Duplikat von ID 3) entfernt |
| 2 | DELETE | 13 | `embedding-sync` 15min mit `CRON_SECRET_VALUE`-Literal entfernt (war 401-Loop seit Anlage) |
| 3 | UPDATE | 14 | `embedding-sync` von 5min Schedule auf `*/15 * * * *` umgestellt (5x weniger Bedrock-Calls) |
| 4 | UPDATE | 1 | `imap-sync` Klartext-Secret durch `process.env.CRON_SECRET` ersetzt |
| 5 | UPDATE | 2 | `retention` Klartext-Secret durch `process.env.CRON_SECRET` ersetzt |

**Korrekturen zur urspruenglichen Spec (in SLC-541 dokumentiert):**
- (b) Embedding-Konsolidierung umgekehrt — der KAPUTTE 15min-Cron geloescht, der 5min funktionale auf 15min umgestellt (statt umgekehrt).
- (c) Retention NICHT geloescht — beide Endpoints sind aktiv mit unterschiedlicher Funktion (`/api/cron/retention` IMAP-Mail-Retention, `/api/cron/recording-retention` Audio-Files). retention bleibt, wurde nur in (e) auf process.env umgestellt.
- (e) Nur 2 Klartext-Crons (nicht 3 wie geplant): `imap-sync` + `retention`.

**Rollback (falls noetig):** `ssh root@91.98.20.191 "docker exec -i coolify-db psql -U coolify -d coolify -c 'TRUNCATE scheduled_tasks RESTART IDENTITY CASCADE;' && cat /tmp/coolify-cron-snapshot-20260429-100851.sql | docker exec -i coolify-db psql -U coolify -d coolify"`.

**Verifikations-Schritt nach jeder Aenderung:** Cron-Logs im Coolify-UI 5–15min nach Aenderung pruefen — der erwartet aktive Cron sollte einen Erfolgs-Output haben, der geloeschte Cron erscheint nicht mehr in der Trigger-Liste.

### REL-018 — V5.3 E-Mail Composing Studio
- Date: 2026-04-28
- Scope: 5 Slices (SLC-531..535), 4 Features (FEAT-531..534), 8 DECs (DEC-088..096), MIG-023 + MIG-024. Branding-Settings + zentrale Mail-Layout-Engine (organization_branding-Tabelle + render.ts + /api/branding/logo Next.js-Proxy), Email-Templates Schema-Erweiterung (is_system + category + language + 8 Systemvorlagen-Seed) + KI-Vorlagen-Generator (Bedrock Server Action), 3-Panel-Composing-Studio (/emails/compose mit Templates-Panel + Erfassen + Live-Preview, KI-Vorausfuellung von An/Betreff aus Deal-Kontext), Live-Preview mit 250ms-Debounce + iframe-Render + Senden-Integration via sendComposedEmail Server Action + Auto-Follow-up-Task + 4 Einstiegspunkte (Deal-Workspace + Mein Tag + Focus + KI-Workspace) auf /emails/compose umgestellt, Inline-Edit-Diktat (Voice + Diff-Modal mit diff@9 diffWords + harte System-Prompt-Constraints gegen Halluzination, Identitaets-Check, maxTokens 6000). Style-Guide-V2-Restyling als BL-403 (Composing-Studio rounded-2xl/border-2/shadow-lg/gradient-Buttons, premium Spacing/Grid).
- Summary: Deployment als Coolify Redeploy auf Commit 8d8e098 (V5.3-Final-Check). Gesamt-QA PASS (RPT-236, 5/5 Slices, alle 12 ACs SLC-535 + Cross-Slice-Wiring End-to-End verifiziert). Final-Check Conditionally Ready (RPT-237, 0 Blocker, 0 High in V5.3). 6 Slice-/qa-Reports vollstaendig (RPT-220 SLC-531 + Logo-Hotfix RPT-225, RPT-227 SLC-532, RPT-228+229 SLC-533, RPT-231 SLC-534, RPT-234+235 SLC-535). MIG-023 + MIG-024 bereits seit 2026-04-27 live auf Hetzner-DB (per ssh-Migration-Pattern). Keine neuen ENV-Variablen — alle benoetigten Vars (AWS_REGION, LLM_MODEL, AWS_ACCESS_*, SMTP_*, OPENAI_API_KEY, NEXT_PUBLIC_APP_URL, TRACKING_BASE_URL) seit V5.0/V5.1/V5.2 in Coolify gesetzt. Keine neuen Cron-Jobs. Neue Dependencies: diff@9.0.0 (BSD-3) + @types/diff@7.0.2 (MIT). 3 Smoke-Test-Faelle SLC-535 (klar/mehrdeutig/problematisch) auf Live-Hetzner PASS — KI hielt System-Prompt-Constraint gegen Halluzination ein, blieb generisch + erfand keine Fakten. tsc 0, Vitest 35/35, Next-Build 60/60.
- Risks: ISSUE-042 (OpenAI-API-Key untracked file) bleibt open — **NICHT V5.3-blockierend** (V5.3-Whisper ist User-Self-Diktat im Composing-Studio, kein Kunden-Audio-Flow). Pre-Pflicht-Marker bleibt fuer V4.1 (Meeting-Recording) + V5.1 (Call-Recording). ISSUE-043 (Color-Picker AC9-Drift in Branding-Form) open — V5.4-Polish-Carryover, kein Datenkorruptions-Risiko. ESLint 10 Strict-Mode-Hinweise (React-19-Hook-Order in new-template-dialog + inline-edit-dialog) Non-Runtime, Build gruen, V5.4-Polish. COMPLIANCE.md V5.3-Update offen (V5.3 fuehrt KEINE neue Datenkategorie ein, Update kann in V5.4-Polish nachgezogen werden). User-E2E-Workflow-Smoke per User-Approval auf Build-Phase-Ende mit Testkunden verschoben (feedback_e2e_smoke_at_buildup_end.md). Outlook-Compatibility-Smoke offen (organisch beim ersten echten Outbound-Mail-Versand). Branding-Daten "Strategaize Tnasition GmbH" Tippfehler (User-Aktion ohne Code).
- Rollback Notes: Rein additiv. Coolify-Redeploy auf Commit 700b17d (V5.2-Stand) oder neueren V5.2-Tag — ~2 Minuten, kein Schema-Rollback noetig. MIG-023 (organization_branding + email_templates is_system/category/language/layout-Spalten) bleibt live (additive Spalten werden von V5.2-Code ignoriert). MIG-024 (Storage-Public-Read fuer Logo) bleibt live. Neue Dependencies (diff, @types/diff) deinstallieren bei lang-laufendem Rollback per `npm uninstall diff @types/diff`. /emails/compose-Route verschwindet aus dem Build automatisch, Einstiegspunkte fallen auf vorherigen EmailSheet zurueck (mein-tag-client + ki-workspace + focus-client + deal-actions in V5.2-Code). 4 V5.3-Server-Actions (template-generate-action + send-action + inline-edit-action + branding/actions) verschwinden. Kein Datenverlust, keine bestehende Funktionalitaet bedroht.

### REL-001 — V1 Legacy (Marketing+CRM)
- Date: 2026-03-31
- Scope: CRM-Basis (Kontakte, Firmen, Pipeline), Marketing-Skills. Ersetzt durch V2.
- Summary: Erste Deployment auf Hetzner. Login + Dashboard funktional. Pipeline mit Seed-Daten.
- Risks: Pipeline hatte noch Fehler mit Seed-Daten. Temporäre Signup-Route.
- Rollback Notes: Docker Image Rollback via Coolify.

### REL-002 — V2 Revenue & Relationship System
- Date: 2026-04-03
- Scope: 13 Slices, 15 V2-Features, 11 Module. Komplette Neuausrichtung als Revenue & Relationship System.
- Summary: Vollständiges BD-System mit Kontakten (erw.), Firmen (erw.), Multiplikatoren, 2 Pipelines (Kanban), strukturierte Gespräche, Aufgaben, E-Mail (SMTP), Angebote, Fit-Gates, Signale, Referrals, Handoffs, Dashboard (8 KPIs). UI-Polish nach Blueprint-Standard. Dark Sidebar mit Logo, Premium-Tabellen, Pipedrive-style Kanban.
- Risks: MIG-002 pending (overall_score INT→NUMERIC, kosmetisch). SMTP nicht konfiguriert (Drafts-Fallback). SSH-Zugang zum Server eingeschränkt.
- Rollback Notes: Docker Image Rollback via Coolify. V1 Image als Fallback verfügbar.

### REL-003 — V2.1 Pipeline-Hardening + Daily Ops
- Date: 2026-04-07
- Scope: 8 Slices (SLC-201–208), 14 Backlog-Items. Pipeline-Hardening, "Mein Tag" Tagesplanung, Voice-Input, Insight-Export, Lead-Pipeline.
- Summary: Deal-Status-Workflow mit Auto-Status bei Stage-Drag. Deal-Detail-Popup (4 Tabs). Deal-Rotting (>7d gelb, >14d rot). Required Fields bei Stage-Wechsel. Pipeline-Filter (Status/Typ) + gewichteter Forecast. "Mein Tag" mit Erledigt-Buttons, Deal-Popup, Kalender-Platzhalter. Voice-Input via OpenAI Whisper API. Insight-Export an System 4. Lead-Management-Pipeline (7 Stages) mit Pipeline-Verschiebung. E-Mails in Aktivitäten-Timeline. Referral-Source auf Deals. Dashboard Overdue-Banner + Forecast-KPI.
- Risks: Insight JSON-Export ephemeral (Container-Restart = Datenverlust). UI-Polish noch ausstehend. Keine automatisierten Tests.
- Rollback Notes: Docker Image Rollback via Coolify. DB-Rollback: MIG-003 (DROP closed_at, ADD lost_reason), MIG-004 (DELETE Lead-Pipeline + Stages). V2 Image als Fallback.

### REL-004 — V2.2 UI-Redesign + KI-Cockpit UI-Shells
- Date: 2026-04-09
- Scope: Kompletter UI-Umbau nach Style Guide V2. Dashboard KI-Analyse-Cockpit (UI-Shell). Mein Tag KI-Assistent (UI-Shell). Search-Bar volle Breite. KI/Voice-Placeholder-Buttons.
- Summary: Letztes rein UI-zentriertes Release. Offene Items (BL-212 Pipeline Style Guide, BL-213 Standort-Filter, BL-214 fehlende Seiten, BL-215 Konsolidierung) in V3-Backlog uebernommen — werden durch V3 Workspace-Konzept und Navigation-Umbau abgedeckt. Strategische Neuausrichtung beginnt mit V3.
- Risks: Keine neuen technischen Risiken. KI-Shells sind Platzhalter ohne Backend.
- Rollback Notes: Docker Image Rollback via Coolify. V2.1 Image als Fallback.

### REL-005 — V3 Operative Kontextlogik
- Date: 2026-04-10
- Scope: 10 Slices (SLC-301..310), 9 Features (FEAT-301..309). Deal-Workspace, Mein Tag V2, Firmen-/Kontakt-Workspace, Bedrock LLM-Integration, Navigation-Umbau (5-Schichten), Governance-Basis (Rollen, RLS, Audit), Meeting/Kalender-Management.
- Summary: Strategische Neuausrichtung abgeschlossen. Workspace-zentriertes Arbeiten mit KI-Unterstuetzung (Bedrock Claude Sonnet). MIG-005 Schema-Migration (Meetings, Calendar Events, Audit-Log, Navigation). Gesamt-QA PASS (RPT-046). Mehrere Hotfixes nach Live-Test.
- Risks: Bedrock-Kosten bei Auto-Load (behoben in V3.1 durch on-click Pattern). SMTP nicht konfiguriert. Keine automatisierten Tests.
- Rollback Notes: Docker Image Rollback via Coolify. V2.2 Image als Fallback. MIG-005 Rollback: DROP meetings, calendar_events, audit_log Tabellen.

### REL-006 — V3.1 UX-Schliff + KI-Kontext
- Date: 2026-04-11
- Scope: 9 Slices (SLC-311..319). Meeting/Kalender UX, Schnellaktionen-Rebuild, KI on-click Pattern, Pipeline KI-Suche + Voice, KI-E-Mail-Composing, Kontext-Intelligenz, Auto-Wiedervorlagen, Tageseinschaetzung erweitert, Templates + Duplikate + Attribution, Activity-Queue.
- Summary: UX-Schliff und KI-Kontext-Integration. Alle Formulare KI-vorbefuellt aus Kontext. Voice-Input in allen Textfeldern. KI-Kostenkontrolle durch on-click statt auto-load. Automatische Wiedervorlagen nach Aktionen.
- Risks: Keine neuen Schema-Migrationen. Bedrock-Kosten kontrolliert durch on-click Pattern.
- Rollback Notes: Docker Image Rollback via Coolify. V3 Image als Fallback.

### REL-007 — V3.2 UI-Polish + Pipeline-Management + PLZ-Karte
- Date: 2026-04-11
- Scope: 6 von 6 Slices (SLC-321..326). Quick Actions Modal-Umbau, Pipeline UI-Overhaul + Selector, Logout-Button, Pipeline-Verwaltung in Settings, Autocomplete/Typeahead Suche, PLZ-Kartensuche mit Heatmap (DE+NL).
- Summary: UI-Quality-Release + Geo-Feature. Pipeline komplett ueberarbeitet. Settings erweitert um Pipeline-CRUD. Wiederverwendbare SearchAutocomplete. Interaktive Leaflet-Karte auf Firmen/Kontakte/Multiplikatoren mit PLZ+Stadtsuche, Umkreis-Filter, Heatmap. 12.384 PLZ (8.298 DE + 4.086 NL). Next.js 16.2.3 Security Patch. Gesamt-QA PASS, Final-Check PASS.
- Risks: Keine Schema-Migrationen. Neue Dependencies: leaflet, react-leaflet, leaflet.heat.
- Rollback Notes: Docker Image Rollback via Coolify. V3.1 Image als Fallback.

### REL-008 — V3.3 UI-Abrundung + Visualisierung
- Date: 2026-04-11
- Scope: 6 von 6 Slices (SLC-331..336). KI-Suchfeld + Voice im KI-Workspace, PLZ/Stadt-Autocomplete, Pipeline Liste-Ansicht Toggle, Unified Timeline, Funnel-Report, Win/Loss-Analyse Dashboard.
- Summary: Reine Frontend-Erweiterungen ohne Schema-Aenderungen. KI-Suchfeld mit Voice-Input direkt im KI-Workspace Header (Bedrock mein-tag-query). PLZ-Autocomplete bei Firma anlegen/bearbeiten. Pipeline hat jetzt 4 View-Modes (Kanban, Liste, Funnel, Win/Loss). Unified Timeline ersetzt Activity-Timeline auf Kontakt- und Firmen-Workspace (5 Datenquellen). Funnel-Report zeigt Conversion-Rates pro Stage. Win/Loss-Analyse mit Verlustgruende-Ranking und 6-Monats-Trend. Gesamt-QA PASS (RPT-071), Final-Check PASS (RPT-072). User Live-Test bestanden.
- Risks: Keine Schema-Migrationen. Keine neuen Dependencies. Responsive nicht explizit getestet (Desktop-only internal-tool).
- Rollback Notes: Docker Image Rollback via Coolify. V3.2 Image als Fallback.

### REL-017 — V5.2 Compliance-Sprint
- Date: 2026-04-26
- Scope: 5 Slices (SLC-521..525), 5 Features (FEAT-521..525), 7 DECs (DEC-081..087), MIG-022. Recording-Retention 7d Hardening (Default 30→7, ENV-konfigurierbar), Azure-Whisper-Adapter Code-Ready (TRANSCRIPTION_PROVIDER=openai bleibt Default), Compliance-Templates Vertical Slice (compliance_templates-Tabelle, /settings/compliance-Page, Variable-Engine, 3 editierbare Templates: meeting_invitation/email_footer/calcom_booking), MeetingTimelineItem UI-Parity zu CallTimelineItem (Calendar-Icon, Purple-Theme, Decisions/Action Items/Next Step/Key Topics/Transkript), DSGVO-Compliance-Doku docs/COMPLIANCE.md mit 8 Pflicht-Sektionen + Anwalts-Disclaimer.
- Summary: Deployment als Coolify Redeploy auf Commit 1ca619e (V5.2-Final-Check). Gesamt-QA PASS (RPT-216, 5/5 Slices, 49/49 AC). Final-Check Conditionally Ready (RPT-217, 0 Blocker, 1 High für ISSUE-042). MIG-022 idempotent angewendet (Tabelle existierte bereits seit SLC-523-Backend, 3 Default-Rows verifiziert). Smoke-Test in Production am 2026-04-26 PASS: A (Settings-Compliance + MeetingTimelineItem + Side-by-Side Parity) ✓, B (V5.1-Regression Pipeline/Deals/Mein Tag/Performance/Login) ✓, C (Recording-Retention-Cron mit retention_days=7 verifiziert via curl). Vitest 16/16 (Azure-Whisper 8 + Compliance-Templates-Engine 8). Vitest neu eingefuehrt als Test-Framework in V5.2 (Deviation Rule 4 Option A in SLC-522). ENV-Befund waehrend Smoke-Test: RECORDING_RETENTION_DAYS war in Coolify auf 30 gesetzt — User hat auf 7 korrigiert + Redeploy.
- Risks: ISSUE-042 (OpenAI-API-Key in untrackter Datei am Repo-Root, NIE committed) bleibt open — Pre-Pflicht vor erstem produktivem Whisper-Call: Key bei OpenAI rotieren + neuen Key in Coolify ENV setzen + lokale Datei beseitigen. Internal-Test-Mode bleibt aktiv (kein produktiver Recording-Einsatz mit echten Kundendaten bis Anwalts-Pruefung der COMPLIANCE.md + Compliance-Templates + Pre-Go-Live-Switch auf Azure-EU-Whisper). 5 NPM-Audit-Vulnerabilities (1 high xmldom, 4 moderate fast-xml-parser/postcss) in transitiven Production-Deps — nicht durch User-Input ausnutzbar in Internal-Tool, akzeptierter Tech-Debt.
- Rollback Notes: Coolify-Redeploy auf Commit 700b17d (V5.1-Stand) — ~2 Minuten. MIG-022 (compliance_templates) bleibt live, kein Schema-Rollback noetig (Tabelle wuerde bei Rollback nur ungenutzt sein, kein Konflikt). RECORDING_RETENTION_DAYS in Coolify zurueck auf 30 setzen + Redeploy fuer V5.1-Verhalten. /settings/compliance-Route verschwindet aus dem Build automatisch.

### REL-016 — V5.1 Asterisk Telefonie + SMAO Voice-Agent-Vorbereitung
- Date: 2026-04-24
- Scope: 5 Slices (SLC-511..515), 4 Features (FEAT-511..514), 12 DECs (DEC-070..081), MIG-020 + MIG-021. Eigene Asterisk-PBX (Docker, Traefik-WSS auf sip.strategaizetransition.com, Echo-Test 600 + Internal 1001 + Outbound-Patterns), WebRTC Click-to-Call aus Deal-Workspace (SIP.js-Hook + CallWidget), Call-Recording-Pipeline (MixMonitor → Supabase Storage → Whisper → Bedrock-Summary → Deal-Activity, E2E verifiziert RPT-197), SMAO Voice-Agent-Adapter vorbereitet (Interface + Webhook + Klassifikations-Dispatcher, SMAO_ENABLED=false default).
- Summary: Deployment als Coolify Redeploy auf Commit 700b17d (Delta: SLC-515 + Enum-Dedupe QA-Fix). Gesamt-QA PASS (RPT-200, 5/5 Slices). Final-Check Conditionally Ready (RPT-201, 0 Blocker, 0 High). Go-Live Conditional Go (RPT-202). Release-Bedingung: **Internal-Test-Mode bis V5.2 Compliance-Sprint (DEC-081)** — produktive Call-Aufnahme mit externen Teilnehmern erst nach Call-Consent-Flow. MIG-020 (calls-Tabelle + Storage-Bucket) + MIG-021 (Storage-Grants + Role-Delegation) bereits seit SLC-514-Deploy 2026-04-24 live. Keine neuen Crons (Webhook ist request-driven). Neue Dependency: sip.js@0.21.2 (MIT). ISSUE-039 (Volume-Permissions) und ISSUE-040 (Storage-Grants) waehrend Pipeline-Test entdeckt und gefixt. ISSUE-041 latent dokumentiert (Cron-Interferenz bei SMAO_ENABLED=true, Pre-SMAO-Go-Live-Fix).
- Risks: Compliance-Gap fuer Call-Recording (Mitigation: Internal-Test-Mode). codec_opus.so Interim (DEC-078, ulaw-only ausreichend fuer Telefonie). Kein echter SIP-Trunk-Provider konfiguriert (V5.1-Scope). Kein Health-Check fuer Asterisk-Container (Coolify zeigt nur "running"). ASTERISK_WEBRTC_PASSWORD muss in Coolify auf starken Wert gesetzt sein (nicht Default "changeme"). SMAO_ENABLED=false und SMAO_WEBHOOK_SECRET ungesetzt belassen.
- Rollback Notes: Coolify-Redeploy auf Commit cc3222b (SLC-514-Stand) — ~2 Minuten, kein Schema-Rollback noetig (MIG-020+021 bleiben live, sind auch fuer Meeting-Pipeline kritisch via MIG-021). SLC-515-Artefakte verschwinden: Webhook-Route, voice-agent-Adapter, Enum-Dedupe. Kein Datenverlust, keine Konsequenz fuer Bestand.

### REL-015 — V5 Automatisierung + Vertriebsintelligenz
- Date: 2026-04-22
- Scope: 7 Slices (SLC-501..507), 4 Features (FEAT-501,504,505,506), MIG-019. Cadences/Sequences (Template-Builder, Enrollment, Cron-Execution, Abort-Check), E-Mail Auto-Zuordnung (3-Stufen: Exact/Domain/KI-Match), E-Mail Open/Click-Tracking (Pixel + Link-Wrapping + TrackingBadge + TrackingDetail + Timeline-Indikatoren), Intelligence-Platform-Export-API (5 Endpoints, API-Key-Auth, Rate-Limiting, Pagination), AssignmentBadge auf Inbox, Sidebar-Refactor (Arbeitsbereiche collapsible, Cadence→Automatisierung).
- Summary: Deployment als Coolify Redeploy. Gesamt-QA PASS (RPT-190, 7/7 Slices, 53 ACs). Final-Check READY (RPT-191, 0 Blocker, 1 Medium). User-Browser-Smoke-Test PASS. 5 neue DB-Tabellen (cadences, cadence_steps, cadence_enrollments, cadence_executions, email_tracking_events). 2 Tabellen erweitert (emails +tracking_id, email_messages +assignment_source). 2 neue Cron-Jobs (cadence-execute 5min, classify 10min). 2 Bugs in QA gefunden und gefixt (signals Tabelle, insights Spalte). Rein additiv, kein Einfluss auf bestehende Features.
- Risks: EXPORT_API_KEY nicht in Coolify ENV gesetzt (API nicht nutzbar bis Key gesetzt). Rate-Limiting in-memory (Container-Restart setzt zurueck). Pre-V5-Mails haben kein assignment_source (Badge korrekt ausgeblendet).
- Rollback Notes: Rein additiv. Docker Image Rollback via Coolify auf V6.1 Image. Schema-Rollback: DROP TABLE cadence_executions, cadence_enrollments, cadence_steps, cadences, email_tracking_events CASCADE; ALTER TABLE emails DROP COLUMN tracking_id, DROP COLUMN tracking_enabled; ALTER TABLE email_messages DROP COLUMN assignment_source, DROP COLUMN ai_match_confidence; Crons cadence-execute + classify in Coolify deaktivieren.

### PRE-V4.1-INFRA — V4.1 Pre-Flight Infrastruktur (Jitsi-Vorbereitung)
- Date: 2026-04-16
- Scope: Infrastruktur-Vorarbeit fuer SLC-412 Jitsi+Jibri Deployment. Hetzner-Cloud-Firewall-Regel fuer Port 10000/udp eingehend geoeffnet (via Hetzner Cloud Console). Coolify-Subdomain `meet.strategaizetransition.com` provisioniert (DNS-A-Record auf 91.98.20.191, Traefik-Ready). VAPID-Keys fuer Browser-Push erzeugt (SLC-418-Vorbereitung). Supabase-Storage-Bucket `meeting-recordings` angelegt (SLC-415-Vorbereitung). Kein Code-Deploy, keine Schema-Migration — reine Preparation.
- Summary: Pre-Flight-Gate fuer V4.1 Meeting-Slices. Server-RAM idle 4.4 GB frei (Zielarchitektur: 1 paralleles Meeting+Recording ~6.5 GB, passt in 8 GB CPX32 mit Upgrade-Pfad CPX42 dokumentiert DEC-040). Alle Infrastruktur-Blocker fuer SLC-412 beseitigt.
- Risks: Hetzner-Cloud-Firewall-UI-Regel erfordert manuelle Re-Verifizierung vor SLC-412-Smoke-Test (nicht via SSH pruefbar, nur Hetzner Cloud Console). Bei NAT-strikten Kunden-Netzwerken kann UDP/10000 blockiert sein — dokumentiert als Risk in ARCHITECTURE V4.1 (TURN-Server deferred auf BL-206-Nachbar).
- Rollback Notes: Keine Artefakte produziert. Rollback = Firewall-Regel entfernen, Coolify-Subdomain abkoppeln, Supabase-Bucket loeschen. Kein Effekt auf V4 Produktion.

### REL-014 — V6.1 Performance Premium UI
- Date: 2026-04-21
- Scope: 3 Slices (SLC-611..613), 1 Feature (FEAT-611), 3 DECs (DEC-061..063). Premium-Styling auf allen Performance-Karten (Gradient-Akzentlinien, Brand-Icons, Shadow-Upgrade), ForecastCard als 4. Kachel im Grid, Label-Korrektur "Abschlussquote" statt "Win-Rate", Wochen-Check mit Mo-Fr Tagesaufloesung und Heute/Woche Toggle.
- Summary: Reine Frontend-UI-Erweiterung. Gesamt-QA PASS (RPT-176, 3/3 Slices, 1/1 Feature). Final-Check READY (RPT-177, 0 Blocker, 0 High, 7/7 Dimensionen). User-Browser-Smoke-Test PASS (4 Kacheln, Gradient-Linien, Toggle, Wochen-Raster). Keine Schema-Migrationen, keine neuen APIs, keine neuen Dependencies, keine neuen Cron-Jobs.
- Risks: Keine V6.1-spezifischen Risiken. Responsive nicht explizit getestet (Desktop-only internal-tool).
- Rollback Notes: Rein additiv. Docker Image Rollback via Coolify auf V6 Image. Keine Schema-Aenderungen.

### REL-013 — V6 Zielsetzung + Performance-Tracking
- Date: 2026-04-20
- Scope: 10 Slices (SLC-601..610), 4 Features (FEAT-601..604), 2 DECs (DEC-055..056), MIG-017 + MIG-018. Produkt-Stammdaten (CRUD + Deal-Zuordnung), Ziel-Objekt-Modell (Umsatz/Deal-Count/Abschlussquote, Jahres-→Monats-/Quartals-Ableitung, CSV-Import), Performance-Cockpit (/performance mit Goal-Cards, Prognose-Engine, KI-Empfehlung via Bedrock, Tages-Check mit Activity-KPIs, Wochen-Vergleich, Trend-Vergleich, Produkt-Breakdown), KPI-Snapshots (taeglicher Cron, 8+ KPI-Typen, Trend-Engine).
- Summary: Deployment als Coolify Redeploy. Gesamt-QA PASS (RPT-171, 10/10 Slices, 4/4 Features, 11 Backlog-Items). Final-Check READY (RPT-172, 0 Blocker, 0 High). User-Browser-Smoke-Test PASS. 2 neue DB-Tabellen-Gruppen (MIG-017: products, deal_products, goals, kpi_snapshots; MIG-018: activity_kpi_targets). 1 neuer Cron-Job (kpi-snapshot, taeglich 02:00). Rein additiv, kein Einfluss auf bestehende Features. todayRange()-Bug in QA gefunden und gefixt (IMP-096).
- Risks: KPI-Snapshot-Cron muss in Coolify konfiguriert werden. userId nicht in KPI-Queries (Single-User OK). Win-Rate-Ableitung semantisch vereinfacht (V1-akzeptabel). Keine automatisierten Tests.
- Rollback Notes: Rein additiv. Docker Image Rollback via Coolify auf V4.3 Image. Schema-Rollback: DROP TABLE activity_kpi_targets, kpi_snapshots, goals, deal_products, products CASCADE; Cron kpi-snapshot in Coolify deaktivieren.

### REL-012 — V4.3 Insight Governance
- Date: 2026-04-19
- Scope: 6 Slices (SLC-431..436), 2 Features (FEAT-402, FEAT-412), 6 DECs (DEC-049..054), MIG-016. Insight-Review-Queue fuer schreibende KI-Aenderungen (Queue-Erweiterung, Signal-Extraktion-Modul, Cron-Hooks, Applier + Approve-Flow, Unified Queue UI, KI-Badge + Manual Trigger). Automatische Signal-Extraktion aus Meeting-Summaries und E-Mails. Manueller Trigger im Deal-Workspace. PropertyChangeCards mit Confidence-Badge, Batch-Approve. KI-Badge auf geaenderten Deal-Properties (30-Tage-Fenster).
- Summary: Deployment als Coolify Redeploy. Gesamt-QA PASS (RPT-154, 41/42 ACs, 5 Medium, 0 Blocker). Final-Check READY (RPT-155, 7/7 Dimensionen PASS). Browser-Smoke-Test bestanden (Mein Tag, Deal-Workspace, Signale-Button). 1 neuer Cron-Job (signal-extract */5). Schema rein additiv (nullable Spalten).
- Risks: Tag/Priority-Apply nicht moeglich (Spalten fehlen, V5). Activity-Limit 20 fuer Badge (1-User-Nutzung unrealistisch). Pipeline-Stages nicht im LLM-Prompt (Human-Review). Bedrock-Kosten: max 6 Calls/5min (weit unter Limits).
- Rollback Notes: Rein additiv. Docker Image Rollback via Coolify auf V4.2 Image. Schema-Rollback: ALTER ai_action_queue DROP COLUMN target_entity_type, target_entity_id, proposed_changes, confidence; ALTER meetings DROP COLUMN signal_status; ALTER email_messages DROP COLUMN signal_status; Cron signal-extract in Coolify deaktivieren.

### REL-011 — V4.2 Wissensbasis Cross-Source
- Date: 2026-04-18
- Scope: 6 Slices (SLC-421..426), 1 Feature (FEAT-401), 8 V4.2-Backlog-Items (BL-350..357). MIG-014 pgvector Extension + knowledge_chunks Tabelle (vector(1024), HNSW-Index). MIG-015 search_knowledge_chunks RPC-Funktion. RAG-Pipeline (pgvector + Bedrock Titan Embeddings V2 eu-central-1). 4 Datenquellen: Meeting-Transkripte, E-Mails, Deal-Activities, Dokumente. Chunking-Service (quelltypspezifisch, Sentence-Boundary), Embedding-Adapter-Pattern (DEC-047), Backfill + Embedding-Sync-Cron, RAG Query API mit Scope-Filter + Confidence-Level, Deal Knowledge Query UI (Wissen-Tab mit Text + Voice), Auto-Embedding Trigger (fire-and-forget bei neuen Daten).
- Summary: Deployment als Coolify Redeploy. Gesamt-QA PASS (RPT-141, 0 Blocker, 0 High). Final-Check PASS (RPT-142, 1 Fix applied: serverExternalPackages fuer pdf-parse). Go-Live GO (RPT-143). 1 neuer Cron-Job (embedding-sync */5). Backfill: 7 Chunks (5 Activities + 2 Emails) aus Bestandsdaten. Browser-Smoke-Test PASS inkl. Voice-Input (Whisper Transkription → RAG Query → Antwort mit Quellen).
- Risks: Deutsche Embedding-Qualitaet Titan V2 erst mit mehr Live-Daten validierbar (Fallback: Cohere per DEC-048). Single-Tenant RLS Pattern (USING true). In-Memory Rate Limiter. Kernel-Upgrade snd-aloop (ISSUE-037, nicht V4.2-spezifisch).
- Rollback Notes: Rein additiv. Docker Image Rollback via Coolify auf V4.1 Image (Wissen-Tab verschwindet, alle anderen Features bleiben). Schema-Rollback: DROP FUNCTION search_knowledge_chunks; DROP TABLE knowledge_chunks; DROP EXTENSION vector;

### REL-010 — V4.1 Meeting Intelligence Basis
- Date: 2026-04-18
- Scope: 9 Slices (SLC-411..419), 3 Features (FEAT-404/409/411), 10 V4.1-Backlog-Items. MIG-011 Schema-Migration (contacts +7 Felder Consent/Opt-out, meetings +11 Felder Recording/Transcript/Summary/Agenda, activities +1 ai_generated, user_settings Tabelle neu, audit_log.actor_id nullable). Jitsi + Jibri Self-Hosted auf CPX32 (shared Infrastruktur), Whisper-Adapter-Pattern (OpenAI mit Azure/Self-hosted tauschbar), Call Intelligence Pipeline (Recording-Upload → Transkription → Summary), DSGVO-Einwilligungsflow (Public-Consent-Pages, IP-Hash, Audit), Meeting-Erinnerungen (extern .ics + intern SMTP/Push), Browser-Push + Service Worker, KI-Agenda (on-click + auto via Bedrock).
- Summary: Deployment als einzelner Coolify Redeploy. Gesamt-QA PASS (RPT-128, 5 Medium, 0 Blocker). Final-Check READY (RPT-129, 0 High). npm audit fix inline (0 vulnerabilities). 4 neue Cron-Jobs (meeting-recording-poll, meeting-transcript, meeting-summary, meeting-reminders) + 2 bestehende erweitert (pending-consent-renewal, recording-retention). 7 Blocker waehrend SLC-412 Jitsi-Deployment geloest (dokumentiert in jitsi-jibri-deployment Rule). Live-Tests: Jitsi Meeting, Jibri Recording, Whisper Transkription, Bedrock Summary + Agenda — alle PASS.
- Risks: OpenAI Whisper geht ueber US-API (akzeptiert fuer Internal-Tool, Azure-Migration in V4.2 geplant, DEC-035). Transcript/Summary permanent gespeichert (DEC-043, V4.2 Retention-Policy bei Kundendaten). VAPID_SUBJECT zeigt auf falsches Mailto (ISSUE-038, Coolify-ENV-Fix). Kernel-Upgrade-Risiko fuer snd-aloop (ISSUE-037). Public-Revoke-Link nach Grant funktionslos (ISSUE-033, V4.2).
- Rollback Notes: Forward-only empfohlen. Jitsi-Stack kann unabhaengig deaktiviert werden (Services in docker-compose auskommentieren). Schema-Rollback: ALTER audit_log ALTER actor_id SET NOT NULL; DROP TABLE user_settings; ALTER contacts/meetings/activities DROP V4.1-Spalten. V4 Image als Fallback via Coolify.

### REL-009 — V4 KI-Gatekeeper + Externe Integrationen
- Date: 2026-04-14
- Scope: 9 Slices (SLC-401..409), 6 Features (FEAT-403/405/406/407/408/410), 7 V4-Backlog-Items. MIG-010 Schema-Migration (5 neue Tabellen: email_messages, email_threads, email_sync_state, ai_action_queue, ai_feedback; calendar_events erweitert um source/external_id/sync_status/booking_link). IMAP-Sync (IONOS direkt), E-Mail-Inbox UI, Gatekeeper-Klassifikation (Bedrock), KI-Wiedervorlagen mit Freigabe, Auto-Reply-Detection, Cal.com Self-Hosted + Webhook-Sync, Gesamtkalender UI, Management-Cockpit LLM-Ausbau (5 Preset-Analysen + Freitext). Zusaetzlich: Mein Tag/Focus UI-Reorganisation (KI-Wiedervorlagen als Tab, Action-Karten unter Focus), KI-Analyse Branding entfernt, 3 Revalidation-Fixes in Cron/Webhook-Routen.
- Summary: Deployment in zwei Phasen. Phase 1 (2026-04-12): SLC-401..403 live mit IMAP + Inbox + Cron-Jobs (classify, followups, imap-sync, retention). Phase 2 (2026-04-14 abends): SLC-404..409 via Coolify Redeploy. Gesamt-QA PASS (52/53 AC, RPT-108). Final-Check Conditionally ready (RPT-109). Go-Live GO mit Smoke-Tests PASS (Login, IMAP-Inbox, Mein Tag, Kalender, KI-Analyse, Focus). MEETING-WORKFLOW.md als Praxis-Anleitung erstellt.
- Risks: CALCOM_API_KEY bewusst leer (Cal.com AGPLv3 bietet keine API-Keys). calcom-sync Cron ist deshalb No-Op (501). Webhook-Signatur-Header bei erster realer Buchung noch zu verifizieren. In-Memory LLM-Cache (1h TTL) verliert bei Container-Restart. 179 Lint-Findings (any/unused) als Tech-Debt fuer V5. Next.js 16 Middleware-Deprecation (funktional OK).
- Rollback Notes: Docker Image Rollback via Coolify. V3.3 Image als Fallback. MIG-010 Rollback: DROP ai_feedback, ai_action_queue, email_sync_state, email_messages, email_threads; ALTER calendar_events DROP source, external_id, sync_status, booking_link.
