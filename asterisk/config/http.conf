; =============================================================
; HTTP Configuration — Strategaize Business System V5.1
; WebSocket for SIP.js on port 8089
; TLS termination at Traefik — Asterisk serves plain HTTP/WS
; =============================================================

[general]
enabled = yes
bindaddr = 0.0.0.0
bindport = 8089
