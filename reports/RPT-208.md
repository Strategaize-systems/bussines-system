---
id: RPT-208
date: "2026-04-25"
skill: /qa
slice: SLC-521
feature: FEAT-521
type: completion
status: completed
reviewOf: RPT-207
result: "PASS — alle 7 AC verifiziert (Code-Review + ENV-Override + Cutoff-Berechnung + Activities-Schutz). TypeScript clean, ESLint clean fuer geaenderte Files. SSH-DB-Check: 0 Recordings im 7-30d-Bereich → Deploy ist risikofrei (kein einmaliger Recording-Loesch-Schwall beim ersten Cron-Run). Stub-Detection: keine Findings."
title: "QA: SLC-521 — Recording-Retention 7d Hardening"
---

# RPT-208 — QA SLC-521 Recording-Retention 7d Hardening

## QA Scope

**Reviewed:** SLC-521 Backend-Implementation (Commit `e8b6d35`)

- 6 geaenderte Files + 2 neu angelegte Files
- 7 Acceptance Criteria (AC1..AC7)
- Stub-Detection auf `recording-retention/route.ts`
- TypeScript-Type-Check
- ESLint-Check (gefiltert auf geaenderte Files)
- SSH-Production-DB-Check zur Risk-Mitigation (0 destructive Operations)

**Delivery Mode:** Internal Tool — pragmatic QA proportional zum Risiko.

## Result

**PASS** — alle 7 AC verifiziert, keine Blocker, keine High-Findings.

## AC-Verification

| AC | Beschreibung | Verifikation | Status |
|----|--------------|--------------|--------|
| AC1 | route.ts Default `"7"` | Line 14 grep: `process.env.RECORDING_RETENTION_DAYS \|\| "7"` | ✓ Pass |
| AC2 | docker-compose.yml `:-7` | Line 75 grep: `RECORDING_RETENTION_DAYS: ${...:-7}` | ✓ Pass |
| AC3 | ENV-Override funktioniert | Code-Logik: `process.env.RECORDING_RETENTION_DAYS \|\| "7"` — wenn ENV gesetzt, wird Wert verwendet. parseInt mit fallback. Override `=14` -> retentionDays=14 -> cutoff=now-14d | ✓ Pass (code review) |
| AC4 | Retention-Cron loescht alte Recordings | Cron-Logik unveraendert: SELECT recordings WHERE recording_started_at < cutoff -> Storage-Removal -> recording_url=null + recording_status=deleted -> Audit-Log. Nur Default-Wert geaendert. | ✓ Pass |
| AC5 | Activities/Summary bleiben erhalten | Code touche AUSSCHLIESSLICH `recording_url` und `recording_status`. NIEMALS `activities`-Tabelle, NIEMALS `ai_summary`-Felder, NIEMALS `transcript`-Felder. Code-Comment Line 53 bestaetigt: "Transcript + ai_summary remain permanently (DEC-043)". | ✓ Pass |
| AC6 | .env.example DSGVO-Kommentar | Datei enthaelt expliziten Hinweis auf "DSGVO Art. 5 Abs. 1 lit. e (Datenminimierung)" + Begruendung "Ab V5.2 Default auf 7 Tage reduziert (vorher 30)" + Erklaerung "Activities, AI-Summary und Transkript bleiben permanent in der DB". | ✓ Pass |
| AC7 | Cron-Cutoff-Berechnung korrekt | Line 21-22: `cutoff = now - retentionDays days`. Line 29: `.lt("recording_started_at", cutoff.toISOString())`. Bei retentionDays=7 + now=2026-04-25: cutoff=2026-04-18T00:00. Recordings mit started_at < 2026-04-18 werden geloescht. Recording mit started_at = 2026-04-18 (genau 7d alt) bleibt. Recording mit started_at = 2026-04-17 (8d alt) wird geloescht. | ✓ Pass |

## Stub Detection

**Result:** Keine Findings.

| Pattern | Hits | Severity |
|---------|------|----------|
| TODO/FIXME/HACK/XXX | 0 | — |
| Empty handlers / catch-empty | 0 | — |
| Hardcoded credentials | 0 | — |
| Dummy data | 0 | — |
| `console.*` calls | 4 | **Acceptable** |

**Console-Calls Review (Line 45, 113, 158, 180):**
- Line 45 + 113: `console.warn(...)` — Storage-Removal-Warnings beim Loeschen aus Supabase Storage. Production-tauglich (Operational-Logging fuer Faelle wo Storage-File nicht mehr existiert).
- Line 158: `console.log(...)` — Final completion log mit Cutoff + Counts. Production-tauglich (Operational-Monitoring fuer den Cron-Lauf).
- Line 180: `console.error(...)` — Fatal error log. Production-tauglich (Error-Tracking).

Alle 4 sind **bewusst** als Operational-Logging gesetzt, kein Debug-Noise.

## Static Verification

| Check | Result | Notes |
|-------|--------|-------|
| TypeScript (`npx tsc --noEmit`) | clean | Bereits in /backend verifiziert |
| ESLint (gefiltert auf geaenderte Files) | clean | Keine Errors/Warnings in `recording-retention/route.ts` |
| `npm run test` | nicht verfuegbar | Es gibt kein Test-Script in package.json (Internal-Tool, kein Vitest-Setup) |

**Pre-existing Lint-Issues:** Beim Lauf von `npm run lint` werden 221 Probleme (166 errors, 55 warnings) in anderen Files gemeldet (kpi-queries.ts, chunker.ts, process-check.ts u.a.). Diese sind **nicht** durch SLC-521 entstanden — sie existierten vorher und sind out-of-scope fuer diesen Slice. Empfehlung: separater Backlog-Eintrag fuer Lint-Cleanup, falls gewuenscht.

## SSH-Production-DB-Check (Risk-Mitigation)

**Anlass:** Slice-Spec markiert als Risk: "Bestehende Recordings (zwischen 7 und 30 Tagen alt) werden beim ersten Cron-Run nach Deploy aktiv geloescht."

**Query:** Read-only SELECT auf Production-DB ueber SSH (per `sql-migration-hetzner.md` Pattern — `docker exec` als `postgres` Superuser).

**Ergebnis:**

| Source | will_be_deleted_7d_to_30d | keep_under_7d | total_with_recording |
|--------|---------------------------|---------------|----------------------|
| meetings | 0 | 0 | 0 |
| calls | 0 | 1 | 1 |

**Auswertung:** **Risk tritt nicht ein.** Es gibt aktuell keine Recordings im 7-30d-Loesch-Bereich. Der einzige Recording (1 Call) ist <7 Tage alt und bleibt. Beim ersten Cron-Run nach Deploy wird **kein** Recording aktiv geloescht.

Hintergrund: V5.1 wurde gestern (2026-04-24) released, im Internal-Test-Mode. Es gab seitdem nur 1 Test-Call mit Aufnahme. Echte externe Calls/Meetings starten erst nach V5.2-Deploy.

## Wiring Verification

**Nicht anwendbar** — SLC-521 ist Backend-only (Default-Wert + Doku), keine Frontend-Komponenten und keine API-zu-UI-Datenflusse.

## Findings

### Blocker
keine

### High
keine

### Medium
keine

### Low
1. **Pre-existing Lint-Issues (out-of-scope):** 221 ESLint-Probleme in anderen Files (V2..V6.1-Code). Nicht durch SLC-521 verursacht. Empfehlung: ggf. eigener Backlog-Eintrag (Tech-Debt-Slice) — nicht V5.2-Scope.

## Open Risks / Missing Checks

- **Live-Cron-Smoke-Test gegen Production-Endpoint:** Nicht durchgefuehrt, da der Cron-Endpoint bei tatsaechlichem Aufruf ECHT loescht. SSH-DB-Check zeigt jedoch, dass nichts zu loeschen ist (siehe oben). Erst-Cron-Run nach Deploy wird automatisch via taeglichem Coolify-Cron um 04:00 UTC laufen — Logs koennen dann beobachtet werden.
- **ENV-Override-Test mit `RECORDING_RETENTION_DAYS=14`:** Nicht live getestet, nur per Code-Review verifiziert. Code-Pfad ist trivial (`parseInt(env || "7", 10)`) — kein realistisches Risiko.
- **Vollstaendige `.env.example`:** Aktuell nur V5.2-Bloecke. Falls als Onboarding-Dokumentation gewuenscht: separates Backlog-Item.

## Readiness Assessment

SLC-521 ist **ready for deploy** als Teil von V5.2.

- Code-Review aller Pfade: stimmig
- Activities-Schutz: technisch und durch Code-Comment dokumentiert
- DSGVO-Begruendung: in .env.example und ARCHITECTURE.md verankert
- Risk-Mitigation: Production-DB-Check zeigt 0 betroffene Recordings beim Deploy

**Vorsicht:** Der Cron-Endpoint wird nicht live in dieser QA aufgerufen. Erste echte Pruefung erfolgt durch den taeglichen Coolify-Cron-Run um 04:00 UTC nach dem naechsten Deploy. Empfehlung: User soll am Tag nach Deploy die Cron-Logs in Coolify oder via SSH (`docker logs` auf app-Container) pruefen.

## Project record updates

- `/docs/STATE.md`: keine Aenderung — Phase bleibt "implementing", SLC-521 bereits als done markiert in /backend
- `/docs/KNOWN_ISSUES.md`: keine neuen Issues — Pre-existing Lint-Issues sind kein V5.2-Defekt
- `/slices/INDEX.md`, `/planning/backlog.json`: bereits in /backend aktualisiert (status `done`)
- `/reports/`: RPT-208 angelegt (dieser Report)

## Recommended next step

**`/backend SLC-522`** (Azure-Whisper-Adapter Implementierung)

- ~1.5 Tage Aufwand
- Kein DB, aber Adapter + Tests
- Kann sofort starten — keine Dependencies zu SLC-521
- Aktivierung NICHT in V5.2 (kein Azure-Account) — Code-Ready ja, ENV-Switch dokumentiert

Optional vorher: User-Entscheidung ob die Pre-existing Lint-Issues als separater Backlog-Eintrag (Tech-Debt-Slice) erfasst werden sollen.
