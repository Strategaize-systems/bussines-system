---
id: RPT-649
date: "2026-06-12"
skill: /qa
slice: SLC-913
feature: FEAT-925
type: review
status: reviewed
reviewOf: RPT-648
result: "MT-5 /qa PASS — 0B/0H/0M/4 Low (1 davon im QA gefixt: Help-Doku nannte noch EXPORT_API_KEY). AC-913-5 vollstaendig, Live-Smoke deferred zu /deploy"
title: "V8.15 SLC-913 MT-5 /qa — Public-Endpoint-Hardening (ISSUE-110/118/119)"
---

# V8.15 SLC-913 MT-5 — /qa Public-Endpoint-Hardening

## 1. QA Scope

Review von RPT-648 (Commit-Stand nach feat(SLC-913/MT-5)): die drei Endpoint-Haertungen (`test-sentry`, `leads/intake`, `push/subscribe`), der `lib/export/auth.ts`-Refactor, der neue `lib/push/endpoint-allowlist.ts`-Helper, send.ts-Re-Check, publicPaths, `.env.example`.

## 2. Verdict: **PASS** (0 Blocker / 0 High / 0 Medium / 4 Low — davon 1 im QA direkt gefixt)

### AC-913-5 Punkt fuer Punkt

| AC-Teil | Ergebnis |
|---|---|
| test-sentry in Prod nicht anonym erreichbar | ✓ doppelt: Handler-404-Gate (`NODE_ENV==='production'`) + aus publicPaths entfernt (anonym → Login-Redirect schon in der Middleware). Test beweist 404 + kein captureException. sentry-State-Feld gedroppt (grep: 0 Treffer publicPaths, isSentryEnabled-Import raus) |
| leads-intake eigener Key + Rate-Limit + Caps | ✓ `verifyLeadIntakeApiKey` (timing-safe erhalten), **Key-Separation testbewiesen** (EXPORT_API_KEY → 401), `checkRateLimit` nach Auth (429-Pass-Through-Test ohne DB-Touch), 12 Felder-Caps mit Feldname-Fehler + Grenzwert-Test exakt am Cap |
| push/subscribe rejected interne/non-allowlist endpoints | ✓ Allowlist-only (https, kein Credential/Port, FCM/APNs exakt + Mozilla/WNS Suffix mit fuehrendem Punkt — Suffix-Bypass `evilpush.…` testbewiesen rejected), 400 ohne DB-Write; Re-Check in send.ts faengt Alt-Subscriptions |
| Vitest je Endpoint | ✓ 4 + 13 + 6 Route-Tests + 33 Helper-Adversarial = 56 Assertions in 50 Tests, TDD RED-Lauf (20 fail) dokumentiert |

### Verifikation (live ausgefuehrt in diesem /qa)

- Slice-Tests Re-Run: **50/50 GREEN**.
- **Regression auth.ts-Refactor:** `__tests__/lib/sec-891-timing-safe.test.ts` (ausserhalb src-include, via temp. Config ausgefuehrt) **7/7 GREEN** — `verifyExportApiKey`-Verhalten inkl. Fehlermeldungs-String nach Generalisierung identisch.
- Wiring-Grep: `verifyExportApiKey`-Konsumenten unveraendert (campaigns/performance, winloss, export-helpers); intake ist der EINZIGE `verifyLeadIntakeApiKey`-Konsument; `isAllowedPushEndpoint` in genau subscribe-Route + send.ts.
- Stub-Scan der 6 Production-Files: 1 Treffer = pre-existing `console.log` in send.ts (V4-Code, siehe L-2).
- Gates (aus RPT-648, Code-Stand identisch): TSC=0, ESLint beruehrte Files=0, Full-Vitest **1585/1585**.

## 3. Findings

#### Low
- **L-1 (GEFIXT im QA): Help-Doku-Drift** — `cockpit/src/content/help/kampagnen.md:55` nannte weiter `Bearer EXPORT_API_KEY` fuer den Intake; jeder, der der Doku folgt, bekaeme nach Deploy 401. Im QA auf `LEAD_INTAKE_API_KEY` + Hinweis korrigiert.
- **L-2 (pre-existing, akzeptiert):** `lib/push/send.ts:83` nutzt rohes `console.log` statt `logSafe` (Site stammt aus V4/SLC-418, vor SLC-907-Logger-Redaction; loggt nur userId+statusCode, kein Secret/PII). Kandidat fuer logSafe-Sweep, kein MT-5-Scope.
- **L-3 (pre-existing, akzeptiert):** `validateInput` castet Pflichtfelder hart (`(b.first_name as string).trim()`) — ein non-string-Wert (z.B. `first_name: 123`) wirft TypeError → 500 statt 400. Bestand vor MT-5; die neue MAX_LEN-Schleife selbst type-checkt sauber (`typeof === "string"`). Kein neuer Vektor (kein Crash-Loop, ein Request = ein 500).
- **L-4 (Beobachtung, akzeptiert):** push/subscribe speichert das komplette Subscription-Objekt ohne Groessen-Cap in `user_settings.push_subscription` (JSONB, eigene Row, authenticated-only). Theoretischer Bloat-Vektor gegen die eigene Row — kein Security-Issue.

## 4. Open Risks / Missing Checks

- **Live-Smoke deferred zu /deploy V8.15** (Standard-Praezedenz): Prod-404 auf test-sentry, 401/429 auf intake, 400 auf subscribe gegen die deployte Instanz.
- **Deploy-Pflicht-ENV:** `LEAD_INTAKE_API_KEY` in Coolify setzen + externe Intake-Caller (System 4) auf neuen Key umstellen — sonst 500 (unkonfiguriert) bzw. 401 (alter Key). In ISSUE-118-Resolution + RPT-648 dokumentiert.
- Rate-Limit-IP-Extraktion bleibt XFF-links (spoofbar) — bewusst MT-6/MT-7-Scope.

## 5. Readiness

MT-5 ist code-side ready. ISSUE-110/118/119 resolved (Code-Side; Live-Bestaetigung im /deploy-Smoke). 5/8 MTs done.

## 6. Next workflow step

`/backend SLC-913 MT-6` (Login-Rate-Limit XFF-Haertung, ISSUE-120) — **frische Session empfohlen**, da R-913-3 einen Live-SSH-Check (91.98.20.191) erfordert, wie Traefik/Kong XFF setzt. Danach MT-7 (offene Founder-Frage Export-Keys) + MT-8 Records-Sync → Gesamt-/qa SLC-913 → /final-check → /go-live → /deploy (MIG-052-Apply + Live-Smokes, gated auf V8.14 STABLE ~2026-06-13) → /post-launch.
