---
id: RPT-615
date: "2026-06-10"
skill: /qa
slice: SLC-911
feature: FEAT-923
type: review
status: reviewed
reviewOf: RPT-614
result: "SLC-911 V8.12 Sentry-Observability /qa PASS-CODE-SIDE — 8/8 ACs Code-Side erfuellt (AC-911-2 Live-Portion PENDING bis Deploy), 0 Blocker / 0 High / 0 Medium / 2 Low (Endpoint-Deviation + Client-Capture-ohne-withSentryConfig, beide accepted). Wiring-Chain verifiziert (instrumentation→config→beforeSend→redactSecrets, test-endpoint→wrapper), Stub-Detection CLEAN, Re-Run TSC=0/ESLint=0/Vitest 1367/1367/next build OK. Slice bleibt in_progress bis Live-Smoke post-Deploy."
title: "V8.12 SLC-911 Sentry-Observability /qa Review Report"
---

# RPT-615 — V8.12 SLC-911 Sentry-Observability /qa Review Report

reviewOf: RPT-614 (/backend Completion). Delivery-Mode: internal-tool.

## 1. QA scope reviewed

- 3 Sentry-Configs `cockpit/sentry.{server,client,edge}.config.ts`
- `cockpit/src/instrumentation.ts` (register-Hook)
- `cockpit/src/lib/monitoring/sentry.ts` (Wrapper) + `redact-event.ts` (beforeSend)
- `cockpit/src/lib/monitoring/__tests__/{sentry,redact-event}.test.ts` (9 Tests)
- `cockpit/src/app/global-error.tsx`, `cockpit/src/app/api/test-sentry/route.ts`
- `cockpit/src/lib/supabase/middleware.ts` (publicPath)
- Slice-Spec `slices/SLC-911-v812-sentry-observability.md`, DEC-277

## 2. Result

**PASS (Code-Side).** 8/8 ACs Code-Side erfuellt; die **Live-Portion von AC-911-2**
(Sentry-Dashboard-Event post-Deploy) ist erst nach `/deploy` verifizierbar. SLC-911 bleibt
daher **`in_progress`** (kein false-done). Kein Blocker.

## 3. Acceptance-Criteria-Matrix

| AC | Erwartung | Verifikation | Verdict |
|---|---|---|---|
| AC-911-1 | Sentry-Project Frankfurt + DSN in Coolify-ENV | Founder: 4 ENVs gesetzt, DSN-Host `o4511540039188480.ingest.de.sentry.io` = EU ✓; Configs lesen ENVs | PASS |
| AC-911-2 | Test-Error-Endpoint → Sentry-Dashboard < 1min | `/api/test-sentry?type=error` (kanonisch) + middleware-publicPath; Code-Side PASS, **Live PENDING bis Deploy** | PASS-CODE / PENDING-LIVE |
| AC-911-3 | beforeSend redactet extra/contexts/tags/user.email | `redact-event.ts` nutzt SLC-907 `redactSecrets`, 4 Vitest GREEN | PASS |
| AC-911-4 | `sendDefaultPii: false` in allen 3 Configs | 3/3 | PASS |
| AC-911-5 | `tracesSampleRate: 0.1` in allen 3 Configs | 3/3 (ENV `SENTRY_TRACES_SAMPLE_RATE`||0.1) | PASS |
| AC-911-6 | TSC=0, ESLint=0, Vitest GREEN | TSC=0, ESLint=0 (0 Warnings), 1367/1367 | PASS |
| AC-911-7 | Vitest Wrapper + beforeSend-Redact ≥3 Cases | 5 wrapper + 4 redact-event = 9 | PASS |
| AC-911-8 | App laeuft ohne SENTRY_DSN (self-warned) | `next build` ohne DSN = Compiled successfully; `dsn: process.env.*` undefined-safe | PASS |

## 4. Done-Gate-Review

| Done-Gate | Soll | Ist | Verdict |
|---|---|---|---|
| Sentry-Dashboard zeigt Test-Event post-Deploy | 1 Event | **offen bis `/deploy`** | PENDING-LIVE |
| Vitest ≥6 neue GREEN-Tests (Wrapper + redact-event) | ≥6 | 9 (5+4) | PASS |
| TSC=0, ESLint=0 | 0/0 | 0/0 | PASS |
| @sentry/nextjs in package.json + lock | vorhanden | `^10.57.0` + lock | PASS |

## 5. Wiring-Verification

- `instrumentation.register()` → `NEXT_RUNTIME=nodejs` import `../sentry.server.config`, `edge` import `../sentry.edge.config` ✓
- alle 3 Configs: `beforeSend: redactSentryEvent` ✓ → `redactSentryEvent` → `redactSecrets` (`@/lib/logger`, SLC-907) ✓
- `/api/test-sentry` → `captureException`/`captureMessage`/`isSentryEnabled` (Wrapper) → `Sentry.*` ✓
- `global-error.tsx` → `captureException` (source `global-error-boundary`) ✓
- `/api/test-sentry` in middleware `publicPaths` (unauth-erreichbar fuer Live-Smoke) ✓

## 6. Stub-Detection (mandatory)

Scan ueber alle 8 neuen Production-Files (3 configs + instrumentation + 2 monitoring + global-error +
route): **CLEAN.** Keine TODO/FIXME/HACK, kein `debugger`, kein `console.log`, keine leeren
catch-Handler, keine Placeholder-Returns, keine Hardcoded-Secrets (DSN kommt aus ENV).

## 7. Findings

### Blocker / High / Medium
- keine

### Low
- **L-1 (accepted): Endpoint-Pfad-Deviation.** `/api/test-sentry` statt Spec-`/api/_debug/throw`.
  Begruendung: kanonischer Strategaize-Pattern (BLOCKING `strategaize-pattern-reuse.md` + Memory
  `feedback_test_sentry_endpoint_pattern`). Funktional identisch zu AC-911-2. In Code-Kommentar
  + RPT-614 dokumentiert.
- **L-2 (accepted): Client-Side-Auto-Capture inaktiv** ohne `withSentryConfig` in `next.config.ts`.
  Bewusste reuse-treue Entscheidung (immoscheckheft SLC-330 faehrt produktiv identisch).
  Server-Capture (instrumentation) + global-error + Test-Endpoint decken AC-911 ab. Falls
  Browser-Error-Capture spaeter gebraucht: eigener Polish-Slice mit Turbopack-Source-Map-Setup.

## 8. Open risks / missing checks

- **AC-911-2 Live-Smoke** ausstehend (geplant `/deploy`): `GET https://business.strategaizetransition.com/api/test-sentry?type=error`
  → Event in Sentry-Issues (Projekt `business-system`, EU-Frankfurt) < 1min, Payload redacted.
- **R-911-1 (Cost-Spike):** MITIGATED — `tracesSampleRate: 0.1` (3/3 Configs).
- **R-911-2 (User-IP-DSGVO):** MITIGATED — `sendDefaultPii: false` (3/3) + beforeSend-Redact.
- **R-911-3 (DSN-Leak):** akzeptiert — Project-DSN ist designed-public (Sentry-Doc), kein Org-Auth.
- **NEXT_PUBLIC_SENTRY_DSN Build-Var** muss beim Coolify-Rebuild inlined werden (User-Schalter gesetzt).

## 9. Readiness assessment

SLC-911 ist **Code-Side ready** — sauber, wiring-vollstaendig, getestet, build-verifiziert,
beforeSend-Redact an SLC-907 angebunden. 0 Blocker/High/Medium, 2 dokumentierte Low ohne
Handlungsbedarf. Der letzte Done-Gate-Punkt (Live-Event) ist bewusst auf `/deploy` verschoben →
Slice bleibt `in_progress`. SLC-911 schaltet als Pre-Cond SLC-909 (Cost-Cap-Alert) + SLC-910
(CSP report-uri) frei.

## 10. Next workflow step

SLC-911 ist **nicht** der letzte Slice von V8.12 (SLC-908/909/910 noch `planned`) → kein
Gesamt-/qa, kein /final-check jetzt.

**Empfohlen:** `/backend SLC-908` (naechster V8.12-Slice). Danach `/backend SLC-909`
(A-V812-2 + MIG-050) → `/backend SLC-910` Phase-A Burn-In → **Gesamt-/qa V8.12** → `/final-check`
→ `/go-live REL-047` → `/deploy` (inkl. SLC-911 + SLC-907 Live-Smoke) → `/post-launch`.

Per IMP-950 BLOCKING + [[module-lifecycle-discipline]] + [[feedback-no-strategaize-live-until-all-systems-ready]]:
V8.12 NICHT released bis Workflow-Closure komplett, kein Customer-Live.
