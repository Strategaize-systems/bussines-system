---
id: RPT-276
date: "2026-05-01"
skill: /qa
slice: SLC-562
feature: FEAT-561
type: completion
status: completed
result: "Pre-Deploy QA PASS. Vitest 113/113, Build PASS, DB-CHECK 4/4, Wiring vollstaendig, 0 Stubs in SLC-562-Files. AC1-AC15+AC18-AC20 PASS, AC16+AC17 deferred zu Live-Smoke nach User-Coolify-Redeploy."
title: "SLC-562 Bedingungs-Dropdown + Skonto-Toggle — /qa Pre-Deploy"
---

## Outcome

Pre-Deploy QA fuer SLC-562 vollstaendig PASS. Code-Stand laut RPT-275 (`/backend` 8/8 MTs done, commit 422c96f). Statische und DB-seitige Verifikation komplett — Live-Smoke (AC16/AC17) wird nach User-getriggertem Coolify-Redeploy in eigener Folgesession verifiziert.

## QA Scope

- Static Code Review (Stub-Detection, Wiring-Chain)
- Test-Suite Re-Run (Vitest)
- Build + Lint
- DB-Schema + CHECK-Constraints gegen Production-DB Hetzner
- AC-Mapping (20 Acceptance Criteria)

## Stub Detection

Gescannt wurden 7 SLC-562-Files (5 NEU, 2 MODIFY) und 4 begleitende Files (workspace, types, zod, actions).

| Pattern | Funde | Severity |
|---------|-------|----------|
| TODO/FIXME (production-relevant) | 0 | — |
| Placeholder-Returns (`return null`/`{}` ohne Logik) | 0 | — |
| Empty Handlers (`catch {}`/`onClick={() => {}}`) | 0 | — |
| Console-Debug-Logs | 0 (nur produktive Diagnose-Logs) | — |
| Hardcoded Secrets/URLs | 0 | — |

**Akzeptierter Stub:** `useSkontoMutex` returnt `false` immer — explizit als SLC-563-Erweiterungs-Punkt im Slice-Spec MT-2 angekuendigt. API-Contract korrekt; SLC-563 fuegt nur Body-Logik hinzu. Kein Bug.

**Result: 0 echte Stubs. Akzeptierter Stub im Hook ist dokumentiert + zukunftsfest.**

## Wiring Verification

```
SkontoSection (UI Toggle/Inputs)
  └─> onChange(percent, days)
       └─> proposal-editor.tsx :: patchAndSave({skonto_percent, skonto_days})
            └─> proposal-workspace.tsx :: handleProposalChange (State-Sync)
            └─> debouncedPersist(500ms) :: updateProposal(proposalId, patch)
                 └─> actions.ts:449 :: zod proposalEditSchema.partial().safeParse
                 └─> actions.ts:468 :: validateSkonto(nextPercent, nextDays)  ← Cross-Field-Check
                 └─> actions.ts:502 :: UPDATE proposals SET skonto_percent, skonto_days
                      └─> DB: proposals_skonto_both_or_none CHECK (defended-in-depth)
                      └─> DB: proposals_skonto_percent_check + _days_check
                 └─> actions.ts:522 :: logAudit (nur bei Wert-Aenderung)
                 └─> revalidatePath(/proposals/[id]/edit)

PaymentTermsDropdown (UI Select)
  └─> onSelectTemplate(template.body)
       └─> proposal-editor.tsx :: patchAndSave({payment_terms: body})
            └─> updateProposal -> SET payment_terms

createProposal (neuer Draft)
  └─> SELECT body FROM payment_terms_templates WHERE is_default=true
  └─> INSERT proposals (payment_terms = template.body || DEFAULT_PAYMENT_TERMS)

generateProposalPdf
  └─> proposal-renderer.ts:352 :: if (skonto_percent !== null && skonto_days !== null)
       └─> content.push({text: 'Skonto: X,XX% bei Zahlung innerhalb Y Tagen'})
  └─> ohne Skonto: kein else-Branch, kein Whitespace -> V5.5 bit-identisch
```

**Wiring 100% intakt. Keine Brueche, keine fehlenden Endpunkte, keine Type-Drift in den Patch-Pfaden.**

## Vitest

`npm run test` — **113/113 PASS** (898ms).

11 Test-Files; SLC-562-spezifische Neuzugaenge:
- `skonto-validation.test.ts`: 11 Cases (Off-State, Edges 0.01/9.99/1/90, halbleerer State, OOR-Werte, nicht-Integer Tage)
- `proposal-renderer.test.ts`: 5 Snapshot-Cases (V5.5 bit-identisch ohne Skonto, 2.50%/7d, 9.99%/90d, 2 persistierte Snapshots)

## Build

`npm run build` — **PASS**. Alle Routen kompilieren inkl. `/proposals/[id]/edit`. Einziger Logeintrag "[insights] Failed to load pending insights: TypeError: fetch failed" ist Build-Time-Prerender-Warnung (kein Fehler, kein DB-Zugriff im Build-Container).

## Lint

`npm run lint` — Pre-existing Baseline (221 Probleme in legacy `lib/ai`, `lib/goals`, `components/ui/search-autocomplete.tsx` etc.). **0 Lint-Findings in SLC-562-Files** (skonto-validation, skonto-section, payment-terms-dropdown, use-skonto-mutex, proposal-editor, proposal-workspace, actions.ts, zod-schemas, proposal-renderer, proposal-payment).

Baseline-Aufraeumung ist kein V5.6-Scope. AC20 PASS fuer SLC-562-Files.

## DB-Smoke gegen Production-DB

Container: `supabase-db-k9f5pn5upfq7etoefb5ukbcg-115054541828` (Hetzner 91.98.20.191).

### Schema (`\d+ proposals`)

```
skonto_percent     | numeric(4,2)             | nullable
skonto_days        | integer                  | nullable
"proposals_skonto_both_or_none"  CHECK (skonto_percent IS NULL AND skonto_days IS NULL OR skonto_percent IS NOT NULL AND skonto_days IS NOT NULL)
"proposals_skonto_days_check"    CHECK (skonto_days IS NULL OR skonto_days > 0 AND skonto_days <= 90)
"proposals_skonto_percent_check" CHECK (skonto_percent IS NULL OR skonto_percent > 0::numeric AND skonto_percent < 10::numeric)
```

MIG-027 vollstaendig live.

### CHECK-Smoke (4/4 PASS)

| Case | Erwartet | Result |
|------|----------|--------|
| INSERT skonto_percent=2.0, skonto_days=NULL | both_or_none feuert | PASS |
| INSERT skonto_percent=10, skonto_days=7 | percent_check feuert | PASS |
| INSERT skonto_percent=2.0, skonto_days=91 | days_check feuert | PASS |
| INSERT skonto_percent=2.0, skonto_days=14 | akzeptiert | PASS |

### Templates (`payment_terms_templates`)

1 Eintrag: "14 Tage netto" (`is_default=true`). Stand entstand im SLC-561-Live-Smoke (User loeschte den initialen "30 Tage netto"-Seed in Step 8 und erstellte stattdessen "14 Tage netto" mit Default). Funktional korrekt — `createProposal` wird neue Drafts mit "14 Tage netto"-Body vorbefuellen.

UX-Hinweis (kein Finding): Dropdown-User sieht aktuell 2 Optionen "(eigene Eingabe)" + "14 Tage netto (Default)". User kann unter `/settings/payment-terms` weitere Templates anlegen.

## Acceptance Criteria — Status

| AC | Inhalt | Status | Evidence |
|----|--------|--------|----------|
| AC1 | Dropdown rendert mit Templates | PASS | payment-terms-dropdown.tsx + listPaymentTermsTemplates |
| AC2 | "(eigene Eingabe)" Erst-Option | PASS | CUSTOM_VALUE Konstante, handleChange-Early-Return |
| AC3 | Auswahl fuellt Textfeld | PASS | onSelectTemplate(template.body) → patchAndSave({payment_terms}) |
| AC4 | Default-Badge | PASS | `{tpl.is_default ? " (Default)" : ""}` |
| AC5 | Manueller Override moeglich | PASS | Textarea hat eigenen onChange-Pfad |
| AC6 | createProposal Default-Pre-Fill | PASS | actions.ts:236-241 SELECT body WHERE is_default=true |
| AC7 | SkontoSection mit Toggle (default off) | PASS | `isOn = skonto_percent !== null` |
| AC8 | Toggle on -> Defaults 2.0/7 | PASS | DEFAULT_PERCENT=2.0, DEFAULT_DAYS=7 |
| AC9 | Toggle off -> null | PASS | `onChange(null, null)` im toggle() |
| AC10 | useSkontoMutex([])=false | PASS | Hook returnt immer false (Stub) |
| AC11 | Validation-Block im Save | PASS | validateSkonto in updateProposal + Inline-Error in SkontoSection |
| AC12 | updateProposal persistiert Skonto | PASS | actions.ts:497 UPDATE mit ...parsed.data |
| AC13 | PDF rendert Skonto-Block | PASS | proposal-renderer.ts:352-359 + 2 Snapshot-Cases |
| AC14 | PDF ohne Skonto bit-identisch | PASS | "ohne Skonto: keine Skonto-Zeile im content" Test + DEC-120 |
| AC15 | Audit-Log bei Skonto-Update | PASS | AUDIT_RELEVANT_FIELDS enthaelt skonto_percent, skonto_days |
| AC16 | Browser-Smoke Desktop | DEFERRED | Live nach Coolify-Redeploy |
| AC17 | Browser-Smoke Mobile | DEFERRED | Live nach Coolify-Redeploy |
| AC18 | TypeScript-Build gruen | PASS | npm run build |
| AC19 | Vitest gruen | PASS | 113/113 |
| AC20 | ESLint gruen | PASS | 0 Findings in SLC-562-Files |

**18/20 PASS, 2 DEFERRED zu Live-Smoke.**

## Findings (gruppiert nach Severity)

### Blocker
- keine

### High
- keine

### Medium
- keine

### Low
- **EditorPatch-Type-Drift** (bekannter, akzeptierter Stand laut RPT-275): `proposal-editor.tsx` und `proposal-workspace.tsx` halten zwei separate `EditorPatch`-Types. Aktuell handsynchronisiert (beide enthalten `skonto_percent`/`skonto_days`). Risiko bei kuenftigen Patches eine zu vergessen. Empfehlung BL-415 fuer Refactor in V5.7+ (kein V5.6-Scope).
- **`SkontoSection`-Tooltip via `title`-Attribut** (RPT-275): kein shadcn-Tooltip-Component im V5.6-Scope. Akzeptable UX, da Toggle nur in SLC-562-Standalone nicht-disabled ist. Wird in SLC-563 echte Mutex-Bedingung haben — Tooltip-Polish dann.

## Open Risks / Verification Gaps

- AC16/AC17 (Browser-Smoke Desktop + Mobile-Tabs) sind nur Live nach Coolify-Redeploy verifizierbar. Pre-Deploy gruen, aber Browser-Rendering der Components (Select-Dropdown-Click, Switch-Toggle, Input-Reactions, Mobile-Layout) wurde nicht direkt visuell geprueft.
- Initial-Default-Pre-Fill: Code-Pfad ist klar in `createProposal` ab Zeile 236, aber ein neu angelegtes Proposal in der UI wurde noch nicht visuell verifiziert. SQL-Test-Insert allein bringt das nicht — der Auth-Pfad geht durch `supabase.auth.getUser()`.

Beide Punkte sind im Live-Smoke-Plan abgedeckt.

## Readiness Assessment

**SLC-562 ist bereit fuer Coolify-Redeploy.** Code-statisch, Test-statisch und DB-statisch alles gruen. Live-Smoke wird die letzten 2 ACs (Browser-Smoke Desktop + Mobile) bestaetigen.

Es existiert kein bekannter Bug, der eine Live-Verifikation blockieren koennte. Mutex-Hook-Stub ist explizit als SLC-563-Erweiterungspunkt vereinbart, kein Workaround.

## Recommended Next Step

1. **User triggert Coolify-Redeploy** auf Hetzner (Commit-Stand 422c96f).
2. **Live-Browser-Smoke** mit folgenden Schritten (im Anschluss als RPT-277 dokumentieren):
   - Editor `/proposals/[id]/edit` aufrufen
   - PaymentTermsDropdown: Auswahl "14 Tage netto" → Textarea-Update verifizieren
   - "(eigene Eingabe)" → Textarea bleibt unveraendert
   - SkontoSection: Toggle on → Defaults 2.0%/7d sichtbar
   - SkontoSection: Toggle on, Prozent=10 → Inline-Fehler
   - Save → reload → Skonto-Werte persistiert
   - "PDF generieren" mit Skonto → PDF zeigt "Skonto: 2,00% bei Zahlung innerhalb 7 Tagen"
   - Toggle off → Skonto-Werte clearten, naechste PDF ohne Skonto-Block
   - Mobile-Viewport: Tab "Erfassen" zeigt beide Bereiche
   - V5.5-Regression: Composing-Studio-Anhang-Picker zeigt Proposals
3. Nach Live-PASS: SLC-562 + BL-414 → done, Cockpit-Records-Update, Commit + Push.
4. Folge-Slice: **/backend SLC-563** (Split-Plan + PDF-Renderer-Erweiterung, ~5-7h).
