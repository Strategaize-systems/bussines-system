---
id: RPT-207
date: "2026-04-25"
skill: /backend
slice: SLC-521
feature: FEAT-521
type: completion
status: completed
reviewOf: null
result: "SLC-521 Recording-Retention 7d Hardening implementiert. Default RECORDING_RETENTION_DAYS auf 7 in route.ts + docker-compose.yml. .env.example angelegt mit DSGVO-Begruendung. ARCHITECTURE.md auf 5 Stellen aktualisiert. TypeScript clean, alle 4 AC erfuellt (AC1..AC4). AC5 (Activities erhalten), AC6 (.env.example DSGVO-Kommentar), AC7 (Cron-Cutoff-Verhalten) sind im /qa zu verifizieren."
title: "SLC-521 — Recording-Retention 7d Hardening (Backend)"
---

# RPT-207 — SLC-521 Recording-Retention 7d Hardening

## Outcome Summary

Slice **fully complete** auf Backend-Code-Ebene. 4 von 5 Micro-Tasks abgeschlossen (MT-1..MT-4). MT-5 (manueller Cron-Smoke-Test) gehoert in den /qa-Schritt und wird dort ausgefuehrt — ist nicht Teil von /backend per Standard-Trennung.

Default `RECORDING_RETENTION_DAYS` ist von **30 auf 7 Tage** reduziert in:
- `cockpit/src/app/api/cron/recording-retention/route.ts` (Code-Default)
- `docker-compose.yml` (ENV-Default)
- `.env.example` (neu angelegt mit DSGVO-Begruendung)
- `docs/ARCHITECTURE.md` (5 Stellen aktualisiert mit V5.2-Hinweis)

ENV-Override bleibt technisch unveraendert. Activities/AI-Summary/Transkripte sind unberuehrt — Retention-Cron-Logik selbst wurde nicht geaendert.

## Files reviewed

- `/slices/SLC-521-recording-retention-hardening.md` — Slice-Spec mit 5 MTs und 7 AC
- `/docs/STATE.md` — aktueller Phase-Stand
- `/docs/ARCHITECTURE.md` — V5.2-Section + alle Retention-Erwaehnungen via grep
- `/cockpit/src/app/api/cron/recording-retention/route.ts` — Cron-Logik
- `/docker-compose.yml` — ENV-Defaults
- `/.gitignore` — Bestaetigt: `.env.example` ist NICHT gitignored, kann committed werden
- `/planning/backlog.json` — BL-391 Eintrag fuer Status-Update
- `/slices/INDEX.md` — V5.2-Section fuer Status-Update

## Files changed

| Pfad | Aenderung |
|------|-----------|
| `/cockpit/src/app/api/cron/recording-retention/route.ts` | Line 14: `process.env.RECORDING_RETENTION_DAYS \|\| "30"` -> `\|\| "7"` (MT-1) |
| `/docker-compose.yml` | Line 75: `RECORDING_RETENTION_DAYS: ${RECORDING_RETENTION_DAYS:-30}` -> `:-7` (MT-2) |
| `/docs/ARCHITECTURE.md` | 5 Stellen aktualisiert: zentrale Recording-Retention-Section (Line ~2034) mit V5.2-Hinweis ergaenzt; ENV-Vorlagen-Beispiele (Line ~1382, ~1965) auf 7 angepasst; V5.1 Call-Recording-ENV-Section (Line ~5254, ~5302) auf 7 aktualisiert (MT-4) |
| `/slices/INDEX.md` | SLC-521 status `planned` -> `done` |
| `/planning/backlog.json` | BL-391 status `open` -> `done` |
| `/docs/STATE.md` | High-Level State `slice-planning` -> `implementing`, Current Focus + Immediate Next Steps aktualisiert |

## Files created

| Pfad | Zweck |
|------|-------|
| `/.env.example` | Beispiel-Env-Datei (gitignored: nein) mit `RECORDING_RETENTION_DAYS=7` und DSGVO-Begruendung im Kommentar (MT-3, AC6). Schlanke Vorlage mit V5.2-relevanten Bloecken (Recording-Retention + Transcription-Provider). Vollstaendige ENV-Vorlage fuer alle Variablen ist nicht in V5.2-Scope und kann spaeter bei Bedarf ergaenzt werden. |
| `/reports/RPT-207.md` | Diese Completion-Report-Datei |

## Files intentionally not changed

- `/cockpit/src/app/api/cron/recording-retention/route.ts` Retention-Logik (alle Zeilen ausser Default-Wert): bewusst unveraendert, da Slice-Scope nur Default-Reduktion vorsieht. Die Logik fuer Meeting-Retention + Call-Retention + Audit-Log + Storage-Removal bleibt 1:1 wie bisher.
- `/docs/MIGRATIONS.md`, `/docs/DECISIONS.md`: keine neuen Migrationen oder Decisions in SLC-521. Alle V5.2-DECs (DEC-083..087) und MIG-022 sind bereits im /architecture-Step persistiert.
- `/docs/KNOWN_ISSUES.md`: keine neuen Issues in dieser Slice — keine Edits noetig.
- `/docs/ARCHITECTURE.md` V4.1-Tradeoff-Tabelle (Line ~2049 "30 Tage Recording-Retention DEC-043"): historische V4.1-Aussage bewusst nicht geaendert — Tradeoff-Tabellen dokumentieren den damaligen Stand, V5.2-Aenderung ist im V5.2-Block referenziert.

## Problems found

Keine im engeren Sinne. Hinweise:

1. **`.env.example` existierte nicht** vor dieser Slice. Die Slice-Spec hatte das antizipiert ("falls nicht vorhanden — sonst aktualisieren"). Ich habe eine schlanke V5.2-fokussierte Variante angelegt, nicht eine vollstaendige Template-Datei mit allen ~30 ENVs aus docker-compose.yml. Begruendung: Scope-Disziplin — eine Komplett-Vorlage waere Out-of-Scope von SLC-521. Falls eine vollstaendige `.env.example` gewuenscht ist, kann das spaeter als separates Backlog-Item nachgereicht werden.

2. **5 Doku-Erwaehnungen statt 1:** Slice-Spec MT-4 sprach nur von "Doku-Zeile zur 7d-Default-Begruendung". Ein vollstaendiger grep zeigte 5 weitere Stellen in ARCHITECTURE.md mit `RECORDING_RETENTION_DAYS=30` als Beispiel oder Aussage. Alle wurden aktualisiert, weil sonst die Doku selbst-widerspruechlich gewesen waere. V4.1-Tradeoff-Tabelle (historische Aussage) wurde explizit unveraendert gelassen.

3. **MT-5 manueller Cron-Smoke-Test:** Wird im /qa-Step durchgefuehrt, nicht in /backend. Slice-Spec hat MT-5 selbst mit "(QA)" annotiert. Verifikation per Cron-Endpoint-Aufruf gegen Staging-DB mit ENV-Override muss in /qa erfolgen.

## Open points / deferred decisions

- **Bestehende Recordings 7-30 Tage alt:** Beim ersten Cron-Run nach Deploy werden 8-30 Tage alte Recordings aktiv geloescht. Das ist gewuenscht, aber im Deploy-Plan zu kommunizieren. Slice-Spec markiert das bereits unter "Risks" -> Mitigation. Empfehlung fuer /qa: Pre-Deploy einmalig manuell pruefen, ob noch Recordings im Bereich 8-30d existieren, bevor Cron-Run greift.
- **Vollstaendige `.env.example`:** Aktuell nur V5.2-relevante Bloecke. Falls als separater Slice gewuenscht, in Backlog aufnehmen. Nicht V5.2-Scope.

## Project record updates

- `/slices/INDEX.md` — SLC-521 Status `planned` -> `done` ✓
- `/planning/backlog.json` — BL-391 Status `open` -> `done` ✓
- `/docs/STATE.md` — High-Level State `slice-planning` -> `implementing`, Current Focus auf SLC-521 done + naechster /qa Schritt, Immediate Next Steps neu sortiert ✓
- `/planning/roadmap.json` — V5.2 Status: bereits `active` aus /requirements-Step, keine Aenderung noetig
- `/docs/KNOWN_ISSUES.md` — keine neuen Issues, keine Aenderung
- `/docs/MIGRATIONS.md` — keine neuen Migrationen, keine Aenderung
- `/docs/DECISIONS.md` — keine neuen DECs, keine Aenderung

## Recommended next step

**`/qa` SLC-521** (mandatory per workflow rules)

- AC5 verifizieren: Activities + AI-Summary bleiben nach Cron-Run unangetastet
- AC6 verifizieren: `.env.example` enthaelt DSGVO-Kommentar mit Begruendung
- AC7 verifizieren: Cron-Run mit Test-Daten zeigt korrekte Cutoff-Berechnung (>=8 Tage alt -> loeschen)
- ENV-Override pruefen: `RECORDING_RETENTION_DAYS=14` produziert 14d-Cutoff
- Optional: Smoke-Test gegen Staging-DB

Nach /qa SLC-521: **/backend SLC-522** (Azure-Whisper-Adapter, ~1.5 Tage, parallel-faehig zu spaeteren Slices).
