---
id: RPT-286
date: "2026-05-03"
skill: /go-live
slice: V5.6
feature: FEAT-561+FEAT-562
type: completion
status: completed
reviewOf: RPT-285
result: "CONDITIONAL GO. Code-side READY (Image a7b787d live, alle Endpoints PASS, Stack 14/15 healthy). Eine ausstehende User-Aktion: Coolify-Cron meeting-briefing Anlage (sonst keine Auto-Briefings). Nach Cron-Anlage: /deploy V5.6 als REL-022."
title: "/go-live V5.6 — CONDITIONAL GO (User-Cron-Anlage Pflicht-Schritt)"
---

# /go-live V5.6 — CONDITIONAL GO

## Release Target

**REL-022 — V5.6 Zahlungsbedingungen + Pre-Call Briefing**
- 4 Slices SLC-561..564, FEAT-561+FEAT-562
- MIG-027 (5 Schema-Aenderungen, idempotent)
- Image-Tag `a7b787d` deployed auf `business.strategaizetransition.com`
- Internal-Test-Mode aktiv

## Decision: **CONDITIONAL GO**

Code-Side ist vollstaendig live und verifiziert. Eine einzige ausstehende User-Aktion verhindert "production-fully-live" Status: **Coolify-Cron `meeting-briefing` muss in Coolify-UI angelegt werden.** Ohne diesen Cron-Job ist der Briefing-Endpoint zwar funktional, aber kein Auto-Trigger erfolgt — User muesste Cron manuell triggern. Nach User-Cron-Anlage: GO.

## Pre-Release Stack-Verifikation

### Container-Health (24 Min uptime)

| Service | Status |
|---------|--------|
| app | ✓ healthy |
| supabase-kong | ✓ healthy |
| supabase-db | ✓ healthy |
| supabase-meta | ✓ healthy |
| calcom + calcom-db | ✓ healthy |
| jitsi-web/jvb/jibri/jicofo/prosody | Up (no healthcheck defined — akzeptiert) |
| supabase-auth/rest/storage | Up (no healthcheck defined — akzeptiert) |
| asterisk | Up (no healthcheck defined — akzeptiert) |
| supabase-studio | ⚠ unhealthy (Carryover, NICHT produktiv genutzt) |

**14 Services produktiv healthy oder Up. supabase-studio Unhealthy = Carryover-Issue, nicht V5.6-induziert, nicht release-blockierend (Self-Hosted-DB-UI nicht im Code-Pfad).**

### Public-HTTPS-Endpoint
- `https://business.strategaizetransition.com/` → 307 redirect zu `/login` ✓ (Auth-gated, korrekt)

### Cron-Endpoint Final-Smoke

| Endpoint | Header | HTTP | Body |
|----------|--------|------|------|
| POST `/api/cron/meeting-briefing` | (kein) | 401 | `{"error":"Unauthorized"}` |
| POST `/api/cron/meeting-briefing` | valid | 200 | `{"success":true,"processedCount":0,"skippedCount":0,"failedCount":0,"retryCount":0}` |
| POST `/api/cron/expire-proposals` (V5.5 Regression) | valid | 200 | `{"success":true,"expiredCount":0,"expiredIds":[]}` |

✓ Endpoint funktional. V5.5-Cron unbetroffen.

## Blockers
Keine.

## Required Action vor /deploy

### EINE User-Aktion: Coolify-Cron `meeting-briefing` Anlage

Anleitung im Detail in `docs/RELEASES.md` REL-022 Schritt 1. Kurzform:

**In Coolify-UI:**

| Feld | Wert |
|------|------|
| Name | `meeting-briefing` |
| Schedule | `*/5 * * * *` (alle 5 Min) |
| Container | `app` |
| Command | `node -e 'fetch("https://business.strategaizetransition.com/api/cron/meeting-briefing", { method: "POST", headers: { "x-cron-secret": process.env.CRON_SECRET } }).then(r => r.text()).then(console.log)'` |

**Sicherheits-Hinweise:**
- `process.env.CRON_SECRET` IMMER referenzieren — niemals Klartext im Command
- Nur **EIN** Cron mit diesem Namen anlegen (Duplikate erzeugen Doppel-Audit-Eintraege)
- Auth-Header ist `x-cron-secret`, NICHT `Authorization: Bearer`

**Verifikations-Smoke nach Anlage** (innerhalb 5-10 Min):
- Coolify-Cron-Logs zeigen Cron-Execution
- `[Cron/MeetingBriefing] No candidates` oder `[Cron/MeetingBriefing] Done. processed=N` im app-Container-Log

## Optional vor /deploy (nicht release-blockierend)

1. **User-Subscribe** via `/settings/briefing` "Browser-Push aktivieren" → ermoeglicht echten Push-Test post-Deploy
2. **Test-Daten-Cleanup** auf Hetzner (DELETE-Statements aus RPT-283 Zeilen 203-207) — Meeting + Activity + Audit-Eintrag aus E2E-Smoke

## Accepted Risks (alle Carryover oder V5.7+)

Aus RPT-285 uebernommen, alle weiterhin gueltig:
1. 221 pre-existing Lint-Errors (Tech-Debt-Slice V5.7+)
2. supabase-studio unhealthy (nicht produktiv genutzt)
3. Disk 80% (Post-Launch-Monitor)
4. npm audit 5 Vulns (alle Carryover)
5. verifyCronSecret nicht constant-time (V7-Multi-User IMP)
6. ISSUE-049 BL-419 Skonto-UI-Drift (Cosmetic, V5.7+)
7. ISSUE-042 OpenAI-Key (V4.1/V5.1, nicht V5.6-relevant)
8. ISSUE-035/036/037 Carryover-Infra
9. AC18 Browser-Smoke = User-bestaetigt PASS

## Operational Concerns

### Pre-Production-Compliance-Gate
Aktiv geplant fuer V7+. Nicht V5.6-Blocker. Internal-Test-Mode bleibt aktiv bis:
- Anwalts-Pruefung COMPLIANCE.md
- Switch auf Azure-EU-Whisper
- ISSUE-042 OpenAI-Key-Rotation

### Disk-Monitoring
80% Disk-Usage auf Hetzner-Root. Briefing-Activity-JSON-Daten + Recordings tragen graduell zu Wachstum bei. Empfehlung Post-Launch:
- Alerting bei 90%
- Cleanup-Cron-Slice fuer alte Activities/Recordings in V5.7+

### Coolify-Cron-Inventar nach REL-022
Neue Cron-Liste:
- `email-sync` (V4.0)
- `signal-extraction` (V4.3)
- `meeting-reminders` (V4.1)
- `cadence-execute` (V5.0)
- `recording-poll` (V5.1)
- `recording-retention` (V5.2)
- `embedding-sync` (V4.2)
- `kpi-snapshot` (V6.0)
- `expire-proposals` (V5.5)
- `meeting-briefing` (**V5.6 NEU — User-Anlage Pflicht**)

10 Cron-Jobs total nach V5.6 Go-Live.

## Recommended Next Step

**1. User-Aktion: Coolify-Cron `meeting-briefing` anlegen** (Detail-Anleitung in RELEASES.md REL-022 Schritt 1).

**2. Bestaetigung an mich:** "Cron angelegt" + ggf. erste Cron-Log-Zeile.

**3. Danach: `/deploy V5.6 als REL-022`** — Records-Update REL-022 von `planned` auf released-Status mit Cron-Liste-Update + finaler Verifikations-Smoke.

**4. Nach 24-48h Stable-Window: `/post-launch V5.6`**.
