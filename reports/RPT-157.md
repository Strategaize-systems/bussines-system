---
id: RPT-157
date: "2026-04-20"
skill: /qa
slice: SLC-601
feature: FEAT-601
type: completion
status: completed
reviewOf: RPT-156
result: "QA PASS — Schema verifiziert (4 Tabellen, 56 Grants, alle Constraints), Code sauber (keine Stubs, kein Security-Issue), Build+TSC gruen. Browser-Test steht aus (nach Deploy)."
title: "/qa SLC-601 — Schema-Migration + Produkt-CRUD"
---

# /qa SLC-601 — Schema-Migration + Produkt-CRUD

## QA Scope

- DB-Schema auf Hetzner (4 Tabellen: products, deal_products, goals, kpi_snapshots)
- Server Actions (5 Funktionen in products.ts)
- UI-Komponenten (ProductForm, ProductList, page.tsx)
- Navigation (Sidebar)
- Build + TypeScript-Kompilierung

## Result: PASS (mit Einschraenkung)

QA ist bestanden. Alle Acceptance Criteria sind auf Code-Ebene erfuellt. Die einzige Einschraenkung ist, dass kein Browser-Smoke-Test moeglich war (erfordert Deploy + Login). Dies ist fuer den naechsten Deploy-Zyklus eingeplant.

## Findings

### DB-Schema (PASS)

| Tabelle | Spalten | Indexes | FK-Constraints | RLS | Grants |
|---|---|---|---|---|---|
| products | 9 ✓ | 3 (PK + status + category partial) ✓ | 1 (created_by → profiles) ✓ | enabled + policy ✓ | auth + service_role ✓ |
| deal_products | 7 ✓ | 4 (PK + deal + product + unique) ✓ | 2 (deal CASCADE, product RESTRICT) ✓ | enabled + policy ✓ | auth + service_role ✓ |
| goals | 12 ✓ | 6 (PK + user_period + type + status partial + product partial + COALESCE unique) ✓ | 2 (user → profiles, product → products SET NULL) ✓ | enabled + policy ✓ | auth + service_role ✓ |
| kpi_snapshots | 9 ✓ | 4 (PK + COALESCE unique + lookup DESC + date DESC) ✓ | 2 (user → profiles, product → products SET NULL) ✓ | enabled + policy ✓ | auth + service_role ✓ |

Gesamt-Grants: 56 (4 Tabellen × 7 Privileges × 2 Rollen) — korrekt.

COALESCE-Unique-Constraints auf goals und kpi_snapshots korrekt implementiert (Sentinel-UUID fuer NULL product_id).

### Stub Detection (PASS)

| Pattern | Ergebnis |
|---|---|
| TODO/FIXME | Keine gefunden |
| Placeholder returns | Nur `return []` bei fehlender Auth (korrekt) |
| Empty handlers | Keine gefunden |
| Console debugging | Keine gefunden |
| Hardcoded values | Keine gefunden |
| Commented-out code | Keiner gefunden |

### Code Quality

**Server Actions (products.ts):**
- Auth-Check in jeder Funktion ✓
- Input-Validierung (Name Pflicht) ✓
- Verwendung adminClient fuer DB-Operationen ✓
- `revalidatePath` nach Mutationen ✓
- Fehlerbehandlung mit benutzerfreundlichen Meldungen ✓
- `updated_at` wird bei Updates explizit gesetzt ✓

**UI-Komponenten:**
- useTransition fuer Pending-States ✓
- Error-State sichtbar im Dialog ✓
- Empty State in Tabelle ✓
- Status-Filter client-side (akzeptabel fuer Internal Tool mit wenigen Produkten) ✓
- Kategorie-Autocomplete mit Duplikat-Filterung ✓
- Archivieren statt Loeschen (kein DELETE) ✓
- Reaktivieren fuer archivierte Produkte ✓

### Wiring Verification

| Schritt | Ergebnis |
|---|---|
| Page → Server Actions | ✓ `listProducts()` + `listProductCategories()` in page.tsx |
| Server Actions → Supabase | ✓ `createAdminClient().from("products")` |
| Supabase → DB | ✓ Tabelle existiert, RLS + Grants korrekt |
| Response → UI | ✓ Data als Props an ProductList + ProductForm |
| Mutation → Refresh | ✓ `revalidatePath` + `router.refresh()` |
| Sidebar → Route | ✓ `/settings/products` in navGroups + verwaltungPaths |

### Acceptance Criteria

| # | Kriterium | Status |
|---|---|---|
| 1 | Migration fehlerfrei auf Hetzner | PASS (Schema verifiziert) |
| 2 | Produkte anlegen (Name Pflicht) | Code PASS |
| 3 | Produkte bearbeiten | Code PASS |
| 4 | Produkte archivieren (Status-Wechsel) | Code PASS |
| 5 | Produktliste mit Status-Filter | Code PASS |
| 6 | Kategorie-Autocomplete | Code PASS |
| 7 | Navigation in Sidebar | Code PASS |
| 8 | `npm run build` gruen | PASS |

### Build + TypeScript

- `npm run build`: PASS (0 Fehler, `/settings/products` als dynamische Route)
- `tsc --noEmit`: PASS (0 Fehler)
- `npm run test`: Kein Test-Script vorhanden (Internal Tool, kein TDD-Requirement)

## Findings by Severity

### Medium

- **Kein Browser-Smoke-Test:** Die UI wurde nicht im Browser verifiziert (Produkt anlegen, bearbeiten, archivieren, Filter, Autocomplete). Dies erfordert einen Deploy + Login. Empfehlung: Nach dem naechsten Deploy auf Hetzner manuell pruefen.

### Low

- **Migration-Nummerierung Inkonsistenz:** Slice-Definition sagt `sql/17_v6_migration.sql`, tatsaechlich erstellt als `19_v6_migration.sql`. Kosmetisch, nicht blockierend.
- **Doppelter Refresh:** `revalidatePath` in Server Action + `router.refresh()` in UI-Komponente fuehrt zu doppeltem Data-Fetch. Nicht schaedlich, aber redundant.

## Open Risks

- Browser-Verifikation steht aus. Code-Logik sieht korrekt aus, aber Render-Verhalten, Dialog-Interaktion und Autocomplete-UX sind nicht direkt geprueft.
- DialogTrigger `render={<span />}` ist ein ungewoehnliches Pattern fuer base-ui. Funktioniert laut Build, sollte aber beim naechsten Deploy im Browser bestaetigt werden.

## Readiness Assessment

**SLC-601 ist QA-bestanden und bereit fuer den naechsten Slice (SLC-602).** Die offenen Punkte (Browser-Test, Migration-Nummerierung) sind nicht blockierend. Der Browser-Test wird beim naechsten Deploy nachgeholt.

## Recommended Next Step

`/backend SLC-602` — Deal-Produkt-Zuordnung (deal_products Server Actions + UI im Deal-Workspace).
