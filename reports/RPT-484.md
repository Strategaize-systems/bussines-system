---
id: RPT-484
date: "2026-05-20"
skill: /qa
slice: SLC-813
feature: FEAT-804
type: completion
status: completed
reviewOf: RPT-483
result: "PASS-with-deferred — AC1..AC10 voll erfuellt code-side, AC11 Live-Smoke deferred bis Master-Merge + Coolify-Redeploy. 1 Medium-Finding (Spec-Inkonsistenz AC6 vs Modal-Section), 3 Low-Findings."
title: "SLC-813 /qa — Stage-Requirements-Modal + KI-Verlustgrund-Vorschlag PASS-with-deferred"
---

# QA Scope reviewed

- AC1..AC11 aus `/slices/SLC-813-stage-requirements-modal.md`
- 8 SLC-813-Files (5 NEU + 3 CHANGED) im Branch `slc-813-stage-requirements-modal` HEAD `0320b5c`
- Pre-Resume Git-State (origin/main = `7ae8d6e`, origin/slc-813 = `0320b5c`)
- Wiring-Chain Component → API → Server-Action → Supabase → Audit-Log → Modal-State → Render
- Pattern-Reuse-Konsistenz mit V7.5 SLC-752 sculptor-Pattern (`feedback_cross_project_reference`)

# Result

**PASS-with-deferred**

- AC1..AC10 (code-side) voll erfuellt.
- AC11 (Live-Smoke) explizit deferred per Workflow-Disziplin: Master-Merge + Coolify-Redeploy stehen aus (`feedback_slice_merge_at_end`, `feedback_no_coolify_branch_switch_ever`). Live-Smoke gegen Production-DB + echter Bedrock-Call kommt in Phase-2 nach Master-Merge.

# AC-Audit (AC1..AC11)

| AC | Status | Verifikation |
|---|---|---|
| AC1 — Modal vor Server-Call bei missing won_lost_reason | PASS | `kanban-board.tsx:158-170` ruft `getMissingStageRequirements` → bei `missing.length > 0` → `openRequirementsModal()` statt direkt `moveDealToStage`. Verifiziert durch Modal-Test "rendert won_lost_reason-Field" + Suggester-Test "Happy-Path". |
| AC2 — KI-Vorschlag pre-filled bei Activity-History ≥ 1 | PASS | `loss-reason-suggester.ts:108-200` activities.length≥1 → invokeLLM → success-Pfad liefert `primary` + `alternatives`. `stage-requirements-modal.tsx:303-326` buildInitialValues setzt `result.won_lost_reason = kiSuggest.primary` bei leerem Feld. Verifiziert durch Suggester-Test + Modal-Test "KI-Pre-Fill". |
| AC3 — Empty-History → leeres Feld + Info-Hint + audit `skipped_empty_context` | PASS | `loss-reason-suggester.ts:113-122` activities.length===0 && emails.length===0 → safeAudit `skipped_empty_context` + return null. `kanban-board.tsx:230-233` bei suggest===null → kiSuggestStatus="unavailable". `stage-requirements-modal.tsx:245-252` rendert `<p data-testid="ki-suggest-unavailable">KI-Vorschlag mangels Activity-History nicht verfuegbar.</p>`. Verifiziert durch Suggester-Test "Empty-Context" + Modal-Test "kiSuggestStatus=unavailable". |
| AC4 — Bedrock/Parse-Error → leeres Feld + Info-Hint + audit | PASS | `loss-reason-suggester.ts:128-141,143-154,160-172` 4 Error-Pfade (invoke throws, !success, parse_error, schema_error) → audit + return null. `kanban-board.tsx:223-240` try/catch um suggestLossReason → kiSuggestStatus="unavailable". Verifiziert durch 4 Suggester-Tests (Bedrock-throw, !success, Parse-Error, Schema-Error). |
| AC5 — Akzeptieren/Editieren/Verwerfen alle 3 Pfade -> moveDealToStage mit won_lost_reason | PASS | `stage-requirements-modal.tsx:105-112` handleConfirm sammelt aktuelle `values` (egal ob pre-filled, editiert oder ueberschrieben). `kanban-board.tsx:243-277` handleModalConfirm ruft `moveDealToStage(modal.dealId, modal.newStageId, modal.newStageName, values as RequirementValues)`. Alle 3 Pfade gehen denselben Code-Pfad. Verifiziert durch 3 Modal-Tests (Confirm-akzeptiert/-editiert/Alternatives-Click). |
| AC6 — Andere Stages (Gewonnen/Angebot offen/Angebot vorbereitet) zeigen Number-EUR-Input + KEIN KI-Suggest + kein Hint | PARTIAL (M-1) | Number-EUR-Input rendert korrekt (`stage-requirements-modal.tsx:140-164`). KI-Suggest wird nicht aufgerufen (`kanban-board.tsx:220` `if (!isLossStage) return;`). **ABER**: Info-Hint "KI-Vorschlag nur fuer Verlustgrund verfuegbar" wird trotzdem gerendert (`modal.tsx:117 showKiSuggestHint` + Z. 258-265). Implementation folgt der Modal-Section in der Spec (Z.166); widerspricht aber AC6 ("kein Info-Hint"). Siehe Finding M-1. |
| AC7 — Verhandlung 2 Felder atomar | PASS | `stage-required-fields.ts:26-29` "Verhandlung / Einwände" → fields=["value", "contact_id"]. `modal.tsx:136-256` rendert beide Felder dynamisch. `actions.ts:454-457` in `moveDealToStage`: `.update({ ...requirementValues, updated_at }).eq("id", dealId)` — beide Felder in 1 SQL-UPDATE. Atomar. Verifiziert durch Modal-Test "rendert 2 Felder Verhandlung" + move-deal-to-stage-Test "Atomar 2x update". |
| AC8 — Cancel → kein DB-Touch + Deal bleibt in Source-Stage | PASS | `kanban-board.tsx:279-283` handleModalCancel: `setModal(null); setModalError(null); setDeals(initialDeals)`. Kein Server-Call. Optimistic-Drop wurde bereits beim Modal-Open zurueckgenommen (`openRequirementsModal:218`), Cancel-Reset ist defensive. KI-Suggest-Insert lief ggf. schon — matched Spec-Ausnahme. Verifiziert durch Modal-Test "Cancel-Pfad". |
| AC9 — audit_log trail: ki_loss_reason_suggested + update + stage_change | PASS | `actions.ts:1079-1086` suggestLossReason inserted `audit_log` mit `action: "ki_loss_reason_suggested"`. `actions.ts:470-476` moveDealToStage mit requirementValues → logAudit `action: "update"`, context "Pflichtfeld-Set bei Stage-Move ...". `actions.ts:527-536` moveDealToStage → logAuditWithId `action: "stage_change"`. Drei separate audit-Eintraege. Verifiziert durch Suggester-Test "Audit status=succeeded" + move-deal-to-stage-Test "Audit-Trail mit Update + stage_change". |
| AC10 — Build clean + Lint keine neuen + Tests +10 minimum | PASS | Build: clean (~36s, Next 16 RC). Tests: 42 neue SLC-813 Vitest-Tests, Full-Suite 1031/1031 PASS (110 Files). Lint: 0 neue Findings — SLC-813 hat 2 pre-existing `as any`-Errors in actions.ts (Z. 483, 721) unveraendert und 1 weiteren pre-existing `as any` in Z. 417 sogar **eliminiert** (Refactor zu `effectiveDeal: Record<string, unknown>`). Lint-Findings reduziert von 3 auf 2 in actions.ts. |
| AC11 — Live-Smoke 5 Drag-Drop-Pfade | DEFERRED | NICHT durchgefuehrt code-side. Per [[feedback_slice_merge_at_end]] + [[feedback_no_coolify_branch_switch_ever]] erst nach Master-Merge + Coolify-Redeploy von main. MT-7 in der Slice-Spec markiert. |

# Wiring-Verification

| Step | Check | Status |
|---|---|---|
| Component → API | kanban-board.tsx ruft `moveDealToStage(dealId, newStageId, stageName, requirementValues)` + `suggestLossReason(dealId)` | PASS |
| API → Server-Action | moveDealToStage erweitert um 4. Parameter — backward-compatible (alte 3-arg-Calls aus pipeline-view + tests funktionieren weiter) | PASS |
| Server-Action → Database | moveDealToStage: 1x SELECT deals+pipeline_stages → 1x UPDATE Pflichtfeld → 1x INSERT activities → 1x UPDATE deals stage_id+status → 1x INSERT activities stage_change. suggestLossReason: 1x SELECT deals → 1x SELECT activities → 1x SELECT email_messages mit Schema-Mapping (from_address→from_email, gatekeeper_summary OR body_text → snippet) → 1x INSERT audit_log | PASS |
| Database → Response | moveDealToStage returns `{ error: "" }` / `{ error: msg }`. suggestLossReason returns `{ primary, alternatives, costUsd } \| null` mit konsistenten Audit-Status-Codes | PASS |
| Response → State | handleModalConfirm:256-272 bei result.error → modalError + state-bleibt; bei success → setDeals(prev mit neuem stage_id+values), setModal(null) | PASS |
| State → Render | StageRequirementsModal rendert errorMessage als Banner (Z. 268-275), KanbanBoard rendert deals nach setDeals, Modal-Open/Close via setModal(null) | PASS |

Wiring-Chain komplett intakt.

# Stub-Detection

Scan aller 6 neuen SLC-813-Files (loss-reason-prompt/parser/suggester, stage-required-fields, stage-requirements-modal, kanban-board) auf TODO/FIXME/HACK/XXX/console.log/debugger:

**0 Findings.** Implementierung ist clean. Audit-Status-Codes sind explizit und vollstaendig (succeeded/skipped_empty_context/bedrock_error/parse_error/schema_error/deal_not_found). Errors werden in audit_log mit `error`-Field persistiert, nicht stillgeschluckt.

# Findings

## Medium

### M-1 — Spec-Inkonsistenz: AC6 vs Modal-Section Hint

**Wo**: `stage-requirements-modal.tsx:114-117` + Z. 258-265
**Beobachtung**: Bei Won/Verhandlung/Angebot-Stages (ohne `won_lost_reason`-Field) rendert die Implementation den Hint "KI-Vorschlag nur fuer Verlustgrund verfuegbar." Das folgt der Slice-Spec Modal-Section Z. 65 ("Bei anderen Pflichtfelder-Stages ohne kiSuggest: Info-Hint 'KI-Vorschlag nur fuer Verlustgrund verfuegbar'"), widerspricht aber AC6 ("kein Info-Hint 'KI nur fuer Verloren'").
**Impact**: UX-positiv (User-Aufklaerung warum KI hier nicht hilft), aber AC6-Wortlaut nicht erfuellt. Modal-Section ist haerter spezifiziert (Implementation-Detail), AC6 ist die hoehere Akzeptanzebene.
**Empfehlung**: Vor Master-Merge entscheiden:
  - **Option A** — AC6-Text in Slice-Spec anpassen: "rendert Number-EUR-Input ohne KI-Vorschlag" (Hint-Klausel entfernen). Implementation bleibt unveraendert.
  - **Option B** — Hint entfernen in modal.tsx und 1 Test anpassen.
Empfehlung ist Option A: Hint ist UX-positiv, eliminiert User-Verwirrung warum KI bei Verloren-Stage anspringt, bei Angebot aber nicht.

## Low

### L-1 — Pre-existing `as any`-Lint-Errors in actions.ts (Z. 483 + Z. 721)

**Wo**: `cockpit/src/app/(app)/pipeline/actions.ts:483, 721`
**Beobachtung**: 2 pre-existing `@typescript-eslint/no-explicit-any` Errors bestehen weiter:
- Z. 483 `const oldStageName = (currentDeal?.pipeline_stages as any)?.name ?? "Unbekannt";`
- Z. 721 `return (data || []).map((r: any) => {` (`getReferralsForSelect`)

Beide existierten bereits in main (`origin/main`-Vergleich: gleicher Code, andere Zeilennummer). SLC-813 hat einen dritten pre-existing `as any` in Z. 417 **eliminiert** (Refactor zu `effectiveDeal: Record<string, unknown>`).
**Impact**: Lint-Findings reduziert von 3 → 2 in actions.ts. Nicht-blockierender Typ-Hygiene-Punkt aus V7-Legacy.
**Empfehlung**: V8 Gesamt-/qa als Aufraeum-Kandidat fuer eigenes Hygiene-Slice (BL-457 oder spaeter), nicht in SLC-813-Scope.

### L-2 — Schema-Spec-Drift bei email_messages

**Wo**: `cockpit/src/app/(app)/pipeline/actions.ts:1051-1075` (suggestLossReason fetchEmails-Dep)
**Beobachtung**: Slice-Spec sagte `email_messages.from_email`/`snippet`. Live-DB-Schema hat `from_address` und kein `snippet`-Feld; Implementation mappt zur Laufzeit: `from_address → from_email` (Type-Boundary) + `(gatekeeper_summary ?? body_text).slice(0, 600) → snippet`. Verifiziert gegen `/sql/12_v4_migration.sql:36-64`.
**Impact**: Funktional korrekt. Spec-zu-Code-Drift, der in MT-7 Live-Smoke verifiziert wird. Nicht blockierend.
**Empfehlung**: IMP-678 (bereits in SKILL_IMPROVEMENTS angelegt per RPT-483) traegt das Pattern fuer kuenftige Slice-Specs: vor Implementation Schema-Spec-Drift-Check als Standard-Schritt.

### L-3 — AC11 Live-Smoke deferred

**Wo**: Slice-MT-7
**Beobachtung**: Live-Smoke 5 Drag-Drop-Pfade (1x KI-akzeptiert Verloren, 1x KI-editiert, 1x KI-verworfen, 1x Gewonnen ohne KI, 1x Verhandlung Contact-Selector) gegen Production-DB + echter Bedrock-Call (Cost > 0 in audit_log) wurde NICHT durchgefuehrt.
**Impact**: Pflicht-Live-Smoke fuer den ABschluss-PASS. Per Workflow-Disziplin nur nach Master-Merge auf main + Coolify-Redeploy moeglich (`feedback_slice_merge_at_end`, `feedback_no_coolify_branch_switch_ever`).
**Empfehlung**: Nach V8 Gesamt-/qa ueber SLC-811 + SLC-812 + SLC-813 → `/final-check` → Master-Merge (3 Branches in main) → User-Coolify-Redeploy → MT-7 Live-Smoke fuer AC11 + Audit-Trail-Cost-Verifikation.

# Open Risks

- **R-1 Bedrock-Cost-Drift bei vielen Verloren-Moves**: Bei jedem Drag-Drop auf "Verloren" mit fehlendem won_lost_reason wird `suggestLossReason` aufgerufen → ~$0.005-0.01 pro Call. Bei 50 Verloren-Moves/Tag = $0.25-0.50/Tag = $7.50-15/Monat. Akzeptabel fuer internal-tool, aber sollte in V8 Post-Launch beobachtet werden.
- **R-2 Modal-State nach Re-Open**: useEffect:89-96 resettet `values` nur auf `open`-Aenderung. Bei zweitem Drag-Drop wahrend Modal noch offen: keine bekannte Reproduktion, aber theoretisches Race. MT-7 verifiziert.
- **R-3 Snippet-Trunkierung 600 -> 200 Zeichen**: actions.ts:1066 schneidet auf 600 Zeichen, Prompt-Builder loss-reason-prompt.ts:87 schneidet nochmal auf 200. Doppelte Kuerzung absichtlich (Token-Budget-Schutz), aber MT-7 sollte verifizieren dass 200 Zeichen Context fuer "Wettbewerber gewann"-Argumentation reichen.

# Missing Checks

- Browser-Drag-Drop-Interaction (haendisch ueber Playwright-MCP oder Chrome-DevTools-MCP nicht durchgefuehrt — deferred MT-7).
- Echter Bedrock-eu-central-1-Call mit Cost > 0 (deferred MT-7).
- audit_log-Insertion-Verifikation gegen Coolify-DB (deferred MT-7).
- Hairpin-Behavior bei langsamer Bedrock-Antwort (≥3s) — Modal sollte weiter responsiv bleiben (Loading-State funktioniert, aber Live-Test sinnvoll).

# Readiness Assessment

**Code-side: READY** fuer Master-Merge nach V8 Gesamt-/qa.

- 1031/1031 Tests gruen, 42 neue SLC-813-Tests
- Build clean, Lint stabil (sogar verbessert), TS keine neuen Errors
- Pattern-Reuse sauber (5 Patterns 1:1 portiert: sculptor-DI-Core, sculptor-Strict-JSON-Prompt, sculptor-cost, heal-JSON-escapes, shadcn-Dialog-Pattern)
- Wiring-Chain Component → API → DB → State → Render komplett intakt
- Audit-Trail vollstaendig (3 Eintraege pro Confirm-Pfad + KI-Insert)
- Stub-Detection: 0 Findings

**Production-side: NOT-READY** bis MT-7 Live-Smoke durchgefuehrt — das ist die Phase-2-/qa nach Coolify-Redeploy. AC11 ist explizit deferred per Workflow-Regel.

# Recommended Next Step

**V8 Gesamt-/qa** ueber SLC-811 + SLC-812 + SLC-813 vor Master-Merge.

Begruendung:
1. Drei separate Branches existieren parallel (`slc-811-settings-refactor` HEAD `12a59fd`, `slc-812` ge-merged in main bei `7ae8d6e`, `slc-813-stage-requirements-modal` HEAD `0320b5c`).
2. Drei Slices teilen sich V8-Scope und sollten als Block geprueft werden bevor User in Coolify den Redeploy macht.
3. M-1 (AC6-vs-Modal-Section Inkonsistenz) sollte vor Merge entschieden werden — Option A (Spec-Update) ist die empfohlene Loesung.

Ablauf:
1. V8 Gesamt-/qa: pruefe ob SLC-811 + SLC-812 + SLC-813 zusammen konsistent sind (Cross-Slice-Wechselwirkungen)
2. `/final-check` V8: Hygiene, Dependencies, Security
3. SLC-811 + SLC-813 Master-Merge (SLC-812 schon in main)
4. User-Coolify-Redeploy
5. MT-7 Live-Smoke fuer SLC-813 AC11 + Live-Verifikation der V8-Anti-Drift fuer alle 3 Slices
6. `/deploy` Records-Sync + `/post-launch` Beobachtungsphase

Erwartung: 1 zusaetzlicher RPT fuer V8 Gesamt-/qa, 1 fuer /final-check, 1 fuer Phase-2 /qa Live-Smoke nach Coolify-Redeploy.

# Files reviewed

- `/slices/SLC-813-stage-requirements-modal.md` — AC1..AC11 + 7 MTs (im Worktree-Branch)
- `/reports/RPT-483.md` — Implementation-Stand (im Worktree-Branch)
- `/cockpit/src/app/(app)/pipeline/actions.ts` — moveDealToStage + suggestLossReason
- `/cockpit/src/components/kanban/kanban-board.tsx` — Drop-Handler + Modal-Wiring
- `/cockpit/src/components/pipeline/stage-requirements-modal.tsx` — Modal-Component
- `/cockpit/src/lib/automation/loss-reason-prompt.ts` — Prompt-Builder
- `/cockpit/src/lib/automation/loss-reason-parser.ts` — Zod-Parser + heal-Fallback
- `/cockpit/src/lib/automation/loss-reason-suggester.ts` — Pure-Core mit DI
- `/cockpit/src/lib/pipeline/stage-required-fields.ts` — Single-Source-of-Truth Konstanten
- `/cockpit/src/lib/automation/__tests__/loss-reason-suggester.test.ts` — 8 Suggester-Tests
- `/slices/INDEX.md` — SLC-813 status `done` (bereits gesynced)
- `/features/INDEX.md` — FEAT-804 status `done` (bereits gesynced)
- `/planning/backlog.json` — BL-455+BL-456 status `done` (bereits gesynced)

# Files changed

- `/reports/RPT-484.md` — NEU, dieser QA-Report.

# Files intentionally not changed

- `/slices/INDEX.md` + `/features/INDEX.md` + `/planning/backlog.json` — bereits in RPT-483-Commit gesynced. Keine zusaetzlichen Aenderungen.
- `/docs/STATE.md` — bleibt unveraendert, da nur SLC-813-/qa noch nicht V8-Gesamt-/qa. STATE-Update kommt nach V8 Gesamt-/qa.
- `/docs/KNOWN_ISSUES.md` — keine neuen Issues; M-1 ist Spec-Inkonsistenz, kein Bug. L-1/L-2/L-3 sind nicht-blockierend, dokumentiert in diesem Report.
- `/docs/SKILL_IMPROVEMENTS.md` — IMP-678 (Schema-Spec-Drift-Pattern) bereits in main angelegt, kein neues Improvement aus SLC-813 /qa.

# Counts

- AC verifiziert: 11 (AC1..AC10 PASS, AC6 PARTIAL, AC11 DEFERRED)
- Findings: 1 Medium + 3 Low = 4 total
- Tests: 1031/1031 PASS (42 neue SLC-813 Tests, 989 bestehende)
- Wiring-Steps verifiziert: 6/6
- Stub-Findings: 0
- Lint-Findings neu: 0 (2 pre-existing unveraendert, 1 pre-existing eliminiert)
- Build-Errors: 0
- TypeScript-neue-Errors: 0
