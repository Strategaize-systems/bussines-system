---
id: RPT-527
date: "2026-05-23"
skill: /frontend
slice: SLC-845
feature: FEAT-824
type: completion
status: completed
result: "SLC-845 Consent-Form DSE-Link code-side DONE. 2 neue Files (lookup-slug.ts + test) + 1 PATCH (consent/[token]/page.tsx). Vitest 6/6 PASS, TSC + ESLint clean. AC1-AC3 + AC6 PASS, AC4+AC5 (Browser-Smoke) deferred bis SLC-847."
title: "SLC-845 /frontend — Consent-Form DSE-Link Patch"
---

## Outcome summary

**SLC-845 code-side DONE.** 2 MTs erfolgreich. AC1-AC3 + AC6 PASS, AC4+AC5 (Browser-Smoke Live-Token + Edge-Case ownerless) deferred bis SLC-847.

Verification confidence: **partial** — Lookup-Helper via Vitest 6/6 PASS verifiziert (Happy-Path + 5 null-Fallback-Pfade), Page-Patch via Code-Inspection + TSC. Browser-Render mit echtem Consent-Token noch nicht verifiziert.

## Files reviewed

- `/slices/SLC-845-consent-form-dse-link.md` — Slice-Spec
- `/cockpit/src/app/consent/[token]/page.tsx` — Pre-Patch (existing Page mit Token-Lookup + ConsentForm)
- `/cockpit/src/lib/supabase/admin.ts` — createAdminClient Reuse

## Files changed (3)

- `/cockpit/src/app/consent/[token]/page.tsx` — 3 Patches:
  - Import `getTenantSlugByOwnerUserId` aus `@/lib/team/lookup-slug`
  - Select-Liste erweitert um `owner_user_id`
  - Conditional `tenantSlug = contact.owner_user_id ? await getTenantSlugByOwnerUserId(...) : null`
  - JSX-Insert zwischen `<p>wir speichern...</p>` und `<div className="mt-6"><ConsentForm/></div>`: `{tenantSlug && (<p className="mt-4 text-sm"><a href={/p/${tenantSlug}/datenschutz} target=_blank rel=noopener noreferrer className="text-primary underline">Datenschutzerklaerung lesen ↗</a></p>)}`
- `/slices/INDEX.md` — SLC-845 Status `planned` → `done`.
- `/docs/STATE.md` — Current Focus + Phase + Next Steps auf SLC-845 code-side DONE + naechster Schritt /qa SLC-845.

## Files created (3 — alle manuell authored)

- `/cockpit/src/lib/team/lookup-slug.ts` (43 Zeilen) — Helper-Function `getTenantSlugByOwnerUserId(ownerUserId: string): Promise<string | null>`. Reuse `createAdminClient`. 2-Step-Lookup mit graceful-null bei jeder Miss-Stufe (empty input, profile missing, no team_id, team missing, no slug).
- `/cockpit/src/lib/team/lookup-slug.test.ts` (105 Zeilen) — Vitest mit 6 Cases (Happy-Path, Empty-Input, Profile-Missing, Team_Id-Null, Team-Missing, Slug-Null).
- `/reports/RPT-527.md` — diese Completion-Report.

## Files intentionally not changed

- `/cockpit/src/app/consent/[token]/consent-form.tsx` (Client-Component) — Spec OUT explizit: "Consent-Form selbst bleibt unveraendert — Patch nur in page.tsx".
- `/cockpit/src/lib/supabase/admin.ts` — Reuse via Import.
- `/planning/backlog.json` BL-488 — bleibt `in_progress`.

## UI states covered

- **DSE-Link sichtbar (Happy-Path):** `contact.owner_user_id` vorhanden + Profile + Team + Slug → Link rendert.
- **DSE-Link versteckt (Empty/Null-Resolution):** `contact.owner_user_id` null ODER lookup returnt null → conditional-Render `{tenantSlug && ...}` rendert nichts → Page rendert ohne Crash.
- **Token-not-found** (pre-existing): Page rendert "Link nicht gefunden"-Shell.
- **Token-expired** (pre-existing): Page rendert "Link abgelaufen"-Shell.
- **Consent-already-granted** (pre-existing): redirect zu confirmed-page.

States DIRECTLY VERIFIED:
- Vitest 6/6: Happy-Path-Slug-Resolution + 5 null-Fallback-Pfade.
- TSC: Type-Korrektheit von `contact.owner_user_id` (nullable) + getTenantSlugByOwnerUserId-Signatur.
- ESLint: Code-Style + Security-Hints (rel=noopener noreferrer auf target=_blank).

States NICHT direkt verifiziert (defer bis SLC-847):
- Browser-Render der Consent-Page mit echtem Token.
- target=_blank-Verhalten (neuer Tab oeffnet).
- Click auf DSE-Link laedt korrekte Public-Route.
- Edge-Case orphaned-contact ohne owner_user_id rendert ohne Link + ohne Crash.

## Problems found

Keine Code-Side-Problems. Anmerkungen:
- **AC4+AC5 Browser-Smoke deferred** — wie bei SLC-843+844 per [[feedback_no_coolify_branch_switch_ever]] + [[feedback_slice_merge_at_end]]. SLC-847 als Gesamt-QA-Slice deckt Live-Smokes ab.
- **Lookup-Cost:** 2 zusaetzliche DB-Hits pro Consent-Page-Aufruf (profiles + teams). Akzeptabel — Consent-Pages werden niedrig-frequent geoeffnet (~10/Tag, kein Hot-Path).

## Open points or deferred decisions

- **AC4 Live-Smoke** (gueltiger Consent-Token zeigt Link, Klick oeffnet `/p/[slug]/datenschutz` im neuen Tab) — defer bis SLC-847.
- **AC5 Edge-Case Smoke** (orphaned-contact rendert ohne Link, kein Crash) — defer bis SLC-847.
- **i18n** des Link-Texts "Datenschutzerklaerung lesen ↗" — V1 deutschsprachig hardcoded, V2 mit Mehrsprachigkeit.
- **React `cache()` fuer Slug-Lookup** — overkill V1, da Consent-Pages low-frequency.

## Runtime verification summary

- **Dev-Server:** nicht gestartet.
- **TSC project-aware:** **0 Errors fuer SLC-845-Files** (lookup-slug.ts, lookup-slug.test.ts, consent/[token]/page.tsx-Patch). 8 pre-existing Errors weiter (vies-client.test 5x, skonto-revert.test 1x, reverse-charge-revert.test 1x, useVoiceCapture.test 1x) — alle unrelated.
- **ESLint:** **clean (0 Findings)** fuer alle SLC-845-Files.
- **Vitest:** **6/6 PASS** in 1.60s (`lookup-slug.test.ts`).
- **Build (Next.js Turbopack):** nicht ausgefuehrt (Code-Side-only).
- **Browser-Smoke:** nicht ausgefuehrt (defer bis SLC-847).

Verifikations-Basis:
- Code inspection ✅
- TypeScript compile ✅
- ESLint static check ✅
- Vitest unit ✅
- Build ❌ (defer)
- Browser rendering ❌ (defer bis SLC-847)
- target=_blank-Verhalten ❌ (defer bis SLC-847)

## Recommended next step

`/qa SLC-845` als Skill ([[feedback_qa_skill_not_inline]] + [[feedback_qa_after_every_slice]]).

Verifikations-Schwerpunkte fuer /qa:
- TSC + ESLint re-run.
- Vitest 6/6 re-run.
- Stub-Detection auf 2 neuen Files + 1 Page-Patch.
- Pattern-Reuse-Compliance: createAdminClient + maybeSingle + graceful-null.
- Page-Patch-Sanity: owner_user_id-Column existiert in contacts-Schema (V3+ standard), conditional-Render-Logic, target=_blank-Security (rel=noopener noreferrer Pflicht).
- Wiring-Walkthrough: Consent-Token → contact-Lookup → owner_user_id → lookup-slug-Helper → profiles → teams → Public-Route /p/[slug]/datenschutz.
- Live-Smoke-Status klar markieren als deferred-bis-SLC-847.
