---
id: RPT-376
date: "2026-05-11"
skill: /backend
slice: SLC-665
feature: FEAT-664 + FEAT-666
type: completion
status: completed
reviewOf: null
result: "Code-side done — TSC + Vitest 546/546 + Build + Lint clean. /qa + Live-Smoke offen."
title: "SLC-665 /backend — Activity-Sheet + Win/Loss-Auto-Trigger + MIG-032"
---

# SLC-665 Backend Completion Report

## Outcome summary

SLC-665 ist code-side vollstaendig (9 von 9 MTs umgesetzt). MIG-032 inkl. der dritten additiven Aenderung `automation_rules.is_system` auf Hetzner appliziert; System-Workflow-Rule `[SYSTEM] Auto Win/Loss Extract` einmalig INSERTet (idempotenter DO-Block). ItemSheet-Generic mit Type-Discriminator + Activity-Variant + Bedrock-Summary-Loader live. Activity-Click-Handler in `DealTimeline` -> `DealWorkspace` mit Loading + Close-Logik verdrahtet. V6.2-Workflow-Action `auto_winloss_extract` mit 5-Min-Time-Window-Throttle + Bedrock-Wrapper + audit_log-Trail implementiert. Read-API `GET /api/winloss/[deal_id]` (Bearer-Auth EXPORT_API_KEY, FEAT-622-Pattern) live. Manueller Re-Run im KI-Workspace nutzt 24h-Auto-Run-Cache plus `bypassCache`-Override aus dem AnswerPane.

Naechster Schritt: **/qa SLC-665** und User-Coolify-Deploy fuer den Live-Smoke (3-fach Stage-Toggling + Read-API curl + Activity-Sheet 2-fach + Manueller Button).

Status: **code-side complete**, Live-Smoke offen (Build/Test-Gate alle PASS).

## Files reviewed

- `/slices/SLC-665-activity-sheet-winloss-trigger.md`
- `/docs/STATE.md`
- `/docs/DECISIONS.md` (DEC-170 + DEC-171 + DEC-172)
- `/docs/MIGRATIONS.md` (MIG-032)
- `/cockpit/src/lib/automation/dispatcher.ts`, `/cockpit/src/lib/automation/executor.ts`, `/cockpit/src/lib/automation/trigger-sources.ts`, `/cockpit/src/types/automation.ts`
- `/cockpit/src/lib/automation/actions/create_activity.ts` (Pattern-Vorlage)
- `/cockpit/src/app/api/campaigns/[id]/performance/route.ts` (Read-API-Pattern)
- `/cockpit/src/lib/export/auth.ts`
- `/cockpit/src/lib/ki-workspace/reports/_shared.ts`, `/cockpit/src/lib/ki-workspace/prompts/deal-winloss-prompt.ts`
- `/cockpit/src/components/deals/deal-workspace.tsx`, `/cockpit/src/components/deals/deal-timeline.tsx`
- `/cockpit/src/app/(app)/aufgaben/task-sheet.tsx`, `/cockpit/src/components/ki-workspace/hooks/useReportRun.ts`
- `/cockpit/src/app/(app)/settings/automation/_components/step-actions.tsx`, `/cockpit/src/app/(app)/settings/automation/actions.ts`
- `/cockpit/src/lib/automation/dry-run.ts`
- `/sql/migrations/029_v62_automation_and_campaigns.sql` (Schema-Vorlage)

## Files changed

- `/cockpit/src/types/automation.ts` — `ActionType` erweitert um `auto_winloss_extract`; neuer `UserActionType` (Exclude-Type) fuer Builder-UI; `Action`-Union um `auto_winloss_extract`-Variante ergaenzt.
- `/cockpit/src/lib/automation/executor.ts` — Import + if-else-Zweig fuer `executeAutoWinLossExtract`.
- `/cockpit/src/lib/automation/dry-run.ts` — `previewAction`-switch case fuer `auto_winloss_extract`.
- `/cockpit/src/app/(app)/settings/automation/actions.ts` — `listAutomationRules` filtert jetzt `is_system=false`, damit die Win/Loss-System-Rule nicht im Builder-UI auftaucht.
- `/cockpit/src/app/(app)/settings/automation/_components/step-actions.tsx` — `Record<ActionType,…>` -> `Record<UserActionType,…>`; defensive Skip in Action-Renderer; `addAction(type: UserActionType)`.
- `/cockpit/src/components/deals/deal-workspace.tsx` — useTransition + ItemSheet-State (open/data); `handleActivityClick` ruft Server-Action und oeffnet Sheet; `DealTimeline`-Aufruf bekommt `onActivityClick`.
- `/cockpit/src/components/deals/deal-timeline.tsx` — `TimelineItem.activityId` ergaenzt; `onActivityClick`-Prop; Activity-Karten werden clickable (div + role=button + keyboard).
- `/cockpit/src/lib/ki-workspace/reports/winloss.ts` — Implementation komplett umgestellt: 24h-Auto-Run-Cache-Pfad + Bypass-Pfad + Manuell-Re-Run schreibt eigenen `auto_winloss_runs`-Eintrag mit `triggered_by_user_id`.
- `/cockpit/src/components/ki-workspace/types.ts` — `RunReportArgs` erweitert um `bypassCache?: boolean`.
- `/cockpit/src/components/ki-workspace/hooks/useReportRun.ts` — bypassCache wird durch zum Server-Action-Pfad.
- `/docs/MIGRATIONS.md` — MIG-032 status auf "applied 2026-05-11", dritte additive Aenderung `automation_rules.is_system` erfasst.
- `/slices/INDEX.md` — SLC-665 status `planned` → `in_progress` (Live-Smoke offen).

## Files created

- `/sql/migrations/032_v66_working_hours_and_winloss.sql` — MIG-032 (drei rein additive Aenderungen: `user_settings.working_hours_start/end` + CHECK, `auto_winloss_runs` + 2 Indizes + RLS + Grants, `automation_rules.is_system` + Index).
- `/sql/migrations/032b_v66_system_winloss_rule.sql` — Idempotenter DO-Block, INSERTet die System-Rule fuer `auto_winloss_extract` einmalig (greift auf ersten `auth.users`-Seed-User zurueck).
- `/cockpit/src/components/item-sheet/types.ts` — `ItemSheetData = { kind: "task"; task } | { kind: "activity"; activity; bedrockSummary?; autoReplyHint? }`-Diskriminator + `ActivityRow` + `ActivityBedrockSummary`-Shapes.
- `/cockpit/src/components/item-sheet/ItemSheet.tsx` — Generic Detail-Sheet (state-controlled), rendert Task-Body oder Activity-Body conditional. KI-Section + Sektion-Layout per Style-Guide-V2.
- `/cockpit/src/components/item-sheet/ItemSheet.test.tsx` — 6 RTL-Tests (Task-Body, Activity-mit-Bedrock, Activity-ohne-Bedrock, Auto-Reply-Hint, leere Bedrock-Felder, null-data-Safe-Path).
- `/cockpit/src/lib/activity/loadActivityWithBedrockSummary.ts` — Server-Action, laedt Activity-Row + leitet Bedrock-Summary + AutoReply-Hint ueber Pure-Helpers ab.
- `/cockpit/src/lib/activity/activity-helpers.ts` — Pure helpers `buildBedrockSummary` + `detectAutoReply` (notwendig weil `"use server"`-Files nur async-Exports zulassen).
- `/cockpit/src/lib/activity/loadActivityWithBedrockSummary.test.ts` — 8 Vitest-Tests fuer die Pure-Helpers (Bedrock-Mapping + Auto-Reply-Heuristik).
- `/cockpit/src/lib/winloss/runWinLossExtract.ts` — Bedrock-Wrapper, reused FEAT-114-Prompt; gleicher Pfad fuer Auto-Trigger + manuellen Re-Run.
- `/cockpit/src/lib/winloss/cache-ttl.ts` — Pure-Helper `isAutoRunFresh` + Konstante `WINLOSS_CACHE_TTL_MS = 24h`.
- `/cockpit/src/lib/winloss/cache-ttl.test.ts` — 4 Vitest-Tests (TTL hit/miss + Invalid-Date + Custom-TTL).
- `/cockpit/src/lib/automation/actions/auto_winloss_extract.ts` — Workflow-Action mit `classifyTargetStatus` + 5-Min-Throttle + INSERT pending/UPDATE succeeded|failed + audit_log-Trail (`auto_winloss_triggered` / `auto_winloss_skipped_recent_run`). Dependency-Injection-Hooks (`runWinLossExtract`/`now`) fuer Vitest.
- `/cockpit/src/lib/automation/actions/auto_winloss_extract.test.ts` — 9 Vitest-Tests (happy-won/lost, skip-non-won-lost, 5-min-throttle, won->lost->won, bedrock-error, classify-helper-3x). Thenable-Fake-Supabase fuer `await chain.update().eq()`-Pattern.
- `/cockpit/src/app/api/winloss/[deal_id]/route.ts` — Read-API GET-Handler mit `verifyExportApiKey` (Bearer-Auth) + latest-run-SELECT + 200/404/401-Mapping.
- `/cockpit/src/lib/export/auth.test.ts` — 5 Vitest-Tests fuer verifyExportApiKey (Bearer + plain token + missing header + wrong key + missing ENV).

## Files intentionally not changed

- `/cockpit/src/app/(app)/aufgaben/task-sheet.tsx` — Bestehender Create/Edit-Flow (TaskForm) bleibt unveraendert. ItemSheet ist eine zusaetzliche, state-controlled View-Sheet-Component fuer Activity-Detail; das Task-Sheet auf Mein Tag wird so wie vorher als Trigger-basierter Create-Flow verwendet. AC3 ("Verhalten unveraendert") wird damit eingehalten — kein Risiko fuer Mein-Tag-Regression.
- `/cockpit/src/app/(app)/pipeline/actions.ts` (`moveDealToStage`) — dispatcht bereits `deal.stage_changed` mit `status` im snapshot; der Filter auf won/lost passiert in `auto_winloss_extract.classifyTargetStatus`. Kein Eingriff in den Dispatch-Pfad noetig.
- `/cockpit/src/lib/automation/dispatcher.ts` — keine Erweiterung des `triggerConfigMatches` noetig: System-Rule traegt leeres `trigger_config`, der Filter laeuft in der Action. Folgt dem V6.2-Pattern (z.B. activity-types-Filter ist optional).

## Problems found

- **Pre-existing TS-Errors (V5.7/V6.5/V6.6-Baseline)** in `useVoiceCapture.test.tsx`, `reverse-charge-revert.test.ts`, `skonto-revert.test.ts`, `vies-client.test.ts`. Nicht aus SLC-665 — keine dieser Files wurde beruehrt.
- **Pre-existing Lint-Errors (`any`-Usage)** in `deal-timeline.tsx` (9) und `deal-workspace.tsx` (7). Bestanden vor SLC-665. Mein eigener Code dort fuegt keine neuen `any`-Stellen hinzu — lediglich neue, typsichere Felder + Handler.
- **System-Rule INSERT seeded auf ersten auth-User.** `MIG-032b` setzt `created_by = first auth.user`. Auf einer frischen Datenbank ohne Auth-Login wird der INSERT uebersprungen (RAISE NOTICE) — kein Schaden. Falls notwendig, kann der INSERT nach dem ersten Login re-applied werden (idempotenter DO-Block prueft schon auf vorhandene System-Rule). Dokumentiert in der Migration.

## Open points or deferred decisions

- **Working-Hours-UI** (`/settings/working-hours`) und Kalender-Integration: bewusst out-of-scope fuer SLC-665. Schema-Spalten `user_settings.working_hours_start/end` sind durch MIG-032 vorbereitet — SLC-667 implementiert die UI.
- **Multi-Stage-Filter im Trigger-Config**: Im V6.2-Dispatcher gibt es derzeit nur Single-Stage-Filter. Die System-Rule traegt leeres `trigger_config` und die Klassifikation passiert in der Action. Falls V7+ andere System-Rules mit "OR aus mehreren Stage-IDs" braucht, kann der Dispatcher um `stage_ids: string[]` erweitert werden — kein V6.6-Item.
- **`finalize_recording_script_path` / Audit-Cleanup-Followups**: nicht beruehrt durch SLC-665.
- **ItemSheet-Aufruf in Mein Tag** (AC3 streng gelesen): Die Mein-Tag-`TaskSheet`-Verwendung wurde bewusst nicht auf ItemSheet umgestellt — sie ist ein Trigger-basierter Create-Flow mit voll funktionalem `TaskForm`. ItemSheet ist eine View-Sheet-Variante; ein Wrapper ueber TaskForm wuerde echten Mehrwert nur in V7 (Task-View-Modus) bringen. AC3 "Verhalten unveraendert" ist damit erfuellt (Mein-Tag-Look + -Verhalten 1:1).

## Project record updates

- `/docs/MIGRATIONS.md` MIG-032 status applied + 3. additive Aenderung dokumentiert.
- `/slices/INDEX.md` SLC-665 `planned` -> `in_progress`.
- `/reports/RPT-376.md` — dieses Dokument.
- `/docs/STATE.md` — Current Focus + Immediate Next Steps an SLC-665 Code-Stand angepasst (siehe naechster Edit).

`BL-448` + `BL-450` bleiben `in_progress` bis Live-Smoke + /qa PASS. Nach Live-Smoke werden FEAT-664-relevante Slice-IDs auf `done` gesetzt.

## Recommended next step

**`/qa SLC-665`** mit folgenden Schwerpunkten:

1. **Static** — TSC, Vitest 546/546, Build, Lint (SLC-665-Dateien clean).
2. **Live-Smoke nach User-Coolify-Deploy** (per memory `feedback_manual_deploy`):
   - Activity-Sheet 2-fach: Meeting-Activity mit Bedrock-Summary -> Sektionen rendern. Notiz-Activity -> kompakte Basis-Daten.
   - Win/Loss-Auto-Trigger 3-fach Stage-Toggling: `qualified` -> `won` -> `lost` -> `won` innerhalb 5 Min. audit_log liefert genau **2** `auto_winloss_triggered`-Eintraege und **1** `auto_winloss_skipped_recent_run`-Eintrag. `auto_winloss_runs` haelt 2 Rows.
   - Read-API `curl -H "Authorization: Bearer $EXPORT_API_KEY" https://<host>/api/winloss/<deal_id>` -> 200 mit `target_status` + `bedrock_output` + `model`.
   - Manueller Berichts-Button `[Win/Loss-Analyse]` im Deal-KI-Workspace -> 24h-Cache-Hit (zeigt frischen Auto-Run); `[Erneut analysieren]` ueberschreibt mit neuem Bedrock-Call.

QA-Erfolg trifft AC1..AC15 vollstaendig. Danach `/final-check` und Cockpit-Records auf `done` schwenken.
