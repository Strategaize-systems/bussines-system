---
id: RPT-253
date: "2026-04-29"
skill: /qa
slice: SLC-551
feature: FEAT-551
type: completion
status: completed
reviewOf: RPT-252
result: "11/11 AC PASS. Keine Blocker, keine High, keine Medium. 2 Low-Findings (deferrable)."
title: "/qa SLC-551 Angebot-Schema-Foundation — PASS"
---

# /qa SLC-551 — Angebot-Schema-Foundation

## 1. QA Scope

Verifikation aller 11 Acceptance Criteria aus `slices/SLC-551-proposal-schema-foundation.md` (Z. 50-62).

Geprueft wurde:
- Schema-State auf Hetzner Business-System nach Coolify-Redeploy (neuer DB-Container `supabase-db-k9f5pn5upfq7etoefb5ukbcg-153552127141`)
- Idempotenz-Verifikation der MIG-026
- V2-Bestand-Backwards-Compat
- Pfad-Helper-Tests (`proposal-pdf-path.test.ts`)
- E2E-DB-Simulation der Server Actions `createProposal` + `getProposalForEdit`
- CHECK-Constraint auf `email_attachments`
- Storage-RLS-Policy-Expressions + Cross-User-Block
- TypeScript-Build + komplette Test-Suite (`npx vitest run`)
- Stub-Scan auf alle 4 geaenderten/neuen Files
- Wiring-Verifikation `createProposalLegacy` (V2-Sheet → Action → DB)
- HTTP-Reach-Check Production-Endpoints

## 2. Result: PASS

Alle 11 ACs erfuellt. Keine blockierenden Findings.

## 3. Findings (gruppiert nach Severity)

### Blocker
keine

### High
keine

### Medium
keine

### Low
- **L1 — Storage-RLS Cross-User-Smoke ueber echten Auth-Pfad nicht durchgefuehrt.** AC8 wurde durch (a) Inspektion der 4 Policy-Expressions in `pg_policies` und (b) SQL-Direkt-Cross-User-Write-Block-Smoke (Case 2 PASS) verifiziert. Ein formaler Test ueber Kong + GoTrue + 2 echte User-Sessions mit Storage-PUT/GET wurde nicht durchgefuehrt — erfordert echte JWT-Tokens, die in dieser Session nicht verfuegbar sind. Risk: minimal; das User-Scope-Pattern (`(auth.uid())::text = (storage.foldername(name))[1]`) ist 1:1 das gleiche Pattern wie in V5.3-Branding-Bucket bzw. V5.4-`email-attachments`-Bucket (beide live + verifiziert). Empfehlung: bei SLC-553-PDF-Renderer-Live-Smoke implizit mit-verifizieren (ein User generiert ein PDF, RLS sollte greifen).
- **L2 — Browser-Visual-Smoke `/proposals`-Listing nach Coolify-Redeploy nicht durchgefuehrt.** Production-Endpoint ist erreichbar (`HTTP 200` auf `/proposals` und `/login`), aber kein visueller Render-Check der Tabelle. Risk: minimal; die DB hat aktuell 0 `proposals`-Rows, das Listing zeigt einen Empty-State (kein Render-Fehler moeglich). Bei SLC-552-Workspace wird das Listing automatisch mit echten Rows getestet werden. Empfehlung: User kann beim Live-Smoke ein V2-Stub-Angebot ueber das alte Sheet anlegen, um den Listing-Render-Pfad zu pruefen.

## 4. Acceptance Criteria — Detail

| AC | Beschreibung | Status | Beleg |
|----|--------------|--------|-------|
| AC1 | MIG-026 Schema vollstaendig live | PASS | Schema-Matrix 8/8 PASS — proposals 28 Spalten (17 V2 + 11 V5.5), proposal_items existiert mit RLS, email_attachments CHECK-Constraint live, Bucket privat, 4 Policies, 3 proposals-Indizes, 2 proposal_items-Indizes |
| AC2 | Idempotenz | PASS | Zweiter Apply-Run gestern beim Backend lief sauber durch (RPT-252 MT-2). Re-Apply heute nicht noetig (Schema-Matrix unveraendert) |
| AC3 | V2-Stub-Rows unveraendert lesbar | PASS | E2E-Smoke: V2-Style INSERT (nur alte Spalten) → tax_rate=19.00 als Default, valid_until/payment_terms/etc NULL, SELECT-Listing OK |
| AC4 | Pfad-Helper | PASS | `npx vitest run src/lib/pdf/proposal-pdf-path.test.ts` → 18/18 Tests gruen (build/parse/round-trip + Edge-Cases) |
| AC5 | `createProposal({...})` Action | PASS | E2E-DB-Sim: Insert mit deal_id, title="QA-Test SLC-551", version=1, status='draft', tax_rate=19.00, valid_until=today+30d (2026-05-29), payment_terms="30 Tage netto" → DB-Row entspricht Spec. Audit-Log-Insert mit `action='create'`, `entity_type='proposal'`, `context='Created from deal/pipeline'` durch |
| AC6 | `getProposalForEdit(id)` Action | PASS | E2E-DB-Sim: Multi-Source-SELECT liefert Proposal + Deal-Title + items_count=0 + branding_rows=1. NULL-Tolerance fuer Stub-Proposals ohne Deal-Link bestaetigt durch Code-Review (`proposal.deal_id ? supabase.from(...) : Promise.resolve({data:null})`). Build gruen + Type-Compat 53/53 |
| AC7 | CHECK-Constraint email_attachments | PASS | 3/3 Cases (RPT-252 MT-7): upload+id rejected, proposal+null rejected, upload+null accepted |
| AC8 | Storage-RLS Cross-User-Block | PASS (mit L1-Caveat) | Policy-Expressions inspiziert: `bucket_id='proposal-pdfs' AND (auth.uid())::text = (storage.foldername(name))[1]` an allen 4 Policies (SELECT/INSERT/UPDATE/DELETE). SQL-Direkt-Cross-User-Write-Block-Smoke: PASS (Case 2). Vollwertiger REST-API-Test deferred (siehe L1) |
| AC9 | `/proposals`-Listing bricht nicht | PASS (mit L2-Caveat) | HTTP-200 auf `https://business.strategaizetransition.com/proposals` (302→login, dann 200). Visueller Render-Check deferred (siehe L2) |
| AC10 | TypeScript-Build gruen | PASS | `npm run build` gruen, alle 41 Routen kompiliert (Production-Build) |
| AC11 | Test-Suite gruen | PASS | `npx vitest run` → 6 Test Files / 53 Tests gruen, 0 Fails |

## 5. Stub Detection Scan

Geprueft alle in SLC-551 geaenderten oder neu erstellten Files:

- `cockpit/src/app/(app)/proposals/actions.ts` — clean. 1× `return null` in `getProposalForEdit` Z. 241 ist legitim (per Spec: "Wenn `id` nicht existiert: Action returnt `null`").
- `cockpit/src/lib/pdf/proposal-pdf-path.ts` — clean.
- `cockpit/src/lib/pdf/proposal-pdf-path.test.ts` — clean.
- `sql/migrations/026_v55_proposal_creation.sql` — clean.

Kein TODO/FIXME/HACK, kein `console.log`/`debugger`, keine hardcodierten Credentials, keine leeren Catch-Blocks.

## 6. Wiring Verification

V2-Sheet-Pfad (Bestand bleibt funktional):

```
proposal-sheet.tsx
  → import { createProposalLegacy } from "./actions"   ✓ (Z. 13)
  → await createProposalLegacy(formData)               ✓ (Z. 38)
  → actions.ts createProposalLegacy                    ✓ (Z. 112)
  → supabase.from("proposals").insert({...})           ✓ Schema akzeptiert V2-Felder
```

V5.5-Pfad (warten auf SLC-552-Caller, einsatzbereit):

```
[Future SLC-552 Quickaction-Trigger]
  → createProposal({ deal_id, contact_id?, company_id?, title? })  ✓ (Z. 161 actions.ts)
  → auth.getUser() guard                                            ✓ (Z. 174-178)
  → INSERT proposals (mit DB-Defaults)                              ✓ E2E-DB-Sim PASS
  → audit_log INSERT                                                ✓ E2E-DB-Sim PASS
  → revalidatePath('/proposals')                                    ✓ (Z. 213)

[Future SLC-552 Workspace-Loader]
  → getProposalForEdit(id)                                          ✓ (Z. 232 actions.ts)
  → Promise.all (proposal + items + branding + deal + company + contact) ✓
  → NULL-tolerance fuer fehlende FK-Verknuepfungen                  ✓ (Z. 252-265)
  → return null wenn proposal nicht existiert                        ✓ (Z. 241)
```

Beide Pfade gruen.

## 7. Open Risks / Missing Checks

- L1 (Storage-RLS REST-API) und L2 (Browser-Visual-Listing) sind dokumentiert, aber nicht-blockierend.
- SLC-552 wird die volle Front-to-Back-Wiring (UI-Trigger → `createProposal` → DB → Workspace-Render) sowieso implementieren und ein-implizit verifizieren. Beide Caveats werden dort sicher mit-aufgeloest.

## 8. Readiness Assessment

**SLC-551 ist released-readyfuer den naechsten Slice.**

- Schema, Pfad-Helper, Server Actions, CHECK-Constraint, Storage-Bucket+Policies, V2-Backwards-Compat: alles live + getestet.
- Keine offenen technischen Schulden in dieser Slice.
- Die zwei Low-Findings sind keine echten Risiken (siehe Begruendung dort).
- Coolify-Redeploy durch User durchgefuehrt; Production-Endpoint reachable.

## 9. Project Record Updates

- `/slices/INDEX.md`: SLC-551 bleibt `done` (gestern bereits gesetzt) ✓
- `/features/INDEX.md`: FEAT-551 bleibt `in_progress` (haengt an SLC-552..555) ✓
- `/planning/backlog.json`: BL-407 bleibt `done` ✓
- `/docs/STATE.md`: Wird nach diesem QA-Report mit "QA SLC-551 PASS" und "Naechster Schritt: /backend+/frontend SLC-552" aktualisiert
- `/docs/KNOWN_ISSUES.md`: keine neuen Issues

## 10. Recommended Next Step

**`/backend + /frontend SLC-552` — Workspace-UI 3-Panel** (~6-8h, 11 MTs)

Entweder direkt jetzt starten oder per User-OK auf separater Session — Slice ist gross genug fuer eigene Session.

Carryover-Items aus L1+L2 werden in SLC-552 implizit mit-verifiziert (keine eigene Aktion noetig).
