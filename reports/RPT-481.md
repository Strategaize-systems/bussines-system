---
id: RPT-481
date: "2026-05-20"
skill: /qa
slice: SLC-812
feature: FEAT-802
type: completion
status: completed
result: "PASS code-side, Live-Smoke deferred to Gesamt-/qa V8"
title: "SLC-812 /qa — KI-Provider-Anzeige abstrahieren"
---

# RPT-481 — SLC-812 /qa PASS (code-side)

## QA Scope Reviewed

V8 SLC-812 (KI-Provider-Anzeige abstrahieren, FEAT-802 / BL-480) — Code-Side-Verifikation von AC1..AC8 nach `/frontend` Commit `b5e213e`. Geprueft via:
- Grep-Verifikation aller Acceptance-Strings (positiv + negativ)
- ARIA-Label-Walkthrough
- Stub-Detection-Scan auf neuen Files
- Wiring-Verification: NA (rein UI-String-Substitution, kein Data-Flow-Change)
- Build/Lint/Vitest-Resultate uebernommen aus /frontend (gleicher Commit, kein neuer Code seitdem)

Delivery-Mode: internal-tool — pragmatische QA. Live-Smoke gegen Production NICHT durchgefuehrt (siehe Open Risks 1).

## Result: **PASS** (code-side)

Alle 8 ACs erfuellt oder mit klar dokumentierter Einschraenkung (AC6 partial — Helper bereit, kein aktueller Caller im User-UI).

## AC-Verifikation

### AC1 — keine "Bedrock"/"Claude Sonnet"/"Anthropic"/"AWS"-Strings im User-UI — **PASS**
- Grep ueber `cockpit/src/**/*.tsx` mit Pattern `>Bedrock<|>Claude<|>Anthropic<|>AWS<|"Bedrock arbeitet|"Bedrock-Aufruf|"Bedrock modifiziert|"Claude Sonnet"`: **0 Treffer in Production-Files**. Verbleibende Treffer ausschliesslich in Test-Files (Assertions: `screen.queryByText(/Bedrock Claude Sonnet/i).toBeNull()` — testen dass Strings NICHT vorhanden sind, bleiben weiter valide), Comments, Code-Identifier (`BedrockSection`, `hasBedrock`, `loadActivityWithBedrockSummary`) — alle per DEC-221 explizit erlaubt.

### AC2 — AnswerPane-Spinner "KI arbeitet ..." — **PASS**
- [cockpit/src/components/ki-workspace/AnswerPane.tsx:83](cockpit/src/components/ki-workspace/AnswerPane.tsx#L83) — `<span>KI arbeitet ...</span>` verifiziert via Grep.

### AC3 — ItemSheet KI-Section-Headers "KI-Zusammenfassung" — **PASS**
- [cockpit/src/components/item-sheet/ItemSheet.tsx:132](cockpit/src/components/item-sheet/ItemSheet.tsx#L132) — `KI-Zusammenfassung` verifiziert (war bereits korrekt aus V6.6 SLC-665).

### AC4 — NL-Builder Error-Toast "KI-Aufruf fehlgeschlagen" — **PASS**
- [cockpit/src/components/ki-workspace/nl-builder-inline.tsx:641](cockpit/src/components/ki-workspace/nl-builder-inline.tsx#L641) — `KI-Aufruf fehlgeschlagen` verifiziert via Grep.

### AC5 — E-Mail-Compose Edit-Dialog "KI modifiziert ..." — **PASS**
- [cockpit/src/app/(app)/emails/compose/inline-edit-dialog.tsx:224](cockpit/src/app/(app)/emails/compose/inline-edit-dialog.tsx#L224) — `KI modifiziert den Body gemaess Anweisung.` verifiziert via Grep.

### AC6 — `formatModelDisplayName()` exportiert + von Cost-Display + Audit-Log-View aufgerufen — **PARTIAL PASS**
- **Exportiert**: ✅ [cockpit/src/lib/llm-display.ts](cockpit/src/lib/llm-display.ts) exportiert `formatModelDisplayName(modelId, { detail? })`.
- **Returnt "KI" fuer aktuell genutzte Model-IDs**: ✅ 7 Vitest-Cases PASS — Short-Form Sonnet, Full-Inference-Profile (eu.anthropic.claude-sonnet-4-6-20250514-v1:0), Unknown-Model-Fallback, Null/Undefined/Empty, Sonnet-Detail "KI Sonnet", Haiku-Detail "KI Haiku", Unknown-Family-Detail.
- **Caller-Site fehlt**: ⚠️ Kein aktuelles User-UI rendert `model_id` als sichtbaren String — `nl-history-table.tsx` hat keine model_id-Spalte, `sculptor-cost.ts` ist Dev-facing (source_note + Error-Strings, nicht UI-rendered). Helper ist als **ready-to-use-Pattern fuer V9+ Provider-Diversifikation** angelegt — beim ersten kuenftigen Use-Case (z.B. Provider-Selector, Multi-Provider-Cost-Display) ist `formatModelDisplayName()` der Pflicht-Display-Wrapper.
- **Konsequenz**: AC6 als "exportiert + getestet, aber 0 aktive Caller im Production-UI weil keine UI-Komponente eine Model-ID rendert" akzeptiert. Aufgenommen unter Findings → Low (vorausschauend implementiert, kein Code-Defekt).

### AC7 — Build/Lint/Test — **PASS**
- `npm run build`: ✅ clean, alle Routes generiert.
- `npm run lint`: ✅ 143 errors / 57 warnings, **alle pre-existing pre-V8**, **0 neue Findings** in den 5 SLC-812-Touch-Files (llm-display.ts, llm-display.test.ts, AnswerPane.tsx, nl-builder-inline.tsx, inline-edit-dialog.tsx). Acceptance "keine neuen Findings" erfuellt.
- `npm run test`: ✅ **989/989 PASS** (slice-spec forderte 779+, Helmholtz-Margin >27%). Inkl. 7 neue `llm-display`-Cases. Test-Files mit Bedrock-Assertions (`.toBeNull`) bleiben gruen, da Substitutionen Strings entfernen, nicht hinzufuegen.

### AC8 — ARIA-/title-Walkthrough — **PASS**
- Grep ueber `cockpit/src/**` mit Pattern `aria-label="[^"]*Bedrock|aria-label="[^"]*Claude|aria-label="[^"]*Anthropic|aria-label="[^"]*AWS|title="[^"]*Bedrock|title="[^"]*Claude|title="[^"]*Anthropic|title="[^"]*AWS`: **0 Treffer**. Keine A11y-Provider-Leaks.

## Findings

### Blocker
- keine

### High
- keine

### Medium
- keine

### Low
- **AC6 Caller-Gap**: Helper `formatModelDisplayName()` exportiert + getestet, aber kein Caller im aktuellen User-UI (Begruendung siehe AC6 oben). Empfehlung: Bei naechstem Feature, das eine Model-ID an den User anzeigen will (z.B. Multi-Provider-Selector in V9+), zwingend ueber diesen Helper rendern. Kein Code-Aenderung im V8-Scope erforderlich.

### Accepted Risks
- **Pre-Existing Lint-Schuld 143 errors / 57 warnings** in Files ausserhalb des SLC-812-Scopes (Goals-Calculator, Process-Check, Parser etc.). Pre-V8 Bestand. V7.7+ Cleanup-Slice geplant (siehe STATE.md). 0 davon in SLC-812-Touch-Files.

## Stub-Detection-Scan

Gescannt:
- [cockpit/src/lib/llm-display.ts](cockpit/src/lib/llm-display.ts) — **0 TODO/FIXME/HACK/debugger/console.log**. Pure-Function, kein State, kein async.
- [cockpit/src/lib/__tests__/llm-display.test.ts](cockpit/src/lib/__tests__/llm-display.test.ts) — **0 debugger/console.log**. 7 Test-Cases, alle PASS.
- 3 Edit-Files (AnswerPane, nl-builder-inline, inline-edit-dialog) — String-Substitutionen, keine neuen Stubs eingefuehrt.

**Kein Stub-Befund.**

## Wiring-Verification

**Not Applicable.** SLC-812 ist eine reine UI-String-Substitution ohne Data-Flow-Aenderung. Component → API → Route → Database → State → Render Chain ist 1:1 wie vor V8. Keine neuen API-Endpunkte, keine Schema-Aenderungen, keine State-Topologie-Aenderungen.

## Open Risks / Missing Checks

1. **Browser-Render-Smoke nicht durchgefuehrt**: Visueller Walkthrough Mein-Tag-NL-Builder-Spinner, Deal-Detail-KI-Workspace, Cockpit-Berichts-Run, E-Mail-Compose-Inline-Edit wurde NICHT in einem laufenden Browser durchgespielt. Mitigation:
   - String-Substitutionen sind statisch — Grep-Verifikation ist canonical fuer diese Aenderungsklasse.
   - 989/989 Vitest inkl. Components-Renderer-Tests gruen.
   - Build clean stellt sicher, dass Komponenten kompilieren und Routing funktioniert.
   - Per User-Direktive `feedback_no_intermediate_coolify_switches.md` wird Live-Smoke erst nach Gesamt-/qa V8 (alle 3 Slices SLC-811..813 durch) durchgefuehrt — dann gemeinsam mit einem einzigen Coolify-Redeploy.
   - Risiko bewertet als **Low**: Reine `<span>`-/Toast-/Description-Text-Substitutionen koennen nicht subtile Rendering-Bugs verursachen, die nur im Browser sichtbar werden.

2. **AC6 Caller-Audit auf V9+ verschoben**: Wenn V9+ ein Feature einfuehrt, das Model-IDs an User rendert, ist die Pflicht-Vorgabe: `formatModelDisplayName()` als Display-Wrapper nutzen. Nicht im V8-Scope verifizierbar mangels Caller.

## Readiness Assessment

SLC-812 ist **ready fuer SLC-811** (naechster V8-Slice). Acceptance-Criteria-Coverage 7/8 voll + 1/8 partial (AC6 vorausschauend). Code-Side-Konfidenz hoch. Live-Smoke deferred zu Gesamt-/qa V8 per User-Direktive.

**Nicht ready fuer**:
- Coolify-Redeploy alleinstehend (Pattern: erst nach Gesamt-/qa V8)
- /go-live (V8 noch nicht ueber alle Slices)
- /deploy (V8 noch nicht final-checked)

## Project Record Updates

- `/reports/RPT-481.md` — angelegt (diese Datei)
- `/docs/STATE.md` — Current Focus + Phase auf "SLC-812 /qa PASS code-side" + Naechster Schritt "/frontend SLC-811"

## Recommended Next Step

**`/frontend` SLC-811** — Settings-Refactor + /performance-Cleanup + Goals-Move + Label-Konsistenz (8 MTs, ~3-4h). Worktree-Isolation empfohlen (groesserer Slice, mehrere Page-Touches). Slice-Spec: [slices/SLC-811-settings-refactor-hygiene.md](slices/SLC-811-settings-refactor-hygiene.md). Bei Context-Druck stattdessen Session-Handoff vor SLC-811.

Ueberspringen von SLC-811 oder direkter Sprung zu /go-live oder Coolify-Redeploy ist **nicht empfohlen** — V8 hat 3 Slices und Live-Smoke wird per Gesamt-/qa V8 nach SLC-813 zentralisiert.
