---
id: RPT-630
date: "2026-06-12"
skill: /qa
slice: SLC-355
feature: FEAT-872
type: completion
status: completed
reviewOf: RPT-629
result: "SLC-355 BS->IS Verdichtungs-Cron /qa Code-Side PASS-WITH-LOW: AC-355-1..8 erfüllt, Stub/Wiring clean, TSC=0/ESLint=0/Vitest 1457 GREEN (57 Slice-Tests), audit_log Spec-Drift schema-verifiziert, actor_id-nullable bestätigt. 0 Blocker/High/Medium, 1 Low (ISSUE-107 Anonymisierung/Objection-Source Live-Verifikation). Next: Gesamt-/qa V8.7-B."
title: "V8.7-B BS->IS Verdichtungs-Cron — /qa (Code-Side)"
---

# /qa — SLC-355 V8.7-B BS→IS Verdichtungs-Cron (Code-Side)

## 1. QA-Scope
Code-Side-QA des einzigen Slices der Version V8.7-B (SLC-355): Acceptance-Matrix AC-355-1..8, Stub-Detection, Wiring-Chain, Re-Run aller Quality-Gates, besonderer Fokus auf R-355-1 (Anonymisierungs-System-Prompt im neuen Bedrock-Pass) und den `audit_log`-Spec-Drift. Live-LLM-Output-Qualität (R-355-1) ist explizit eine /deploy-Live-Smoke-Aufgabe, **nicht** Teil dieser Code-Side-QA.

## 2. Ergebnis: **PASS-WITH-LOW**
- **Blocker:** 0
- **High:** 0
- **Medium:** 0
- **Low:** 1 (ISSUE-107 — neu angelegt)

Der Slice ist Code-Side ready. Keine Code-Defekte. Die einzige offene Position ist eine Live-Verifikations-Posture (ISSUE-107), die bewusst dem /deploy-Live-Smoke zugeordnet ist.

## 3. Quality-Gates (re-verifiziert in dieser Session)
| Gate | Befehl | Ergebnis |
|---|---|---|
| TypeScript | `npm run test:tsc` | **PASS** (exit 0, keine Fehler) |
| ESLint (touched) | `npm run lint -- src/lib/knowledge-push src/app/api/cron/knowledge-push src/lib/is-knowledge` | **PASS** (0 Findings) |
| Slice-Tests | `vitest run knowledge-push ingest` | **PASS** (6 Files / **57 Tests** GREEN) |
| Full-Suite (Regression) | `npm run test` | **PASS** (158 Files / **1457 Tests** GREEN, 0 Regression — deckt sich mit RPT-629) |

## 4. Acceptance-Criteria-Matrix
| AC | Befund | Verdikt |
|---|---|---|
| AC-355-1 `ingestKnowledge` Header/Status/Return | `client.ts:278` POSTet `/api/knowledge/ingest` mit `x-strategaize-service-key` + `x-strategaize-consumer: business-system` + `Content-Type: json`; 200 UND 207 fallen durch `response.ok` → Schema-Parse (Erfolg); 401→auth, 429→rate_limit(Retry-After), 4xx/5xx→server; empty- + >100-Guard. 10 ingest-Tests. | PASS |
| AC-355-2 Win/Loss-Bucketing + k-min + keine PII | `aggregate-winloss.ts` gruppiert succeeded Runs (7d via `created_at`) nach Branche×Größe×won/lost, `dealCount < minBucket` gefiltert; Output-Typ `WinLossBucket` trägt nur branche/sizeBucket/targetStatus/dealCount/runMarkdowns — KEIN company.name/contact/deal.title. 12 aggregate-Tests. | PASS |
| AC-355-3 Objection-Klassifikation, keine Namen im Prompt, fail-soft | `distill.ts classifyObjections` via `queryLLM` (temp 0.2), System-Prompt mit harter Anonymisierungs-Auflage + Taxonomie-Hint; pre-Prompt `redactPiiString`; LLM-Fehler/leer/Exception → `null` → Bucket-Skip, Cron läuft weiter. Test verifiziert „kein Name im userPrompt" + fail-soft. | PASS (Code-Side; Live-Output → ISSUE-107) |
| AC-355-4 deterministischer week-stamped `source_reference`, domain/source_system | `build-items.ts`: `bs-winloss-<YYYY-Www>-branche:<slug>-size:<bucket>-<won\|lost>` / `bs-objection-<...>`; `isoWeekOf` ISO-8601-korrekt; `slugifyBranche` Umlaut-Map; `domain:"sales"`, `source_system:"business_system"`. Gleiche Inputs → identische Referenz → IS-Dedup. 9 Tests. | PASS |
| AC-355-5 Redact-before-send Email/Telefon | `redact.ts redactItemsBeforeSend` strippt Email+Telefon non-destruktiv aus title+body_markdown; Email-zuerst-Reihenfolge gegen Phone-False-Match. 6 Tests. | PASS |
| AC-355-6 401 / ENABLED-Gate / genau 1 audit_log-Row | `route.ts POST`: `verifyCronSecret` (401), `KNOWLEDGE_PUSH_ENABLED!=="true"` → `{success,skipped}` ohne IS-Call; genau 1 `audit_log`-Insert `action='is_knowledge_pushed'`, `actor_id:null`, `entity_type:'knowledge_push_run'`, Counts. Route-Tests verifizieren beide Gates + Row-Felder. | PASS |
| AC-355-7 Chunks ≤100 / 207 gezählt / Cron bleibt success | `chunk(redacted,100)`; Ingest-Chunk-Fehler → `failed += batch.length`, Cron `success:true`; 207-Teilfehler durchgereicht. Tests: 150 Items → 2 Chunks (100+50), ingest-throw → failed=2 + audit trotzdem geschrieben. | PASS |
| AC-355-8 Cost via `calculateSculptCost` in audit_log | `distill.ts safeCost` → `calculateSculptCost(usage, modelId)` (try/catch → 0 bei unknown model), pro Call an Caller; Route aggregiert `bedrock_cost_usd` in `changes` + `context`. Tests: 0.0081 USD-Berechnung + unknown-model→0. BS-lokal, kein ai_jobs/ai_cost_ledger (DEC-291, schema-bestätigt: existiert nicht in BS). | PASS |

## 5. Stub-Detection
Scan über alle Implementierungs-Files (`knowledge-push/*`, `cron/knowledge-push/*`, `is-knowledge/client.ts`) auf TODO/FIXME/HACK/debugger/console.log/placeholder/dummy/hardcoded-Returns:
- **Keine** TODO/FIXME/HACK, **kein** `console.log`/`debugger`, **kein** Hardcoded-Secret/URL/User-ID, **keine** auskommentierten Code-Blöcke.
- Einzige `return null`-Treffer (4x in `distill.ts`) sind die **dokumentierten fail-soft-Skips** (LLM-Fehler/leer/Exception). Reviewed & accepted — per Spec (AC-355-3) gewolltes Verhalten, kein Stub.
- `console.error` in `route.ts` ist korrektes Error-Logging (Ingest-Chunk-Fehler + Fatal-Catch), kein Debug-Stub.

## 6. Wiring-Chain
POST-Handler injiziert die **realen** Implementierungen (Tests injizieren Mocks → echte Wiring-Verifikation):
```
POST → verifyCronSecret → KNOWLEDGE_PUSH_ENABLED-Gate → createAdminClient
  → aggregateWinLoss(admin) / gatherObjectionNotes(admin)   [echte Supabase-Reads]
  → distillWinLossBucket / classifyObjections (queryLLM)     [echtes Bedrock eu-central-1]
  → buildWinLossItem / buildObjectionItem
  → redactItemsBeforeSend
  → ingestKnowledge (chunk ≤100)                            [echter POST an IS /api/knowledge/ingest]
  → audit_log INSERT (1 Row)
```
Alle Glieder im POST-Handler real verdrahtet (`route.ts:189-201`). Schema-Datengrundlage live gegen die SQL-Definitionen geprüft:
- `auto_winloss_runs` hat `deal_id`/`target_status`/`bedrock_output`/`status`/`created_at` → Embed `deals(value, company_id, companies(industry))` valide.
- `activities.company_id UUID REFERENCES companies(id)` existiert → Embed `companies(industry)` valide.
- IS-Ingest-Contract (URL/Header/Body/erzwungene Server-Felder) deckt sich mit dem live verifizierten Contract (REL-025, [[project-is-knowledge-ingest-endpoint-shared-receiver]]).

## 7. audit_log Spec-Drift — schema-verifiziert (Handoff-Fokus)
Spec-Intention „Counts in `context` als JSON" vs. Schema-Realität:
- `audit_log.changes` ist **JSONB** ✓ (`sql/08_v3_schema.sql:91`) → strukturierte Counts dort.
- `audit_log.context` ist **TEXT** ✓ (`sql/08_v3_schema.sql:92`) → Summary-String dort.
- `audit_log.entity_id` ist **UUID NOT NULL** ✓ (`sql/08_v3_schema.sql:90`) → generierte runId-UUID erfüllt NOT-NULL.
- `audit_log.actor_id` Base-Schema `NOT NULL`, aber **DROP NOT NULL** in `sql/13_v41_migration.sql:74` ✓ + **9 bestehende Cron-Routes** inserten `actor_id:null` → `actor_id:null` läuft zur Laufzeit, kein FK/NOT-NULL-Fehler.

**Verdikt:** Die akzeptierte Spec-Drift-Auflösung ist schema-korrekt und folgt der `click-log-cleanup`-Konvention. Kein neuer DEC nötig (RPT-629 korrekt).

## 8. Findings nach Severity

### Low
- **ISSUE-107 (neu) — Anonymisierungs-Qualität + Objection-Source erst Live verifizierbar.** (a) Firmen-/Personennamen werden NICHT regex-gestrippt (nur Email/Telefon), sondern verlassen sich allein auf die LLM-Anonymisierung → reale Güte erst im /deploy-Live-Smoke prüfbar (R-355-1). (b) `gatherObjectionNotes` zieht ALLE `activities.description` der letzten 7 Tage (typ-unspezifisch), nicht die dedizierte `activities.objections`/`summary`-Spalte — per Spec (AC-355-3), aber Klassifikations-Relevanz hängt von der realen Datenlage ab. Internal-Test-Mode (BS-eigene Vertriebsdaten) → Re-Id-Risiko bewusst akzeptiert (DEC-290). Severity Low: kein Code-Defekt, fail-soft. → tracked als ISSUE-107 für /deploy-Live-Smoke.

### Reviewed & accepted (keine eigenen Findings)
- Fenster-Feld `created_at` statt `bedrock_completed_at` für die 7-Tage-Aggregation — deckt den Run unabhängig vom Completion-Timing ab, akzeptabel.
- `branche="unknown"`-Fallback bei NULL-`industry` / fehlendem `activities.company_id` — gewolltes Verhalten, k-min-Filter greift weiterhin.

## 9. Open Risks / nicht in dieser Code-Side-QA prüfbar
- **R-355-1 Live-LLM-Output (Medium, akzeptiert/deferred):** Anonymisierungs-Güte + Halluzinations-Verhalten der realen Bedrock-Ausgabe → /deploy-Live-Smoke (erste real destillierte Items inspizieren: 0 Klarnamen, thematisch sinnvolle Objection-Items). Code-Side-Mitigation steht (Constrained Prompt, temp 0.2, pre-Prompt-Redact, Branchen-Aggregat-Input, fail-soft).
- **Keine Live-DB-RLS-Tests nötig:** Slice hat keine Migration / keine neuen Tabellen/Policies; Reads laufen via `createAdminClient` (service_role). Der node:20-Sidecar-RLS-Test (coolify-test-setup.md) ist hier **N/A**.

## 10. Readiness
SLC-355 ist Code-Side **ready**. Quality-Gates grün, Acceptance vollständig, Wiring real verdrahtet, Schema-Grundlagen verifiziert. Die einzige offene Position ist die Live-Verifikations-Posture (ISSUE-107 / R-355-1), die korrekt dem /deploy-Live-Smoke zugeordnet bleibt — kein /qa-Blocker.

## 11. Project-Record-Updates
- `/slices/INDEX.md` — SLC-355 `in_progress` → `done`.
- `/features/INDEX.md` — FEAT-872 `in_progress` → `done` (alle Slices der Version done).
- `/planning/backlog.json` — BL-494 `in_progress` → `done`.
- `/docs/STATE.md` — Current Focus + Immediate-Next-Step (item 0) auf /qa Code-Side PASS + Gesamt-/qa-Next aktualisiert.
- `/docs/KNOWN_ISSUES.md` — ISSUE-107 (Low, open) neu angelegt.
- `/reports/RPT-630.md` — dieser Report.
- roadmap `v8-7-b` bleibt `active` (nicht released). Last Stable bleibt V8.12 (IMP-950).

## 12. Next workflow step
**Gesamt-/qa V8.7-B** → danach `/final-check` → `/go-live` (REL-XXX) → `/deploy` (Founder-ENV `KNOWLEDGE_PUSH_ENABLED=true` + `CRON_SECRET` + Coolify-Scheduled-Task `knowledge-push` weekly So 02:00 + R-355-1/ISSUE-107 Live-Push-Smoke gegen IS) → `/post-launch`.

V8.7-B besteht nur aus diesem einen Slice; die Gesamt-/qa ist daher eine leichtgewichtige Cross-Cut-Bestätigung (dieser /qa-Lauf hat die End-to-End-Wiring inkl. Live-IS-Ingest-Contract bereits abgedeckt) — sie ist als formaler Version-Gate vor `/final-check` dennoch erforderlich (CLAUDE.md Workflow-Disziplin). Kein Customer-Outreach ([[module-lifecycle-discipline]] + IMP-950). **BS NICHT pushen** bis nach Gesamt-/qa + Founder-ENV.
