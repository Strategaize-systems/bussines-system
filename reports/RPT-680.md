---
id: RPT-680
date: "2026-07-08"
skill: /qa
slice: SLC-915
feature: FEAT-927
type: completion
status: completed
reviewOf: RPT-679
result: "MT-4 QA: AC-915-5 PASS-LIVE (in-tx probe gegen Coolify-DB 8/8), INSERT-Strictness PASS, Suite 1623/1623 grün, tsc clean, 0 Stubs. Persistent Live-Apply = /deploy. Verdict: code-done + behavior-live-verified."
title: "QA SLC-915 MT-4 — MIG-055 changed-FK-only Trigger (ISSUE-140)"
---

# QA — SLC-915 MT-4 (MIG-055 changed-FK-only Trigger + UPDATE-WITH-CHECK-OR)

## 1. QA scope reviewed

- `sql/migrations/055_v817_slc915_class_c_changed_fk_trigger.sql` (MIG-055 — 9× UPDATE-Policy-Revert + Guard-Function + Trigger)
- `cockpit/__tests__/migrations/055-v817-class-c-changed-fk-trigger.test.ts` (DB-Layer-Test, node:20-Sidecar)
- OQ-A1 in-tx-Probe-Output gegen Coolify-DB (RPT-679)
- Betroffene AC: **AC-915-5** (MT-4-Kern), **AC-915-8** (INSERT-Strictness + Suite-grün-Teil)

## 2. Result — MIXED (code-done + behavior-live-verified)

MT-4 ist code-complete; die DB-Enforcement-Behavior wurde live gegen die Coolify-DB verifiziert (in-tx-Probe). Einziger deferred Teil: die **persistente** Anwendung der Migration = /deploy-Ops-Schritt. → Slice-Status bleibt `in_progress` / MT-4 `code-done` (nicht `done`), da SLC-915 als Ganzes MT-5/MT-6 offen hat.

## 3. Per-AC-Verdicts

### AC-915-5 (MIG-055 Kern) — **PASS-LIVE** (Behavior) / Live-Apply PENDING (/deploy)
In-tx-Probe (BEGIN → apply MIG-055 → seed mixed-owner → 8 Checks → ROLLBACK) gegen `supabase-db-k9f5pn5upfq7etoefb5ukbcg` — die vom AC geforderten drei Kernpunkte + Bypass/Struktur alle bestätigt:

| AC-915-5-Forderung | Check | Ergebnis |
|---|---|---|
| Status-Update (unveränderte FKs) auf mixed-owner-Row für Non-Admin besteht | CHK1 | **PASS** |
| UPDATE mit FK-Change auf unsichtbaren Parent → insufficient_privilege | CHK2 | **PASS** (42501) |
| INSERT bleibt strikt (AND) | CHK4 | **PASS** (blocked) |
| DB-Verify positiv | CHK1/CHK3/CHK5/CHK6 | **PASS** |
| DB-Verify negativ | CHK2/CHK4 | **PASS** |
| DB-Verify mixed-owner | CHK1/CHK3 (mixedSignal) | **PASS** |

Zusätzlich (OQ-A1): SECURITY INVOKER (CHK7, prosecdef=false auf allen 9 Guard-Functions) + parent-RLS-Sichtbarkeit im Subquery (CHK2 blockt / CHK3 lässt durch = parent-RLS wirkt) bestätigt; Struktur 9 Trigger / 9 UPDATE-OR / 9 INSERT-AND (CHK8). Live-DB unverändert (0 residual triggers post-ROLLBACK).
**Deferred:** persistente Live-Apply + Lauf des committeten DB-Layer-Tests dagegen → /deploy (Ops-Pre-Cond, `sql-migration-hetzner.md` + node:20-Sidecar). Das ist ein Deploy-Schritt, kein QA-Gap.

### AC-915-8 (MT-4-relevanter Teil) — **PASS**
`npm run test` grün (176 Files / **1623/1623**), `tsc --noEmit` clean. MIG-055 fasst `img-src`/`connect-src`/CSP nicht an (reine RLS/Trigger-Migration) → globale CSP-Direktiven unverändert. INSERT-AND-Strictness erhalten (CHK4). Voll-AC-915-8 (enforced CSP-Header, Feature-Flow-Smoke) ist Gesamt-QA-/Deploy-Scope, nicht MT-4.

## 4. Findings

Keine. 0 Blocker / 0 High / 0 Medium / 0 Low.

- **Stub-Scan:** clean (kein TODO/FIXME/placeholder-return/empty-handler in Migration + Test).
- **Wiring-Chain:** MT-4 ist DB-Layer; die Enforcement-Kette (Trigger → geänderte-FK-Erkennung → can_see_owner-Subquery → RAISE/PASS) ist durch die 8 Probe-Checks Ende-zu-Ende bewiesen. Kein Code-Time-Consumer (per Slice: MT-4 produziert MIG-055, kein anderer MT konsumiert es zur Code-Zeit). Einziger Produktions-Wirksamkeits-Vorbehalt: die Migration muss im /deploy tatsächlich appliziert werden (geflaggt in STATE/MIGRATIONS/ISSUE-140).
- **Silent-Skip-Audit:** src-Suite 1623 gelaufen, 0 skipped. Der neue DB-Layer-Test ist ENV-gated (harter `throw` bei fehlendem `TEST_DATABASE_URL` — kein silent skip) und aus dem Default-`include` (`src/**`) ausgeschlossen → läuft im /deploy-node:20-Sidecar. **Surrogat-Mapping (1:1):** in-tx-Probe CHK1-8 ↔ Test-Cases (POS mixed-owner / NEG fk-invisible / POS fk-visible / NEG insert-strict / POS service_role / POS admin / 9-Trigger / 9-UPDATE-OR+9-INSERT-AND). Nachhol-Pflicht: committeten Test post-apply im /deploy fahren.

## 5. Open risks / missing checks

- **Persistente Live-Apply steht aus** (/deploy) — bis dahin ist der ISSUE-140-Fix in Produktion nicht wirksam (heute durch Single-Founder-`is_admin()`-Betrieb ohnehin 0-Impact, siehe ISSUE-140-Impact).
- Kein Risiko aus MT-4 auf legitime INSERTs (INSERT-Policy unangetastet, CHK4 bestätigt).

## 6. Readiness assessment

MT-4 ist **ready to proceed**: Migration korrekt, idempotent, rollback-dokumentiert, gegen die Live-DB verhaltensverifiziert, Suite + tsc grün. Keine offenen Findings. Verbleibende Slice-Arbeit: MT-5 (4 Call-Sites), MT-6 (E-Mail-Viewer), dann Gesamt-QA V8.17.

## 7. Next workflow step

MT-4 ist NICHT der letzte MT von SLC-915 → **`/backend SLC-915 MT-5`** (4 Call-Sites Fehlerprüfung/Kompensation: `mein-tag/followup-actions.ts:126`, `fit-assessment/signal-actions.ts:73`, `emails/imap-actions.ts:135`, `meetings/actions.ts:228`) in frischer Session, danach MT-6. Wenn alle MTs code-done: **Gesamt-/qa V8.17** (mit funktionalem Feature-Flow-CSP-Smoke, IMP-1401) VOR `/final-check`. Push bleibt bis Bundle-Vollständigkeit gehalten.
