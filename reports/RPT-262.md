---
id: RPT-262
date: "2026-04-30"
skill: /qa
slice: SLC-555
feature: FEAT-555
type: completion
status: completed
reviewOf: RPT-261
result: "SLC-555 Static-QA PASS (Build/TSC/Vitest 97/97/Lint-on-File/Stub-Scan/Wiring-Audit/AC1..AC11+AC14+AC15+AC18+AC19). Live-Browser-Smokes (AC12, AC13, AC16, AC17 + Junction-Inspect + CHECK-Negativ) BLOCKED bis User-Coolify-Deploy. Status MIXED — Slice bleibt in_progress."
title: "SLC-555 /qa Composing-Studio Proposal-Picker + Send-Pfad-Erweiterung"
---

## QA-Scope reviewed

- /slices/SLC-555-compose-proposal-attachment.md (AC1..AC19, MT-1..MT-10)
- /reports/RPT-261.md (/backend Code-Complete-Report als Audit-Baseline)
- Statisch ueberprueft:
  - /cockpit/src/lib/email/attachments-whitelist.ts (AttachmentMeta-Type-Erweiterung)
  - /cockpit/src/app/(app)/proposals/actions.ts (`getProposalsForDeal`, `transitionProposalStatus` Idempotenz-Check)
  - /cockpit/src/app/(app)/emails/compose/attachment-actions.ts (`attachProposalToCompose` mit pdf_storage_path-Validation)
  - /cockpit/src/components/email/proposal-attachment-picker.tsx (Dialog, Loader, Confirm-Modal)
  - /cockpit/src/components/email/attachments-section.tsx (Conditional Button, Picker-Mount, Icon-Diff, Storage-Skip-fuer-Proposal)
  - /cockpit/src/components/email/attachments-preview.tsx (Icon-Diff)
  - /cockpit/src/app/(app)/emails/compose/compose-form.tsx (dealId-Prop-Reichung)
  - /cockpit/src/lib/email/send.ts (Bucket-Diskriminator)
  - /cockpit/src/app/(app)/emails/compose/send-action.ts (Validation-Loop, Junction-Insert mit source_type+proposal_id, Auto-Sent-Trigger)
  - /cockpit/src/lib/proposal/transitions.ts (ALLOWED_TRANSITIONS Whitelist)
  - /sql/migrations/026_v55_proposal_creation.sql (CHECK-Constraint email_attachments_source_type_check)
  - /docs/RELEASES.md (REL-020-Notes)

## Result

**MIXED PASS** — Static-Layer **PASS**. Live-Browser-Smoke-Layer **BLOCKED** bis User-Coolify-Deploy von Commit `b805b24` (oder neueren V5.5-Final-Build). Slice-Status bleibt `in_progress`, Wechsel auf `done` erst nach Live-Smoke 1..6 PASS.

### Static-Layer (PASS)

- `npm run build` gruen — vollstaendiger Next.js-Build ohne Errors, alle 73 Routen kompilieren (inkl. `/emails/compose`, `/proposals/[id]/edit`, `/api/cron/expire-proposals`).
- `npm run test` gruen — 97/97 Vitest-Tests PASS in 873ms, keine Regression aus den V5.5-Pre-Slices.
- `npm run lint` zeigt 166 Project-Wide-Errors / 55 Warnings — **keiner auf SLC-555-Files**. Alle Errors sind Carryover aus aelteren Features (`lib/ai/cache.ts`, `lib/goals/calculator.ts`, `components/ui/search-autocomplete.tsx`, etc.). Diese sind dokumentiert in IMP-203/IMP-204 und nicht V5.5-blockierend.
- Stub-Detection-Scan: keine TODO/FIXME/HACK/debugger in SLC-555-Touch-Files. Akzeptable Logs: `console.log` im send.ts Z168 (strukturiertes Operations-Audit `[sendEmailWithTracking] attachments=N (upload=N, proposal=M)`), `console.warn`/`console.error` im send-action.ts Z197/Z200/Z212/Z220/Z225 (Junction-Insert-Fehler + Status-Transition-Fallback). Alle Logs sind absichtliche Server-Audit-Trails, keine Debug-Reste.
- Wiring-Chain-Verifikation: vollstaendig intakt:
  1. `<AttachmentsSection dealId>` mountet `<ProposalAttachmentPicker>` nur bei `dealId != null`
  2. Picker-Loader → `getProposalsForDeal(dealId)` Server Action mit `ORDER BY version DESC, created_at DESC`
  3. User-Klick → `attachProposalToCompose({composeSessionId, proposalId})` validiert `pdf_storage_path != NULL`
  4. Returnt `AttachmentMeta` mit `source_type='proposal'` + `proposalId` + Filename `Angebot-{slug(deal.title)}-V{n}.pdf` (Test-Mode-Detection aus Pfad-Suffix `.testmode.pdf`)
  5. State-Push via `onSelect(att)` → `onAdd(att)` → React-State in compose-form.tsx
  6. `sendComposedEmail` validiert Mix mit Diskriminator: `proposal` braucht `proposalId` + MIME `application/pdf`, `upload` durchlaeuft `validateAttachment`-Whitelist
  7. `sendEmailWithTracking` laedt Buffer pro Anhang aus richtigem Bucket (`proposal-pdfs` vs `email-attachments`), Audit-Log mit Breakdown
  8. Nach Send: Junction-Insert mit `source_type` + `proposal_id` (NULL fuer Upload, gesetzt fuer Proposal) — CHECK-Constraint `email_attachments_source_type_check` aus MIG-026 deckt invalid States
  9. Pro Proposal-Anhang: `transitionProposalStatus(proposalId, 'sent')` — idempotent (Z956 Skip bei `current.status === newStatus`)

### Acceptance-Criteria-Audit

| AC | Status | Evidence |
|---|---|---|
| AC1 — Button nur bei dealId | PASS | attachments-section.tsx Z283 `{dealId && <Button>}` |
| AC2 — Picker-Liste fuer Deal | PASS | proposal-attachment-picker.tsx Z85-115 useEffect → getProposalsForDeal |
| AC3 — Title+Version+Status+Date | PASS | proposal-attachment-picker.tsx Z255-268 |
| AC4 — Confirm-Modal bei expired/rejected | PASS | proposal-attachment-picker.tsx Z146-149 + Z310-353 |
| AC5 — Empty-State bei dealId=null | PASS | proposal-attachment-picker.tsx Z156-170 |
| AC6 — pdf_storage_path-Validation | PASS | attachment-actions.ts Z70-76 |
| AC7 — Filename-Pattern via sanitizeProposalFilename | PASS | attachment-actions.ts Z96-100 + Test-Mode-Detection Z93-94 |
| AC8 — Icon-Differenzierung | PASS | attachments-section.tsx Z342-351 (ANGEBOT-Badge vs MIME-Badge) |
| AC9 — Loeschen entfernt nur State | PASS | attachments-section.tsx Z210-231 (proposal → kein deleteViaApi) |
| AC10 — Bucket-Diskriminator | PASS | send.ts Z148-150 |
| AC11 — Junction-Row mit source_type+proposal_id | PASS (Code) | send-action.ts Z182-194 + MIG-026 CHECK-Constraint |
| **AC12 — Multipart-Mail-Empfang Gmail** | **BLOCKED** | Erfordert Live-Send + Mail-Empfang |
| **AC13 — Tracking-Pixel-Open-Event** | **BLOCKED** | Erfordert Live-Mail-Open in Gmail |
| AC14 — transitionProposalStatus-Call | PASS (Code) | send-action.ts Z216-230 |
| AC15 — Idempotenz | PASS | transitions.ts Z23-25 + actions.ts Z955-958 |
| **AC16 — V5.4 PC-Upload-Regression** | **BLOCKED** | Erfordert Live-Send mit V5.4-Default-Pfad |
| **AC17 — Mix Proposal+PC-Upload** | **BLOCKED** | Erfordert Live-Multipart-Test |
| AC18 — TypeScript-Build gruen | PASS | npm run build durch |
| AC19 — REL-020-Notes komplett | PASS | RELEASES.md Z3-50 (Scope, Summary, Risks, Rollback, Cron-Setup, Smoke-Test) |

**Static-AC: 14/14 PASS. Live-AC: 4/4 BLOCKED auf User-Coolify-Deploy.**

## Findings grouped by Severity

### Blocker
*(keine)*

### High
*(keine)*

### Medium

**M1 — Live-Browser-Smokes ausstehend (4 ACs blockiert).**
AC12, AC13, AC16, AC17 sind nicht statisch verifizierbar. Code-Pfade sind audit-clean, aber das Verhalten unter realer SMTP-Pipeline + Gmail-Empfang + Tracking-Pixel-Render kann nur Live verifiziert werden. RPT-261 enumeriert 6 Browser-Smokes als Required-Coverage:
1. Compose-mit-Deal → Picker → Proposal mit `pdf_storage_path != NULL` anhaengen → Mail an Gmail-Test → PDF downloadbar mit Filename `Angebot-{slug}-V{n}.pdf` (oder `.testmode.pdf`) → Tracking-Pixel-Open verifizieren → Junction-Row mit `source_type='proposal'` + `proposal_id` → `proposals.status='sent'` + Audit-Eintrag (AC12, AC13, AC14)
2. Compose-mit-Deal → PC-Upload (V5.4-Regression) → Mail kommt an → Junction-Row mit `source_type='upload'` + `proposal_id=NULL` (AC16)
3. Compose-mit-Deal → Proposal + PC-Upload Mix → Multipart-Mail mit beiden Anhaengen → 2 Junction-Rows (AC17)
4. Compose-ohne-Deal → "Angebot anhaengen"-Button **darf nicht erscheinen** (AC1-Live)
5. Picker mit Proposal-`pdf_storage_path=NULL` → Item disabled + Tooltip (AC6-Live)
6. Picker mit Proposal-Status `expired` → Confirm-Modal → bei OK anhaengen → Send → Mail geht raus, ABER `transitionProposalStatus(id,'sent')` returnt `{ ok: false, error: "Status-Wechsel von 'expired' zu 'sent' ist nicht erlaubt." }` (per `transitions.ts` Z23-25 sind `expired`/`rejected`/`accepted` terminal). Verhalten erwuenscht — Mail-Erfolg bleibt, Proposal-Status bleibt `expired`. (AC4-Live + AC15-Edge)

**Mitigation:** Static-Code-Pfade sind audit-clean, alle Logik-Pfade wurden in der Wiring-Chain durchgeprueft. Der Live-Smoke ist Live-Verifikation, nicht Discovery von neuen Bugs zu erwarten. Slice bleibt `in_progress` bis Live-PASS.

**M2 — F1 Hydration #418 auf `/proposals` (Carryover aus RPT-260).**
SLC-555 hat `/proposals` nicht angefasst (Composing-Studio liegt unter `/emails/compose`). Touch durch SLC-555 ist neutral. Investigation als V5.5.x-Patch ODER vor V5.5-Final-Deploy laut User-Entscheid.

### Low

**L1 — Cross-Deal-Block in `attachProposalToCompose` ist RLS-getrieben, kein expliziter Code.**
Slice-Spec MT-2 nennt einen optionalen "Cross-Deal-Block" als Server-Side-Re-Check (`if (proposal.deal_id !== currentDealId) return error("forbidden")`). RPT-261 begruendet die Auslassung mit RLS-Coverage und Single-Tenant-Internal-Tool-Mitigation. Innerhalb der Tenant-Grenze ist die Defense-in-Depth-Schicht ohne diesen Check duenner — bei Frontend-State-Manipulation kann theoretisch ein Proposal aus Deal-X mit `composeSessionId` von Deal-Y angehaengt werden. Risiko gering im aktuellen Internal-Test-Mode-Single-User-Tenant. **Entscheidung:** akzeptiert fuer V5.5, in Multi-User-V7-Plan nachziehen.

**L2 — Mehrfach-Proposal-Anhang erzeugt nur Console-Warning, kein Hard-Block.**
send-action.ts Z211-215 loggt `console.warn` bei `proposalAtts.length > 1`. Slice-Spec FEAT-555 listet "max ein Angebot" als Out-of-Scope-Hinweis — UX-Hint nur per Warning. Akzeptiert.

**L3 — `npm run lint` Project-Wide hat 166 Errors / 55 Warnings.**
Carryover aus aelteren Features (`lib/ai/cache.ts`, `lib/goals/calculator.ts`, `components/ui/search-autocomplete.tsx`, etc.). Keiner der Errors auf SLC-555-Files. Bereits dokumentiert. Nicht V5.5-blockierend.

**L4 — `attachProposalToCompose` setzt `sizeBytes: 0` ohne Storage-Metadata-Roundtrip.**
Bewusst in der Slice-Spec ("SizeBytes ist beim Anhaengen nicht trivial verfuegbar"). Folge: Total-Size-Limit (25MB-Cap aus V5.4) hat fuer Proposal-Anhaenge keinen Effekt. Mitigation: Proposal-PDFs sind typisch <500KB, Multi-Proposal ist out-of-scope. Akzeptabel.

## Open Risks / Missing Checks

- **Live-Smokes 1..6** (siehe M1) — bleibt /qa-Abschluss-Bedingung. Nach User-Coolify-Deploy von `b805b24` (oder neueren) muss /qa fortgesetzt werden:
  - 6 Browser-Smokes durchspielen
  - DB-SQL-Smoke: CHECK-Constraint Negativ-Tests (manueller INSERT mit `source_type='upload'` + `proposal_id=uuid` → Reject; `source_type='proposal'` + `proposal_id=NULL` → Reject)
  - Idempotenz-Smoke: Proposal mit Status `sent` manuell setzen → erneut senden → Status bleibt `sent`, kein doppelter Audit-Eintrag (AC15-Live)
  - Cadence-Regression: V5.4-Cadence-Send (ohne Anhaenge) bit-identisch zur V5.4-Baseline
- **Storage-Object existiert tatsaechlich** — `pdf_storage_path != NULL` in DB heisst nicht zwangslaeufig dass das PDF-File im Bucket liegt. Falls zwischenzeitlich geloescht (z.B. via Storage-Cleanup-Cron in V5.6+), schlaegt der Storage-Download in `send.ts` mit `Storage-Download fehlgeschlagen` fehl, Mail wird nicht versendet. Akzeptabel — User bekommt klare Fehlermeldung. Live-Smoke nicht erforderlich, weil V5.5 keinen Cleanup-Cron einfuehrt.
- **Coolify-Cron `expire-proposals` Erstlauf 02:00 Berlin** — User-Verifikation am Folgetag (nicht SLC-555-blockierend, nicht V5.5-Release-blockierend, nur Auto-Expire-Bestaetigung).

## Readiness Assessment

- **Code-Static-Layer:** PRODUCTION-READY. Build/Test/Lint-on-File/Stub-Scan/Wiring-Audit/AC-Audit alle PASS.
- **Live-Smoke-Layer:** PENDING. 6 Browser-Smokes + DB-SQL-Smokes + Idempotenz-Smoke + Cadence-Regression-Smoke ausstehend.
- **Slice-Status:** `in_progress` bleibt. Wechsel auf `done` erst nach Live-Smoke-PASS.
- **V5.5-Final-Release:** REL-020 als `(planned)` korrekt markiert. Final-Date wird beim /deploy V5.5 final auf das tatsaechliche Datum gesetzt.

## Recommended Next Step

1. **User-Coolify-Deploy** von Commit `b805b24` (Business System main) — aktuelle SLC-555-Backend-Build.
2. **Fortsetzung /qa SLC-555 (Live-Smoke-Phase)** mit den 6 Browser-Smokes aus M1 + DB-SQL-Smokes + Idempotenz-Smoke + Cadence-Regression. Bei PASS: SLC-555 → `done`, FEAT-555 → `done`, BL-411 → `done`.
3. Nach SLC-555 `done`: Gesamt-`/qa` V5.5 → `/final-check` → `/go-live` → `/deploy` V5.5 als REL-020 Final-Release.
4. F1 Hydration #418 auf `/proposals` als V5.5.x-Patch nachziehen ODER explizit vor V5.5-Final-Deploy einplanen (User-Entscheid).
