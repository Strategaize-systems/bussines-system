---
id: RPT-682
date: "2026-07-08"
skill: /qa
slice: SLC-915
feature: FEAT-927
type: completion
status: completed
reviewOf: null
result: "MT-6 (E-Mail-Bild-Block by-default + opt-in route-scoped Body-Route, ISSUE-139) — AC-915-7 PASS-CODE/PENDING-LIVE, 0 Findings"
title: "QA SLC-915 MT-6 — E-Mail-Viewer Bild-Block + route-scoped Body-Route (ISSUE-139)"
---

# QA — SLC-915 MT-6 (ISSUE-139, E-Mail-Bild-Block by-default + opt-in route-scoped Viewer)

## QA scope reviewed

MT-6 (full-stack, letzter MT des V8.17-Bundles). Verifikation gegen **AC-915-7**. Code-Commit `9c22374` (main).

Geprueft:
- `cockpit/src/lib/email/email-frame.ts` (NEU — geteilte Iframe-Doc-Huelle)
- `cockpit/src/lib/email/sanitize-email-html.ts` (MODIFY — `blockRemoteImages`-Option + `hasRemoteImages`)
- `cockpit/src/app/api/emails/[id]/body/route.ts` (NEU — RLS-scoped Body-Route + route-scoped CSP)
- `cockpit/src/components/email/email-html-iframe.tsx` (MODIFY — 2 Zustaende + Banner)
- `cockpit/src/app/(app)/emails/email-detail.tsx` (MODIFY — Prop-Durchreichung)
- Test-Files: `sanitize-email-html.test.ts` (+8), `email-html-iframe.test.tsx` (+5)

## Result

**MIXED → PASS-CODE / PENDING-LIVE.** AC-915-7 code-seitig vollstaendig; 0 Findings. Browser-Rendering (Bild-Load, CSP-Enforcement, Hoehenmessung unter `allow-same-origin`) + Live-RLS-Route-Verhalten (fremde id → 404, `body_html`-Spalte OQ-A2) sind browser/live-gebunden → Nachhol im Gesamt-/qa-Feature-Flow-Smoke + /deploy.

### AC-Matrix

| AC | Verdict | Evidenz |
|---|---|---|
| AC-915-7 — Inbound-Viewer blockt Remote-Bilder + Banner „Externe Bilder blockiert [Bilder laden]"; Bilder laden via `/api/emails/[id]/body` (route-scoped CSP, RLS-scoped, kein Proxy) | **PASS-CODE / PENDING-LIVE** | sanitize strippt http(s)-img-src (8 Vitest); Banner+Load-State (5 Vitest, Klick → src=/api/emails/abc123/body + sandbox=allow-same-origin); Route RLS-User-Client + route-scoped CSP-Header (Code-verifiziert); tsc0/eslint0(neue Files)/build PASS; Suite 1636/1636. Browser-Render + Live-RLS-404 deferred. |

## Test execution

- `npx tsc --noEmit` → **0**.
- `npx eslint` (7 MT-6-Files) → 6 neue/geaenderte Files **0**; `email-detail.tsx` 1 error + 2 warnings **alle pre-existing** (Zeilen 12/28/38 — ausserhalb meines Diffs, verifiziert via `git diff -U0`: Diff nur Zeile 9 + 231; = dokumentierte Lint-Baseline L-1, RPT-653/666).
- `npm run test` → **176 Files / 1636 Tests passed** (Baseline 1623 → **+13**: 8 sanitize [6 blockRemoteImages + 2 hasRemoteImages], 5 iframe). 0 Skip-Anomalien.
- `npm run build` (next build) → **exit 0**; neue Route `ƒ /api/emails/[id]/body` registriert.

Silent-Skip-Audit: beide neuen Test-Files matchen das `vitest.config` include (`src/**/*.test.ts(x)`) und liefen im Default-Lauf (verifiziert per gezieltem `vitest run` 64/64). Keine DB-/Live-gated Suite eingefuehrt.

## Stub detection

Scan der 5 Prod-Files (email-frame, sanitize, iframe, route, email-detail) auf TODO/FIXME/HACK/`console.*`/empty-catch/debugger → **NONE FOUND**.

## Wiring verification (Ende-zu-Ende)

Kette Component → API → Route → DB → Response → Render vollstaendig, per Grep bestaetigt:
1. **email-detail** (`EmailBody`, :233-235): reicht `emailId={email.id}`, `hasBlockedImages={hasRemoteImages(email.body_html)}`, `html={sanitizeEmailHtml(body_html,{blockRemoteImages:true})}` durch → blocked-State startet mit gestrippten Bildern.
2. **EmailHtmlIframe**: `showBanner = hasBlockedImages && emailId && !loaded`; Button `onClick={setLoaded(true)}` → loaded-State `src=/api/emails/${emailId}/body` + `sandbox="allow-same-origin"`. blocked-State bleibt srcDoc + `sandbox=""` (Defense-in-Depth P-079 erhalten).
3. **Route** `GET /api/emails/[id]/body`: `createClient()` (User-Client, RLS) → `getUser()` 401-Gate → `email_messages.select("body_html").eq("id",id).maybeSingle()` (fremde/unsichtbare id → `!email` → 404) → `sanitizeEmailHtml(body,{blockRemoteImages:false})` → `buildEmailDocument` → Response mit route-scoped `Content-Security-Policy: default-src 'none'; img-src https: data: blob: cid:; style-src 'unsafe-inline'; font-src 'self'` + `X-Content-Type-Options: nosniff` + `Cache-Control: private, no-store`.
4. **Rueckkanal:** kein Mutate (read-only Viewer) → kein revalidatePath noetig. `getEmailDetail` markiert die Mail separat als gelesen (unveraendert).
5. **Bidirektional:** `sanitizeEmailHtml`-Signatur-Aenderung gegen ALLE Caller gesweept — 2 Caller (`email-detail.tsx`, `legal/markdown.ts`); Default `false` haelt `legal/markdown` + 20+ Bestandstests unveraendert. `EmailHtmlIframe`-neue Props optional → 7 Bestand-Iframe-Tests gruen ohne Aenderung.

## Findings

**Blocker / High / Medium: keine.**

### Low / Beobachtungen (reviewed & accepted)

- **B-1 (Defense-in-Depth, kein Bug):** loaded-State nutzt `sandbox="allow-same-origin"` OHNE `allow-scripts`; die Route-Response traegt zusaetzlich `default-src 'none'` → das geladene Dokument kann keinen Code ausfuehren (weder Inline noch extern), obwohl same-origin. Bilder rendern via `img-src https: data: blob: cid:`. XSS bleibt zusaetzlich durch DOMPurify gestrippt (dreifache Schicht: sanitize + Route-CSP + kein-Script-Sandbox).
- **B-2 (`cid:`-Bilder):** Route-CSP erlaubt `cid:`, aber ein Standalone-Browser-Dokument kann `cid:`-Referenzen (MIME-multipart-Inline) nicht aufloesen → `cid:`-Bilder rendern im Viewer generell nicht (unabhaengig von MT-6). Erwartetes Verhalten, kein Regressionspunkt; im Smoke nur https-Remote-Bilder pruefen.
- **B-3 (blocked-State):** `<img>` mit gestripptem `src` rendert als Broken-/Alt-Placeholder (Gmail-Modell) — bewusst, kein Layout-Bug.

## Open risks / missing checks (Nachhol im Gesamt-/qa + /deploy)

Browser-/Live-Smoke-Gate (User-Interaction + Conditional-Render + neuer Infra-Pfad) → PASS-CODE/PENDING-LIVE, Nachhol-Punkt = Gesamt-/qa Feature-Flow-Smoke (IMP-1401) + /deploy.

**2-Minuten-Browser-Smoke-Checkliste (gegen deployed main, Emails-Detail):**
1. Inbound-Mail mit Remote-Bild oeffnen → Bilder **blockiert**, Banner „Externe Bilder blockiert (Tracking-Schutz) [Bilder laden]" sichtbar; 0 CSP-Console-Errors im blocked-State.
2. „Bilder laden" klicken → iframe wechselt auf `src=/api/emails/<id>/body`, Remote-Bilder **rendern**, Banner verschwindet; Netzwerk-Tab: Bild-Requests gehen direkt (kein Server-Proxy).
3. Iframe-Hoehe wird korrekt gemessen (Content nicht abgeschnitten) unter `sandbox="allow-same-origin"`.
4. **RLS-Check:** `GET /api/emails/<fremde-id>/body` (fremde/nicht sichtbare Mail) → **404** (kein Body-Leak). `GET` ohne Session → **401**.
5. **OQ-A2 Live-Confirm:** `email_messages.body_html` existiert + ist RLS-lesbar via User-Client (implizit durch (2) bestaetigt).
6. Globale App-CSP unveraendert (curl `/mein-tag` → `img-src` weiterhin ohne generelles `https:` ausser der Route).

## Readiness assessment

MT-6 ist code-seitig komplett, 0 Findings, alle statischen Gates gruen, atomar committed (`9c22374`). Damit sind **alle 6 MTs von SLC-915 code-done** (MT-1..3 RPT-677/678, MT-4 RPT-679/680 PASS-LIVE in-tx, MT-5 RPT-681, MT-6 RPT-682). Slice bleibt `in_progress` bis Gesamt-/qa + /deploy (browser/live-gebundene ACs offen). Kein Deploy-Blocker aus MT-6 selbst.

## Project record updates

- `docs/KNOWN_ISSUES.md` — ISSUE-139 Next Action: MT-6 CODE-DONE; offen Live-Smoke + /deploy.
- `docs/STATE.md` — Current Focus: MT-6 CODE-DONE, alle 6 MTs code-done, NEXT = Gesamt-/qa V8.17.
- `slices/INDEX.md` — SLC-915 bleibt `in_progress` (Gesamt-/qa + Live offen).
- `reports/RPT-682.md` — dieser Report.

## Next workflow step

**Gesamt-/qa V8.17** (Cross-Cut aller 6 MTs von SLC-915, inkl. Stub/Wiring-Cross-Cut + der BLOCKING funktionale CSP-Feature-Flow-Smoke Telefonie/Mikrofon/Meeting/E-Mail-Viewer, IMP-1401) — Pflicht VOR `/final-check`. Danach `/final-check V8.17` → `/go-live` → `/deploy` (MIG-055 Live-Apply + DB-Verify node:20-Sidecar + funktionaler Feature-Flow-Smoke) → `/post-launch` T+24h. Kein Customer-Live (module-lifecycle-discipline + IMP-950). Push bleibt bis zum kompletten V8.17-Bundle gehalten.
