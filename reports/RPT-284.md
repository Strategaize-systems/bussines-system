---
id: RPT-284
date: "2026-05-03"
skill: /qa
slice: V5.6
feature: FEAT-561+FEAT-562
type: completion
status: completed
reviewOf: RPT-283
result: "V5.6 Gesamt-QA PASS — Cross-Cut alle 4 Slices SLC-561..564 nach VAPID-Fix-Redeploy (a7b787d). Build + 154/154 Tests + Schema + Cron-Endpoints + V5.5-Regression alle gruen. AC18 Code-Pfad gegen aktuelles Image verifiziert (Subscribe-Hint rendert), Browser-User-Smoke verbleibt. Bereit fuer /final-check."
title: "/qa V5.6 Gesamt — PASS Cross-Cut SLC-561..564 + V5.5 Regression-Frei"
---

# /qa V5.6 Gesamt — PASS

## Scope

Cross-Cut-Verifikation aller 4 V5.6-Slices nach User-Coolify-Redeploy auf Image-Tag `a7b787d` (VAPID-Fix). Foci aus User-Anweisung:
1. Skonto-Mutex bei Split-Plan (SLC-562/563 Interaktion)
2. Briefing-Activity (SLC-564) neben anderen Activity-Types ohne Regression
3. Settings-Sidebar 3 Items komplett
4. V5.5 Regression-Frei (Angebote, Composing-Studio, E-Mail-Anhaenge)
5. AC18 (Push-Subscribe-Hint VAPID) jetzt testbar

## Result: PASS — alle 5 Foci verifiziert

| Fokus | Status | Beleg |
|-------|--------|-------|
| 1. Skonto-Mutex Split-Plan | ✓ Code | `use-skonto-mutex.ts` checkt `on_signature && percent===100`. Auto-Clear-useEffect in `proposal-editor.tsx`. SLC-563 Smoke RPT-280 Pfad 4 live verifiziert. |
| 2. Briefing-Activity neben anderen Types | ✓ Code+DB | `deal-timeline.tsx:111-126` Switch-Case fuer briefing/briefing_error, `nonCallActivities`-Filter unbeintraechtigt. DB: 1 briefing-Activity neben 0 Errors, 8 Meetings, alle anderen Activity-Types unangetastet. |
| 3. Settings-Sidebar 3 Items | ✓ Live | Alle 3 Routes 307 (Auth-gated, Routes existieren). `settings/layout.tsx` rendert Branding+Zahlungsbedingungen+Briefing klickbar. |
| 4. V5.5 Regression-Frei | ✓ Live | `/proposals` 307 (Auth), `/api/cron/expire-proposals` 200 mit `expiredCount:0,expiredIds:[]`, 5 V5.5-Proposals unveraendert in DB, `proposal_payment_milestones` neue Tabelle stoert nicht. |
| 5. AC18 VAPID Push-Subscribe-Hint | ✓ Code post-Redeploy | Image `a7b787d` deployed, ENV `VAPID_PUBLIC_KEY=BJnd...` gesetzt im Container, `page.tsx:12` liest korrekt, `briefing-form.tsx:139` rendert Hint bei `pushEnabled && !hasPushSubscription`. User-Stand: push=true, subscription=false → Hint MUST sichtbar sein. Browser-User-Smoke verbleibt. |

## Code-Side Verifikation

### Stub-Detection (alle V5.6-Files)

Grep-Scan ueber:
- `cockpit/src/app/(app)/settings/{briefing,payment-terms,layout}` (alle .ts/.tsx)
- `cockpit/src/app/api/cron/meeting-briefing/route.ts`
- `cockpit/src/app/(app)/proposals/[id]/edit/` (alle .ts/.tsx)
- `cockpit/src/components/deals/{deal-timeline.tsx,activity-briefing-card.tsx}`

Pattern: `TODO|FIXME|HACK|XXX|debugger`

**Result: 0 Matches.** Alle V5.6-Files clean. (4 console.log im Cron sind Status-Logging, identisch zu V5.5 expire-proposals, RPT-282 reviewed and accepted.)

### Build + Test + Lint

| Check | Status | Detail |
|-------|--------|--------|
| `npm run build` | ✓ PASS | exit 0, alle Routes registriert inkl. `/settings/{branding,briefing,payment-terms}`, `/api/cron/meeting-briefing`, `/proposals/[id]/edit`, `/api/proposals/[id]/pdf` |
| `npm run test` | ✓ 154/154 PASS | 13 Test-Files, 920ms |
| `npm run lint` | n/a | 221 pre-existing Errors aus V5.6-Vorgaengern (search-autocomplete, ai/cache, ai/parser, goals/calculator, knowledge/chunker, process-check). 0 NEUE Errors in V5.6-Files (RPT-281 baseline). |

## Live-Smoke gegen Hetzner

Ziel: `business.strategaizetransition.com`. Container `app-k9f5pn5upfq7etoefb5ukbcg-074821898392` Up 3 Min mit Image `a7b787d`. DB-Container `supabase-db-...936116`.

### Block A — V5.6 Schema komplett (MIG-027)

```
\d payment_terms_templates           — 6 Spalten + UNIQUE WHERE is_default=true + RLS
\d proposal_payment_milestones        — 9 Spalten + 3 Indizes + 2 CHECK + FK CASCADE
\d proposals (skonto)                 — skonto_percent NUMERIC, skonto_days INTEGER
\d meetings.briefing_generated_at     — TIMESTAMPTZ vorhanden
\d user_settings.briefing_*           — 3 Spalten DEFAULT 30/true/true
```

**3 CHECK-Constraints proposals:**
- `proposals_skonto_percent_check`: `NULL OR (>0 AND <10)`
- `proposals_skonto_days_check`: `NULL OR (>0 AND <=90)`
- `proposals_skonto_both_or_none`: `(both NULL) OR (both NOT NULL)` — sehr wichtig fuer Toggle-Off-Path

✓ Schema 1:1 wie MIG-027 spezifiziert.

### Block B — Cron-Endpoints

| Endpoint | Header | HTTP | Body |
|----------|--------|------|------|
| POST `/api/cron/meeting-briefing` | (kein) | 401 | `{"error":"Unauthorized"}` |
| POST `/api/cron/meeting-briefing` | valid | 200 | `{"success":true,"processedCount":0,"skippedCount":0,"failedCount":0,"retryCount":0}` |
| POST `/api/cron/expire-proposals` (V5.5) | valid | 200 | `{"success":true,"expiredCount":0,"expiredIds":[]}` |

✓ V5.6-Cron live + funktional, V5.5-Cron unbeeintraechtigt (Regression-frei).

### Block C — Settings-Sidebar Routes

| Route | HTTP | Bedeutung |
|-------|------|-----------|
| `/settings/branding` | 307 | Auth-gated, Route registriert |
| `/settings/payment-terms` | 307 | Auth-gated, Route registriert |
| `/settings/briefing` | 307 | Auth-gated, Route registriert |

✓ Alle 3 Sidebar-Items haben funktionsfaehige Server-Routes. Sidebar-Component `settings/layout.tsx` rendert sie alle als klickbare Links (kein `disabled: true` mehr fuer Briefing — siehe `layout.tsx:24-28`).

### Block D — V5.5-Regression in DB

```
proposals_total           : 5
proposals_skonto_set      : 0  (User hat in SLC-562-Smoke wieder NULL gesetzt, both_or_none-Constraint greift)
milestones_total          : 0  (User hat in SLC-563-Smoke Plan wieder geleert)
payment_terms_templates   : 1  (Seed "30 Tage netto")
meetings_total            : 8
meetings_briefed          : 1  (RPT-283 E2E Test-Meeting)
activities_briefing       : 1  (RPT-283 E2E)
activities_briefing_error : 0
audit_briefing_generated  : 1
audit_briefing_retry_armed: 0
audit_briefing_failed_perm: 0
user_settings_rows        : 1  (Single-User)
```

✓ Konsistent mit RPT-280 + RPT-283. Keine Drift seit letzten Slice-QAs. V5.5-Daten unangetastet, V5.6-Schema-Erweiterungen ohne Daten-Loss appliziert.

### Block E — User-Settings Stand fuer AC18-Verifikation

```
trigger=30 | push=true | email=true | subscription=false
```

→ AC18-Voraussetzung ist erfuellt: `pushEnabled && !hasPushSubscription`. Mit Image `a7b787d` (VAPID-Fix) liest `page.tsx` jetzt `process.env.VAPID_PUBLIC_KEY` (statt `NEXT_PUBLIC_VAPID_PUBLIC_KEY`), uebergibt als Prop. `briefing-form.tsx:139-153` rendert Subscribe-Hint mit lila Banner + `<PushSubscribeButton>`.

**User-Browser-Smoke verbleibt** als finale Bestaetigung — Code-Pfad + ENV + Image-Tag alle korrekt.

## Wiring-Chain Cross-Cut

| Link | Status |
|------|--------|
| Settings-Sidebar → 3 Settings-Pages | ✓ alle 3 Routes 307 |
| Skonto-Toggle Editor → `useSkontoMutex(milestones)` | ✓ Hook liest `due_trigger==='on_signature' && percent===100` |
| `useSkontoMutex` → Auto-Clear via `useEffect` in `proposal-editor.tsx` | ✓ patchAndSave({skonto_percent:null,skonto_days:null}) bei false→true Transition |
| Editor `skonto_*` → `proposals_skonto_both_or_none` CHECK | ✓ DB-Constraint enforced (RPT-277 Validation-Smoke) |
| Cron → user_settings → both-off-skip | ✓ RPT-283 Block 2 + RPT-284 Block B |
| Cron → meetings filter → `briefing_generated_at IS NULL AND deal_id NOT NULL` | ✓ RPT-283 Block 4 |
| Cron → activities INSERT (briefing) → `source_type='meeting' + source_id + ai_generated=true` | ✓ RPT-282 Wiring-Fix bestaetigt + RPT-283 Live-DB-Verifikation |
| Activity → `deal-timeline.tsx` Switch-Case → `<ActivityBriefingCard>` | ✓ RPT-283 Workspace-Render |
| Other Activity-Types (note/call/email/inbox/meeting/stage_change/signal) | ✓ Switch faellt durch zur generischen Render-Lane, unbeintraechtigt |
| AC18: Subscribe-Hint Code-Pfad | ✓ post-VAPID-Fix Image `a7b787d` |

**Wiring-Chain komplett, keine Cross-Cut-Gaps.**

## Findings

### Blocker
Keine.

### High
Keine.

### Medium
Keine. (RPT-283 hatte 1 Medium-Finding VAPID-ENV-Mismatch — sofort gefixt im selben Run, jetzt mit Image `a7b787d` deployed verifiziert.)

### Low

- **AC18 Browser-User-Smoke verbleibt:** Subscribe-Hint-Render im Browser kann nur via realer User-Login verifiziert werden. Code-Pfad + ENV + Image alle korrekt — nur visuelle Bestaetigung steht aus. Nicht release-blockierend (User-Side post-Redeploy in 30s pruefbar).
- **Coolify-Cron `meeting-briefing`-Anlage** muss vor Go-Live durch User in Coolify-UI angelegt werden. REL-022-Notes haben die Anleitung. Ohne diesen Schritt bleibt der Cron-Endpoint zwar erreichbar, aber kein Auto-Trigger. Wird in `/go-live V5.6` geprueft.
- **ISSUE-049 BL-419** UI-State-Drift im Skonto-Toggle nach Auto-Save-Error — Low-Priority, Cosmetic, selbstheilend nach Reload, V5.7+ (RPT-277 carry-over).
- **221 pre-existing Lint-Errors** — kein V5.6-Verschulden, separater Tech-Debt-Slice.

## Open Risks

- **AC18 Browser-Verifikation** — User-Side, ~30s nach Redeploy.
- **Coolify-Cron-Anlage** — User-Aktion in `/go-live` (Anleitung in REL-022).
- **Mobile-Browser-Smoke /settings/briefing** — RPT-283 Desktop-Smoke PASS, Mobile separat (Browser-DevTools oder echtes Geraet, low-Risk durch responsive `md:`-Breakpoints).

## V5.6 Gesamt-Status

**Alle 4 Slices done + Live-Smoke PASS:**

| Slice | Phase | Live-Verifikation | Report |
|-------|-------|-------------------|--------|
| SLC-561 | done | Schema MIG-027 + Sidebar + Templates-CRUD live | RPT-272 + RPT-273 + RPT-274 |
| SLC-562 | done | PaymentTermsDropdown + Skonto-Toggle + PDF live | RPT-275 + RPT-276 + RPT-277 |
| SLC-563 | done | Split-Plan + Skonto-Mutex + Auto-Clear + PDF-Konditionen-Block live | RPT-278 + RPT-279 + RPT-280 |
| SLC-564 | done | Cron + Settings-Page + ActivityBriefingCard + E2E-Bedrock-JSON live | RPT-281 + RPT-282 + RPT-283 + VAPID-Fix `a7b787d` |

V5.6 ist **bereit fuer /final-check**.

## Test-Datenstand auf Hetzner (unveraendert seit RPT-283)

- Meeting `d1c0a843-24b9-4f30-9e95-8712c68740cd` (SLC-564 E2E Smoke Test)
- Activity `17130780-7c54-4384-a8de-d09e30f1b05d` (type='briefing')
- Audit-Log `ai_briefing_generated` Eintrag
- User-Settings: trigger=30, push=true, email=true, subscription=false (User-Browser-Subscribe steht aus)

Cleanup auf User-Wunsch (DELETE-Statements in RPT-283).

## Records-Updates

- `docs/STATE.md` → Phase `qa-pass`, Current Focus auf "V5.6 Gesamt-QA PASS, naechste = /final-check V5.6"
- `docs/KNOWN_ISSUES.md` → keine neuen Issues. ISSUE-049 (BL-419) bleibt open Low (V5.7+). VAPID-Drift nicht als Issue aufgenommen, da bereits gefixt + deployed.
- `reports/RPT-284.md` → diese Datei

## Recommended Next Step

1. **(Optional 30s)** User-Browser-Smoke `/settings/briefing` → AC18 Subscribe-Hint sichtbar bestaetigen → optional auf Subscribe-Button klicken fuer push_subscription-Setup (ermoeglicht spaeter echten Push-Test in /go-live).
2. **/final-check V5.6** — Hygiene + Dependencies + Security + Lint-Cleanup-Plan.
3. **/go-live V5.6** — User-Aktion: Coolify-Cron `meeting-briefing` anlegen (Anleitung in REL-022).
4. **/deploy V5.6** als REL-022.
5. **/post-launch** nach 24-48h Stable-Window.
