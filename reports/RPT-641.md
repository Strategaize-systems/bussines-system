---
id: RPT-641
date: "2026-06-12"
skill: /qa
slice: SLC-913
feature: FEAT-925
type: review
status: reviewed
reviewOf: RPT-640
result: "PASS Code-Side (0B/0H/0M/2 Low) — MIG-052-Semantik live bewiesen via transaktionale Probe 7/7, Live-Apply + DB-Verify-Lauf deferred zu /deploy (gated)"
title: "V8.15 SLC-913 MT-1 /qa — MIG-052 profiles authz-Spalten-Lock PASS Code-Side"
---

# V8.15 SLC-913 MT-1 — /qa (MIG-052 profiles authz-Spalten-Lock)

## 1. QA Scope Reviewed

Commit `cbaad69` (MT-1): `sql/migrations/052_v815_slc913_profiles_authz_protect.sql`, `cockpit/__tests__/migrations/052-v815-profiles-authz-protect.test.ts`, `sql/_probe/mig052_semantic_probe.sql`, Records (MIGRATIONS.md/STATE.md/slices/INDEX.md/RPT-640). Abgleich gegen Slice-Spec MT-1 + AC-913-1 + R-913-1.

## 2. Result: PASS (Code-Side) — 0 Blocker / 0 High / 0 Medium / 2 Low

## 3. Verifikation im Detail

### AC-913-1-Abgleich

| AC-Bestandteil | Status |
|---|---|
| Guard deckt ALLE authz-Spalten (role + team_id) | PASS — Inventar live via `\d profiles` + Policy/Helper-Read; `team` (text) begruendet ausgenommen (kein Policy-Bezug) |
| Trigger BEFORE INSERT OR UPDATE | PASS — INSERT-Branch mit TG_OP-Guard (OLD bei INSERT nicht referenziert), non-service_role nur role='member' + team_id NULL |
| authenticated team_id-UPDATE → BLOCKED | PASS — Live-Probe T1 (42501) + Test-052 Case 1 |
| service_role team_id-UPDATE → OK | PASS — Live-Probe T2 + Test-052 Case 2 |
| authenticated role-UPDATE → BLOCKED (MIG-051-Regression) | PASS — Live-Probe T3 + Test-052 Case 3 |
| harmlose Spalte → OK | PASS — Live-Probe T4 + Test-052 Case 4 |
| authenticated INSERT role='admin' → BLOCKED | PASS — Live-Probe T5 + Test-052 Case 5 |
| DB-Verify-Test als `cockpit/__tests__/migrations/052-*.test.ts` | PASS — 8 Cases (5 Spec + unprivilegierter INSERT + service_role-INSERT/Invite-Pfad + tgtype-Bit-Check) |
| Live-Apply via sql-migration-hetzner.md | DEFERRED by design → /deploy V8.15 (gated auf V8.14 STABLE) |

### R-913-1 (BLOCKING) — entschaerft

- Gate ist `current_user = 'service_role'`-Early-Return (NICHT `NOT is_admin()`).
- changeRole (`lib/team/actions.ts:163`) + Invite-Insert (`lib/auth/invite.ts:73`) laufen via createAdminClient/service_role — code-verifiziert; kein weiterer App-Pfad schreibt role/team_id.
- **Live-Semantik-Probe 7/7 PASS** auf der Coolify-DB (BEGIN..ROLLBACK, Nach-Kontrolle: Live-Trigger tgtype=19 = MIG-051-Stand unveraendert, nichts persistiert).
- Kein auth.users-Trigger legt profiles an (live, 0 Rows) — Signup-Pfad nicht betroffen.

### Konsistenz-Checks

- **Drift-Check Migration vs. Probe:** Funktions-Body diff = nur Kommentarzeilen → die Probe hat exakt die Migration-Semantik getestet.
- **Test-051-Kompatibilitaet:** role-Fehlermeldung wortgleich zu MIG-051 (`profiles.role change denied …`) → Regex `/role change denied/i` + Trigger-/Function-Existenz-Checks in Test 051 bleiben gueltig, kein Test-Touch.
- **Idempotenz:** CREATE OR REPLACE + DROP TRIGGER IF EXISTS + CREATE TRIGGER. Rollback dokumentiert (Re-Apply MIG-051).
- **Vitest-Include-Hygiene:** Suite-Count unveraendert 162 Files / 1485 Tests → Test 052 korrekt ausserhalb default-include (laeuft nur explizit im /deploy-Fenster).

### Stub-Detection

0 Treffer (TODO/FIXME/HACK/console.log/debugger) in beiden neuen Files. Keine Platzhalter-Returns, keine leeren Handler.

### Gates (IMP-1108)

TSC=0 ✓, ESLint neue Files=0 ✓, Full-Vitest jsdom 1485/1485 GREEN ✓ (0 Regress vs. V8.14-Baseline).

## 4. Findings

#### Low
- **L-1 (by design):** MIG-052 NICHT live-applied; DB-Verify-Test-052-Lauf gegen Coolify-DB steht aus → /deploy V8.15 (gated auf V8.14 STABLE /post-launch T+24h ~2026-06-13 13:35 UTC). ISSUE-109/122 bleiben korrekt `open`.
- **L-2 (pre-existing Baseline):** `npx eslint .` meldet 218 Probleme in Alt-Files (bekannt seit V8.11-/final-check "142e") — keines in V8.15-Files. Kein Fix per surgical-changes-Rule.

## 5. Open Risks / Missing Checks

- Die Live-Probe lief als postgres-Session mit `set_config('role', …)` — das ist semantisch aequivalent zu PostgREST-`SET ROLE`, aber der finale Beweis im echten Stack (PostgREST-Request) kommt mit dem DB-Verify-Test + Self-Mutation-Smoke im /deploy (analog AC-912-10-Praezedenz).

## 6. Readiness Assessment

MT-1 ist Code-Side ready. Migration semantisch live bewiesen, Tests vorbereitet, Records konsistent. Kein Blocker fuer MT-2.

## 7. Next Workflow Step

`/backend SLC-913 MT-2` — createAdminClient-Ownership-Sweep (ISSUE-111/112/117/121, Pattern-Reuse V8.12 SLC-906 User-Client-Switch). Danach /qa MT-2. (V8.15 hat 1 Slice; Gesamt-/qa folgt nach MT-7, dann /final-check → /go-live → /deploy mit MIG-052-Apply → /post-launch.)
