---
id: RPT-539
date: "2026-05-24"
skill: /frontend
slice: SLC-852
feature: BL-491
type: completion
status: completed
reviewOf: null
result: "SLC-852 Compose-Preview tenantSlug-Drift-Fix code-side komplett. Vitest 13/13 PASS, TSC clean fuer SLC-852-Files, Build clean. ISSUE-081 RESOLVED. Live-Smoke deferred bis Coolify-Redeploy."
title: "SLC-852 Compose-Preview tenantSlug-Drift-Fix (ISSUE-081) — /frontend Completion"
---

# RPT-539 — SLC-852 Compose-Preview tenantSlug-Drift-Fix Completion

## 1. Outcome Summary

SLC-852 Code-Side ist **vollstaendig abgeschlossen**. Vier Files modifiziert (3 Component-Files + 1 Test-File), 5 MTs aus der Slice-Spec planmaessig durchlaufen (MT-1..MT-4 + Verifikation, MT-5 Live-Smoke deferred bis Coolify-Redeploy per `feedback_no_coolify_branch_switch_ever`). ISSUE-081 (Preview vs. Send-HTML-Drift) ist **RESOLVED**. Pattern aus V8.4 `send-action.ts` (Server-Side `getTenantSlugByOwnerUserId` → Prop-Chain → 4. Param fuer `renderBrandedHtml`) 1:1 portiert. Bit-Identity-Garantie (DEC-095 Single-Source-of-Truth) durch +2 Vitest-Cases enforced.

Verifikations-Konfidenz: **mittel-hoch** — Build + Vitest + TSC + Lint clean; Live-Smoke (Browser-DOM-Assertion gegen Compose-Preview-iframe) deferred bis Master-Push + Coolify-Redeploy.

## 2. Files Reviewed

- `/slices/SLC-852-compose-preview-tenantslug-fix.md` — Spec mit 5 MTs + 8 ACs + Worktree-Isolation (optional fuer Internal-Tool)
- `/cockpit/src/app/(app)/emails/compose/page.tsx` — Server Component (MT-1 Target)
- `/cockpit/src/app/(app)/emails/compose/compose-studio.tsx` — Client Component (MT-2 Target)
- `/cockpit/src/app/(app)/emails/compose/live-preview.tsx` — renderBrandedHtml-Caller (MT-3 Target)
- `/cockpit/src/lib/team/lookup-slug.ts` — `getTenantSlugByOwnerUserId` Pattern-Source (Read-only, kein Edit)
- `/cockpit/src/app/(app)/emails/compose/send-action.ts` — Pattern-Source fuer Server-Resolve (Read-only)
- `/cockpit/src/app/consent/[token]/page.tsx` — Pattern-Validation (Read-only)
- `/cockpit/src/lib/email/send.ts` — Send-Pfad Pattern-Validation (Read-only)
- `/cockpit/src/lib/email/render.test.ts` — MT-4 Target (13/13 PASS)
- `/cockpit/src/lib/email/render.ts` — Signatur-Bestaetigung (`tenantSlug?: string` als 4. Param vorhanden)
- `/cockpit/src/app/(app)/mein-tag/page.tsx` — Pattern-Validation `supabase.auth.getUser()` (Read-only)
- `/docs/KNOWN_ISSUES.md` — ISSUE-081 Status-Update
- `/docs/STATE.md` — Current Focus + Immediate Next Steps Update
- `/slices/INDEX.md` — V8.5 Slice-Status Update
- `/planning/backlog.json` — BL-491 Status-Update
- `/planning/roadmap.json` — V8.5 Progress-Update

## 3. Files Changed

- `/cockpit/src/app/(app)/emails/compose/page.tsx`
  - **MT-1:** Import `getTenantSlugByOwnerUserId` ergaenzt. Im Body vor `Promise.all` neu: `supabase.auth.getUser()` + `currentUserTenantSlug = user ? (await getTenantSlugByOwnerUserId(user.id)) ?? undefined : undefined`. Prop `currentUserTenantSlug={...}` an `<ComposeStudio>` ergaenzt. Grund: AC1 Server-Side-Resolve fuer Bit-Identity mit Send-Pfad. Pattern mirror aus `send-action.ts` Zeilen 104-108.
- `/cockpit/src/app/(app)/emails/compose/compose-studio.tsx`
  - **MT-2:** Props-Interface `ComposeStudioProps` um `currentUserTenantSlug?: string` mit JSDoc erweitert. Function-Destructure-Param + Pass-Through als `tenantSlug={currentUserTenantSlug}` an `<LivePreview>`. Grund: AC2 Prop-Chain. Null-Toleranz dokumentiert (Solopreneur ohne team_id → graceful Fallback).
- `/cockpit/src/app/(app)/emails/compose/live-preview.tsx`
  - **MT-3:** Props-Interface `LivePreviewProps` um `tenantSlug?: string` mit JSDoc erweitert. Function-Destructure-Param. `useMemo`-Block: `renderBrandedHtml(debouncedBody, branding, vars, tenantSlug)` (4. Param) + Dep-Array `[debouncedBody, branding, vars, tenantSlug]`. Grund: AC3 Render-Call-Erweiterung + Re-Render bei Slug-Change. ESLint `react-hooks/exhaustive-deps` deckt Dep-Array-Korrektheit.
- `/cockpit/src/lib/email/render.test.ts`
  - **MT-4:** Neue `describe`-Suite `"renderBrandedHtml — SLC-852 Preview-vs-Send Bit-Identity"` mit 2 Tests: (a) "Preview = Send: bit-identical output with tenantSlug (DEC-095 SST)" — doppelter Call mit `'strategaize-transition-bv'`, Assertions `toBe(sendHtml)` + DSE-URL-Substring; (b) "Preview = Send: bit-identical output without tenantSlug (Solopreneur-Fallback)" — doppelter Call ohne tenantSlug, Assertions `toBe(sendHtml)` + `not.toContain("/datenschutz")`. `beforeEach`/`afterEach` setzen `NEXT_PUBLIC_APP_URL`. Vitest gesamt 13/13 PASS (11 vorher + 2 neu). Grund: AC4 (existing 11 gruen) + AC5 (DSE-Substring) + AC8 (Bit-Identity).
- `/docs/KNOWN_ISSUES.md`
  - ISSUE-081 Status `open → resolved`. Heading-Suffix `[RESOLVED 2026-05-24]`. Neue `Resolution:`-Section beschreibt SLC-852-Loesung: Server-Component-Resolve + Prop-Chain + 4. Param + Vitest +2 Cases + Tracking-Layer-Out-of-Scope-Hinweis. Workaround-Label angepasst auf `Workaround (pre-SLC-852):`. `Next Action`-Section entfernt (deprecated). Grund: ISSUE-Lifecycle-Update + Audit-Trail.
- `/docs/STATE.md`
  - Current Focus aktualisiert: `SLC-851 DONE → SLC-852 DONE 2026-05-24 RPT-539`. Beschreibt Server-Component-Resolve + Prop-Chain + Vitest 13/13 + ISSUE-081 RESOLVED + Live-Smoke deferred. Current Phase: `SLC-851 done` → `SLC-851 + SLC-852 done`. Immediate Next Steps reorganisiert: (1) `/qa SLC-852`, (2) `/backend SLC-853`, (3) Master-Merge + Coolify-Redeploy + Live-Smoke SLC-852 AC7 + /post-launch V8.5. Grund: Cockpit-Visibility + Next-Step-Engine.
- `/slices/INDEX.md`
  - SLC-852-Zeile Status `planned → done`. Grund: V8.5-Bundle-Progress 1/3 → 2/3.
- `/planning/backlog.json`
  - BL-491 Status `in_progress → done`. Description um Done-Hinweis ergaenzt: `DONE 2026-05-24 RPT-539: page.tsx + compose-studio.tsx + live-preview.tsx + render.test.ts (+2 Cases) — Vitest 13/13 PASS, TSC clean SLC-852-files, Build clean. Live-Smoke deferred bis Coolify-Redeploy.`. Grund: Roadmap-Progress-Calc + Backlog-Hygiene.
- `/planning/roadmap.json`
  - V8.5 Summary aktualisiert: SLC-851 + SLC-852 als `DONE 2026-05-24` markiert. Progress-Hint `Progress: 2/3 done` + Naechster Schritt `/qa SLC-852, dann /backend SLC-853`. Grund: Roadmap-Visibility.

## 4. Files Created

- `/reports/RPT-539.md` (dieser Report)
  - Zweck: Frontend-Completion-Report fuer SLC-852 per mandatory-completion-report-Regel. Enthaelt Audit-Trail fuer V8.5 + /qa-Input.

**Keine** anderen neuen Files. Insbesondere KEIN neues Helper-Module — Pattern-Reuse `getTenantSlugByOwnerUserId` ohne Refactor (per `strategaize-pattern-reuse.md` Rule).

## 5. Files Intentionally Not Changed

- `/cockpit/src/lib/email/render.ts` — Signatur `renderBrandedHtml(body, branding, vars, tenantSlug?)` bleibt unveraendert (V8.4 SLC-846 Public-API). Grund: AC-Out-of-Scope + bestehende DSE-Footer-Implementierung deckt SLC-852-Erweiterung ohne neuen Code.
- `/cockpit/src/lib/email/send.ts` + `/cockpit/src/app/(app)/emails/compose/send-action.ts` — Send-Pfad bleibt wie V8.4-Hotfix. Grund: AC-Out-of-Scope. Bit-Identity wird durch identische Function-Calls (gleiche Args) implizit garantiert; render.test.ts MT-4 macht das explizit testbar.
- `/cockpit/src/app/(app)/emails/compose/compose-form.tsx` — Form-Component nicht beruehrt, da `compose-studio.tsx` der State-Owner ist und `tenantSlug` direkt an `live-preview.tsx` reicht. Grund: Lean Surgical Change (keine unnoetigen Prop-Chains).
- `/cockpit/src/app/(app)/emails/compose/new-template-dialog.tsx` + `/templates-panel.tsx` — Template-Pfad nutzt nicht `renderBrandedHtml` als Preview, sondern Template-Pure-Text. Grund: Kein Drift in diesem Pfad, AC-Scope respektiert.
- `/cockpit/src/lib/team/lookup-slug.ts` + `lookup-slug.test.ts` — Helper bleibt wie V8.4. Grund: 100% Pattern-Reuse, kein Anpassungsbedarf.
- Whisper-/Bedrock-/Cron-Adapter — komplett out-of-scope.

## 6. UI States Covered

**Implementiert (Code-Side):**
- Default-State (User mit team_id): Live-Preview zeigt DSE-Footer-Block bit-identisch zur Send-Mail (`/p/<slug>/datenschutz` Link mit "Datenschutzerklaerung"-Linktext)
- Solopreneur-Fallback (User ohne team_id, `currentUserTenantSlug = undefined`): Live-Preview zeigt KEIN DSE-Footer (graceful, konsistent mit Send-Verhalten)
- Loading-Phase (debounce 250ms via `useDebouncedValue`): unveraendert, useMemo neu rendert auf Slug-Change
- HTML-Escape-Safety (XSS-Resistant): existing render.ts deckt das via Vitest "escapes tenantSlug in DSE-URL to prevent XSS via slug"

**Verifiziert (direkt):**
- Vitest 13/13 PASS deckt: Default-State (bit-identity-with-tenantSlug), Solopreneur-Fallback (bit-identity-without-tenantSlug), DSE-URL-Substring-Match, Anti-Regression (kein DSE-Footer wenn tenantSlug=undefined), HTML-Escape-Safety (existing).
- TSC clean fuer alle 4 modifizierten SLC-852-Files (deckt Prop-Type-Konformitaet, useMemo-Dep-Array-Korrektheit, optional-Prop-Handling).
- Build clean (Next.js Route-Table generiert, kein Compile-Fehler in der Compose-Page).
- Lint clean (kein neuer Warning/Error in den 4 Files).

**NICHT direkt verifiziert (Code-Inspection-only, Live-Smoke deferred):**
- Browser-Rendering: Compose-Studio-iframe-DOM enthaelt sichtbar den DSE-Footer-Block
- Interaction: typing in Body-Textarea → 250ms-Debounce → re-Render mit DSE-Footer
- Mobile-Tab-State: `mobileTab === "preview"` rendert previewSlot mit tenantSlug-Prop korrekt
- Responsive-Verhalten: 3-Panel-Desktop-Layout vs. Mobile-Tabs (Layout unveraendert, aber nicht visuell verifiziert)

## 7. Problems Found

1. **TSC-Errors in 4 unrelated Test-Files (Pre-Existing, NOT verursacht durch SLC-852):**
   - `cockpit/src/components/ki-workspace/hooks/__tests__/useVoiceCapture.test.tsx(69,36)`: TS2493 Tuple length 0
   - `cockpit/src/lib/proposal/reverse-charge-revert.test.ts(16,38)`: TS2559
   - `cockpit/src/lib/proposal/skonto-revert.test.ts(19,31)`: TS2559
   - `cockpit/src/lib/validation/vies-client.test.ts` (5 Errors): TS2345 + TS2741 NODE_ENV-Drift
   - **Root Cause:** Pre-existing Test-Hygiene-Drift (vermutlich nach Next.js 16 / TypeScript-Upgrade ohne Test-Folge-Anpassung). Build skipt Test-Files via `tsconfig.json`, Production-Code unbetroffen.
   - **NICHT in V8.4 ISSUE-084 enthalten** (ISSUE-084 betrifft `legal-documents-rls.test.ts` UNIQUE-Konflikt, anderer Bug).
   - **Empfehlung:** Mit ISSUE-084 in einen V8.6 Test-Hygiene-Slice bundeln (~30-45 Min). Neue ISSUE-085 koennte angelegt werden — oder einfach in der V8.6-Slice-Spec dokumentieren.

2. **Live-Smoke MT-5 deferred bis Coolify-Redeploy:**
   - Per `feedback_no_coolify_branch_switch_ever` darf nicht zwischen Slice-Branches geswitcht werden.
   - Aktueller Stand: alles auf `main`, kein Branch-Wechsel noetig.
   - Aber: `feedback_deferred_live_smoke_completion` warnt vor "deferred Live-Smoke fuer Output-aendernde Slices" — der Output (HTML der Preview) AENDERT sich, dies ist also output-relevant.
   - **Mitigation:** Vitest +2 Cases (`Preview = Send bit-identical`) machen die Hauptaussage AUTOMATISCH testbar. Live-Smoke wird dadurch zur Bestaetigungs-Pflicht (nicht Discovery-Pflicht).
   - **Empfehlung:** Live-Smoke nach Coolify-Redeploy V8.5-Bundle (gemeinsam mit SLC-853-Bundle, OQ-2 aus Handoff).

3. **OQ-2 aus Handoff bleibt offen:** Master-Merge-Strategie SLC-852 sofort vs. Bundle mit SLC-853.
   - Empfehlung weiterhin: Bundle-Merge nach SLC-853 (kleinerer Coolify-Ops-Aufwand, folgt V8.4-Pattern aus `feedback_slice_merge_at_end`).

## 8. Open Points or Deferred Decisions

- **MT-5 Live-Smoke (deferred):** AC7 Compose-Studio Preview-iframe-DOM-Assertion (Playwright-MCP oder User-Action) erst nach Master-Merge + Coolify-Redeploy. Verifiziert: iframe-Content endet mit DSE-Footer-`<tr>`-Block + `Datenschutzerklaerung`-Link, sichtbar im Browser.
- **OQ-2 Master-Merge-Strategie:** Bundle SLC-852 + SLC-853 vs. sofortiger Single-Slice-Deploy. Empfehlung Bundle, finale Entscheidung User.
- **ISSUE-085-Kandidat:** Test-Hygiene fuer 4 unrelated `.test.ts/.tsx`-Files (TSC-Errors pre-existing). V8.6-Slice oder als Quick-Fix vor SLC-853 (~30-45 Min). Aufnahme in KNOWN_ISSUES.md nicht erfolgt — auf User-Entscheidung warten ob neu-anlegen oder mit ISSUE-084 bundlen.

## 9. Runtime Verification Summary

- **Dev-Server:** Nicht gestartet (Code-Inspection + Build + Vitest reichen fuer Frontend-Slice ohne neue UI-States; Browser-Smoke wird in MT-5 nachgeholt).
- **Production-Build:** `npm run build` **PASS** (Next.js Route-Table komplett generiert, kein Compile-Fehler in der Compose-Page-Chain).
- **Vitest:** `npm run test -- src/lib/email/render.test.ts` **13/13 PASS** (~1.54s). Pre-V8.5 Baseline 11/11.
- **TSC:** `npx tsc --noEmit` clean fuer SLC-852-Files. 8 Pre-Existing-Errors in 4 unrelated Test-Files (siehe Problem 1).
- **Lint:** `npx eslint <SLC-852-files>` **clean** (kein Warning/Error in den 4 modifizierten Files).
- **Browser-Render:** NICHT direkt verifiziert (deferred MT-5).
- **Interaction:** NICHT direkt verifiziert (deferred MT-5).
- **Persistence:** N/A (Preview ist read-only Render).
- **Responsive:** NICHT direkt verifiziert (3-Panel-Desktop vs. Mobile-Tabs Layout unveraendert).

Verifikation basiert primaer auf: **Build + Static Code Inspection + Vitest (Pure-Unit, deckt Render-Output-Bit-Identity)**.

## 10. Recommended Next Step

**`/qa SLC-852`** — Per mandatory-completion-report Rule §9 ("QA after implementation steps") + `feedback_qa_after_every_slice`. /qa wird systematisch AC1..AC8 verifizieren (Server-Resolve-Pfad, Prop-Chain, useMemo-Dep, Vitest-Coverage, TSC, Build, Lint, plus ggf. Test-Coverage-Gap-Pruefung).

Nach `/qa SLC-852` PASS:
- **`/backend SLC-853`** (Mail-Renderer-Audit + DSE-Footer fuer Consent + Briefing, ~2-3h, hoechster Compliance-Value).
- Dann: Master-Merge + Coolify-Redeploy + Live-Smoke SLC-852 AC7 + `/post-launch` V8.5 Burn-In.

**Optional vor SLC-853:** ISSUE-085-Quickfix (Test-Hygiene 4 pre-existing TSC-Errors), wenn User in derselben Session weiter macht — sonst V8.6-Defer mit ISSUE-084 bundeln.
