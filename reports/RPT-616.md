---
id: RPT-616
date: "2026-06-10"
skill: /backend
slice: SLC-908
feature: FEAT-922
type: completion
status: completed
reviewOf: null
result: "SLC-908 V8.12 Passwort-Policy 12+ + zxcvbn /backend KOMPLETT Code-Side — 6 MTs, zxcvbn ^4.4.2 + @types/zxcvbn ^4.4.5. password-policy.ts validatePasswordStrength (12-Hard-Floor + zxcvbn-Score>=3, dynamic import R-V812-3) + 8 Vitest; set-password/actions.ts Policy-Enforcement + 5 Vitest; PasswordStrengthIndicator 5-Segment-Bar + 3 Vitest; page.tsx Indicator-Wiring + minLength 8->12. Spec-Drift (akzeptiert): accept-invitation/actions.ts existiert nicht — set-password ist einziger Neu-Passwort-Entry-Point, deckt Invite-Accept ab (analog SLC-906, kein neuer DEC). Bundle: zxcvbn in genau 1 Async-Chunk (800K), nicht im Main-Bundle -> AC-908-5 PASS. TSC=0, ESLint=0 (7 Files), Vitest Full 1383/1383 (war 1367 +16), next build OK. Browser-Manual-Smoke des Indicators offen -> /qa. Strategaize-Cross-Repo-Origin-Pattern fuer OP/IS/immoscheckheft."
title: "V8.12 SLC-908 Passwort-Policy /backend Completion Report"
---

# RPT-616 — V8.12 SLC-908 Passwort-Policy 12+ + zxcvbn /backend Completion Report

## 1. Outcome summary

SLC-908 (BL-502, FEAT-922 Phase 2.2) **Code-Side KOMPLETT**. Zentrale Passwort-Policy
(Hard-Floor 12 Zeichen + zxcvbn-Score ≥ 3, DEC-278/DEC-282) eingefuehrt, im
einzigen Neu-Passwort-Entry-Point (set-password-Flow) durchgesetzt, mit Live-Visual-
Indicator. zxcvbn via dynamic import als Lazy-Chunk (R-V812-3). Alle 6 MTs erledigt,
alle Quality-Gates gruen. **TDD eingehalten** (RED→GREEN fuer Helper + Action).

Strategaize-Cross-Repo-Origin-Pattern (BS V8.12 ist Origin, RPT-608) — wird Reuse-
Vorlage fuer OP V9.x+ / IS V4.x+ / immoscheckheft V3.x+.

Status: **fully complete (Code-Side)**. Offen: Browser-Manual-Smoke des Indicators (→ /qa).

## 2. Files reviewed

- `/cockpit/src/app/auth/set-password/actions.ts` (vor Edit)
- `/cockpit/src/app/auth/set-password/page.tsx` (vor Edit)
- `/cockpit/src/lib/auth/invite.ts` (Invite-Flow-Mapping — bestaetigt: kein accept-invitation-Action)
- `/cockpit/src/lib/auth/invite.test.ts` (Test-Konvention)
- `/cockpit/package.json` (Deps + Scripts)
- `/cockpit/vitest.config.ts`, `/cockpit/vitest.setup.ts` (Test-Setup)
- `/cockpit/src/app/(app)/settings/compliance/customer-dse/actions.test.ts` (Mock-Pattern fuer redirect+createClient)
- `/cockpit/src/app/(app)/deals/deal-card.test.tsx` (Component-Test-Pattern)
- `/cockpit/qa/SLC-906-grep-audit.md` (qa/-Dir-Konvention)
- `/slices/SLC-908-v812-password-policy.md` (Slice-Spec)

## 3. Files changed

- `/cockpit/package.json` — `zxcvbn ^4.4.2` (dep) + `@types/zxcvbn ^4.4.5` (devDep) ergaenzt.
- `/cockpit/package-lock.json` — Lockfile-Update durch npm install.
- `/cockpit/src/app/auth/set-password/actions.ts` — alte `password.length < 8`-Pruefung ersetzt durch `await validatePasswordStrength()`; reasons→deutsche Fehlermeldungen; typeof-Guard beibehalten; Header-Kommentar auf realen Caller (page.tsx) + V8.12-Policy aktualisiert.
- `/cockpit/src/app/auth/set-password/page.tsx` — `PasswordStrengthIndicator` unter Password-Input gewired (controlled `password`-State); `minLength` 8→12 + Placeholder „Mindestens 12 Zeichen" fuer beide Inputs.

## 4. Files created

- `/cockpit/src/lib/auth/password-policy.ts` — `validatePasswordStrength()` + `PASSWORD_MIN_LENGTH`/`PASSWORD_MIN_SCORE`/`PasswordStrengthResult`. Origin-Pattern.
- `/cockpit/src/lib/auth/__tests__/password-policy.test.ts` — 8 Vitest (AC-908-1..4 + 3 Length/Score-Edges + Konstanten).
- `/cockpit/src/app/auth/set-password/__tests__/actions.test.ts` — 5 Vitest (Policy-Enforcement + Match-Check + updateUser/redirect + Error-Pfad).
- `/cockpit/src/components/auth/PasswordStrengthIndicator.tsx` — Client-Component, 5-Segment Tailwind-Bar, debounced zxcvbn via dynamic import.
- `/cockpit/src/components/auth/__tests__/PasswordStrengthIndicator.test.tsx` — 3 Vitest (empty/weak/strong, zxcvbn gemockt).
- `/cockpit/qa/SLC-908-bundle-analysis.md` — Lazy-Chunk-Verifikation (AC-908-5 / R-V812-3).
- `/reports/RPT-616.md` — dieser Report.

## 5. Files intentionally not changed

- `/cockpit/src/lib/auth/invite.ts` — Invite-Flow setzt KEIN Passwort selbst (generateLink → Mail → /auth/callback → /auth/set-password). Policy greift im set-password-Action; invite.ts braucht keine Aenderung.
- `/cockpit/src/lib/supabase/middleware.ts`, `/cockpit/src/app/auth/callback/route.ts` — Login-/Session-/verifyOtp-Pfad ist explizit OUT-OF-SCOPE (DEC-278: nur neue Passwoerter, Bestands-User/Login unangetastet).
- Kein neues DEC/MIG — keine Schema-Aenderung, DEC-278/DEC-282 decken die Policy bereits ab.

## 6. Problems found

- **Spec-Drift D-908-1 (akzeptiert, kein Blocker):** Spec MT-3 verlangt Edit von `accept-invitation/actions.ts`. Diese Datei/Route existiert im BS NICHT. Code-Audit (`grep updateUser` + forgot/reset/recovery-Suche) zeigt **set-password ist der einzige Neu-Passwort-Entry-Point**; Invites routen ueber `invite.ts`→`/auth/callback`→Redirect `/auth/set-password`. MT-2 deckt den Invite-Accept-Fall damit vollstaendig ab. AC-908-6 ist via set-password erfuellt. Handhabung analog SLC-906-Spec-Drift (dokumentieren, anpassen, kein neuer DEC) — KEINE Fake-accept-invitation-Route angelegt (waere Scope-Expansion).
- **Spec-Drift D-908-2 (akzeptiert):** Spec MT-2 schlaegt `return { error: reasons.join(', ') }` vor (Maschinen-Codes). Stattdessen deutsche User-Messages gemappt (konsistent mit bestehenden set-password-Strings) — bessere UX, AC-908-6-Intent erfuellt.
- **Verifikations-Gap (→ /qa):** PasswordStrengthIndicator ist Build- + Component-Test-verifiziert, aber NICHT live im Browser gerendert. Done-Gate „PasswordStrengthIndicator visuell verifiziert in set-password-Page (Manual-Smoke)" bleibt offen — gehoert in /qa.
- npm meldet 8 vorbestehende Vulnerabilities (7 moderate / 1 high) — NICHT durch zxcvbn eingefuehrt (Audit-Baseline pre-existing, /final-check-Thema).

## 7. Open points or deferred decisions

- Browser-Manual-Smoke des Live-Indicators (/qa).
- Force-Reset fuer Bestands-User → Pre-Customer-Live-Slot (Spec OUT-OF-SCOPE, DEC-278).
- Pwned-Password-Check (HaveIBeenPwned) → V8.x-Polish-Slot (Spec OUT-OF-SCOPE).
- Cross-Repo-Mirror OP+IS → separate Slots (Spec OUT-OF-SCOPE).

## 8. Project record updates

- `/slices/INDEX.md` — SLC-908 `planned` → `done`.
- `/planning/backlog.json` — BL-502 `in_progress` → `done`.
- `/features/INDEX.md` — FEAT-922 bleibt `in_progress` (SLC-909 + SLC-910 noch offen) — bewusst nicht geaendert.
- `/planning/roadmap.json` — V8.12 bleibt `active` — bewusst nicht geaendert.
- `/docs/STATE.md` — Current Focus auf SLC-908 /backend, alter SLC-911-Focus inline demoviert.
- `/docs/MIGRATIONS.md` — kein Update (keine Schema-Aenderung).
- `/docs/DECISIONS.md` — kein Update (kein neuer DEC).

Per IMP-950 BLOCKING: V8.12 NICHT released bis /go-live + /deploy + /post-launch T+24h. „Last Stable Version" in STATE bleibt unveraendert.

## 9. QA after implementation

`/qa SLC-908` ist der mandatorische naechste Schritt. Quality-Gates dieser /backend-Session:
- **TSC=0** ✓
- **ESLint=0** (7 Files) ✓
- **Vitest Full-Suite jsdom 1383/1383 GREEN** (150 Files; Baseline 1367 + 16 neue = exakt) ✓
- **`next build` standalone** — Compiled successfully, zxcvbn als 1 isolierter Async-Chunk (800K) ✓

## 10. Recommended next step

**`/qa SLC-908`** (~30 min): AC-908-1..8 Code-Side-Verifikation + Stub-Detection + Wiring-Chain + **Browser-Smoke des Live-Indicators** (schliesst den offenen Done-Gate-Punkt). Danach `/backend SLC-909` (A-V812-2 Pre-Check + MIG-050).
