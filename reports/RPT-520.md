---
id: RPT-520
date: "2026-05-23"
skill: /qa
slice: SLC-841
feature: FEAT-824
type: review
reviewOf: RPT-519
status: completed
result: "QA PASS — 7/7 RLS-Tests reproducible, 0 Stubs, 0 neue TSC-Errors, DB-State idempotent korrekt. AC3-Spec-Drift Low (Doku, kein Issue)."
title: "V8.4 SLC-841 QA — MIG-038 + RLS-Test Verifikation"
---

# V8.4 SLC-841 — QA Report

## 1. QA scope reviewed

- Migration-File `sql/migrations/038_v84_customer_dse.sql` (~117 Zeilen, neu)
- Vitest-Suite `cockpit/__tests__/rls/legal-documents-rls.test.ts` (~248 Zeilen, neu)
- DB-Live-Zustand auf Hetzner (Coolify-Supabase via SSH)
- Cockpit-Records-Konsistenz (STATE/MIGRATIONS/INDEX/backlog/roadmap)
- RPT-519 Backend-Completion-Report

Scope explizit AUSGESCHLOSSEN: Frontend (SLC-841 ist backend-only), Wiring-Verifikation (keine Component/API/UI im Slice), Lint-Sweep (kein TS-Production-Code-Touch).

## 2. Pass / fail / mixed result

**PASS** — alle pruefbaren ACs erfuellt, RLS-Tests reproduzierbar 7/7, kein Stub, kein neuer TSC-Fehler durch SLC-841-Code.

## 3. Findings grouped by severity

### Blocker
keine.

### High
keine.

### Medium
keine.

### Low
- **L-1 AC3-Spec-Drift (Dokumentations-Mismatch, kein Code-Issue)** — Slice-Spec AC3 nannte Erwartung `slug='strategaize-transition-bv'` fuer die einzige existing Production-Team. Tatsaechlich heisst Team `Strategaize`, Backfill ergab `slug='strategaize'`. Zweite Row `[TEST] Test-Team` korrekt zu `test-test-team` normalisiert. Slugs sind unique, lowercase, deterministisch. Behoben durch Doku-Update in `docs/MIGRATIONS.md` MIG-038-Verification + RPT-519 F-1. KEINE Code-Aenderung noetig. KEIN neuer ISSUE-Eintrag (Doku, nicht Bug).

### Reviewed and Accepted
- `__tests__/rls/legal-documents-rls.test.ts` enthaelt Test-Markierungen (`[TEST-SLC-841]`-Prefix, Sentinel-UUID `000...0b8841`) — Standard-Pattern aus `custom-reports-rls.test.ts` + `v7-rls-matrix.test.ts`, beforeAll/afterAll Cleanup garantiert keine Pollution.

## 4. Detail-Verifikation

### 4a. Stub-Detection
- `sql/migrations/038_v84_customer_dse.sql`: 0 Treffer fuer TODO/FIXME/HACK/XXX/debugger/console.log.
- `cockpit/__tests__/rls/legal-documents-rls.test.ts`: 0 Treffer fuer TODO/FIXME/HACK/debugger/console.log/empty handlers/placeholder returns.

### 4b. RLS-Test-Re-Run (Smoke-Reproduzierbarkeit)
```
docker run --rm --network k9f5pn5upfq7etoefb5ukbcg_business-net \
  -v /opt/business-system-test/cockpit:/app -w /app \
  -e TEST_DATABASE_URL='postgresql://postgres:***@supabase-db-...:5432/postgres' \
  node:20 npx vitest run --config vitest.rls.config.ts \
  __tests__/rls/legal-documents-rls.test.ts
```
Resultat: **Test Files 1 passed (1), Tests 7 passed (7), Duration 338ms** (transform 33ms, setup 0ms, import 79ms, tests 75ms).

Tests (alle PASS):
1. Tenant-A-Admin INSERT customer-dse erlaubt
2. Tenant-A-Member INSERT geblockt (`row-level security policy` Error)
3. Tenant-A-Member SELECT eigene Tenant-Row
4. Tenant-A-Member SELECT Tenant-B-Row → 0 (cross-tenant isolation)
5. Tenant-A-Member UPDATE eigene Row → 0 affected (admin-only mutate)
6. UNIQUE(tenant_team_id, kind) Duplicate → 23505
7. FK teams ON DELETE CASCADE (confdeltype='c')

### 4c. TSC-Check (Sanity)
- `npx tsc --noEmit` (project tsconfig.json) auf `/opt/business-system-test/cockpit`: **0 neue Fehler in `__tests__/rls/legal-documents-rls.test.ts`**.
- Pre-existing Fehler (nicht von SLC-841 verursacht, nicht in Scope): useVoiceCapture.test.tsx (TS2493), reverse-charge-revert.test.ts (TS2559), skonto-revert.test.ts (TS2559), vies-client.test.ts (TS2345/TS2741 ×5). Diese gehoeren in Test-Infra-Cleanup-Slice ggf. V8.5-Hygiene.

### 4d. DB-State-Re-Verifikation (idempotent-pruefbar)
- `legal_documents` table_count = 1 ✓
- `teams.slug IS NOT NULL` count = 2 (alle Teams haben Slug nach Backfill) ✓
- `legal_documents` row_count = 0 (Phase 5 deferred auf SLC-842, korrekt) ✓
- `pg_policy` count fuer legal_documents = 2 ✓
- `information_schema.role_table_grants` GRANTs fuer legal_documents auf authenticated+service_role = 8 (4×4 + 4×4) ✓

### 4e. Cockpit-Records-Konsistenz
- `/slices/INDEX.md` — SLC-841 = `done` ✓
- `/features/INDEX.md` — FEAT-824 = `in_progress` (1/7 Slices done, korrekt) ✓
- `/planning/backlog.json` — BL-488 = `in_progress` ✓
- `/planning/roadmap.json` — V8.4 = `active` ✓
- `/docs/STATE.md` — High-Level State `implementing`, Current Focus reflect SLC-841 DONE + Naechster Schritt /qa→/backend SLC-842 ✓
- `/docs/MIGRATIONS.md` — MIG-038 Status `Phase 1-4 APPLIED 2026-05-23` + Verification ✓

## 5. Open risks or missing checks

### Geprueft + akzeptiert
- **R-1 Phase 5 Defer-Risiko** — `legal_documents` ist post-Apply LEER. Public-Route SLC-843 wuerde 404 zurueck geben fuer alle Tenants. **Mitigation**: DEC-238 zwingt Reihenfolge SLC-842 vor SLC-843. Akzeptiertes Defer-Risiko, im Slice-Spec R2 + RPT-519 OP-1 dokumentiert.
- **R-2 Super-Admin sieht alle Tenants (V7-`is_admin()`-Semantik vs Multi-Tenant)** — Policy `legal_documents_select_team` mit `is_admin() OR tenant_team_id = get_my_team_id()` laesst Super-Admin alle Tenants sehen. Fuer V8.4 Internal-Test-Mode + Single-Customer = unkritisch. Bei echter Multi-Tenant-Realitaet ggf. Tenant-Admin-Rolle schaerfen — als V8.4-Followup-Discovery-Item dokumentiert in RPT-519 OP-2.

### Nicht geprueft (out of scope)
- Performance-Tests fuer Backfill (akzeptabel — 2 Rows, kein Lock-Hold, < 100ms)
- Negative-Tests fuer CHECK-Constraint kind != 'customer-dse' (durch UNIQUE-Constraint implizit, kein Lekprozess)
- Cross-Tenant-Admin-Mutate-Block (ein Admin in Tenant-A versucht Update in Tenant-B) — Policy WITH-CHECK `tenant_team_id = get_my_team_id()` blockt das implizit. Test-Coverage in SLC-844 wenn Editor-Server-Actions geschrieben werden.

## 6. Readiness assessment

**Ready for next slice.** SLC-841 ist code-side fully complete. Migration LIVE auf Hetzner, idempotent, mit voller RLS-Coverage. 7/7 RLS-Tests reproducible. Cockpit-Records aktuell + Cockpit-Visualisierungs-konform.

Confidence-Hoehe: **HIGH** fuer Code-Korrektheit, **MEDIUM-HIGH** fuer Multi-Tenant-Semantik (Super-Admin-Pattern bewusst akzeptiert).

Master-Merge: **NICHT JETZT** — laut Slice-Spec + DEC-238 + `feedback_slice_merge_at_end` erfolgt Master-Merge erst am Slice-Ende SLC-847 (alle 7 V8.4-Slices fertig).

## 7. Recommended next step

**`/backend SLC-842`** — Slug-Generator-Lib (1:1 OP-V7 Reuse aus `reference_partner_slug_pattern`) + Reserved-Slugs-Liste + Default-Markdown-File `cockpit/src/content/legal/customer-dse-default.md` + MIG-038 Phase 5 Apply (Default-Seed-INSERT). Aufwand ~2h. Spec siehe `slices/SLC-842-tenant-slug-generator-default-seed.md`.

Vorher: Commit + Push QA-Artefakte (RPT-520) auf Slice-Branch `slc-841-customer-dse-schema-migration`.
