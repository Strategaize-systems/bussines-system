---
id: RPT-653
date: "2026-06-14"
skill: /final-check
slice: SLC-913
feature: FEAT-925
type: completion
status: completed
reviewOf: null
result: "Final-Check V8.15 SLC-913 = READY for /go-live. 0 Blocker / 0 High / 0 Medium / 3 Low (alle pre-existing/accepted). next build PASS, tsc=0, Vitest 1608/1608, 0 secrets/console/dep-changes im Diff, ENV+Migrationen hygienisch. /deploy bleibt gated auf V8.14 STABLE + DB-Verify 052/053 + Live-Smokes."
title: "Final-Check V8.15 Security-Hardening — READY for go-live"
---

# Final-Check V8.15 — SLC-913 Security-Hardening Bundle

Pre-Release-Audit nach Gesamt-/qa PASS (RPT-652). Hygiene-, Secret-, Dependency-, Env-, Build- und Release-Readiness-Dimensionen. Die Security-Substanz selbst ist intrinsisch abgedeckt: dieser Slice IST das Ergebnis des Fable-5 Multi-Agent Re-Audits, und /qa hat Wiring 7/7 + Guards + MIG-052 verifiziert.

## 1. Scope reviewed
- 28 geänderte Prod-Files (Diff `origin/main..HEAD`, 18 Commits inkl. RPT-652-Doc)
- `cockpit/.env.example`, `sql/migrations/051..053`, `docs/KNOWN_ISSUES.md`, `docs/STATE.md`, `package.json`/Lockfile
- Production-Build (`next build`)
- Carry-in aus /qa: L-1 (rotes Projekt-Lint-Baseline)

## 2. Overall assessment: **READY for `/go-live`**

Code-Side release-credible. Keine Blocker/High/Medium. `/deploy` bleibt planmäßig gated (V8.14 STABLE + Live-DB-Verify) — das ist eine /deploy-Pre-Condition, **kein** /final-check-Blocker.

## 3. Findings by severity

### Blocker / High / Medium
Keine.

### Low (alle pre-existing oder accepted residual)
- **L-1 — Projekt-Lint-Baseline rot (`npm run lint` Exit 1, 142 Fehler + 76 Warnings).** Durchweg pre-existing (`ai/parser.ts`, `goals/*`, `knowledge/chunker.ts`, `process-check.ts` …), **0 in SLC-913-Files** (git-blame: SLC-507/302/204). Style-Klasse (`no-explicit-any`, `prefer-const`), keine Korrektheit/Security. Identische Baseline in V8.12/V8.14 ausgeliefert. → **Accepted residual** für Internal-Tool; Empfehlung: als eigenständiges Tech-Debt-Cleanup-Item adressieren (nicht V8.15-blockierend).
- **L-2 — Toter Code `guardExportRequest` + `verifyExportApiKey`** nach MT-7-Migration. Cleanup-Follow-up.
- **L-3 — `console.log` in `lib/push/send.ts:83`** (Subscription-Expiry-Log). Diff-Scan bestätigt: **pre-existing, nicht durch SLC-913 hinzugefügt**. Konsistenz-Drift vom logSafe-Standard, Low.

## 4. Audit by dimension

| Dimension | Ergebnis |
|---|---|
| **Code quality** | Diff sauber; 0 neue `console.*`/`debugger`; keine neuen oversized Files; dead code = L-2 (Cleanup) |
| **Security & privacy** | Slice-Substanz = Security-Hardening; /qa Wiring 7/7 + MIG-052 (role+team_id, BEFORE INSERT OR UPDATE, service_role-gated); **0 hardcoded secrets im Diff**; MIG-053 persistiert nur key_hash, REVOKE anon/authenticated |
| **Legal & compliance** | Keine PII-Handling-Änderung; **0 Dependency-Changes** → kein Lizenz-/Supply-Chain-Risiko |
| **Testing depth** | tsc=0 · Vitest **1608/1608** · DB-Verify 052/053 planmäßig /deploy-gated (außerhalb `src/**`) · OWASP-Adversarial-Tests für safeExternalHref |
| **CI/CD & deploy** | **`next build` PASS (exit 0)** · ENV `LEAD_INTAKE_API_KEY`+`TRUSTED_PROXY_COUNT` in `.env.example` · MIG 051/052/053 sequenziell, keine Collision, Rollback-Notes vorhanden · Provisionierung out-of-band dokumentiert |
| **Observability & ops** | logSafe-Standard (L-3 pre-existing Ausnahme); In-Memory-Rate-Limit dokumentiert (persistenter Store = Folge-Item) |
| **Post-go-live** | KNOWN_ISSUES trackt ISSUE-109/116/122 (DB-gated, resolve im /deploy); REL-Eintrag folgt im /deploy |

## 5. Explicit release risks
- DB-seitige Findings (ISSUE-109/116/122) sind erst nach MIG-052/053-Live-Apply + DB-Verify geschlossen → **/deploy-Verantwortung**, nicht vor go-live verifizierbar.
- Per-Tenant-Export-Key muss im /deploy provisioniert + System 4 auf Bearer umgestellt werden, sonst brechen die Export-Reads (401). Im Deploy-Runbook erfasst.
- In-Memory-Rate-Limit überlebt keinen Multi-Container/Deploy-Restart — akzeptiert im Single-Container-Internal-Test-Mode (R-912-4), persistenter Store ist dokumentiertes Folge-Item.

## 6. Required fixes before go-live
Keine.

## 7. Accepted residual risks
- L-1 rotes Lint-Baseline (pre-existing, style-only, Internal-Tool) — akzeptiert, Cleanup empfohlen.
- L-2 toter Export-Key-Code — Cleanup-Follow-up.
- L-3 pre-existing console.log push/send.ts:83.
- In-Memory-Rate-Limit (R-912-4) — akzeptiert.

## 8. Recommended next step
**`/go-live V8.15`** — formelle Release-Gate-Entscheidung (GO/NO-GO) mit den akzeptierten Residual-Risiken. Anschließend `/deploy V8.15` **erst nach V8.14 STABLE** (/post-launch T+24h): MIG-052+053 applien, per-Tenant-Key provisionieren + System 4 umstellen, `LEAD_INTAKE_API_KEY`-ENV setzen, DB-Verify 052+053, Live-Smokes (test-sentry-404, intake-401/429, push-subscribe-400, Self-Promotion-blocked, Export-Tenant-Scope) → `/post-launch`.
