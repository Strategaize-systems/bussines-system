---
id: RPT-664
date: "2026-07-05"
skill: /qa
slice: SLC-914
feature: FEAT-926
type: completion
status: completed
reviewOf: null
result: "SLC-914 MT-3 (ISSUE-133 + ISSUE-134 Low-Hygiene) /backend + /qa PASS code-side. 0 Blocker / 0 High / 0 Medium / 0 Low. ISSUE-133: AnswerPane.tsx:274 → safeExternalHref (protocol-relative + javascript: → #, +Render-Test). ISSUE-134: lib/export/rate-limit.ts:checkRateLimit → extractClientIp (rightmost-offset statt leftmost-XFF). Beide App-Layer, delegieren an bereits getestete/gehärtete Helper. TSC=0, ESLint=0, Full-Vitest 1616/1616 (+1). ISSUE-133/134 resolved code-side. NEXT: Gesamt-/qa V8.16."
title: "SLC-914 MT-3 /backend + /qa — ISSUE-133/134 Low-Hygiene PASS"
---

# /backend + /qa — SLC-914 MT-3 (ISSUE-133 + ISSUE-134 Low-Hygiene-Bundle)

## Outcome
MT-3 **DONE** code-side. Zwei One-Liner-Härtungen, beide delegieren an in V8.15 bereits gehärtete + getestete Helper. **/qa PASS — 0/0/0/0.** App-Layer, keine Migration → ISSUE-133/134 **resolved code-side** (MT-1-Präzedenz).

## ISSUE-133 — AnswerPane-Link-Guard → safeExternalHref
- `cockpit/src/components/ki-workspace/AnswerPane.tsx`: Inline-Regex `/^(https?:|mailto:|\/)/.test(...) ? linkHref : "#"` → `safeExternalHref(linkHref)` (Import `@/lib/utils/safe-external-href`).
- Der Helper (SLC-913, bereits getestet in `safe-external-href.test.ts`) blockt protocol-relative `//evil.com` und `javascript:`/`data:` → `#`, lässt https/mailto/relative durch, prefixt scheme-lose Hosts mit `https://`.
- **Neuer Wiring-Test** `__tests__/AnswerPane.test.tsx`: `[sicher](https://example.com)`→href unverändert, `[boese](//evil.com)`→`#`, `[js](javascript:x)`→`#`.

## ISSUE-134 — export/rate-limit → extractClientIp
- `cockpit/src/lib/export/rate-limit.ts:checkRateLimit`: `request.headers.get("x-forwarded-for")?.split(",")[0]` → `extractClientIp(request.headers) ?? "unknown"` (Import `@/lib/security/ip-hash`).
- `extractClientIp` (SLC-913 MT-6, ISSUE-120, live-verifiziert 2026-06-13, 9 Tests) nimmt den rechten(-Offset)-Eintrag (`TRUSTED_PROXY_COUNT`, Default 1) + x-real-ip-Fallback → nicht per client-XFF-Rotation spoofbar. Key-Präfix `ip:` + `"unknown"`-Fallback erhalten.
- Kein Test-Bruch: `leads/intake/route.test.ts` mockt `@/lib/export/rate-limit` vollständig.

## Files
### Created
- `/reports/RPT-664.md`
### Modified
- `/cockpit/src/components/ki-workspace/AnswerPane.tsx` — Import + Zeile 274 (ISSUE-133)
- `/cockpit/src/components/ki-workspace/__tests__/AnswerPane.test.tsx` — +1 Wiring-Test
- `/cockpit/src/lib/export/rate-limit.ts` — Import + checkRateLimit IP-Extraktion (ISSUE-134)
- `/docs/KNOWN_ISSUES.md` — ISSUE-133 + ISSUE-134 → resolved
- `/docs/STATE.md` — Current Focus auf MT-3-Stand

## Gates
- TSC=0 (`tsc --noEmit`).
- ESLint geänderte Files=0 (AnswerPane.tsx + Test + rate-limit.ts).
- Full-Vitest jsdom **1616/1616** (+1 AnswerPane-Test vs 1615-Baseline, 0 Regression).

## Problems found
Keine. Beide Fixes sind Delegationen an bereits getestete/live-verifizierte Helper; ISSUE-134 war gegen den deployten Traefik ohnehin nicht exploitbar (Defense-in-Depth-Konsistenz).

## Open / Deferred (V8.16-Slot)
- SLC-910 CSP Phase-B: `cockpit/next.config.ts` Header-Key `Content-Security-Policy-Report-Only` → `Content-Security-Policy` (enforce) — Switch + BLOCKING `csp-check.mjs`-Browser-Smoke laufen im **/deploy** (security-headers-live-smoke.md).
- MT-4 Records (slices/INDEX, features/INDEX, backlog, roadmap, finaler STATE) nach Gesamt-/qa + SLC-910.
- MIG-054 Live-Apply + DB-Verify im /deploy (ISSUE-132 open bis dahin).

## Next
Gesamt-`/qa V8.16` (Cross-Cut MT-1..3) → SLC-910 Phase-B + MT-4 im /deploy-Fenster → /final-check → /go-live → /deploy → /post-launch. Danach `/code-review ultra` vor Multi-User (security-audit-fable5-standard, Founder-Closure).
