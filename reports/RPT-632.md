---
id: RPT-632
date: "2026-06-12"
skill: /final-check
slice: null
feature: FEAT-872
type: completion
status: completed
reviewOf: RPT-631
result: "/final-check V8.7-B CONDITIONALLY READY: 0 Blocker/High/Medium, 1 Low (.env.example-Doku-Gap — in diesem Schritt gefixt). Code release-ready + safe-to-deploy (fail-closed Gate). Go-Live-Bedingungen: Founder-ENV (KNOWLEDGE_PUSH_ENABLED=true + Coolify-Scheduled-Task) + R-355-1/ISSUE-107 Live-Push-Smoke. Next: /go-live."
title: "Final-Check V8.7-B BS->IS Verdichtungs-Cron"
---

# /final-check — V8.7-B BS→IS Verdichtungs-Cron

## 1. Scope reviewed
Pre-Release-Audit über V8.7-B (1 Slice SLC-355). Geprüft: Code-Quality, Security/Privacy, Legal/Compliance (Data-Residency), Testing-Depth, CI/CD + Deployment-Readiness, Observability, Post-Go-Live-Readiness. Aufbauend auf /qa (RPT-630) + Gesamt-/qa (RPT-631).

## 2. Gesamt-Assessment: **CONDITIONALLY READY**
Der Code ist release-ready und **safe-to-deploy** (fail-closed Gate → Cron ist ohne `KNOWLEDGE_PUSH_ENABLED=true` inert, kein IS-Push). Go-Live erfordert zwei **operative** Bedingungen (kein Code-Blocker):
1. **Founder-ENV** vor scharfem Betrieb: `KNOWLEDGE_PUSH_ENABLED=true` + `CRON_SECRET` (vorhanden) + Coolify-Scheduled-Task `knowledge-push` weekly So 02:00.
2. **R-355-1 / ISSUE-107 Live-Push-Smoke** nach dem ersten realen Lauf (Anonymisierungs-Güte + Objection-Relevanz).

- Blocker: 0 · High: 0 · Medium: 0 · Low: 1 (in diesem Schritt gefixt)

## 3. Findings nach Dimension

### Code-Quality & Maintainability — PASS
ESLint 0 (touched), TSC 0, kein `console.log`/`debugger`, keine TODO/FIXME, keine Dead-Code/auskommentierten Blöcke (RPT-630 Stub-Scan). `console.error` nur für echte Fehlerpfade (Ingest-Chunk-Fail + Fatal-Catch). Pure-Function-Extraktion (`runKnowledgePush`) hält die Route testbar.

### Security & Privacy — PASS (mit akzeptierter Posture)
- **Keine Hardcoded-Secrets** in den geänderten Files (Scan auf sk-/AKIA/password=/api_key= → 0).
- **Service-Key server-only** — kein `NEXT_PUBLIC_`-Reference; `getServiceKey()` wirft expliziten Fehler bei fehlender ENV.
- **PII:** Email/Telefon werden pre-Prompt (vor Bedrock) UND pre-Send (vor IS) gestrippt (`redactPiiString`/`redactItemsBeforeSend`). k-Anonymität via `KNOWLEDGE_PUSH_MIN_BUCKET`. IS-Seite erzwingt zusätzlich `pii_redacted=true`/`aggregation_level='aggregated'`/`source_tenant_id=null`.
- **Akzeptierte Posture (ISSUE-107, Low):** Firmen-/Personennamen verlassen sich allein auf LLM-Anonymisierung (kein Namen-Regex). Internal-Test-Mode (BS-eigene Vertriebsdaten, keine Mandanten-Daten) → Re-Id-Risiko bewusst akzeptiert (DEC-290). Live-Verifikation = /deploy-Smoke.

### Legal & Compliance (Data-Residency) — PASS
- **Bedrock hart auf `eu-central-1` gepinnt** (`bedrock-client.ts:13 REQUIRED_REGION`, `assertBedrockRegion()` wirft bei Drift — `data-residency.md`-Pflicht). Der neue Destillations-Pass läuft über `queryLLM` → EU-Endpoint.
- **Keine Non-EU-Endpoints** im neuen Code (kein `api.openai.com`/`api.anthropic.com`/us-Region).
- IS-Ziel ist EU-gehostet (is.strategaizetransition.com, Strategaize-eigene Infra).

### Testing-Depth — PASS
57 Slice-Tests (TDD) + Full-Suite **1457/1457 GREEN** (0 Regression). Deckt Happy-Path, fail-soft, Chunking, Gate-Pfade, Cost, Idempotenz, PII-Redact, Anonymisierungs-Prompt-Assertion. Live-LLM-Output ist bewusst /deploy-Smoke (gemockter `queryLLM`).

### CI/CD & Deployment-Readiness — PASS (mit Low-Fix)
- `next build` → Compiled successfully, Route `ƒ /api/cron/knowledge-push` registriert, 0 Errors.
- **Keine neue Dependency** (package.json/lock vom Slice unberührt) → kein Supply-Chain-/License-Delta, kein npm-audit-Delta.
- **Safe-Deploy:** `maxDuration=300` für lange LLM-Läufe; fail-closed Gate (Cron inert bis ENV-Flip).
- **Low (gefixt):** Die neuen ENV-Vars (`KNOWLEDGE_PUSH_ENABLED`/`_MIN_BUCKET`/`_SIZE_THRESHOLDS`) + die V8.7-A `STRATEGAIZE_KNOWLEDGE_*` fehlten in `cockpit/.env.example`. **In diesem Schritt ergänzt** (mit fail-closed Default `KNOWLEDGE_PUSH_ENABLED=false`). Coolify-ENV bleibt die operative Source-of-Truth (in Slice-Spec dokumentiert).

### Observability & Operations — PASS
- Genau 1 `audit_log`-Row pro Lauf (`action='is_knowledge_pushed'`) mit Counts + `bedrock_cost_usd` in `changes` (JSONB) + Summary in `context` (TEXT) → Cost-/Run-Audit BS-lokal (DEC-291).
- `console.error` für Chunk-Fail + Fatal; BS-Sentry wrappt API-Routes automatisch.
- fail-soft: einzelne LLM-Skips/Ingest-Chunk-Fehler senken das Cron-Gesamtergebnis nicht (zählt `failed`/`buckets_skipped`).

### Post-Go-Live-Readiness — PASS
- **Rollback trivial:** 0 Migrationen (DEC-289) → Rollback = App-Container-Revert, keine DB-Reversal nötig. Idempotenz via IS-`UNIQUE(source_system, source_reference)` → Re-Run unschädlich.
- Issue-Visibility: ISSUE-107 trackt die Live-Verifikations-Posture.
- Versionierung: roadmap `v8-7-b` aktiv; REL-XXX wird in `/deploy` vergeben.

## 4. Explizite Release-Risiken
- **R-355-1 (Medium, deferred):** Reale Anonymisierungs-Güte/Halluzination der Bedrock-Ausgabe erst live prüfbar. Mitigation steht (Constrained Prompt, temp 0.2, pre-Prompt-Redact, Branchen-Aggregat-Input, fail-soft). → /deploy-Live-Push-Smoke (erste Items inspizieren: 0 Klarnamen, sinnvolle Objection-Items).
- **ISSUE-107 (Low):** Objection-Source = `activities.description` (typ-unspezifisch) statt dedizierter `objections`/`summary`-Spalte. Per Spec; Live-Datenlage entscheidet Relevanz.

## 5. Required fixes before go-live
- **Keine Code-Blocker.** Der einzige Code-Hygiene-Gap (.env.example) ist in diesem Schritt gefixt.
- **Operative Go-Live-Bedingungen** (Founder, in `/deploy`): `KNOWLEDGE_PUSH_ENABLED=true` + `CRON_SECRET` (vorhanden) + Coolify-Scheduled-Task `knowledge-push` weekly So 02:00. `STRATEGAIZE_KNOWLEDGE_API_BASE_URL`/`_SERVICE_KEY` sind seit V8.7-A in Prod.

## 6. Akzeptierte Residual-Risiken
- Namen-Anonymisierung via LLM (kein Regex) — Internal-Test-Mode, DEC-290.
- Pre-existing npm-audit-Findings (Framework/SDK, dev-only wie vitest) — **kein V8.7-B-Delta** (keine neue Dep). Aus Vorgänger-Versionen dokumentiert.

## 7. Project-Record-Updates
- `/cockpit/.env.example` — IS-Knowledge-API + V8.7-B Verdichtungs-Cron ENV-Block ergänzt (Low-Fix).
- `/docs/STATE.md` — Current Focus auf /final-check CONDITIONALLY READY → Next `/go-live`.
- `/reports/RPT-632.md` — dieser Report.
- ISSUE-107 bleibt `open` (Live-Smoke-Tracking). roadmap `v8-7-b` bleibt `active`. Last Stable bleibt V8.12 (IMP-950).

## 8. Recommended next step
**`/go-live` V8.7-B** — formale Release-Gate-Entscheidung (GO/NO-GO) mit dokumentierten akzeptierten Risiken (R-355-1/ISSUE-107) + den operativen Founder-ENV-Bedingungen. Danach `/deploy` (Master-Merge entfällt — Single-Branch main; Push + Coolify-Redeploy + Founder-ENV + R-355-1 Live-Push-Smoke gegen IS, REL-XXX) → `/post-launch`. Kein Customer-Outreach ([[module-lifecycle-discipline]] + IMP-950). **BS NICHT pushen** bis Founder-ENV gesetzt ist (Push triggert Coolify-Rebuild).
