---
id: RPT-419
date: "2026-05-15"
skill: /backend
slice: SLC-712a
feature: FEAT-712
type: completion
status: completed
reviewOf: null
result: "SLC-712a Backend code-side done — PipelineView readOnly + viewAsUserId Reuse-Pattern etabliert, Drilldown-Pipeline-Pages umgeschrieben, 4 neue Vitest gruen, 765/765 Suite PASS, Build clean. Naechste: /qa SLC-712a Live-Smoke."
title: "SLC-712a Backend — Pipeline-Drilldown Vollausbau via PipelineView-Reuse"
---

# RPT-419 — SLC-712a Backend

## Outcome Summary

SLC-712a Backend-Implementation **vollstaendig done** code-side. Alle 4 MTs aus
der Slice-Spec abgearbeitet (MT-2 als dokumentierter No-Op weil PipelineView
heute keine localStorage-Persistence hat). TSC clean, Vitest 765/765 PASS (+4
neue Tests), Build clean, Lint-Differenz +1 pre-existing useMemo-Error durch
Line-Shift. Drilldown-Pipeline-Pages laden jetzt die echte `<PipelineView>`
read-only statt der reduzierten Tabelle.

**Status:** code-side done. Live-Smoke (AC4 + AC7) folgt im /qa SLC-712a.

## Files reviewed (read-only Reuse-Analyse)

- `/cockpit/src/app/(app)/pipeline/pipeline-view.tsx` — Hot-Path Component
- `/cockpit/src/app/(app)/pipeline/[slug]/page.tsx` — Self-Reference fuer
  Load-Pattern
- `/cockpit/src/app/(app)/pipeline/pipeline-table.tsx` — Read-Only (kein Mutate)
- `/cockpit/src/app/(app)/pipeline/deal-sheet.tsx` — Bestaetigt nur via
  Neuer-Deal-Trigger erreichbar
- `/cockpit/src/components/kanban/kanban-board.tsx` — DnD-Container
- `/cockpit/src/components/kanban/kanban-column.tsx` — Droppable + Sortable
- `/cockpit/src/components/kanban/kanban-card.tsx` — Sortable + Aktions-Dropdown
- `/cockpit/src/app/(app)/team/[user_id]/layout.tsx` — Drilldown-Layout +
  ReadOnlyContextProvider (Defense-in-Depth)
- `/cockpit/src/app/(app)/team/[user_id]/_components/drilldown-sub-nav.tsx` —
  pathname.startsWith greift auch fuer Sub-Routes /pipeline/{slug}
- `/cockpit/vitest.config.ts`, `/cockpit/vitest.setup.ts` — Vitest+jsdom+RTL
  Pattern
- `/cockpit/src/app/(app)/deals/deal-card.test.tsx` — Pattern fuer RTL-Test

## Files changed

### `/cockpit/src/app/(app)/pipeline/actions.ts`
- `getDealsForPipeline(pipelineId, options?)` Signatur erweitert um
  `ownerUserId?: string | null` Option (additiv, Self-Pipeline regression-frei).
- Filter `.eq("owner_user_id", ownerUserId)` aktiv wenn Option gesetzt.

### `/cockpit/src/app/(app)/pipeline/pipeline-view.tsx`
- `PipelineViewProps` erweitert um `readOnly?: boolean` und
  `viewAsUserId?: string`.
- `pipelinePathBase` waehlt `/team/{viewAsUserId}/pipeline` oder `/pipeline`
  fuer Pipeline-Tab-Links (DEC-200 light — Path-basiert statt Storage-Key).
- PageHeader-Title `"Pipeline (Read-Only)"` wenn readOnly, sonst unveraendert.
- `"Neuer Deal"`-Button gated `{!readOnly && ...}`.
- DealSheet gated `{!readOnly && showNewDeal && ...}` als Defense-in-Depth.
- KanbanBoard erhaelt `readOnly={readOnly}`-Prop.

### `/cockpit/src/components/kanban/kanban-board.tsx`
- `KanbanBoardProps` erweitert um `readOnly?: boolean`.
- Read-Only-Branch: rendert nur Columns ohne DndContext (kein
  handleDragStart/Over/End, keine moveDealToStage-Server-Action moeglich).

### `/cockpit/src/components/kanban/kanban-column.tsx`
- `KanbanColumnProps` erweitert um `readOnly?: boolean`.
- `useDroppable({ id, disabled: readOnly })` deaktiviert Drop-Zone in read-only.
- SortableContext-Wrapper nur fuer non-readOnly; readOnly rendert Cards ohne
  SortableContext.

### `/cockpit/src/components/kanban/kanban-card.tsx`
- `KanbanCardProps` erweitert um `readOnly?: boolean`.
- `useSortable({ id, disabled: readOnly })` deaktiviert Drag-Handle in
  read-only.
- Spread `...attributes` und `...listeners` nur wenn `!readOnly`.
- Cursor `cursor-pointer` statt `cursor-grab` wenn readOnly.
- Aktions-DropdownMenu (Angebot erstellen) komplett gehidden wenn readOnly.

### `/cockpit/src/app/(app)/team/[user_id]/pipeline/page.tsx` (REWRITE)
- Heutige reduzierte Tabelle (SLC-706 MT-4) ersetzt durch echtes
  `<PipelineView readOnly viewAsUserId>`.
- Laedt Default-Pipeline (erste sortierte) + alle Daten parallel via
  Promise.all (gleicher Daten-Shape wie /pipeline/[slug]/page.tsx).
- ownerUserId-Filter aktiv (semantischer Member-Scope, RLS deckt es ohnehin).
- Header-Kommentar erklaert ReadOnlyContext-Wrap im Drilldown-Layout.

## Files created

### `/cockpit/src/app/(app)/team/[user_id]/pipeline/[slug]/page.tsx` (NEW)
- Slug-Variante analog zu `/pipeline/[slug]/page.tsx`, aber mit
  `readOnly + viewAsUserId`.
- Ziel der Pipeline-Switcher-Tabs in PipelineView (`/team/{id}/pipeline/{slug}`).
- Gleiches Daten-Load-Pattern wie Default-Page.

### `/cockpit/src/app/(app)/pipeline/pipeline-view.test.tsx` (NEW)
- 4 RTL-Tests mit Vitest (jsdom):
  - readOnly=true: kein "Neuer Deal"-Button.
  - readOnly=false (Default): "Neuer Deal"-Button findable
    (Regression-Check).
  - viewAsUserId postfixt Pipeline-Tab-Links auf
    `/team/{id}/pipeline/{slug}`.
  - Self-Pipeline: Pipeline-Tab-Links unveraendert `/pipeline/{slug}`.
- vi.mock fuer `@/app/(app)/pipeline/actions` + `@/app/(app)/proposals/actions`
  + `next/navigation` + `@/app/(app)/deals/type-ahead-search`.

### `/reports/RPT-419.md` (NEW)
- Dieser Completion-Report.

## Files intentionally not changed

- `/cockpit/src/app/(app)/pipeline/pipeline-table.tsx` — bereits read-only
  (onDealClick navigiert nur, keine Mutate-Buttons). Kein readOnly-Prop noetig.
- `/cockpit/src/app/(app)/pipeline/funnel-report.tsx` — read-only Report.
- `/cockpit/src/app/(app)/pipeline/win-loss-report.tsx` — read-only Report.
- `/cockpit/src/app/(app)/pipeline/deal-sheet.tsx` — wird in PipelineView durch
  `!readOnly && showNewDeal &&` gated, eigenes Component-Refactor nicht noetig.
- `/cockpit/src/components/ui/view-toggle.tsx`,
  `/cockpit/src/app/(app)/deals/type-ahead-search.tsx` — UI-state-only, keine
  Mutate-Actions.

## Problems found

- **Pre-existing Lint-Drift (+1 vs V6.6-Baseline 142e/54w):** Aktuell 143e/54w.
  Der zusaetzliche Error ist auf `react-hooks/preserve-manual-memoization`
  fuer einen useMemo-Block in pipeline-view.tsx, dessen Code-Inhalt unveraendert
  ist. Durch SLC-712a-Edits oberhalb der useMemo-Bloecke wurden die
  Zeilennummern verschoben — React-Compiler-Heuristik triggert auf neuer
  Position. Kein neuer Code-Smell, Refactor pre-existing useMemo waere out of
  scope.
- **Lint-Errors in geanderten Files alle pre-existing:**
  - `actions.ts` 3 `no-explicit-any` Errors in moveDealToStage/getReferralsForSelect
    (Lines 417/427/665) — pre-existing, nicht aus SLC-712a-Aenderungen
    (mein Diff ist nur in getDealsForPipeline um Line 105-125).
  - `pipeline-view.tsx` 6 `react-hooks/preserve-manual-memoization` Errors
    auf useMemo (filteredDeals/forecast/avgChance) — pre-existing, nur Line-Shift.
  - `kanban-card.tsx` 1 Warning `'e' is defined but never used` Line 79 —
    pre-existing.
- **Drag-Drop-AC2 nicht im Vitest gecovered:** AC2 fordert "Drag-Drop
  deaktiviert" — das wird im Code via `useSortable({ disabled: readOnly })`
  und readOnly-Branch in KanbanBoard ohne DndContext garantiert, aber im
  Vitest nicht durch DnD-Simulation geprueft. Live-Smoke in /qa wird das
  abdecken (AC7 Drilldown-Sicht ohne Drag-Drop-Effekt).
- **Pipeline-Switcher-Test nicht im Vitest:** AC4 fordert "alle Pipelines des
  Target-Members". Vitest prueft Link-Targets, nicht den Pipeline-Switch im
  Browser. Live-Smoke in /qa.

## Open points or deferred decisions

- **MT-2 No-Op:** Filter-State-Storage-Key mit viewAsUserId-Postfix laut DEC-200
  vorbereitet, aber PipelineView nutzt heute kein localStorage-Persistence
  fuer stageFilter/viewMode/showNewDeal (nur React-useState). Slice-Spec sagt
  explizit: "Falls heute kein Persistence-Pattern existiert: dann ist diese MT
  no-op und wird im Slice-Report dokumentiert." → wenn spaeter Persistence
  hinzukommt, muss `pipeline-filter-state-viewAs-${viewAsUserId}`-Key-Pattern
  implementiert werden. Pure-Function `getStorageKey(viewAsUserId?)` ist nicht
  noetig solange kein Storage-Code existiert.
- **Live-Smoke ausstehend** (AC4 + AC7 + Drag-Drop-Verhalten + Cursor-State):
  Naechster Schritt /qa SLC-712a mit Playwright-MCP.
- **DEC-199 + DEC-200 in DECISIONS.md:** existieren bereits aus V7.1
  Slice-Planning, Status `accepted`. Kein DEC-Update noetig.

## Project record updates

- `/slices/INDEX.md`: SLC-712a Status `planned` → `done` (nach Live-Smoke
  bleibt status `done` per feedback_slice_status_done_not_deployed.md).
- `/features/INDEX.md`: FEAT-712 Status `planned` → `in_progress` (SLC-712a
  done, SLC-712b noch planned).
- `/planning/backlog.json`: BL-468 bleibt `in_progress` (Pipeline-Teil done,
  Aufgaben + Mein-Tag noch nicht).
- `/docs/STATE.md`: Current Focus + Phase + Immediate Next Steps zu /qa
  SLC-712a aktualisieren.

## Verification

- **TSC**: `npx tsc --noEmit` — keine neuen Errors in SLC-712a-Files. Alle
  vorhandenen Errors sind in V5.7+V6 Test-Files (pre-existing baseline).
- **Vitest**: `npx vitest run` — **765/765 PASS** (+4 neue Tests aus
  pipeline-view.test.tsx, war 761/761 vor SLC-712a).
- **Build**: `npx next build` — clean, Routes registriert:
  - `/team/[user_id]/pipeline` (Dynamic)
  - `/team/[user_id]/pipeline/[slug]` (Dynamic)
- **Lint**: 143e/54w (Baseline V6.6 142e/54w, +1 Line-Shift-Error, kein neuer
  Code-Smell).

## Recommended next step

`/qa SLC-712a` — Vitest-Subset PASS bestaetigen + Playwright-MCP Live-Smoke
gegen `business.strategaizetransition.com` (nach User-Coolify-Redeploy):
- AC4: Klick auf Team-Member → /team/{id}/pipeline zeigt volle PipelineView
  read-only.
- AC7: Stage-Filter + Funnel-Toggle + Forecast funktionieren in der Drilldown.
- Drag-Drop-Versuch hat keinen Effekt (Card haengt visuell sofort am Cursor,
  bewegt sich aber nicht).
- "Neuer Deal"-Button + KanbanCard-Aktions-Dropdown beide nicht sichtbar.
- Pipeline-Switcher schickt auf `/team/{id}/pipeline/{slug}`.

Per feedback_no_intermediate_coolify_switches.md kein Coolify-Switch zwischen
MTs — User-Coolify-Redeploy ist Pflicht-Aktion am Slice-Ende.
