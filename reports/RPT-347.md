---
id: RPT-347
date: "2026-05-08"
skill: /backend
slice: SLC-651
feature: FEAT-651
type: completion
status: completed
result: "SLC-651 Theming-Tokens-Setup code-side done. 6 Brand-Tokens als CSS-Vars `--color-brand-*` im Tailwind-v4-`@theme inline`-Block. 8 Drift-Stellen in mein-tag-client.tsx migriert. Build/Vitest/Lint clean. Wartet auf Coolify-Redeploy + Live-Smoke."
title: "SLC-651 Theming-Tokens-Setup (BL-441 Phase A) — /backend done code-side"
---

# RPT-347 — SLC-651 Theming-Tokens-Setup (BL-441 Phase A)

## Outcome

SLC-651 code-side vollstaendig durchgefuehrt:
- 6 Brand-Color-Tokens als CSS-Variablen im `@theme inline`-Block in `cockpit/src/app/globals.css` ergaenzt.
- 8 Single-Color-Drift-Stellen in `cockpit/src/app/(app)/mein-tag/mein-tag-client.tsx` auf Tokens migriert.
- Build clean (Next.js 16.2.3 compile in 14.0s), Vitest 405/405 PASS, Lint 166/55 (V5.7-Baseline unveraendert).
- Records-Sync: `slices/INDEX.md`, `planning/backlog.json` (BL-441 → in_progress + Phase-A-Anmerkung), `docs/STATE.md` (V6.5 in Umsetzung, 1/7 Slices).

Status: **fully complete code-side**. Nur MT-5 (Coolify-Redeploy + Live-Smoke) wartet auf User.

## Spec-Drift entdeckt und adaptiert

Die SLC-651-Spec setzte Tailwind v3 voraus (`tailwind.config.ts`). Cockpit laeuft auf **Tailwind v4** mit `@tailwindcss/postcss`. In v4 gibt es keinen Config-File mehr — Tokens leben im `@theme inline`-CSS-Block.

Adaption (in Abstimmung mit User vor Implementierung):
- Token-Naming: `brand-primary` / `brand-success` / `brand-warning` statt `primary` / `success` / `warning`. Begruendung: shadcn nutzt `--color-primary` als oklch-Greyscale; ein Brand-`--color-primary: #4454b8` wuerde shadcn-Buttons sofort visuell brechen. Spec-Risk-Section nennt diesen Konflikt explizit als Fallback-Pfad.
- Gradient-Utility (MT-2): geskippt. `.gradient-primary` existiert bereits seit Style-Guide-V2-Restyling (globals.css Z. 141). AC-2-Klausel "oder Brand-Gradient direkt nutzbar" erfuellt.
- Drift-Stellen-Migration (MT-3): 8 Stellen migriert (Spec sagte "2-3 als Beweis"); alle Single-Color-Drift in mein-tag-client.tsx. Gradient-Drift in `from-[#XXX] to-[#XXX]` belassen — out-of-scope SLC-651.

## Files reviewed

- `cockpit/tailwind.config.ts` — **existiert nicht** (Tailwind v4)
- `cockpit/postcss.config.mjs` — `@tailwindcss/postcss` v4-Plugin
- `cockpit/package.json` — `tailwindcss: ^4`, `@tailwindcss/postcss: ^4`
- `cockpit/src/app/globals.css` — `@theme inline`-Block, bestehende `.gradient-primary` etc.
- `cockpit/src/app/(app)/mein-tag/mein-tag-client.tsx` — 8 Drift-Stellen identifiziert + migriert
- `cockpit/src/app/(app)/mein-tag/ki-workspace.tsx` — 4 weitere Drift-Stellen (out-of-scope, SLC-652+)
- `slices/SLC-651-theming-tokens-setup.md` — Spec-Quelle
- `docs/STYLE_GUIDE_REFERENCE.md` — Brand-Hex-Werte-Quelle (#120774, #4454b8, #00a84f, #4dcb8b, #f2b705, #ffd54f)

## Files changed

- `cockpit/src/app/globals.css` — 6 Brand-Tokens im `@theme inline`-Block (`--color-brand-primary`, `--color-brand-primary-dark`, `--color-brand-success`, `--color-brand-success-dark`, `--color-brand-warning`, `--color-brand-warning-light`).
- `cockpit/src/app/(app)/mein-tag/mein-tag-client.tsx` — 8 Single-Color-Drift-Stellen migriert: Z. 63, 65 (event-bg-Map), 233, 240, 247 (Aufgaben-Toggle/Link), 333, 470 (Deals/Termine-Link), 642 (Meeting-Start-Button bg). Hover-Wert `#3a4aa0` (einzigartig zwischen primary und primary-dark) als Hex belassen — AC-3 "visuell unveraendert".
- `slices/INDEX.md` — SLC-651 status `planned` → `done`.
- `planning/backlog.json` — BL-441 status `open` → `in_progress`, description um Phase-A-done-Notiz und v4-Adapt-Begruendung erweitert, Title `tailwind.config.ts` → `globals.css @theme`.
- `docs/STATE.md` — High-Level-State `slice-planning` → `implementing`, Current Focus auf SLC-651-done-Status, Immediate Next Steps neu sortiert (1=Coolify-Redeploy, 2=/qa, 3=SLC-652).

## Files created

- `reports/RPT-347.md` — dieses Completion-Report.

## Files intentionally not changed

- `cockpit/src/app/(app)/mein-tag/ki-workspace.tsx` — 4 Drift-Stellen vorhanden, aber out-of-scope SLC-651. SLC-652 Theming-Migration Tranche 1 nimmt Pipeline+Proposals; ki-workspace gehoert tendenziell zu SLC-652 oder einer weiteren Tranche.
- `cockpit/src/lib/proposal/reverse-charge-revert.test.ts`, `cockpit/src/lib/proposal/skonto-revert.test.ts` — pre-existing TS-Errors aus V5.7 Pure-Function-Extraction. Build kompiliert trotzdem clean (Next.js compiliert ohne tsc-noEmit-Strict). Werden separat in V5.7-Pre-Existing-Backlog adressiert; nicht im Scope von SLC-651.

## Problems found

1. **Slice-Spec-Drift (V3 vs V4):** SLC-651-Spec verlangte Edits in `cockpit/tailwind.config.ts`, der Pfad existiert nicht (Tailwind v4 nutzt `@theme inline` in CSS). Adapt-Pfad (CSS statt Config-File) wurde vor Implementierung mit User abgestimmt; Slice-Spec selbst NICHT updated, weil RPT + IMP-XXX die Adaption dokumentieren. **IMP-XXX folgt unten** als Skill-Improvement im Dev-System.
2. **Pre-existing Lint/TS-Errors:** `lib/proposal/*-revert.test.ts` haben TS-Errors aus V5.7. Build und Vitest laufen trotzdem clean. Nicht im SLC-651-Scope.
3. **AC-2 Gradient-Utility:** Slice-Spec sagte "neuer Block `.bg-gradient-primary`". Existing `.gradient-primary` erfuellt die "oder direkt nutzbar"-Klausel. Spec ist vom Wortlaut her erfuellt; AC-Auslegung dokumentiert.

## Open points / deferred decisions

- **MT-5 Live-Smoke:** wartet auf User-Coolify-Redeploy. Erst nach `/mein-tag` ohne Visual-Regression ist SLC-651 "fully done".
- **Gradient-Migration (`from-[#XXX] to-[#XXX]`):** Tailwind-v4 unterstuetzt Token-Refs in Gradient-Stops via `from-(--color-brand-primary)`-Syntax. Out-of-scope SLC-651; SLC-652 Theming-Migration Tranche 1 entscheidet, ob/wie das in mein-tag/ki-workspace abgefruehstueckt wird.
- **Token-Naming-Konsistenz:** `brand-primary-dark` (mit `-dark` suffix, dunkler) vs. `brand-success-dark` (mit `-dark`, **dunkler** = #00a84f) vs. `brand-warning-light` (mit `-light`, heller = #ffd54f). Konvention: `-dark` immer dunkler, `-light` immer heller, `DEFAULT` ist Mid-Ton. Bei brand-primary ist `DEFAULT` = #4454b8 (heller Mid-Ton) und `-dark` = #120774; bei brand-success ist `DEFAULT` = #4dcb8b (heller) und `-dark` = #00a84f. Konsistent: `-dark` = der dunklere der zwei Hex-Werte aus dem Style-Guide.

## Skill Improvement (zu uebernehmen ins Dev-System)

**IMP-XXX — Slice-Planning muss aktive Tailwind-Major-Version pruefen vor Token-Spec**

- Source: SLC-651 V6.5
- Observation: Slice-Spec setzte Tailwind v3 voraus (`tailwind.config.ts`-Edits). Cockpit laeuft auf v4 — der File existiert nicht. Implementation musste vor Code-Start einen Adapt-Plan mit User abstimmen.
- Suggested Improvement: `/slice-planning` und `/architecture` muessen bei Token-/Theme-Slices die aktive Tailwind-Version aus `package.json` pruefen (`tailwindcss` major). Bei v4: Spec auf `globals.css @theme inline` referenzieren, nicht auf `tailwind.config.ts`. Bei v3: Spec auf `tailwind.config.ts` referenzieren.
- Affected Area: `/slice-planning` skill (Pre-Pruefung Tailwind-Major), `/architecture` skill (Token-System-Pfad), `.claude/rules/frontend.md` (Tailwind-v4-Hinweis).
- Status: open (zu uebernehmen ins Dev-System SKILL_IMPROVEMENTS.md)

## Recommended next step

1. **User: Coolify-Redeploy** auf den Commit, der SLC-651 enthaelt. Image baut, Container restart, Live-Smoke `https://business.strategaizetransition.com/mein-tag` zeigt /mein-tag ohne Visual-Regression.
2. Nach Live-Smoke-PASS: **/qa SLC-651** (Final-Quality-Gate).
3. Nach /qa-PASS: **/backend SLC-652 Theming-Migration Tranche 1** (Pipeline+Proposals).

## Verification summary

| Gate | Result |
|---|---|
| `npx tsc --noEmit` | 2 pre-existing V5.7-Errors in `*-revert.test.ts`, NICHT in SLC-651-Files |
| `npm run build` | ✓ Compiled successfully in 14.0s |
| `npm run test` | 405/405 PASS (31 Test-Files), 2.71s |
| `npm run lint` | 166 errors / 55 warnings — V5.7-Baseline unveraendert |
| Drift-Stellen migriert | 8/8 in mein-tag-client.tsx |
| Verbliebene Hex in mein-tag-client.tsx | 9 (alle `from-[#XXX] to-[#XXX]` Gradient-Stops, out-of-scope) |
| Records-Sync | slices/INDEX.md ✓, planning/backlog.json ✓, docs/STATE.md ✓ |
| Live-Smoke | wartet auf Coolify-Redeploy |
