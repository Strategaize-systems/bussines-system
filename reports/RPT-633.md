---
id: RPT-633
date: "2026-06-12"
skill: /go-live
slice: null
feature: FEAT-872
type: completion
status: completed
reviewOf: RPT-632
result: "/go-live V8.7-B: CONDITIONAL GO. Code-Deploy unbedingt safe (fail-closed Gate). Betrieb des Crons konditional auf Founder-ENV (KNOWLEDGE_PUSH_ENABLED=true + Coolify-Scheduled-Task) + R-355-1/ISSUE-107 Live-Push-Smoke. 0 Blocker. Internal-Test-Mode, kein Customer-Outreach. Next: /deploy."
title: "Go-Live V8.7-B BS->IS Verdichtungs-Cron — CONDITIONAL GO"
---

# /go-live — V8.7-B BS→IS Verdichtungs-Cron

## 1. Release-Target
V8.7-B (Slice SLC-355 / FEAT-872) — wöchentlicher BS→IS Verdichtungs-Cron, der anonymisierte/aggregierte Win/Loss- + Einwand-Lessons an die live IS-Knowledge-API pusht. Delivery-Mode: **Internal Tool** (Strategaize-eigenes Business-System, eigene Vertriebsdaten). Ziel: BS-Prod via Coolify (91.98.20.191), App-only-Deploy (keine Migration).

## 2. Entscheidung: **CONDITIONAL GO**
- **Code-Deploy: GO (unbedingt).** Der Cron ist fail-closed (`KNOWLEDGE_PUSH_ENABLED !== "true"` → sofortiger Skip ohne IS-Call). Die Code-Änderung ist additiv (V8.7-A-Konsumenten-Pfad unberührt, Full-Suite GREEN) → das Ausrollen ändert das Laufzeitverhalten NICHT, bis der Founder die ENV scharf schaltet. Risikoarmer Deploy.
- **Cron-Betrieb (scharf): CONDITIONAL** auf zwei operative Bedingungen (siehe §5).

Für ein Internal Tool mit fail-closed Gate, 0 Blocker/High/Medium, trivialem Rollback und vollständiger QA/Final-Check-Kette ist das ein sauberer Conditional-Go.

## 3. Readiness-Bestätigung (Gate-Kette)
| Gate | Ergebnis | Report |
|---|---|---|
| /qa SLC-355 (Code-Side) | PASS-WITH-LOW (AC-355-1..8, Stub/Wiring clean) | RPT-630 |
| Gesamt-/qa V8.7-B | PASS (Cross-Version-Integration, Full-Suite, Build, Records) | RPT-631 |
| /final-check V8.7-B | CONDITIONALLY READY (0 Blocker/High/Medium, 1 Low gefixt) | RPT-632 |
| Quality-Gates | TSC=0 · ESLint=0 · Vitest 1457/1457 · `next build` OK | RPT-630/631 |

## 4. Blocker
**Keine.** 0 Blocker, 0 High, 0 Medium. Der einzige Low (.env.example-Doku-Gap) wurde im /final-check gefixt.

## 5. Required vor scharfem Betrieb (operative Go-Live-Bedingungen — Founder/Deploy)
1. **Coolify-ENV** auf BS-Prod:
   - `KNOWLEDGE_PUSH_ENABLED=true` (ohne dies bleibt der Cron inert)
   - `CRON_SECRET` (bereits vorhanden — von 17 bestehenden Cron-Routes genutzt)
   - `STRATEGAIZE_KNOWLEDGE_API_BASE_URL` + `STRATEGAIZE_KNOWLEDGE_SERVICE_KEY` (seit V8.7-A in Prod — verifizieren)
2. **Coolify-Scheduled-Task** `knowledge-push` weekly So 02:00 (POST `/api/cron/knowledge-push` mit `x-cron-secret`-Header).
3. **R-355-1 / ISSUE-107 Live-Push-Smoke** nach dem ersten realen Lauf: erste destillierte Items inspizieren → 0 Klarnamen im `body_markdown`/`title`, thematisch sinnvolle Objection-Items. Bei Namen-Leak: Namen-Redact ergänzen / `objections`-Spalte als Source prüfen.

## 6. Akzeptierte Risiken (GO trotz)
- **R-355-1 (Medium):** Reale LLM-Anonymisierungs-Güte erst live verifizierbar. Mitigation steht (Constrained Prompt, temp 0.2, pre-Prompt-Redact, Branchen-Aggregat-Input, fail-soft). Akzeptiert für Internal-Test-Mode; Live-Smoke ist Pflicht-Folgeschritt.
- **ISSUE-107 (Low):** Objection-Source `activities.description`. Akzeptiert (per Spec), Live-Datenlage entscheidet Relevanz.
- **Namen-Anonymisierung via LLM** (kein Regex) — Internal-Test-Mode, BS-eigene Vertriebsdaten, keine Mandanten-Daten (DEC-290).
- **Pre-existing npm-audit-Findings** — kein V8.7-B-Delta (keine neue Dependency).

## 7. Operative Concerns
- **Rollback:** trivial — 0 Migrationen → App-Container-Revert auf Vorgänger; IS-`UNIQUE(source_system, source_reference)`-Dedup macht Re-Runs idempotent (kein Daten-Schaden bei Wiederholung).
- **Blast-Radius bei Fehlverhalten:** fail-soft (einzelne LLM-/Ingest-Fehler kippen den Cron nicht); schlimmstenfalls werden Items übersprungen, nichts wird korrumpiert. Sofort-Stopp via `KNOWLEDGE_PUSH_ENABLED=false`.
- **Data-Residency:** Bedrock eu-central-1 gepinnt; IS-Ziel EU-gehostet.
- **Customer-Impact:** keiner — Internal-Test-Mode, kein Customer-Outreach ([[module-lifecycle-discipline]] + IMP-950).

## 8. Project-Record-Updates
- `/docs/STATE.md` — Current Focus auf /go-live CONDITIONAL GO → Next `/deploy`.
- `/reports/RPT-633.md` — dieser Report (GO-Verdikt dokumentiert).
- REL-XXX wird **NICHT** hier vergeben — das erfolgt in `/deploy` (Live-Deploy + Live-Push-Smoke). roadmap `v8-7-b` bleibt `active` bis /deploy. Last Stable bleibt V8.12 (IMP-950).

## 9. Recommended next step
**`/deploy` V8.7-B** — Single-Branch main (kein Master-Merge nötig): Founder setzt Coolify-ENV (§5) → BS-Push (triggert Coolify-Rebuild) → Container-Health-Check → R-355-1 Live-Push-Smoke gegen IS → REL-XXX in `/docs/RELEASES.md` + roadmap `v8-7-b` released + Slice/Feature → `deployed`. Danach `/post-launch` (T+24h → STABLE). **BS bleibt unpushed bis der Founder die ENV gesetzt + den Push autorisiert hat.**
