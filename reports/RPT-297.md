---
id: RPT-297
date: "2026-05-04"
skill: /backend
slice: SLC-571
feature: FEAT-571
type: completion
status: completed
reviewOf: null
result: "MT-8 PDF-Renderer Code-complete. reverse-charge-block.ts mit bilingualer Phrase-Constant + pdfmake-Block-Builder NEU. proposal-renderer.ts erweitert um summaryStack-Refactoring + Reverse-Charge-Insert direkt unter Tax-Row + Strategaize-vat_id-Footer mit kontextabhaengigem Bezeichner (NL='BTW-Nr.', DE='USt-IdNr.'). actions.ts PDF-Path-Selects um vat_id/business_country/address_country erweitert (vorher unsynced mit ProposalEditPayload-Type). 8 neue Vitest-Cases inkl. 3 Snapshot-Tests. Existierende V5.5/V5.6-Baseline-Snapshots passen unveraendert (AC21 regression-frei verifiziert). 231/231 Vitest + TSC + Build PASS. Lint clean fuer alle MT-8-Files. Live-Browser-Smoke offen nach User-Coolify-Redeploy."
title: "V5.7 SLC-571 MT-8 backend done — PDF-Renderer Reverse-Charge-Block + Strategaize-vat_id-Footer"
---

# RPT-297 — V5.7 SLC-571 MT-8 Backend (PDF-Renderer)

## Outcome Summary

`/backend` Code-side fuer V5.7 SLC-571 MT-8 vollstaendig durchgezogen. Neue Datei `reverse-charge-block.ts` enthaelt die hardcoded bilinguale Phrase ("BTW verlegd / Reverse Charge — Article 196 VAT Directive 2006/112/EC") + pdfmake-Block-Builder. Der proposal-renderer wurde im Summary-Block refactored zu einem Stack-Builder-Pattern, das den Reverse-Charge-Block strikt conditional zwischen Tax-Row und canvas-line einfuegt. Footer-vat_id rendert wenn `branding.vat_id` gesetzt ist, mit Bezeichner abhaengig von `business_country` (NL/DE).

Wichtigster Side-Find: Die zweite branding-Select-Stelle in `actions.ts` (PDF-Generation-Path bei `generateProposalPdf`, Zeile ~1046-1064) hatte `vat_id` und `business_country` nicht im Select, obwohl der `ProposalEditPayload`-Type sie als nicht-optional typisiert. Zur Laufzeit wuerden diese Felder undefined sein. Same fuer `companies` (vat_id + address_country gefehlt). Beide Selects synchronisiert.

Status: Code-side complete — Browser-Smoke offen nach User-Coolify-Redeploy. Empfohlen: /qa SLC-571 MT-8 mit Live-PDF-Render `/api/proposals/0c1f7e10-a02e-4393-8185-c175f9864c40/pdf` (RC-eligible Proposal aus Live-System, branding=NL859123456B01).

## Files Reviewed

- `slices/SLC-571-nl-vat-reverse-charge.md` (MT-8-Spec + ACs AC18..22)
- `cockpit/src/lib/pdf/proposal-renderer.ts` (Renderer-Pattern, summaryContent-Stelle Zeile 301-328, footerStripped-Pfad)
- `cockpit/src/lib/pdf/proposal-renderer.test.ts` (Snapshot-Convention, V5.5/V5.6-Baseline-Cases)
- `cockpit/src/types/branding.ts` (Branding-Type + BusinessCountry)
- `cockpit/src/app/(app)/proposals/actions.ts` (ProposalEditPayload Zeile 87-116, getProposalForEdit Zeile 354-368, generateProposalPdf Zeile 1046-1064)

## Files Changed

- `cockpit/src/lib/pdf/proposal-renderer.ts` — Import `buildReverseChargeBlock`. Refactor des Summary-Stack zu einem Builder-Pattern mit conditional Reverse-Charge-Insert (`if (proposal.reverse_charge)` direkt nach Tax-Row, vor canvas-line). Footer-Block erweitert: `if (footerStripped || strategaizeVatId)` triggert die Trennlinie + optional vat_id-Zeile + optional footer-text. Bezeichner via `business_country` ('NL' → 'BTW-Nr.', sonst 'USt-IdNr.'). Strikt conditional damit V5.5/V5.6-Snapshots bit-identisch bleiben (verifiziert).
- `cockpit/src/app/(app)/proposals/actions.ts` — `generateProposalPdf` branding-Select um `vat_id, business_country` erweitert. Companies-Select um `vat_id, address_country` erweitert. Vorher: Type-Lie zur Laufzeit (Type sagt vorhanden, DB-Roundtrip lieferte sie nicht). Inline-Kommentar-Verweis auf MT-8.
- `cockpit/src/lib/pdf/proposal-renderer.test.ts` — Import `REVERSE_CHARGE_HEADER` + Type-Imports. `getContentSnapshot` um optionalen `overrides`-Parameter (branding/company) erweitert. Neue Helper `makeBranding`, `makeCompany`. 8 neue Test-Cases unter neuem `describe`-Block "Reverse-Charge + Strategaize-vat_id (DEC-124, DEC-125)" — AC21 (regression-frei via Strings + Snapshot), AC20 NL-Mode, AC20 DE-Mode, AC18 bilingual + beide BTW-IDs, AC18 Reihenfolge unter Tax-Row, AC19 false-Pfad, AC22 Edge-Case (defensives "—"), 2 Snapshot-Cases.

## Files Created

- `cockpit/src/lib/pdf/reverse-charge-block.ts` — Hardcoded NL/EN-Phrase als Constants (`REVERSE_CHARGE_PHRASE_NL`, `REVERSE_CHARGE_PHRASE_EN`, `REVERSE_CHARGE_HEADER`). Pure Function `buildReverseChargeBlock(strategaizeVatId, companyVatId)` returnt pdfmake `Content`-Stack mit zwei Zeilen (fett + normal). Defensive Renderung: null/empty-Werte → "—" Platzhalter (Server-Action soll diesen Fall blocken, Renderer crasht nicht).

## Files Intentionally Not Changed

- `cockpit/src/app/(app)/proposals/[id]/edit/proposal-editor.tsx` — Editor-side bereits aus MT-6 fertig, keine MT-8-spezifischen Aenderungen noetig.
- `cockpit/src/lib/pdf/filename-helper.ts` — Filename-Logik ist Reverse-Charge-unabhaengig.
- `slices/INDEX.md`, `features/INDEX.md`, `planning/backlog.json`, `docs/STATE.md` — Records-Update kommt mit MT-9 (Final-Step) nach erfolgreicher AC-Erfuellung inkl. Browser-Smoke.

## Problems Found

- **Type-Lie in actions.ts (PDF-Path)**: `ProposalEditPayload.branding.vat_id` und `business_country` sind als nicht-optional typisiert, aber der Select in `generateProposalPdf` lieferte sie nicht. Zur Laufzeit waeren sie undefined. Same fuer Company. Vor MT-8 nicht aufgefallen, weil V5.6 diese Felder nicht las. **Behoben** im selben Commit. Empfehlung fuer IMP: Branding-/Company-Select-Strings als geteilte Constants extrahieren, damit Editor-Path und PDF-Path nicht drift-anfaellig sind.

## Open Points / Deferred Decisions

- Live-Browser-Smoke fuer MT-8 ist /qa-Schritt nach User-Coolify-Redeploy. Test-Plan: 3 Live-PDFs rendern via `/api/proposals/[id]/pdf` mit (a) RC=true + beide vat_ids, (b) RC=false + Strategaize-vat_id im Footer, (c) altes V5.6-Proposal ohne reverse_charge → bit-identisch zum V5.6-Output.
- MT-9 (`docs/COMPLIANCE.md` NL-VAT-Sektion + Cockpit-Records-Update) folgt nach MT-8 QA-PASS.

## Recommended Next Step

User triggered Coolify-Redeploy via Coolify-UI (Push-Auto-Deploy aktiviert auf main). Sobald deployed: `/qa SLC-571 MT-8` — Live-PDF-Render-Smokes gegen `/api/proposals/0c1f7e10-a02e-4393-8185-c175f9864c40/pdf` (RC-eligible Test-Proposal aus Live-System, Strategaize-Branding NL+vat_id, Empfaenger Strategaize Transition mit DE-vat_id) + visuelle Verifikation aller AC18..22.

## Verification Summary

- TSC `tsc --noEmit` — clean (kein Output)
- Vitest `vitest run` — 231/231 PASS, 16 Test-Files. Davon 27/27 in `proposal-renderer.test.ts` (vorher 19, +8 neue MT-8-Cases). 3 neue Snapshots geschrieben.
- ESLint fuer MT-8-Files (`reverse-charge-block.ts`, `proposal-renderer.ts`, `proposal-renderer.test.ts`, `actions.ts`) — clean. Pre-existing Lint-Issues in anderen Repo-Files unveraendert (out-of-scope).
- Build `npm run build` — PASS.
- Browser-Smoke — offen nach User-Redeploy.
