---
id: RPT-100
date: "2026-04-13"
skill: /qa
slice: SLC-405
feature: FEAT-407
type: completion
status: completed
reviewOf: null
result: "QA PASS — 6/6 Acceptance Criteria erfuellt, 2 Low-Findings"
title: "QA SLC-405 KI-Wiedervorlagen mit Freigabe"
---

# QA Report — SLC-405 KI-Wiedervorlagen mit Freigabe

## QA Scope

Vollstaendige Pruefung aller 5 Micro-Tasks:
- MT-1: Followup-Generierung Engine (Kandidaten-Erkennung)
- MT-2: Bedrock-Begruendung + Queue-Eintraege
- MT-3: /api/cron/followups Endpoint
- MT-4: Wiedervorlagen-Sektion in Mein Tag
- MT-5: Freigabe-Workflow (Freigeben / Verschieben / Abbrechen)

## Result: PASS

## Acceptance Criteria

| # | Kriterium | Status | Evidenz |
|---|-----------|--------|---------|
| 1 | KI generiert Wiedervorlagen-Vorschlaege basierend auf CRM-Kontext | PASS | followup-engine.ts: 3 Kandidaten-Kategorien (stagnant deals >14d, open proposals >7d, unanswered emails >3d). CRM-Kontext (Deal-Wert, Phase, Firma, Kontakt) wird korrekt in Prompt eingebaut. |
| 2 | Vorschlaege erscheinen in Mein Tag mit Begruendung | PASS | followup-suggestions.tsx: Amber-themed Card mit Lightbulb-Icon, entity-type-Icons (Deal/Mail/Contact), action_description als Titel, reasoning als Subtitle, Priority-Badge. |
| 3 | 3 Aktionen funktionieren: Freigeben / Verschieben / Abbrechen | PASS | followup-actions.ts: approveFollowup(), postponeFollowup(), rejectFollowup() — alle mit User-Auth-Check, Admin-Client-DB-Zugriff, revalidatePath. |
| 4 | Freigegebene Vorschlaege werden zu echten Tasks | PASS | approveFollowup: Erstellt Task mit [KI]-Prefix, type="follow_up", Priority-Mapping (dringend→high, niedrig→low), due_date=morgen, entity-lookup fuer deal_id/contact_id/company_id. |
| 5 | Abgelehnte Vorschlaege werden nicht wiederholt (Deduplizierung) | PASS | processFollowupCandidates: dedup_key = "followup-{entityType}-{entityId}", Check vor LLM-Call. rejectFollowup: Status → "rejected" + ai_feedback Entry. createAction hat eigene dedup-Pruefung. |
| 6 | Max 20 Vorschlaege pro Batch | PASS | Cron-Route: processFollowupCandidates(20). Engine: Kandidaten-Limits pro Kategorie (8+5+7=20), finale slice(0, limit). |

## Stub Detection

| Pattern | Gefunden | Details |
|---------|----------|---------|
| TODO/FIXME | Keine | Sauber |
| Placeholder returns | Keine | return null nur in Validator (korrekt) und LLM-Failure-Handler (korrekt, Caller handelt) |
| Empty handlers | Keine | Alle catch-Bloecke loggen Fehler |
| Console debugging | Keine | Nur operatives console.log/error in Engine und Cron (erwartet) |
| Hardcoded values | Keine | Thresholds (14d, 7d, 3d) und Limits (8, 5, 7) als Konstanten — angemessen |
| Commented-out code | Keine | Sauber |

## Wiring Verification

| Schritt | Status | Details |
|---------|--------|---------|
| Cron Route → processFollowupCandidates() | OK | Import und Aufruf korrekt, limit=20 |
| Engine → findFollowupCandidates() | OK | 3 Supabase-Queries mit korrekten Thresholds |
| Engine → generateFollowupSuggestion() | OK | queryLLM + parseLLMResponse korrekt verdrahtet |
| Engine → createAction() | OK | Mapping von Suggestion zu AIActionQueueItem korrekt, dedup_key gesetzt |
| page.tsx → getPendingFollowups() | OK | In Promise.all integriert, Prop korrekt durchgereicht |
| MeinTagClient → FollowupSuggestions | OK | Import, Prop-Uebergabe, Platzierung nach KIWorkspace |
| FollowupItem → approveFollowup() | OK | Server Action aufgerufen, Status-Transition korrekt |
| FollowupItem → postponeFollowup() | OK | Server Action aufgerufen, 3 Tage default |
| FollowupItem → rejectFollowup() | OK | Server Action aufgerufen, ai_feedback Entry erstellt |
| approveFollowup → tasks INSERT | OK | Entity-Lookup (deal/contact/email_message), Priority-Mapping, [KI]-Prefix |

## Findings

### Low

1. **Redundante entityType-Zuweisung** — [followup-engine.ts:224](cockpit/src/lib/ai/followup-engine.ts#L224)
   `const entityType = proposal.deal_id ? "deal" : "deal"` — Beide Pfade ergeben "deal". Korrekt im Verhalten (Proposals haben immer eine Deal-Beziehung), aber die Bedingung ist sinnlos. Kosmetisch.

2. **Inactive contacts explizit ausgelassen** — [followup-engine.ts:110-112](cockpit/src/lib/ai/followup-engine.ts#L110-L112)
   Die Slice-Spec erwaehnt "Kontakte ohne Interaktion >30d", aber die Engine implementiert nur 3 von 4 Kategorien. Das ist korrekt dokumentiert mit Comment ("deferred to a later version"), aber die Spec-Abweichung sollte sichtbar sein. Akzeptabel fuer internal-tool, da die 3 implementierten Kategorien die geschaeftskritischsten abdecken.

## Open Risks

- **Bedrock-Kosten**: Jeder Kandidat kostet einen Bedrock-Call. Bei 20 Kandidaten pro Cron-Run (alle 6h) sind das max 80 Calls/Tag. Kontrolliert.
- **Browser-Verifikation**: Mein-Tag Wiedervorlagen-Card wurde nicht im Browser geprueft (Build-only). Muss nach Deploy gecheckt werden.
- **Cron-Job**: `/api/cron/followups` muss in Coolify eingerichtet werden (empfohlen: alle 6h).

## Readiness Assessment

**PASS** — Alle 6 Acceptance Criteria erfuellt. Keine Blocker, keine High-Findings. 2 Low-Findings (kosmetisch/dokumentiert). Code kompiliert und baut sauber. Bereit fuer Deploy.

## Recommended Next Step

Deploy auf Hetzner → Browser-Check → Cron-Job `/api/cron/followups` einrichten → SLC-406 Auto-Reply Detection
