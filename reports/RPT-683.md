---
id: RPT-683
date: "2026-07-08"
skill: /qa
slice: SLC-915
feature: FEAT-927
type: completion
status: completed
reviewOf: null
result: "Gesamt-/qa V8.17 (SLC-915, 6 MTs Cross-Cut) — MIXED → PASS-CODE/PENDING-LIVE, 0 neue Findings; Code-Gates alle grün (tsc0/suite 1636/build/lint-net-verbessert), CSP-MT1↔MT6-Boundary test-gelockt; browser-gebundene ACs → funktionaler Feature-Flow-Smoke @ /deploy"
title: "Gesamt-/qa V8.17 — Cross-Cut aller 6 MTs von SLC-915"
---

# Gesamt-/qa V8.17 — SLC-915 Cross-Cut (alle 6 MTs)

Aggregierender Version-Cross-Cut über das V8.17-Regressions-Fix-Bundle. Grundlage: die 6 per-MT-/qa-Reports (RPT-677 MT-1, RPT-678 MT-2+MT-3, RPT-679/680 MT-4 PASS-LIVE in-tx, RPT-681 MT-5, RPT-682 MT-6). Dieser Lauf verifiziert das **Zusammenspiel** der MTs + den repo-weiten Suite-/Hygiene-Stand + die BLOCKING funktionale CSP-Feature-Flow-Smoke-Checkliste (IMP-1401). Kein Deploy lag zwischen den per-MT-Läufen und diesem Cross-Cut → per-MT bereits erreichte PASS-LIVE (MT-4 in-tx-Probe) werden nicht re-verifiziert (Verdikt-Taxonomie).

## QA scope reviewed

Diff-Range `143d08d..HEAD` (Parent des ersten MT-Commits `26e56a6` bis `0eefded`), 19 Code/SQL/Test-Files:

- **MT-1 (ISSUE-138):** `cockpit/src/lib/security/csp.ts`, `cockpit/next.config.ts`, `cockpit/src/lib/security/__tests__/csp.test.ts`
- **MT-2 (ISSUE-142):** `cockpit/src/lib/meetings/consent-check.ts` (+ `.test.ts`)
- **MT-3 (ISSUE-141):** `cockpit/src/app/(app)/mein-tag/actions.ts`, `cockpit/src/app/(app)/mein-tag/mein-tag-client.tsx`
- **MT-4 (ISSUE-140 / MIG-055):** `sql/migrations/055_v817_slc915_class_c_changed_fk_trigger.sql`, `cockpit/__tests__/migrations/055-v817-class-c-changed-fk-trigger.test.ts`
- **MT-5 (ISSUE-140):** `cockpit/src/app/(app)/mein-tag/followup-actions.ts`, `cockpit/src/app/(app)/fit-assessment/signal-actions.ts`, `cockpit/src/app/(app)/emails/imap-actions.ts`, `cockpit/src/app/(app)/meetings/actions.ts`
- **MT-6 (ISSUE-139):** `cockpit/src/lib/email/sanitize-email-html.ts` (+ `.test.ts`), `cockpit/src/lib/email/email-frame.ts`, `cockpit/src/app/api/emails/[id]/body/route.ts`, `cockpit/src/components/email/email-html-iframe.tsx` (+ `.test.tsx`), `cockpit/src/app/(app)/emails/email-detail.tsx`

## Result

**MIXED → PASS-CODE / PENDING-LIVE.** 0 neue Findings über den Cross-Cut. Alle statischen/Code-Gates grün, keine MT-Interaktions-Regressionen, CSP-Boundary zwischen MT-1 (global) und MT-6 (route-scoped) sauber und test-gelockt. Die 5 browser/interaktions-gebundenen ACs (915-1/2/4/6/7) bleiben PENDING-LIVE → Nachhol = funktionaler Feature-Flow-Smoke im **/deploy** gegen deployed main (Checkliste unten). Slice bleibt `in_progress` (code-done) bis /deploy-Live-Smoke.

### AC-Aggregat-Matrix (Version-Ebene)

| AC | Verdict | Evidenz (Cross-Cut) |
|---|---|---|
| AC-915-1 — SIP-Softphone unter enforced CSP (wss in connect-src) + `microphone=(self)`; funktionaler Telefonie-Smoke 0 CSP/Perm-Errors | **PASS-CODE / PENDING-LIVE** | csp.test.ts lockt wss-in-connect-src + `microphone=(self)`; next.config.ts-Caller mit wss-2.-Param (`sip.strategaizetransition.com`-Fallback). Call-Flow-Browser-Smoke → /deploy. |
| AC-915-2 — `camera=()` bleibt; kein App-Origin-Kamera-Flow; Meeting-Start ohne Perm-Block | **PASS-CODE / PENDING-LIVE** | csp.test.ts: `camera=()` gesperrt, `not camera=(self)`, exakt 5 Features (Step-0 DRIFT-1). Meeting-Start-Smoke (window.open Jitsi) → /deploy. |
| AC-915-3 — `checkConsentStatus` fail-closed (returned-Count < angefragt ⇒ missing, allGranted=false), caller-unabhängig | **PASS-LIVE (unit)** | Vitest deckt RLS-weggefilterte Row (2 angefragt/1 Row → allGranted=false); ISSUE-142 resolved. Reine Server-Logik, kein Browser-Gate. |
| AC-915-4 — `MeetingPrepCard` wertet `res.error` (sichtbare Meldung); keine RLS-unsichtbare rohe `contact_id` | **PASS-CODE / PENDING-LIVE** | Grep: `(meeting as any).contact_id` entfernt (actions.ts:449 → `contact?.id ?? null`, senkte zugleich Lint 34→33); MeetingPrepCard `useState`-Fehlerzustand + Inline-Render. Dead-Button→Meldung-Interaktion → /deploy. |
| AC-915-5 — MIG-055: Status-Update (unveränd. FK) auf mixed-owner-Row für Non-Admin PASS; FK-Change auf unsichtbaren Parent BLOCK; INSERT strikt (AND) | **PASS-LIVE (in-tx, RPT-680)** | In-tx-Probe 8/8 gegen Coolify-DB (BEGIN→apply→seed→8 Checks→ROLLBACK, Dev-IMP-1694). Kein Deploy dazwischen → kein Re-Verify. Live-Apply = /deploy-Ops-Pre-Cond (`sql-migration-hetzner.md`). |
| AC-915-6 — 4 Call-Sites prüfen Insert/Update-Fehler + kompensieren | **PASS-CODE / PENDING-LIVE** | Grep: kein unchecked `.insert(`/`.update(` mehr auf den 4 Pfaden (followup:126, signal:73, imap:135, meetings:228); Kompensationslogik unit-abgedeckt. Interaktions-Pfade → /deploy. |
| AC-915-7 — Inbound-Viewer blockt Remote-Bilder + Banner; „Bilder laden" via route-scoped `/api/emails/[id]/body` (RLS, kein Proxy) | **PASS-CODE / PENDING-LIVE** | sanitize strippt http(s)-img (blockRemoteImages default via email-detail); Route RLS-User-Client + route-scoped CSP; Build registriert `ƒ /api/emails/[id]/body`. Browser-Render/Höhe/RLS-404 → /deploy. |
| AC-915-8 — `npm run test` grün inkl. Regressionstests; enforced CSP-Header bleibt (Report-Only 0); globale img-src/connect-src strikt außer wss-SIP | **PASS-LIVE** | Suite 176 Files / **1636 Tests passed**; csp.test.ts: `img-src 'self' data: blob:` app-weit unverändert, kein generisches `https:` global (nur im isolierten Route-Header). |

**Aggregat:** 3 PASS-LIVE (915-3/5/8), 5 PASS-CODE/PENDING-LIVE (915-1/2/4/6/7). Kein FAIL, kein CONDITIONAL, kein STATIC-ONLY-ohne-Wiring.

## Test execution (repo-weiter Cross-Cut)

- `npx tsc --noEmit` → **exit 0**.
- `npm run test` (vitest run) → **176 Files / 1636 Tests passed**, 0 failed. Baseline vor Slice 1623 → **+13** (MT-6: 8 sanitize + 5 iframe; csp/consent-Erweiterungen ersetzten bestehende Cases in-place). Dauer 102s. Keine Skip-Anomalien.
- `npm run build` (next build) → **Compiled successfully**, exit 0; neue Route `ƒ /api/emails/[id]/body` registriert (separat von `/api/emails/attachments`).
- **Silent-Skip-Audit:** neue Test-Files (`csp.test.ts` unter `src/lib/security/__tests__/`, `sanitize`/`iframe`/`consent`) matchen `vitest.config` include (`src/**/*.test.ts(x)`) und liefen im Default-Lauf. MIG-055-DB-Test (`cockpit/__tests__/migrations/055-*.test.ts`) ist **Coolify-DB-Layer** (`vitest.rls.config.ts`, node:20-Sidecar) — bewusst NICHT im Default-Lauf; verifiziert per in-tx-Probe (RPT-680), Live-Apply = /deploy. 0 gelaufene ≠ hier: alle Default-Tests liefen.

## Stub detection (Cross-Cut)

`git diff 143d08d..HEAD` auf allen **added** Prod-Zeilen (Test-Files ausgenommen) gegen TODO/FIXME/HACK/`debugger`/`console.log|debug`/`return null;`/`return [];`/`return {};`/empty-catch → **NONE FOUND**. Deckt sich mit den per-MT-Stub-Scans (RPT-677..682 je 0).

## Wiring verification (Cross-Cut, MT-übergreifend)

Der einzige MT-übergreifende Berührungspunkt ist die **CSP-Schicht** (Slice-Cross-Slice-Dependencies §158): MT-1 ändert die globale CSP (`csp.ts`/`next.config.ts`), MT-6 fügt einen **route-scoped** CSP-Header in der neuen Body-Route hinzu. Verifiziert:

1. **Global img-src (MT-1-Umfeld):** `img-src 'self' data: blob:` — kein generisches `https:`. Unverändert gegenüber Baseline.
2. **Route-scoped CSP (MT-6):** `default-src 'none'; img-src https: data: blob: cid:; …` lebt **ausschließlich** im Response-Header von `/api/emails/[id]/body` (Zeile 27/65), nicht in `csp.ts`. Das generische `https:` für Bilder ist damit auf genau diese eine RLS-scoped Route begrenzt.
3. **connect-src (MT-1):** wss-SIP-Origin additiv neben 'self'/sentry/kong/bedrock; `.filter(len>0)` verhindert leeres Segment.
4. **Kein Konflikt:** MT-6 ändert `csp.ts` nicht; MT-1 ändert die Body-Route nicht. File-disjunkt wie in der Slice-Spec deklariert.
5. **Test-Lock (Regressions-Guard):** `csp.test.ts` asserted die Boundary hart — wss in connect-src, `img-src 'self' data: blob:` app-weit, `microphone=(self)`, `camera=()` gesperrt, exakt 5 Permissions-Features. Ein späteres versehentliches Aufweichen der globalen CSP bricht den Test.

Die per-Capability-Ketten (Consent-Guard caller-unabhängig, MeetingPrepCard Render-Ende, 4 Call-Site-Rückkanäle, Email-Viewer Component→Route→DB→Render) sind in RPT-678/681/682 Ende-zu-Ende bestätigt; kein Deploy dazwischen → hier nicht re-verifiziert.

## Lint-Baseline (Cross-Cut, ehrlich)

`npx eslint` über alle 13 berührten Prod-Files: nur **email-detail.tsx** trägt 4 Problems (1 error + 3 warnings, Zeilen 12/28/38 — **pre-existing**, dokumentierte Baseline L-1, RPT-653/666, außerhalb des SLC-915-Diffs Zeile 9+231). **mein-tag/actions.ts**: Baseline `143d08d` 34 Problems → jetzt **33** (SLC-915 MT-3 **entfernte** einen `as any`-Cast). Alle übrigen 11 berührten Files: **0**. → **Kein neuer Lint-Fehler durch V8.17**, netto −1.

## Findings

**Blocker / High / Medium / Low: keine neuen.** Aus den per-MT-Läufen offen und weiterhin gültig (nicht V8.17-blockierend):
- **ISSUE-143** (Low) — Consent-Banner leerer Name (MT-2-Umfeld), deferred.
- **ISSUE-144** (Low) — 3 weitere ungeprüfte Write-Pfade derselben Klasse wie ISSUE-140 (activities:211 / createSignal:56 / updateMeeting:301), Multi-User-latent, außerhalb SLC-915 MT-5-Scope, deferred.

## Open risks / missing checks — funktionaler Feature-Flow-Smoke (BLOCKING @ /deploy)

Der Slice ist ein Security-Header-Slice mit verhaltensänderndem CSP `connect-src`/`Permissions-Policy` + neuem Infra-Pfad (Body-Route). Kein volles PASS ohne funktionalen Browser-Smoke (nicht `curl -I`-only, nicht nur Hydration — RPT-672-Trugschluss-Prävention, IMP-1401). Ausführung gegen **deployed main** (SOURCE_COMMIT-Match prüfen), Playbook `security-headers-live-smoke.md`:

**Header-Baseline:**
1. `curl -I <prod-URL>` → enforced `Content-Security-Policy` gesetzt (Report-Only 0); `connect-src` enthält `wss://sip.strategaizetransition.com`; `Permissions-Policy: …microphone=(self)…camera=()…`.
2. `node tests/_probe/csp-check.mjs <URL>` → Hydration-Baseline, 0 CSP-Console-Errors.

**Funktionaler Feature-Flow (BLOCKING):**
3. **Telefonie (AC-915-1):** Softphone-Call-Flow auslösen → WSS verbindet, Mikrofon-Zugriff da, **0** `connect-src`/`Permissions-Policy`-Console-Errors auf dem Call-Flow.
4. **Meeting (AC-915-2):** „Meeting starten" in Mein Tag → `window.open` öffnet Jitsi (separater Origin), **kein** Permissions-Block; App-CSP unberührt.
5. **E-Mail-Viewer (AC-915-7):** Inbound-Mail mit Remote-Bild öffnen → Bilder **blockiert** + Banner; „Bilder laden" → iframe `src=/api/emails/<id>/body`, Bilder **rendern** (Netzwerk-Tab: direkt, kein Server-Proxy), Höhe korrekt unter `sandbox="allow-same-origin"`. RLS: `GET /api/emails/<fremde-id>/body` → **404**, ohne Session → **401**.
6. **MeetingPrepCard (AC-915-4):** Fehlerfall → sichtbare Inline-Meldung statt Dead-Button.
7. **Global unverändert (AC-915-8):** `curl` `/mein-tag` → `img-src` weiterhin ohne generisches `https:`.

**Migration-Live (AC-915-5, /deploy-Ops-Pre-Cond):** MIG-055 Live-Apply (`sql-migration-hetzner.md`) + DB-Verify positiv/negativ/mixed-owner (node:20-Sidecar, Precedent MIG-054).

**Bei FAIL:** Hotfix-Path — `csp.ts`/`next.config.ts` reapply + Coolify-Redeploy (`coolify-autonomous-redeploy.md`); MIG-055 reapply via `sql-migration-hetzner.md`.

## Readiness assessment

V8.17 SLC-915 ist **code-seitig vollständig und cross-cut-verifiziert**: alle 6 MTs code-done, repo-weite Suite 1636/1636, tsc0, build0, 0 Stubs, 0 neue Lint-Fehler (netto −1), CSP-MT1↔MT6-Boundary sauber und test-gelockt, MIG-055 in-tx 8/8 PASS-LIVE. Die verbleibende Verifikation ist ausschließlich browser-/live-gebunden und deploy-inhärent (Serving-Pfad existiert erst nach /deploy). **Kein Deploy-Blocker aus dem Code-Cross-Cut.** Slice bleibt `in_progress` bis der funktionale Feature-Flow-Smoke @ /deploy grün ist.

Kein Customer-Live — internes Workflow-Closure (module-lifecycle-discipline, IMP-950). Last Stable bleibt V8.16 bis /post-launch T+24h.

## Project record updates

- `docs/STATE.md` — Current Focus: Gesamt-/qa V8.17 PASS-CODE/PENDING-LIVE (RPT-683), alle 6 MTs cross-cut-verifiziert; NEXT = /final-check V8.17.
- `slices/INDEX.md` — SLC-915 bleibt `in_progress` (browser-gebundene ACs offen bis /deploy).
- `docs/KNOWN_ISSUES.md` — keine Statusänderung (ISSUE-138..142 bereits per-MT gepflegt; 143/144 bleiben Low/deferred).
- `reports/RPT-683.md` — dieser Report.

## Next workflow step

**`/final-check V8.17`** — Pre-Release-Audit der ganzen Version (Hygiene, Security, Dependencies, Secrets, Test-Baseline, "use server"-Export-Check, Hotfix-Ancestor-Check). Danach `/go-live` (GO-Verdict) → `/deploy` (MIG-055 Live-Apply + funktionaler Feature-Flow-Smoke oben) → `/post-launch` T+24h (dann erst Last-Stable-Flip V8.16→V8.17). Push bleibt bis zum kompletten V8.17-Bundle gehalten. Kein Customer-Live.
