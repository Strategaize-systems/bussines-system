---
id: RPT-624
date: "2026-06-10"
skill: /deploy
slice: SLC-906
feature: FEAT-921
type: completion
status: completed
reviewOf: RPT-623
result: "V8.12 DEPLOYED — Coolify-Redeploy GREEN auf Hotfix-Commit 8b08996, Live-Smoke-Bundle PASS. 1. Redeploy FAILED (Dockerfile selective-COPY liess sentry.*.config.ts aus), gefixt."
title: "V8.12 /deploy DONE + Live-Smoke PASS (inkl. Dockerfile sentry-config COPY Hotfix)"
---

# V8.12 Defense-in-Depth Sprint — /deploy

## Outcome
V8.12 ist **DEPLOYED** (nicht stable — /post-launch T+24h ausstehend, kein Customer-Live per [[module-lifecycle-discipline]] + IMP-950). Coolify-Redeploy auf `91.98.20.191` GREEN. **Vollständig: ja**, mit einem während des Deploys gefundenen + gefixten Build-Blocker.

## Deploy-Story
1. **Phase 1 Prerequisites:** git `main=origin/main=365b3b5` clean; neue Deps `@sentry/nextjs`+`zxcvbn` in package.json **und** lock; SSH-Zugang OK; Disk 76%/8.8G frei; App `Up 3 days healthy` (V8.11-Image). Founder hat 5 Coolify-ENVs gesetzt (4× Sentry + `SENTRY_CSP_REPORT_URI` aus DSN abgeleitet).
2. **1. Redeploy (Commit `365b3b5`) → FAILED** (~1 min, Docker-Build `RUN npm run build` exit 1). Log aus coolify-db gezogen: Turbopack `Module not found: ../sentry.server.config` + `../sentry.edge.config` in `src/instrumentation.ts`.
3. **Root-Cause:** Die drei `sentry.*.config.ts` liegen im cockpit-Root; der Dockerfile-Builder-Stage hat eine **Whitelist-`COPY`** (`src/`, `public/`, `next.config.ts`, `tsconfig.json`, `postcss.config.mjs`, `components.json`) und kopierte die Root-Configs nie ins Build-Image. Lokaler `next build` (in `/qa` RPT-615 + `/final-check` RPT-622) lief durch, weil dort alle Dateien auf der Platte liegen — der selektive COPY wird nur im Docker-Build scharf. **OOM-Hypothese (Swap war 100% voll) verifiziert widerlegt** vor Aktion.
4. **Hotfix `8b08996`** (push origin/main): `COPY sentry.client.config.ts sentry.server.config.ts sentry.edge.config.ts ./` im Builder-Stage + Kommentar. Vorab-Check: keine weiteren root-escapenden Build-Imports (alle anderen `../`-Imports bleiben in `src/`).
5. **2. Redeploy (Commit `8b08996`) → GREEN.** Container `app-k9f5pn5upfq7etoefb5ukbcg-162812117067` Up healthy, RestartCount=0, Image `sha256:a55874aa9a9ced93e67781857e08ef8e8787bee9dd4f3ff135780e97184ce87a`, `SOURCE_COMMIT=8b08996`, alle Sentry-ENVs + CSP-Report-URI im Container present.

## Live-Smoke-Bundle (16:31–16:34 UTC) — PASS
| Pfad | Ergebnis |
|---|---|
| App-Health | `Up healthy`, RestartCount=0 |
| Production HTTP | `/` 307 (→Login), `/auth/set-password` 200 m. Passwort-Input (SLC-908) |
| 6 Security-Header (SLC-910 Phase-A) | CSP-Report-Only **mit `report-uri`→Sentry** + Permissions-Policy + HSTS + X-Content-Type-Options + Referrer-Policy + X-Frame-Options:DENY |
| Sentry (SLC-911 AC-911-2) | `sentry:"enabled"`, 2 Test-Events (`?type=error` 500 by-design + `?type=message` 200) gesendet |
| Log-Redaction (SLC-907) | 0 Secret-Leak in 5-min-Logs |
| RLS Done-Gate (V8.11) | `list_tables_with_authenticated_full_access()` = **0** nach Full-Restack-Recompose |
| Error-Logs | 0 |

## Files changed
- `/cockpit/Dockerfile` — **Hotfix:** `COPY sentry.*.config.ts ./` im Builder-Stage ergänzt (Commit `8b08996`). Behebt prod-Build Module-not-found.
- `/docs/RELEASES.md` — REL-047 Image-Tag `tbd-coolify-redeploy` → `8b08996` (+ Image-Digest), Date, Deploy-Story (1. FAILED + Hotfix), Live-Smoke-Sektion, `NEXT_PUBLIC_SENTRY_DSN`-ARG-Hinweis.
- `/slices/INDEX.md` — SLC-906/907/908 `done` → `deployed`; SLC-910/911 bleiben `in_progress` (Phase-B / Sentry-Visual).
- `/features/INDEX.md` — FEAT-921 `done` → `deployed`; FEAT-922/923 bleiben `in_progress`.
- `/docs/STATE.md` — High-Level State `go-live` → `deploying`; Current Focus = /deploy DONE; Immediate Next Steps top auf V8.12 /post-launch re-baselined.

## Files created
- `/reports/RPT-624.md` — dieser Report.

## Problems found
- **P-1 (gefixt) — Dockerfile selective-COPY liess neue Root-Level-Build-Inputs aus.** Deploy-Blocker, der weder `/qa` noch `/final-check` fangen konnte, weil beide nur lokalen `next build` fahren (kein selektiver COPY). → Phase-3-Learning (Dev-System-IMP + Playbook).
- **P-2 (akzeptiert) — `NEXT_PUBLIC_SENTRY_DSN` nicht als `ARG` im Dockerfile** → nicht ins Client-Bundle inlined. Unkritisch: Client-Capture in SLC-911 ohnehin nicht verdrahtet (accepted L-2); server-seitige Capture via Runtime-`SENTRY_DSN` funktioniert (Live-Smoke bestätigt). Wird relevant erst wenn Client-Capture verdrahtet wird.
- **P-3 (Beobachtung) — Swap war Pre-Deploy 100% voll** (4.0G/4.0G), ~3G RAM frei. Build lief trotzdem durch (war nicht OOM). In /post-launch T+24h Memory/Swap beobachten.

## Verification gaps
- **SLC-906 AC-906-6 5-Pfad-UI-Session-Smoke:** Code-Layer Vitest-verifiziert (49 Tests) + DB-Layer-RLS live-confirmed (Done-Gate=0). Die UI-Session-5-Pfade brauchen einen Login → nach /post-launch T+3h mit Founder-Session oder seeded Test-User.
- **SLC-911 Sentry-Dashboard-Visual:** 2 Events server-seitig gesendet + `sentry:enabled`; das tatsächliche Erscheinen im Sentry-Dashboard (`javascript-nextjs`, vorher „No activity yet") ist Founder-Visual-Confirm → schliesst SLC-911.

## Project record updates
SLC-906/907/908 + FEAT-921 → `deployed`; REL-047 Image-Tag gepinnt; STATE re-baselined. SLC-910/911 + FEAT-922/923 bleiben `in_progress` (Live-Portionen). roadmap V8.12 bleibt `released` (Last Stable Version bleibt V8.11 bis /post-launch T+24h).

## Recommended next step
`/post-launch V8.12 T+3h` Light-Check (~+3h, ~19:31 UTC) → `/post-launch V8.12 T+24h` Full-Check (~2026-06-11 16:31 UTC) → V8.12 STABLE. SLC-910 Phase-B CSP strict-Switch erst NACH 1-2 Wo Burn-In separat (Sentry-Security-Violations sichten + `@playwright/test` install + `csp-check.mjs`).
