---
id: RPT-442
date: "2026-05-16"
skill: /backend
slice: SLC-751
feature: FEAT-752
type: completion
status: completed
reviewOf: null
result: "SLC-751 Backend-Code-Side done — Middleware-Header + Layer-2-Guard + 13 neue Vitest-Cases PASS. Pending: User-Coolify-Deploy + /qa MT-5 Playwright-MCP-Live-Smoke + audit_log-Verifikation."
title: "/backend SLC-751 — ISSUE-066 Middleware-Mitigation (Layer-2 Header-Read)"
---

# /backend SLC-751 — ISSUE-066 Middleware-Mitigation

## Outcome summary

Code-Side **done** fuer MT-0 bis MT-4 + MT-6 (Test-Run + Commit). Pending: User-Coolify-Deploy + /qa MT-5 (Playwright-MCP-Live-Smoke + audit_log-SELECT-Verifikation) + Master-Merge + Records-Sync.

13 neue Vitest-Cases PASS (792 vs. 779-Baseline auf main):
- `src/middleware.test.ts` — 9 Pfad-Cases (DEC-210 Match-Tabelle).
- `src/lib/auth/read-only-context.test.ts` — +4 Defense-in-Depth-Cases (header / no-header / ALS / both).

Branch `slc-751-issue066-middleware` Commit `be87351` auf origin gepusht. Master-Merge wartet auf /qa Live-Smoke PASS per `feedback_slice_merge_at_end`.

## Files reviewed

- `/cockpit/src/middleware.ts` (pre-state)
- `/cockpit/src/lib/supabase/middleware.ts` (pre-state — Supabase-Cookie-Refresh-Closure verstanden)
- `/cockpit/src/lib/auth/read-only-context.ts` (pre-state — SLC-706 AsyncLocalStorage)
- `/cockpit/src/lib/auth/read-only-context.test.ts` (pre-state — 7 existing Tests)
- `/cockpit/src/lib/supabase/admin.ts` (Audit-Insert via service-role-Client)
- `/cockpit/src/lib/team/view-as-audit.ts` (Audit-Insert-Pattern aus SLC-706 MT-1)
- `/sql/08_v3_schema.sql` Lines 85-94 (`audit_log`-Schema: actor_id NOT NULL UUID, entity_type+entity_id NOT NULL, context TEXT JSON)
- `/cockpit/src/app/(app)/team/[user_id]/__tests__/read-only-context.test.ts` (SLC-706 Mutate-Lockdown-Tests bleiben gruen)
- `/cockpit/src/app/(app)/pipeline/actions.ts` (10x `await assertNotReadOnlyContext()` Callsites — bestaetigt async-Compat ohne Breaking-Change)
- `/slices/SLC-751-issue066-middleware-mitigation.md` (komplette Spec)

## Files changed

- `/cockpit/src/middleware.ts` — Re-Export `pathMatchesReadOnlyDrilldown` aus `lib/auth/read-only-paths` (Pure-Function in separater Datei, vermeidet Circular-Import zwischen `middleware.ts` und `lib/supabase/middleware.ts`).
- `/cockpit/src/lib/supabase/middleware.ts` — In `updateSession()` wird beim Pfad-Match `X-Read-Only-Mode: 1` auf `requestHeaders` (Klon der incoming-Headers) gesetzt. Header geht ueber `NextResponse.next({ request: { headers: requestHeaders } })` durch — sowohl beim Initial-Response als auch beim Supabase-Cookie-Refresh-`setAll`-Callback. So bleibt der Header verlaesslich auch wenn Supabase Cookies refreshed.
- `/cockpit/src/lib/auth/read-only-context.ts` — `assertNotReadOnlyContext()` wird **async**. Layer 1 (AsyncLocalStorage) short-circuits, Layer 2 liest `await headers()` und prueft `X-Read-Only-Mode === "1"`. Bei Header-Block: Best-Effort `audit_log`-Insert mit Action `read_only_context_blocked` via Lazy-Import von `createAdminClient` + `createClient` (Server). Bei Audit-Fail: `console.warn` + dennoch Throw. Kommentar-Block (Zeilen 3-29) auf "ISSUE-066 closed in V7.5" aktualisiert.
- `/cockpit/src/lib/auth/read-only-context.test.ts` — 2 existierende ALS-Tests auf `await expect(...).rejects.toThrow(...)`-Pattern umgestellt (war vorher sync `.toThrow()`). `vi.mock("next/headers")` plus `mockHeaders`-Helper. +4 neue Tests fuer Defense-in-Depth-Permutations.

## Files created

- `/cockpit/src/lib/auth/read-only-paths.ts` — Pure-Function `pathMatchesReadOnlyDrilldown(pathname): boolean`. Regex `/^\/team\/[^/]+\//`. Separate Datei statt im `middleware.ts`-Root um Circular-Import zwischen `middleware.ts` (importiert `updateSession`) und `lib/supabase/middleware.ts` (jetzt Konsument der Pfad-Pure-Function) zu vermeiden.
- `/cockpit/src/middleware.test.ts` — 9 Vitest-Cases nach DEC-210 Match-Tabelle (3 positive + 6 negative).

## Files intentionally not changed

- `/cockpit/src/lib/audit.ts` — Existiert nicht. Audit-Insert geht direkt via `createAdminClient().from('audit_log').insert(...)` analog zum `view-as-audit.ts`-Pattern aus SLC-706.
- `/docs/KNOWN_ISSUES.md` — ISSUE-066-Status wird in **MT-6 nach /qa Live-Smoke PASS** auf `resolved` gesetzt (per Slice-Spec). Aktuell bleibt `open`.
- `/docs/DECISIONS.md` — DEC-210/DEC-211-Status-Sync ist Teil von MT-6 nach Live-Smoke.
- `/slices/INDEX.md` + `/features/INDEX.md` + `/planning/backlog.json` — Status-Updates auf `done` ebenfalls MT-6 nach Live-Smoke.

## Problems found

- **Slice-Spec-AC8-Zahl veraltet:** Spec sagt 917-Baseline. Aktueller `npm run test`-Run auf `main` zeigt 779 PASS (82 Files). Mit SLC-751: 792 PASS (83 Files). Differenz +13 = AC8-Erwartung (4+9) erfuellt. Die 917 in der Slice-Spec ist ein veralteter Doc-Wert; die +13-Math ist intakt.
- **Snapshot-Line-Ending-Drift auf Windows:** Vitest-Run modifizierte zwei pre-existing Snapshot-Files (`render.test.ts.snap`, `proposal-renderer.test.ts.snap`) durch CRLF/LF-Umstellung. Modifications zurueckgesetzt vor Commit — gehoeren nicht zu SLC-751. Bekanntes Windows-Vitest-Drift-Problem, kein Implementation-Issue.
- **Pre-Existing TSC-Errors** in unrelated Test-Fixtures (`useVoiceCapture.test.tsx`, `reverse-charge-revert.test.ts`, `vies-client.test.ts`). Sie liegen nicht in SLC-751-Touch-Files und sind pre-V7.5-Drift. Verfolgung gehoert in einen separaten Slice/Doctor-Step.
- **`audit_log.entity_id` NOT NULL UUID, aber Read-Only-Block hat keine konkrete Entity** — geloest mit `entity_id = actor_id` (Self-Reference). `entity_type='read_only_context'` macht klar, dass die Entity hier nicht eine fachliche Entity ist sondern der Block-Pfad selbst.

## Open points or deferred decisions

- **MT-5 Live-Smoke**: Wartet auf User-Coolify-Deploy. Pattern aus Memory `reference_playwright_live_smoke_pattern`. Playwright-MCP via Admin-API-Login Teamlead → Navigate `/team/<member-uuid>/pipeline` → `browser_evaluate` Direct-Fetch mit `Next-Action`-Header → Expect Error-Response + audit_log-Eintrag verifiziert per `docker exec` psql.
- **Risk R1 aus Slice-Spec** (`next/headers`-Verfuegbarkeit in Server-Action-Context) — Vitest-Mock simuliert erfolgreich beide Pfade. Real-World-Bestaetigung kommt erst durch MT-5 Live-Smoke. Falls Drift: Architecture-Adjustment via `cookies()`-Fallback.
- **Master-Merge** wartet auf MT-5 PASS per `feedback_slice_merge_at_end`.
- **Records-Sync (MT-6)**: ISSUE-066 `resolved`, SLC-751 `done`, FEAT-752 `done`, BL-476 `done`, DEC-210+DEC-211 `accepted`-Sync wird in `/qa`-Wrap nach Live-Smoke gemacht (Per `feedback_cockpit_records_auto`).

## Project record updates

Diese Sektion bewusst leer auf Backend-Stufe — alle Records-Sync-Aktionen liegen in MT-6 nach /qa MT-5 PASS gemaess Slice-Spec. STATE.md, INDEX-Files, backlog.json, KNOWN_ISSUES.md werden im naechsten Schritt erst angepasst. RPT-442 selbst ist der einzige Cockpit-sichtbare Record-Add aus diesem /backend-Schritt.

## QA after implementation steps

`/qa SLC-751` ist der zwingend folgende Schritt. MT-5 Live-Smoke gehoert zum QA-Step, daher zaehlt die Code-Side-Done nicht als komplette Slice-Lieferung — erst nach MT-5-PASS + Master-Merge + MT-6-Records-Sync.

Per `feedback_qa_mandatory` + `feedback_qa_after_every_slice`: vor dem User-Coolify-Deploy bitte `/qa SLC-751` aufrufen, sodass:
1. Vitest-Suite und ggf. test:rls validation (RLS-Tests fuer audit_log-Insert) bestaetigt sind
2. Coolify-ENV-Pre-Check + Deploy-Plan formuliert
3. Live-Smoke-Schritte vorbereitet und nach User-Coolify-Redeploy abgearbeitet werden koennen.

## Recommended next step

**`/qa SLC-751`** — zunaechst Code-Side-/qa (TSC-Check fuer SLC-751-Files, Vitest-Suite, Coverage). Nach User-Coolify-Redeploy MT-5 Live-Smoke + audit_log-SELECT-Verifikation auf Hetzner-Server. Bei Live-Smoke-PASS: Master-Merge + MT-6 Records-Sync (ISSUE-066 resolved, SLC-751 done, FEAT-752 done, BL-476 done) + /qa-RPT.
