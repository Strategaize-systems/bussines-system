---
id: RPT-681
date: "2026-07-08"
skill: /qa
slice: SLC-915
feature: FEAT-927
type: completion
status: completed
reviewOf: null
result: "MT-5 (4 Call-Sites Fehlerpruefung + Kompensation, ISSUE-140) — AC-915-6 PASS-CODE/PENDING-LIVE, 0 Findings"
title: "QA SLC-915 MT-5 — 4 Call-Sites Fehlerpruefung + Kompensation (ISSUE-140)"
---

# QA — SLC-915 MT-5 (ISSUE-140, 4 Call-Sites Fehlerpruefung + Kompensation)

## QA scope reviewed

MT-5 des V8.17-Regressions-Fix-Bundles (backend-only). Ziel: verschluckte Insert/Update-Fehler an den 4 in ISSUE-140 benannten Call-Sites pruefen + kompensieren. Verifikation gegen **AC-915-6**. Backend-Commit `ca81ca5` (main).

Geprueft:
- `cockpit/src/app/(app)/mein-tag/followup-actions.ts` (`approveFollowup`)
- `cockpit/src/app/(app)/fit-assessment/signal-actions.ts` (`createSignalForActivity`)
- `cockpit/src/app/(app)/emails/imap-actions.ts` (`assignEmailToContact`)
- `cockpit/src/app/(app)/meetings/actions.ts` (`createMeeting`)

## Result

**MIXED → PASS-CODE / PENDING-LIVE.** AC-915-6 code-seitig vollstaendig erfuellt; 0 Findings. Der Kompensations-/Fehler-Branch feuert nur bei RLS-42501-Fehlern, die im heutigen Single-User-`is_admin()`-Betrieb maskiert sind (ISSUE-140 = Multi-User-latent) → Live-Exercise erst beim Multi-User-Start.

### AC-Matrix

| AC | Verdict | Evidenz |
|---|---|---|
| AC-915-6 — 4 Call-Sites pruefen Insert/Update-Fehler + kompensieren | **PASS-CODE / PENDING-LIVE** | 4/4 Sites umgesetzt (siehe Wiring); tsc0/eslint0/Suite 1623/1623/build PASS; Grep-Check 0 unchecked insert/update auf den betroffenen Pfaden. Live-Trigger (RLS-42501) Multi-User-gated. |

## Test execution

- `npx tsc --noEmit` → **0 Fehler**.
- `npx eslint` (4 Files) → **0**.
- `npm run test` (vitest run) → **176 Files / 1623 Tests passed (1623)**, 0 skip-Anomalien (Baseline unveraendert; MT-5 fuegt keine neuen Tests hinzu — Spec-Verifikation ist Grep + Suite-Green + /qa-Interaktion, RLS-Fehlerpfad nicht ohne Live-Multi-User ausloesbar).
- `npm run build` (next build) → **exit 0**.

Silent-Skip-Audit: keine neue DB-/Live-gated Suite eingefuehrt; Baseline-Count identisch zu MT-4-Lauf (1623).

## Stub detection

Scan der 4 Files auf TODO/FIXME/HACK/`console.log`/empty-catch/debugger → **NONE FOUND**. Keine Placeholder-Returns, keine leeren Handler.

## Wiring verification (Kompensations-Ketten)

Alle 4 sind Edits an bestehenden, produktiv gewireten Server-Actions (Happy-Path unveraendert, durch die 1623 gruenen Tests abgedeckt):

1. **followup-actions `approveFollowup`** — approve-Update auf `ai_action_queue` bekommt Fehlerpruefung (`approveError`); bei Task-Insert-Fehler (`taskError`) wird die Freigabe auf `status:"pending", decided_at:null, decided_by:null` zurueckgerollt → kein „approved ohne Task"-Zustand. (followup-actions.ts:69-79, :151-160)
2. **signal-actions `createSignalForActivity`** — rohe RLS-Meldung (`error.code==="42501"`) → klare deutsche Meldung; andere Fehler behalten `error.message`. (signal-actions.ts:80-90)
3. **imap-actions `assignEmailToContact`** — `emailBefore` (thread_id + alter contact_id) wird vor dem Message-Update gelesen; scheitert der Thread-Update (`threadError`), wird die Message-`contact_id` auf den alten Wert zurueckgesetzt und geworfen → keine Message/Thread-Divergenz. (imap-actions.ts:122-152) — merged zugleich die zuvor doppelte `email_messages`-Select-Query.
4. **meetings/actions `createMeeting`** — `calendar_events`-Insert bekommt `calendarError`; bei Fehler ehrliche Teil-Erfolg-Meldung („Meeting erstellt, aber Kalendereintrag fehlgeschlagen") statt stillem Schlucken (Meeting + Activity bestehen bereits). (meetings/actions.ts:228-248)

## Findings

**Blocker / High / Medium: keine.**

### Low (Beobachtungen, ausserhalb MT-5-Scope — nicht durch MT-5 eingefuehrt, pre-existing)

- **L-1 (Twin, `meetings/actions.ts:211`):** `createMeeting` hat einen zweiten ungeprueften Insert — `activities.insert(...)` (gleiche Funktion, zwischen Meeting- und Calendar-Insert). Analoges „meeting ohne activity"-Divergenz-Risiko, aber in ISSUE-140/MT-5 NICHT als eine der 4 Call-Sites benannt → bewusst im Scope belassen. Kandidat fuer Folge-Haertung.
- **L-2 (Twin, `fit-assessment/signal-actions.ts:56`):** `createSignal` (Schwester von `createSignalForActivity`) gibt weiterhin die rohe `error.message` zurueck. MT-5 benennt nur `:73` (createSignalForActivity). Identisches Muster, gleiche Datei — Kandidat fuer Konsistenz-Fix.
- **L-3 (`meetings/actions.ts:301`):** `updateMeeting` `calendar_events.update(...)` ungeprueft — separate Funktion, ausserhalb MT-5-Scope.

Alle drei sind bestehende Muster, nicht Regressionen dieses MTs. Keine Blocker; als Beobachtung dokumentiert (nicht als neue ISSUE angelegt, um KNOWN_ISSUES nicht mit Multi-User-latenten Low-Twins zu fluten — bei einem Konsistenz-Folge-Slot mitnehmen).

## Open risks / missing checks

- **Live-Exercise der Kompensations-Branches:** ohne echten RLS-42501-Fehler (nur Multi-User erzeugbar) laufen die Fehler-Pfade in keiner automatisierten Suite und nicht im Founder-Mode. Nachhol-Punkt = Multi-User-Start (identisch mit ISSUE-140 Gesamt-Resolution + MIG-055 Live-Apply). Kein Browser-Smoke-Gate fuer MT-5 (kein UI-/CSP-/Header-Change).
- Partial-Success-Rueckgabe in `createMeeting` liefert `{error:"..."}` obwohl das Meeting angelegt wurde — bewusster „nicht verwerfen"-Kompromiss (User wird informiert statt stillem Divergieren). Kein Bug; im /qa als Design-Entscheidung notiert.

## Readiness assessment

MT-5 ist code-seitig fertig, sauber verifiziert (0 Findings, 0 Stubs, alle Gates gruen) und atomar committed. Slice SLC-915 bleibt `in_progress` (MT-6 offen). Die Live-Verifikation der Fehler-Pfade ist Multi-User-gated und bewusst deferred — kein Deploy-Blocker fuer MT-5 selbst.

## Project record updates

- `docs/STATE.md` — Current Focus: MT-5 CODE-DONE + /qa PASS-CODE/PENDING-LIVE, NEXT = MT-6.
- `docs/KNOWN_ISSUES.md` — ISSUE-140 Next Action: MT-5 als CODE-DONE vermerkt; offen bleiben MIG-055 Live-Apply + MT-6 + Gesamt-/qa.
- `slices/INDEX.md` — SLC-915 bleibt `in_progress` (MT-6 offen).
- `reports/RPT-681.md` — dieser Report.

## Next workflow step

`/backend SLC-915 MT-6` (ISSUE-139 — E-Mail-Viewer Bild-Block by-default + opt-in route-scoped Body-Route ueber `email_messages`; groesster/full-stack MT, in FRISCHER Session empfohlen). Danach Gesamt-/qa V8.17 (Cross-Cut aller 6 MTs, inkl. funktionalem CSP-Feature-Flow-Smoke, IMP-1401) → `/final-check` → `/go-live` → `/deploy` (MIG-055 Live-Apply + DB-Verify node:20-Sidecar) → `/post-launch` T+24h. Kein Customer-Live (module-lifecycle-discipline + IMP-950); Push bleibt bis zum kompletten V8.17-Bundle gehalten.
