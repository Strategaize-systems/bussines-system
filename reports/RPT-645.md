---
id: RPT-645
date: "2026-06-12"
skill: /qa
slice: SLC-913
feature: FEAT-925
type: review
status: reviewed
reviewOf: RPT-644
result: "PASS (0B/0H/0M/0 Low) — Helper an allen 4 Sites verdrahtet, OWASP-Suite 29/29, href-Sweep der Pages clean, ISSUE-113/114 resolved"
title: "V8.15 SLC-913 MT-3 /qa — href-Scheme-Guard-Sweep PASS"
---

# V8.15 SLC-913 MT-3 — /qa (href-Scheme-Guard-Sweep)

## 1. QA Scope Reviewed

Commit `ea4e4ed` (MT-3): safe-external-href.ts (+29 Tests), companies/contacts-Detail-Pages, companies/actions.ts, leads-intake route. Abgleich gegen AC-913-3 + ISSUE-113/114 + R-913-4.

## 2. Result: PASS — 0 Blocker / 0 High / 0 Medium / 0 Low

## 3. Verifikation

### Wiring (Helper-Verwender-Audit)

`safe-external-href` importiert an exakt den 4 Ziel-Sites (companies page + actions, contacts page, leads-intake) + Test-File. Beide Caller-Ketten geprueft.

### href-Sweep beider Pages (ueber den Spec-Scope hinaus)

Alle verbleibenden rohen `href={`-Sites klassifiziert:
- `mailto:${email}`-Templates → Scheme fix vorgegeben, nicht injizierbar
- interne Routen-Templates (`/contacts/…`, `/deals/…`, `/companies/…`) → sicher
- `calcomUrl` (contacts:225) → `buildCalcomBookingUrl`: ENV-basiert (`CALCOM_PUBLIC_URL`/`CALCOM_BOOKING_PATH`, admin-kontrolliert) + `url.searchParams.set` (URL-encoded) → kein User-Scheme-Einfluss
- `meeting_link` (contacts:329) → bestehender https-Prepend-Guard (`javascript:` startet nicht mit http → wird harmlos geprefixt)

→ Kein weiterer ungeschuetzter user-beeinflusster href-Sink in den beiden Pages.

### AC-913-3-Matrix

| Anforderung | Status |
|---|---|
| `javascript:`/`data:`-URL in website/linkedin_url rendert als `#` | PASS (Helper-Tests; 15 OWASP-Payloads inkl. case-mixed/tab/base64/blob/file/protocol-relative) |
| OWASP-XSS-Adversarial-Vitest fuer den Helper | PASS — 29/29 GREEN |
| Write-Path-Reject (companies actions + intake) | PASS (Insert/Update Fehler-Return; intake 400) |
| R-913-4 scheme-lose `www.`-URLs nicht gebrochen | PASS (https-Prepend, 3 Testfaelle) |
| Cross-Repo-Reuse-Quelle | Helper standalone + dokumentiert; pattern-reuse-Row in MT-8 |

### Stub-Detection

0 Treffer im Helper + geaenderten Files.

### Gates (aus RPT-644 re-bestaetigt)

TSC=0 ✓ · neue Files ESLint 0/0 ✓ (3 pre-existing Findings auf unberuehrten Zeilen = Baseline) · Full-Vitest **1531/1531** ✓.

## 4. Findings

Keine. ("Problems found: none" gilt — Wiring vollstaendig, Sweep ueber Spec hinaus clean, keine Verifikations-Luecke ausser dem ohnehin /deploy-gebuendelten Browser-Smoke.)

## 5. Records-Updates

ISSUE-113/114 → `resolved` mit Resolution-Notes. STATE.md Current Focus aktualisiert.

## 6. Readiness Assessment

MT-3 ready. Helper ist Cross-Repo-Reuse-Quelle (OP/IS/immoscheckheft haben analoge Render-Sites — MT-8/Dev-System-Sync).

## 7. Next Workflow Step

`/backend SLC-913 MT-4` — Mass-Assignment-Whitelist `moveDealToStage` (ISSUE-115). Danach /qa MT-4.
