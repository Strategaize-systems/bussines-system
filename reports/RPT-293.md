---
id: RPT-293
date: "2026-05-04"
skill: /backend
slice: SLC-571
feature: FEAT-571
type: completion
status: completed
reviewOf: null
result: "MT-5 (Eligibility-Hook 24/24 Vitest) + MT-6 (Editor-Dropdown + Reverse-Charge-Section) Code-side done. TSC clean, 208/208 Vitest, Build PASS, Lint clean. Browser-Smoke offen nach User-Coolify-Redeploy."
title: "V5.7 SLC-571 MT-5+MT-6 backend done — Reverse-Charge-Eligibility + Editor-Dropdown + Section"
---

# RPT-293 — V5.7 SLC-571 Backend MT-5+MT-6

## Outcome Summary

`/backend` Code-side fuer V5.7 SLC-571 MT-5 + MT-6 vollstaendig durchgezogen. Pure-Function-Eligibility-Hook mit Country-Name-Mapper (24/24 Vitest gruen via TDD), neue Editor-Komponenten `TaxRateDropdown` + `ReverseChargeSection`, Editor-Wiring mit Reverse-Charge-Lock-Logic, Datenflusserweiterung im Workspace-Loader (`getProposalForEdit`), zod-Schema-Whitelist auf {0,7,9,19,21} + `reverse_charge:boolean`, `createProposal`-Default abhaengig von `business_country` (NL=21, DE=19), Audit-Field-Erweiterung. Status: Code-side complete — Browser-Smoke offen nach User-Coolify-Redeploy.

Vor /qa Pflicht:
1. User: Coolify-Redeploy mit aktuellem main-Stand.
2. Anschliessend /qa MT-5+MT-6 mit den im Slice-QA-Focus dokumentierten Voraussetzungs-Permutationen.

## Files Reviewed

- `slices/SLC-571-nl-vat-reverse-charge.md` (Spec + ACs)
- `cockpit/src/lib/validation/vat-id.ts` (EU_COUNTRY_CODES + Validators, MT-2)
- `cockpit/src/types/branding.ts` (BusinessCountry-Type, MT-3)
- `cockpit/src/app/(app)/companies/actions.ts` (vat_id-Sanitizer, MT-4)
- `cockpit/src/app/(app)/companies/company-form.tsx` (vat_id-Feld + Country-Field)
- `cockpit/src/app/(app)/proposals/actions.ts` (Proposal-Type + getProposalForEdit + createProposal + updateProposal)
- `cockpit/src/app/(app)/proposals/[id]/edit/proposal-editor.tsx` (vor Wiring)
- `cockpit/src/app/(app)/proposals/[id]/edit/proposal-workspace.tsx` (EditorPatch)
- `cockpit/src/lib/proposal/zod-schemas.ts` (proposalEditSchema)
- `cockpit/src/lib/pdf/proposal-renderer.test.ts` (Test-Fixture)
- `cockpit/src/lib/proposal/skonto-validation.test.ts` (Test-Convention-Referenz)

## Files Created

- `cockpit/src/app/(app)/proposals/[id]/edit/use-reverse-charge-eligibility.ts` — Pure-Function-Hook + `countryNameToCode`-Mapper (DE-Mode out-of-scope, NL-Mode 3 Voraussetzungen, deterministische `missing`-Reihenfolge, sibling-Test-Convention).
- `cockpit/src/app/(app)/proposals/[id]/edit/use-reverse-charge-eligibility.test.ts` — 24 Vitest-Cases (10 Mapper, 14 Eligibility), TDD-RED-GREEN durchlaufen.
- `cockpit/src/app/(app)/proposals/[id]/edit/tax-rate-dropdown.tsx` — Country-gefilterte `<select>`-Komponente mit Legacy-Wert-Behandlung + Lock-Mode bei Reverse-Charge.
- `cockpit/src/app/(app)/proposals/[id]/edit/reverse-charge-section.tsx` — Section mit Toggle, Tooltip-Liste der fehlenden Voraussetzungen, Quick-Links (`/settings/branding`, `/companies`), Aktiv-Hinweis bei eingeschaltetem RC. DE-Mode rendert null (BL-421).
- `reports/RPT-293.md` — dieser Report.

## Files Changed

- `cockpit/src/lib/proposal/zod-schemas.ts` — `PROPOSAL_TAX_RATES` von `[0,7,19]` auf `[0,7,9,19,21]` erweitert; `proposalEditSchema.tax_rate` Union um literale 9 + 21; `reverse_charge: z.boolean().optional()` ergaenzt. Begruendung: Editor + Server-Action muessen die neuen NL-Saetze + RC-Flag akzeptieren, sonst zod blockt jeden Save.
- `cockpit/src/app/(app)/proposals/actions.ts` —
  - `Proposal.reverse_charge: boolean` ergaenzt (DEC-126).
  - `ProposalEditPayload.branding`: `vat_id`, `business_country` ergaenzt (MT-3-Datenfluss).
  - `ProposalEditPayload.company`: `vat_id`, `address_country` ergaenzt (MT-4-Datenfluss).
  - `ProposalUpdatePatch.tax_rate` von `0|7|19` auf `0|7|9|19|21`; `reverse_charge?: boolean` ergaenzt.
  - `AUDIT_RELEVANT_FIELDS` um `reverse_charge` erweitert (Vorbereitung MT-7-Audit-Insert).
  - `before`-Select um `reverse_charge` erweitert (Audit-Diff-Quelle).
  - `getProposalForEdit`: branding-Select + companies-Select um neue Spalten erweitert.
  - `createProposal`: liest `branding_settings.business_country` und setzt Default-tax_rate `19.0` fuer DE bzw. `21.0` fuer NL (DEC-128).
- `cockpit/src/app/(app)/proposals/[id]/edit/proposal-workspace.tsx` — `EditorPatch.tax_rate` widening + `reverse_charge?: boolean` ergaenzt; `editorSlot` bekommt `branding={payload.branding}` als zusaetzlichen Prop.
- `cockpit/src/app/(app)/proposals/[id]/edit/proposal-editor.tsx` —
  - Imports `TaxRateDropdown`, `ReverseChargeSection`, `useReverseChargeEligibility`.
  - `EditorTaxRate`-Type, `EditorPatch.tax_rate` widening + `reverse_charge` ergaenzt.
  - Neuer Prop `branding: ProposalEditPayload["branding"]`.
  - Default-Fallback `taxRateFallback` haengt von `branding.business_country` ab.
  - `lastUserTaxRateRef` merkt vorletzten User-Wert fuer Toggle-OFF-Restore.
  - `useReverseChargeEligibility(...)` ruft Hook live auf den Editor-Props.
  - `handleTaxRateChange` + `handleReverseChargeToggle` als Callbacks.
  - Inline-`<select>` durch `<TaxRateDropdown>` ersetzt; `<ReverseChargeSection>` direkt unter Steuersatz/Gueltig-bis-Grid eingefuegt.
- `cockpit/src/lib/pdf/proposal-renderer.test.ts` — Test-Fixture-Proposal um `reverse_charge: false` ergaenzt (Type-Compatibility nach Proposal-Erweiterung).
- `slices/SLC-571-nl-vat-reverse-charge.md` — MT-5 + MT-6 markiert als DONE 2026-05-04 mit Verification-Details.
- `docs/STATE.md` — Current Focus, Current Phase, Immediate Next Steps + Active Scope um MT-5+MT-6-DONE-State aktualisiert; naechste Schritte = Coolify-Redeploy + /qa MT-5+MT-6 oder /backend MT-7.

## Files Intentionally Not Changed

- `cockpit/src/app/(app)/proposals/actions.ts` — `updateProposal`-Reject-Pfade (Cross-Field-Validation reverse_charge⇄tax_rate, branding.vatId, company.vat_id, country in EU): bewusst MT-7-Scope. Aktuell uebernimmt der DB-CHECK `proposals_reverse_charge_consistency` die Konsistenz-Pruefung; Editor sendet bereits konsistente Patches. Server-Action-Validation mit aussagekraeftigen Fehlermeldungen + Audit-Insert kommt in MT-7.
- `cockpit/src/lib/pdf/proposal-renderer.ts` + `cockpit/src/lib/pdf/reverse-charge-block.ts` — MT-8-Scope. PDF-Renderer rendert aktuell `reverse_charge=true`-Proposals ohne den bilingualen Block — wird in MT-8 nachgeliefert.
- `cockpit/src/app/(app)/proposals/actions.ts:974` — zweite `branding_settings`-Select-Stelle (PDF-Generation-Pfad) noch ohne `vat_id`/`business_country`. MT-8 erweitert das, da der PDF-Renderer dort ansetzt.
- `cockpit/src/app/(app)/proposals/[id]/edit/page.tsx` — Workspace-Page-Loader unangetastet, weil `getProposalForEdit` bereits den erweiterten Payload liefert.

## Problems Found

- Lint im Gesamtprojekt zeigt 221 Pre-existing Errors/Warnings (any-Types, unused vars). Keiner davon in den von MT-5+MT-6 beruehrten Dateien — getargeteter Lint auf alle 8 geaenderten/neuen Files ist clean. Pre-existing Issues sind nicht Teil dieses Slice.
- TSC-Build hatte initial 2 Fehler (Type-narrowing `||`-Operator beim Toggle-OFF-Restore + fehlendes `reverse_charge` in `proposal-renderer.test.ts`-Fixture) — beide gefixt, finaler `tsc --noEmit` clean.
- ESLint react/no-unescaped-entities Error fuer doppelte Anfuehrungszeichen im Aktiv-Hinweis-Text — gefixt mit `&quot;`-Encoding.
- Quick-Link `companyEditHref` zeigt aktuell auf `/companies` (Listing-Seite) statt direkt auf `/companies/[id]`-Edit-Modus; das Cockpit hat keinen direkten Edit-Deep-Link, der Listing-Eintrag mit Sheet ist der existierende Edit-Pfad. Spaetere Verbesserung waere ein Deep-Link auf das Sheet vorbei am Listing.
- Build-Output zeigt eine Warnung `[insights] Failed to load pending insights: TypeError: fetch failed` waehrend Static-Generation — nicht durch MT-5+MT-6 ausgeloest, allgemeines Build-Time-Verhalten ohne Insights-Endpoint-Connectivity.

## Open Points or Deferred Decisions

- **Browser-Smoke MT-5+MT-6 offen** — User muss vorher Coolify redeployen, dann /qa SLC-571 mit den Editor-Smoke-Permutationen aus dem Slice-QA-Focus.
- **MT-7 saveProposal-Validation noch nicht implementiert** — DB-CHECK greift, aber aussagekraeftige Server-Fehlermeldungen + Audit-Insert (`action='reverse_charge_toggled'`) fehlen.
- **MT-8 PDF-Renderer fehlt** — `reverse_charge=true`-Angebote wuerden aktuell ohne bilingualen Block + ohne Strategaize-vat_id-Footer rendern.
- **Mobile-Tab-Layout der Reverse-Charge-Section** noch nicht im Browser auf Mobile geprueft — Tooltip-Liste ist als list-disc gerendert, sollte auf Mobile lesbar sein, aber visuelle Verifikation steht aus.
- **`createProposal`-Default-Fallback** bei fehlendem `branding_settings`-Row → 21% (NL-Default). Konsequenz: Wenn User noch nie Branding angelegt hat, kommt das Angebot mit 21% raus. Akzeptabel, weil DB-Default fuer business_country='NL' ist und Internal-Test-Mode ohnehin aktiv.

## Project Record Updates

- `slices/SLC-571-nl-vat-reverse-charge.md` — MT-5 + MT-6 als DONE markiert mit Verification-Details.
- `docs/STATE.md` — Current Focus, Current Phase, Immediate Next Steps + Active Scope aktualisiert.
- `slices/INDEX.md` — SLC-571 bleibt `in_progress` (3/9 MTs sind Backend, 6/9 sind code-complete; 3 MTs offen).
- `features/INDEX.md` — FEAT-571 bleibt `in_progress`.
- `planning/backlog.json` — BL-417 bleibt `in_progress` (Slice ist nicht complete).
- `docs/KNOWN_ISSUES.md` — keine Aenderungen, keine neuen Issues.

## Recommended Next Step

1. **User: Coolify-Redeploy Business System** mit aktuellem main-Code-Stand.
2. Direkt im Anschluss `/qa SLC-571` mit Fokus auf MT-3+MT-4+MT-5+MT-6 (alle UI-Permutationen aus dem Editor-Smoke + Settings-Smoke + Company-Smoke des Slice-QA-Focus). MT-1+MT-2 sind via RPT-291+292 bereits live-verifiziert.
3. Nach /qa-PASS: `/backend SLC-571 MT-7` (saveProposal Server-Action-Validation + Audit-Log) — kann gut parallel zur Browser-Smoke-Auswertung laufen, da MT-7 unabhaengig von der UI ist.

Alternative-Routing: Wenn /qa-Befund auf UI-Issue zeigt, erst Fix in MT-6, dann /qa-Re-Test, dann MT-7.
