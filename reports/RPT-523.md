---
id: RPT-523
date: "2026-05-23"
skill: /frontend
slice: SLC-843
feature: FEAT-824
type: completion
status: completed
result: "SLC-843 Public-Route code-side DONE. 2 neue Files + 2 PATCHes. TSC clean, ESLint clean, Pattern-Reuse 1:1 V8.2-LegalPageShell + V8.3-help-content + V8.2-renderLegalMarkdown. AC1-AC4 PASS, AC5-AC8 Live-Smokes deferred bis Master-Merge SLC-847."
title: "SLC-843 /frontend — Public-Route /p/[tenant-slug]/datenschutz"
---

## Outcome summary

**SLC-843 code-side DONE.** 3 MTs erfolgreich. AC1-AC4 (code-side) alle PASS. AC5-AC8 (Live-Smokes via curl + Browser) deferred bis SLC-847-Master-Merge + Coolify-Redeploy (User-Direktive `feedback_no_coolify_branch_switch_ever` + `feedback_slice_merge_at_end`).

Verification confidence: **partial** — Code-Side ist verifiziert (TSC + ESLint clean + Pattern-Reuse compliant), Runtime/Browser-Live-Smokes sind defer-pending.

## Files reviewed

- `/slices/SLC-843-customer-dse-public-route.md` — Slice-Spec
- `/cockpit/src/app/datenschutz/page.tsx` — V8.2-Pattern-Quelle (Plattform-DSE Server-Component)
- `/cockpit/src/components/layout/legal-page-shell.tsx` — V8.2 Shell-Pattern
- `/cockpit/src/components/layout/legal-footer.tsx` — Reuse Footer
- `/cockpit/src/lib/legal/markdown.ts` — V8.2 `renderLegalMarkdown`
- `/cockpit/src/lib/supabase/admin.ts` — `createAdminClient` Pattern
- `/cockpit/src/lib/supabase/middleware.ts` — `publicPaths`-Array Position (Zeile 48)
- `/cockpit/src/app/globals.css` — `.legal-content` + `.help-content` Schichten als CSS-Reuse-Basis (Zeile 281-369 + 374-)
- `/cockpit/src/lib/team/reserved-slugs.ts` — `isReservedSlug()` aus SLC-842

## Files changed (3)

- `/cockpit/src/app/globals.css` — neue `.customer-dse-content` Schicht (~95 Zeilen) eingefuegt zwischen `.legal-content em` und `.help-content` mit Selectors fuer h1/h2/h3/p/ul/ol/li/a/table/th/td/blockquote/code/hr/strong/em. Pattern 1:1 aus `.legal-content` (User-Direktive: eigene Schicht parallel, unabhaengig polierbar).
- `/cockpit/src/lib/supabase/middleware.ts` — `publicPaths` Array um `"/p/"` ergaenzt (IMP-736-Pflicht: vor Deploy in Whitelist, sonst Build-Static-Prerender greift nicht durch und Auth-Wand zeigt 307 → /login).
- `/slices/INDEX.md` — SLC-843 Status `planned` → `done`.
- `/docs/STATE.md` — Current Focus + Phase + Next Steps auf SLC-843 code-side DONE + naechster Schritt /qa SLC-843.

## Files created (3)

- `/cockpit/src/app/p/[tenant-slug]/datenschutz/page.tsx` (50 Zeilen) — Server-Component fuer `/p/[tenant-slug]/datenschutz`. Liest `params.tenant-slug`, checkt `isReservedSlug()` → notFound(), `createAdminClient().from("teams").select("id,name").ilike("slug",$).maybeSingle()` → notFound() bei null, `.from("legal_documents").select("content_md").eq("tenant_team_id", team.id).eq("kind", "customer-dse").maybeSingle()` → notFound() bei null, `renderLegalMarkdown(content_md)` → rendert `<CustomerDsePageShell html tenantName={team.name} />`. Reuse: createAdminClient + renderLegalMarkdown + isReservedSlug.
- `/cockpit/src/components/layout/customer-dse-page-shell.tsx` (32 Zeilen) — Shell-Komponente analog `LegalPageShell` mit zusaetzlichem Tenant-Name-Header `<h1>Datenschutzerklaerung — {tenantName}</h1>` ueber dem rendered HTML. Reuse: `LegalFooter`.
- `/reports/RPT-523.md` — diese Completion-Report.

## Files intentionally not changed

- `/cockpit/src/app/datenschutz/page.tsx` (V8.2 Plattform-DSE) — Plattform-eigener Pfad, nicht Tenant-Customer-DSE. Bleibt unveraendert.
- `/cockpit/src/components/layout/legal-page-shell.tsx` (V8.2) — gemeinsame Shell fuer Plattform-DSE + Impressum, nicht ueberschrieben. Eigene Tenant-Shell `customer-dse-page-shell.tsx` neben dran (User-Direktive: parallele Schicht).
- `/cockpit/src/lib/legal/markdown.ts` (V8.2 renderLegalMarkdown) — 1:1 Reuse, nicht angefasst (Strategaize-Standard remark@15 + remark-html@16 + remark-gfm@4 bleibt).
- `/cockpit/src/lib/supabase/admin.ts` — Reuse, nicht angefasst.
- `/cockpit/src/components/layout/legal-footer.tsx` — Reuse im CustomerDsePageShell, nicht angefasst.
- `/planning/backlog.json` BL-488 — bleibt `in_progress`.

## UI states covered

- **Empty/404 (Reserved-Slug):** `isReservedSlug()` → `notFound()` triggert Next.js 404-Page.
- **Empty/404 (Unknown Slug):** `teams.maybeSingle()` null → `notFound()`.
- **Empty/404 (Missing Document):** `legal_documents.maybeSingle()` null → `notFound()`.
- **Success/Render:** `renderLegalMarkdown(content_md)` → `<CustomerDsePageShell />` mit `<h1>Tenant-Name</h1>` Header + Markdown-HTML + LegalFooter.

States DIRECTLY VERIFIED:
- Code-Pfade (alle 3 notFound-Branches sichtbar via Code-Inspection)
- TypeScript-Types (PageProps Promise-Params Next.js 15-Pattern, korrekte Types fuer Supabase-Queries)

States NICHT direkt verifiziert:
- Browser-Render von Markdown (Auftragsverarbeiter-Tabelle, h1>h2>h3-Hierarchie, Links, Code-Blocks) — pending User-Deploy AC8.
- HTTP-Status-Codes (200/404 via curl) — pending User-Deploy AC5/AC6/AC7.
- CSS-Effekt `.customer-dse-content` auf rendered HTML — pending User-Deploy AC8 (Visual).
- Middleware-Whitelist `/p/` Effekt (kein 307 zu `/login`) — pending User-Deploy AC5.

## Problems found

Keine Code-Side-Problems. Hinweise:

- **Verifikations-Gap:** AC5-AC8 sind Live-Smokes via curl + Browser. Da User per `feedback_no_coolify_branch_switch_ever` Coolify nicht auf den Slice-Branch zeigen laesst (Branch-Switch verboten) UND per `feedback_slice_merge_at_end` Master-Merge erst am Slice-Ende SLC-847 erfolgt, sind Live-Smokes erst nach SLC-847 verifizierbar. Code-Side TSC + ESLint clean ist Pre-Deploy-Confidence, aber kein Run-Time-Proof.
- **Markdown-Render-Confidence:** `renderLegalMarkdown` ist seit V8.2 produktiv (REL-036). `.customer-dse-content` CSS-Schicht ist 1:1-Klon der bereits live `.legal-content`-Schicht. Beide Faktoren machen das Run-Time-Verhalten hoch-wahrscheinlich, aber nicht 100% verifiziert.

## Open points or deferred decisions

- **Live-Smokes AC5-AC8** — defer bis SLC-847 Master-Merge + User-Coolify-Redeploy.
- **Rate-Limit auf Public-Route** — V2-Iteration (Spec OUT). V1 ohne explicit Cache-Control oder Rate-Limit.
- **Platzhalter-Auto-Replacement** (`{{tenant_name}}` etc.) — V1 NICHT implementiert (Spec OUT). Tenant-Admin muss via Editor (SLC-844) befuellen. V1 Public-Route zeigt Platzhalter im Klartext (Internal-Test-Mode + ENTWURF-Banner deckt es ab).
- **Cache-Control-Header** — V1 ohne explicit Header (Next.js default).
- **Tenant-Name-Header im Shell** — fuer V1 mit 1 Tenant rein semantisch. Falls Multi-Tenant-Zukunft den Header anders darstellen soll: Shell-Component anpassbar.

## Runtime verification summary

- **Dev-Server:** nicht gestartet (Code-Side-only).
- **TSC:** clean fuer alle 3 SLC-843-Files (page.tsx, customer-dse-page-shell.tsx, middleware.ts-Patch). 8 pre-existing Errors bestehen (vies-client.test 5x, skonto-revert.test 1x, reverse-charge-revert.test 1x, useVoiceCapture.test 1x — alle unrelated).
- **ESLint:** clean (0 Findings) fuer alle 3 SLC-843-Files.
- **Build (Next.js Turbopack):** nicht ausgefuehrt (Code-Side-only).
- **Browser-Smoke:** nicht ausgefuehrt (defer bis SLC-847).
- **Persistence-/Interaction-Smoke:** N/A (Pure Server-Component, kein Client-State).

Verification basis:
- Code inspection ✅
- TypeScript compile ✅
- ESLint static check ✅
- Build ❌ (defer)
- Browser rendering ❌ (defer bis SLC-847)
- HTTP-Status-Codes ❌ (defer bis SLC-847)
- Visual CSS ❌ (defer bis SLC-847)

## Recommended next step

`/qa SLC-843` als Skill (User-Direktive `feedback_qa_skill_not_inline` + `feedback_qa_after_every_slice`).

Verifikations-Schwerpunkte fuer /qa:
- TSC re-run + Lint clean.
- Stub-Detection auf page.tsx + customer-dse-page-shell.tsx.
- Pattern-Reuse-Compliance (LegalPageShell-Pattern → CustomerDsePageShell, renderLegalMarkdown-Reuse, createAdminClient-Reuse).
- Middleware-publicPaths-Patch IMP-736-konform.
- Code-Pfad-Walkthrough (3 notFound-Branches sichtbar).
- Live-Smoke-Status klar markieren als deferred-bis-SLC-847.
