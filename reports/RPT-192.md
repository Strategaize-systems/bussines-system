---
id: RPT-192
date: "2026-04-22"
skill: /qa
slice: SLC-511
feature: FEAT-511,FEAT-512,FEAT-513,FEAT-514
type: completion
status: completed
result: "QA PASS — alle 8 ACs erfuellt, 1 Bug gefunden und sofort gefixt"
title: "QA SLC-511 — Schema-Migration + Types (calls)"
---

## QA Scope

SLC-511: calls-Tabelle, TypeScript Types, Server Actions, Supabase Storage Bucket.

## Result: PASS

Alle 8 Acceptance Criteria erfuellt. 1 Bug gefunden (Medium) und sofort gefixt.

## Schema-Verifikation (Hetzner DB)

| Pruefpunkt | Ergebnis |
|---|---|
| Spalten-Anzahl | 24 Spalten — korrekt |
| Spalten-Typen | UUID, TEXT, TIMESTAMPTZ, INT, JSONB, BOOLEAN — alle korrekt |
| NOT NULL Constraints | id, direction, status — korrekt |
| Defaults | direction='outbound', status='initiated', recording_status='not_recording', transcript_status='pending', summary_status='pending', voice_agent_handled=false, created_at=now(), updated_at=now() — alle korrekt |
| PK | calls_pkey (btree, id) — korrekt |
| FK constraints | calls_deal_id_fkey (ON DELETE SET NULL), calls_contact_id_fkey (ON DELETE SET NULL), calls_created_by_fkey (profiles) — alle 3 korrekt |
| Custom Indexes | idx_calls_deal (partial), idx_calls_contact (partial), idx_calls_status, idx_calls_recording (partial), idx_calls_direction, idx_calls_created (DESC) — alle 6 korrekt |
| RLS | ENABLED — korrekt |
| Policy | authenticated_full_access (PERMISSIVE, ALL, {authenticated}) — korrekt |
| GRANTs | authenticated + service_role — korrekt |

## Storage Bucket Verifikation

| Pruefpunkt | Ergebnis |
|---|---|
| Bucket ID | call-recordings — korrekt |
| Public | false — korrekt |
| File size limit | 524288000 (500 MB) — korrekt |
| Allowed MIME types | audio/wav, audio/wave, audio/x-wav, audio/mpeg, audio/ogg, audio/webm — korrekt |
| Storage Policies | 4 Policies: auth_select, auth_insert, auth_delete (authenticated), service_all (service_role) — korrekt |

## TypeScript Code Review

| Pruefpunkt | Ergebnis |
|---|---|
| "use server" Directive | Vorhanden — korrekt |
| Import Pattern | createClient from @/lib/supabase/server — konsistent mit meetings |
| Call Type | 22 Felder + 2 Relations — matched DB-Schema |
| Status Types | CallDirection, CallStatus, RecordingStatus, TranscriptStatus, SummaryStatus, VoiceAgentClassification — alle korrekt |
| CallAiSummary Type | outcome, action_items[], next_step, key_topics[] — konsistent mit Architektur |
| createCall | Auth-Check, Insert mit korrekten Feldern, revalidatePath — korrekt |
| updateCallStatus | Dynamische Updates, extra-Parameter fuer Zeitstempel/Recording — korrekt |
| getSipConfig | Auth-Check BEVOR Credentials zurueckgegeben werden — sicherheitskonform |
| getCallsByDeal | Select mit contacts + deals Join, sortiert DESC — korrekt (nach Fix) |
| getCallsByContact | Select mit contacts Join, sortiert DESC — korrekt |
| getCallById | Single-Select mit contacts + deals Join — korrekt |
| getRecentCalls | Select mit Limit, sortiert DESC — korrekt |

## Stub Detection

| Pattern | Ergebnis |
|---|---|
| TODO/FIXME/HACK | Keine gefunden |
| Placeholder returns | Keine gefunden |
| Empty handlers | Keine gefunden |
| console.log/debugger | Keine gefunden |
| Hardcoded credentials | Keine (ENV-Fallbacks sind akzeptabel) |
| Commented-out code | Keines |
| Dummy data | Keines |

## Findings

### Medium (gefixt)
- **Bug: falsche companies-Relation in getCallsByDeal** — `companies:contacts(company_id)` war als Supabase-Alias-Syntax falsch. calls hat keinen direkten FK zu companies (anders als meetings). Gefixt: companies-Relation entfernt, stattdessen korrekte deals(id, title) Join. companies-Feld aus Call-Type entfernt. Commit: bf3a9ba.

### Low
- **Keine Audit-Logging bei createCall/updateCallStatus** — meetings/actions.ts loggt Audit-Events. Fuer calls noch nicht implementiert. Kann in SLC-514 (Recording-Pipeline) oder spaeter ergaenzt werden. Kein Blocker fuer V5.1 Internal Testing.

## Acceptance Criteria Abgleich

| AC | Beschreibung | Status |
|---|---|---|
| AC1 | calls-Tabelle mit allen Spalten | PASS — 24 Spalten verifiziert |
| AC2 | 6 Indexes angelegt | PASS — 6 Custom + 1 PK verifiziert |
| AC3 | RLS authenticated + service_role | PASS — Policy + GRANTs verifiziert |
| AC4 | TypeScript Call-Type definiert | PASS — kompiliert fehlerfrei |
| AC5 | Storage Bucket call-recordings | PASS — Bucket + 4 Policies verifiziert |
| AC6 | createCall Server Action | PASS — implementiert + kompiliert |
| AC7 | updateCallStatus Server Action | PASS — implementiert + kompiliert |
| AC8 | getCallsByDeal sortiert | PASS — implementiert + kompiliert |

## Readiness Assessment

SLC-511 ist **ready for next slice**. Schema, Types und Server Actions sind vollstaendig und korrekt. Die calls-Tabelle ist die Basis fuer alle V5.1-Folge-Slices. Kein Blocker.

## Recommended Next Step

`/backend SLC-512` — Asterisk Docker-Setup + Traefik-WSS
