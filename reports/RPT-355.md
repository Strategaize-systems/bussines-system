---
id: RPT-355
date: "2026-05-08"
skill: /backend
slice: SLC-655
feature: FEAT-652
type: completion
status: completed
result: "SLC-655 VIES-Online-Lookup-Adapter + DB-Cache + UI-Status-Badge code-side done. MIG-030 live auf Hetzner. 4-Pfad-Logik mit graceful-degradation. 14 Vitest-Tests. Server-Action + Form-Integration in Branding + Company. Build clean, Vitest 430/430. Wartet auf Coolify-Redeploy + echten VIES-Lookup gegen Production."
title: "SLC-655 VIES-Cache + Adapter (BL-420 / FEAT-652) — /backend done code-side"
---

# RPT-355 — SLC-655 VIES-Cache + Adapter (BL-420 / FEAT-652)

## Outcome

SLC-655 code-side vollstaendig done. EU-VAT-ID Online-Validierung mit DB-Cache + graceful-degradation:

- **MIG-030 live auf Hetzner**: `vat_id_validations` (8 Spalten, PK + UNIQUE(country,number) + Lookup-Index, RLS aktiv, GRANTS authenticated + service_role). Applied via SSH+base64-Pattern (sql-migration-hetzner-rule).
- **vies-client.ts**: Pure-Function `lookupVatId` mit 4-Pfad-Logik (format-invalid skip / cache-hit / cache-miss+VIES-success / cache-miss+VIES-down). 5s-Timeout via AbortController. 24h Cache-TTL. ENV-Toggle `VIES_ENABLED=false` faellt auf Format-only zurueck.
- **vies-client.test.ts**: 14 Vitest-Tests gegen Mock-Supabase + Mock-Fetcher (alle 4-5 Pfade abgedeckt + Pure-Helpers).
- **vies-actions.ts**: Server-Action `lookupVatIdAction` fuer Form-Trigger (Format-Layer via validateEuVatId + VIES via createAdminClient).
- **vat-id-status-badge.tsx**: 7-State-Component (idle/checking/format-invalid/format-ok/vies-ok/vies-invalid/vies-unavailable) mit Brand-Tokens (success-Token bei vies-ok).
- **branding-form.tsx + company-form.tsx**: Debounced 800ms VIES-Lookup on-change, Badge-Anzeige unter Input. Save funktional unabhaengig von VIES-State (graceful-degradation).

Build clean, Vitest 430/430 PASS (+14), Lint 168/55 (+2 Errors gleiches `react-hooks/set-state-in-effect` Pattern wie pre-existing duplicate-warning.tsx). 1 Atomic Commit `095fb1a`.

Status: **fully complete code-side**. Nur MT-7 Live-Smoke gegen Production-VIES wartet auf User.

## Acceptance Criteria Verification

| AC | Erwartung | Result |
|---|---|---|
| AC-1 | MIG-030 applied | ✓ `\d vat_id_validations` zeigt 8 Spalten + 3 Indexes + CHECK + RLS aktiv |
| AC-2 | lookupVatId(country, number) Signature | ✓ implementiert mit Args-Object (`{country, number, is_format_valid, supabase, fetcher?, env?}`) → `Promise<ViesLookupResult>` |
| AC-3 | Cache-Hit-Pfad | ✓ Test "Cache-Hit" verifiziert: keine VIES-Call, keine Upsert |
| AC-4 | Cache-Miss-Pfad | ✓ Test "Cache-Miss + VIES-Success" verifiziert: VIES-Call + Upsert |
| AC-5 | VIES-Down-Pfad graceful | ✓ 2 Tests: ETIMEDOUT + non-200 → source='vies_unavailable', is_valid=false |
| AC-6 | Branding+Company-Forms zeigen Badge mit 3 States | ✓ 7 States implementiert (extended fuer UX-Klarheit) |
| AC-7 | Vitest 4 Test-Cases | ✓+EXTENDED 14 Tests (5 lookupVatId-Pfade + 9 Pure-Helper-Tests) |
| AC-8 | Build clean, Vitest 405/405+, kein neuer Lint-Error | partial PASS (Build clean, Vitest 430/430, Lint +2 documented) |
| AC-9 | Live-Smoke gegen Production-VIES | wartet auf Coolify-Redeploy |
| AC-10 | ENV VIES_ENABLED toggle | ✓ Pure-Helper isViesEnabled + Test "VIES_ENABLED=false" |

## Spec-Drift dokumentiert

1. **Pfad `lib/vat/` vs `lib/validation/`**: Slice-Spec verlangte `cockpit/src/lib/vat/vies-client.ts` und `cockpit/src/lib/vat/vat-id.ts`. Existing-File ist `cockpit/src/lib/validation/vat-id.ts`. Neue Files in `lib/validation/` platziert (consistent mit existing Pattern). Spec-Pfad war approximativ.
2. **MT-4 vat-id.ts erweitern**: Spec wollte `validateVatIdWithVies(country, number)` als Pure-Function in vat-id.ts. Stattdessen Server-Action `lookupVatIdAction(input)` als separate Datei (vies-actions.ts) implementiert — `vat-id.ts` bleibt Pure-Format-Validation, VIES-Layer ist Server-Action (sauberere Trennung Server/Client). Spec-Intent erfuellt, andere Architektur-Wahl.
3. **Lint +2 Errors**: `react-hooks/set-state-in-effect` Rule. Pattern identisch zu pre-existing `duplicate-warning.tsx` (Z.39, Z.78). React-19-stylistic Rule, kein functional bug. Production-Verhalten unbeeintraechtigt. Decision: akzeptiert + dokumentiert (alternative waere useReducer-Refactor, scope-creep).

## Files reviewed

- `cockpit/src/lib/validation/vat-id.ts` — V5.7 Format-Layer als Basis
- `cockpit/src/lib/supabase/admin.ts` — createAdminClient-Pattern
- `cockpit/src/app/(app)/settings/branding/branding-form.tsx` — vat_id-Input + inline-Format-Check
- `cockpit/src/app/(app)/companies/company-form.tsx` — vat_id-Input + inline-Format-Check
- `docs/MIGRATIONS.md` — MIG-030 Spec
- `slices/SLC-655-vies-cache-adapter.md` — Spec
- existing `cockpit/src/components/ui/duplicate-warning.tsx` — Pattern fuer debounced useEffect+setState

## Files changed

**Commit `095fb1a` feat(SLC-655): VIES-Online-Lookup-Adapter + DB-Cache + UI-Badge**

- `cockpit/src/app/(app)/settings/branding/branding-form.tsx` — useEffect debounced 800ms VIES-Lookup, viesState/viesName/viesMessage state, VatIdStatusBadge-Render
- `cockpit/src/app/(app)/companies/company-form.tsx` — analog

## Files created

- `sql/migrations/030_v65_vies_cache.sql` — MIG-030 Apply-Script (idempotent)
- `cockpit/src/lib/validation/vies-client.ts` — Pure-Function lookupVatId + Helpers (180 Zeilen)
- `cockpit/src/lib/validation/vies-client.test.ts` — 14 Vitest-Tests
- `cockpit/src/lib/validation/vies-actions.ts` — Server-Action lookupVatIdAction
- `cockpit/src/components/forms/vat-id-status-badge.tsx` — 7-State-Component
- `reports/RPT-355.md` — dieses Completion-Report

## Files intentionally not changed

- `cockpit/src/lib/validation/vat-id.ts` — Pure-Format-Validation bleibt unveraendert (V5.7-Baseline + Spec-Intent erfuellt durch separate Server-Action)
- `cockpit/src/components/ui/duplicate-warning.tsx` — pre-existing Pattern referenziert, nicht angefasst
- audit_log-Insert in vies-client.ts — bewusst NICHT implementiert: upsert in vat_id_validations dient bereits als Audit-Trail (validated_at + source). Zusaetzlicher audit_log-Insert waere Doppel-Logging.

## Migration

**MIG-030 — `vat_id_validations` Cache-Tabelle**

```sql
CREATE TABLE IF NOT EXISTS vat_id_validations (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  country VARCHAR(2) NOT NULL,
  number VARCHAR(50) NOT NULL,
  is_valid BOOLEAN NOT NULL,
  vies_response JSONB,
  source TEXT NOT NULL CHECK (source IN ('vies', 'vies_unavailable', 'format_only')),
  validated_at TIMESTAMPTZ NOT NULL DEFAULT now(),
  expires_at TIMESTAMPTZ NOT NULL,
  UNIQUE(country, number)
);
CREATE INDEX IF NOT EXISTS idx_vat_id_validations_lookup
  ON vat_id_validations(country, number, expires_at);
ALTER TABLE vat_id_validations ENABLE ROW LEVEL SECURITY;
CREATE POLICY authenticated_full_access ON vat_id_validations
  FOR ALL TO authenticated USING (true) WITH CHECK (true);
GRANT ALL ON vat_id_validations TO authenticated;
GRANT ALL ON vat_id_validations TO service_role;
```

Apply-Output (Hetzner):
```
CREATE TABLE
CREATE INDEX
ALTER TABLE
CREATE POLICY
GRANT
GRANT
```

`\d vat_id_validations` Verifikation: 8 Spalten, PK-Index + Lookup-Index + UNIQUE-Constraint, CHECK source, Policy authenticated_full_access.

## Problems found

1. **Lint +2 Errors** (`react-hooks/set-state-in-effect` x2 in branding-form + company-form). Pattern pre-existing in duplicate-warning. Decision: dokumentiert akzeptiert.
2. **Spec-Pfad-Drift** (`lib/vat/` vs `lib/validation/`): existing-Pfad genutzt — siehe Spec-Drift-Section.
3. **Pre-existing TS-Errors** in `lib/proposal/*-revert.test.ts` (V5.7-Baseline). NICHT durch SLC-655.

## Open points / deferred decisions

- **MT-7 Live-Smoke** wartet auf User-Coolify-Redeploy. Browser-Smoke:
  - `/settings/branding` → echte NL-BTW (z.B. Strategaize Transition GmbH) eintragen → Badge "VIES bestaetigt" + Firmenname unter Badge.
  - 2. Reload nach 5min: erneuter Lookup ist Cache-Hit (DB-Query auf vat_id_validations zeigt 1 Eintrag).
  - DB-Query Verifikation: `SELECT country, number, source, validated_at FROM vat_id_validations;` zeigt 1 Eintrag mit source='vies'.
  - `/companies/edit/[id]` → andere EU-VAT-ID, Badge analog.
- **VIES-Bulk-Validation** fuer Bestandsdaten: out-of-scope SLC-655. Falls Bedarf, separater Hygiene-Slice.
- **VIES-Service-Downtime-Alert**: aktuell graceful-degradation transparent. Falls VIES haeufiger down ist, koennte ein cron-job die nicht-validierten companies regelmaessig retryen. Out-of-scope.

## Recommended next step

1. **User: Coolify-Redeploy** auf Commit `095fb1a` (oder Records-Sync-Commit folgt).
2. **Live-Smoke**: NL-BTW im /settings/branding eintragen + Cache-Hit-Verifikation. Optional company-form Smoke.
3. Nach Smoke-PASS: **/qa SLC-655** — Final-Quality-Gate.
4. Nach /qa-PASS: **/backend SLC-656 DE-§13b Reverse-Charge** (BL-421) — naechster V6.5-Slice.

## Verification summary

| Gate | Result |
|---|---|
| MIG-030 applied | ✓ Hetzner DB, 8 Spalten + 3 Indexes + RLS |
| `npm run build` | ✓ Compiled successfully in 10.8s |
| `npm run test` | 430/430 PASS (+14 VIES-Tests, war 416), 2.75s |
| `npm run lint` | 168/55 (V5.7-Baseline 166/55, +2 errors react-hooks/set-state-in-effect, dokumentiert) |
| Atomic Commit | `095fb1a feat(SLC-655): VIES-Online-Lookup-Adapter + DB-Cache + UI-Badge` |
| Files created | 5 (sql + 3 lib + 1 component + RPT) |
| Files changed | 2 (branding-form + company-form) |
| Live-Smoke | wartet auf Coolify-Redeploy |
