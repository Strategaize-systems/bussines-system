---
id: RPT-359
date: "2026-05-08"
skill: /backend
slice: SLC-657
feature: FEAT-653
type: completion
status: completed
result: "SLC-657 Source-Migration + npm audit + ISSUE-058 backend code-side done. Pre-Audit zeigte 0 Legacy-Source-Daten in Production → MIG-031 ist Ready-when-needed (Pfad A): Mapping-JSON + Bulk-UPDATE-SQL als Repo-Artefakt vorbereitet, kein Production-Apply. contact-form + company-form Legacy-Felder entfernt + conditional read-only-Display + hidden-input-Preserve. CSV-Export campaign_name statt source_detail. npm audit von 5 auf 2 moderate reduziert (postcss + next bleiben ISSUE-058). Build clean, Vitest 444/444. Wartet auf Coolify-Redeploy + /qa."
title: "SLC-657 Source-Migration + npm audit + ISSUE-058 (BL-424 + BL-430) — /backend done code-side"
---

# RPT-359 — SLC-657 Source-Migration + npm audit + ISSUE-058 (BL-424 + BL-430)

## Outcome

SLC-657 backend code-side done. Hygiene-Slice in 9 MTs strukturiert; per User-Entscheidung Pfad A (Skripte ready-when-needed) gewaehlt nach Pre-Audit-Befund.

**Pre-Audit-Befund (MT-1):** Production-DB hat 0 Legacy-Source-Daten (1 contact + 1 company total, 0 campaigns, 0 mit `source`/`source_type`/`source_detail`). Bulk-Migration MIG-031 ist inhaltlich No-Op fuer aktuellen Daten-Stand.

**User-Entscheidung Pfad A:** Skripte trotzdem erstellen + commit als Repo-Artefakt fuer spaetere Anwendung (sobald produktive Daten anfallen). Mapping-JSON ist `[]` mit Schema-Doku, Bulk-UPDATE-SQL ist idempotent + audit-log-aware + nicht ausgefuehrt auf Hetzner.

**Code-Aenderungen:**

- **MIG-031 Skripte (3 Files + 1 README)**: `031_v65_pre_audit.sql` read-only Audit, `031_v65_source_to_campaign_mapping.json` leer, `031_v65_bulk_update.sql` idempotent + audit-log, `031_v65_source_to_campaign_mapping.README.md` Schema + Apply-Anweisung.
- **Forms read-only**: `contacts/contact-form.tsx` + `companies/company-form.tsx` — Legacy-Source-Dropdown + Source-Detail-Input entfernt. Conditional read-only-`<dl>`-Display nur wenn Legacy-Werte nicht-leer (DEC-160). Hidden Inputs preserven die Werte ueber Form-Updates damit `actions.ts` sie nicht versehentlich auf NULL setzt.
- **CSV-Export**: `/api/campaigns/[id]/export/route.ts` — `source_detail` durch `campaign_name` ersetzt (LEFT JOIN auf campaigns). Header-Row geaendert, externe CSV-Konsumenten muessen ggf. ueber Aenderung informiert werden.
- **npm audit**: `npm audit fix` (non-force) reduziert 5 → 2 moderate Vulnerabilities. hono + ip-address + express-rate-limit gefixt. postcss + next bleiben (ISSUE-058, fix waere catastrophic Next 9.3.3-Downgrade per DEC-161).
- **ISSUE-058**: Bereits durch /architecture in `KNOWN_ISSUES.md` dokumentiert (Status=open, Severity=Low, Workaround=Upstream-warten).

Build clean, Vitest 444/444 PASS, Lint 168/55 unveraendert zur SLC-656-Baseline.

Status: **fully complete code-side**. MT-8 Live-Smoke wartet auf User-Coolify-Redeploy.

## Acceptance Criteria Verification

| AC | Erwartung | Result |
|---|---|---|
| AC-1 | `031_v65_pre_audit.sql` zeigt distinct source-Werte mit Counts | ✓ erstellt + auf Hetzner ausgefuehrt; Output zeigte 0 Legacy-Daten |
| AC-2 | User-Pause: Mapping-JSON-File | ✓ `[]` Template + README mit Schema + Apply-Anweisung |
| AC-3 | `031_v65_bulk_update.sql` idempotent + Audit-Log-Insert | ✓ erstellt mit `WHERE campaign_id IS NULL` + `IS NOT DISTINCT FROM` Match + audit_log-INSERT mit Stats; nicht auf Hetzner ausgefuehrt (No-Op) |
| AC-4 | contact-form + company-form Legacy-Felder read-only wenn nicht-leer | ✓ Conditional `<dl>`-Display + Hinweis "Legacy-Quelle, neue Eingaben via Kampagne" + hidden inputs |
| AC-5 | CSV-Export campaign_name statt source_detail (LEFT JOIN) | ✓ route.ts SELECT + Header + Row-Mapping geaendert |
| AC-6 | ISSUE-058 in KNOWN_ISSUES dokumentiert | ✓ bereits vor SLC-657 durch /architecture (Status=open, Severity=Low) |
| AC-7 | npm audit 0 high/0 critical, moderate reduziert ODER dokumentiert | ✓ 0 high/0 critical, 5→2 moderate (postcss + next als ISSUE-058 dokumentiert) |
| AC-8 | Build clean, Vitest 405/405+, kein neuer Lint-Error | ✓ Build clean, Vitest 444/444, Lint 168/55 unveraendert |
| AC-9 | Live-Smoke nach Redeploy: Forms + CSV-Export | wartet auf User-Coolify-Redeploy |

## Spec-Drift dokumentiert

1. **MT-1 No-Op-Befund**: Pre-Audit zeigte 0 Legacy-Daten + 0 Kampagnen. Spec erwartete dass User auf Basis der Audit-Ergebnisse das Mapping pflegt. User-Entscheidung Pfad A: trotzdem Skripte erstellen (Repo-Artefakt) statt Slice abkuerzen, damit MIG-031 ready ist sobald produktive Daten anfallen.
2. **MT-2 Mapping-File leer**: Statt User-Pause mit echtem Mapping ist die JSON-Datei `[]` mit README. README dokumentiert Re-Apply-Pfad fuer spaetere Anwendung.
3. **MT-3 Apply uebersprungen**: Bulk-UPDATE-SQL existiert + ist idempotent-getestet im Code, aber NICHT auf Hetzner ausgefuehrt (waere 0-Row-UPDATE). Erst-Anwendung kommt sobald Legacy-Daten vorhanden sind.
4. **MT-4 hidden-input-Preserve hinzugefuegt**: Spec sagte nur "read-only-Display wenn nicht-leer". Aber: `actions.ts` liest source/source_type + source_detail aus FormData; ohne hidden inputs waeren die Werte beim ersten Form-Update auf NULL gesetzt → Backup-Pattern (DEC-160) gebrochen. Hidden inputs preserven die Werte. Pragmatischer Fix.

## Files reviewed

- `cockpit/src/app/(app)/contacts/contact-form.tsx` — Legacy-Source-Felder
- `cockpit/src/app/(app)/companies/company-form.tsx` — Legacy-Source-Felder
- `cockpit/src/app/(app)/contacts/actions.ts` — FormData-Parsing fuer source/source_detail
- `cockpit/src/app/(app)/companies/actions.ts` — FormData-Parsing fuer source_type/source_detail
- `cockpit/src/app/api/campaigns/[id]/export/route.ts` — CSV-Export
- `docs/DECISIONS.md` — DEC-159, DEC-160, DEC-161
- `docs/MIGRATIONS.md` — MIG-031
- `docs/KNOWN_ISSUES.md` — ISSUE-058
- `slices/SLC-657-source-migration-npm-audit.md` — Spec
- `cockpit/package.json` — Pre-Audit npm dependencies

## Files changed

- `cockpit/src/app/(app)/contacts/contact-form.tsx` — Source-Dropdown + Source-Detail-Input entfernt; conditional read-only-`<dl>`-Display fuer Legacy-Werte; hidden inputs `source` + `source_detail` zur Preservation
- `cockpit/src/app/(app)/companies/company-form.tsx` — analog: Source-Type-Dropdown + Source-Detail-Input entfernt; conditional read-only-Display; hidden inputs `source_type` + `source_detail`
- `cockpit/src/app/api/campaigns/[id]/export/route.ts` — SELECT erweitert um `campaigns(name)`, ContactExportRow-Interface aktualisiert, Header `source_detail`→`campaign_name`, Row-Mapping aktualisiert
- `cockpit/package.json` + `cockpit/package-lock.json` — npm audit fix (non-force) reduzierte 5→2 moderate Vulnerabilities (hono + ip-address + express-rate-limit gefixt)
- `slices/INDEX.md` — SLC-657 status `planned` → `done`
- `planning/backlog.json` — BL-424 + BL-430 status `open` → `done`, descriptions auf SLC-657-Outcome aktualisiert
- `docs/MIGRATIONS.md` — MIG-031 Apply-History + Status="ready-when-needed" + Skript-Liste
- `docs/STATE.md` — Current Focus + Phase auf "SLC-657 backend done code-side, V6.5 7/7" + Next Steps angepasst

## Files created

- `sql/migrations/031_v65_pre_audit.sql` — Read-only Pre-Migration-Audit-SQL
- `sql/migrations/031_v65_source_to_campaign_mapping.json` — leeres Mapping-Array `[]` (User-Pflege-Datei)
- `sql/migrations/031_v65_source_to_campaign_mapping.README.md` — JSON-Schema-Doku + Apply-Anweisung
- `sql/migrations/031_v65_bulk_update.sql` — Idempotenter Bulk-UPDATE mit Audit-Log-Insert
- `reports/RPT-359.md` — dieses Backend-Completion-Report

## Files intentionally not changed

- `cockpit/src/app/(app)/contacts/actions.ts` — FormData-Parsing fuer source/source_detail bleibt, weil hidden inputs die Werte liefern. Kein Risiko von versehentlichem NULL-Set.
- `cockpit/src/app/(app)/companies/actions.ts` — analog
- `docs/KNOWN_ISSUES.md` ISSUE-058 — bereits durch /architecture vor SLC-657 dokumentiert; keine Aenderung noetig
- DB-Schema (`contacts.source`, `contacts.source_detail`, `companies.source_type`, `companies.source_detail`) — bleiben als read-only-Backup-Felder (DEC-160). KEIN DROP COLUMN.

## Migration

**MIG-031 — V6.5 Source-zu-Kampagne Bulk-Migration**: Status `ready-when-needed`. Skripte als Repo-Artefakt commited; **kein Production-Apply** weil Pre-Audit 0 Legacy-Daten zeigte. Re-Apply-Pfad in `031_v65_source_to_campaign_mapping.README.md` dokumentiert.

## Problems found

1. **Pre-Audit-No-Op-Befund**: Production-DB ist Internal-Test-Mode mit 1 contact + 1 company + 0 campaigns. Slice geht von realistischen Daten aus. Pragmatische Loesung: Pfad A (Repo-Artefakt-only) per User-Entscheidung.
2. **Form hidden-input-Preserve nicht in Slice-Spec**: Spec sagte nur "read-only-Display"; ohne hidden inputs haetten Form-Updates die Backup-Werte zerstoert (DEC-160 verletzt). Fix umgesetzt.
3. **Pre-existing Lint 168/55** unveraendert zur SLC-656-Baseline. Keine neuen Errors aus SLC-657.
4. **postcss + next moderate Vulnerabilities** bleiben (ISSUE-058). Per DEC-161 kein Force-Fix; warten auf Upstream-Next-Release.
5. **Externe CSV-Konsumenten**: CSV-Export-Spalten-Schema-Aenderung von `source_detail` auf `campaign_name`. Falls Tools die alte Spalte parsen, bricht der Parse. Aktuell: vermutlich keine externen Konsumenten in Internal-Test-Mode, aber Production-Mitigation ggf. notwendig.

## Open points / deferred decisions

- **MT-8 Live-Smoke** wartet auf User-Coolify-Redeploy. Browser-Smoke:
  - `/contacts/<id>` mit existing Contact: KEIN Legacy-Source-Display (alle Test-Daten haben source=NULL → erwartet)
  - `/companies/<id>` mit existing Company: KEIN Legacy-Source-Display (analog)
  - **Optional**: User koennte testweise einen Contact mit `source` befuellen (z.B. via SQL UPDATE) und dann das Edit-Form besuchen → read-only-Display sollte erscheinen. Nicht zwingend.
  - **CSV-Export**: kein Test moeglich da 0 Kampagnen in DB. Nur statisch verifizierbar via Build-Success.
- **MIG-031 Re-Apply-Plan**: bei produktivem Daten-Anfall (post-Pre-Production-Compliance-Gate) Pre-Audit erneut + Mapping pflegen + Bulk-UPDATE ausfuehren.
- **Externe CSV-Konsumenten-Mitigation**: aktuell keine bekannt; Mitteilung bei Bedarf.

## Recommended next step

1. **User: Coolify-Redeploy** auf den anstehenden Commit (folgt nach diesem Report).
2. **/qa SLC-657** — Final-Quality-Gate. Wegen No-Op-Daten-Stand wird AC-9 Live-Smoke partial (Forms statisch verifiziert + Build clean, Browser-Test minimal).
3. **Nach /qa SLC-657**: Gesamt-/qa V6.5 (alle 7 Slices) → /final-check → /go-live → /deploy als REL-027.

## Verification summary

| Gate | Result |
|---|---|
| `npm run build` | ✓ Compiled successfully |
| `npm run test` | 444/444 PASS |
| `npm run lint` | 168/55 unveraendert zur SLC-656-Baseline |
| `npm audit` | 0 high, 0 critical, 2 moderate (postcss + next, ISSUE-058) |
| Pre-Audit auf Hetzner | ✓ ausgefuehrt, 0 Legacy-Daten bestaetigt |
| Bulk-UPDATE auf Hetzner | bewusst NICHT ausgefuehrt (No-Op) |
| Atomic Commit | folgt nach diesem Report |
| Files created | 5 (4 SQL/JSON + 1 RPT) |
| Files changed | 8 (cockpit + records + docs) |
| Live-Smoke | wartet auf Coolify-Redeploy |
