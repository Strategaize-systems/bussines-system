---
id: RPT-679
date: "2026-07-08"
skill: /backend
slice: SLC-915
feature: FEAT-927
type: completion
status: completed
reviewOf: null
result: "MT-4 CODE-DONE: MIG-055 changed-FK-only BEFORE-UPDATE-Trigger + UPDATE-WITH-CHECK-OR über 9 Klasse-C-Tabellen. OQ-A1 live in-tx-Probe gegen Coolify-DB 8/8 PASS. Live-Apply pending /deploy."
title: "SLC-915 MT-4 — MIG-055 changed-FK-only Trigger (ISSUE-140)"
---

# SLC-915 MT-4 — MIG-055 changed-FK-only Trigger + UPDATE-WITH-CHECK-OR

## 1. Outcome summary

MT-4 (ISSUE-140 / DEC-307) ist **code-complete + live in-tx-verifiziert**. Die additive Migration MIG-055 kehrt die MIG-054-`WITH CHECK`-AND-Conjunction auf dem UPDATE-Pfad der 9 Multi-Parent-Klasse-C-Tabellen zurück auf USING-konsistentes OR (unveränderte mixed-owner-Rows passieren wieder) und ergänzt pro Tabelle einen `BEFORE UPDATE`-Trigger (SECURITY INVOKER, service_role-aware), der ausschließlich **geänderte** FK-Spalten gegen `can_see_owner` prüft. INSERT-`WITH CHECK` bleibt MIG-054-AND (strikt). Netto: identischer Cross-Tenant-FK-Injection-Schutz wie MIG-054, ohne den False-Positive-Freeze legitimer Status-Updates.

**Live-Apply ist NICHT erfolgt** — bewusst: die Verifikation lief als non-destruktive in-tx-Probe (BEGIN → apply → seed → 8 Checks → ROLLBACK) gegen die Coolify-DB; die persistente Anwendung ist /deploy-Ops-Pre-Cond (Slice-Vorgabe). Push bleibt gehalten (Bundle unvollständig — MT-5/MT-6 offen).

## 2. Files reviewed

- `slices/SLC-915-v817-regressions-fix-bundle.md` (MT-4-Block, Schema-Grounding, FK→Parent-Map)
- `docs/DECISIONS.md` DEC-307 (Founder-Ansatz a, verworfene b/c)
- `docs/ARCHITECTURE.md` V8.17-Addendum (FK→Parent-Map, OQ-A1, Datenfluss)
- `sql/migrations/054_v816_slc914_class_c_withcheck_harden.sql` (Stil-Vorlage + exakte USING-OR-Klauseln je Tabelle)
- `sql/migrations/051_v814_slc912_profiles_role_protect.sql` (P-080 service_role-aware Change-Guard-Scaffolding)
- `sql/migrations/035_v7_rls_switch.sql` (`can_see_owner`-Definition — enthält `is_admin()` → Admin-Bypass implizit)
- `cockpit/__tests__/migrations/054-v816-class-c-withcheck.test.ts` (Seed-Modell + Struktur-Marker-Konvention)
- Playbooks: `docs/playbooks/coolify-test-setup.md`, `docs/playbooks/sql-migration-hetzner.md` (Trigger via playbook-index)

## 3. Files changed

- `docs/MIGRATIONS.md` — MIG-055-Eintrag von "RESERVIERT" → "GESCHRIEBEN + in-tx-verifiziert, Live-Apply pending /deploy"; File-Pfade + OQ-A1-8/8-PASS-Ergebnis ergänzt; Date → 2026-07-08.
- `docs/KNOWN_ISSUES.md` — ISSUE-140 Next Action → TEIL-BEHOBEN (Ansatz a): MT-4 code-done + in-tx-PASS; offen bleiben MT-5-Call-Sites + Live-Apply. Status weiter `open` (Slice nicht abgeschlossen).
- `docs/STATE.md` — Current Focus: MT-4 CODE-DONE + OQ-A1-8/8-PASS-Detail; NEXT von MT-4 → MT-5.

## 4. Files created

- `sql/migrations/055_v817_slc915_class_c_changed_fk_trigger.sql` — additive Migration: 9× (UPDATE-Policy-Revert auf OR + Guard-Function + BEFORE-UPDATE-Trigger), Helper-Existenz-Guard, idempotent (DROP IF EXISTS + CREATE OR REPLACE), Rollback-Notes + Done-Gate-Queries im Header.
- `cockpit/__tests__/migrations/055-v817-class-c-changed-fk-trigger.test.ts` — node:20-Sidecar-DB-Layer-Test (assume MIG-055 applied, läuft im /deploy-Fenster): 7 Verhaltens-Cases (POS mixed-owner / NEG fk-invisible / POS fk-visible / NEG insert-strict / POS service_role / POS admin) + 2 Struktur-Cases (9 Trigger, 9 UPDATE-OR + 9 INSERT-AND).
- `reports/RPT-679.md` — dieser Report.

## 5. Files intentionally not changed

- `sql/migrations/054_*.sql` — MIG-054 ist die Rollback-Basis (MIG-055 setzt darauf auf); unangetastet.
- INSERT-Policies der 9 Tabellen — bleiben MIG-054-AND (strikt); MIG-055 fasst nur UPDATE + Trigger an.
- Die 4 Call-Sites (MT-5) und der E-Mail-Viewer (MT-6) — separate MTs, out of scope für MT-4.
- `docs/DECISIONS.md` — DEC-307 existiert bereits (accepted); kein neuer DEC nötig.

## 6. Problems found

- **Keine offenen Schema-Drifts.** FK→Parent-Map (ARCHITECTURE-Addendum) 1:1 gegen die live-abgeleiteten MIG-054-Policies verifiziert; alle 9 Tabellen + Parent-`owner_user_id` durch die in-tx-Probe implizit bestätigt (Migration applied ohne Fehler, alle Subqueries auflösbar).
- **`documents`-Policy-Präfix-Falle** korrekt berücksichtigt: Policy heißt `documents_table_update` (nicht `documents_update`) — aus MIG-054 übernommen, im Struktur-Test + Rollback abgebildet.
- **Admin-Bypass ohne expliziten `is_admin()`-Zweig im Trigger** — bewusst: `can_see_owner()` enthält `is_admin()` (MIG-035:62), daher ist der Trigger-Check für Admins stets erfüllt (CHK6 live bestätigt). Vermeidet Redundanz + hält die Semantik identisch zu MIG-054.

## 7. Verifikation (OQ-A1, live gegen Coolify-DB)

Non-destruktive in-tx-Probe (Dev-IMP-1694), `supabase-db-k9f5pn5upfq7etoefb5ukbcg-081218581096`, BEGIN → MIG-055 apply → seed (ownDeal/ownContact/foreignContact + mixed-/own-Signal) → 8 Checks → ROLLBACK:

| Check | Erwartung | Ergebnis |
|---|---|---|
| CHK1 mixed-owner Status-Update (member, kein FK geändert) | PASS (rowcount 1) | **PASS** — behebt MIG-054-Freeze |
| CHK2 member ändert FK auf unsichtbaren Parent | BLOCK insufficient_privilege | **PASS** |
| CHK3 member ändert FK auf sichtbaren Parent | PASS | **PASS** |
| CHK4 member INSERT cross-tenant-FK | BLOCK (INSERT-AND strikt) | **PASS** |
| CHK5 service_role ändert FK auf unsichtbaren Parent | PASS (Bypass) | **PASS** |
| CHK6 admin ändert FK | PASS (can_see_owner⊇is_admin) | **PASS** |
| CHK7 alle 9 Guard-Functions SECURITY INVOKER | prosecdef=false | **PASS** |
| CHK8 Struktur | 9 Trigger / 9 UPDATE-OR / 9 INSERT-AND | **PASS** |

Post-ROLLBACK-Kontroll-Query: 0 residual `%_changed_fk_guard`-Trigger auf der Live-DB → **Probe war vollständig non-destruktiv**. OQ-A1 (SECURITY INVOKER + parent-RLS im Subquery gegen Live-DB) damit aufgelöst.

Nicht ausgeführt: `npm run test` (src/**) — MT-4 berührte keine `src/**`-Datei (nur 1 SQL + 1 `__tests__/`-Test außerhalb des vitest-`include`), die src-Suite ist unverändert vom letzten grünen Baseline-Stand. Der committete DB-Layer-Test läuft im /deploy-Fenster (node:20-Sidecar).

## 8. Project record updates

- `docs/MIGRATIONS.md` (MIG-055 aktualisiert), `docs/KNOWN_ISSUES.md` (ISSUE-140 Teil-Behoben), `docs/STATE.md` (Focus + NEXT) — erledigt.
- `slices/INDEX.md` SLC-915 = `in_progress` (bereits gesetzt; Slice nicht abgeschlossen — MT-5/MT-6 offen). `planning/backlog.json` BL-520 = `in_progress` (bereits gesetzt). `roadmap.json` v8-17 = active (bereits gesetzt). Keine `done`/`deployed`/`released`-Statusänderung (Release-Gate-Defense: kein /go-live/-Report vorhanden — korrekt).

## 9. Open points / deferred

- **MT-5** — 4 Call-Sites (`mein-tag/followup-actions.ts:126`, `fit-assessment/signal-actions.ts:73`, `emails/imap-actions.ts:135`, `meetings/actions.ts:228`) Fehlerprüfung/Kompensation. Logisch nach MT-4, file-disjunkt.
- **MT-6** — E-Mail-Bild-Block + route-scoped Viewer (frische Session, OQ-A2/A3).
- **Live-Apply MIG-055** — /deploy-Ops-Pre-Cond (`sql-migration-hetzner.md`); committeter DB-Layer-Test dann gegen die persistente DB.
- **Push** bleibt gehalten bis das V8.17-Bundle vollständig ist.

## 10. Recommended next step

**Naechster Schritt: /qa** (MT-4-Verifikation gegen die AC — AC-915-5). Danach `/backend SLC-915 MT-5` (4 Call-Sites) in frischer Session, dann MT-6, dann Gesamt-/qa V8.17 → /final-check → /go-live → /deploy (MIG-055 Live-Apply + DB-Verify) → /post-launch.
