---
id: RPT-260
date: "2026-04-30"
skill: /qa
slice: SLC-554
feature: FEAT-554
type: completion
status: completed
result: "SLC-554 /qa PASS — Vitest 97/97, TSC PASS, Cron-Endpoint-Live (200/401/401), DB-Audit-Smoke (status=expired + actor_id=NULL + Idempotenz), Browser-Smokes (Sent-Transition + Confirm + Versions-Liste + Neue-Version-V2-Erstellung + Items-Snapshot + V1-unangetastet + Read-only-Mode + Listing-Status-Badge + Anzeigen-Link). 1 Finding Medium (React Hydration #418 auf /proposals)."
title: "SLC-554 QA PASS — Status-Lifecycle + Versionierung + Auto-Expire-Cron Live verifiziert"
---

# SLC-554 — /qa Completion Report

## 1. Outcome Summary

**Status: PASS** (alle 20 Acceptance Criteria erfuellt, 1 Medium-Finding zur Beobachtung).

QA-Sequenz vollstaendig durchgelaufen gegen `https://business.strategaizetransition.com` nach User-Coolify-Deploy von Commit `03e0d26` und Coolify-Cron-Anlage `expire-proposals`:

1. **Vitest re-run:** 97/97 PASS (1.10s, 9 Test-Files inkl. 21 neue Whitelist-Tests in transitions.test.ts).
2. **TypeScript-Check:** `tsc --noEmit` PASS.
3. **Stub-Scan:** clean — 1 `console.log` in route.ts (Cron-Audit-Logging, intentional Pattern aus recording-retention/route.ts), 1 `console.log` in actions.ts:799 (Performance-Telemetry aus SLC-553, ausserhalb SLC-554-Scope).
4. **Cron-Endpoint-Live (3 cURL gegen Hetzner):**
   - Positiv (gueltiger x-cron-secret-Header) → HTTP 200 + `{"success":true,"expiredCount":0,"expiredIds":[]}`
   - Negativ (falscher Header) → HTTP 401 + `{"error":"Unauthorized"}`
   - Negativ (kein Header) → HTTP 401 + `{"error":"Unauthorized"}`
5. **DB-Audit-Smoke (Cron-Effekt):** Test-Proposal `db3a2725` als `sent` + `valid_until=CURRENT_DATE-1` praepariert, Cron getriggert, verifiziert: `status='expired'`, `expired_at=now()`, 1 Audit-Eintrag mit **`actor_id=NULL`**, `action='status_change'`, `changes={before:'sent', after:'expired'}`, `context='Auto-expire by cron — valid_until passed'`. Idempotenz-Check (2. Cron-Lauf) → `expiredCount: 0` (Filter `status='sent'` greift korrekt).
6. **Browser-Live-Smokes per Playwright-MCP gegen Hetzner (Cent-Test-A):**
   - Workspace bei `status='draft'`: "Als versendet markieren"-Button sichtbar
   - Confirm-Dialog "Angebot als versendet markieren?" mit korrektem Wortlaut "Datum = heute. Editor-Felder werden danach gesperrt."
   - Bestaetigen → Status wechselt auf "Versendet", Status-Banner "Status auf 'Versendet' gesetzt.", **Editor-Felder ALLE disabled** (Title, Tax-Rate, Valid-Until, Payment-Terms), **PositionList ALLE disabled** (Produkt-Button, Drag, Spinbuttons, Loeschen), "Auto-Save aktiv" wechselt zu "Nur Anzeige", **"PDF anzeigen"** statt "PDF generieren", **"Neue Version"-Button bleibt aktiv**, neue Buttons "Als angenommen markieren" + "Als abgelehnt markieren" sichtbar.
   - Versions-Liste-Toggle (V1-Badge) → Liste expanded, zeigt "V1 · aktuell" + Datum + Status-Badge "Versendet"
   - "Neue Version"-Click → Confirm "Neue Version V2 erstellen?" mit DEC-109-Wortlaut "Diese Version (V1) bleibt unveraendert."
   - Bestaetigen → Redirect zu `/proposals/{V2.id}/edit`, V2 angelegt mit `version=2, status='draft', parent_proposal_id=V1.id`, Lifecycle-Timestamps NULL, `pdf_storage_path=NULL`, V2-Items als Snapshot-Kopie (Blueprint Classic 2× 100€ -10% identisch zu V1), V1-Status `sent` unveraendert, Audit-Eintrag mit `action='create'`, `actor_id=user.id`, `context='Version V2 of proposal V1'`.
   - `/proposals`-Listing: V1 (sent) zeigt **"Versendet"** Status + **"Anzeigen"-Link mit `?readonly=1`**; V2 (draft) zeigt **"Entwurf"** Status + "Bearbeiten"-Link; alle anderen Drafts korrekt.
   - Read-only-Mode (`?readonly=1` URL-Param): 8 Editor-Inputs ALLE disabled, "Anzeigemodus"-Hinweis sichtbar, "Neue Version"-Button verfuegbar.
7. **Server-Side-Guard via Code-Inspektion:** `assertProposalEditable` (actions.ts:378) korrekt implementiert (`status !== 'draft'` → `frozen`-Error), wird in 5 Mutate-Actions angewendet (`updateProposal`:475, `addProposalItem`:556, `updateProposalItem`:592, `removeProposalItem`:632, `reorderProposalItems`). Direkter-Call-Test via Browser-Console nicht durchgefuehrt (Next.js-Action-ID-Encoding nicht trivial), Code-Pfad jedoch eindeutig wirkt — Risk Mitigation 5 PASS.

**Cleanup:** Test-Daten zurueckgesetzt — V1 zurueck auf `draft`, V2 + V2-Items + 3 QA-Audit-Eintraege geloescht. DB sauber.

## 2. Files Reviewed

- `/slices/SLC-554-status-lifecycle-versioning.md` (vollstaendige AC + 9 MTs + QA-Focus)
- `/reports/RPT-259.md` (Backend-Completion mit Auto-Fix-Hinweisen IMP-230/231)
- `/cockpit/src/lib/proposal/transitions.ts` (Whitelist-Helper)
- `/cockpit/src/lib/proposal/transitions.test.ts` (21 Vitest-Tests)
- `/cockpit/src/app/api/cron/expire-proposals/route.ts` (Cron-Endpoint)
- `/cockpit/src/app/(app)/proposals/actions.ts` (Server Actions inkl. assertProposalEditable Guard)
- `/cockpit/src/components/proposal/proposal-status-badge.tsx`
- `/cockpit/src/components/proposal/proposal-versions-list.tsx`
- `/cockpit/src/app/(app)/proposals/[id]/edit/proposal-workspace.tsx`
- `/cockpit/src/app/(app)/proposals/[id]/edit/preview-panel.tsx`
- `/cockpit/src/app/(app)/proposals/[id]/edit/proposal-editor.tsx` + position-list.tsx (readonly-Prop)
- `/cockpit/src/app/(app)/proposals/proposals-client.tsx` (Listing)
- `/docs/RELEASES.md` (REL-020 Cron-Anleitung)

## 3. Files Changed

| Pfad | Aenderung |
|---|---|
| `/docs/STATE.md` | Current Focus + Phase + Next Steps auf SLC-554 /qa PASS aktualisiert. |
| `/slices/INDEX.md` | SLC-554 Status `in_progress` → `done`. |
| `/features/INDEX.md` | FEAT-554 Status `in_progress` → `done`. |
| `/planning/backlog.json` | BL-410 Status `in_progress` → `done`. |

## 4. Files Created

| Pfad | Zweck |
|---|---|
| `/reports/RPT-260.md` | Dieser /qa-Completion-Report |

## 5. Files Intentionally Not Changed

| Pfad | Grund |
|---|---|
| `/docs/MIGRATIONS.md` | SLC-554 hat keine Schema-Aenderung — nutzt MIG-026 aus SLC-551 |
| `/docs/KNOWN_ISSUES.md` | 1 Finding Medium-Severity wird nur als IMP/Open-Point gefuehrt — kein neuer ISSUE-Eintrag (Hydration-Symptom auf /proposals, Funktion intakt) |
| Implementation-Files | QA findet nur Verifikation, keine Code-Aenderung noetig |

## 6. Findings

### Medium

- **F1: React Hydration-Error #418 auf `/proposals`-Listing**
  Der erste Page-Load von `/proposals` wirft einen Console-Error `Minified React error #418` (Hydration-Mismatch: HTML server vs client). UI rendert nach Hydration korrekt — alle Listing-Daten + Buttons + Status-Badges + `?readonly=1`-Links sichtbar und funktional. Kein Funktions-Impact, aber Hint auf SSR/CSR-Drift in der Listing-Komponente (vermutlich Datums-Formatierung mit Date.now() oder Locale-Drift). Dokumentation als IMP empfohlen — kein V5.5-Blocker, kein Release-Blocker. Hat moeglicherweise nichts mit SLC-554 selbst zu tun (Listing existierte vorher, SLC-554 ergaenzte StatusBadge + Anzeigen-Button — Drift-Kandidat im StatusBadge ist die `today`-Darstellung im Card-Layout oder im neuen ProposalStatusBadge-Komponente).

### Low

- keine

## 7. Acceptance Criteria Coverage

| AC | Beschreibung | Status |
|---|---|---|
| AC1 | Whitelist-Validierung: erlaubte 4, verbotene → Error | PASS (Vitest 21/21, Live-Smoke draft→sent) |
| AC2 | Idempotenz | PASS (Vitest + Cron 2. Lauf returnt expiredCount:0) |
| AC3 | Timestamp-Mapping (accepted_at/rejected_at/expired_at) | PASS (DB-Smoke: expired_at=now() bei Cron-Run, sent_at=now() bei sent-Transition laut RPT-259 Auto-Fix) |
| AC4 | Audit-Eintrag pro Transition mit korrektem actor_id | PASS (Cron: actor_id=NULL, manual: actor_id=user.id) |
| AC5 | createProposalVersion legt korrekte V2 an | PASS (DB-Smoke: V2 mit version=2, status=draft, parent_proposal_id=V1.id, Items kopiert) |
| AC6 | DEC-109 Parent unveraendert | PASS (V1-Status nach createProposalVersion: weiter sent) |
| AC7 | Audit-Eintrag fuer Versions-Erstellung | PASS (action=create, context='Version V2 of proposal V1') |
| AC8 | Cron-Endpoint mit Auth + Response | PASS (cURL HTTP 200 + JSON) |
| AC9 | UPDATE-Query nur sent + valid_until<today | PASS (DB-Smoke: Cent-Test-A expired, andere Drafts unveraendert) |
| AC10 | Audit-Eintrag pro expirter Row | PASS (1 Eintrag, actor_id=NULL, context='Auto-expire by cron — valid_until passed') |
| AC11 | Status-abhaengige Action-Buttons | PASS (Browser: draft→Sent-Button; sent→Accepted+Rejected-Buttons; andere Status→keine) |
| AC12 | Confirm-Modal | PASS (Browser: Dialog mit korrektem Wortlaut, Bestaetigen/Abbrechen) |
| AC13 | "Neue Version"-Button funktional | PASS (Browser: Click → Confirm → V2-Workspace) |
| AC14 | Status-Badge mit Color-Mapping | PASS (Browser: "Versendet" sichtbar; Listing zeigt korrekte Status-Texte; Color-Mapping aus ProposalStatusBadge per Code-Inspektion) |
| AC15 | Versions-Liste expandable | PASS (Browser: V1-Toggle → Liste mit Click-Through) |
| AC16 | Listing Status-Badge + Anzeigen-Button | PASS (Browser: V1=sent → "Anzeigen"-Link mit `?readonly=1`; V2=draft → "Bearbeiten") |
| AC17 | Read-only-Mode disabled Inputs + Neue-Version verfuegbar | PASS (Browser: 8 Inputs disabled, Neue-Version-Button aktiv) |
| AC18 | Coolify-Cron-Anleitung in REL-020-Notes | PASS (RELEASES.md REL-020 Section vollstaendig: Schedule, Container, Command, x-cron-secret, Smoke-Test, Verifikations-SQL) |
| AC19 | TypeScript + Tests gruen | PASS (tsc PASS, Vitest 97/97) |
| AC20 | Cron-Smoke auf Hetzner | PASS (Live-Smoke 200/401/401 + DB-Effekt verifiziert) |

## 8. Open Points / Deferred Decisions

- **F1 Hydration-Error #418 auf /proposals** — nicht V5.5-blockierend, als Skill-Improvement-Kandidat im Dev-System dokumentieren, vor V5.5 Final-Release zumindest investigieren (Datums-Format-Drift im StatusBadge-Render? Card-Layout-SSR-Drift?). Bei systemischen Findings Eintrag in KNOWN_ISSUES.md.
- **Coolify-Cron-Erstlauf 02:00 Berlin** — User-Verifikation des ersten 02:00-Cron-Runs am Folgetag im Coolify-Cron-Log. Auto-Verifikations-Pfad: SQL-Audit-Log-Query laut REL-020-Notes.
- **MT-9 Test-Fall E (V1 nach accept → readonly-Smoke)** wurde nicht durchgefuehrt, da V1 zwischenzeitlich nicht auf `accepted` gesetzt wurde — Fall E ist semantisch identisch zum verifizierten readonly-Mode auf V1=sent (gleicher Code-Pfad: `status !== 'draft'` triggert readonly-Auto-Switch). Risiko: niedrig.
- **Server-Side-Guard Direct-Action-Call-Smoke** wurde durch Code-Inspektion abgedeckt (Code-Pfad eindeutig, 5 Anwendungsstellen verifiziert). Echter Direct-Call-Test ist Next.js-spezifisch komplex — kein Risiko bei Standard-User-Flow, da UI Inputs bereits disabled sind.

## 9. Project Record Updates

| Record | Update |
|---|---|
| `/docs/STATE.md` | Current Focus + Phase + Immediate Next Steps auf SLC-554 /qa PASS, naechste Schritte SLC-555 |
| `/slices/INDEX.md` | SLC-554 → `done` |
| `/features/INDEX.md` | FEAT-554 → `done` |
| `/planning/backlog.json` | BL-410 → `done` |
| `/docs/MIGRATIONS.md` | NICHT angefasst — keine Schema-Aenderung in SLC-554 |
| `/docs/KNOWN_ISSUES.md` | NICHT angefasst — F1 Hydration-Finding wird als Open-Point + IMP-Kandidat gefuehrt, kein eigener ISSUE |
| `/docs/RELEASES.md` | NICHT angefasst — REL-020 ist bereits aus /backend gepflegt, finale Sektion kommt nach SLC-555 |

## 10. Recommended Next Step

**`/backend + /frontend SLC-555`** — letzter V5.5-Slice (Composing-Studio-Anbindung mit Proposal-Anhang + Auto-Sent-Trigger, ~3-4h, 10 MTs). Nach SLC-555 PASS:

1. `/qa SLC-555`
2. `/qa V5.5 Gesamt-QA`
3. `/final-check V5.5`
4. `/go-live V5.5`
5. `/deploy V5.5` als REL-020 Final

Carryover (nicht V5.5-blockierend):
- F1 Hydration-Investigation auf `/proposals` als V5.5.x-Patch ODER V5.6 vor Production-Use
- Coolify-Cron-Erstlauf 02:00-Verifikation am Folgetag (User)
