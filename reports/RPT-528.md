---
id: RPT-528
date: "2026-05-23"
skill: /qa
slice: SLC-845
feature: FEAT-824
type: completion
status: completed
reviewOf: RPT-527
result: "QA PASS code-side — AC1-AC3 + AC6 alle PASS, Vitest 6/6 PASS re-run, TSC + ESLint clean fuer SLC-845, Stub-Detection clean, Wiring konsistent, owner_user_id-Column Hetzner-verifiziert (uuid + FK + Index), target=_blank rel-Sicherheit erfuellt. AC4+AC5 (Browser-Smoke) deferred bis SLC-847."
title: "SLC-845 /qa — Consent-Form DSE-Link Code-Side Verifikation"
---

## QA scope reviewed

`/qa SLC-845` — Verifikation von SLC-845 Code-Side Implementation gegen Slice-Spec AC1-AC3 + AC6 (Code-Side) + Pattern-Reuse + Wiring + Schema-Sanity + target=_blank-Security.

Files in-scope:
- `cockpit/src/lib/team/lookup-slug.ts` (43 Zeilen) — Helper
- `cockpit/src/lib/team/lookup-slug.test.ts` (105 Zeilen) — Vitest 6 Cases
- `cockpit/src/app/consent/[token]/page.tsx` (Patch) — owner_user_id-Select + Slug-Lookup + Conditional-Link

AC4 (Live-Token-Browser-Smoke) + AC5 (Edge-Case ownerless rendert ohne Crash) deferred bis SLC-847 Master-Merge per [[feedback_no_coolify_branch_switch_ever]] + [[feedback_slice_merge_at_end]].

## Result

**PASS code-side.** Alle Code-Side-AC erfuellt, Pattern-Reuse compliant, Wiring konsistent, Schema-Sanity Hetzner-verifiziert, Vitest 6/6 PASS.

## Acceptance Criteria

| AC | Beschreibung | Result | Evidence |
|---|---|---|---|
| AC1 | `lib/team/lookup-slug.ts` exportiert `getTenantSlugByOwnerUserId(ownerUserId: string): Promise<string \| null>` mit 2-stufiger Aufloesung (profiles.team_id → teams.slug) | ✅ PASS | `lookup-slug.ts:20-42` Helper, Zeile 27-31 profiles-Select, Zeile 35-39 teams-Select |
| AC2 | `consent/[token]/page.tsx` laedt `owner_user_id` + ruft `getTenantSlugByOwnerUserId` + graceful-null Fallback | ✅ PASS | `page.tsx:20` Select-Liste enthaelt `owner_user_id`. `page.tsx:57-59` `const tenantSlug = contact.owner_user_id ? await getTenantSlugByOwnerUserId(...) : null` |
| AC3 | JSX-Block zwischen `<p>wir speichern...</p>` und `<ConsentForm>`: conditional Link mit `/p/{tenantSlug}/datenschutz`, target=_blank, rel=noopener noreferrer, Text "Datenschutzerklaerung lesen ↗" | ✅ PASS | `page.tsx:75-86` `{tenantSlug && (<p className="mt-4 text-sm"><a href={/p/${tenantSlug}/datenschutz} target="_blank" rel="noopener noreferrer" className="text-primary underline">Datenschutzerklaerung lesen ↗</a></p>)}` |
| AC4 | Live-Smoke gueltiger Token: Link sichtbar, Klick oeffnet Public-Route im neuen Tab, DSE rendert | ⏸ DEFERRED | bis SLC-847 Master-Merge + Coolify-Redeploy |
| AC5 | Live-Smoke `tenantSlug=null` (orphaned-contact): KEIN Link gezeigt, kein Crash | ⏸ DEFERRED | bis SLC-847 |
| AC6 | Vitest 3-4+ Tests fuer Lookup-Helper | ✅ PASS | 6/6 PASS in 1.73s (Happy + Empty-Input + Profile-Missing + Team_Id-Null + Team-Missing + Slug-Null) |

## Stub Detection

Pattern-Scan (TODO/FIXME/HACK/XXX/console.log/debugger) auf SLC-845-Files:
- `lookup-slug.ts` — 0 Findings
- `lookup-slug.test.ts` — 0 Findings
- `consent/[token]/page.tsx` (Patch) — 0 Findings

**Stub-Detection: CLEAN.**

## Wiring Verification

SLC-845 verbindet Frontend-Page mit Backend-Schema (contacts → profiles → teams). Wiring-Chain pruefen:

| Step | Verifikation |
|---|---|
| URL `/consent/[token]` → page.tsx params | `page.tsx:14` `const { token } = await params` Next.js 15-Pattern |
| Token → contacts-Lookup | `page.tsx:17-23` `.from("contacts").select("id, first_name, last_name, email, consent_status, consent_token_expires_at, owner_user_id").eq("consent_token", token).maybeSingle()` |
| contact → owner_user_id-Branch | `page.tsx:57-59` ternary `contact.owner_user_id ? await getTenantSlugByOwnerUserId(...) : null` |
| owner_user_id → profiles-Lookup | `lookup-slug.ts:27-31` `.from("profiles").select("team_id").eq("id", ownerUserId).maybeSingle()` |
| profile.team_id → teams-Lookup | `lookup-slug.ts:35-39` `.from("teams").select("slug").eq("id", profile.team_id).maybeSingle()` |
| team.slug → JSX Render | `page.tsx:75-86` `{tenantSlug && <a href={/p/${tenantSlug}/datenschutz}>...</a>}` |
| Link → Public-Route SLC-843 | `/p/[tenant-slug]/datenschutz` Route existiert (SLC-843), Middleware-Whitelist `/p/` erlaubt unauth-Zugriff (SLC-843 AC4) |

**Wiring: CONSISTENT.** Alle Steps existieren als Code-Pfad. Public-Route ist via SLC-843 verfuegbar.

## Schema-Sanity Verifikation

Hetzner-Live-Check via `\d contacts`:
- `owner_user_id | uuid` Column existiert ✓
- FK `contacts_owner_user_id_fkey REFERENCES profiles(id) ON DELETE SET NULL` ✓
- Index `idx_contacts_owner_user_id btree (owner_user_id)` ✓
- RLS-Policies referenzieren owner_user_id (V7-Multi-User-Stack) ✓

`owner_user_id` ist V3+ standard (SLC-301 V3-Schema-Migration). Bei `ON DELETE SET NULL` koennen orphaned-Contacts existieren (User wurde geloescht → owner_user_id wird NULL) — exakt der AC5-Edge-Case. Code-Pfad ist via `contact.owner_user_id ? ... : null` defended.

**Schema-Sanity: PASS.**

## Pattern-Reuse Compliance

| Pattern | Quelle | Verwendung in SLC-845 | Status |
|---|---|---|---|
| `createAdminClient` | `lib/supabase/admin.ts` | `lookup-slug.ts:24` 1:1 import | ✅ |
| `.maybeSingle()` | Supabase JS pattern | 2x in lookup-slug.ts | ✅ |
| Graceful-null Fallback | Strategaize-Stil (z.B. teams_size.ts) | Empty-Input + Profile-Missing + Team_Id-Null + Team-Missing + Slug-Null = 5 Null-Pfade | ✅ |
| `target=_blank` mit `rel="noopener noreferrer"` | Strategaize-Security-Standard | `page.tsx:80-81` Pflicht erfuellt | ✅ Security |
| Next.js 15 `await params` | App-Router-Pattern | `page.tsx:14` (pre-existing, nicht durch SLC-845 angefasst) | ✅ |
| Conditional-Render `{x && <...>}` | React-Standard | `page.tsx:75` graceful-no-link-render | ✅ |

**Pattern-Reuse: 6/6 COMPLIANT.**

## Type Checking + Linting

- `npx tsc --noEmit -p tsconfig.json` filter SLC-845: **0 Errors**. 8 pre-existing unrelated weiter (vies, skonto, useVoiceCapture).
- `npx eslint <SLC-845-Files>` — **0 Findings (clean)**.

## Findings

### Blocker / High / Medium / Low

Keine.

## Open risks or missing checks

- **AC4 Live-Token-Smoke + AC5 Edge-Case Smoke** deferred bis SLC-847.
- **Slug-Lookup-Cost** 2 DB-Hits pro Consent-Page-Aufruf. Konsumiert <50ms im normalen Pfad, akzeptabel fuer ~10/Tag-Consent-Frequency.
- **Link-Text "Datenschutzerklaerung lesen ↗"** ist hardcoded deutsch. V2 mit Mehrsprachigkeit (i18n-key).
- **Wenn `/p/[tenant-slug]/datenschutz` 404 returnt** (Race: Tenant-Admin loescht legal_documents-Row zwischen Consent-Token-Versand und Customer-Click) — User sieht 404, kein Crash. Akzeptable Edge-Race-Condition.

## Readiness Assessment

**SLC-845 ist READY fuer SLC-846 (Mail-Footer-Patch).**

Gruende:
- AC1-AC3 + AC6 (Code-Side) alle PASS mit harten Verifikations-Evidenzen
- Vitest 6/6 PASS — Happy + 5 null-Fallback-Pfade
- 0 TSC-Errors, 0 ESLint-Findings, 0 Stub-Findings
- Pattern-Reuse 6/6 compliant inkl. target=_blank-Security-Standard
- Wiring konsistent: 7 Steps von URL bis JSX-Link, Public-Route via SLC-843 verfuegbar
- Schema-Sanity Hetzner-verifiziert (uuid + FK + Index)
- Live-Smokes AC4+AC5 bewusst auf SLC-847 verschoben

Master-Merge weiter defer bis SLC-847 (DEC-238).

## Recommended next step

`/backend SLC-846` — Mail-Footer-Auto-Insert mit DSE-URL des sendenden Tenants. ~1.5h. Branch weiter auf `slc-841-customer-dse-schema-migration`.

Context-Druck: niedrig. Context OK → kein Handoff noetig.
