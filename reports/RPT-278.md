---
id: RPT-278
date: "2026-05-02"
skill: /backend
slice: SLC-563
feature: FEAT-561
type: completion
status: completed
reviewOf: null
result: "Backend-Portion durch (MT-1+MT-7+MT-8): 22+13 neue Vitest-Tests, 2 Server Actions, PDF-Konditionen-Block + Trigger-Label-Helper, V5.5-Snapshot bit-identisch."
title: "/backend SLC-563 — Split-Plan Backend-Portion (MT-1 + MT-7 + MT-8)"
---

# /backend SLC-563 — Backend-Portion (MT-1 + MT-7 + MT-8)

## Outcome summary

Backend-Anteil von SLC-563 (Split-Plan + PDF-Renderer-Konditionen-Block) implementiert. Frontend-MTs (MT-2..MT-6) und Smoke (MT-9) bewusst out-of-scope dieser Session — kommen im naechsten /frontend SLC-563.

**Status:** Slice partially complete. Backend-Portion fully done. Slice-Status `in_progress`.

**Verifikation:**
- TypeScript: clean (`tsc --noEmit` ohne Output)
- ESLint: clean
- Vitest: 148/148 PASS (vor SLC-563: 113, +22 milestones-validation +13 renderer = +35)
- Build: ✓ Compiled successfully in 15.3s
- Existing V5.5-Snapshot + SLC-562-Skonto-Snapshot weiterhin matched → DEC-120 Bit-Identitaet bestaetigt

## Files reviewed

- `cockpit/src/lib/proposal/skonto-validation.ts` — Pattern-Vorlage fuer pure Validation
- `cockpit/src/lib/proposal/skonto-validation.test.ts` — Vitest-Pattern
- `cockpit/src/types/proposal-payment.ts` — `PaymentMilestone`-Typ + Trigger-Enum bereits aus SLC-561
- `cockpit/src/app/(app)/proposals/actions.ts` — `updateProposal`, `assertProposalEditable`, `logAudit`-Pattern, `generateProposalPdf`
- `cockpit/src/lib/pdf/proposal-renderer.ts` — `buildProposalDocDefinition`, Skonto-Block-Stelle aus SLC-562
- `cockpit/src/lib/pdf/proposal-renderer.test.ts` — Snapshot-Test-Pattern
- `cockpit/src/lib/audit.ts` — `logAudit`-Helper
- `sql/migrations/027_v56_payment_terms_and_briefing.sql` — `proposal_payment_milestones`-Schema verifiziert (UNIQUE auf (proposal_id, sequence), CHECK auf percent + due_trigger, FK ON DELETE CASCADE)

## Files created

- `cockpit/src/lib/proposal/milestones-validation.ts` — Pure Validation-Functions (Single-Source-of-Truth fuer UI + Server-Action). Exports: `validateMilestonesSum`, `validateMilestoneTrigger`, Type `MilestoneInput`. Strict 100% Sum-Check via `Number(sum.toFixed(2)) === 100` (DEC-115 strict, kein Toleranz-Bereich). Sequenz-Check 1-based + lueckenlos. Trigger-Check: enum + `due_offset_days` only-on-`days_after_signature`.
- `cockpit/src/lib/proposal/milestones-validation.test.ts` — 22 Vitest-Cases: empty, single 100, 50/50, 33.33+33.33+33.34, 33+33+33=99, 50+50+50=150, Sequence-Lueckenfaelle, Trigger-Faelle (alle 4 Enum-Werte + ungueltige + days-Edge-Cases).
- `reports/RPT-278.md` — dieser Report.

## Files changed

- `cockpit/src/app/(app)/proposals/actions.ts` —
  - Import: `validateMilestonesSum`, `validateMilestoneTrigger`, `MilestoneInput`, `PaymentMilestone`
  - **Neu:** `getProposalMilestones(proposalId): Promise<PaymentMilestone[]>` — SELECT mit ORDER BY sequence ASC, returnt leeres Array bei DB-Error (defensive).
  - **Neu:** `saveProposalPaymentMilestones({proposalId, milestones, totalGross}): MutationResult` — Auth-Check + `assertProposalEditable`-Guard. Empty-Plan → DELETE all + Audit `(n=0)`. Non-Empty → Trigger-Check pro Milestone + Sum-Check + totalGross-Validation + Transaction (DELETE + INSERT). `amount` als Snapshot persistiert (`Number(((totalGross * percent) / 100).toFixed(2))`). Audit `Milestones updated (n=N)`.
  - `generateProposalPdf` erweitert: Promise.all um sechsten Query (`proposal_payment_milestones` ORDER BY sequence). `milestones` an `pdfmakeRenderer.renderProposalPdf` weitergegeben.
- `cockpit/src/lib/pdf/proposal-renderer.ts` —
  - Import: `PaymentMilestone`-Type
  - `RenderProposalInput.milestones?: PaymentMilestone[]` (optional → Backwards-compat).
  - **Neu Export:** `formatMilestoneTriggerLabel(m): string` — 4 Enum-Werte → DE-Strings. `on_milestone` mit Label → "Bei Meilenstein: {label}", ohne → "Bei Meilenstein". Whitespace-only Label faellt auf Default zurueck.
  - `buildProposalDocDefinition`: `milestonesList = milestones ?? []` destructured. Konditionen-Block strict conditional: `if (milestonesList.length > 0)`. Tabelle "Teilzahlung | Faelligkeit | Anteil | Betrag" mit gleichem Layout/Stil wie Item-Tabelle (Primary-Color-HLine, paddingTop 5). Reihenfolge: Konditionen-Tabelle → Skonto-Zeile (existing SLC-562). Bei nur Skonto, keinen Plan: identisch zu SLC-562.
- `cockpit/src/lib/pdf/proposal-renderer.test.ts` —
  - Import: `formatMilestoneTriggerLabel`, `PaymentMilestone`-Type
  - `getContentSnapshot` erweitert um `milestones?`-Param.
  - `makeMilestone(partial)`-Helper.
  - **Neue describe-Blocks (13 Tests):**
    - "Konditionen-Block (DEC-120, SLC-563)": 7 Cases (ohne beides V5.5-bit-identisch, nur Milestones, nur Skonto, beides mit Reihenfolge-Check, 3 Snapshots).
    - "formatMilestoneTriggerLabel": 6 Cases (alle 4 Enum-Werte + on_milestone-Edge-Cases).
- `cockpit/src/lib/pdf/__snapshots__/proposal-renderer.test.ts.snap` — 3 neue Snapshots geschrieben: "ohne beides V5.5 Baseline strict", "mit 3 Milestones", "mit Milestones UND Skonto". Existing 2 Snapshots (V5.5 + SLC-562 Skonto) unveraendert → bit-identitaet bestaetigt.
- `slices/INDEX.md` — SLC-563 Status `planned` → `in_progress`.
- `planning/backlog.json` — BL-415 Status `planned` → `in_progress`.
- `docs/STATE.md` — Current Focus + Phase + Next Steps aktualisiert.

## Files intentionally not changed

- `cockpit/src/app/(app)/proposals/[id]/edit/use-skonto-mutex.ts` — MT-2 Stub-Refactor ist Frontend-Concern (Hook-Body, client-side). Bleibt out-of-scope.
- `cockpit/src/app/(app)/proposals/[id]/edit/[id]-edit-form.tsx` — MT-6 Editor-Integration ist Frontend.
- 3 neue UI-Komponenten (split-plan-section, milestone-row, sum-indicator) — MT-3..MT-5 Frontend.
- `docs/MIGRATIONS.md`, `docs/DECISIONS.md` — keine neuen Schema-Migrationen oder neuen Decisions in dieser Session. MIG-027 + DEC-115/116/120 bereits in V5.6-Architektur dokumentiert.
- `docs/KNOWN_ISSUES.md` — keine neuen Issues entdeckt.

## Problems found

Keine. Alle Verifikations-Schritte (TS, Lint, Vitest, Build, Snapshot-Match) PASS.

**Hinweis zur Snapshot-Bit-Identitaet:** Die existierenden V5.5-Snapshot + SLC-562-Skonto-Snapshot wurden beim Test-Run nicht ueberschrieben — Vitest hat sie als matched gemeldet. Das bestaetigt DEC-120: Output ohne Milestones (also `milestones?: PaymentMilestone[] = undefined` oder `[]`) ist bit-identisch zum vorherigen Output, weil `milestonesList.length > 0` strict-conditional ist und keinen Whitespace einfuegt.

## Open points

- **Frontend-MTs (MT-2..MT-6, MT-9):** Hook-Refactor + 3 UI-Komponenten + Editor-Integration + Skonto-Auto-Clear-useEffect + Browser-Smoke + Records-Update. Geschaetzt ~3-4h. Wird im naechsten /frontend SLC-563 erledigt.
- **Smoke-Verifikation:** Code-Path im Live-System nicht getestet (kein Server-Restart, kein Browser-Smoke). Wird im /qa SLC-563 nach /frontend + Coolify-Redeploy gemacht.
- **`amount`-Stale-Risk:** Wenn nach Milestone-Save eine Position hinzugefuegt wird (`total_gross` aendert sich), bleiben die persistierten `amount`s stale. Per V5.6-Design akzeptiert (User klickt erneut Save, oder regeneriert PDF). Aus Slice-Spec (Risks-Sektion) bewusst out-of-scope.

## Recommended next step

**/frontend SLC-563** — Frontend-Anteil:
- MT-2: useSkontoMutex Hook Real-Body (`milestones.some(m => m.due_trigger === 'on_signature' && m.percent === 100)`)
- MT-3: SumIndicator-Komponente (Live-Sum + Badge gruen/rot)
- MT-4: MilestoneRow (6-Spalten Desktop / 2-zeilig Mobile, dnd-kit Drag-Handle, Trigger-Select, conditional Tage-Input)
- MT-5: SplitPlanSection (Toggle + Liste + Add + SumIndicator + AlertDialog Toggle-Off-Confirm)
- MT-6: Editor-Form-Integration + Skonto-Auto-Clear-useEffect + Save-Pfad ruft `saveProposalPaymentMilestones` parallel zu `updateProposal`
- MT-9: Browser-Smoke + Records-Update (slices/INDEX → done, BL-415 → done, STATE.md → SLC-564 next)

Geschaetzt ~3-4h. Anschliessend /qa SLC-563 mit Pre-Deploy + Live-Smoke (analog SLC-562 RPT-276+277).
