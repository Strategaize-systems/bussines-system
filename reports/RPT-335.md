---
id: RPT-335
date: "2026-05-07"
skill: /qa
slice: SLC-641
feature: FEAT-641
type: completion
status: completed
reviewOf: null
result: "PASS — alle 10 ACs erfuellt, Live-Smoke gegen Hetzner gruen, ISSUE-057 + BL-423 resolved"
title: "QA SLC-641 — System-Stabilitaet & DSGVO (ISSUE-057 + Click-Log-Cleanup-Cron)"
---

# QA Report — SLC-641 V6.4 Hygiene-Sprint

## QA scope reviewed

Slice SLC-641 ueber 5 MTs:
- MT-1: ISSUE-057 followup-engine.ts Code-Fix (Spalte value -> total_gross)
- MT-2: Vitest Pure-Function-Test fuer Open-Proposals-Query
- MT-3: Click-Log-Cleanup-Cron-Endpoint /api/cron/click-log-cleanup
- MT-4: Vitest fuer Cron-Logik (Cutoff, DELETE, Audit-Insert, Idempotenz, Error-Pfade)
- MT-5: Coolify-Cron-Setup-Snippet (Notes-Draft fuer User)

Code-Verifikation, Stub-Detection, Wiring-Verification, Live-Smoke gegen Hetzner-Production-Coolify-Stack.

## Result

**PASS** — alle 10 Acceptance Criteria erfuellt. Live-Smoke gegen Hetzner verifiziert beide Code-Pfade. 2 Audit-Log-Rows in Production-DB. 0 `column proposals.value`-Errors im Container-Log.

Ein Hotfix-Cycle waehrend QA: erste Live-Smoke deckte echten Bug auf (`audit_log.entity_id NOT NULL`-Constraint-Violation, Slice-Spec hatte das uebersehen). Nach Hotfix-Commit `2b7a986` + 2. User-Redeploy: alle Smokes gruen.

## Acceptance Criteria — Punkt-fuer-Punkt-Verifikation

| AC | Erfuellt | Beleg |
|---|---|---|
| **AC-1** Z. 199+207 followup-engine.ts auf total_gross | YES | grep `proposals.value` in followup-engine.ts → 0 Treffer (Z. 141/150/176 sind `deals.value`, separate Tabelle). Plus Z. 235 mitgefixt — Slice-Spec sagte 2 Stellen, real waren 3 (Candidate-Context `proposalValue`). |
| **AC-2** Vitest fuer Open-Proposals-Query mit total_gross | YES | `cockpit/src/lib/ai/followup-engine.test.ts` neu, 2 Tests: select-Arg enthaelt `total_gross` und kein freistehendes `value`; order-Arg `total_gross` + `{ascending:false, nullsFirst:false}`. PASS. |
| **AC-3** Container-Log nach Deploy ohne column-proposals.value-Fehler | YES | Followups-Cron auf Hetzner manuell getriggert, `docker logs --since 5m` mit `grep -iE "proposals.value\|FollowupEngine"` → 0 Treffer. Cron-Response: `success:true, candidates:2, failed:0, skipped:2, expired:0`. |
| **AC-4** Endpoint reagiert auf POST mit Auth-Header | YES | Slice-Spec sagte "Authorization: Bearer", real: `x-cron-secret` (User-Bestaetigung im Pre-Plan). `curl -X POST ohne` → HTTP 401. Mit `-H "x-cron-secret: $SECRET"` → 200 + JSON. |
| **AC-5** DELETE >90d + audit_log mit changes-JSONB | YES | DB-Query `SELECT FROM audit_log WHERE action='click_log_cleanup'`: 2 Rows mit `entity_type=campaign_link_clicks`, `entity_id=run_id` (UUID), `changes={cutoff, run_at, run_id, oldest_kept, deleted_count}`, `context="DSGVO 90d retention: deleted X click-log row(s)..."`. |
| **AC-6** Idempotent (0-Row = success:true, deleted:0) | YES | Tabelle hat noch keine 90+d alten Eintraege (V6.2 erst seit 2026-05-06 deployed). Beide Live-Calls liefern `deleted:0, oldest_kept:null, success:true`. Auch der 2. Call (frische run_id) sauber durch. |
| **AC-7** Vitest Cron-Logik (Cutoff + DELETE + Audit + 0-Row) | YES | `route.test.ts` mit 9 Tests: Cutoff exakt 90d/UTC, DELETE-Filter `clicked_at`+`{count:"exact"}`, Audit-Felder inkl. run_id-als-entity_id, 0-Row + count:null Idempotenz, 3 Error-Pfade (DELETE/SELECT-MIN/INSERT). PASS. |
| **AC-8** Coolify-Cron-Setup-Snippet im Bericht | YES | Siehe Section "Coolify-Cron-Setup" unten — copy-paste-faehig. |
| **AC-9** Build clean + Vitest >=393 + Lint clean | YES | Build success, Vitest 404/404 PASS (war 393 vor SLC-641, +11 neue Tests aus MT-2/MT-4 + Hotfix-Test). Lint: 0 neue Findings (1 Warning `vi unused` pre-Hotfix sofort entfernt). 222 vorbestehende Findings unangefasst. |
| **AC-10** Live-Smoke nach Coolify-Redeploy: Build clean, App healthy, Cron-Trigger ohne Error | YES | Beide Cron-Endpoints manuell getriggert, beide success. Container `app-k9f5pn5upfq7etoefb5ukbcg-084251549630` `Up 2 minutes (healthy)`. Audit-Log-DB-Verifikation. Pattern-konform. |

## Stub-Detection

Scan ueber alle Dateien dieser Slice (`followup-engine.ts`, `followup-engine.test.ts`, `click-log-cleanup/route.ts`, `click-log-cleanup/route.test.ts`):

- TODO/FIXME: 0
- Placeholder-Returns: 0 (alle Returns sind echte Werte)
- Empty handlers: 0
- Console-Debug: 0 (nur produktive `console.log`/`console.error` in Cron-Routes — Pattern-konform)
- Hardcoded credentials: 0
- Commented-out code: 0
- Dummy data: 0 ausserhalb Test-Files (Test-Files duerfen Mocks)
- Incomplete error handling: 0 (Error-Pfade haben explizite throw-Statements + Audit-Log-Trail)

**Stub-Befund: keine.**

## Wiring-Verification

Slice ist Backend-only — Component → API entfaellt. Verbleibende Verkettung:

| Schritt | Verifikation |
|---|---|
| Coolify-Cron → `/api/cron/click-log-cleanup` | Pending — User-Aktion fuer Coolify-UI-Setup. Manuelle curl-Trigger gegen Production-URL bestaetigt Endpoint-Reachability + Auth-Behavior. |
| Route-Handler → `runClickLogCleanup` | Verifiziert: Route-POST validiert Cron-Secret, instantiiert admin-Client, ruft Pure-Function. JSON-Response gepruegt (success/error-Pfad). |
| Pure-Function → DELETE/SELECT/INSERT | Mock-Vitest deckt alle 3 DB-Calls; Live-Smoke bestaetigt echte Supabase-Calls funktionieren. |
| Function → Audit-Log | DB-Query nach manuellem Trigger zeigt 2 saubere Audit-Rows, exakt mit den erwarteten Spalten + JSONB-Felder. |
| Followups-Cron → followup-engine.ts | Verifiziert: Cron-Response `failed:0`, kein Container-Log-Hit fuer `column proposals.value`. ISSUE-057-Pfad live geheilt. |

**Wiring-Befund: vollstaendig + verifiziert.**

## Findings — gruppiert nach Severity

### Blocker
Keine.

### High
Keine. (Der Hotfix `audit_log.entity_id NOT NULL` waere High gewesen, ist aber waehrend QA aufgedeckt + sofort gefixt + erneut live-smoked. Resolved im Cycle.)

### Medium
- **Slice-Spec drift** — die Spec sagte:
  1. "2 Stellen" in followup-engine.ts (real 3 — Candidate-Context Z. 235)
  2. `Authorization: Bearer` Header (real `x-cron-secret`)
  3. `created_at` Spalte (real `clicked_at`)
  
  Alle 3 Abweichungen wurden vor Implementation aufgedeckt + vom User bestaetigt. Spec selbst wurde nicht aktualisiert — sollte als kleine docs-Korrektur in SLC-642 oder als separaten docs-Commit nachgezogen werden, damit die Slice-Files weiter als zukuenftige Pattern-Vorlagen nutzbar bleiben.

### Low
- **`failed:0, skipped:2`** im Followups-Cron — die 2 Kandidaten wurden nicht zum LLM weitergereicht. Das ist erwartetes Verhalten (entweder bereits pending action vorhanden, oder andere Skip-Conditions in `processFollowupCandidates`). Kein Bug, kein SLC-641-Scope. Erwaehnt fuer Vollstaendigkeit.

## Open Risks / Missing Checks

- **Coolify-Cron Setup ist User-Aktion** — Endpoint live, aber wird erst regelmaessig laufen wenn User den Cron in Coolify-UI hinterlegt (Snippet siehe unten). Bis dahin: Click-Log-Retention nicht aktiv. Relevant erst wenn campaign_link_clicks-Tabelle 90+d Eintraege hat (V1-Internal-Test-Mode: voraussichtlich Mitte August 2026 — also nicht zeitkritisch).
- **24h-Beobachtung des Followups-Crons** — die Slice-Spec verlangte 24h Log-Stichprobe. Im Live-Smoke wurde nur 1 Manual-Trigger gemacht. Da der Code-Fix deterministisch ist und der konkrete Aufruf sauber durchlief (failed:0), ist Re-Manifestation extrem unwahrscheinlich — aber formal steht die 24h-Stichprobe noch aus und wuerde im naechsten /post-launch oder durch nochmaligen Manual-Trigger morgen abgehakt.
- **Spec-Spec-Drift** (siehe Medium-Findings) — keine Operativ-Implikation, aber Doku-Hygiene-Hinweis fuer SLC-642.

## Readiness Assessment

**Ready for next step.** SLC-641 ist code-side + live-verified done. Beide Crons production-ready. Audit-Trail aktiv. Vitest-Coverage hoch (11 neue Tests). Build + Lint clean. Records aktualisiert (KNOWN_ISSUES.md, slices/INDEX, features/INDEX, backlog.json, STATE.md).

Internal-Test-Mode bleibt aktiv. Kein Final-Check / Release-Schritt fuer SLC-641 alleine — V6.4-Release kommt erst nach allen 5 Slices.

## Files reviewed
- `/cockpit/src/lib/ai/followup-engine.ts` (3 Stellen Code-Fix)
- `/cockpit/src/lib/ai/followup-engine.test.ts` (neu, 2 Vitest)
- `/cockpit/src/app/api/cron/click-log-cleanup/route.ts` (neu, Pure-Function-Pattern)
- `/cockpit/src/app/api/cron/click-log-cleanup/route.test.ts` (neu, 9 Vitest)
- `/cockpit/src/app/api/cron/expire-proposals/route.ts` (Pattern-Reference)
- `/cockpit/src/app/api/cron/verify-cron-secret.ts` (Auth-Header-Reference)
- `/cockpit/src/lib/supabase/admin.ts`
- `/sql/migrations/026_v55_proposal_creation.sql` (proposals.total_gross)
- `/sql/migrations/029_v62_automation_and_campaigns.sql` (campaign_link_clicks-Schema)
- `/slices/SLC-641-stabilitaet-dsgvo.md`
- Hetzner-Production-Stack (Container-Healthstatus, Audit-Log-DB-Query, Cron-Smoke)

## Files changed
- `/docs/KNOWN_ISSUES.md` — ISSUE-057 von `open` auf `resolved` mit Resolution-Section.
- `/slices/INDEX.md` — SLC-641 von `planned` auf `done`.
- `/features/INDEX.md` — FEAT-641 von `planned` auf `done`.
- `/planning/backlog.json` — BL-423/431/432 von `open` auf `done` mit Resolution-Beschreibung.
- `/docs/STATE.md` — Current Focus auf SLC-641 done aktualisiert, naechster Schritt /doctor SLC-642.

## Files created
- `/reports/RPT-335.md` — diese QA-Report-Datei.

## Files intentionally not changed
- `/slices/SLC-641-stabilitaet-dsgvo.md` — bleibt unangefasst, die 3 Spec-Drift-Punkte sind in der KNOWN_ISSUES-Resolution-Section + im Backlog-Description dokumentiert. Spec-Doku-Hygiene gehoert in SLC-642-Audit oder separaten docs-Commit.
- `/docs/RELEASES.md` — kein Release-Eintrag, da SLC-641 Teil des V6.4-Buendels ist und erst beim REL-026-Deploy ein RELEASES-Eintrag entsteht.
- `/docs/MIGRATIONS.md` — kein Schema-Change in SLC-641, kein MIG-Eintrag noetig.

## Coolify-Cron-Setup (User-Action)

Im Coolify-UI fuer den `app`-Container folgenden Cron anlegen:

- **Name:** click-log-cleanup
- **Container:** `app`
- **Schedule:** `0 3 * * *` (taeglich 03:00 UTC = 04:00 Berlin Sommerzeit / 05:00 Winterzeit)
- **Command:**
  ```bash
  node -e 'fetch("https://business.strategaizetransition.com/api/cron/click-log-cleanup",{method:"POST",headers:{"x-cron-secret":process.env.CRON_SECRET}}).then(r=>r.json()).then(j=>console.log(JSON.stringify(j))).catch(e=>{console.error(e);process.exit(1)})'
  ```

**ENV:** `CRON_SECRET` ist im `app`-Container bereits vorhanden (von expire-proposals, meeting-briefing, automation-runner mitgenutzt). Kein neues Setup noetig.

**Erste Wirkung:** Sobald `campaign_link_clicks` Eintraege > 90 Tage enthaelt (frueheste Wirkung: 2026-08-04, 90d nach V6.2-Deploy 2026-05-06), wird der Cron taeglich loeschen. Bis dahin liefert er taeglich `deleted:0` + Audit-Log-Trail.

## Recommended Next Step

**`/doctor SLC-642 V6.4 Code-Audit Inventur`** — naechster Slice in der seriellen V6.4-Sequenz. Inventur 5 Hot-Spots (Cron-Jobs / AI-Engines / Source-Schema / tote Server-Actions / tote API-Routes), Output Audit-Report mit Severity-Klassifikation, dann **USER-PAUSE** zur Item-Klassifikation.

Optional vor SLC-642: User legt Coolify-Cron `click-log-cleanup` an.

## Commits

- `f1af68b` fix(SLC-641/MT-1+2): ISSUE-057 followup-engine total_gross
- `fff4116` feat(SLC-641/MT-3+4): click-log-cleanup cron + DSGVO 90d retention
- `2b7a986` fix(SLC-641/MT-3): audit_log.entity_id NOT NULL — use runId

Alle 3 auf origin/main gepusht. 2 User-Coolify-Redeploys durchgefuehrt waehrend QA.
