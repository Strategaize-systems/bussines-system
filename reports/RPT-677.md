---
id: RPT-677
date: "2026-07-08"
skill: /qa
slice: SLC-915
feature: FEAT-927
type: completion
status: completed
result: "MT-1 (ISSUE-138 CSP) code-done — 12/12 CSP-Tests + volle Suite grün, tsc/build clean; Telefonie/Mikro-Runtime-ACs PASS-CODE/PENDING-LIVE (Browser-Smoke = /deploy)"
title: "QA SLC-915 MT-1 — ISSUE-138 CSP connect-src SIP-WSS + Permissions-Policy microphone=(self)"
---

# QA — SLC-915 MT-1 (ISSUE-138 CSP)

## 1. QA-Scope
Erste Implementierungs-Einheit von SLC-915 (V8.17 Regressions-Fix-Bundle): ISSUE-138 —
CSP `connect-src` um die WSS-SIP-Softphone-Origin erweitern + `Permissions-Policy`
`microphone=()`→`microphone=(self)` unter dem seit SLC-910 Phase-B **enforced** CSP-Header.
`camera=()` bleibt (Step-0 DRIFT-1). Reines Header-/Config-Change, keine Schema-/Runtime-Logik.

Geprüfte Dateien:
- `cockpit/src/lib/security/csp.ts` (MODIFY — `buildCSP`-Signatur + `connect-src` + `PERMISSIONS_POLICY`)
- `cockpit/next.config.ts` (MODIFY — einziger `buildCSP`-Caller)
- `cockpit/src/lib/security/__tests__/csp.test.ts` (MODIFY — migriert + ISSUE-138-Fälle)
- `cockpit/src/hooks/use-sip-phone.ts` / `cockpit/src/app/(app)/calls/actions.ts` (read-only — Origin-Abgleich)

## 2. Ergebnis
**MIXED — code-done.** Statische/Code-Verifikation vollständig grün; die runtime-verhaltensändernden
ACs (WSS-Connect, Mikrofon, 0 CSP/Permissions-Console-Errors auf dem Call-Flow) sind
**PASS-CODE / PENDING-LIVE** — der funktionale Browser-Smoke ist erst gegen den deployten Container
möglich (BS `main` 2 Commits vor `origin`, Webhook inaktiv → noch nicht deployed). Kein Blocker/High.

## 3. Per-AC-Verdicts (MT-1-Scope)

| AC | Inhalt | Verdict | Begründung |
|---|---|---|---|
| AC-915-1 | SIP-Softphone verbindet unter enforced CSP (`wss://sip…` in `connect-src`) + `microphone=(self)` | **PASS-CODE / PENDING-LIVE** | `connect-src` enthält `wss://${NEXT_PUBLIC_SIP_DOMAIN\|\|sip.strategaizetransition.com}` (Origin-Match gegen `calls/actions.ts:268` `wssUrl` verifiziert); `PERMISSIONS_POLICY` = `microphone=(self)`. Runtime-WSS-Connect + Mikro + 0 Console-Errors = /deploy-Browser-Smoke. |
| AC-915-2 | `camera=()` unverändert; Meeting-Audio kein Permissions-Block | **PASS-CODE / PENDING-LIVE** | `camera=()` im Policy-String bestätigt (Test asserted `camera=(self)` NICHT vorhanden). Jitsi = separater Origin via `window.open` → Policy gated es nicht. Meeting-`window.open`-Smoke = /deploy. |
| AC-915-8 (MT-1-Teil) | Suite grün; enforced `Content-Security-Policy` bleibt (Report-Only 0); globale `img-src`/`connect-src` unverändert strikt außer der einen WSS-Origin | **PASS-CODE** | Header-Key = `Content-Security-Policy` (next.config.ts:24; „Report-Only" nur in Rollback-Kommentaren). `img-src 'self' data: blob:` per Test unverändert. Volle AC-915-8-Aggregation erst in der Gesamt-/qa (hängt an den restlichen MT-Tests). |

Deferred/Pending zählt NICHT als PASS-LIVE → Slice-Status bleibt `in_progress` / code-done.

## 4. Test-Ausführung + Silent-Skip-Audit
- **Volle Suite** (`npm run test`, vitest.config.ts, jsdom): **176 Files / 1621 Tests, 0 fail** (im /backend-Schritt derselben Session; seither kein Code-Change).
- **Gezielt** `__tests__/csp.test.ts` (aus `cockpit/`): **12/12 grün** (9 buildCSP + 3 PERMISSIONS_POLICY).
  - Fallstrick dokumentiert: `npx vitest run <pfad>` aus dem **Repo-Root** (statt `cockpit/`) meldet fälschlich „no tests / 1 failed" — cwd-Artefakt, kein realer Fail. Aus `cockpit/` grün.
- **Include-Coverage:** `csp.test.ts` liegt unter `src/**/__tests__/*.test.ts` → vom Default-`include` (`src/**/*.test.ts`) erfasst, lief in der vollen Suite mit (kein Silent-Skip).
- **ENV-Gating:** MT-1 berührt keine DB-/Live-gated Suite → kein Skip-Guard relevant. 0 silent-skipped Tests im Scope.
- **tsc** `--noEmit` clean; **`next build`** ✓ Compiled successfully (81/81 Pages) — CSP-Change-Done-Gate.

## 5. Stub- / Wiring-Audit
- **Stub-Scan** (csp.ts, next.config.ts): keine TODO/FIXME/console.log/placeholder-return/empty-catch. Sauber.
- **Wiring-Chain (Header-Serving-Pfad):** `buildCSP` → `CSP_VALUE` (next.config.ts:9) → `SECURITY_HEADERS` (:19) → `headers()` `source:"/:path*"` (:33) → auf jeder Route ausgeliefert. **Bidirektional:** erlaubte Origin `wss://${sipDomain}` ↔ tatsächliche Softphone-Connect-Origin `wss://${sipDomain}/ws` (`use-sip-phone.ts:95` `transportOptions.server` ← `calls/actions.ts:268`) — Host identisch, CSP matcht host-basiert, `/ws`-Pfad irrelevant. Kette geschlossen.
- Kein neuer Production-Caller/Route nötig (Config-Change) — Wiring reduziert sich auf den Serving-Pfad, verifiziert.

## 6. Findings

### Low / Informational
- **STUN out-of-scope (kein Defect):** Der Softphone nutzt zusätzlich `stun:stun.l.google.com:19302` (`use-sip-phone.ts:100`). STUN/WebRTC-ICE wird vom CSP-`webrtc`-Directive gated (default-allow, wenn ungesetzt), **nicht** von `connect-src` — daher durch diese Policy nicht blockiert und korrekt außerhalb MT-1. Beim /deploy-Telefonie-Smoke mitbeobachten (falls ein Browser WebRTC doch an `connect-src` koppelt).
- **Commit-Message-Zähler:** MT-1-Commit `26e56a6` nennt „7->14 Tests"; real 7→**12**. Kosmetisch, keine History-Amendment nötig.

## 7. Offene Risiken / fehlende Checks
- Funktionaler Feature-Flow-Browser-Smoke (Telefonie: WSS verbindet + Mikro-Zugriff + 0 `connect-src`/`Permissions-Policy`-Console-Errors auf dem **Call-Flow**, nicht nur Hydration — RPT-672-Trugschluss-Prävention, IMP-1401) steht aus bis Deploy. Ready-to-run beim /deploy: `curl -I <prod-URL>` (Header-Assertion) → `node tests/_probe/csp-check.mjs <URL>` (Hydration-Baseline) → manueller Softphone-Call + Meeting-Start. Playbook `security-headers-live-smoke.md`.

## 8. Readiness-Assessment
MT-1 ist **code-done und QA-grün auf allen statisch verifizierbaren Ebenen**. Die einzige verbleibende
Unsicherheit ist rein runtime (Header-Serving + Media-Permissions), strukturell nicht statisch schließbar
und sauber als PENDING-LIVE mit konkretem Nachhol-Snippet deferiert. Freigabe, mit MT-2 fortzufahren.

## 9. Next workflow step
Weitere MTs verbleiben im Slice → **`/backend SLC-915 MT-2`** (ISSUE-142 — `checkConsentStatus`
Count-Guard fail-closed), danach `/qa`. Reihenfolge unverändert 138→142→141→140→139. Der ausstehende
Telefonie-/Meeting-Live-Smoke wird bei der Gesamt-/qa V8.17 (mit Feature-Flow-CSP-Smoke) bzw. /deploy
nachgeholt — **nicht** vorher als PASS-LIVE gewertet.
