---
id: RPT-449
date: "2026-05-17"
skill: /qa
slice: SLC-753
feature: FEAT-751
type: completion
status: needs-rework
reviewOf: RPT-448
result: "Code-Side PASS — TSC 0 neue Errors, Vitest 899/899 (Baseline 885 + 14 SLC-753), Stub-Scan clean, Wiring-Chain sauber, AC1..AC9 PASS. AC10 Live-Smoke + Real-Bedrock-Probe deferred bis Master-Merge + User-Coolify-Redeploy."
title: "SLC-753 /qa — Code-Side PASS, AC10 Live-Smoke pending Master-Merge"
---

# RPT-449 — SLC-753 /qa Code-Side PASS, AC10 pending Deploy

## QA Scope Reviewed

- Code-Side-Aenderungen aus RPT-448:
  - `cockpit/src/app/(app)/mein-tag/actions/sculpt-nl-rule.ts` + Test
  - `cockpit/src/components/mein-tag/nl-rule-builder-card.tsx` + Test
  - `cockpit/src/app/(app)/mein-tag/page.tsx` (mod)
  - `cockpit/src/app/(app)/mein-tag/mein-tag-client.tsx` (mod)
- Acceptance Criteria AC1..AC10 aus `/slices/SLC-753-mein-tag-nl-surface.md`
- Stub-Scan in 4 neuen Implementierungs-Dateien
- Wiring-Chain: page → MeinTagClient → NLRuleBuilderCard → sculptNlRule → sculptor.ts → Bedrock
- Regression vs. RPT-446 SLC-752-Baseline (Default-Vitest 885, RLS-Suite 146)

## Result

**Code-Side PASS.** AC10 Live-Smoke Browser + Real-Bedrock-Probe **NOT YET PERFORMED** — Code liegt im Branch `slc-753-mein-tag-nl-surface` (Worktree), Production-Coolify haengt auf `main` HEAD `1de2529` ohne SLC-753. Per `feedback_slice_deploy_procedure` + `feedback_slice_merge_at_end` erfolgt Live-Smoke nach Master-Merge + User-Coolify-Redeploy.

Slice-Status bleibt **`in_progress`** in `/slices/INDEX.md`, NICHT `done`, bis AC10 PASS.

## Findings

### Blocker
- _keine_

### High
- _keine_

### Medium
- _keine_

### Low

- **L-1: AC9 Spec-Baseline-Schaetzung war falsch** — Spec sagt "~960 → ~960+5 PASS". Echte Default-Vitest-Baseline aus RPT-446 ist 885 → SLC-753 +14 = 899 PASS. test:all (Default + RLS) = 899 + 146 = 1045. SLC-753 hat +14 Tests statt erwartete +5 (defensiver geschrieben: 6 Server-Action + 5 Component-States + 3 Cost-Helper). Kein Funktionsfehler — Note fuer Slice-Spec-Update.
- **L-2: `assertRole`-Misfit fuer Server-Actions** — bereits in RPT-448 dokumentiert. `assertRole()` macht `redirect()`, fuer Server-Actions mit Result-Pattern unbrauchbar. `getProfile()` direkt + ok:false-Variante war richtige Wahl. Bei Wiederholung waere `requireRoleOrError(roles)` Helper sinnvoll → IMP-Kandidat fuer Dev-System (nicht in dieser Slice erstellt).

### Cosmetic
- _keine_

## Stub Scan Results

Scanned `nl-rule-builder-card.tsx`, `sculpt-nl-rule.ts`, `sculpt-nl-rule.test.ts`, `nl-rule-builder-card.test.tsx`.

| Pattern | Result |
|---|---|
| TODO/FIXME/HACK | 0 |
| console.log / debugger | 0 |
| Empty error handlers `catch {}` | 0 |
| Empty handlers `onClick={() => {}}` | 0 |
| Placeholder returns | 1 acceptable (`if (!canSculpt) return null` — Defense-in-Depth, dokumentiert) |
| Hardcoded credentials/URLs | 0 |
| Commented-out code | 0 |
| Dummy data in production code | 0 |

**Acceptable placeholders (NICHT Stubs):**
- Mic-Button `type="button" disabled` mit Tooltip "Spracheingabe folgt — SLC-755" — explizit per Spec-Out-of-Scope.
- Trockenlauf-Button `disabled` im Karte-4-Block mit Text "Trockenlauf + Aktivieren folgen in SLC-754" — explizit per Spec.

Beide sind sichtbar markiert (data-testid + Tooltip + Hinweis-Text), NICHT versteckte unfertige Funktionalitaet.

**Stub-Scan: CLEAN.**

## Wiring Chain Verification

```
page.tsx (Server-Component)
  → getProfile() (cached pro Request)
  → canSculpt = role in [admin, teamlead]
  → <MeinTagClient canSculpt={canSculpt} ... />
        │
        ▼
mein-tag-client.tsx (Client-Component)
  → {!readOnly && canSculpt && <NLRuleBuilderCard canSculpt={canSculpt} />}
        │
        ▼
NLRuleBuilderCard (Client-Component)
  → import { sculptNlRule, type SculptNlRuleResult }
  → form onSubmit → startTransition(async () => await sculptNlRule(fd))
        │
        ▼
sculpt-nl-rule.ts ("use server" Server-Action)
  → getProfile() role-guard → ok:false forbidden ODER
  → sculptRule(nlInput, profile.user_id, undefined)
        │
        ▼
sculptor.ts (SLC-752 Production-deployed)
  → queryLLM (Bedrock eu-central-1 mit assertBedrockRegion)
  → JSON-Parse + heal-Fallback + zod-Validate
  → audit_log INSERT (best-effort)
  → return SculptResult
        │
        ▼
sculptNlRule wraps → return { ok: true, result }
        │
        ▼
NLRuleBuilderCard setActionResult → Re-Render mit Karte 2+3+Cost
```

Alle 6 Links inspiziert.

| Link | Status | Notes |
|---|---|---|
| page → MeinTagClient | ✓ | Prop passed, default false |
| MeinTagClient → Card | ✓ | Doppel-Guard (Mount-Cond + Card-internal `return null`) |
| Card → sculptNlRule | ✓ | Type-only-Import korrekt (`import { fn, type Type }`) |
| sculptNlRule → sculptRule | ✓ | userId aus getProfile().user_id |
| sculptRule → Bedrock | ✓ | Production-getested in SLC-752 |
| Return → Re-Render | ✓ | setActionResult triggert State-Update |

`feedback_use_server_value_export_forbidden` konform: aus `"use server"`-File werden nur async function + Type exportiert; Consumer importiert mit `type`-Keyword.

**Wiring: CLEAN.**

## Acceptance Criteria Coverage

| AC | Verifikation | Status |
|---|---|---|
| AC1 Card sichtbar fuer Admin+Teamlead | page.tsx canSculpt + Mount-Block | ✓ Code PASS (Browser-Confirm = AC10) |
| AC2 Card NICHT bei Member | Doppel-Guard Server+Client | ✓ Code PASS (DOM-Assertion = AC10) |
| AC3 Role-Guard + sculptRule-Call | Vitest 6/6 Tests | ✓ PASS |
| AC4 Klarsprache-Echo | Vitest "Stage-Wechsel" + "Folge-Aufgabe" | ✓ PASS |
| AC5 Schema editierbar useState | Code: 3 Trigger + 4 Actions + Cond Add/Remove | ✓ Code PASS (Interaktion = AC10) |
| AC6 Reject-Karte rendert, Schema NICHT | Vitest | ✓ PASS |
| AC7 Cost-Display ~$0.003/~$0.006 | Vitest 3 formatBedrockCost-Tests | ✓ PASS |
| AC8 Native HTML form + useTransition (kein RHF) | Grep + Code: 0 react-hook-form/useForm, `useTransition` + `disabled={isPending}` | ✓ PASS |
| AC9 Vitest ~960+5 PASS | Default 899 (Baseline 885 + 14); test:all 1045 inkl. RLS-Suite-Regression (146 unveraendert) | ✓ PASS (Spec-Baseline-Schaetzung war 75 zu hoch, kein Funktionsproblem) |
| AC10 Browser-Smoke + Real-Bedrock-Probe | PENDING — Code im Worktree, Production-Coolify haengt auf 1de2529 ohne SLC-753 | ❓ pending Deploy |

**9 von 10 AC PASS Code-Side. AC10 strukturell deferred bis Master-Merge + Redeploy.**

## Regression Check

- TSC `npx tsc --noEmit` → **0 neue Errors**. 8 pre-existing Errors in 4 unrelated Files (useVoiceCapture.test.tsx, reverse-charge-revert.test.ts, skonto-revert.test.ts, vies-client.test.ts) unveraendert vs. RPT-446-Baseline.
- Default Vitest `npx vitest run` → **92 Files PASS, 899 Tests PASS** (RPT-446 Baseline 885 + 14 SLC-753). Dauer 62.22s.
- RLS-Suite `npm run test:rls` → **nicht erneut gefahren in dieser Session.** SLC-753 enthaelt KEINE Schema-Aenderungen, keine RLS-Policy-Aenderungen, keine neuen DB-Queries (sculptRule-Audit-Insert ist Lazy-Import in SLC-752 unveraendert). Letzte 146/146 PASS in RPT-446 same-day. Re-Run wuerde gegen Production-Coolify-DB im node:20-Container laufen — bei Production-Smoke-Window nachholbar, kein Blocker hier.

## Open Risks / Missing Checks

- **AC10 Browser-Smoke gegen Production-Bedrock** — strukturell bedingt deferred. Erfuellt zugleich AC3-Real-Bedrock-Probe-Defer aus RPT-446. Drei mögliche Pfade:
  1. **Recommended**: Master-Merge + Push → User-Coolify-Redeploy → Live-Smoke via Playwright-MCP gegen Production-Cockpit (Strategaize-Standard-Procedure).
  2. Lokaler `next dev` mit Production-ENV → Playwright-MCP-Smoke vor Master-Merge — schneller Feedback-Loop, aber lokaler Bedrock-Aufruf gegen Production-Konto.
  3. Lokaler `next dev` mit gemockter Bedrock + nur UI-Smoke — bestaetigt nur Render-Pfad, NICHT Real-Bedrock-Probe.
- **RLS-Suite Production-Re-Run** nach Master-Merge optional, aber empfohlen als Defense-in-Depth (kein DB-Schema-Touch in SLC-753, kein erwartetes Drift-Risiko).

## Readiness Assessment

**Code-Side: READY** fuer Master-Merge + Push.

**Production-Live: PENDING AC10**. Nach Master-Merge + User-Coolify-Redeploy + Live-Smoke-PASS ist Slice-Status `done`-faehig.

Bei Live-Smoke-FAIL: Hotfix-Slice gegen Production-Drift, Slice bleibt `in_progress` bis Hotfix PASS.

## Recommended Next Step

**Recommended Sequence:**

1. **Push** Branch `slc-753-mein-tag-nl-surface` zu origin.
2. **Master-Merge** Fast-Forward (Branch ist aufgrund clean Branch-from-main moeglich) + Push master.
3. **User-Coolify-Redeploy** auf neuen Image-Tag.
4. **Live-Smoke (MT-5/AC10)** via Playwright-MCP:
   - Admin-Login → /mein-tag → Screenshot Card sichtbar
   - Eingabe "Wenn ein Deal in Phase Angebot bewegt wird, leg mir eine Follow-up-Task in 2 Tagen an" + Sculpt-Klick
   - Karte 2+3 rendern + Cost-Display zeigt `~$0.003 fuer 1 Versuch`
   - Member-Login → /mein-tag → DOM-Assertion `not.toBeInTheDocument()`
5. Nach Live-Smoke-PASS: **RPT-450** Slice-Schluss + slices/INDEX SLC-753 → `done` + STATE-Sync. SLC-754 ist naechster Slice.

**Alternative Sequence (falls User vor Deploy Live-Verifikation will):**

A1. `npm run dev` lokal mit Production-ENV.
A2. Playwright-MCP-Smoke gegen localhost:3000 (echte Bedrock-Calls, echter Audit-Insert).
A3. Bei PASS: Master-Merge + Push + User-Coolify-Redeploy als Routine-Action.
A4. Live-Smoke gegen Production NUR als finalen Sanity-Check (analog Punkt 4 oben).

User waehlt.
