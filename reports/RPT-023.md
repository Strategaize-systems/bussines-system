---
id: RPT-023
date: "2026-04-04"
skill: /qa
slice: SLC-203
feature: BL-124, BL-125
type: completion
status: completed
result: "PASS — Deal-Rotting-Indikatoren + Required Fields Validierung bei Stage-Wechsel"
title: "QA SLC-203: Deal-Rotting + Required Fields"
---

## QA Report — SLC-203

### Result: PASS

### Acceptance Criteria — All passed
- Deal-Rotting: Cards mit >7d in Stage → gelber Rand + Ring
- Deal-Rotting: Cards mit >14d in Stage → roter Rand + Ring
- "Stagniert X Tage" Badge mit passendem Icon (Clock/AlertTriangle)
- Rotting berechnet aus `updated_at` (jede Deal-Aktivität resettet Timer)
- Required Fields: STAGE_REQUIRED_FIELDS Mapping für Unternehmer-Pipeline
- Validierung: Deal-Wert Pflicht ab "Angebot vorbereitet"
- Validierung: Kontakt Pflicht ab "Verhandlung / Einwände"
- Validierung: Verlustgrund Pflicht bei "Verloren"
- Error Banner mit Dismiss-Button bei fehlgeschlagenem Stage-Wechsel
- Optimistic Update wird bei Validierungsfehler revertiert
- Build: OK (no errors)

### Stub Detection: Clean
- No TODO/FIXME/HACK comments
- No console.log/debugger statements
- No empty handlers
- No hardcoded credentials

### Wiring: All 5 steps verified
1. KanbanCard receives `deal.updated_at` → `getDaysInStage()` calculation ✓
2. KanbanCard renders conditional border + badge based on days ✓
3. KanbanBoard `handleDragEnd` → calls `moveDealToStage()` ✓
4. `moveDealToStage` validates via STAGE_REQUIRED_FIELDS → returns error string ✓
5. KanbanBoard receives error → reverts `setDeals(initialDeals)` + shows banner ✓

### Low
- **Unused import**: `useCallback` imported in kanban-board.tsx but never used.
- **Multiplikatoren pipeline has no required fields**: STAGE_REQUIRED_FIELDS only maps Unternehmer-Chancen stage names. Multiplikatoren stages pass through without validation. This is correct behavior (relationship pipeline has no deal-value requirements), but worth documenting.
- **Rotting reset**: Any deal field update resets `updated_at` and thus the rotting timer. Editing a tag or note clears the "stagnating" indicator. Acceptable since any activity = not stagnating.

### Next Step: SLC-204 (Pipeline-Filter + Forecast)
