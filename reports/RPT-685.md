---
id: RPT-685
date: "2026-07-08"
skill: /go-live
slice: SLC-915
feature: FEAT-927
type: completion
status: completed
reviewOf: null
result: "Go-Live V8.17 — CONDITIONAL GO. Alle Entry-Conditions erfüllt (Gesamt-/qa RPT-683, Final-Check CONDITIONALLY READY RPT-684, 0 Blocker, Single-Branch main clean). GO unter 3 akzeptierten /deploy-Bedingungen: MIG-055 Live-Apply+DB-Verify, funktionaler Feature-Flow-Smoke, Header-Baseline. Internes Workflow-Closure, kein Customer-Live."
title: "Go-Live-Entscheidung V8.17 (SLC-915 Regressions-Fix-Bundle)"
---

# Go-Live-Entscheidung — V8.17 (SLC-915 Regressions-Fix-Bundle)

Formale Release-Entscheidung nach Final-Check (RPT-684 CONDITIONALLY READY). **Internes Workflow-Closure** gegen das eigene Repo (module-lifecycle-discipline) — KEIN Customer-Go-Live, keine Pilot-/Outreach-/Legal-Pre-Live-Schritte.

## Release target considered
- **Version:** V8.17 (SLC-915, 6 MTs, MIG-055). Commit `a425307` (main).
- **Target:** Coolify/Hetzner self-hosted Production-Stack, Single-Branch `main`. Auto-Deploy-Konvention gilt beim Push.
- **Delivery Mode:** internal-tool, Internal-Test-Mode (nur Founder als Nutzer, keine Kundendaten).

## Entry conditions — alle erfüllt
- **Gesamt-/qa V8.17 PASS:** RPT-683 (Cross-Cut aller 6 MTs, MIXED → PASS-CODE/PENDING-LIVE, Suite 1636/1636).
- **Final-Check-Report vorhanden:** RPT-684, Verdikt **CONDITIONALLY READY**, alle 3 Mechanical Gates PASS.
- **Blocker:** keine (Final-Check 0 Blocker/0 High).
- **Pre-Merge-State clean:** Single-Branch `main`, HEAD = Tip (`a425307`), **kein Worktree, kein unmerged Branch** → keine Pre-Merge-Re-Check-Fragmentierung. Precedent V8.12/14/15/16.

## Readiness decision: **CONDITIONAL GO**

V8.17 ist freigegeben zum Deploy. Der Code-Stand ist vollständig verifiziert (tsc0, Suite 1636/1636, build0, 0 Stubs, 0 neue Lint-Fehler, CSP-Boundary test-gelockt, MIG-055 in-tx 8/8). Die verbleibende Verifikation ist **deploy-inhärent** und wird als 3 akzeptierte /deploy-Gates mitgetragen (unten). Kein offener Code-Fix.

## Blockers
- **Keine.**

## Accepted risks
- **AR-1:** 5 browser-/live-gebundene ACs (915-1/2/4/6/7) sind erst nach Deploy verifizierbar → als /deploy-Gate-2 mitgetragen (nicht go-live-blockierend, da vor Deploy technisch nicht ausführbar).
- **AR-2:** MT-5-Kompensations-Branches Multi-User-latent (heute is_admin-maskiert, 0 Impact im Founder-Mode) → Live-Exercise beim Multi-User-Start.
- **AR-3:** Voller `/security-audit` (Fable-5) auf den Gate VOR Multi-User-/Customer-Übergang gelegt (nicht dieser interne Hardening-Release; security-relevante MTs bereits per-MT adversarisch verifiziert; V8.17 verbessert die Posture). Übernommen aus RPT-684 AR-1.
- **AR-4:** ISSUE-143/144 (Low) deferred.

## Required fixes before release
- **Keine.**

## Deploy Gates handed to /deploy (verifizierbar wie formuliert)

### Gate A — DB/Test-Regression (MIG-versioned)
- **Vitest-Regression:** 0 neue Failures in der von V8.17 berührten Surface. Baseline = Suite **1636/1636** grün auf `a425307` (bereits erreicht, pre-deploy). Kein „ganze DB-Suite gegen Prod grün"-Anspruch.
- **MIG-055 Live-Apply + DB-Verify** (node:20-Sidecar SAVEPOINT, Precedent MIG-054, `sql-migration-hetzner.md`): **positiv** = Status-Update mit unveränderten FKs auf mixed-owner-Row als Non-Admin PASST; **negativ** = UPDATE mit FK-Change auf unsichtbaren Parent → BLOCK `insufficient_privilege`; **INSERT strikt** = Cross-Tenant-FK-Insert → BLOCK; **service_role** unberührt. (In-tx 8/8 vorverifiziert RPT-680 — hier gegen live-applied Schema zu bestätigen.)
- MIG-055 ist idempotent (`CREATE OR REPLACE` / `DROP … IF EXISTS`); Rollback-Notes im File.

### Gate B — Funktionaler Feature-Flow-Smoke (BLOCKING, gegen deployed main)
Vorbedingung: `SOURCE_COMMIT` des Containers == `a425307` (Smoke NICHT gegen Branch-/Alt-Deploy). Playbook `security-headers-live-smoke.md`, IMP-1401. `curl -I`-only oder Hydration-only ist **KEIN** PASS.
1. **Telefonie (AC-915-1):** Softphone-Call-Flow → WSS verbindet, Mikrofon-Zugriff da, **0** `connect-src`/`Permissions-Policy`-Console-Errors auf dem Call-Flow.
2. **Meeting (AC-915-2):** „Meeting starten" (Mein Tag) → `window.open` öffnet Jitsi (separater Origin), **kein** Permissions-Block.
3. **E-Mail-Viewer (AC-915-7):** Inbound-Mail mit Remote-Bild → Bilder **blockiert** + Banner; „Bilder laden" → iframe `src=/api/emails/<id>/body`, Bilder **rendern** (Netzwerk: direkt, kein Proxy), Höhe korrekt unter `sandbox="allow-same-origin"`. RLS: fremde id → **404**, ohne Session → **401**.
4. **MeetingPrepCard (AC-915-4):** Fehlerfall → sichtbare Inline-Meldung statt Dead-Button.

### Gate C — Header-Baseline + Stack-Health
- `curl -I <prod-URL>` → enforced `Content-Security-Policy` (Report-Only 0); `connect-src` enthält `wss://sip.strategaizetransition.com`; `Permissions-Policy: …microphone=(self)…camera=()…`; globale `img-src` weiterhin ohne generisches `https:`.
- `node tests/_probe/csp-check.mjs <URL>` → Hydration-Baseline, 0 CSP-Console-Errors.
- Alle Container `(healthy)` (nicht nur `running`); Reverse-Proxy routet auf den gesunden Container.

**Bei FAIL eines Gates:** Hotfix-Path — `csp.ts`/`next.config.ts` reapply + Coolify-Redeploy (`coolify-autonomous-redeploy.md`); MIG-055 reapply via `sql-migration-hetzner.md`. Kein Weiterschalten auf /post-launch bis alle 3 Gates grün.

## Operational concerns
- Push ist bis zu dieser GO-Entscheidung gehalten worden → /deploy pusht das V8.17-Bundle (`26e56a6..a425307`) gebündelt und triggert Auto-Deploy.
- Kein Customer-Live, keine externen Tester (module-lifecycle-discipline, IMP-950). Last Stable bleibt V8.16 bis /post-launch T+24h PASS.

## Project record updates
- `docs/STATE.md` — Current Focus: Go-Live V8.17 CONDITIONAL GO (RPT-685); NEXT = /deploy.
- `reports/RPT-685.md` — dieser Report.
- `slices/INDEX.md` — SLC-915 bleibt `in_progress` (Live offen bis /deploy-Gates grün).
- RELEASES REL-052 wird beim /deploy geschrieben (nicht hier).

## Recommended next step
**`/deploy V8.17`** — Push `26e56a6..a425307`, MIG-055 Live-Apply (Gate A), funktionaler Feature-Flow-Smoke (Gate B) + Header-Baseline/Health (Gate C). Bei 3× PASS → REL-052 + slices/features → `deployed`. Danach `/post-launch` T+24h (dann Last-Stable-Flip V8.16→V8.17). Kein Customer-Live.
