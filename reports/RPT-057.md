---
id: RPT-057
date: "2026-04-10"
skill: /qa
slice: SLC-318
feature: FEAT-106, FEAT-107, FEAT-101, FEAT-102
type: completion
status: completed
reviewOf: null
result: "QA PASS — Alle 8 Acceptance Criteria erfuellt. Wiring-Gap gefixt (Templates in EmailSheet)."
title: "QA SLC-318 Templates + Duplikate + Attribution"
---

# QA Report — SLC-318 Templates + Duplikate + Attribution

## QA Scope
- Slice: SLC-318 — Templates + Duplikate + Attribution
- Version: V3.1
- Type: Code-Level QA
- Files reviewed: 10 geaenderte/neue Dateien + 1 SQL-Migration
- Method: Acceptance Criteria Verification, Stub Scan, Wiring Verification

## Result: PASS

Alle 8 Acceptance Criteria erfuellt. 1 Wiring-Gap gefunden und sofort gefixt.

## Acceptance Criteria

| AC | Pruefung | Status |
|----|----------|--------|
| 1 | Templates CRUD (Erstellen, Bearbeiten, Loeschen) | PASS |
| 2 | Jedes Template hat DE/NL/EN Tab | PASS |
| 3 | Platzhalter beim Einfuegen ersetzt | PASS |
| 4 | Kontakt-Anlage mit bestehender E-Mail → Warnung | PASS |
| 5 | Firmen-Anlage mit bestehendem Namen → Warnung | PASS |
| 6 | "Trotzdem anlegen" umgeht Warnung | PASS |
| 7 | Kontakte + Firmen haben strukturiertes source-Dropdown | PASS |
| 8 | Source-Werte konsistent filterbar | PASS |

## Wiring Verification

| Flow | Status | Details |
|------|--------|---------|
| Templates CRUD | WIRED | Settings → getEmailTemplates → TemplatesConfig → CRUD Actions → DB |
| Template in Email Compose | WIRED (nach Fix) | EmailsPage laed Templates → EmailsClient → EmailSheet → EmailCompose mit Placeholder-Ersetzung |
| Duplikat-Erkennung | WIRED | ContactForm/CompanyForm → onBlur → checkDuplicate → DuplicateWarning |
| Source/Attribution | WIRED | Form Dropdowns → createContact/createCompany Actions → DB Spalten |

## Findings waehrend QA

### Medium (gefixt)
- **Wiring-Gap: Templates nicht in EmailSheet durchgereicht**: EmailsPage und EmailsClient haben Templates nicht an EmailSheet weitergegeben. Fix: EmailsPage laed Templates parallel, EmailsClient reicht sie an EmailSheet weiter. Gefixt in gleicher Session.

### Low
- **Templates in Deal-Actions EmailSheet fehlen**: EmailSheet wird auch von Deal-Actions, Mein Tag und Kontakt-Detail aufgerufen — dort werden Templates nicht durchgereicht. Fuer internes Tool akzeptabel: Hauptzugang ueber /emails ist abgedeckt. Kann spaeter erweitert werden.

## Stub Scan
Alle 8 Dateien clean. Keine TODO, FIXME, Placeholder Returns, Empty Handlers oder Hardcoded Values.

## Deployment-Voraussetzung

```sql
-- Auf Hetzner ausfuehren (nach SSH + psql):
CREATE TABLE IF NOT EXISTS email_templates (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  title TEXT NOT NULL,
  subject_de TEXT, subject_nl TEXT, subject_en TEXT,
  body_de TEXT, body_nl TEXT, body_en TEXT,
  placeholders JSONB DEFAULT '[]',
  created_by UUID,
  created_at TIMESTAMPTZ DEFAULT now()
);
ALTER TABLE email_templates ENABLE ROW LEVEL SECURITY;
CREATE POLICY IF NOT EXISTS authenticated_full_access ON email_templates FOR ALL TO authenticated USING (true) WITH CHECK (true);
GRANT ALL ON email_templates TO authenticated;
ALTER TABLE companies ADD COLUMN IF NOT EXISTS source_type TEXT;
ALTER TABLE companies ADD COLUMN IF NOT EXISTS source_detail TEXT;
ALTER TABLE contacts ADD COLUMN IF NOT EXISTS source_detail TEXT;
CREATE INDEX IF NOT EXISTS idx_contacts_email ON contacts(email);
CREATE INDEX IF NOT EXISTS idx_companies_name_lower ON companies(LOWER(name));
```

## Recommended Next Step

Weiter mit SLC-319 (Activity-Queue / Focus View) — letzter Slice von V3.1.
