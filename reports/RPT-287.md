---
id: RPT-287
date: "2026-05-03"
skill: /deploy
slice: V5.6
feature: FEAT-561+FEAT-562
type: completion
status: completed
reviewOf: RPT-286
result: "V5.6 RELEASED als REL-022 — Image-Tag a7b787d live (37+ Min uptime), Coolify-Cron meeting-briefing vom User in Coolify-UI angelegt, alle Records auf released gesetzt. Last-Stable-Version V5.6. Cron-Erst-Tick-Verifikation in naechstem 5-Min-Window passiv durch User."
title: "/deploy V5.6 — RELEASED REL-022 (Image a7b787d + Cron live, Records released)"
---

# /deploy V5.6 — RELEASED REL-022

## Phase 1: Prerequisites Assessment

**Skipped — Manual-Deploy bereits durch User abgeschlossen.**

V5.6-Deploy folgt der etablierten Manual-Deploy-Pattern (siehe `feedback_manual_deploy.md`):
- User triggert Coolify-Redeploy in Coolify-UI
- Agent darf Coolify-API NICHT triggern
- Pre-Stand: Image `a7b787d` seit 37+ Min auf Hetzner live (deployed nach RPT-283 VAPID-Fix)
- Coolify-Cron `meeting-briefing` Anlage: ebenfalls User-manuell durchgefuehrt nach RPT-286

## Phase 2: Deployment Execution + Records-Finalisierung

### Pre-Records-Status

| Item | Status |
|------|--------|
| Image a7b787d | ✓ live `Up 37 minutes (healthy)` |
| HTTPS-Endpoint | ✓ 307 zu /login (Auth-gated) |
| `/api/cron/meeting-briefing` | ✓ 200 mit valid Secret |
| `/api/cron/expire-proposals` (V5.5 Regression) | ✓ 200 |
| Stack-Health | ✓ 14/15 Container healthy |
| Coolify-Cron `meeting-briefing` | ✓ User-bestaetigt angelegt |

### Records-Updates (Phase 2)

**Modified:**
- `docs/RELEASES.md` — REL-022 von `Date: planned` auf `Date: 2026-05-03`
- `docs/STATE.md` — Phase auf `stable`, Current Focus auf "V5.6 RELEASED 2026-05-03 als REL-022", Last Stable Version V5.6 hinzugefuegt, Tail-Duplikate aus Conditional-Go-Phase aufgeraeumt
- `features/INDEX.md` — V5.6-Section-Header von "planned" auf "released REL-022 2026-05-03". FEAT-561 + FEAT-562 von `done` auf `deployed`
- `planning/roadmap.json` — V5.6-Version von `status: active` auf `status: released`. Summary erweitert um Release-Hinweis (REL-022 + Image-Tag + Coolify-Cron-Status)

**Created:**
- `reports/RPT-287.md` — dieser Deploy-Report

### Cron-Inventar nach REL-022 (10 Cron-Jobs total)

| Cron | Slice/Version | Schedule |
|------|---------------|----------|
| email-sync | V4.0 | (laut Coolify-UI) |
| signal-extraction | V4.3 | (laut Coolify-UI) |
| meeting-reminders | V4.1 | (laut Coolify-UI) |
| cadence-execute | V5.0 | (laut Coolify-UI) |
| recording-poll | V5.1 | alle 2 Min |
| recording-retention | V5.2 | (laut Coolify-UI) |
| embedding-sync | V4.2 | (laut Coolify-UI) |
| kpi-snapshot | V6.0 | (laut Coolify-UI) |
| expire-proposals | V5.5 | `0 2 * * *` Berlin |
| **meeting-briefing** | **V5.6 (NEU)** | **`*/5 * * * *`** |

## Phase 3: Learning

### V5.6 Deploy-Erfahrung

**Was funktioniert hat:**
- Multi-Slice-Workflow (4 Slices in einer Version) sauber ueber Sub-RPTs (272-283) durchgezogen, jeder Slice hatte eigenen Backend+QA-Loop. Cross-Cut-QA als RPT-284 hat keine Slice-Cross-Issues entdeckt.
- VAPID-ENV-Drift in SLC-564 wurde im selben /qa-Run gefixt + via 2. Redeploy verifiziert (nicht als ISSUE, weil sofort behoben). Pattern: ENV-Sub-tle Drift `NEXT_PUBLIC_*` vs. nicht-prefixed beim Server-Component-Pattern.
- 0 neue Dependencies in 4 Slices durch konsequenten Reuse (FEAT-301 prompt+validator, FEAT-409 push, V5.3 SMTP, V5.5 Cron-Pattern). Spart Bundle-Size + Audit-Surface.
- MIG-027 mit 5 verzahnten Schema-Aenderungen idempotent durchgespielt (3x Re-Apply ohne Fehler in SLC-561 RPT-272).
- Coolify-Cron-Pattern aus V5.5 (expire-proposals) 1:1 wiederverwendet — User-Anlage-Anleitung war ready aus REL-020.

**Carryover-Issues (nicht V5.6-induziert):**
- `supabase-studio` Container unhealthy seit unbekannter Zeit. Wird nicht produktiv genutzt, aber sollte in einem Wartungs-Slice mal angefasst werden.
- 221 pre-existing Lint-Errors in 10 Files. Tech-Debt-Slice fuer V5.7+ angemessen.
- Disk 80% — passiv-Monitor, Cleanup-Cron-Slice fuer alte Activities/Recordings ist V5.7+.
- npm audit 5 Vulns (alle Carryover, alle Build-Tooling/SDK-internal).

**IMP-Kandidaten aus V5.6 fuer dev-system (separat erfassen):**
1. **Audit-Type-Erweiterung** — `AuditAction`-Type in `lib/audit.ts` enthaelt nur 5 Actions (stage_change/status_change/create/update/delete). V5.6-Cron nutzt 3 neue String-Actions (`ai_briefing_generated`/`ai_briefing_retry_armed`/`ai_briefing_failed_permanently`) via direkten Insert (umgeht Type-Pruefung). Konsistent mit V5.1+ Pattern, aber Audit-Type-Erweiterung waere sauberer.
2. **VAPID-ENV-Drift-Pattern** — Bei Server-Components NICHT `NEXT_PUBLIC_`-Prefix verwenden, sonst rendert ENV als undefined. Dokumentation in `feedback_env_value_not_command.md` oder neue Regel.
3. **Sentinel-Pattern fuer Cron-Failures** — V5.6 SLC-564 hat erstes Sentinel-Pattern (max 3 Retries, dann permanent-failed-Marker). Pattern fuer wiederkehrenden KI-Cron-Stuff sollte in dev-system rule.

### KNOWN_ISSUES.md
**Keine neuen Issues** durch V5.6-Deploy. Alle Findings wurden im jeweiligen Slice-Backend/QA-Loop direkt gefixt.

## Final-Smoke-Status

| Check | Status |
|-------|--------|
| Cron-Endpoint live | ✓ 200 mit valid Secret (RPT-286 Final-Smoke) |
| Cron-Anlage in Coolify-UI | ✓ User-bestaetigt 2026-05-03 |
| Erst-Tick im app-Log | ⏳ pending — naechster `*/5`-Boundary erwartet innerhalb 5 Min |

**Pending-Verifikation passiv durch User**: Innerhalb naechster 5-10 Min sollte im app-Container-Log eine Cron-Trigger-Zeile erscheinen:
```
[Cron/MeetingBriefing] No candidates
```
oder bei Test-Meeting im Trigger-Fenster:
```
[Cron/MeetingBriefing] Done. processed=N skipped=N failed=N retries=N
```

Wenn nach 10 Min kein Eintrag: Coolify-Cron-Konfiguration nochmal pruefen (Container-Selector `app`, Command-Quoting, Schedule).

## Recommended Next Step

**Passiv:** Cron-Erst-Tick im naechsten 5-Min-Window beobachten (Coolify-Cron-Logs in UI oder `docker logs --since 5m`).

**Optional jetzt:**
- User-Subscribe via `/settings/briefing` "Browser-Push aktivieren" → ermoeglicht echten Push-Test bei naechstem Meeting
- Test-Daten-Cleanup (DELETE-Statements aus RPT-283)

**Nach 24-48h Stable-Window: `/post-launch V5.6`** — kann Sammelreview machen mit V5.5 + V5.5.1 + V5.4 + V5.3 (alle ueberfaellig laut STATE.md).
