---
id: RPT-656
date: "2026-06-14"
skill: /deploy
slice: SLC-913
feature: FEAT-925
type: completion
status: completed
reviewOf: null
result: "Deploy V8.15 SLC-913 SUCCESS (REL-050). App 3ab8121 healthy, MIG-052+053 live appliziert + 5/5 Trigger-Probe + Export-Tenant-Scope-Smoke PASS, per-Tenant-Key provisioniert. ISSUE-109/116/122 live resolved. OFFEN operativ: LEAD_INTAKE_API_KEY-ENV fehlt (intake 500), System-4-Bearer-Switch, Disk 89%. NICHT stable bis /post-launch T+24h."
title: "Deploy V8.15 Security-Hardening — SUCCESS (REL-050)"
---

# Deploy V8.15 — SLC-913 Security-Hardening Bundle (REL-050)

## Phase 1 — Prerequisites (confirmed)
Stack: Next.js + self-hosted Supabase (Coolify + Hetzner, 91.98.20.191). Update-Deploy. Gate V8.14 STABLE erfüllt (RPT-655). Export-Key-Owner = Founder `96322a0a…` (richard@bellaerts.de). Host build-tight (ISSUE-108) → Jitsi-Stop-Mitigation geplant. Founder-Freigabe Option A.

## Phase 2 — Execution

| Step | Ergebnis |
|---|---|
| MIG-052 live-apply (postgres, Base64) | ✅ CREATE FUNCTION/TRIGGER, Trigger tgtype 19→**23** (BEFORE INSERT OR UPDATE) |
| MIG-053 live-apply | ✅ Tabelle export_api_keys, RLS forced **t\|t**, GRANT nur postgres+service_role, REVOKE anon/authenticated verifiziert |
| Per-Tenant-Key provisionieren | ✅ INSERT 0 1 (nur SHA-256-Hash gespeichert, owner=Founder, label system-4) |
| Jitsi-Stop (OOM-Mitigation) | ✅ 5 Container gestoppt → mem-avail 2,1→2,9 Gi |
| `git push` 7a7b225..3ab8121 (21 Commits) | ✅ PUSH_EXIT=0 |
| Coolify-Redeploy (Founder-manual, kein Webhook) | ✅ neuer App-Container `app-…-102609558933` Image `3ab8121…` healthy, RestartCount=0, "✓ Ready" |
| Jitsi-Restart | ✅ 5/5 zurück, voller Stack healthy |

### Live-Verify

**MIG-052 transaktionale psql-Probe (BEGIN..ROLLBACK, nichts persistiert) — 5/5 PASS:**
- authenticated UPDATE team_id → **BLOCKED** (ISSUE-109)
- authenticated UPDATE role → **BLOCKED** (MIG-051-Regression)
- authenticated UPDATE non-authz `team` → **OK** (kein Kollateral-Block)
- service_role UPDATE team_id (existing team) → **OK** (R-913-1 legit Admin-Pfad)
- authenticated INSERT role='admin' → **BLOCKED** (ISSUE-122)

**App-Live-Smokes (extern gegen business.strategaizetransition.com):**
- export/deals WRONG key → **401** ✅; NO auth → **401** ✅; CORRECT key → **200** + founder-scoped (`data:[], total:0`, kein Cross-Owner-Leak) ✅ (AC-913-7 ISSUE-116 live)
- test-sentry (anon) → **307→login** ✅ (nicht anonym exponiert; Middleware-gated, Ziel ISSUE-110 erreicht)
- push-subscribe (anon) → **307** ✅ (auth-gated by design; SSRF-Check für authed User, durch Unit-Tests gedeckt)
- leads-intake (no key) → **500** ⚠️ (ENV `LEAD_INTAKE_API_KEY` fehlt im Container → fail-closed/safe, aber funktional offen)

### Records
KNOWN_ISSUES ISSUE-109/116/122 → resolved (Live-Resolution-Notes). REL-050 angelegt. roadmap v8-15 → released. slices SLC-913 → deployed. features FEAT-925 → deployed. backlog BL-516 → done. STATE.md aktualisiert. (MIG-052/053 bereits in MIGRATIONS.md aus /backend.)

## Phase 3 — Learnings / Problems

- **LEAD_INTAKE_API_KEY ENV fehlt** im laufenden Container (length 0) → `/api/leads/intake` 500 statt 401. Coolify-ENV-Änderungen brauchen Redeploy; ENV war beim Build nicht gesetzt. **Follow-up Founder:** ENV in Coolify setzen + redeploy → dann 401/funktional. (Bestätigt ISSUE-095-Klasse: ENV-Drift zwischen .env.example und Coolify.)
- **Disk 82%→89%** (32G/38G, 4,3G frei) durch neuen Image-Layer. ISSUE-108-Eskalation. **Follow-up:** `docker image prune` (altes e0d5e28-Image freigeben).
- **Coolify kein Auto-Webhook** (ISSUE-095 erneut) — Push triggert keinen Build, Founder-Manual-Redeploy nötig. Bekannt.
- **Migrationen + Key überleben Coolify-Full-Stack-Redeploy** (DB-Volume persistent) — Post-Redeploy-Probes liefen gegen den neuen DB-Container und bestätigten Trigger + Key live. Bestätigt: Reihenfolge „Migration vor Redeploy" ist sicher.
- DB-Verify via vitest-Harness NICHT gefahren (`/opt/business-system-test` existiert nicht; Host disk-tight). Stattdessen äquivalente transaktionale psql-SAVEPOINT-Probe (Präzedenz V8.14 MT-1). Für künftige Deploys: Test-Dir-Setup als Einmal-Schritt erwägen.

## Outcome
**Deploy SUCCESS — REL-050.** Alle Security-Findings ISSUE-109..121 jetzt live geschlossen (Code seit per-MT, DB-Findings jetzt live-verifiziert). V8.15 ist DEPLOYED, **NICHT stable bis /post-launch T+24h** (IMP-950). Last Stable bleibt V8.14.

## Recommended next step
**`/post-launch V8.15`** (T+3h Light + T+24h Full → STABLE). Davor/parallel Founder-Follow-ups: (1) Coolify-ENV `LEAD_INTAKE_API_KEY` + Redeploy, (2) System-4 auf Bearer-Export-Key, (3) `docker image prune` (Disk).
