---
id: RPT-629
date: "2026-06-12"
skill: /backend
slice: SLC-355
feature: FEAT-872
type: completion
status: completed
reviewOf: null
result: "SLC-355 BS->IS Verdichtungs-Cron /backend Code-Side KOMPLETT: alle 6 MTs (TDD). TSC=0, ESLint=0/0, Vitest 1457/1457 GREEN, next build OK. Founder-Override DEC-290 min_bucket Default 1 (kein Skip). Keine Migration. Next: /qa SLC-355."
title: "V8.7-B BS->IS Verdichtungs-Cron — /backend"
---

# /backend — SLC-355 V8.7-B BS→IS Verdichtungs-Cron

## 1. Outcome Summary
Alle 6 Micro-Tasks Code-Side **vollständig** implementiert (TDD, RED→GREEN je MT), keine Migration. Ein wöchentlicher Cron (`POST /api/cron/knowledge-push`) verdichtet succeeded Win/Loss-Analysen + Activity-Einwand-Notizen der letzten 7 Tage zu anonymisierten, aggregierten Wissens-Bausteinen (Branche × Deal-Größe) und pusht sie an die live IS-Knowledge-API. **Status: Code-Side complete, /qa pending** (Slice bleibt `in_progress`).

**Quality-Gates:** TSC=0 · ESLint=0/0 (touched) · Vitest Full-Suite **1457/1457 GREEN** (158 Files, +57 neue Tests, 0 Regression) · `next build` exit 0 (Route `ƒ /api/cron/knowledge-push` registriert).

## 2. Files reviewed
- `/slices/SLC-355-v87b-bs-is-verdichtungs-cron.md` (Spec, Source of Truth)
- `/docs/DECISIONS.md` (DEC-289/290/291)
- `/cockpit/src/lib/is-knowledge/{client,types,redact-pii}.ts` + `__tests__/client.test.ts` (Mirror-Vorlage)
- `/cockpit/src/lib/ai/bedrock-client.ts` (`queryLLM`-Signatur)
- `/cockpit/src/lib/automation/sculptor-cost.ts` (`calculateSculptCost`) + `sculptor.ts` (audit-context-Präzedenz)
- `/cockpit/src/app/api/cron/meeting-summary/route.ts` + `/cockpit/src/app/api/cron/verify-cron-secret.ts` + `/cockpit/src/app/api/cron/click-log-cleanup/{route,route.test}.ts` (Cron + audit_log + Mock-Konvention)
- `/cockpit/src/lib/supabase/admin.ts` (`createAdminClient`)
- `/cockpit/sql/01_schema.sql`, `/cockpit/sql/08_v3_schema.sql`, `/cockpit/sql/13_v41_migration.sql`, `/cockpit/sql/migrations/032_v66_working_hours_and_winloss.sql` (Daten-Modell)
- `/cockpit/vitest.config.ts` (Include-Glob)

## 3. Files changed (Modified — additiv)
- `/cockpit/src/lib/is-knowledge/types.ts` — `IsKnowledgeIngestItem`-Interface + `IsKnowledgeIngestResponseSchema`/`IsKnowledgeIngestResult` ergänzt. Warum: Wire-Shape für den Ingest-Endpoint; V8.7-A-Exporte unberührt.
- `/cockpit/src/lib/is-knowledge/client.ts` — `ingestKnowledge(items)` ergänzt (Mirror `searchKnowledge`), `startRequestController` um optionalen `timeoutMs`-Param erweitert (Default unverändert → searchKnowledge/getKnowledgeItem unbeeinflusst), Ingest-Typen re-exportiert. Warum: MT-1 POST-Client mit 200/207/401/429/400/5xx-Handling + Batch-Guard ≤100.

## 4. Files created
**Implementation (`/cockpit/src/lib/knowledge-push/`):**
- `config.ts` — ENV-Helper: `getMinBucket()` (Default **1**, Founder-Override), `getSizeThresholds()`/`sizeBucketOf()` (Default 10k/50k EUR), Window/Notes-Konstanten.
- `types.ts` — `WinLossBucket`, `ObjectionGroup`, `TargetStatus` (keine PII-Identifier-Felder).
- `aggregate-winloss.ts` (MT-2) — `aggregateWinLoss(admin, opts)` pure/read: succeeded Runs (7d) → Branche×Größe×won/lost-Buckets, k-min-Filter.
- `gather-objections.ts` (MT-2) — `gatherObjectionNotes(admin, opts)` pure/read: Activity-Notizen pro Branche, k-min-Filter, Notes-Cap.
- `distill.ts` (MT-3, R-355-1) — `distillWinLossBucket`/`classifyObjections` via `queryLLM` (DI), temp 0.2, Anonymisierungs-System-Prompt, pre-Prompt-Redact, fail-soft, BS-lokale Cost via `calculateSculptCost`.
- `build-items.ts` (MT-4) — `buildWinLossItem`/`buildObjectionItem` + `isoWeekOf` + `slugifyBranche`; deterministisches week-stamped `source_reference`.
- `redact.ts` (MT-5) — `redactPiiString` + `redactItemsBeforeSend` (Email/Telefon-Strip, non-destruktiv).
- `__tests__/aggregate.test.ts` (12), `__tests__/distill.test.ts` (10), `__tests__/build-items.test.ts` (9), `__tests__/redact.test.ts` (6).

**Cron (`/cockpit/src/app/api/cron/knowledge-push/`):**
- `route.ts` (MT-6) — pure `runKnowledgePush(deps, now, runId?)` (orchestriert MT-2→3→4→5→ingest, Chunks ≤100, fail-soft, 1 audit_log-Row) + thin `POST` (verifyCronSecret + `KNOWLEDGE_PUSH_ENABLED`-Gate + Wiring), `maxDuration=300`.
- `route.test.ts` (10).

**Tests gesamt: 57 (10 ingest + 12 aggregate + 10 distill + 9 build-items + 6 redact + 10 route), alle GREEN.**

## 5. Files intentionally not changed
- `/cockpit/src/lib/is-knowledge/redact-pii.ts` — bewusst nicht geteilt/refactored; Pattern in `knowledge-push/redact.ts` re-implementiert (item-orientiert vs. query-orientiert), Surgical-Changes-Disziplin.
- Keine Migration / kein SQL — per DEC-289 (audit_log.actor_id bereits nullable, IS-Dedup, Cost in audit_log).
- `package.json` — keine neue Dependency nötig (ingestKnowledge nutzt `fetch`, Bedrock/Cost vorhanden).

## 6. Problems found
- **Spec-Drift `audit_log.context` (akzeptiert):** Spec sagte Counts in `context` als JSON. Schema-Realität: `audit_log.context` ist **TEXT**, `changes` ist **JSONB**, `entity_id` ist **UUID NOT NULL**. Gelöst nach Repo-Konvention (`click-log-cleanup`): strukturierte Counts in `changes` (JSONB), Summary-String in `context` (TEXT), `entity_id` = generierte runId-UUID. Kein neuer DEC nötig (Schema-konforme Umsetzung der Spec-Intention).
- **Cron-Secret:** Spec/Founder-Pre nannte `KNOWLEDGE_PUSH_CRON_SECRET (oder CRON_SECRET reuse)`. Umgesetzt mit geteiltem `verifyCronSecret`/`CRON_SECRET` (Strategaize-Konvention, 17 Cron-Routes) — kein separates Secret nötig.
- **R-355-1 Live-Gap (Verifikations-Limit):** Der NEUE Bedrock-Einwand-Klassifikations-Pass ist nur mit **gemocktem `queryLLM`** getestet. Anonymisierungs-Qualität + Halluzinations-Verhalten der realen LLM-Ausgabe sind **noch nicht live verifiziert** → /qa-Review des System-Prompts + /deploy-Live-Smoke-Aufgabe. Code-Side-Mitigation steht (Constrained Prompt, temp 0.2, pre-Prompt-Redact, Branchen-Aggregat-Input, fail-soft).
- **Namen-Leak-Restrisiko:** `bedrock_output`/Activity-Notizen können Firmen-/Personennamen enthalten. Email/Telefon werden pre-Prompt + pre-Send gestrippt; **Namen** werden NICHT regex-gestrippt, sondern verlassen sich auf die LLM-Anonymisierung. Akzeptiert per DEC-290 + Internal-Test-Mode (BS-eigene Vertriebsdaten, keine Mandanten-Daten). Für /qa als bewusster Posture-Punkt markiert.

## 7. Open points / deferred decisions
- **Founder-Direktive umgesetzt:** kein Skip am Anfang → `KNOWLEDGE_PUSH_MIN_BUCKET` Default 1 (DEC-290 aktualisiert). Mechanismus bleibt, später hochsetzbar.
- **Founder-Pre vor /deploy (NICHT vor /qa):** Coolify-ENV `KNOWLEDGE_PUSH_ENABLED=true` + `CRON_SECRET` (vorhanden) + Coolify-Scheduled-Task `knowledge-push` weekly So 02:00. `STRATEGAIZE_KNOWLEDGE_API_BASE_URL`/`_SERVICE_KEY` seit V8.7-A in Prod.
- **Push deferred:** Code LOKAL committed, **nicht gepusht** (BS-main-Push triggert Coolify-Rebuild) — Push nach /qa + Founder-ENV.
- V8.14 Security-Hotfix (SLC-912) bleibt bewusst nachgelagert.

## 8. Project record updates
- `/docs/DECISIONS.md` — DEC-290 Consequence um Founder-Override (min_bucket Default 1, kein Skip) + ENV-Schwellen ergänzt.
- `/slices/INDEX.md` — SLC-355 `planned` → `in_progress`.
- `/planning/backlog.json` — BL-494 Description auf /backend DONE aktualisiert (Status bleibt `in_progress`).
- `/docs/STATE.md` — High-Level State `slice-planning` → `implementing`, Current Focus auf /backend DONE.
- `/reports/RPT-629.md` — dieser Report.
- features/INDEX FEAT-872 bleibt `in_progress` (korrekt — Version nicht deployed). roadmap v8-7-b bleibt `active`. Last Stable bleibt V8.12 (IMP-950).

## 9. QA
**Mandatory next:** `/qa SLC-355` (AC-355-1..8 + Stub-Detection + Wiring-Chain + System-Prompt-Anonymisierungs-Review für R-355-1; Live-LLM-Output-Smoke ist /deploy-Bundle-Aufgabe).

## 10. Recommended next step
`/qa SLC-355` — Code-Side-QA inkl. Re-Run Quality-Gates + Stub/Wiring + besondere Aufmerksamkeit auf R-355-1 (Anonymisierungs-System-Prompt) und den audit_log-Spec-Drift. Danach: Gesamt-/qa V8.7-B → /final-check → /go-live → /deploy (Founder-ENV + Live-Push-Smoke gegen IS) → /post-launch.
