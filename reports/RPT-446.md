---
id: RPT-446
date: "2026-05-17"
skill: /qa
slice: SLC-752
feature: FEAT-751
type: completion
status: completed
reviewOf: null
result: "SLC-752 /qa PASS. TSC 8 pre-existing Errors (4 unrelated Files, alle nicht-SLC-752). Default-Vitest 885/885 PASS, 90 Files. Hetzner-RLS-Suite 146/146 PASS, 7 Files inkl. 8 SLC-752 Live-DB-Tests (sculptor-dedup-live 4 + nl-history-live 4). MIG-036 live verifiziert (NOT NULL DEFAULT click_wizard CHECK IN). Schema-Single-Source-Probe PASS (UPDATE_FIELD_WHITELIST + TriggerEvent + UserActionType direkt importiert). Stub-Scan PASS (keine TODO/FIXME/leere Catches in SLC-752-neuen Files). Keine Blocker, keine High. 4 Low/Cosmetic Findings dokumentiert. Naechster Schritt: Master-Merge slc-752-sculptor-core → main + User-Coolify-Redeploy."
title: "SLC-752 /qa Completion — Sculptor-Core + MIG-036 PASS"
---

# SLC-752 — /qa Completion-Report

## QA Scope reviewed

- **TSC noEmit** in `cockpit/` — pre-existing-Error-Klassifikation
- **Vitest Default-Suite** (`npm run test`) — Full-Regression
- **Vitest RLS-Suite** auf Hetzner via `node:20` + Coolify-Netzwerk gegen Live-Coolify-DB (`vitest.rls.config.ts`)
- **MIG-036 Live-Verifikation** auf Coolify-DB (`automation_rules.created_via` Column + Constraint + Data)
- **Schema-Single-Source-Probe** (`sculptor-schema.ts` Imports gegen V6.2-Whitelist)
- **Stub-Scan** in SLC-752-neuen Files (TODO/FIXME/HACK + leere Catches + placeholder returns + console.log/debugger)
- **Wiring-Verification** — N/A fuer diesen Slice (pure Backend-Lib ohne UI-Aufrufer, UI kommt SLC-753)
- **Snapshot-Drift-Pruefung** auf den 2 uncommitted Files in der Worktree

## Overall result

**PASS** — alle Checks gruen, keine Blocker, keine High-Findings. SLC-752 ist ready fuer Master-Merge + Coolify-Redeploy.

## Detail-Results

### TSC noEmit (cockpit)

Exit-Code 0 mit 8 Errors in 4 vorhandenen Files. **0 neue Errors in SLC-752-Code.**

Pre-existing Files mit Errors (alle V5.7/V6.5/V7-Vintage, keine SLC-752-Beruehrung):

| File | Errors | Origin |
|---|---|---|
| `cockpit/src/components/ki-workspace/hooks/__tests__/useVoiceCapture.test.tsx` | 1 | V6.6 KI-Workspace |
| `cockpit/src/lib/proposal/reverse-charge-revert.test.ts` | 1 | V5.7 Reverse-Charge |
| `cockpit/src/lib/proposal/skonto-revert.test.ts` | 1 | V5.7 Skonto |
| `cockpit/src/lib/validation/vies-client.test.ts` | 5 | V6.5 VIES |

Match mit RPT-445-Vorhersage exakt (8 = 8). Klassifizierung: **Low**, in spaeterem Hygiene-Sprint adressieren.

### Vitest Default-Suite

```
 Test Files  90 passed (90)
      Tests  885 passed (885)
   Duration  69.78s
```

**885/885 PASS**, 0 FAIL, 0 SKIP. Match mit RPT-445.

SLC-752-neue Test-Files (alle gruen):
- `bedrock-client.test.ts` 7 Tests (Region-Pin-Probe)
- `heal-json-escapes.test.ts` 15 Tests (10 heal + 5 tryParse)
- `sculptor-schema.test.ts` 28 Tests (10 valid + 5 drift + 5 malformed + 3 reject + 3 convenience + 2 standalone)
- `sculptor-cost.test.ts` 13 Tests (Pricing + Cumulative)
- `sculptor.test.ts` 17 Tests (4 Success + 2 Reject + 2 Edge + 3 Re-Prompt + 3 Audit + 2 Infra + 1 SessionId)
- `sculptor-dedup.test.ts` 8 Tests (Pure-Function Match-Probe)
- `nl-history.test.ts` 5 Tests (Parse-Robustness)

Gesamt **93 neue Tests** (vs. 779 vorher → 885 jetzt = +106; differenz +13 zum SLC-752-Count erklaert durch SLC-751-Tests die in 779-Baseline noch nicht enthalten waren).

### Vitest RLS-Suite (Hetzner gegen Coolify-DB)

Run-Command (per `feedback_coolify_test_setup`-Pattern):
```bash
ssh root@91.98.20.191 "POSTGRES_PW=\$(docker exec supabase-db-k9f5pn5upfq7etoefb5ukbcg-160512978269 printenv POSTGRES_PASSWORD); \
  docker run --rm \
    --network k9f5pn5upfq7etoefb5ukbcg_business-net \
    -v /opt/strategaize-business-system-test/cockpit:/app -w /app \
    -e TEST_DATABASE_URL=\"postgresql://postgres:\$POSTGRES_PW@supabase-db:5432/postgres\" \
    node:20 npx vitest run --config vitest.rls.config.ts"
```

Ergebnis:
```
 Test Files  7 passed (7)
      Tests  146 passed (146)
   Duration  957ms
```

Aufschluesselung:
| File | Tests | SLC-752? |
|---|---|---|
| `__tests__/team/bulk-reassign.test.ts` | 20 | nein (V7) |
| `__tests__/team/aggregate-queries.test.ts` | 6 | nein (V7) |
| `__tests__/rls/v7-rls-matrix.test.ts` | 96 | nein (V7) |
| **`__tests__/automation/nl-history-live.test.ts`** | **4** | **SLC-752 NEU** |
| `__tests__/rls/helper-functions.test.ts` | 12 | nein (V7) |
| **`__tests__/automation/sculptor-dedup-live.test.ts`** | **4** | **SLC-752 NEU** |
| `__tests__/team/drilldown.test.ts` | 4 | nein (V7) |

**8 SLC-752 Live-DB-Tests PASS** — Match mit RPT-445.

ISSUE-073 + ISSUE-074 (V7-Test-Infra-Drift) waren in der V7.2-Cleanup-Welle (REL-031) bereits behoben — `v7-rls-matrix.test.ts` 96/96 PASS bestaetigt es.

### MIG-036 Live-Verifikation

Hetzner-DB `\d automation_rules` zeigt:
```
created_via | text | not null | 'click_wizard'::text
"automation_rules_created_via_check" CHECK (created_via = ANY (ARRAY['click_wizard'::text, 'nl_sculptor'::text]))
```

Data-Probe:
```
 created_via  | count
--------------+-------
 click_wizard |     2
(1 row)
```

**AC1 verifiziert:** Column NOT NULL DEFAULT, CHECK-Constraint exakt 2-Werte-Whitelist, 2 bestehende Rules trafen DEFAULT.

### Schema-Single-Source-Probe

`cockpit/src/lib/automation/sculptor-schema.ts` Lines 12-23:
```typescript
import { z } from "zod";
import type {
  // ... TriggerEvent, UserActionType ...
} from "../../types/automation";
import { UPDATE_FIELD_WHITELIST } from "./field-whitelist";
```

**Pass:** Keine Duplicate-Constants. zod-Schemas werden aus den Single-Source-of-Truth-Constants konstruiert (Line 35 `TriggerEventSchema = z.enum(TRIGGER_EVENTS)`, Line 50 `ActionTypeSchema = z.enum(USER_ACTION_TYPES)`, Line 160-161 Iteration ueber `UPDATE_FIELD_WHITELIST`). Drift gegen V6.2-Whitelist ausgeschlossen.

### Stub-Scan

Scan auf TODO/FIXME/HACK/console.log/debugger/leere Catches in den 12 neuen SLC-752-Files:

| Datei | Stub-Pattern? | Status |
|---|---|---|
| sculptor.ts | `return null;` Line 157 in `extractCandidateJson` | OK (legitime "kein JSON gefunden"-Sentinel) |
| heal-json-escapes.ts | `return null;` Lines 110+114 in `tryParseHealedJson` | OK (legitime "Parse-Failure"-Sentinel) |
| sculptor-schema.ts | keine | sauber |
| sculptor-prompts.ts | keine | sauber |
| sculptor-cost.ts | keine | sauber |
| sculptor-dedup.ts | keine | sauber |
| nl-history.ts | keine | sauber |
| 7x Test-Files | keine | sauber |

Auffaellig (pre-existing, nicht SLC-752): `dispatcher.ts:8` enthaelt `// ... TODO unten` als Kommentar-Pointer zum bestehenden SLC-622-Executor. Kein Stub, sondern Referenz-Kommentar.

**Stub-Scan PASS** — keine Code-Stubs, keine vergessenen Debug-Statements.

### Snapshot-Drift-Pruefung

`git diff -w HEAD -- cockpit/src/lib/email/__snapshots__/render.test.ts.snap cockpit/src/lib/pdf/__snapshots__/proposal-renderer.test.ts.snap` zeigt nur LF→CRLF-Warnings ohne Content-Diff. Beide Snapshots sind funktional unveraendert. Klassifizierung: **Cosmetic**, durch lokales Git-Auto-CRLF-Verhalten verursacht — keine echte Drift, kein Merge-Risiko.

## Findings (gruppiert nach Severity)

### Blocker
*keine*

### High
*keine*

### Medium
*keine*

### Low

#### F-1 — Pre-existing TSC-Errors in 4 unrelated Files (8 Errors gesamt)

Files: `useVoiceCapture.test.tsx`, `reverse-charge-revert.test.ts`, `skonto-revert.test.ts`, `vies-client.test.ts`. Alle V5.7/V6.5/V6.6-Vintage, keine Beruehrung in SLC-752. RPT-445 hat diese explizit vorhergesagt.

Empfehlung: In V7.6-Hygiene-Welle oder als BL-Kandidat erfassen, nicht V7.5-blockierend.

#### F-2 — Slice-Spec-Pfad-Drift: `lib/llm/bedrock-client.ts` vs Real-Pfad `lib/ai/bedrock-client.ts`

Slice-Spec SLC-752-sculptor-core.md referenziert `cockpit/src/lib/llm/bedrock-client.ts`. Tatsaechlicher Pfad ist `cockpit/src/lib/ai/bedrock-client.ts` (V3-Convention). Implementation folgte dem realen Pfad — kein Code-Issue, sondern Doku-Drift.

Empfehlung: IMP-584 fuer Dev-System bereits angelegt laut RPT-445-Hinweis. Slice-Spec-Template-Konvention `lib/ai/` cementieren.

#### F-3 — BEDROCK_REGION ENV nicht explizit in Coolify gesetzt

Coolify-ENV setzt aktuell nur `AWS_REGION=eu-central-1`. Code-Pfad in `assertBedrockRegion()` mit `AWS_REGION`-Fallback ist abgesichert — daher **nicht-blockierend** fuer SLC-753-Deploy.

Empfehlung: User setzt `BEDROCK_REGION=eu-central-1` zusaetzlich in Coolify als Discipline-Massnahme bevor SLC-753 deployed wird (siehe Open Question 1 im Handoff). Verhindert dass spaeterer AWS-SDK-Region-Override durch andere Konsumenten den Fallback ueberraschend ueberschreibt.

### Cosmetic

#### F-4 — TextDecoder-Pad-Leftover in `sculptor.ts` defaultSculptDeps

`TEXT_DECODER_PAD = ""` no-op-Konstante aus historischem String-Concat-Refactor. Funktional unproblematisch. Bei naechstem Touch entfernen.

#### F-5 — Snapshot-Drift `render.test.ts.snap` + `proposal-renderer.test.ts.snap` durch LF→CRLF

Lokales Git-Auto-CRLF-Verhalten produziert wiederkehrend "M" in `git status`. Kein Content-Drift. Optional: User koennte `git update-index --skip-worktree` setzen oder `.gitattributes` mit `* text=auto eol=lf` fuer diese Files anpassen — kein Slice-Blocker, kein QA-Action-Item.

## Open Risks / Missing Checks

- **AC3-Real-Bedrock-Probe deferred** — SLC-752 hat 8 Real-World-Prompts gegen **Mock-Bedrock** verifiziert (Architecture-Standard pro RPT-445 Open Points). Erste echte Sculpt-Anfrage gegen Production-Bedrock erfolgt in /qa SLC-753 wenn die UI ihre erste Sculpt-Anfrage absetzt. Risiko: niedrig, weil zod-Schema + healJsonEscapes Drift-Robustheit abdecken; Real-Drift wird in SLC-753-Live-Smoke aufgefangen.
- **Cost-Audit erst SLC-753-relevant** — Cost-Berechnung ist Unit-getestet (13 Cases AC5 inkl. 0.0105-USD-Reference), aber Production-Bedrock-Pricing-Drift kann sich aendern. SLC-753-Live-Smoke sollte Cost-Audit erstmals gegen reale Bedrock-Usage validieren.

## Readiness Assessment

**SLC-752 ist ready fuer Master-Merge + User-Coolify-Redeploy.**

Begruendung:
- Alle 10 ACs (AC1-AC10) entweder direkt verifiziert (AC1 Live, AC4-AC9 via 93 Unit + 8 Live-DB Tests) oder pro Architecture-Standard auf Mock-Basis verifiziert (AC3 Mock-Bedrock).
- 0 Blocker, 0 High, 0 Medium Findings.
- 3 Low + 2 Cosmetic Findings sind dokumentiert und nicht-blockierend.
- TSC + Default-Vitest + RLS-Vitest auf Hetzner alle gruen.
- MIG-036 Live-DB-Smoke bestaetigt.
- Schema-Single-Source-of-Truth-Pflicht eingehalten — Drift-Schutz gegen V6.2-Whitelist aktiv.
- Pure-Backend-Lib-Slice ohne UI-Aufrufer-Pfad → kein Wiring-Risiko in diesem Slice. Wiring kommt SLC-753.

**Bedrock-Region-Pin-Effekt ist erst NACH Coolify-Redeploy aktiv.** Solange `main` nicht deployed ist, bleiben bestehende Bedrock-Konsumenten (signals/extractor, winloss, ki-workspace) ohne Region-Drift-Schutz. Deployment ist daher empfohlen, aber nicht zeitkritisch (V7.5-Pfad kommt erst mit SLC-753 zur UI-Sichtbarkeit).

## Recommended next step

1. **Master-Merge** `slc-752-sculptor-core` → `main` (Fast-Forward sollte gehen, da `main` seit SLC-751-Merge `07eb0a3` unveraendert).
2. **User-Coolify-Redeploy** auf `main` (manuell pro `feedback_manual_deploy`-Pattern). Image-Tag = post-merge Commit-Hash. Begruendung: Bedrock-Region-Pin in `bedrock-client.ts` ist erst nach Deploy aktiv.
3. **RPT-447** fuer Master-Merge + Records-Sync (slices/INDEX bleibt `done`, BL-435 description um /qa-Done erweitern).
4. **Optional**: BEDROCK_REGION=eu-central-1 in Coolify-ENV als Discipline-Massnahme (F-3) bevor SLC-753 deployed wird.
5. **Nicht in dieser Session**: SLC-753 NL-Surface — eigener Slice mit ~2-3h Scope.

## Project record updates

- `/docs/STATE.md` MOD — Current Focus + Phase + Next-Step von "SLC-752 done /qa-next" auf "SLC-752 /qa PASS → Master-Merge next" umgeschrieben
- `/docs/KNOWN_ISSUES.md` unveraendert — keine neuen Issues durch /qa entstanden
- `/slices/INDEX.md` unveraendert — SLC-752 bleibt `done`
- `/features/INDEX.md` unveraendert — FEAT-751 bleibt `in_progress` (5 V7.5-Slices folgen)
- `/planning/backlog.json` unveraendert — BL-435 bleibt `in_progress`
- `/planning/roadmap.json` unveraendert — V7.5 bleibt `active`

Cockpit-Refresh nach Master-Merge (RPT-447) empfohlen — V7.5-Progress zeigt dann 2/6 (~33%).
