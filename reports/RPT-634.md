---
id: RPT-634
date: "2026-06-12"
skill: /deploy
slice: SLC-355
feature: FEAT-872
type: completion
status: completed
reviewOf: RPT-633
result: "/deploy V8.7-B DONE als REL-048: Container 16aab52 healthy, ENVs gesetzt, Scheduled-Task aktiv. Live-Smoke Plumbing-PASS (Cron success, audit_log-Row mit actor_id=NULL/changes JSONB/context TEXT live bestätigt), 0 Items mangels Quelldaten. Deploy-Story: 2x Host-OOM (ISSUE-108), erst nach Jitsi-Stop + Swap erfolgreich. R-355-1/ISSUE-107 echte LLM-Anonymisierung offen (erster Real-Daten-Lauf). DEPLOYED, nicht stable bis /post-launch. Next: /post-launch."
title: "Deploy V8.7-B BS->IS Verdichtungs-Cron — REL-048"
---

# /deploy — V8.7-B BS→IS Verdichtungs-Cron (REL-048)

## Phase 1 — Prerequisites (Founder-confirmed)
Update-Deploy zum laufenden BS-Stack (Hetzner 91.98.20.191 + Coolify), Single-Branch `main`, **0 Migration**. Vom Founder vor Deploy bestätigt: Coolify-ENV `KNOWLEDGE_PUSH_ENABLED=true` + `CRON_SECRET` + `STRATEGAIZE_KNOWLEDGE_API_BASE_URL`/`_SERVICE_KEY` (seit V8.7-A), Scheduled-Task `knowledge-push` weekly So 02:00 angelegt, Push autorisiert.

## Phase 2 — Execution

### Push
5 lokale Commits `23eaf6e`→`16aab52` nach `origin/main` gepusht (vorher origin `d8c8504`).

### Deploy-Story (ISSUE-108 — Host-OOM, 2 Fehlversuche vor Erfolg)
1. **1. Coolify-Redeploy (Commit `d8c8504`, vor Push):** `next build` ohne Cache (15 min) + laufender Full-Stack inkl. Jitsi auf 7,6-GB-Host (Swap 97% voll) → **OOM, Host-Hänger** (SSH-Banner-Timeout, Coolify-UI 502, dmesg-OOM 10:04 killte `next-server`). Host erholte sich selbst (OOM-Killer reapte den Build, **kein Reboot nötig**), alter V8.12-Container überlebte.
2. **2. Redeploy (Commit `16aab52`):** Build diesmal **gecacht** (`#55 DONE 60.7s`, voller Route-Manifest gedruckt) — Fehler verschob sich auf den **Container-Start-Overlap** (alt+neu+Stack → OOM beim Health-Check, avail → 307 MB im Live-Monitoring). Coolify „Deployment Failed", alter Container blieb.
3. **Mitigation + 3. Redeploy GREEN:** Jitsi/Jibri-Stack (5 Container) temporär gestoppt + 2 GB Swap (`/swapfile2`) → avail 417 MB → **3,4 GiB**. Founder-Redeploy → neuer Container kam bei avail ≥2,8 GB **healthy** hoch (Live-Monitoring fing `app-…-164315=16aab52` bei t+126s). Jitsi danach zurückgestartet (Coolify hatte den Full-Stack ohnehin mit neuen Suffixen `104504…` recreated).

### Container-Health (verifiziert)
- `app-k9f5pn5upfq7etoefb5ukbcg-104504164315` — **Up (healthy)**, `SOURCE_COMMIT=16aab52260ec`.
- ENV im Container: `KNOWLEDGE_PUSH_ENABLED=true`, `STRATEGAIZE_KNOWLEDGE_API_BASE_URL`/`_SERVICE_KEY`/`CRON_SECRET` alle SET.
- Interner Health-Call serviert App-HTML. Alter Container sauber ersetzt. Jitsi (5) + Supabase (db/kong/auth/rest/storage healthy) zurück.

### Live-Smoke (R-355-1) — Plumbing-PASS, LLM-Pfad nicht durchlaufen
Manueller Cron-Trigger (`POST /api/cron/knowledge-push` via Container-`node`-Fetch = Scheduled-Task-Command):
```json
{ "success": true, "run_id": "8c908715-…", "iso_week": "2026-W24",
  "items_built": 0, "inserted": 0, "deduped": 0, "failed": 0,
  "bedrock_cost_usd": 0, "buckets_skipped": 0 }
```
**audit_log-Row live verifiziert** (via supabase-db psql): `action='is_knowledge_pushed'`, **`actor_id=NULL`** (nullable-Insert läuft zur Laufzeit), `entity_type='knowledge_push_run'`, `entity_id`=Run-UUID, `context` (TEXT)=Summary-String, `changes` (JSONB) `inserted/items_built`=0 → **die akzeptierte Spec-Drift-Auflösung (changes JSONB / context TEXT / entity_id UUID / actor_id NULL) funktioniert live.**
Quelldaten-Check: `winloss_7d=0 / activities_7d=0` → die **0 Items sind legitim** (keine Win/Loss-Runs + keine Activity-Notizen im 7-Tage-Fenster), kein Bug. → **R-355-1 (echte LLM-Anonymisierung) wurde NICHT durchlaufen** (nichts zu destillieren) → bleibt ISSUE-107.

## Phase 3 — Learning
- **ISSUE-108 (High, neu):** Coolify-Build OOM-killt den speicher-engen BS-Prod-Host. Workaround (funktioniert): Jitsi-Stop + Swap vor Redeploy. Root-Cause-Fix offen (RAM-Resize / ISSUE-106-Cleanup / off-host-Build). → Dev-System SKILL_IMPROVEMENT + self-hosted-playbook (Memory-Mitigation für Build-on-Prod-Host).
- **ISSUE-095 erneut bestätigt:** Coolify kein Auto-Webhook → Founder-Manual-Redeploy.
- **ISSUE-106 sichtbar:** 1007 Sessions / Load-15min 86 = chronischer Session-Sprawl, trägt zur Host-Fragilität bei. Eigene `/doctor`-Runde empfohlen (separat).

## Project-Record-Updates
- `/docs/RELEASES.md` — REL-048 (V8.7-B deployed, Deploy-Story, Live-Smoke, Rollback).
- `/planning/roadmap.json` — `v8-7-b` `active` → `released`.
- `/slices/INDEX.md` — SLC-355 `done` → `deployed`.
- `/features/INDEX.md` — FEAT-872 `done` → `deployed`.
- `/docs/STATE.md` — High-Level-State `post-launch`, Current Focus auf DEPLOYED REL-048 → Next /post-launch.
- `/docs/KNOWN_ISSUES.md` — ISSUE-108 (Host-OOM, High) neu; ISSUE-107 mit Smoke-Ergebnis aktualisiert (R-355-1 offen).
- `/reports/RPT-634.md` — dieser Report.
- **Last Stable bleibt V8.12** (IMP-950) — V8.7-B ist `deployed`, nicht `stable` bis /post-launch T+24h.

## Recommended next step
`/post-launch` V8.7-B — T+3h + T+24h Burn-In (Container healthy über 24h, 0 echte Errors, Memory/Swap-Trend nach Deploy-OOM-Stress beobachten, ISSUE-108-Nachwirkung). **Plus: R-355-1 verifizieren, sobald reale Win/Loss-/Activity-Daten im 7-Tage-Fenster liegen** (manueller Cron-Trigger → IS-`knowledge_items` auf 0 Klarnamen prüfen) → dann ISSUE-107 schließen. Bei T+24h-PASS: V8.7-B → STABLE, Last Stable → V8.7-B. Kein Customer-Outreach ([[module-lifecycle-discipline]] + IMP-950).
