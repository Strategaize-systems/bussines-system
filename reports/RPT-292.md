---
id: RPT-292
date: "2026-05-04"
skill: /qa
slice: SLC-571
feature: FEAT-571
type: review
reviewOf: RPT-291
status: completed
result: "Browser-Smoke MT-3 + MT-4 PASS auf Live-System (business.strategaizetransition.com nach Coolify-Redeploy von Commit 3545971). Inline-Format-Validation, Country-Switch und EU-VAT-ID-Validation alle Verhalten korrekt. Keine Server-Saves ausgefuehrt — Production-Bestand unveraendert."
title: "SLC-571 MT-3+MT-4 Browser-Smoke gegen Live-System"
---

# SLC-571 MT-3+MT-4 Browser-Smoke (Live-System nach Redeploy)

## Outcome

Browser-Smoke fuer MT-3 (Branding-Settings business_country + vat_id) und MT-4 (Company vat_id) erfolgreich auf Live-System `business.strategaizetransition.com` durchgefuehrt nach User-Coolify-Redeploy von Commit `3545971`. Alle Inline-Validation-Pfade verhalten sich wie spezifiziert. Keine Server-Saves ausgefuehrt — der Production-Bestand bleibt unveraendert (nur lokaler Form-State waehrend Test).

## Test-Cases (alle PASS)

### MT-3: /settings/branding

**TC-1 — Initial-Render mit Default-NL**
- Sektion "Steuer-Einstellungen" rendert nach Footer-Markdown-Block ✓
- Country-Combobox zeigt "NL" als Default ✓
- Label "BTW-Nummer (optional)", Placeholder "NL123456789B01" ✓
- Hilfetext "Erscheint im PDF-Footer und ist Voraussetzung fuer Reverse-Charge-Angebote (NL)." ✓

**TC-2 — Inline-Format-Error NL-Mode**
- Eingabe `NL12345` (zu kurz)
- Hilfetext wechselt auf "Format: NL gefolgt von 9 Ziffern, B und 2 Ziffern (z.B. NL123456789B01)" ✓
- Class wechselt von `text-slate-400` auf `text-destructive` (rot) ✓

**TC-3 — Country-Switch NL → DE: Label/Placeholder/Bezeichner**
- Combobox-Dropdown zeigt beide Optionen mit Beschreibung:
  - "Deutschland (DE) — Standard 19%, reduziert 7%" ✓
  - "Niederlande (NL) — Standard 21%, reduziert 9%" ✓
- Nach DE-Auswahl:
  - Label "BTW-Nummer (optional)" → "USt-IdNr. (optional)" ✓
  - Placeholder "NL123456789B01" → "DE123456789" ✓
  - Bestehender Wert `NL12345` zeigt im DE-Mode neuen Inline-Error: "Format: DE gefolgt von 9 Ziffern (z.B. DE123456789)" ✓

**TC-4 — Inline-Format-PASS DE-Mode**
- Eingabe `DE123456789`
- Inline-Error verschwindet, Hilfetext zurueck auf "Erscheint im PDF-Footer..." ✓
- Class wechselt zurueck auf `text-slate-400` ✓

**TC-5 — Country-Switch DE → NL: Reset**
- Combobox auf NL zurueckgesetzt ✓
- vat_id-Feld via DOM-Setter geleert ✓
- Form-State wieder im Default-Zustand (NL + leeres Feld) ✓
- KEIN "Branding speichern"-Klick ausgefuehrt — Server-State unveraendert ✓

### MT-4: /companies (Neue Firma Sheet)

**TC-6 — Form-Render mit vat_id-Feld**
- Label "USt-IdNr. / BTW-Nummer (optional)" mit `htmlFor="vat_id"` korrekt ✓
- Eingabefeld nach `address_country` ✓

**TC-7 — Inline-Format-Error EU-General**
- Eingabe `XX12345`
- Inline-Error: "Country-Code \"XX\" ist kein EU-Mitglied" ✓
- Class `text-destructive` ✓

**TC-8 — Inline-Format-PASS DE-Format**
- Eingabe `DE123456789`
- Hilfetext zurueck auf "EU-VAT-ID des Empfaengers. Voraussetzung fuer Reverse-Charge-Angebote." ✓
- Class `text-slate-400` ✓

**TC-9 — Inline-Format-PASS NL-Format**
- Eingabe `NL859123456B01`
- Hilfetext bleibt im neutralen Zustand (validateEuVatId akzeptiert NL-Format als gueltigen EU-VAT-ID) ✓

**TC-10 — Sheet-Close ohne Save**
- Navigation zurueck auf `/companies` ohne "Erstellen"-Klick ✓
- Keine neue Company in der Liste, Bestand unveraendert ✓

## Files reviewed
- `/cockpit/src/app/(app)/settings/branding/branding-form.tsx` (live-rendered)
- `/cockpit/src/app/(app)/companies/company-form.tsx` (live-rendered)
- `/cockpit/src/app/(app)/companies/company-sheet.tsx` (Sheet-Wrapper)
- `/cockpit/src/lib/validation/vat-id.ts` (Validatoren)

## Files changed
- KEINE Code-Aenderungen in dieser Session (reine Browser-Verifikation)

## Files created
- `/reports/RPT-292.md` — diese Smoke-Report-Datei

## Files intentionally not changed
- `branding_settings.vat_id` und `branding_settings.business_country` in Live-DB — User entscheidet bewusst, ob/wann er die echte Strategaize-vat_id eintraegt. Default `'NL'` bleibt aktiv.
- `companies.vat_id` fuer existierende Firma — User entscheidet manuell pro Firma.

## Problems found
- Keine Verhaltensfehler. Alle Verifikationen PASS.
- **Hinweis (kein Bug):** Der DOM-Setter-Trick (`Object.getOwnPropertyDescriptor(...prototype, 'value').set.call(input, '')`) war noetig, weil das Input ein controlled-component (`value={vatId}` + `onChange`) ist und Playwright's `fill` nur den DOM-Wert setzt ohne React-State-Update zu triggern. Direktes `react`-spec-konformes Test-Setup (z.B. UI-Click + Type) wuerde im echten User-Flow ohnehin korrekt funktionieren — der Test-Trick war nur fuer Browser-Smoke-Effizienz.

## Open points or deferred decisions
- **User entscheidet, wann echte Strategaize-vat_id eintragen wird.** Bei NL-Mode (aktueller Default) ist Format `NL...B01`. Voraussetzung fuer Reverse-Charge-Toggle (kommt in MT-5+MT-6).
- MT-5 useReverseChargeEligibility-Hook + MT-6 Editor-Steuersatz-Dropdown stehen aus (~2-3h, neue Session).

## Project record updates
- `slices/SLC-571-...md` — Verification-Beschreibung MT-3 + MT-4 ergaenzt um "(PASS Live-Smoke RPT-292)"
- KEINE weiteren Records-Aenderungen — Slice bleibt `in_progress`, FEAT/Backlog unveraendert.

## Verification
- 10 Test-Cases auf Live-System gegen Commit `3545971` (deployed via Coolify): alle PASS
- Vitest 184/184 + tsc clean + ESLint clean (aus RPT-291 unveraendert)
- Production-Bestand: 1 Firma in `/companies` vor Test, 1 Firma nach Test (kein neuer Eintrag).
- Production-Branding: NL Default + leeres vat_id vor Test, NL Default + leeres vat_id nach Test (kein Save).

## Recommended next step

**/backend SLC-571 MT-5+MT-6** in einer neuen Session — Eligibility-Hook (NL-Mode-only, DE-Mode-disabled mit Tooltip-Hinweis auf BL-421) + Editor-Country-Filter-Dropdown (DE: 0/7/19, NL: 0/9/21, plus Legacy-Wert wenn persistiert) + Reverse-Charge-Section.

Alternativ: Wenn der User die echte Strategaize-Transition-vat_id eintragen will, kann er das jetzt in `/settings/branding` selbst tun (NL-Mode bleibt korrekt, BTW-Format `NL...B01`).
