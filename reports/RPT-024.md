---
id: RPT-024
date: "2026-04-04"
skill: /qa
slice: SLC-204
feature: BL-119, BL-121, BL-122
type: completion
status: completed
result: "PASS — Pipeline-Filter, Probability-Forecast, Referral-Source auf Deals"
title: "QA SLC-204: Pipeline-Filter + Forecast + Referral-Source"
---

## QA Report — SLC-204

### Result: PASS

### Acceptance Criteria — All passed
- Filter-Leiste über Kanban: Status (active/won/lost) + Opportunity-Typ Dropdowns
- Client-seitige Filterung mit useMemo — reduziert sichtbare Deals
- "Filter zurücksetzen" Link bei aktiven Filtern
- Gewichteter Forecast: Stage-Probability × Deal-Wert im Pipeline-Header
- Dashboard: Forecast KPI Card mit TrendingUp Icon
- getForecastValue() Server Action für Dashboard
- Referral-Source Dropdown im Deal-Form (referral_source_id)
- getReferralsForSelect() lädt Referrer→Target Labels
- createDeal/updateDeal speichern referral_source_id
- Beide Pipeline-Pages (Unternehmer + Multiplikatoren) laden Referrals
- Build: OK (no errors)

### Stub Detection: Clean
- No TODO/FIXME/HACK comments
- No console.log/debugger statements
- No empty handlers
- No hardcoded credentials

### Wiring: All paths verified
1. pipeline-view.tsx → useMemo filter on deals array ✓
2. Forecast calculation → stages.find + probability × value ✓
3. Dashboard getForecastValue() → deals + pipeline_stages queries → weighted sum ✓
4. DealForm referral_source_id → formData → createDeal/updateDeal → Supabase insert/update ✓
5. getReferralsForSelect() → referrals table with joins → label mapping ✓
6. Both pipeline pages load referrals via getReferralsForSelect() ✓

### Low
- **Deal type missing `referral_source_id`**: `deal-form.tsx:131` uses `(deal as any)?.referral_source_id` — existing deal's referral won't pre-select when editing. Minor UX gap.
- **Unused import**: `ArrowRightLeft` still imported in dashboard/page.tsx (was for Übergaben card, replaced by Forecast).
- **Unused data**: `pendingHandoffs` still fetched in `getDashboardStats` but no longer displayed on dashboard. Minor dead query.

### Next Step: SLC-206 (Voice-Input / Whisper)
