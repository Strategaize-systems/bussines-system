---
id: RPT-676
date: "2026-07-08"
skill: /qa
slice: SLC-915
feature: FEAT-927
type: completion
status: completed
result: "Plan-QA SLC-915 PASS mit 1 Medium-Fix (Body-Route-Tabelle emails→email_messages korrigiert); alle Gates/AC-MT-Mapping/Test-Reachability verifiziert"
title: "/qa Plan-QA — SLC-915 V8.17 Regressions-Fix-Bundle"
---

## QA-Scope

Plan-QA der Slice-Spec `slices/SLC-915-v817-regressions-fix-bundle.md` (kein Code implementiert). Geprüft: Spec-Grounding-Gate-Vollständigkeit, AC↔MT-Konsistenz + Track-Zuordnung, Test-AC-Erreichbarkeit gegen reale Test-Infra (Silent-Skip-Audit), Reuse-Claims, Cross-Slice-Deps, Live-Verification-Plan (CSP). Unabhängige Re-Verifikation der Hochrisiko-Claims (nicht Vertrauen auf die Planungs-Session).

## Verdict: PASS (mit 1 Medium-Finding im Plan-QA behoben)

Plan ist implementierungsreif. Ein Medium-Finding gefunden und direkt in der Spec korrigiert; kein Blocker/High offen.

## Findings

### Medium — Body-Route-Tabelle: Spec/DEC-306 nannte `emails.id`, reale Viewer-Quelle ist `email_messages` (BEHOBEN)
- Befund: Der Inbound-Viewer (`email-detail.tsx` → `EmailBody` → `sanitizeEmailHtml(email.body_html)`) nutzt `email: InboxEmail`; `InboxEmail extends EmailMessage` (`imap-actions.ts:12`) und alle `getInboxEmail*` selektieren `.from("email_messages")`. Die `.from("emails")`-Queries (`emails/actions.ts`) sind eine separate Entity. DEC-306 + Spec-Schema-Grounding hatten die Body-Route um `emails.id` formuliert → hätte /backend eine für Inbound-Mails leere/dead Route bauen lassen (AC-915-7 „Bilder laden" tot).
- OQ-A2 hatte die Ambiguität antizipiert; Plan-QA hat sie aufgelöst.
- Fix: Spec MT-6 + Schema-Grounding auf `email_messages an id` korrigiert; OQ-A2 downgraded auf Live-Confirm (RLS + `body_html`-Spalte auf `email_messages`).

### Low — accepted
- Spec MT-4 nennt einen konkreten MIG-055-Dateinamen; final wird die File-Nummer beim /backend-MT-Start per `ls` vergeben (in der Spec so vermerkt, MIG-055-ID system-weit reserviert). Kein Handlungsbedarf.

## Verifizierte Gates (unabhängig re-geprüft)

- **Schema (MT-4):** FK→Parent-Map 1:1 gegen `sql/migrations/054_*.sql` bestätigt — tasks(deal/contact/company), signals(+activity), calendar_events(+meeting), handoffs(deal/company), + documents/email_threads/referrals/email_attachments; alle Parents via `can_see_owner(owner_user_id)`; UPDATE-USING bereits OR, UPDATE-WITH-CHECK AND → MT-4-Mechanik (OR + Trigger) korrekt. `is_admin`+`can_see_owner` existieren.
- **Call-Sites (MT-5):** `followup-actions.ts:126` (`taskError` erfasst, ungeprüft), `signal-actions.ts:73` (roher error an UI), `meetings/actions.ts:228` (**kein** error-Capture — matcht ISSUE-140), `emails/imap-actions.ts:135` (email_threads.update ungeprüft) — alle 4 Zeilen live bestätigt.
- **Test-Infra / Silent-Skip:** `vitest.rls.config.ts` include deckt `__tests__/migrations/**/*.test.ts` (Z.31) → MT-4-DB-Verify wird von `npm run test:rls` collected (kein Silent-Skip). jsdom-Config deckt csp/consent/sanitize-Tests. Baselines: consent-check 3, sanitize 19.
- **AC↔MT:** alle 8 ACs auf MTs gemappt, Track-Zuordnung korrekt (backend/frontend/full-stack); keine Frontend-AC in Backend-only-MT.
- **Step-0-Drift:** camera=() bleibt (Jitsi separater Origin) — Architektur-Grounding korrekt übernommen.
- **Live-Verification-Plan:** CSP-Feature-Flow-Smoke (Telefonie-WSS/Mikrofon/Meeting/E-Mail-Viewer, nicht nur Hydration) BLOCKING in der Spec — konform mit `security-headers-live-smoke.md` + IMP-1401.

## Test-Ausführung

Kein Code → `npm run test` nicht ausgeführt (reine Plan-QA). Test-AC-Erreichbarkeit statisch gegen die reale Test-Infra verifiziert (siehe oben). Erst-Ausführung erfolgt in den /backend+/frontend-MTs.

## Readiness

Plan implementierungsreif. Slice bleibt `planned`. Nach dem einen Spec-Fix keine offenen Plan-Blocker. Restrisiko liegt in der Implementierung (MT-4 Trigger über 9 Tabellen, MT-6 iframe-Höhenmessung) — durch Live-Verification-Plan + DB-Verify abgedeckt.

## Next workflow step

`/backend SLC-915 MT-1` (ISSUE-138 CSP) — Beginn der Implementierung in Fix-Reihenfolge ISSUE-138→142→141→140→139; `/qa` nach jedem Block; danach Gesamt-/qa V8.17 → /final-check → /go-live → /deploy (MIG-055 Live-Apply) → /post-launch T+24h. Kein Customer-Live (module-lifecycle-discipline + IMP-950).
