---
id: RPT-686
date: "2026-07-08"
skill: /deploy
slice: SLC-915
feature: FEAT-927
type: completion
status: completed
reviewOf: null
result: "V8.17 DEPLOYED (code live c0709b3, app healthy). MIG-055 live-appliziert + Semantik-Probe persistente DB 4/4 PASS (Gate A). Gate C Header-Baseline + csp-check.mjs PASS. Gate B funktionaler Feature-Flow-Smoke = Founder-Browser offen -> roadmap NICHT released / SLC+FEAT NICHT deployed / ISSUE-138/139/141 open bis Smoke. REL-052. Kein Customer-Live."
title: "/deploy V8.17 SLC-915 — code live c0709b3, MIG-055 live (Gate A/C PASS, Gate B Founder-pending)"
---

# /deploy V8.17 (SLC-915 Regressions-Fix-Bundle)

Phasen-Ausführung nach /go-live CONDITIONAL GO (RPT-685). Internes Workflow-Closure, kein Customer-Live.

## Phase 1 — Prerequisites + Pre-Deploy-Server-Precheck

- **Stack:** Next.js + Supabase + Kong auf Hetzner/Coolify, Server `91.98.20.191` (`coolify-ubuntu-4gb-nbg1-1`), App `business.strategaizetransition.com`. Update-Deploy.
- **SSH ✓**, Identität = erwarteter BS-Host. Agent deployt selbst.
- **Ressourcen (WARNING, kein Block):** Disk 86 % (>85 %), MemAvailable 2,1 GB + SwapFree 1,5 GB (<4 GB Headroom → OOM-Risiko). Mitigation angewandt (Phase 2): `docker builder prune` (0 B, Cache schon leer) + **Jitsi-Stack 5 Container gestoppt** → MemAvailable 2,9 GB + SwapFree 1,6 GB.
- **IST:** V8.16 live (`8ebcf6c`, app healthy), Stack 22 Container, supabase-studio unhealthy = pre-existing/unrelated. DB-Container `supabase-db-…-081218581096`.
- **Deploy-Trigger:** kein Auto-Webhook (ISSUE-095) → autonomer Coolify-API-Redeploy (Resource `k9f5pn5upfq7etoefb5ukbcg`).
- Founder-Bestätigung "los" eingeholt vor Phase 2.

## Phase 2 — Execution

### Step 1 — Push
`git push origin main` → `143d08d..c0709b3` (18 Commits). origin/main = `c0709b3`.

### Gate 1 / Gate A — MIG-055 Live-Apply (BLOCKING, vor Code-Deploy)
- **Pre-Apply Live-DB-Audit** (`sql-migration-hetzner.md`): `pg_dump -s`-Snapshot (9149 Zeilen, `/tmp/schema_pre_mig055_20260708.sql`); 0 bestehende changed-fk/fk_guard-Trigger auf den 9 Tabellen (net-new, keine Kollision); alle 9 UPDATE-Policies im MIG-054-AND-Shape vorhanden; `can_see_owner` vorhanden. Live-DB == Schema-Grounding.
- **Apply** als `postgres` via Base64 (536/536 Zeilen transferiert, head/tail intakt). **2× ausgeführt (Idempotenz), beide EXIT=0**, nur skip-NOTICEs, 0 ERROR.
- **Post-Apply-Struktur-Verify:** 9 `*_changed_fk_guard`-Trigger (1/Tabelle); `tasks_update` WITH CHECK = **OR-Shape**; `tasks_insert` WITH CHECK = **AND-strikt**; changed_fk_guard-Functions **SECURITY INVOKER** (prosecdef=false).
- **Semantik-Probe gegen die persistente Live-DB — 4/4 PASS** (DO-block: seed reale mixed-owner-Row via echte deals/contacts als postgres → assert als `authenticated` member `…082` → ROLLBACK, pollution-frei):
  - CHK1 mixed-owner Status-Update (FKs unverändert) → **PASS** (rows=1) — MIG-054-Freeze behoben
  - CHK2 FK-Change auf unsichtbaren Parent → **PASS** (BLOCK, 42501)
  - CHK3 legitimer FK-Change auf sichtbaren Parent → **PASS** (erlaubt)
  - CHK4 INSERT strikt Cross-Tenant → **PASS** (BLOCK)
  - Kontroll: nach ROLLBACK 9 Trigger, 0 Probe-Rows → Live-DB unverändert.
- Ergänzt die code-side in-tx-Probe 8/8 (RPT-680) gegen die jetzt persistente Schema-Realität.

### Gate 2 — ENV
Keine neue/geänderte ENV. `NEXT_PUBLIC_SIP_DOMAIN` bleibt leer → next.config-Fallback `sip.strategaizetransition.com` (server-seitiger CSP-Header, kein Client-Bundle) — live im Header bestätigt (Gate C).

### Step 4 — Deploy + Gate 3 Delivery-Chain-Verify
- Autonomer Redeploy: temp Sanctum-Token in coolify-db (id 2), `is_api_enabled` temporär true, `GET /api/v1/deploy?uuid=k9f5pn5upfq7etoefb5ukbcg` → Deployment `w13sjmfh8aprwxglzw1rseeb`.
- Status-Poll via `application_deployment_queues` → **finished** (~7 min; Mem-Dip auf ~1,4 GB im Build überstanden, kein OOM).
- **Delivery-Chain-Verify:** neuer Container `app-k9f5pn5upfq7etoefb5ukbcg-103000879099`, **SOURCE_COMMIT `c0709b3`** ✓, Status running → **healthy** (health-poll 1), RestartCount=0. Full-Stack-Recreate: neuer DB-Container `-103000902567` (Volume persistent, MIG-055-Trigger 9/9 survived), Jitsi via Recreate **5/5 zurück**. 22 Container up. Frisches App-Log: 0 echte Errors.
- **PFLICHT-Cleanup verifiziert:** Token-Row `DELETE 1`, `is_api_enabled` zurück auf `f`, Token-Count 0, lokales Token-File entfernt.

### Gate C — Header-Baseline + Hydration (automated, PASS)
- `curl -I /login` → **HTTP 200**, **enforced** `Content-Security-Policy` (kein Report-Only-Header), `connect-src` enthält `wss://sip.strategaizetransition.com`, `Permissions-Policy: camera=(), microphone=(self), …`, globale `img-src 'self' data: blob:` (kein generisches `https:`).
- `csp-check.mjs /login` → **PASS**: hasReactProps/hasReactFiber/onSubmitAttached=true, **cspErrors: []**.
- Body-Route `GET /api/emails/[id]/body`: registriert (nicht 404-missing); ohne Session **307→/login** (kein Body-Leak; route-scoped CSP greift erst für authentifizierte Serves).

### Gate B — Funktionaler Feature-Flow-Smoke (BLOCKING, Founder-Browser OFFEN)
Erfordert authentifizierte Session; `never_touch_user_passwords` → kein Agent-Login. Founder-2-Min-Smoke (gegen `c0709b3`, siehe unten). **Bis PASS:** roadmap V8.17 NICHT `released`, SLC-915/FEAT-927 NICHT `deployed`, ISSUE-138/139/141 bleiben `open`.

## Phase 3 — Learning
Keine neuen Deploy-Blocker. Precedents bestätigt: autonomer Coolify-Redeploy (RPT-669), OOM-Jitsi-Stop, Full-Stack-Recreate mit Volume-Persistenz, DO-block-Semantik-Probe gegen persistente DB (neue Anwendung des `sql-migration-hetzner.md`-Live-DB-Realitäts-Checks post-apply). Playbooks aktuell.

## Project record updates
- `docs/MIGRATIONS.md` — MIG-055 → **LIVE APPLIED** + Semantik-Probe 4/4.
- `docs/RELEASES.md` — **REL-052** (Scope, Live-Verify, offener Gate B, Rollback auf REL-051/`8ebcf6c`).
- `docs/KNOWN_ISSUES.md` — **ISSUE-140 → resolved** (MIG-055 live + semantisch verifiziert).
- `docs/STATE.md` — High-Level State `deploying`; Focus = deploy executed, Gate B Founder-pending.
- `reports/RPT-686.md` — dieser Report.
- **DEFERRED bis Founder-Smoke (Gate 4 Cockpit-Consistency-Sweep abschließen nach PASS):** roadmap `v8-17` → released, SLC-915/FEAT-927 → deployed, ISSUE-138/139/141 → resolved, backlog BL-520 → done.

## Gate-B Founder-Browser-Smoke-Checkliste (2 Min, gegen business.strategaizetransition.com)
1. **Telefonie (AC-915-1):** Softphone-Call auslösen → WSS verbindet, Mikrofon-Freigabe, DevTools-Console **0** `connect-src`/`Permissions-Policy`-Errors.
2. **Meeting (AC-915-2):** „Meeting starten" (Mein Tag) → `window.open` öffnet Jitsi, kein Permissions-Block.
3. **E-Mail (AC-915-7):** Inbound-Mail mit Remote-Bild → **blockiert** + Banner „Externe Bilder blockiert [Bilder laden]"; „Bilder laden" → Bilder **rendern** (Netzwerk-Tab: direkt, kein Proxy), Höhe korrekt (kein Abschnitt). Foreign-id: `/api/emails/<fremde-id>/body` → 404.
4. **MeetingPrepCard (AC-915-4):** Fehlerfall → sichtbare Inline-Meldung (kein Dead-Button).

## Verdict
**V8.17 DEPLOYED (code live `c0709b3`, MIG-055 live).** Automated Gates A + C PASS. **Gate B funktionaler Feature-Flow-Smoke = Founder-Browser offen** → formaler Deploy-Abschluss (Records-Flip + /post-launch) erst nach Founder-Smoke-PASS. Per IMP-950 NICHT stable bis /post-launch T+24h — Last Stable bleibt V8.16.

## Recommended Next Step
Founder-2-Min-Browser-Smoke (oben). Bei PASS: Records-Flip (roadmap released, SLC/FEAT deployed, ISSUE-138/139/141 resolved, BL-520 done) + Push der Post-Deploy-Doc-Commits → `/post-launch V8.17` T+24h → Last-Stable-Flip V8.16→V8.17. Bei FAIL: Hotfix-Path (`csp.ts`/`next.config.ts` reapply + `coolify-autonomous-redeploy.md`).
