---
id: RPT-646
date: "2026-06-12"
skill: /backend
slice: SLC-913
feature: FEAT-925
type: completion
status: completed
reviewOf: null
result: "MT-4 Mass-Assignment-Whitelist DONE — cleanValues-Projektion in moveDealToStage (Whitelist-Pick + Typ-Validierung), 4 neue Adversarial-Tests, Vitest 1535/1535"
title: "V8.15 SLC-913 MT-4 /backend — Mass-Assignment-Whitelist moveDealToStage (ISSUE-115)"
---

# V8.15 SLC-913 MT-4 — Mass-Assignment-Whitelist (/backend)

## 1. Outcome Summary

**Vollstaendig DONE.** `moveDealToStage` (`cockpit/src/app/(app)/pipeline/actions.ts`) projiziert `requirementValues` (Raw-Client-Input) jetzt auf eine bereinigte `cleanValues`-Map bevor irgendetwas damit passiert:

- **Whitelist-Pick:** nur Keys aus `STAGE_REQUIRED_FIELDS[stageName].fields` (pro Ziel-Stage deklariert) ueberleben; unbekannte Keys (`owner_user_id`/`status`/`created_at`/`pipeline_id`/`campaign_id`/`value` wo nicht erlaubt/…) werden verworfen.
- **Typ-Validierung:** nur `string | number | null` — Objekte/Arrays werden verworfen (TS-Typ ist zur Laufzeit erased).
- **Konsequente Nutzung:** effectiveDeal-Merge (Pflichtfeld-Validation), deals-UPDATE-Payload, before/after-Audit-Snapshot UND Activity-Titel nutzen ausschliesslich `cleanValues` — schliesst auch die Audit-Evasion (vorher loggte der Titel nur deklarierte Keys, geschrieben wurde alles).

### Tests (AC-913-4): 4 neue Adversarial-Tests, alle GREEN

1. Payload mit 6 Angriffs-Keys → UPDATE-Payload enthaelt exakt `[updated_at, won_lost_reason]`, Stage-Move setzt `status` regulaer selbst.
2. Nur Angriffs-Keys → Pflichtfeld-Phase entfaellt komplett (nur regulaerer Stage-Move).
3. Objekt-Wert fuer `value` → verworfen → Pflichtfeld-Validation greift (Fehler, 0 Updates).
4. Activity-Titel listet nur Whitelist-Keys (Audit-Evasion geschlossen).

Plus 6 bestehende SLC-813-Regression-Tests unveraendert GREEN (Whitelist bricht legitime Modal-Flows nicht).

### Gates (IMP-1108)

TSC=0 ✓ · ESLint: neue/beruehrte Zeilen 0 (2 pre-existing `any`-Errors auf unberuehrten Zeilen 499/737 = Baseline) ✓ · Full-Vitest jsdom **1535/1535 GREEN** (166 Files, +4).

## 2. Files Reviewed

- `/cockpit/src/lib/pipeline/stage-required-fields.ts` (Whitelist-Quelle, 5 Stages)
- `/cockpit/src/app/(app)/pipeline/__tests__/move-deal-to-stage.test.ts` (bestehende Mock-Infrastruktur, wiederverwendet)
- `/docs/KNOWN_ISSUES.md` (ISSUE-115)

## 3. Files Changed

- `/cockpit/src/app/(app)/pipeline/actions.ts` — cleanValues-Projektion + alle 4 Nutzungsstellen (Merge/Persist/Audit-Snapshot/Activity-Titel) auf die bereinigte Map
- `/cockpit/src/app/(app)/pipeline/__tests__/move-deal-to-stage.test.ts` — +4 Adversarial-Tests (neuer describe-Block)

## 4. Files Created

- `/reports/RPT-646.md` — dieser Report

## 5. Files Intentionally Not Changed

- `/cockpit/src/lib/pipeline/stage-required-fields.ts` — Whitelist-Definition korrekt, keine Aenderung noetig
- `/cockpit/src/components/kanban/kanban-board.tsx` + `stage-requirements-modal.tsx` — Caller-Signatur unveraendert, legitime Flows senden nur Whitelist-Felder

## 6. Problems Found

- 2 pre-existing ESLint-`any`-Errors auf unberuehrten Zeilen (Baseline-218) — nicht gefixt per surgical-changes.

## 7. Open Points / Deferred

- ISSUE-115 → `resolved` im /qa-Schritt.

## 8. Recommended Next Step

`/qa SLC-913 MT-4` (sofort) — danach `/backend` MT-5 (Public-Endpoint-Hardening ISSUE-110/118/119).
