---
id: RPT-678
date: "2026-07-08"
skill: /qa
slice: SLC-915
feature: FEAT-927
type: completion
status: completed
result: "MT-2 (ISSUE-142 Consent fail-closed) + MT-3 (ISSUE-141 MeetingPrepCard res.error / kein RLS-unsichtbarer contact_id) code-done — Suite 176/1623 grün, tsc+build clean; Meeting-Start-Interaktion PENDING-LIVE (Smoke)"
title: "QA SLC-915 MT-2+MT-3 (Small-Fixes-Block) — ISSUE-142 Consent-Guard + ISSUE-141 MeetingPrepCard"
---

# QA — SLC-915 MT-2 + MT-3 (Small-Fixes-Block)

## 1. QA-Scope
Zweiter QA-Block von SLC-915 (die kleinen Backend/Frontend-Fixes, per Handoff-Scope zusammen):
- **MT-2 (ISSUE-142, backend):** `checkConsentStatus` fail-closed Count-Guard.
- **MT-3 (ISSUE-141, frontend):** `MeetingPrepCard` wertet `res.error` aus (kein Silent-Dead-Button); `getNextMeetingWithContext` reicht keine RLS-unsichtbare rohe `contact_id` durch.

Geprüfte Dateien:
- `cockpit/src/lib/meetings/consent-check.ts` (+ `.test.ts`)
- `cockpit/src/app/(app)/mein-tag/actions.ts` (@449)
- `cockpit/src/app/(app)/mein-tag/mein-tag-client.tsx` (`MeetingPrepCard`)
- `cockpit/src/app/actions/meetings.ts` (:265 read-only — Consent-Consumer) · `cockpit/src/components/meetings/start-meeting-modal.tsx` (:121 read-only — `missingConsent`-Render)

## 2. Ergebnis
**MIXED — code-done.** Kein Blocker/High. Alle statisch/unit verifizierbaren Ebenen grün; die einzige
Live-Restunsicherheit ist die MT-3-Klick-Interaktion (Dead-Button → sichtbare Meldung), sauber als
PENDING-LIVE deferiert.

## 3. Per-AC-Verdicts

| AC | Inhalt | Verdict | Begründung |
|---|---|---|---|
| AC-915-3 (MT-2) | `checkConsentStatus` fail-closed: returned-Rows < contactIds ⇒ fehlende IDs `missing`, `allGranted=false`; caller-unabhängig; Vitest deckt RLS-weggefilterte Row ab | **PASS-CODE** | Synthetischer `missing`-Eintrag pro angeforderter ID ohne Row (via `returnedIds`-Set); `allGranted=missing.length===0` bleibt. +2 Vitest-Cases (1 Row von 2 angefordert; leere Query) → 3→5 grün. Logik vollständig unit-verifiziert, kein Runtime-Gate nötig; echte RLS-Filterung im Meeting-Flow deckt der Gesamt-/qa-Smoke mit ab. |
| AC-915-4 (MT-3) | `MeetingPrepCard` sichtbare Meldung statt No-Op; `getNextMeetingWithContext` kein RLS-unsichtbarer roher `contact_id` | **PASS-CODE / PENDING-LIVE** | Statisch: `contact?.id ?? null` (Grep: 0 `(meeting as any).contact_id`-Treffer); `res.error`-Zweig + lokaler `useState meetingError` + `role="alert"`-Inline-Banner gewired; `res.error===""` bei Erfolg → korrekt kein Fehler. Klick→sichtbare-Meldung-Interaktion = Browser-Smoke (Gesamt-/qa/deploy). |

## 4. Test-Ausführung + Silent-Skip-Audit
- **Volle Suite** (`npm run test`): **176 Files / 1623 Tests, 0 fail** (nach MT-2: +2 Consent-Tests; nach MT-3: keine neuen Tests, bestehende grün).
- **Gezielt** `consent-check.test.ts` + `meetings.test.ts`: 2 Files / 9 grün. `consent-check` 3→5.
- **tsc** `--noEmit` clean — bestätigt u.a. `res.error` typsicher auf der `StartMeetingResult`-Union (kein `as any`).
- **`next build`** ✓ Compiled successfully — Done-Gate für den `'use server'`-Touch (`mein-tag/actions.ts`; Export-Sanity: nur async/type-Exports) + die Client-Komponente.
- **Include-Coverage:** beide Test-Files unter `src/**/*.test.ts` → in voller Suite gelaufen. Kein DB-/Live-Gate im Block → 0 silent-skipped.

## 5. Stub- / Wiring-Audit
- **Stub-Scan** (consent-check.ts, mein-tag/actions.ts, mein-tag-client.tsx): keine TODO/FIXME/console.log/placeholder/empty-catch im geänderten Scope.
- **Wiring MT-2:** `checkConsentStatus.missing` → einziger Consent-Consumer `meetings.ts:265` mappt `{name, email}` → `start-meeting-modal.tsx:121` rendert Namen. `recordingEnabled = allGranted && length>0` (`meetings.ts:148`) wird durch synthetische `missing`-Einträge korrekt `false`. (Die `.missing`-Treffer in `azure.ts`/`reverse-charge-*` sind fremde Felder, nicht Consent.)
- **Wiring MT-3:** `NextMeetingPrep.contactId` (`string|null`) → `handleStartMeeting` (`mein-tag-client.tsx:571`) bildet `contactIds = contactId ? [contactId] : []`. Entfernter Raw-Fallback ⇒ bei RLS-Filter `contactId=null` ⇒ `[]` an `startMeeting` (statt geleakter ID). `res.error`-Rückkanal → `setMeetingError` → Inline-Banner-Render. Kette geschlossen.

## 6. Findings

### Low
- **Cosmetic — leere Namen im Consent-Banner (MT-2, by design):** Synthetische `missing`-Einträge tragen leere `first_name`/`last_name`/`email` (die Contact-PII ist RLS-unsichtbar und darf NICHT geleakt werden — korrektes Datenschutz-Verhalten). Der Modal-Banner (`start-meeting-modal.tsx:121`, `missingConsent.map(c=>c.name).join(", ")`) zeigt für solche Einträge einen leeren Namen bzw. führende/anhängende Kommas. Rein kosmetisch; die Security-Wirkung (`recordingEnabled=false`) ist korrekt. Kandidat für einen späteren Micro-Fix („unbekannter Teilnehmer" statt leer) — kein V8.17-Blocker.

## 7. Offene Risiken / fehlende Checks
- **MT-3-Interaktions-Smoke (PENDING-LIVE):** Im Browser gegen deployten Stand — Meeting mit fehlgeschlagenem `startMeeting` (z.B. fehlender Deal/Consent-Fehler) → das rote `role="alert"`-Banner erscheint sichtbar (nicht nur Konsole). Kein RTL-Test ergänzt (kein bestehendes mein-tag-client-Test-Harness; Spec designierte die Interaktion als /qa-Smoke). Nachhol beim Gesamt-/qa V8.17 / /deploy.

## 8. Readiness-Assessment
MT-2 ist **vollständig code-verifiziert** (fail-closed-Logik unit-bewiesen). MT-3 ist statisch/Wiring
vollständig, nur die visuelle Interaktion bleibt Live-pending. Beide freigegeben; Slice bleibt
`in_progress` (MT-4/5/6 offen).

## 9. Next workflow step
Weitere MTs offen → **`/backend SLC-915 MT-4`** (MIG-055 changed-FK-only Trigger + UPDATE-WITH-CHECK OR
über 9 Klasse-C-Tabellen, node:20-Sidecar DB-Verify) — der schwerste MT, **verdient frischen Kontext**
(Handoff-Empfehlung). Reihenfolge unverändert; danach MT-5 (4 Call-Sites) und MT-6 (E-Mail-Viewer,
ebenfalls fresh-context). Anschließend Gesamt-/qa V8.17 MIT Feature-Flow-CSP-Smoke → /final-check →
/go-live → /deploy (MIG-055 Live-Apply) → /post-launch T+24h. **Kein** `deployed`/`released`/`stable`-Write
ohne die jeweiligen Workflow-Reports (Release-Gate-Defense).
