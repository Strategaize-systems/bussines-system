---
id: RPT-620
date: "2026-06-10"
skill: /qa
slice: SLC-910
feature: FEAT-922
type: review
status: reviewed
reviewOf: RPT-619
result: "SLC-910 Phase-A /qa PASS-CODE-SIDE — 4/4 Phase-A ACs (910-1/2/3/8), stub-scan clean, wiring verified. 0B/0H/0M/2L. AC-910-4/5/6/7 deferred to Phase-B/Burn-In."
title: "V8.12 SLC-910 CSP-Headers Phase-A /qa"
---

# V8.12 SLC-910 CSP-Headers Phase-A — /qa Completion Report

## 1. QA scope reviewed

Phase-A Code-Side von SLC-910 (review of RPT-619). Geprueft: `cockpit/src/lib/security/csp.ts`,
`cockpit/src/lib/security/__tests__/csp.test.ts`, `cockpit/next.config.ts`,
`tests/_probe/csp-check.mjs`. AC-910-4/5/6/7 (Burn-In / Phase-B-strict / Live-Smoke) sind
post-Deploy/post-Burn-In und NICHT Gegenstand dieser Phase-A-QA.

## 2. Result

**PASS-CODE-SIDE.** 4/4 Phase-A-ACs erfuellt. 0 Blocker / 0 High / 0 Medium / 2 Low (beide accepted).

## 3. Findings by severity

### Blocker / High / Medium
Keine.

### Low
- **L-1 (accepted): `report-uri` braucht Founder-ENV.** Phase-A Report-Only meldet Violations nur in der Browser-Console, bis `SENTRY_CSP_REPORT_URI` in Coolify auf den Sentry-Security-Endpoint zeigt. Korrekt fail-safe implementiert (kein Crash ohne ENV), als Founder-TODO in Spec + STATE vermerkt. Operativ, nicht code-blockierend.
- **L-2 (accepted): `@playwright/test` nicht installiert** → `tests/_probe/csp-check.mjs` ist noch nicht ausfuehrbar. Bewusst: der Probe ist erst fuer AC-910-6 (Phase-B strict) + AC-910-7 (Live-Smoke) post-Deploy noetig. devDep wird vor MT-5/MT-6 ergaenzt.

## 4. Stub detection

**CLEAN.** Scan ueber alle 4 Files: 0 TODO/FIXME/HACK, 0 `return null`/`return []`/`return {}`,
0 leere catch-Bloecke, 0 `debugger`, 0 `console.log("test")`, 0 hardcoded Credentials/AKIA/Bearer.
Der `process.exit(1)` im Probe ist legitimes Done-Gate, kein Stub.

## 5. Wiring verification

`csp.ts` (`buildCSP` + `PERMISSIONS_POLICY`) → import in `next.config.ts` → `SECURITY_HEADERS` →
`async headers()` → emittierter Response-Header. **End-to-end via `curl -I` gegen `next dev` bestaetigt**
(RPT-619): alle 6 Header vorhanden, CSP-Value identisch zum `buildCSP`-Output, `connect-src` enthaelt
den aufgeloesten `NEXT_PUBLIC_SUPABASE_URL` (lokal `http://localhost:8000`), `report-uri` korrekt
absent ohne ENV. Beide `report-uri`-Zweige (on/off) per Vitest abgedeckt.

## 6. AC-by-AC

| AC | Inhalt | Status |
|---|---|---|
| AC-910-1 | buildCSP returnt 9 Direktiven | PASS (curl-verifiziert: default-src/script-src/connect-src/img-src/style-src/font-src/frame-ancestors/base-uri/form-action) |
| AC-910-2 | Vitest ≥4 Cases | PASS (7 Cases) |
| AC-910-3 | headers() setzt CSP-Report-Only + Permissions-Policy auf alle Routes | PASS (`source: "/:path*"`, curl /login) |
| AC-910-8 | TSC=0, ESLint=0, Vitest GREEN | PASS (TSC=0, ESLint=0, Vitest 7/7 Subset + 1390/1390 Full) |
| AC-910-4 | Burn-In + Sentry-Violations | DEFERRED (post-Deploy) |
| AC-910-5 | Inline-Script-Inventur + Iter-Fix | DEFERRED (Phase-B) |
| AC-910-6 | Phase-B strict + Probe-PASS | DEFERRED (post-Burn-In) |
| AC-910-7 | Live-Smoke 5 Routes | DEFERRED (post-Deploy) |

## 7. Open risks / missing checks

- Kein funktionaler Browser-Smoke in dieser Session — **regelkonform**: `security-headers-live-smoke.md` fordert den BLOCKING Browser-Smoke fuer den **strict**-Header (Phase-B, AC-910-6). Phase-A Report-Only blockt nichts → kein Hydration-Outage-Risiko.
- Risk R-910-1 (strict bricht 80+ Pages) bleibt offen bis Phase-B — durch Report-Only-Burn-In + Iter-Fix mitigiert (per Design).

## 8. Readiness assessment

SLC-910 **Phase-A Code-Side ready**. Slice bleibt `in_progress` bis Phase-B strict live +
Probe-PASS. Phase-A-Deploy laeuft im `/deploy V8.12`-Bundle mit und startet damit die 1-2 Wochen
Burn-In.

## 9. Project record updates

slices/INDEX (SLC-910 bleibt `in_progress`), docs/STATE.md Current Focus → /qa PASS, RPT-620
angelegt. KNOWN_ISSUES: keine neuen Issues. backlog BL-501 / roadmap V8.12 / FEAT-922 unveraendert.

## 10. Next workflow step

V8.12 hat noch SLC-911 `in_progress` (Sentry Live-Smoke bis /deploy) und SLC-910 `in_progress`
(Phase-B post-Burn-In). Alle Code-Side-Slices (906/907/908/910-PhaseA/911) sind /qa-PASS.
**Nächster Schritt: `/qa` Gesamt-QA über V8.12** (alle Slices zusammen) als Voraussetzung für
`/final-check`. Per IMP-950 + module-lifecycle-discipline: V8.12 NICHT released bis
`/final-check` → `/go-live` → `/deploy` → `/post-launch`; kein Customer-Live.
