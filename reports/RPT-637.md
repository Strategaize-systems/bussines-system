---
id: RPT-637
date: "2026-06-12"
skill: /final-check
slice: SLC-912
feature: FEAT-924
type: completion
status: completed
reviewOf: RPT-636
result: "V8.14 /final-check CONDITIONALLY READY fuer /go-live. 0B/0H/0M/1L. 0 neue Deps, Secret-Scan clean, Gates GREEN. Conditions: Founder-ENV (TRACKING_HMAC_SECRET + GOTRUE_RATE_LIMIT_*) + MIG-051-Live-Apply (AC-912-10, schliesst ISSUE-098-Blocker) + L-1 Impressum/Help-Render-Smoke + ISSUE-108 OOM-Mitigation."
title: "V8.14 SLC-912 Security-Hotfix /final-check — CONDITIONALLY READY"
---

# V8.14 SLC-912 Security-Hotfix — /final-check (Pre-Release Audit)

## 1. Scope
Pre-Release-Audit ueber 7 Dimensionen fuer V8.14 (Single-Slice Security-Hotfix SLC-912). Funktionale Tiefe wurde in /qa (RPT-636) verifiziert; final-check ergaenzt Secret-Scan, Dependency-/Deploy-Readiness, Observability, Release-Hygiene + Go/No-Go.

## 2. Gesamtbewertung: **CONDITIONALLY READY** fuer `/go-live`
0 Blocker / 0 High / 0 Medium / **1 Low** (Code-Seite). Die offenen Punkte sind **operative Deploy-Conditions**, keine Code-Blocker.

## 3. Audit nach Dimensionen

### 1. Code-Quality — PASS
tsc=0, ESLint=0, Stub-Detection CLEAN (0 TODO/FIXME/debugger/empty-catch). 0 `console.log(` in geaenderten Prod-Files (cron auf `logSafe` umgestellt; `console.error` bewusst belassen). Kein Dead-Code/keine ungenutzten Imports eingefuehrt. Diff +1170/-47 ueber 32 Files (davon ~9 Records + 3 Reports + V8.7-B-Deploy-Doc-Mitnahme).

### 2. Security/Privacy — PASS (das IST das Release)
Secret-Scan ueber Release-Diff: nur Test-Mock-Variablennamen (`signInWithPassword`, `password`-Param), **0 Secret-Literale**. Fixes verbessern die Posture: Privilege-Escalation-Block (098), Brute-Force-Limit (099), Stored-XSS-Sanitize (100), Branding-Hardening (101/102), Log-PII-Redaction + Auth-fail-closed (103/104). **Caveat:** ISSUE-098 (Blocker) ist erst nach MIG-051-Live-Apply real geschlossen (Condition C2).

### 3. Legal/Compliance — PASS (positiv)
DSE-Markdown-XSS-Sanitize + Log-PII-Redaction (17 Redact-Keys inkl. from_address/body_text) sind DSGVO-positiv. 0 neue Dependencies → keine neuen Lizenz-/Vendor-Risiken.

### 4. Testing-Depth — PASS
Vitest jsdom **1485/1485** (162 Files), ~33 neue Tests decken alle Security-Pfade (Rate-Limit-Lockout, OWASP-XSS render+write, uploadLogo-Role/SVG, fail-closed-Role, Redact-Keys). MIG-051-DB-Verify-Test existiert, laeuft im /deploy gegen Coolify-DB (AC-912-10) — bewusst kein lokaler Gate, dokumentiert.

### 5. CI/CD & Deployment-Readiness — CONDITIONAL
- **0 package.json/lock-Changes** → keine npm-install-Phase, aber `cockpit/src`-Changes → **Coolify-Redeploy noetig** (manuell, GitHub-Webhook inaktiv = ISSUE-095).
- 0 docker-compose/next.config/.env-Changes.
- **MIG-051** additiv, Apply im /deploy via `sql-migration-hetzner.md` (postgres-Superuser/base64), Rollback = `DROP TRIGGER`/`DROP FUNCTION`.
- **Rollback gesamt:** App-Image auf REL-047 (V8.12) + `DROP TRIGGER` — unkritisch (additiv).
- **ISSUE-108 (OOM-Risk):** BS-Host speicher-eng — Redeploy braucht ggf. Jitsi-Stop + temp-Swap-Mitigation (im letzten Deploy bewaehrt).
- **Founder-ENV** (Condition C1) noch nicht gesetzt.

### 6. Observability/Operations — PASS
`logSafe`-Redaction haelt PII aus Coolify-Container-Logs (DSGVO + Operator-Hygiene). Sentry vorhanden (V8.12 SLC-911), kein Regress. Health-Checks unveraendert.

### 7. Post-Go-Live-Readiness — PASS
KNOWN_ISSUES aktuell (6/7 resolved, 098 live-gated transparent). Rollback klar. RELEASES.md-REL-Eintrag entsteht in `/go-live`. Cross-Repo-Mirror-Tracker (`CROSS_REPO_V814_SECURITY_HOTFIX.md`) gepflegt.

## 4. Release-Risiken / Conditions vor Go-Live
- **C1 (Founder, blockierend vor /deploy):** `TRACKING_HMAC_SECRET=$(openssl rand -hex 32)` + `GOTRUE_RATE_LIMIT_TOKEN_REFRESH`/`GOTRUE_RATE_LIMIT_EMAIL_SENT` in Coolify-BS-ENV.
- **C2 (Deploy-Gate, schliesst Blocker):** MIG-051 auf Coolify-DB applizieren + Self-Promotion-Live-Smoke (AC-912-10) — erst danach ISSUE-098 resolved.
- **C3 (L-1, Deploy-Smoke):** visuell bestaetigen, dass `/impressum`, `/datenschutz` + 1 Help-Hotspot nach dem breiteren Sanitize weiter rendern.
- **C4 (Ops):** ISSUE-108 OOM-Mitigation beim Redeploy bereithalten.

## 5. Findings

### Low
- **L-1** (aus /qa uebernommen): MT-3 sanitisiert alle `renderLegalMarkdown`-Consumer (breiter als ISSUE-100-Scope) → Deploy-Smoke-Visual-Check (C3).

## 6. Akzeptierte Residual-Risiken
- In-Memory-Rate-Limit (R-912-4) — Single-Container-Internal-Test-Mode-konform, GoTrue-ENV als 2. Bremse.
- ISSUE-098 Production-DB bis /deploy exponiert — Blast-Radius 0 (Single-Founder).
- Beobachtung (kein Blocker): DEC-300 war referenziert ohne Heading (Pre-Slice-Gap, in /backend geschlossen) — IMP-Kandidat fuer Dev-System (dangling-ID-Reference-Check).

## 7. Required vor Go-Live
Keine Code-Fixes. Nur Conditions C1-C4 (operativ, im /go-live + /deploy abzuarbeiten).

## Recommended next step
**`/go-live V8.14`** — formaler Release-Gate (GO mit accepted-risks + Conditions C1-C4 als Deploy-Vorbedingungen), REL-Eintrag in RELEASES.md. Danach `/deploy V8.14` → `/post-launch` T+24h.
