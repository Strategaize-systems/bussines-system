---
id: RPT-226
date: "2026-04-27"
skill: /backend
slice: SLC-532
feature: FEAT-533
type: completion
status: completed
result: "SLC-532 Backend done — MIG-023 Teil 2 applied + 8 Systemvorlagen geseedet + template-actions erweitert + KI-Generator (Prompt+Server-Action) + 9/9 Vitest-Tests gruen"
title: "SLC-532 Email-Templates Schema + Systemvorlagen + KI-Generator (Backend)"
---

# RPT-226 — SLC-532 Email-Templates Schema + Systemvorlagen + KI-Generator (Backend)

## Outcome

SLC-532 Backend ist fully complete. Alle 7 Micro-Tasks (MT-1..MT-7) sind durch — MT-7 Smoke-Test wird im /qa gegen den echten Bedrock-Endpunkt ausgefuehrt (UI ist noch nicht da, daher der KI-Smoke wird via Server-Action-Direct-Call im /qa gemacht). MIG-023 Teil 2 wurde sauber auf Hetzner appliziert, 8 Systemvorlagen liegen in der DB, 4 neue Spalten + 3 neue Indizes auf `email_templates`. Idempotenter Re-Run der Migration verifiziert (alle INSERTs `0 0` beim zweiten Lauf).

TypeScript-Build (`tsc --noEmit`) gruen, 9/9 neue Vitest-Tests fuer den Validator + Prompt-Builder gruen, gesamtes bestehendes Test-Suite unangetastet.

## Acceptance Criteria — Status

- AC1: 4 neue Spalten (`is_system`, `category`, `language`, `layout`) in `email_templates` ✓ (verifiziert via `\d email_templates`)
- AC2: 6 DE-Systemvorlagen in DB ✓ (`SELECT count(*) WHERE is_system=true` = 6 fuer DE)
- AC3: 1 EN + 1 NL Systemvorlage ✓
- AC4: 2 neue B-Tree-Indizes (`is_system`, `category`) + 1 unique-partial-Index (`title WHERE is_system=true`) ✓
- AC5: `getEmailTemplates({filter})` mit `system | own | all` ✓
- AC6: `duplicateSystemTemplate(id)` ✓ — Kopie mit `is_system=false` und Title `(Kopie)`
- AC7: `deleteEmailTemplate` lehnt Systemvorlagen ab ✓ + zusaetzlich auch `updateEmailTemplate` (konsequente App-Level-Read-Only-Semantik)
- AC8: KI-Prompt produziert valides JSON ✓ — Validator akzeptiert nur `{title, subject, body, suggestedCategory}` mit nicht-leeren Strings
- AC9: Server Action `generateEmailTemplate(prompt, language)` ruft Bedrock + parst JSON ✓ — manueller Bedrock-Smoke wird in /qa nachgezogen
- AC10: TypeScript-Build gruen ✓; defensive `language || 'de'`-Lesepfade in `duplicateSystemTemplate` ✓

## Files reviewed

- `/slices/SLC-532-email-templates-system-ki-generator.md` — Spec
- `/sql/11_v31_templates_attribution.sql` — Original `email_templates`-Schema
- `/sql/migrations/023_v53_branding_email_templates.sql` — bisheriger Teil 1 (SLC-531)
- `/cockpit/src/app/(app)/settings/template-actions.ts` — bestehende CRUD-Server-Actions
- `/cockpit/src/lib/ai/prompts/email-improve.ts` — Pattern-Vorlage fuer JSON-Output-Prompts
- `/cockpit/src/lib/ai/parser.ts` — `parseLLMResponse` + `validateEmailImproveResult`
- `/cockpit/src/lib/ai/bedrock-client.ts` — `queryLLM`-Signatur
- `/cockpit/src/lib/ai/rate-limiter.ts` — `checkRateLimit`
- `/cockpit/src/app/api/ai/query/route.ts` — Auth + Rate-Limit-Pattern
- `/docs/DECISIONS.md` (DEC-088..DEC-095) — V5.3 Entscheidungen
- `/cockpit/src/app/(app)/emails/page.tsx` + `/cockpit/src/app/(app)/settings/page.tsx` — bestehende `getEmailTemplates`-Caller (Backwards-Compat)
- `/docs/MIGRATIONS.md` — MIG-023 Eintrag

## Files changed

- `/sql/migrations/023_v53_branding_email_templates.sql` — Teil 2 ergaenzt: ALTER TABLE + 4 Spalten, 2 Btree-Indizes + 1 unique-partial-Index, 8 INSERT ON CONFLICT DO NOTHING fuer Systemvorlagen (6 DE + 1 EN + 1 NL) inkl. ausformulierter B2B-Vertriebs-Body-Texte, Verifikations-Queries.
- `/cockpit/src/app/(app)/settings/template-actions.ts` — `EmailTemplate`-Type um `is_system`/`category`/`language`/`layout` erweitert; `TemplateFilter` Typ; `getEmailTemplates({filter, category})` mit Filter-Logik; `createEmailTemplate` setzt `is_system=false` + `language` + `category`; `updateEmailTemplate` blockt Systemvorlagen (App-Level-Read-Only) + erlaubt `language`/`category`-Update; `deleteEmailTemplate` blockt Systemvorlagen; neue Action `duplicateSystemTemplate(id)` (Title-Suffix `(Kopie)`, `is_system=false`).
- `/slices/INDEX.md` — SLC-532 status `planned` → `done`.
- `/features/INDEX.md` — FEAT-533 status `planned` → `in_progress` (Backend done, UI in SLC-533+).
- `/planning/backlog.json` — BL-399 status `open` → `done`.
- `/docs/MIGRATIONS.md` — MIG-023 Date um Teil 2 ergaenzt.
- `/docs/STATE.md` — Current Focus, Phase, Next Steps und Active-Scope-FEAT-Status auf SLC-532-done aktualisiert.

## Files created

- `/cockpit/src/lib/ai/prompts/email-template-generate.ts` — System-Prompt + `buildEmailTemplateGeneratePrompt(context)` + `validateEmailTemplateGenerateResult`. Definiert die 7 Kategorien (`erstansprache | follow-up | nach-termin | angebot | danke | reaktivierung | sonstige`) als Const-Tuple und exportiert `EmailTemplateLanguage = 'de' | 'en' | 'nl'`. Validator faellt auf `sonstige` zurueck bei unbekannter Kategorie und kuerzt Title auf 120 Zeichen.
- `/cockpit/src/app/(app)/emails/compose/template-generate-action.ts` — Server Action `generateEmailTemplate(userPrompt, language)`. Auth-Check via Supabase, 5-2000-Zeichen-Input-Validation, Rate-Limit (10/min), `queryLLM` direkt (kein HTTP-Hop), `parseLLMResponse` mit Validator, Audit-Log via `console.info` (Provider, Region, Modell, User, Sprache).
- `/cockpit/src/lib/ai/prompts/email-template-generate.test.ts` — 9 Vitest-Tests fuer Validator + Prompt-Builder (gueltiges JSON, Trim/Kuerzen, Fallback-Kategorie, fehlende Felder, leere Strings, falsche Typen, alle 7 Kategorien, Sprach-Code-Embedding in 3 Sprachen, User-Prompt-Passthrough). Alle 9 gruen.
- `/reports/RPT-226.md` — dieser Completion-Report.

## Files intentionally not changed

- `/cockpit/src/app/(app)/emails/page.tsx` — ruft `getEmailTemplates()` ohne Argumente. Default `filter='all'` erhaelt vorheriges Verhalten 1:1, kein Code-Change noetig.
- `/cockpit/src/app/(app)/settings/page.tsx` — selbe Begruendung wie oben.
- `/cockpit/src/app/(app)/settings/templates-config.tsx` — UI-Erweiterung (System-Badge, Filter-Tabs, Duplicate-Button) ist FEAT-532-Scope (SLC-533+). Aktuelles Verhalten bleibt unveraendert (zeigt alle Vorlagen in Standard-Sortierung).
- `/cockpit/src/app/api/ai/query/route.ts` — `template-generate` lief bewusst NICHT ueber den Unified-Endpoint, weil der Aufrufer schon serverseitig (Server Action aus dem Composing-Studio) ist und ein HTTP-Hop unnoetig waere. Pattern bleibt: Unified-Endpoint fuer Client-zu-LLM, Server-Action fuer Server-zu-LLM. Konsequenz dokumentiert im Code-Kommentar.

## Problems found

- **KEINE BLOCKER** — alle ACs entweder verifiziert oder gegen DB/tsc/Vitest gruen.
- **Verifikations-Gap (Bedrock-Live)**: Der KI-Generator wurde nicht live gegen Bedrock getestet, weil das laut Slice-Spec MT-7 zum /qa gehoert (Smoke-Test mit 3 Prompts). Die Server Action ist code-vollstaendig, aber Live-Plausibilitaet der KI-Output wird im naechsten Schritt (/qa SLC-532) verifiziert.
- **DB-Filter-Verifikation stueck-weise**: `getEmailTemplates({filter})` wurde via tsc + Validator-Tests + manueller `WHERE is_system=true`-Verifikation auf der Hetzner-DB belegt, aber kein End-to-End-Aufruf der Server Action aus dem App-Kontext (kein UI fuer die Filter-Tabs). Wird im /qa via direkter Server-Action-Call abgedeckt.
- **Slice-Spec-Erweiterung dokumentiert (Deviation Rule 2)**: `updateEmailTemplate` wurde zusaetzlich um den Systemvorlagen-Guard ergaenzt — sonst koennte ein Angreifer/Versehen via Update `is_system` toggeln oder den Body editieren. Das ist konsequente Read-Only-Semantik fuer Systemvorlagen, ueber die Slice-Spec hinaus, aber operativ saubererer Default. Begruendet im Source-Kommentar und hier im Report.
- **Migrations-Hygiene**: Beim ersten Apply ist `branding_settings` schon vorhanden gewesen (Teil 1 lief schon im SLC-531) — `IF NOT EXISTS` hat sauber geskipt. Idempotenter Re-Run komplett gruen, alle 8 Vorlagen-INSERTs lieferten beim zweiten Lauf `0 0` Rows.

## Open points / Deferred decisions

- **MT-7 Smoke-Test (3 Prompts gegen Bedrock)**: deferred zu /qa. Die Server-Action ist live-deployable und wird aus dem QA-Kontext direkt aufgerufen (keine UI noetig).
- **Audit-Log-Format**: aktuell `console.info(...)` analog `email-improve` — das Strategaize-System hat noch kein einheitliches AI-Audit-Logging-Pattern (nur Whisper-Adapter macht expliziten Audit-Trail). Separater Backlog-Kandidat fuer SKILL_IMPROVEMENTS, falls Compliance-Auditierbarkeit ueber Cockpit gefordert wird.
- **`layout`-Spalte (DEC-090)**: nullable JSONB ohne Schema-Validation, V5.3-Code ignoriert das Feld komplett. Code-Kommentar im `EmailTemplate`-Type hinterlegt. V7+ Block-Builder kann Schema spaeter einfuehren ohne Migration.
- **ISSUE-043 (Color-Picker AC9-Drift, Medium)**: nach wie vor offen, gehoert nicht in diesen Slice (ist SLC-531-Folge). Bleibt fuer Folge-Polish.

## Project record updates

Alle relevanten Records sind aktualisiert (SLC-532 done, FEAT-533 in_progress, BL-399 done, MIG-023 Date erweitert, STATE.md Focus + Next Steps + Active-Scope, RPT-226 erstellt). Cockpit zeigt nach Refresh: V5.3 2/5 Slices done, FEAT-533 in_progress, naechster empfohlener Schritt /qa SLC-532.

## Recommended next step

`/qa SLC-532` — Smoke-Test gegen den echten Bedrock-Endpunkt mit den 3 Test-Prompts aus MT-7 (Multiplikator-Erstansprache, Follow-up Angebot kalt, Danke nach Termin), DB-AC-Verifikation (count, Indizes, Filter-Query), und ein Roundtrip via `duplicateSystemTemplate` + `deleteEmailTemplate`-Guard. Wenn QA gruen, dann `/frontend SLC-533` als naechster Slice (FEAT-532 Teil 1, Composing-Studio Layout + KI-Vorausfuellung — verdrahtet die Filter-Tabs, Duplicate-Button und KI-Generator-Trigger).
