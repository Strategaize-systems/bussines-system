---
id: RPT-273
date: "2026-05-01"
skill: /qa
slice: SLC-561
feature: FEAT-561
type: completion
status: completed
result: "PASS-with-deferrals. 11/19 ACs direkt verifiziert (Schema + DB-Constraints + Build/Test/Lint + Wiring + Stub-Free). 8/19 ACs warten auf Coolify-Redeploy + Live-Browser-Smoke. Keine Blocker."
title: "SLC-561 Payment-Terms Foundation — /qa"
---

## QA Scope
SLC-561 /backend Code-Stand `57e32e3` gegen die 19 Acceptance Criteria + Slice-QA-Focus-Liste. Code-Review (Stub-Detection, Wiring-Chain), DB-Smokes via SSH (Schema, Constraints, Idempotenz), Build/Lint/Vitest. Live-Browser-Smoke deferred bis Coolify-Redeploy (User-Aktion).

## Result: PASS (with deferrals)

11/19 ACs direkt verifiziert. 8/19 ACs warten auf Live-Browser-Smoke nach Coolify-Redeploy. Keine Blocker. Zwei Low-Severity-UX-Nits dokumentiert.

## Verification Coverage

### Direkt verifiziert (11)

| AC | Status | Beweis |
|----|--------|--------|
| AC1 | ✓ | MIG-027 1./2./3. Apply auf Hetzner — Idempotenz 3x bestaetigt |
| AC2 | ✓ | `\d payment_terms_templates` zeigt 6 Spalten + UNIQUE-Index `WHERE is_default=true` + RLS |
| AC3 | ✓ | `\d proposal_payment_milestones` zeigt 9 Spalten + UNIQUE `(proposal_id, sequence)` + FK CASCADE + 2 CHECK + RLS |
| AC4 | ✓ | `proposals.skonto_percent NUMERIC(4,2)` + `skonto_days INTEGER` + 3 CHECK-Constraints (`proposals_skonto_percent_check`, `proposals_skonto_days_check`, `proposals_skonto_both_or_none`) |
| AC5 | ✓ | `meetings.briefing_generated_at TIMESTAMPTZ` + Partial-Index `idx_meetings_briefing_pending ON meetings(scheduled_at) WHERE briefing_generated_at IS NULL AND deal_id IS NOT NULL` |
| AC6 | ✓ | `user_settings.briefing_trigger_minutes INT NOT NULL DEFAULT 30` + `briefing_push_enabled BOOLEAN NOT NULL DEFAULT true` + `briefing_email_enabled BOOLEAN NOT NULL DEFAULT true` |
| AC7 | ✓ | `SELECT * FROM payment_terms_templates` zeigt Seed `b2f63256-...` "30 Tage netto" mit `is_default=true` |
| AC8 | ✓ | 5 V5.5-Proposals lesbar, alle alten Spalten unveraendert, neue Skonto-Spalten NULL |
| AC17 | ✓ | `npm run build` PASS (`/settings/payment-terms` Route registriert) |
| AC18 | ✓ | Vitest 97/97 PASS — keine Regressions |
| AC19 | ✓ | `npx eslint` clean fuer alle SLC-561-Files (221 Pre-Existing-Issues unbeeinflusst) |

### Deferred bis Coolify-Redeploy (8)

| AC | Begruendung |
|----|-------------|
| AC9 | V5.5-PDF-Regression nur in Live-UI testbar (`/proposals/[id]/edit` "PDF generieren") — Code-Pfad unbetroffen, geringes Risiko |
| AC10 | `/settings/payment-terms` Page-Render — Build verifiziert SSR-Fitness, Live-Render erst nach Deploy |
| AC11 | Create-Dialog-Pfad — Click-Smoke nur live moeglich |
| AC12 | Edit-Dialog mit Pre-Fill — Click-Smoke nur live moeglich |
| AC13 | Delete-Confirm + Default-Block-Toast — Click-Smoke nur live moeglich |
| AC14 | Default-Toggle Badge-Wandern — Click-Smoke nur live moeglich (DB-Race separat verifiziert ✓) |
| AC15 | Audit-Log-Eintraege fuer create/update/delete — werden on-action geschrieben, Live-Smoke noetig |
| AC16 | Sidebar-Active-State zwischen `/settings/branding` und `/settings/payment-terms` — DOM-Render nur live |

## Stub-Detection
Grep-Pattern (`TODO|FIXME|HACK|console.log|debugger|empty handlers|placeholder returns`) auf allen 7 SLC-561-Files: **0 matches**. Code ist stub-frei.

## Wiring-Verification

```
PaymentTermsManager (Client)        ─> 5 Server Actions imported
  ├─ list  via page.tsx (SSR)       ─> listPaymentTermsTemplates ─> SELECT public.payment_terms_templates ✓
  ├─ list  via refresh() (Client)   ─> listPaymentTermsTemplates ─> SELECT (idempotent) ✓
  ├─ create button + Dialog         ─> createPaymentTermsTemplate ─> INSERT + audit_log + revalidatePath ✓
  ├─ edit button + Dialog           ─> updatePaymentTermsTemplate ─> SELECT-then-UPDATE + audit_log + revalidatePath ✓
  ├─ delete button + Confirm        ─> deletePaymentTermsTemplate ─> SELECT-then-DELETE (default-block) + audit_log + revalidatePath ✓
  └─ "Default setzen" button        ─> setDefaultPaymentTermsTemplate ─> UPDATE (clear) + UPDATE (set) + audit_log + revalidatePath ✓

SettingsLayout (Client)             ─> usePathname()              ─> Active-State auf "Branding" / "Zahlungsbedingungen"
  └─ TOP_LEVEL_PATHS check          ─> Settings-Landing rendert OHNE Sidebar (kein Breaking-Change)
```

Action-Signaturen passen exakt zu den Call-Sites:
- `createPaymentTermsTemplate({label, body})` — Manager ruft mit `{label, body}` ✓
- `updatePaymentTermsTemplate({id, label, body})` — Manager ruft mit `{id: mode.template.id, label, body}` ✓
- `deletePaymentTermsTemplate({id})` — Manager ruft mit `{id: mode.template.id}` ✓
- `setDefaultPaymentTermsTemplate({id})` — Manager ruft mit `{id: tpl.id}` ✓
- `listPaymentTermsTemplates()` — Manager ruft ohne Args ✓

## DB-Constraint-Smokes (transactional, ROLLBACK)

```
✓ INSERT (label='14 Tage netto', is_default=false) + INSERT (label='Vorkasse', is_default=false)
  → ORDER BY is_default DESC, label ASC liefert: "30 Tage netto" (default) → "14 Tage netto" → "Vorkasse"

✓ INSERT (label='Test-Default', is_default=true) bei bestehender Default-Row
  → ERROR: duplicate key value violates unique constraint "idx_payment_terms_templates_default"
  → UNIQUE-Default-Mutex via Partial-Index funktioniert

✓ UPDATE proposals SET skonto_percent=2.5 (ohne skonto_days)
  → ERROR: violates check constraint "proposals_skonto_both_or_none"

✓ UPDATE proposals SET skonto_percent=15.0, skonto_days=14
  → ERROR: violates check constraint "proposals_skonto_percent_check"

✓ UPDATE proposals SET skonto_percent=2.5, skonto_days=14
  → 1 row updated, RETURNING zeigt 2.50/14

✓ audit_log Schema: kein CHECK auf entity_type, actor_id nullable
  → entity_type='payment_terms_template' wird DB-seitig akzeptiert
```

## Findings

### Blocker
keine

### High
keine

### Medium
- **Live-Browser-Smoke deferred (8 ACs)**. Aktueller Live-Stand ist V5.5.1 (Image-Tag `4415928`). Nach `git push 57e32e3` muss User Coolify manuell triggern (Memory: feedback_manual_deploy.md). Nach Redeploy: Live-Smoke der CRUD-Pfade + Sidebar-Active-State + V5.5-Regression `/proposals/[id]/edit` + Audit-Log-SQL.

### Low
- **setDefault-Race-Error-Text**: Bei zwei parallelen `setDefaultPaymentTermsTemplate`-Calls produziert die UNIQUE-Index-Verletzung den rohen Postgres-Error-Text ("duplicate key value violates..."). Spec-konform (Risk 3 in SLC-561), aber UX-unschoen. Mitigation in V7+ via Try/Catch und benutzerfreundlicher Meldung "Default wurde bereits gesetzt — bitte Liste aktualisieren".
- **Kein Success-Toast**: Erfolg-Mutationen feedbacken nur via Card-Refresh. Kein visueller "Vorlage gespeichert"-Toast. Bestehendes Repo hat kein Toast-System. /qa entscheidet: ausreichend fuer V5.6 Internal-Test-Mode. Backlog-Kandidat fuer spaetere Polish-Iteration.

## Open Risks / Missing Checks

- `PaymentTermsManager.refresh()` ist Fire-and-Forget (`void`). User sieht kurz Stale-State, dann Auto-Update. OK fuer Single-User V5.6.
- Pre-Deploy ist die `/settings/payment-terms`-Route NICHT live (V5.5.1-Image hat sie nicht). Coolify-Redeploy ist Voraussetzung fuer alle UI-Tests.
- Layout-Wrapper `<div>` (statt `<main>`) verhindert HTML-`<main>`-Nesting-Konflikt mit den Sub-Pages. Visual-Layout-Smoke nach Deploy noetig (Sidebar-Padding nicht ueberlappend mit Branding-Page-Padding).

## Readiness Assessment

Code- und DB-seitig ist SLC-561 ready fuer Coolify-Redeploy. Schema-Migration ist appliziert + verifiziert (idempotent), Server Actions wired, UI-Komponenten compiliert + Lint-clean. Keine offene technische Sorge.

Live-Browser-Smoke ist Pre-Bedingung fuer Slice-Status `done`. Bis dahin: `in_progress`.

## Recommended Next Step

1. **User-Aktion: Coolify-Redeploy** des aktuellen Code-Stands (`57e32e3` auf branch `main`). Zielt auf Image-Tag-Update der `app`-Container.
2. **Live-Browser-Smoke** nach Deploy (kann Agent ueber playwright/chrome-devtools machen):
   - `/settings/payment-terms` lautet auf
   - Sidebar zeigt aktiven "Zahlungsbedingungen"-Link
   - "+ Neue Vorlage": Dialog → Save → Card erscheint
   - "Bearbeiten": Dialog vorbefuellt → Save → Card aktualisiert
   - "Default setzen" auf Non-Default: Badge wandert
   - "Loeschen" auf Default: Hinweis "Setze zuerst ein anderes Template als Default"
   - "Loeschen" auf Non-Default: Confirm → Card verschwindet
   - V5.5-Regression: `/proposals/[id]/edit` PDF-Generierung weiterhin funktional
   - Audit-SQL: `SELECT * FROM audit_log WHERE entity_type='payment_terms_template' ORDER BY created_at DESC LIMIT 10;`
3. Nach Live-Smoke-PASS: SLC-561 Status auf `done`, BL-413 auf `done`. Dann **/backend SLC-562**.
