---
id: RPT-095
date: "2026-04-12"
skill: /qa
slice: SLC-401
feature: FEAT-405, FEAT-407, FEAT-408, FEAT-406
type: completion
status: completed
reviewOf: RPT-094
result: "PASS — alle lokalen Checks bestanden, DB-Verifikation auf Hetzner offen (SSH nicht verfuegbar)"
title: "QA SLC-401 — V4 Schema-Migration"
---

# QA Report — SLC-401 V4 Schema-Migration

## QA Scope

Vollstaendige Pruefung aller 6 Acceptance Criteria fuer SLC-401:
1. 5 neue Tabellen existieren in PostgreSQL
2. calendar_events hat 4 neue Spalten
3. RLS auf allen neuen Tabellen aktiviert
4. imapflow + mailparser installiert
5. TypeScript Types definiert
6. Build laeuft ohne Fehler

## Result: PASS (mit Einschraenkung)

### Verified (lokal)

| Check | Result | Details |
|-------|--------|---------|
| Dependencies | PASS | imapflow@1.3.1, mailparser@3.9.8, @types/mailparser@3.4.6 |
| TypeScript Types (email.ts) | PASS | EmailMessage, EmailThread, EmailSyncState, EmailMessageWithRelations, CalendarEventSource, CalendarSyncStatus — alle Felder matchen SQL exakt |
| TypeScript Types (ai-queue.ts) | PASS | AIActionQueueItem, AIFeedback, AIActionQueueItemWithContext — alle Felder matchen SQL exakt |
| SQL Migration (12_v4_migration.sql) | PASS | 5 Tabellen + ALTER calendar_events + RLS + Grants + 16 Indexes — idempotent, gut strukturiert |
| Next.js Build | PASS | Build kompiliert fehlerfrei, alle 22 Routes |
| Stub Detection | PASS | Keine TODOs, FIXMEs, console.logs, Placeholder-Returns in neuen Dateien |
| MIG-010 Dokumentation | PASS | MIGRATIONS.md Eintrag vorhanden mit Rollback Notes |

### Not verified (SSH nicht verfuegbar)

| Check | Status | Workaround |
|-------|--------|------------|
| 5 Tabellen auf Hetzner | offen | User muss via psql verifizieren |
| calendar_events Spalten | offen | User muss via psql verifizieren |
| RLS Policies aktiv | offen | User muss via psql verifizieren |
| Grants angewendet | offen | User muss via psql verifizieren |

**Verifikations-Command fuer User:**
```bash
ssh root@91.98.20.191
docker exec -it supabase-db psql -U postgres -d postgres

-- Tabellen pruefen:
SELECT table_name FROM information_schema.tables WHERE table_schema='public' AND table_name IN ('email_threads','email_messages','email_sync_state','ai_action_queue','ai_feedback');

-- calendar_events Spalten:
SELECT column_name FROM information_schema.columns WHERE table_name='calendar_events' AND column_name IN ('source','external_id','sync_status','booking_link');

-- RLS:
SELECT tablename, rowsecurity FROM pg_tables WHERE tablename IN ('email_threads','email_messages','email_sync_state','ai_action_queue','ai_feedback');
```

## Findings

### Low

1. **MIG-010 Date war "TBD"** — in `/docs/MIGRATIONS.md`
   Migration wurde am 2026-04-12 ausgefuehrt, Date war noch Platzhalter.
   → **Auto-fixed** (Deviation Rule 1): Date auf 2026-04-12 gesetzt.

2. **STATE.md Next Steps veraltet** — in `/docs/STATE.md`
   Step 1 war noch "SLC-401 SQL auf Hetzner ausfuehren", obwohl bereits erledigt.
   → **Auto-fixed** (Deviation Rule 1): Next Steps aktualisiert (SLC-402 → SLC-403 → SLC-404).

3. **Slice-Spec Dateiname-Diskrepanz** — in `/slices/SLC-401-v4-schema-migration.md`
   Spec referenziert `cockpit/sql/10_v4_migration.sql`, tatsaechliche Datei ist `sql/12_v4_migration.sql`.
   → Kosmetisch, kein Impact auf Funktionalitaet.

4. **imapflow ohne TypeScript-Typen** — kein `@types/imapflow`, keine eingebauten `.d.ts`
   Wird in SLC-402 relevant. Loesung: Declaration File oder `require()` statt `import`.
   → Kein Blocker fuer SLC-401 (kein imapflow-Code geschrieben).

## Type-Schema Cross-Check (Detail)

### email_messages (22 Spalten SQL → 22 Felder TS)
Alle Felder 1:1 gemappt. Nullability korrekt. JSONB-Felder als Record<string, unknown> oder typisiert (EmailAttachment[]).

### email_threads (9 Spalten SQL → 9 Felder TS)
Vollstaendig. FK-Referenzen als string | null.

### email_sync_state (9 Spalten SQL → 9 Felder TS)
Vollstaendig. Status-Enum korrekt eingeschraenkt ("idle" | "syncing" | "error").

### ai_action_queue (16 Spalten SQL → 16 Felder TS)
Vollstaendig. Enums korrekt typisiert (AIActionType, AIActionSource, AIActionStatus).

### ai_feedback (5 Spalten SQL → 5 Felder TS)
Vollstaendig. feedback_type als Union-Type.

### calendar_events Extension
CalendarEventSource + CalendarSyncStatus Types definiert, matchen SQL-Kommentare.

## Readiness Assessment

**SLC-401 ist PASS.** Alle lokalen Acceptance Criteria erfuellt. DB-seitige Verifikation muss vom User auf Hetzner bestaetigt werden (SSH war in dieser Session nicht verfuegbar).

## Recommended Next Step

→ User bestaetigt DB-Verifikation auf Hetzner (kurzer psql-Check)
→ Dann: `/backend` fuer SLC-402 (IMAP-Sync Service)
