---
id: RPT-672
date: "2026-07-07"
skill: /code-review
slice: SLC-914
feature: FEAT-926
type: review
status: reviewed
reviewOf: RPT-669
result: "ultrareview-Nachbau (lokal high-effort, 8 Finder-Winkel + 1-Vote-Verify) über c9efb41..HEAD: 45 Kandidaten → 10 verifizierte Findings (1 refuted). 1 High (ISSUE-138 CSP-Enforce blockt SIP-Telefonie+Mikrofon, Feature deployed+scharf), 4 Medium (139 E-Mail-Bilder, 140 MIG-054-mixed-owner-freeze Multi-User-latent, 141 startMeeting-Dead-Button, 142 Consent fail-open). → ISSUE-138..142 + BL-520."
title: "Code-Review (ultrareview-Nachbau) V8.16-Range — 5 Regressionen (1 High)"
---

# Code-Review V8.16-Range (c9efb41..HEAD) — ultrareview-Nachbau

## Kontext

Founder wollte `claude ultrareview c9efb41` fahren (erster freier Cloud-Run). Der Cloud-Run wird vom Founder im Terminal gestartet; ich habe als Vorab-Probe einen **lokalen High-Effort-Multi-Agent-Review** über denselben Range gefahren (Workflow `wf_defeb900-8a3`, 35 Agenten, 8 Finder-Winkel + adversarische 1-Vote-Verifikation, recall-biased). Range: `c9efb41..HEAD` (`7f1a57f`) = kompletter V8.16-Bogen seit dem letzten Fable-5-Audit-Stand: startMeeting User-Client-Switch, MIG-054, CSP Phase-B enforce, AnswerPane/rate-limit-Fixes.

## Ergebnis: 45 Kandidaten → 27 dedupliziert → **10 verifiziert / 1 refuted**

Die 10 verifizierten Findings clustern zu **5 distinkten Regressionen**, alle durch V8.16 eingeführt und von allen bisherigen QA-Stufen (Slice-/qa, Gesamt-/qa RPT-665, /final-check RPT-666, /post-launch) übersehen:

| # | Sev | Kern | Warum QA es verpasste |
|---|---|---|---|
| **ISSUE-138** | **High** | CSP-Enforce-Flip blockt den SIP-WebSocket (`connect-src` ohne `wss://sip`) + `Permissions-Policy: microphone=()` blockt das Mikro → Click-to-Call gebrochen | Unter Report-Only lief alles; `csp-check.mjs` probt nur Hydration auf /login+/mein-tag, nicht die Telefonie-Flows |
| ISSUE-139 | Medium | CSP-Enforce blockt Remote-Bilder in der Inbound-E-Mail-Vorschau (`img-src` ohne `https:`, srcdoc erbt CSP) | Viewer wird vom Probe nicht getestet; silent |
| ISSUE-140 | Medium | MIG-054 AND-Conjunction friert legitime mixed-owner-Rows für Non-Admins ein (UPDATE+INSERT über 9 Tabellen) | Heute durch `is_admin()`-Single-User maskiert; Multi-User-latent |
| ISSUE-141 | Medium | startMeeting-Ownership-Gate → Silent-Dead-Button in Mein Tag (rohe unsichtbare contact_id, `res.error` ignoriert) | Tests treffen nur die Server-Action, nicht den Caller |
| ISSUE-142 | Medium | checkConsentStatus fail-OPEN mit RLS-Client (RLS-weggefilterte Kontakte → Recording ohne Consent, TOCTOU) | §201/DSGVO-Race, heutiger Caller hat Pre-Gate |

Refuted (1): meetings.ts:152 recording-UPDATE ohne error-check — provably impossible, weil self-owned (`owner_user_id: user.id`), RLS kann bei gleicher `auth.uid()` nicht denyen; Pattern zudem pre-existing.

## Live-Verifikation ISSUE-138 (High)

Direkt am deployten Stand bestätigt: enforced `Content-Security-Policy` mit `connect-src 'self' https://*.sentry.io …/supabase …bedrock…` (**kein `wss://sip`**), `Permissions-Policy: microphone=()`. Asterisk/SIP-Infra live konfiguriert (`SIP_TRUNK_ENABLED`, `ASTERISK_WEBRTC_PASSWORD`, `SERVICE_FQDN_ASTERISK` gesetzt); `NEXT_PUBLIC_SIP_DOMAIN` leer → Client-Fallback `sip.strategaizetransition.com`. Der Break ist real; akuter Impact nur bei aktiver Telefonie-Nutzung (Single-Founder-Mode).

## Klasse / Prozess-Lehre

ISSUE-138+139 sind exakt die Klasse, vor der `security-headers-live-smoke.md` warnt — nur eine Ebene tiefer: der funktionale Browser-Smoke (`csp-check.mjs`) prüfte Hydration, aber **nicht die Feature-Flows, deren Endpoints in `connect-src`/`img-src`/`Permissions-Policy` fehlen** (Telefonie-WSS, E-Mail-Remote-Bilder, Mikrofon). Report-Only-Burn-In fängt nur Flows, die im Fenster tatsächlich ausgeübt werden. → IMP + Playbook-Ergänzung (Feature-Flow-Coverage im CSP-Enforce-Gate).

## Records

- `docs/KNOWN_ISSUES.md`: ISSUE-138 (High) + ISSUE-139/140/141/142 (Medium) angelegt.
- `planning/backlog.json`: BL-520 (Fix-Bundle, high) angelegt.

## Empfehlung

1. **ISSUE-138 zuerst entscheiden** (einziges akutes): Hotfix-Slice (connect-src + Permissions-Policy + Feature-Flow-Smoke) ODER CSP temporär zurück auf Report-Only bis Fix. Founder-Entscheidung nötig (Security-Posture vs. Telefonie-Funktion).
2. ISSUE-140 in den Multi-User-Schritt bündeln (mit BL-519 Passwort-vergessen) — es ist der eigentliche Multi-User-Blocker.
3. 139/141/142 in BL-520 mit-abräumen.
4. Cloud-`claude ultrareview c9efb41` (Founder) als unabhängige Gegenprobe — Ergebnismengen danach gegen diese 5 Findings dedupliziten.
