---
id: RPT-027
date: "2026-04-06"
skill: /qa
slice: SLC-208
feature: BL-127
type: completion
status: completed
result: "PASS — Lead-Management-Pipeline mit 7 Stages, Route, Sidebar, Move-to-Pipeline"
title: "QA SLC-208: Lead-Management-Pipeline"
---

## QA Report — SLC-208

### Result: PASS

### Acceptance Criteria — All passed
- SQL seed: Lead-Management pipeline (fixed UUID) + 7 stages
- Pipeline insert uses `ON CONFLICT (id) DO NOTHING` — idempotent for pipeline
- /pipeline/leads route with full PipelineView (filter, forecast, referrals)
- Sidebar entry "Leads" under Pipeline group
- PIPELINE_SLUGS extended: "leads" → "Lead-Management"
- `moveDealToPipeline()`: moves deal to first stage of target, resets status to active, logs activity
- Deal-Detail edit tab: "In andere Pipeline verschieben" buttons (filtered to exclude current pipeline)
- `getPipelines()` loaded in DealDetailSheet for pipeline list
- Build: OK, /pipeline/leads route registered

### Stub Detection: Clean
- No TODO/FIXME/HACK comments
- No console.log/debugger statements
- No empty handlers
- No hardcoded credentials

### Wiring: All 6 steps verified
1. Sidebar → /pipeline/leads link ✓
2. leads/page.tsx → getPipelineBySlug("leads") → PipelineView ✓
3. DealDetailSheet → getPipelines() → allPipelines state ✓
4. Pipeline move buttons → moveDealToPipeline(dealId, pipelineId) ✓
5. moveDealToPipeline → pipeline_stages query (first stage) + deals update + activity insert ✓
6. revalidatePath → UI refresh ✓

### Medium
- **SQL stages not fully idempotent**: `pipeline_stages` has no unique constraint on `(pipeline_id, name)`. The `ON CONFLICT DO NOTHING` on the stages insert won't catch duplicates since IDs are auto-generated. Running SQL twice would create duplicate stages. Pipeline itself IS idempotent (fixed UUID). **Mitigation**: Run SQL only once, or add `WHERE NOT EXISTS` check.

### Low
- No "Geparkt" or "Verloren" stage in Lead pipeline (by design — leads are either qualified or not)

### V2.1 Completion Status
All 8 slices done: SLC-201, 202, 203, 204, 205, 206, 207, 208

### Next Step: Gesamt-QA V2.1 → /final-check → UI-Polish Sprint → Go-Live V2.1
