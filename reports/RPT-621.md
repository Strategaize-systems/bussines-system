---
id: RPT-621
date: "2026-06-10"
skill: /qa
slice: V8.12 (Gesamt-QA)
feature: FEAT-921, FEAT-922, FEAT-923
type: completion
status: completed
reviewOf: null
result: "Gesamt-QA V8.12 PASS-CODE-SIDE — 5 Slices, 0B/0H/0M/3L. TSC=0, ESLint 0 V8.12-Regression (142 pre-existing project-debt), Vitest 1390/1390, next build OK. Cross-Slice SLC-907→SLC-911 redactSecrets-Chain wired."
title: "Gesamt-QA V8.12 Defense-in-Depth Sprint (5 Slices)"
---

# Gesamt-QA V8.12 Defense-in-Depth Sprint

## 1. QA-Scope reviewed

Whole-version QA über BS V8.12 (Defense-in-Depth Sprint, Pre-Customer-Live-Closure), alle 5 Code-Side-Slices als Bundle:

| Slice | Thema | Status pre-QA |
|---|---|---|
| SLC-906 | Phase 1 Code-Layer-Closures (7 Server-Actions, assertRole/User-Client-Switch) | done |
| SLC-907 | Logger-Redaction-Layer (`redactSecrets` + `logSafe`) | done |
| SLC-908 | Passwort-Policy 12+ + zxcvbn (dynamic-import Lazy-Chunk) | done |
| SLC-910 | CSP-Headers Phase-A (Report-Only + 5 Hardening-Header) | in_progress (Phase-A) |
| SLC-911 | Sentry-Observability EU Frankfurt + beforeSend-Redact | in_progress |

SLC-909 (LLM-Cost-Cap) DEFERRED per DEC-288 (`ai_cost_ledger` existiert in BS nicht, A-V812-2 Live-Check) — nicht im QA-Scope.

Gesamt-QA-Fokus (über Per-Slice-QA hinaus): Cross-Slice-Konsistenz + Full-Suite-Quality-Gates über den kombinierten Endstand aller 5 Slices.

## 2. Result: PASS (Code-Side) — mixed nur durch deferred Live-Smoke-Portionen

**0 Blocker / 0 High / 0 Medium / 3 Low.** Alle Quality-Gates GREEN. 0 V8.12-Regression. Live-Smoke-Portionen (SLC-910 Phase-B strict, SLC-911 Sentry-Dashboard, AC-906-6 5-Pfad) sind per Design post-Deploy und bleiben deferred zum `/deploy V8.12`-Bundle.

### Quality-Gates (kombinierter Endstand, HEAD 4fc02b9)

| Gate | Ergebnis | Detail |
|---|---|---|
| TSC (`tsc --noEmit`) | **0** ✓ | exit 0 |
| Vitest Full-Suite (jsdom) | **1390 / 1390 GREEN** ✓ | 151 Test-Files, 103s, exakt der Post-SLC-910-Baseline = 0 Regression |
| `next build` (standalone) | **exit 0** ✓ | Compiled successfully, voller Route-Tree inkl. `/api/test-sentry` |
| ESLint (V8.12-scope) | **0 V8.12-Regression** ✓ | siehe L-1 |
| Stub-Detection (13 Production-Files) | **CLEAN** ✓ | 2 legitime Treffer (accepted) |

### Stub-Detection (13 V8.12-Production-Files)

CLEAN. Zwei Nicht-Stub-Treffer, beide bereits in Per-Slice-QA akzeptiert:
- `src/lib/logger/index.ts:27,44` — `console[level]` ist der bewusste `logSafe`-Wrapper-Dispatch (kein Debug-Stub).
- `src/components/auth/PasswordStrengthIndicator.tsx:46` — `return null` Render-Guard bei leerem Passwort (legitim).

## 3. Findings by severity

### Blocker / High / Medium
Keine.

### Low

**L-1 — ESLint 142 Full-Project-Errors sind pre-existing Project-Debt, 0 V8.12-Regression.**
Full-Project `npm run lint` zeigt 218 Probleme (142 Errors, 76 Warnings). Scoped-Lint über die 35 V8.12-geänderten Source-Files isoliert nur **4 Errors** (`no-explicit-any`): `deal-products.ts:39`, `goals.ts:67/311/356`. Git-Blame der 4 Zeilen: alle aus **V6-Ära** (SLC-602 `6dd52e7`, SLC-603 `13d198e`, SLC-608 `80a5091`) — von SLC-906 NICHT eingeführt (SLC-906 änderte die createAdminClient/assertRole-Logik, nicht die `any`-typisierten Query-Result-Zeilen). Per-Slice-"ESLint=0 (touched Files)" war auf geänderte Zeilen scoped. **V8.12 hat 0 neue Lint-Errors eingeführt.** `next build` ist von diesen Errors nicht blockiert. → Pre-existing Tech-Debt, Kandidat für künftigen Hygiene-Sprint, nicht V8.12-blockierend. `/final-check` Hygiene-Dimension nimmt das auf.

**L-2 — Doku-Text-Drift in roadmap.json + FEAT-922 ("SLC-910 CSP offen").**
roadmap V8.12-Summary und features/INDEX FEAT-922-Zeile beschreiben SLC-910 noch als "offen (nächster /backend)" — Phase-A ist mittlerweile done (Slice bleibt korrekt `in_progress` bis Phase-B). Die Status-Felder (FEAT-922 `in_progress`, BL-501 `in_progress`, SLC-910 `in_progress`) sind korrekt; nur der Freitext lagged. Wird beim `/go-live`-Re-Baseline ohnehin neu geschrieben. Kein Cockpit-Status-Impact.

**L-3 — Live-Smoke-Portionen deferred zu `/deploy V8.12` (accepted, by design).**
SLC-910 Phase-B strict-Switch + `tests/_probe/csp-check.mjs` 5-Routes-Browser-Smoke (`@playwright/test` noch nicht installiert), SLC-911 Sentry-Dashboard-Test-Event, AC-906-6 5-Pfad-Live-Smoke. Alle drei sind Deploy-Validation, nicht Code-Side-Gates. Per V8.11-Präzedenz im `/deploy`-Bundle. SLC-910 + SLC-911 bleiben deshalb korrekt `in_progress`.

## 4. Cross-Slice-Konsistenz (Gesamt-QA-Kern)

Die Architektur-Dependency-Kette (RPT-608: "SLC-907 ist Pre-Cond für SLC-911 beforeSend") wurde end-to-end verifiziert — das ist der Mehrwert der Gesamt-QA über die isolierten Per-Slice-Läufe:

| Check | Ergebnis |
|---|---|
| X1: SLC-911 `redact-event.ts` importiert `redactSecrets` aus `@/lib/logger` (SLC-907) + wendet es auf event.extra/contexts/tags/user an | ✓ wired |
| X2: `logger/index.ts` re-exportiert `redactSecrets` + `DEFAULT_REDACT_KEYS` als Named-Export (für Sentry-Reuse, AC-907-7) | ✓ |
| X3: `next.config.ts` importiert `buildCSP` + `PERMISSIONS_POLICY` aus `csp.ts`, wired alle 6 Header (CSP-Report-Only + Permissions-Policy + HSTS + nosniff + Referrer-Policy + X-Frame DENY) | ✓ |
| X4: alle 3 Sentry-Configs (server/client/edge) wired `beforeSend: redactSentryEvent` + `sendDefaultPii:false` | ✓ |

Keine Cross-Slice-Drift. Keine doppelten/divergenten Redaction-Implementierungen. Der SLC-907→SLC-911-Single-Source-of-Truth-für-Redaction ist intakt.

## 5. Open risks / missing checks

- Live-Funktionalität von CSP-Report-Only-Reports (Sentry-Security-Endpoint) ungetestet bis Founder `SENTRY_CSP_REPORT_URI` in Coolify setzt + Deploy (L-3, Founder-TODO).
- Sentry-Event-Delivery (EU Frankfurt) erst post-Deploy verifizierbar (SLC-911 Live-Portion).
- Phase-B strict-CSP-Funktional-Smoke (React-Hydration unter strict script-src) erst nach 1-2 Wochen Burn-In + `@playwright/test`-Install (SLC-910 MT-5/MT-6).

Keine dieser Lücken ist ein Code-Side-Blocker; alle sind Teil des geplanten post-Deploy-Verlaufs.

## 6. Readiness assessment

V8.12 ist **Code-Side ready für `/final-check`**. Alle 5 Slices kompilieren, testen und builden sauber im kombinierten Endstand; die Cross-Slice-Redaction-Kette ist verifiziert; 0 neue Regression gegenüber jedem Per-Slice-Baseline. Die offenen Punkte sind ausschließlich deploy-gebundene Live-Validierungen (erwartet) und pre-existing Project-Lint-Debt (nicht V8.12).

Per IMP-950 BLOCKING + module-lifecycle-discipline: V8.12 ist NICHT released bis `/go-live` GO, NICHT stable bis `/post-launch` T+24h. Kein Customer-Live (Internal-Test-Mode).

## 7. Files reviewed (nicht geändert)

- `cockpit/src/lib/security/csp.ts`, `cockpit/next.config.ts` (SLC-910)
- `cockpit/src/lib/logger/redact.ts`, `cockpit/src/lib/logger/index.ts` (SLC-907)
- `cockpit/src/lib/auth/password-policy.ts`, `cockpit/src/components/auth/PasswordStrengthIndicator.tsx` (SLC-908)
- `cockpit/src/lib/monitoring/sentry.ts`, `cockpit/src/lib/monitoring/redact-event.ts`, `cockpit/src/instrumentation.ts`, `cockpit/src/app/global-error.tsx`, `cockpit/sentry.{server,client,edge}.config.ts` (SLC-911)
- 35 V8.12-Source-Files (Scoped-ESLint), `planning/roadmap.json`, `planning/backlog.json`, `features/INDEX.md`, `slices/INDEX.md` (Records-Konsistenz)

## 8. Next workflow step

**`/final-check V8.12`** — Pre-Release-Audit (Code-Quality, Security/Privacy, Dependencies, Secrets, Test-State, Deployment-Readiness) über alle 5 Slices. Gesamt-QA ist die letzte inhaltliche Prüfung vor dem Hygiene-/Security-Gate; beide sind getrennte Schritte. `/final-check` sollte L-1 (Lint-Debt) in der Hygiene-Dimension bewerten.

Danach: `/go-live` REL-047 → `/deploy V8.12` (inkl. SLC-906/907/908/911 Live-Smoke-Bundle + SLC-910 Phase-A Deploy = Start 1-2 Wochen Burn-In) → `/post-launch` T+3h + T+24h. SLC-910 Phase-B strict-Switch (MT-5/MT-6) folgt NACH Burn-In separat.

**Founder-TODO vor `/deploy`:** `SENTRY_CSP_REPORT_URI` in Coolify auf Sentry-Security-Endpoint setzen (sonst Phase-A-CSP-Violations nur browser-console-lokal — Dev-System IMP-1174).
