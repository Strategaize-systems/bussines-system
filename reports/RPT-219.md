---
id: RPT-219
date: "2026-04-26"
skill: /post-launch
slice: V5.2
feature: FEAT-521,522,523,524,525
type: completion
status: completed
reviewOf: RPT-218
result: "STABLE — V5.2 ist seit ~4h in Production stabil. App-Container healthy (Up 4 hours, healthy), alle 17 Service-Container running, 13 von 13 mit Healthcheck haben healthy-Status (ausser supabase-studio unhealthy = Pre-existing aus V5.1, nicht V5.2-relevant). Keine Errors/Exceptions/Crashes in app-Logs. Coolify-Cron-Schedule fuer Recording-Retention ist konfiguriert (0 4 * * * UTC, ruft /api/cron/recording-retention via x-cron-secret). Compliance-Templates-Tabelle hat 3 Rows, email_footer wurde waehrend Smoke-Test berechtigt modifiziert + reset (length=default). Disk 78% (28G/38G) — beobachten, nicht kritisch. ENV-Bereinigung in Coolify durchgefuehrt waehrend Smoke-Test (RECORDING_RETENTION_DAYS 30→7). Auto-Cron-Run um 04:00 UTC am 2026-04-27 wird erste echte Iteration sein; Container-Lifetime seit Deploy ist <24h. STATE auf 'Post-Launch confirmed stable' hochgestuft. ISSUE-042 (OpenAI-Key) bleibt User-Pflicht vor erstem produktivem Whisper-Call."
title: "Post-Launch V5.2 — REL-017 Compliance-Sprint Live-Beobachtung"
---

# RPT-219 — /post-launch V5.2 Compliance-Sprint Live-Beobachtung

## Release Reviewed

**REL-017 — V5.2 Compliance-Sprint** (deployed 2026-04-26 ca. 07:51 UTC, ~4h Live-Zeit zum Bericht-Zeitpunkt 11:56 UTC).

## Environment Reviewed

- **Server:** Hetzner Business System, 91.98.20.191
- **Coolify-App:** business.strategaizetransition.com
- **App-Container:** `app-k9f5pn5upfq7etoefb5ukbcg-074859629176`
- **DB-Container:** `supabase-db-k9f5pn5upfq7etoefb5ukbcg-074859684441`
- **Stack:** 17 Container (App, Supabase x6, Jitsi x4, Asterisk, Cal.com x2, Coolify-Proxy + Sentinel, ein objective_archimedes-Misc)

## Observed Live Behavior

### Container-Status (alle 17 Service)

| Service | Status | Uptime | Health |
|---|---|---|---|
| `app` | Up 4h | started 2026-04-26T07:51:30 UTC | **healthy** |
| `supabase-db` | Up 4h | — | healthy |
| `supabase-kong` | Up 4h | — | healthy |
| `supabase-studio` | Up 4h | — | **unhealthy** (Pre-existing aus V5.1, nicht V5.2-relevant — Studio nicht Production-kritisch) |
| `supabase-storage` / `-rest` / `-auth` / `-meta` | Up 4h | — | running (meta healthy) |
| `jitsi-web` / `-jicofo` / `-jvb` / `-jibri` / `-prosody` | Up 4h | — | running |
| `calcom` | Up 4h | — | healthy |
| `calcom-db` | Up 4h | — | healthy |
| `asterisk` | Up 4h | — | running (kein Health-Check, V5.1-Stand) |
| `coolify-proxy` | Up 5h | — | healthy |

**Bewertung:** Stack ist stabil. Einziges "unhealthy" ist supabase-studio, das ist seit V5.1 bekannt (Pre-existing) und kein Produktions-Pfad — Studio ist nur das Admin-UI, App geht direkt ueber Kong → REST/Auth.

### Error-Scan

`docker logs --since 4h` auf `app`-Container nach `error|exception|fatal|crash|unhandled` (Pre-existing Lint-Hits ausgeschlossen):

> **0 Errors, 0 Exceptions, 0 Fatals seit Deploy.**

### Cron-Aktivitaet

Coolify-Scheduled-Tasks (aus `coolify-db.scheduled_tasks`):

| Cron | Schedule | Status |
|---|---|---|
| **Recording Retention** | `0 4 * * *` (taeglich 04:00 UTC) | konfiguriert ✓ — naechster Auto-Run: 2026-04-27 04:00 UTC |
| Cadence Execute | `*/5 * * * *` | aktiv, fuehrt sauber aus (`processed=0, errors=0`) |
| Classify | `*/10 * * * *` | aktiv, sauber |
| Meeting Recording Poll | `*/2 * * * *` | aktiv, Volume size 0 |
| call-processing | `*/2 * * * *` | aktiv |
| classify-emails | `*/10 * * * *` | aktiv (Duplikat zu "Classify" — pre-existing) |
| embedding-sync x2 | `*/5` + `*/15` | duplicate, eines mit kaputtem Hardcoded-Placeholder (pre-existing, nicht V5.2) |
| imap-sync | `*/5 * * * *` | aktiv (CRON_SECRET hardcoded — pre-existing) |
| meeting-reminders / -summary / -transcript | `*/5 * * * *` | aktiv |
| signal-extract | `*/5 * * * *` | aktiv, sauber |
| pending-consent-renewal | `15 3 * * *` | konfiguriert |
| kpi-snapshot | `0 2 * * *` | konfiguriert |
| followup-suggestions | `0 */6 * * *` | konfiguriert |
| retention | `0 3 * * *` | duplicate zu "Recording Retention", aber **anderer Endpoint** `/api/cron/retention` (das ist der **alte** OpenAI-Whisper-Retention-Cron aus V4.1, nicht V5.2-Recording-Retention) — pre-existing |

**V5.2-spezifischer Recording-Retention-Cron** wird heute manuell via curl getriggert (siehe RPT-218):

```json
{"success":true,"retention_days":7,"cutoff_date":"2026-04-19T07:52:30.037Z","meetings":{"candidates":0,"deleted":0,"errors":0},"calls":{"candidates":0,"deleted":0,"errors":0}}
```

Log-Trace im Container:

```
[Cron/RecordingRetention] Retention complete: meetings=0/0 calls=0/0 (cutoff: 2026-04-19T07:52:30.037Z)
```

### DB-State

`compliance_templates`:

| template_key | body_len | default_len | updated_at |
|---|---|---|---|
| calcom_booking | 582 | 582 | 2026-04-25 13:46:48 (Initial-Insert SLC-523-Backend) |
| email_footer | 352 | 352 | 2026-04-26 07:31:24 (Smoke-Test Edit + Reset durch User) |
| meeting_invitation | 740 | 740 | 2026-04-25 13:46:48 (Initial-Insert SLC-523-Backend) |

- 3 Rows wie erwartet
- email_footer: User hat im Smoke-Test 1.4 editiert + 1.5 reset → `body_markdown` ist gleich `default_body_markdown` (length 352 == 352) → Reset hat funktioniert ✓
- updated_at zeigt nur den Berührungs-Zeitpunkt, nicht ob die Aenderung bestehen blieb — der Inhalts-Vergleich ist beweisend

### Audit Log letzte 6h

`SELECT FROM audit_log WHERE created_at > now() - interval '6 hours' LIMIT 10;` → **0 Rows.**

Erwartet, weil:
- Recording-Retention-Cron schreibt audit_log nur bei tatsaechlichen Loeschungen (`meetings=0/0 calls=0/0` → keine Eintraege)
- Compliance-Template-Edits schreiben kein audit_log (out-of-scope per SLC-523-Spec)
- Keine Activity-Modifikation, kein Deal-Stage-Wechsel waehrend Smoke-Test

### Disk Usage

```
/dev/sda1   38G   28G   8.1G   78% used
```

Beobachten, nicht kritisch. Bei Steigerung > 85% sollte ggf. Hetzner-Disk-Erweiterung oder Backup-Bereinigung erfolgen — kein V5.2-Triggered-Wachstum.

## Issues Found

### Blocker
keine

### High
keine

### Medium
keine

### Low (Pre-existing, nicht V5.2-relevant — der Vollstaendigkeit halber dokumentiert)

1. **Coolify Cron-Duplikate**: `Classify` ↔ `classify-emails`, `embedding-sync` (2x mit unterschiedlichen Schedules), `Recording Retention` ↔ `retention` (aber anderer Endpoint!). Sollten konsolidiert werden — aber NICHT V5.2-Scope. Empfehlung: Backlog-Item fuer Coolify-Cron-Cleanup.

2. **Kaputter Cron `embedding-sync` mit `'x-cron-secret':'CRON_SECRET_VALUE'`**: Literaler Placeholder als Secret-Wert, ergibt 401 Unauthorized. Pre-existing, kein V5.2-Defekt. Backlog-Item fuer Coolify-Cron-Cleanup mit-bereinigen.

3. **CRON_SECRET-Klartext in 3 Coolify-Cron-Commands** (`imap-sync`, `retention`, `embedding-sync`): Statt `process.env.CRON_SECRET` wurde der literale Wert `d7212456...` eingetragen. Sicherheits-Issue im Sinne von "Coolify-DB-Backup hat Klartext-Secret drin", aber nicht V5.2-eingefuehrt. Pre-existing. Empfehlung: bei naechstem CRON_SECRET-Rotation alle 3 auf process.env-Pattern umstellen.

4. **supabase-studio unhealthy**: Pre-existing aus V5.1, nicht Production-Pfad (Studio = Admin-UI), nicht V5.2-Relevanz. Kein Backlog-Item noetig — Studio ist seit V5.1 funktional aber Healthcheck-Konfig falsch.

## Stability Assessment

**STABLE.**

V5.2 ist seit 4h in Production live, ohne dass irgendein App-Error, Crash, oder unerwartetes Verhalten aufgetreten ist. Alle V5.2-relevanten Komponenten (compliance_templates-Tabelle, Settings-Page-Route, MeetingTimelineItem, Recording-Retention-Cron) verhalten sich wie spezifiziert.

Caveats:
- Container-Lifetime <24h — der Auto-Cron um 04:00 UTC ist morgen erstmals dran. Bis dahin nur manuelle curl-Validierung als Beweis (siehe RPT-218).
- Kein produktiver Whisper-Call mit Kundendaten seit Deploy — V5.2-Internal-Test-Mode-Annahme bleibt valid.
- ISSUE-042 (OpenAI-Key) bleibt User-Pflicht vor erstem Produktiv-Call.

Diese Caveats sind **nicht** Stabilitaets-Risiken, sondern bewusste V5.2-Deferred-Tasks.

## Required Follow-up Actions

### Innerhalb 24h
1. **2026-04-27 morgens (nach 06:00 deutscher Zeit):** Coolify-`app`-Container-Logs nach `[Cron/RecordingRetention]` greppen — pruefen ob Auto-Run um 04:00 UTC sauber durchgelaufen ist. Erwartet: `Retention complete: meetings=0/0 calls=0/0`.

### Vor erstem Produktiv-Whisper-Call mit Kundendaten (User-Aktion)
2. **ISSUE-042**: OpenAI-Key bei platform.openai.com rotieren + neuen Key in Coolify ENV `OPENAI_API_KEY` setzen + lokale `open AI Business system.txt` beseitigen.
3. **Anwaltliche Pruefung** der `docs/COMPLIANCE.md` und der 3 Compliance-Templates.
4. **Switch auf Azure-EU-Whisper**: Azure-OpenAI-EU Account + DPA, AZURE_OPENAI_*-ENVs in Coolify, `TRANSCRIPTION_PROVIDER=azure`.

### Tech-Debt-Backlog (nicht zeitkritisch)
5. Coolify-Cron-Cleanup: 4 Duplikate konsolidieren, kaputten `embedding-sync` reparieren, 3 hardcoded CRON_SECRETs auf process.env umstellen.
6. supabase-studio Healthcheck-Konfig fixen (Pre-existing).
7. NPM-Audit-Vulnerabilities (5 in transitiven Prod-Deps) — separater Tech-Debt-Slice.
8. ESLint 221 Probleme — separater Tech-Debt-Slice.
9. ISSUE-033 Public-Revoke-Link — bei Anwalts-Pruefung der Templates mit-thematisieren.
10. **GitHub App Strategaize-systems Org an Coolify anbinden**, dann Repos auf Private umstellen (User-erwaehnt in Final-Check-Diskussion).

## Project Record Updates

- `/docs/STATE.md` — Current Phase auf "Post-Launch confirmed stable nach V5.2-Release" hochstufen
- `/reports/RPT-219.md` — dieser Post-Launch-Report
- `/docs/KNOWN_ISSUES.md` — keine neuen V5.2-Issues; ISSUE-042 bleibt unveraendert open

## Recommended Next Step

V5.2-Release-Zyklus ist abgeschlossen — keine `/`-Skill-Aktion mehr V5.2-bezogen noetig.

**Naechster Workflow-Schritt:** entweder
- **Morgen frueh kurzer Cron-Log-Check** (~1 Min, optional) — bestaetigt Auto-Run-Funktionalitaet
- **Pre-Pflicht User-Aktionen** (ISSUE-042 OpenAI-Key, Anwalt-Pruefung)
- **Naechste Version V5.3** E-Mail Composing Studio (BL-386) — Beginn mit `/requirements V5.3` wenn User bereit
