---
id: RPT-652
date: "2026-06-14"
skill: /qa
slice: SLC-913
feature: FEAT-925
type: completion
status: completed
reviewOf: null
result: "Gesamt-QA SLC-913 (V8.15 Security-Hardening, 8 MTs) PASS code-side — 0 Blocker / 0 High / 0 Medium / 3 Low. tsc=0, Vitest 1608/1608, wiring 7/7 export-APIs scoped, MIG-052 role+team_id-Guard korrekt. DB-Verify + Live-Smokes /deploy-gated."
title: "Gesamt-QA SLC-913 V8.15 Security-Hardening Bundle"
---

# Gesamt-QA SLC-913 — V8.15 Security-Hardening Bundle

Cross-Cut-QA über alle 8 Micro-Tasks (MT-1..MT-8) nach Abschluss der per-MT-/qa-Pässe (RPT-640..651). Fokus laut Auftrag: Stub-/Placeholder-Detection, Cross-MT-Wiring-Chain-Integrität, AC-913-6 (Login-Lockout) + AC-913-7 (Export-Tenant-Scope).

## 1. QA-Scope reviewed

- Slice-Spec `slices/SLC-913-v815-security-hardening.md` (8 MTs, AC-913-1..7, R-913-1..4, Done-Gate)
- 28 geänderte Production-Files (`git diff origin/main..HEAD`), 17 Commits
- 16 zugehörige Test-Files (14 in `src/**` + 2 DB-Verify in `__tests__/migrations/`)
- Migrationen `sql/migrations/052_*.sql` + `053_*.sql`
- Fokus-Implementierungen: `lib/security/ip-hash.ts`, `app/(auth)/login/actions.ts`, `lib/export/tenant-auth.ts`, `lib/export/helpers.ts`, alle 7 Read-API-Routes, `lib/utils/safe-external-href.ts`

## 2. Verdict: **PASS (Code-Side)** — 0 Blocker / 0 High / 0 Medium / 3 Low

Code-Side ready für `/final-check`. DB-Findings (MIG-052/053-Apply) + Live-Smokes bleiben planmäßig `/deploy`-gated.

## 3. Gates

| Gate | Ergebnis |
|---|---|
| `tsc --noEmit` | **0 Fehler** |
| `vitest run` (Full-Suite) | **1608/1608 GREEN** (174 Files, 106s) |
| ESLint — SLC-913-Files (scoped) | **0 neue Fehler** (4 Lint-Fehler in berührten Files sind alle pre-existing — git-blame: SLC-507/SLC-302/SLC-204) |
| ESLint — Codebase-weit (`npm run lint`) | 142 Fehler / 76 Warnings (alle pre-existing, **keine in SLC-913-Files** — siehe L-1) |

## 4. Findings nach Severity

### Blocker / High / Medium
Keine.

### Low

- **L-1 — Codebase-weite ESLint-Baseline failt (`npm run lint` Exit 1, 142 Fehler).** Alle Fehler in pre-existing Files (`ai/parser.ts`, `goals/calculator.ts`, `goals/kpi-queries.ts`, `knowledge/chunker.ts`, `process-check.ts` u.a.), keiner in einem SLC-913-File. **Nicht durch diesen Slice eingeführt** (Pre-existing Tech-Debt, war auch vor V8.15 so). Reklassifiziert für SLC-913 als informativ; sollte bei `/final-check` explizit als Baseline-Befund anerkannt oder adressiert werden, da der Projekt-Lint-Gate aktuell rot ist.
- **L-2 — Toter Code: `guardExportRequest` + `verifyExportApiKey` (lib/export/helpers.ts + auth.ts).** Nach MT-7-Migration aller 7 Read-APIs auf `guardExportTenant` haben beide keinen Route-Caller mehr (nur noch Kommentar-Referenzen + eigene Definition). `@deprecated` markiert. Folge-Cleanup, kein Risiko (Founder-Toleranz, in Handoff bereits als Follow-up notiert).
- **L-3 — `console.log` statt `logSafe` in `lib/push/send.ts:83`** (Subscription-Expiry-Removal-Log, interpoliert `userId` + statusCode). Kein Secret/PII-Key, operativer Log. Konsistenz-Abweichung vom Logger-Redaction-Standard (BS V8.12 SLC-907); Low.

## 5. Stub-Detection — CLEAN

Scan über alle 28 Production-Files: gefundene `return []` / `return null` sind durchweg legitime Guard-Clauses (unauth → leere Liste, Error → leere Liste, `return null; // Auth OK`-Sentinel), keine Stubs. Kein `TODO`/`FIXME`/`debugger`/Hardcoded-Credential/Dummy-Data. Test-Files: alle 16 mit echten Assertions (9–36 expects/File), kein `expect(true).toBe(true)`, kein `.skip`/`.todo`.

## 6. Wiring-Chain — 7/7 INTAKT (AC-913-7)

Alle Export-/Reporting-Read-APIs rufen `guardExportTenant` und scopen auf `identity.teamMemberIds` mit der korrekten **heterogenen** Tenant-Spalte (live-`\d`-verifiziert, MT-7):

| Route | Guard | Tenant-Filter |
|---|---|---|
| export/activities | guardExportTenant | `.in("owner_user_id", …)` (Count + Data) |
| export/contacts | guardExportTenant | `.in("owner_user_id", …)` (Count + Data) |
| export/deals | guardExportTenant | `.in("owner_user_id", …)` (Count + Data) |
| export/insights | guardExportTenant | `.in("decided_by", …)` (Count + Data) |
| export/signals | guardExportTenant | `.in("created_by", …)` (Count + Data) |
| winloss/[deal_id] | guardExportTenant | Deal-Owner-Resolve → `teamMemberIds.includes(dealOwner)` else reject (fail-closed bei NULL) |
| campaigns/[id]/performance | guardExportTenant | Campaign-Owner-Resolve → `teamMemberIds.includes(campOwner)` else reject (fail-closed bei NULL) |

Sowohl Count- als auch Data-Query gescopt → keine Pagination-Total-Leakage. Kein Route-Caller des deprecated globalen `EXPORT_API_KEY`/`guardExportRequest` mehr (vollständig migriert).

## 7. AC-Matrix

| AC | Status | Evidenz |
|---|---|---|
| AC-913-1 (MIG-052 role+team_id-Guard, BEFORE INSERT OR UPDATE, service_role-gated) | **Code PASS** / DB-Verify `/deploy`-gated | `052_*.sql`: service_role-Short-Circuit, INSERT blockt privileg. role/team_id, UPDATE blockt role+team_id-Change, Trigger `BEFORE INSERT OR UPDATE`. DB-Verify-Test (8 Cases) liegt korrekt außerhalb `src/**` → läuft im /deploy gegen Coolify-DB. |
| AC-913-2 (createAdminClient-Ownership-Sweep, 4 Stellen) | PASS | RPT-643 + Vitest GREEN |
| AC-913-3 (href-Scheme-Guard) | PASS | `safeExternalHref`: blockt `javascript:`/`data:`/protocol-relative `//`, prefixt schemelose Hosts mit `https://`; 16 expects/5 Tests OWASP-adversarial |
| AC-913-4 (Mass-Assignment-Whitelist) | PASS | RPT-647 + Vitest GREEN |
| AC-913-5 (Public-Endpoint-Hardening) | PASS | test-sentry Prod-404 + aus `middleware.publicPaths` entfernt; intake eigener Key + Rate-Limit; push-subscribe Allowlist |
| AC-913-6 (Login-Lockout, XFF-Rotation umgeht nicht) | PASS | `login`: Peek beider Buckets vor signIn, account-scoped `login-acct:<email>` (20/15min) IP-unabhängig, Count-both-on-fail, Clear-both-on-success, generische Message; `extractClientIp` rightmost-(offset) `TRUSTED_PROXY_COUNT` |
| AC-913-7 (Export-Tenant-Scope ODER DEC + Rate-Limit-Key) | PASS | Full-Fix: per-Tenant `export_api_keys` (MIG-053) + `guardExportTenant` Identitäts-Rate-Limit + 7/7 Routes gescopt (siehe §6); DEC-302 |

## 8. Offene Risiken / `/deploy`-gated (by design, keine QA-Blocker)

- **DB-Verify MIG-052 + MIG-053** gegen Coolify-DB (node:20-Sidecar) erst nach Live-Apply im `/deploy` (ISSUE-109/116/122 bleiben `open` bis dahin).
- **Live-Smokes** (test-sentry-404, intake-401/429, push-subscribe-400, Self-Promotion-blocked, Export-Tenant-Scope) im `/deploy`-Fenster.
- **Provisionierung**: per-Tenant-Export-Key anlegen + System 4 auf Bearer umstellen; Coolify-ENV `LEAD_INTAKE_API_KEY`.
- **5 Low ISSUE-122..126** deferred (Founder-Toleranz); ISSUE-122 vom MT-1-Fix mit-erledigt.

## 9. Readiness

Code-Side vollständig und konsistent; alle High+Medium-Findings adressiert, Wiring vollständig, Gates (außer pre-existing Lint-Baseline) grün. Bereit für `/final-check`.

## Next workflow step

**`/final-check V8.15`** — Pre-Release-Audit (Security, Hygiene, Dependencies, Secrets, Test-State). Bei `/final-check` ist L-1 (pre-existing Lint-Baseline rot) explizit zu bewerten. Danach `/go-live` → `/deploy` (MIG-052+053-Apply + Key-Provisionierung + Live-Smokes; **Pre-Condition: V8.14 STABLE** via /post-launch T+24h) → `/post-launch`.
