---
id: RPT-436
date: "2026-05-16"
skill: /go-live
slice: SLC-721
feature: FEAT-721
type: completion
status: completed
reviewOf: RPT-435
result: "GO fuer REL-031 (data-only). Beide Production-Container healthy. Disk 86% (5.2GB free) ausreichend. Keine Deployment-Action am Container noetig — Coolify-DB-Daten bereits live (MT-3). Naechster Schritt /deploy = RELEASES.md REL-031-Eintrag + Master-Merge."
title: "V7.2 SLC-721 /go-live — GO fuer REL-031"
---

# V7.2 SLC-721 /go-live — Decision

## 1. Release Target

**REL-031** — V7.2 Test-Infra-Cleanup (data-only release).

- Branch: `slc-721-test-infra-cleanup` HEAD `6e48e85`
- Production-Image bleibt `770dd55` (V7.1)
- Keine Schema-Migration
- Kein Coolify-Redeploy
- Aenderungen sind bereits Live-applied (MT-3 hat Coolify-DB seed + auth-users geschrieben)

## 2. Readiness Decision

### **GO** ✅

## 3. Blockers

Keine.

## 4. Accepted Risks (uebernommen aus RPT-435)

| Risk | Status |
|---|---|
| L1 Test-Passwoerter hardcoded in qa-test-users.mjs | Accepted — Convention seit SLC-702 |
| DEC-202 Container-Bootstrap-Hook fehlt | Accepted — bewusst V7.3+ BL-475 |
| Server-Repo Default-Single-Branch-Fetch | Accepted — V7.3+ Hygiene |
| Mount-Pfad campaigns-schema.test.ts | Accepted — Skill-Improvement-Kandidat |

Keine neuen Risks seit /final-check.

## 5. Required Fixes Before Release

Keine.

## 6. Operational Pre-Check (Self-Hosted Stack Readiness)

| Check | Status |
|---|---|
| All containers healthy | ✅ `app` + `supabase-db` Up 3h (healthy) |
| Env var propagation | N/A — keine ENV-Aenderung in V7.2 |
| Internal connectivity verified | ✅ MT-3 SSH-Apply via node:20 im Coolify-Netz hat sowohl supabase-db (DB) als auch supabase-kong (Auth via app-Container) erreicht |
| Auth flow tested end-to-end | ✅ create-qa-test-users.mjs hat 3 qa-* User erfolgreich angelegt/aktualisiert via GoTrue-Admin-API |
| No hairpin NAT reliance | N/A — kein Production-Code-Touch |
| Health checks proven | ✅ beide Container in Up (healthy) Zustand 3h post Production-State |
| Reverse proxy routing confirmed | N/A — kein neuer Endpoint, bestehendes Traefik-Routing unveraendert |
| Disk capacity | ⚠️ 86% (5.2GB free). Ausreichend fuer data-only Release, aber ISSUE-071 weekly Cron weiter offen. Disk-Trend seit V7.1: 83% → 86% (node:20 Image-Pull). User-Pflicht-Action bleibt. |

## 7. Operational Concerns

- **Disk**: 86% — operativ aktuell unkritisch (V7.2 hat keinen neuen Disk-Footprint im App-Container), aber ISSUE-071 weekly Cron-Setup bleibt User-Pflicht (Akkumulation in 1-2 Wochen).
- **Master-Merge**: nach /deploy noch zu erledigen (Fast-Forward von main auf `slc-721-test-infra-cleanup` — 0 behind, 6 ahead).
- **REL-031-Eintrag**: muss in `docs/RELEASES.md` in /deploy als data-only Release dokumentiert werden (Pattern PRE-V4.1-INFRA als Vorlage).

## 8. Recommended Next Step

**/deploy V7.2 SLC-721** — Mechanik:
1. RELEASES.md REL-031-Eintrag schreiben (data-only, no image change).
2. Master-Merge `slc-721-test-infra-cleanup` → `main` (Fast-Forward).
3. Push main.
4. Optional: Tag `v7.2.0`.
5. Records-Update: STATE.md High-Level State `stable`, V7.2 → `released` in roadmap.json, features INDEX FEAT-721 → `deployed`.
6. RPT-437 /deploy completion report.

**KEIN Coolify-Redeploy noetig.** Production-Container behaelt Image `770dd55`.
