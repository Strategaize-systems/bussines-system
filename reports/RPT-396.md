---
id: RPT-396
date: "2026-05-12"
skill: /frontend
slice: SLC-702
feature: FEAT-701
type: completion
status: completed
reviewOf: null
result: "V7 SLC-702 Frontend-Foundation /frontend code-complete: 7 MTs durch, 68 neue Vitest-Tests gruen, Build clean, TSC clean fuer SLC-702-Files, Lint-Baseline unveraendert. Code-Stand bereit fuer User-Coolify-Deploy + /qa SLC-702 Live-Smoke."
title: "/frontend SLC-702 — Frontend-Foundation (Layout + Sidebar-Config + Server-Side-Guards)"
---

## Outcome summary

**Status: fully complete (code-side).** Live-Smoke + Visual-Diff via MCP-Playwright erfolgen in /qa SLC-702 nach User-Coolify-Deploy.

7 Micro-Tasks vollstaendig abgearbeitet, alle Acceptance-Criteria AC1..AC9 code-side erfuellt. AC10 (Live-Smoke) wartet auf Coolify-Redeploy. Commits chronologisch:

| Commit | MT | Inhalt |
|---|---|---|
| `e69c22f` | MT-1 | Auth-Helper-Layer (8 Files, 24/24 Vitest) |
| `d0ca678` | MT-2 | SIDEBAR_CONFIG single-source-of-truth (15/15 Vitest) |
| `d6ea643` | MT-3+4 | Sidebar-Refactor + Layout-Integration (14/14 Vitest) |
| `ef9ae41` | MT-5 | middleware Rollen-Schutz (15/15 Vitest) |
| `76817db` | MT-6 | Playwright Visual-Diff Test-Recipe |
| `<final>` | MT-7 | Lint-Fix + Cockpit-Records-Sync + RPT-396 |

## Files reviewed

- `/slices/SLC-702-frontend-foundation.md` — Slice-Spec mit 10 AC + 7 MT-Beschreibungen
- `/docs/ARCHITECTURE.md` Zeile 8852-8993 — V7 Sidebar-Config + Server-Side-Rollen-Guard + Mobile-Hamburger + DEC-181..195 Tabelle
- `/cockpit/src/components/layout/sidebar.tsx` (Pre-Refactor) — V6.6-Stand `navGroups`-Hardcoded mit 4 Sektionen, 20 Eintraege
- `/cockpit/src/app/(app)/layout.tsx` (Pre-Refactor) — V6.6 Authenticated-Group-Layout (echtes Verzeichnis `(app)/`, nicht `(authenticated)/` wie im Spec)
- `/cockpit/src/middleware.ts` + `/cockpit/src/lib/supabase/middleware.ts` (Pre-Refactor) — V6.6 Auth-Session-Refresh + Login-Redirect
- `/cockpit/src/lib/supabase/server.ts` + `admin.ts` — Supabase Server-Client-Pattern
- `/cockpit/scripts/seed-multi-user.ts` — SLC-701 Seed (1 Teamlead + 5 Members)
- `/cockpit/vitest.config.ts` + `vitest.setup.ts` + `vitest.rls.config.ts` — Test-Konventionen
- `/cockpit/package.json` — Dependencies, npm-Scripts
- `/cockpit/src/lib/deals/typeahead.test.ts` — Vitest-Pattern-Vorlage
- Live Hetzner profiles-Schema via SSH (`docker exec ... \d profiles`) — display_name, role, team_id confirmed

## Files created

- `/cockpit/src/lib/auth/types.ts` — `Role` Union-Type + `ROLES` + `isRole` Type-Guard + `Profile` Interface
- `/cockpit/src/lib/auth/get-profile.ts` — async Server-Function `getProfile()` mit React `cache()`-Memoization, redirect zu /login bei Session-Miss, throw bei profile-row-Missing oder invalid-Role
- `/cockpit/src/lib/auth/assert-role.ts` — Pure `roleAllowed()` + async `assertRole()` (redirect /mein-tag bei Mismatch) + `requireRole()` (notFound bei Mismatch)
- `/cockpit/src/lib/auth/read-only-context.ts` — AsyncLocalStorage-basierter Server-Context fuer Drilldown-Pattern (`runWithReadOnlyContext` + `getReadOnlyContext` + `assertNotReadOnlyContext`); Konsumenten kommen in SLC-704/706
- `/cockpit/src/lib/auth/read-only-context-client.tsx` — Client-Provider + `useReadOnlyContext()` + `useIsReadOnly()` Hooks
- `/cockpit/src/lib/auth/assert-role.test.ts` — 9 Vitest fuer roleAllowed/assertRole/requireRole inkl. Mock von `next/navigation` + `./get-profile`
- `/cockpit/src/lib/auth/get-profile.test.ts` — 9 Vitest fuer isRole + getProfile-Pfade (admin/teamlead/member/no-session/no-row/invalid-role)
- `/cockpit/src/lib/auth/read-only-context.test.ts` — 7 Vitest fuer ALS-Propagation + parallel-Isolation
- `/cockpit/src/lib/navigation/sidebar-config.ts` — `SIDEBAR_CONFIG: SidebarItem[]` mit 22 Eintraegen + `SidebarSection` Union + `SECTION_LABEL` + `SECTION_ORDER` + `filterByRole()` + `groupBySection()` + `groupVisualMerged()`
- `/cockpit/src/lib/navigation/sidebar-config.test.ts` — 15 Vitest inkl. Inline-Snapshot fuer alle 3 Rollen
- `/cockpit/src/components/layout/sidebar.test.tsx` — 14 Vitest fuer isItemActive + Role-Matrix-Rendering + Collapsible-Verhalten via fireEvent
- `/cockpit/src/lib/auth/middleware-guards.ts` — `evaluateRouteGuard()` pure-Function + `ROUTE_GUARDS` Array mit 14 Patterns + `ROLE_REDIRECT_TARGET`
- `/cockpit/src/lib/auth/middleware-guards.test.ts` — 15 Vitest fuer 14 Route-Patterns × 3 Rollen
- `/cockpit/__tests__/playwright/sidebar-visual-diff.spec.ts` — Visual-Diff-Test-Recipe fuer /qa SLC-702 (MCP-Playwright-driven, kein lokales @playwright/test installiert)
- `/cockpit/__tests__/playwright/__snapshots__/.gitkeep` — Snapshot-Verzeichnis-Anker

## Files changed

- `/cockpit/src/components/layout/sidebar.tsx` — Refactor von Hardcoded `navGroups` zu SIDEBAR_CONFIG-Filter. `Sidebar({ role })` Prop-Interface. `groupVisualMerged` kollabiert VERWALTUNG-Sub-Sections. Pipeline-children-Branch entfernt (V6.6 nutzt das nicht mehr). `data-testid="sidebar"` + `data-role`-Attribut fuer MCP-Playwright. Exportiert `isItemActive` als pure Function.
- `/cockpit/src/app/(app)/layout.tsx` — Async-Layout, ruft `await getProfile()` als erste Action, passt `role={profile.role}` an Sidebar. Spec-Pfad war `(authenticated)/`, echtes Verzeichnis ist `(app)/` — Layout am echten Pfad angepasst (routing-effekt identisch).
- `/cockpit/src/lib/supabase/middleware.ts` — Nach bestehendem Auth-Check + Login-Redirect zusaetzlich `profile.role` geladen und `evaluateRouteGuard` auf Pathname gerufen; bei Block: `NextResponse.redirect` zu `/mein-tag`. Nur fuer authenticated nicht-public Routes (sonst +1 DB-Hit pro public-Navigation).
- `/slices/INDEX.md` — SLC-702 Status `planned` → `done`
- `/features/INDEX.md` — FEAT-701 Status `planned` → `in_progress`
- `/docs/STATE.md` — Current Focus + Phase + Immediate Next Steps auf SLC-702 done + naechstes /qa SLC-702 aktualisiert
- `/reports/RPT-396.md` — diese Datei

## Files intentionally not changed

- `/cockpit/src/middleware.ts` — Top-Level-Middleware-Datei delegiert weiterhin an `updateSession()` aus `/lib/supabase/middleware.ts`, die wir intern erweitert haben. Top-Level-Datei bleibt 13 Zeilen wie V6.6.
- `/cockpit/package.json` — Bewusst KEIN `@playwright/test` installiert. Visual-Diff in /qa SLC-702 ueber MCP-Playwright wie in V6.6-Baseline (Memory `reference_playwright_browser_smoke.md`).
- `/cockpit/scripts/seed-multi-user.ts` — V7-SLC-701 Seed bleibt unveraendert. Erzeugt profiles aber nicht auth.users mit Passwoertern. Test-User-Creation fuer Playwright-Auth in /qa-Vorbereitung (siehe Open Points).
- `/docs/DECISIONS.md` — Keine neuen DECs. DEC-190+191+192 aus Architektur sind durch MT-1+MT-2+MT-5 implementiert; keine Architektur-Aenderung.
- `/docs/MIGRATIONS.md` — Keine Schema-Migration in SLC-702.
- `/docs/KNOWN_ISSUES.md` — Keine neuen Issues entdeckt.
- `/planning/backlog.json` — Keine BL-Items beruehrt; SLC-702 ist kein Backlog-Item (Slice).
- `/planning/roadmap.json` — V7 bleibt `active`. Update folgt erst bei V7-Release (REL-029).
- `/cockpit/src/app/layout.tsx` (Root) — Root-Layout bleibt unveraendert; getProfile() laeuft nur im authenticated-Subtree.

## Problems found

1. **Spec-Pfad-Drift `(authenticated)/` vs. `(app)/` Repo-Reality:** Slice-Spec referenziert `cockpit/src/app/(authenticated)/layout.tsx`, echtes Repo nutzt `(app)/`. Loesung: an echtem Pfad gearbeitet, Routing-Effekt identisch. Hinweis ergaenzt im Commit-Log + Report. Kein Bug — Spec-Drift dokumentiert.

2. **/cockpit-Route-Pattern in middleware-guards trotz fehlender Route:** Pattern `/cockpit/*` ist in `ROUTE_GUARDS` enthalten, aber `/cockpit/` existiert in V6.6 nicht als Route (das echte Pendant ist `/dashboard`). Bewusste Aufnahme zur Defense-in-Depth bei spaeterer Route-Anlage. Wird in /qa als Nicht-Defekt notiert.

3. **Test-User-Vorbereitung fuer Playwright Auth ist OFFEN:** seed-multi-user erzeugt `profiles` aber NICHT `auth.users` mit Passwoertern. Live-Smoke (AC10) braucht 3 Test-User-Logins (admin/teamlead/member). Pre-Bedingung fuer /qa SLC-702: User oder /qa-Session muss vorab `supabase.auth.admin.createUser` fuer 2 Test-User mit profile_id=078/081 ausfuehren ODER /qa entscheidet alternative Verifikation (z.B. tempraerer Role-Switch im DB via UPDATE auf eigene Profile).

4. **AC8 Visual-Diff nicht code-side ausfuehrbar:** @playwright/test ist nicht installiert. MT-6-Recipe ist deklarativ. Visual-Diff erfolgt erst in /qa SLC-702 via MCP-Playwright-Session. Nicht-Defekt — bewusste Design-Entscheidung (Konsistenz mit V6.6-Smoke-Pattern).

5. **8 TSC-Errors vorbestehend ausserhalb SLC-702-Files:** in `useVoiceCapture.test.tsx`, `reverse-charge-revert.test.ts`, `skonto-revert.test.ts`, `vies-client.test.ts`. SLC-702 hat 0 neue TSC-Errors. Akzeptiert als V6.6-Baseline.

## Open points or deferred decisions

- **Test-User Auth-Creation:** /qa-Vorbereitung muss `auth.users` fuer Teamlead + Member-Test-User anlegen (z.B. via `supabase.auth.admin.createUser({email, password})` Server-Action oder One-Off-Script). Empfohlene E-Mails: `seed-teamlead@strategaize-test.local`, `seed-member1@strategaize-test.local` mit profile-Link auf 078/081.
- **Visual-Diff Snapshot-Bless:** Erste MCP-Playwright-Session in /qa erzeugt die 3 Reference-PNGs in `__tests__/playwright/__snapshots__/`. Manuell sichten und committen.
- **VERWALTUNG-Split visuell (SLC-707):** Sub-Section-Header `VERWALTUNG_MEIN` vs. `VERWALTUNG_SETUP` werden in SLC-702 unter einem `VERWALTUNG`-Label gerendert (via `groupVisualMerged`). Erst SLC-707 implementiert die visuelle Trennung.
- **Mobile-Hamburger:** Sidebar nutzt `max-md:hidden` (V6.6-Pattern). Mobile-Top-Bar + Sheet-Drawer kommt erst in SLC-707 (DEC-192).
- **Pipeline-Children-Removal:** V6.6-`sidebar.tsx` hatte einen `renderNavItem`-Branch fuer `item.children`. Da `navGroups` in V6.6 keine children mehr definiert hat, war der Branch toter Code. Entfernt im Refactor — falls Pipeline-Sub-Navigation wieder benoetigt wird (z.B. Multiplikatoren/Unternehmer/Leads), als neues Pattern in SIDEBAR_CONFIG via separate Items oder neue children-Property nachzieren.

## Recommended next step

**1. User-Coolify-Deploy (du, manuell):** Push `main` zu origin → Coolify-Auto-Redeploy. Erwartete Deployment-Dauer ~2-3 Minuten.

**2. /qa SLC-702 (eigene Session, danach):** Verifikation AC1..AC10 inkl. 3-Rollen-Visual-Diff (admin/teamlead/member via MCP-Playwright) + Direkt-URL-Probe (member→/dashboard → /mein-tag-Redirect). Vorbedingung: Test-User-Auth-Creation (siehe Open Points). Bei /qa PASS: SLC-702 = vollstaendig done. Status in slices/INDEX bereits auf `done` gesetzt (Konvention: code-complete = done; /qa verifiziert, nicht flippt).

**3. /backend + /frontend SLC-703 (nach /qa SLC-702 PASS, eigene Session):** Verwaltungs-UI fuer `/settings/team` (Liste, Invite, Rolle-aendern, Profile-Delete). Erste Konsumenten von `assertRole(['admin','teamlead'])` aus MT-1.
