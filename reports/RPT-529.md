---
id: RPT-529
date: "2026-05-23"
skill: /backend
slice: SLC-846
feature: FEAT-824
type: completion
status: completed
result: "SLC-846 Mail-Footer-Auto-Insert code-side DONE. render.ts + send.ts gepatcht mit tenantSlug-Param + Resolution. Vitest 11/11 PASS (7 Regression + 4 neu). TSC + ESLint clean. AC1-AC4 + AC7 PASS, AC5+AC6 V2-Followup dokumentiert, AC8 (Live-Smoke) defer bis SLC-847."
title: "SLC-846 /backend — Mail-Footer-Auto-Insert (render.ts + send.ts)"
---

## Outcome summary

**SLC-846 code-side DONE.** MT-1 + MT-2 erfolgreich, MT-3 + MT-4 als V2-Followup dokumentiert (eigene HTML-Builder ohne renderBrandedHtml).

AC1-AC4 + AC7 (Code-Side) PASS. AC5+AC6 als V2-Followup dokumentiert (Spec R5: eigene HTML-Builder). AC8 (Live-Smoke Test-Mail) deferred bis SLC-847 Master-Merge.

Verification confidence: **partial** — Snapshot-Regression-Safety 100% bestaetigt (7 existierende Snapshots blieben Bit-fuer-Bit gruen), DSE-Footer-Pfad via 4 neuer Tests + XSS-Escape verifiziert. Browser-Render + Live-Mail-Send noch nicht verifiziert.

## Files reviewed

- `/slices/SLC-846-mail-footer-auto-insert.md` — Slice-Spec
- `/cockpit/src/lib/email/render.ts` — Pre-Patch (renderBrandedHtml Pure-Function ohne tenantSlug)
- `/cockpit/src/lib/email/render.test.ts` — Existing 7 Tests inkl. Snapshot-Regression
- `/cockpit/src/lib/email/send.ts` — sendEmailWithTracking mit ownerUserId-Param (V7-Standard)
- `/cockpit/src/lib/email/send-consent-mail.ts` — Eigener HTML-Builder via `consentRequestHtml`, KEIN renderBrandedHtml
- `/cockpit/src/lib/email/templates/consent-request-de.ts` — Template-Datei
- `/cockpit/src/app/api/cron/meeting-briefing/route.ts` — Cron nutzt `renderBriefingEmail` aus `templates/briefing-html.ts`, KEIN renderBrandedHtml
- `/cockpit/src/lib/team/lookup-slug.ts` — Reuse aus SLC-845

## Files changed (5)

- `/cockpit/src/lib/email/render.ts` — neuer interner `buildDseFooterBlock(tenantSlug, primary)` Helper + Signatur-Erweiterung `renderBrandedHtml(body, branding, vars, tenantSlug?)` + `dseLinkBlock` append am Ende des Mail-Tables nach footerText.
- `/cockpit/src/lib/email/render.test.ts` — Import `beforeEach, afterEach` + neuer `describe("renderBrandedHtml — SLC-846 DSE-Footer-Block")` mit 4 Tests (Append-bei-Set, Omit-bei-undefined, Graceful-no-ENV, XSS-Escape).
- `/cockpit/src/lib/email/send.ts` — Import `getTenantSlugByOwnerUserId` + Resolution-Block vor `renderBrandedHtml`-Call (Zeile 102-118): `const tenantSlug = params.ownerUserId ? await getTenantSlugByOwnerUserId(...) ?? undefined : undefined; htmlContent = renderBrandedHtml(body, branding, vars, tenantSlug)`.
- `/slices/INDEX.md` — SLC-846 Status `planned` → `done`.
- `/docs/STATE.md` — Current Focus + Phase + Next Steps auf SLC-846 code-side DONE + naechster Schritt `/qa SLC-846`.

## Files created (1 — manuell authored)

- `/reports/RPT-529.md` — diese Completion-Report.

## Files intentionally not changed

- `/cockpit/src/lib/email/send-consent-mail.ts` — nutzt eigenen Template-Builder `consentRequestHtml`, NICHT renderBrandedHtml. Spec MT-3 + R5: "Falls die Funktion renderBrandedHtml nicht aufruft sondern eigenen HTML-Builder hat: prueft + dokumentiert in Slice-Notes" → V2-Followup. DSE-Link im Consent-Mail-Footer waere wertvoll, aber V1-Scope passt.
- `/cockpit/src/lib/email/templates/consent-request-de.ts` — Template-Datei, V2-Followup.
- `/cockpit/src/app/api/cron/meeting-briefing/route.ts` — nutzt `renderBriefingEmail` aus `templates/briefing-html.ts`, NICHT renderBrandedHtml. Spec MT-4 + R5: V2-Followup dokumentiert.
- `/cockpit/src/lib/email/templates/briefing-html.ts` — V2-Followup.
- `/cockpit/src/lib/team/lookup-slug.ts` — 1:1 Reuse aus SLC-845.
- `/planning/backlog.json` BL-488 — bleibt `in_progress`.

## Schema / Migration implications

Keine. SLC-846 ist Pure-Code-Patch ohne Schema-Aenderung. `legal_documents` + `teams.slug` aus SLC-841 bleibt Source-of-Truth.

## Policy / RLS implications

Keine. `getTenantSlugByOwnerUserId` (SLC-845) nutzt `createAdminClient` (BYPASSRLS), das gleiche Pattern wie SLC-843 Public-Route — RLS-Policy `legal_documents_select_team` schuetzt User-facing-Pfad, Admin-Client darf cross-team lesen fuer Tenant-Resolution.

## API implications

`renderBrandedHtml`-Signatur erweitert um optionalen 4. Param `tenantSlug?: string`. Backwards-kompatibel: alle existing-Caller (Live-Preview-Client + Cadence-Engine + Composing-Studio) brauchen keine Aenderung.

## UI states covered

N/A — Backend-Slice ohne UI.

## Problems found

Keine Code-Side-Problems. Anmerkungen:
- **V2-Followup #1: send-consent-mail.ts DSE-Footer.** Consent-Request-Mail nutzt eigenes Template (`consentRequestHtml(templateInput)`) statt renderBrandedHtml. DSE-Link im Consent-Mail-Footer waere besonders wertvoll (Customer bekommt DSE-Link direkt im DSGVO-Consent-Request), aber Slice-Spec sagt explizit V2-Followup falls eigener Builder. Vorschlag fuer V8.5+: entweder consentRequestHtml-Template um `dseLink?`-Param erweitern ODER auf renderBrandedHtml umstellen.
- **V2-Followup #2: meeting-briefing-Cron DSE-Footer.** Briefing-Mail-Cron nutzt `renderBriefingEmail` aus `templates/briefing-html.ts`, KEIN renderBrandedHtml. Briefing-Mails gehen an internen User (Mitarbeiter), nicht an Customer — DSE-Footer dort weniger kritisch.
- **AC8 Live-Smoke Test-Mail deferred** — wie bei allen SLC-843..846 Live-Smokes, defer bis SLC-847 Master-Merge per [[feedback_no_coolify_branch_switch_ever]] + [[feedback_slice_merge_at_end]].
- **`NEXT_PUBLIC_APP_URL` Production-Setting** — User-Pflicht-Verifikation in SLC-847 Pre-Deploy: ENV muss in Coolify als `https://business.strategaizetransition.com` gesetzt sein, sonst graceful-no-footer in Production.

## Open points or deferred decisions

- **V2-Followup send-consent-mail DSE-Footer** — entweder consentRequestHtml erweitern oder auf renderBrandedHtml umstellen (V8.5+).
- **V2-Followup meeting-briefing DSE-Footer** — nicht zeitkritisch (interne Mails).
- **AC8 Live-Smoke** — defer bis SLC-847.
- **React `cache()` fuer Slug-Lookup** — overkill V1 (Mails sind low-frequency <100/Tag).
- **Cadence-Owner-Resolution rueckwirkend** — V2 falls Legacy-Cadences ohne owner_user_id Auto-Footer brauchen.

## Recommended next step

`/qa SLC-846` als Skill ([[feedback_qa_skill_not_inline]] + [[feedback_qa_after_every_slice]]).

Verifikations-Schwerpunkte fuer /qa:
- Vitest 11/11 re-run (7 alte + 4 neue Snapshot-Tests).
- TSC + ESLint re-run.
- Stub-Detection auf render.ts + send.ts.
- Pattern-Reuse-Compliance: getTenantSlugByOwnerUserId aus SLC-845, NEXT_PUBLIC_APP_URL als Strategaize-Standard.
- Snapshot-Regression-Safety: existing 7 Tests gruen ohne Snapshot-Updates.
- XSS-Escape-Verifikation: tenantSlug wird escaped.
- Wiring-Walkthrough: send.ts → ownerUserId → lookup-slug → tenantSlug → renderBrandedHtml → DSE-Footer-Block → SMTP-Send.
- V2-Followup-Doku-Sanity: send-consent-mail + meeting-briefing als dokumentierte Gap.
- Live-Smoke-Status klar markieren als deferred-bis-SLC-847.
