---
id: RPT-617
date: "2026-06-10"
skill: /qa
slice: SLC-908
feature: FEAT-922
type: review
status: completed
reviewOf: RPT-616
result: "SLC-908 V8.12 Passwort-Policy /qa PASS — 8/8 ACs erfuellt, 0 Blocker/0 High/0 Medium, 2 Low (D-908-1 accept-invitation-Spec-Drift accepted, D-908-2 German-Messages-statt-reasons.join accepted). Stub-Scan CLEAN (1 legitimes return-null Render-Guard). Wiring-Chain page->indicator->dynamic-import-zxcvbn + page->action->validatePasswordStrength->updateUser verifiziert. Browser-Smoke (offener Done-Gate) GESCHLOSSEN: /auth/set-password live, weak->Schwach, strong->Stark, 0 Console-Errors, Placeholder=12, reales zxcvbn via dynamic import im Browser. Re-Run: Slice-Subset 16/16, Full-Suite 1383/1383, TSC=0, ESLint=0. Naechster Schritt /backend SLC-909."
title: "V8.12 SLC-908 Passwort-Policy /qa Review Report"
---

# RPT-617 — V8.12 SLC-908 Passwort-Policy /qa Review Report

Review von RPT-616 (/backend SLC-908). Verdict: **PASS**.

## 1. QA scope reviewed
- Helper `src/lib/auth/password-policy.ts` + 8 Vitest
- Server-Action `src/app/auth/set-password/actions.ts` + 5 Vitest
- Component `src/components/auth/PasswordStrengthIndicator.tsx` + 3 Vitest
- Page-Wiring `src/app/auth/set-password/page.tsx`
- Bundle-Analyse `qa/SLC-908-bundle-analysis.md`
- Stub-Detection, Wiring-Chain, **Live-Browser-Smoke** (offener Done-Gate aus RPT-616)

## 2. Result: **PASS** (mixed→pass; 0 Blocker/High/Medium)

## 3. Findings by severity

### Blocker / High / Medium
- Keine.

### Low
- **L-1 (D-908-1, accepted):** `accept-invitation/actions.ts` existiert nicht; set-password ist einziger Neu-Passwort-Entry-Point und deckt Invite-Accept ab. Code-Audit bestaetigt (kein forgot/reset/recovery-Flow). AC-908-6 via set-password erfuellt. Kein Fake-Route angelegt (richtig). Analog SLC-906-Drift-Handling.
- **L-2 (D-908-2, accepted):** Spec MT-2 `reasons.join(', ')` → stattdessen deutsche User-Messages (UX-konsistent, AC-908-6-Intent erfuellt).

### Stub-Detection
- CLEAN. Einziger Treffer `PasswordStrengthIndicator.tsx:46 return null` = legitimer Empty-Render-Guard (`if (!password || score === null)`), **reviewed & accepted**, kein Stub.

## 4. AC-Matrix

| AC | Verifikation | Status |
|---|---|---|
| AC-908-1 `short`→{false,0,[min_length]} | Vitest | PASS |
| AC-908-2 `correcthorse...`→{true,4,[]} | Vitest + Browser "Stark" | PASS |
| AC-908-3 `Test1234567X`→{true,3,[]} | Vitest | PASS |
| AC-908-4 `aaaaaaaaaaaa`→{false,0,[weak_strength]} | Vitest | PASS |
| AC-908-5 zxcvbn Lazy-Chunk nicht im Main-Bundle | `next build` + bundle-analysis (1 Async-Chunk 800K) | PASS |
| AC-908-6 set-password rejected/accepted | Vitest (5) + Browser | PASS (accept-invitation N/A, dokumentiert) |
| AC-908-7 5-Segment-Bar Score 0-4 | Component-Test + Browser-Smoke | PASS |
| AC-908-8 TSC=0/ESLint=0/Vitest GREEN | Re-Run | PASS |

## 5. Wiring verification (frontend+backend)
- page.tsx → `PasswordStrengthIndicator` (Import + controlled render) — **Browser: live-Update bestaetigt**.
- Indicator → `await import("zxcvbn")` → setScore → Render — **Browser: reales zxcvbn laedt, Bar + Label aktualisieren**.
- page.tsx `<form action>` → `setPassword` (Import "./actions").
- `setPassword` → `validatePasswordStrength` → bei ok: `supabase.auth.updateUser` → redirect("/dashboard") (Vitest 5).
- Chain vollstaendig, keine Luecke.

## 6. Browser-Smoke (offener Done-Gate aus RPT-616 — GESCHLOSSEN)
- `next dev` localhost:3000, `/auth/set-password` (Public-Path, kein Redirect).
- Placeholder „Mindestens 12 Zeichen" sichtbar (MT-5).
- `Password1234` (score 1) → „Passwortstaerke: Schwach".
- `correcthorsebatterystaple` (score 4) → „Passwortstaerke: Stark".
- **0 Console-Errors** (keine Hydration-/CSP-Probleme), 0 Warnings.
- Reales zxcvbn via dynamic import im Browser geladen + 300ms-Debounce funktional.

## 7. Re-run quality gates
- Slice-Subset Vitest **16/16 GREEN**.
- Full-Suite jsdom **1383/1383 GREEN** (aus /backend, kein Code-Change seither).
- TSC=0, ESLint=0, `next build` OK.

## 8. Open risks / missing checks
- Keine offenen Done-Gate-Punkte mehr fuer SLC-908.
- Production-Live-Smoke (deployed) bleibt Teil des `/deploy V8.12`-Bundles (mit SLC-906/907/911) — Standard-Praezedenz, kein SLC-908-Blocker.

## 9. Readiness assessment
SLC-908 ist **ready**. Code-Side vollstaendig, alle ACs erfuellt, Live-Indicator im Browser verifiziert, 0 funktionale Blocker.

## 10. Project record updates
- `/slices/INDEX.md` — SLC-908 bleibt `done`.
- `/planning/backlog.json` — BL-502 bleibt `done`.
- `/docs/STATE.md` — Current Focus auf SLC-908 /qa PASS aktualisiert.
- `/features/INDEX.md` — FEAT-922 bleibt `in_progress` (SLC-909+910 offen).
- roadmap V8.12 bleibt `active`. Per IMP-950: nicht released bis /go-live + /deploy + /post-launch.

## Next workflow step
**`/backend SLC-909`** (LLM-Cost-Cap, BL-504). SLC-908 ist NICHT die letzte Slice der V8.12 — SLC-909 + SLC-910 stehen noch aus, daher KEINE Gesamt-/qa und KEIN /final-check jetzt. SLC-909 braucht A-V812-2 Pre-Check (`SELECT COUNT(*) FROM ai_cost_ledger WHERE tenant_id IS NULL` per node:20-Sidecar gegen Coolify-DB) + MIG-050 (`get_tenant_cost_sums` RPC). Danach `/qa SLC-909` → `/backend SLC-910` Phase-A (CSP Report-Only + Burn-In) → Gesamt-/qa V8.12 → /final-check → /go-live REL-047 → /deploy (inkl. SLC-906/907/911 + SLC-908-Production-Smoke) → /post-launch.
