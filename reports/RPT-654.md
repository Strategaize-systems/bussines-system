---
id: RPT-654
date: "2026-06-14"
skill: /go-live
slice: SLC-913
feature: FEAT-925
type: completion
status: completed
reviewOf: null
result: "Go-Live V8.15 SLC-913 = CONDITIONAL GO. Release-Substanz grün (/qa PASS RPT-652, /final-check READY RPT-653, build PASS). BLOCKING-Bedingung vor /deploy: V8.14 muss erst STABLE sein (/post-launch T+24h fehlt — Last Stable = V8.12). Danach /deploy-Live-Checklist (MIG-052/053 + Key-Provisionierung + Live-Smokes)."
title: "Go-Live V8.15 Security-Hardening — CONDITIONAL GO"
---

# Go-Live V8.15 — SLC-913 Security-Hardening Bundle

## 1. Release target
Production (Coolify + Hetzner, Single-Container Internal-Test-Mode). Self-hosted Supabase. Branch `main` (19 Commits ahead of origin, unpushed). Delivery-Mode: internal-tool. Kein Customer-Live (module-lifecycle-discipline).

## 2. Readiness decision: **CONDITIONAL GO**

Die Release-Substanz von V8.15 ist freigegeben — Code, QA und Final-Check sind grün. Der GO ist an **eine harte Vorbedingung** geknüpft, die NICHT die Qualität von V8.15 betrifft, sondern die Release-Sequenz:

> **`/deploy V8.15` ist BLOCKIERT, bis V8.14 STABLE ist** (`/post-launch` T+24h PASS). Aktueller „Last Stable" = **V8.12**; ein `/post-launch`-Report für V8.14 existiert nicht. V8.14 ist `released` (REL-049, deployed 2026-06-12), aber noch nicht stabilitäts-bestätigt.

Begründung der Sequenz-Disziplin: Stacking eines zweiten Security-Releases auf einen noch nicht burn-in-bestätigten Stand verwischt im Fehlerfall die Rollback-Grenze (welcher Release ist schuld?) und verletzt IMP-950 / Release-Gate-Defense.

## 3. Blockers (vor /deploy, nicht vor GO-Entscheidung)
- **B-1 (Sequenz-Gate):** V8.14 nicht STABLE — `/post-launch V8.14` T+24h muss zuerst PASSen. T+24h-Fenster (~2026-06-13 13:35 UTC) ist chronologisch verstrichen → der Check kann jetzt gefahren werden.

## 4. Accepted risks
- L-1 pre-existing rotes Lint-Baseline (142 Fehler, 0 in SLC-913-Files, style-only) — Internal-Tool, identisch zu V8.12/V8.14. Cleanup empfohlen, nicht release-blockierend.
- L-2 toter Export-Key-Code (`guardExportRequest`/`verifyExportApiKey`) — Cleanup-Follow-up.
- L-3 pre-existing `console.log` push/send.ts:83.
- In-Memory-Rate-Limit (R-912-4) überlebt keinen Restart/Multi-Container — akzeptiert im Single-Container-Mode; persistenter Store = dokumentiertes Folge-Item.
- DB-Findings ISSUE-109/116/122 bleiben `open` bis Live-Apply im /deploy (by design).

## 5. Required before release (/deploy-Live-Checklist)
1. **Zuerst:** `/post-launch V8.14` T+24h → STABLE (klärt B-1).
2. `git push` (19 Commits) → Coolify-Redeploy.
3. MIG-052 + MIG-053 live applien (`sql-migration-hetzner.md`, postgres-User, Base64).
4. Per-Tenant-Export-Key provisionieren (`INSERT export_api_keys(encode(digest('<RAW>','sha256'),'hex'), <founder-id>, 'system-4')`) + System 4 auf `Authorization: Bearer <RAW>` umstellen.
5. Coolify-ENV `LEAD_INTAKE_API_KEY` setzen (aus MT-5); `TRUSTED_PROXY_COUNT` default 1 OK.
6. DB-Verify gegen Coolify-DB (node:20-Sidecar): `052-*.test.ts` + `053-*.test.ts`.
7. Live-Smokes: test-sentry-404, leads-intake 401/429, push-subscribe-400, Self-Promotion-blocked, Export-Tenant-Scope (fremder Owner → leer/403).
8. KNOWN_ISSUES ISSUE-109/116/122 → `resolved` nach Live-Verify; REL-050 in RELEASES.md; roadmap/slice/feature → deployed.

## 6. Operational concerns
- Host ist speicher-eng (ISSUE-108): Coolify-Build-OOM-Risiko → bei Bedarf Jitsi-Stack temporär stoppen (wie V8.14-Deploy).
- Rollback: Image-Rollback auf V8.14-Image; MIG-052 Rollback = Re-Apply 051; MIG-053 additiv (DROP TABLE export_api_keys, Reads fallen auf 401 — vorher EXPORT_API_KEY-Pfad war eh deprecated).

## 7. Recommended next step
**`/post-launch V8.14`** (T+24h Burn-In → STABLE) zur Auflösung von B-1. Erst danach **`/deploy V8.15`** mit der Live-Checklist (§5.2–§5.8). V8.15 bleibt bis dahin `active`/`in_progress`, NICHT released/deployed (IMP-950).
